# adversarial review -- japes

- when: 2026-09-16T04:03:34Z
- range: 9cb6162..HEAD (since the last review) (c1c5701)
- diff: 400 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree (ran the pages and routes, plus the whitespace-path claim).

WAS: plato/api/info.py :: relaxed-only-wider-than-strict-mode -- FIXED (`JAPES_ENVIRONMENT=staging JAPES_STRICTNESS=relaxed` and `ENVIRONMENT=prodution` both render `RELAXED_WRITES = "false"` on packs.html and info.html now, and the new parametrize asserts it against `relaxed_only` itself).
WAS: plato/boot/schema_version.py :: blank-path-misdiagnosed-as-memory -- FIXED (comparison is now exact, matching `DbStore`'s `path == ":memory:"` after `str(path or ":memory:")`; ran it: `sqlite_path="   "` creates an 8KB file named `"   "` and a second `DbStore` reads the row back, so `unmigratable_reason` correctly returns `None`).
WAS: plato/api/database.py :: audit-helper-claims-all-branches -- FIXED (docstring now scopes the sharing to the two branches that reach it and says why `UnmigratableStoreError` does not).
WAS: tests/test_plato_info_api.py :: gate-agreement-asserted-against-a-table -- FIXED (the row now PATCHes a real `create_config_router`; checked the statuses are 403/200 and not a 404 or 503 that would read as accepted).

[DEFECT] plato/api/info.py:362 :: strict-override-not-plumbed-to-pages -- both renderers call `relaxed_write_allowed()` with no argument, so the `strict=` override that `relaxed_only` honours at the route never reaches the page, and the two answer differently in exactly the direction this diff set out to close.
Ran it: `create_packs_router(store_for=..., tenant_from_request=..., prefix="/api/v1", strict=True)` with `JAPES_ENVIRONMENT=local` renders `const RELAXED_WRITES = "true" === "true";` on `/api/v1/packs/ui` while `POST /api/v1/packs/initialize` and `DELETE /api/v1/packs/{id}/{v}` both answer 403 -- the Delete button and the seed panel are on screen and every click 403s. Same for the details page: `create_database_router(db=None, prefix="/api/v1", strict=True)` beside `create_info_router(prefix="/api/v1")` renders the Migrate button and `POST /api/v1/database/migrate` answers 403. The reverse too: `strict=False` on a `production` tier admits the write while the page hides the control. The sibling already has the knob -- `dashboard_html(..., auth=..., strict=...)` at `plato/api/info.py:312` takes both and forwards them to `withholding`, and its docstring states the rule ("what the app was built with ... so a caller passing neither gets the same verdict"); `packs_html` has no `strict` parameter and `packs_page` (`plato/api/packs.py:152`) does not forward the router's. `tests/test_plato_packs_api.py:329` builds precisely the `strict=True` router, so the route half of this override is already under test while the page half is not; the new `test_a_relaxed_only_control_is_offered_exactly_where_the_route_admits_it` sweeps only environment variables, which is why it passes.
IMPORTANCE: medium -- `plato/app.py:186-250` and `plato/__main__.py:308` pass no `strict=` to either router, so no shipped Plato wiring reaches it; it is reachable by any consumer that builds `create_packs_router`/`create_database_router` directly with the documented override, which is a public factory kwarg the pack tests already use. Fails loudly at the click, not silently. The details-page half is the harder one: the button belongs to a route another router owns, so plumbing it means the info router taking the database router's `strict`.

[NOTE] tests/test_plato_info_api.py:684 :: unset-tier-row-missing -- the parametrize sets a tier in every row and never sweeps the unset or blank case, which is the laptop default and the one `unreadable_tier_vars`' docstring calls the whole subtlety (a blank `ENVIRONMENT=` is *not configured*, not an unrecognised tier).
IMPORTANCE: low -- test-only; the shipped behaviour is correct today (`env_text` owns the blank rule and `environment_tier()` resolves `local`), so this would catch a future regression in that rule rather than a defect now. One row with `env={}` and `offered=True` closes it.

[NOTE] plato/boot/posture.py:179 :: three-conditions-claim-narrower -- "The same three conditions, in one place" holds only on the `strict is None` path; with an explicit `strict` the function returns `not strict` and ignores both the tier and the unreadable-variable condition, which is the reading under which passing no argument from the renderers looks safe.
IMPORTANCE: low -- mirrors `relaxed_only`'s own short-circuit, so the two still agree for any given argument; the cost is the docstring being what a caller trusts when deciding not to forward `strict`, which is the defect above.

VERDICT: BLOCK
