# adversarial review -- japes

- when: 2026-09-11T04:37:24Z
- range: e722b47..HEAD (since the last review) (ff39daf)
- diff: 188 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/posture.py :: blank-tier-var-reads-as-unreadable -- FIXED; `unreadable_tier_vars` skips a value that strips to empty, and `ENVIRONMENT=""` / `APP_ENV="   "` now reach the 503 (no pack store) rather than the 403.
WAS: plato/posture.py :: unreadable-tier-predicate-belongs-in-envvars -- FIXED; the predicate now lives beside `environment_tier`/`deployed_posture` in `jazzx_sdk/config/envvars.py`, uses the public `ENVIRONMENT_NAMES`, and `check_settings` covers `wiring_default`'s readers for the serving roles (`build_app` → `create_plato_app` raises → `/info` only).
WAS: plato/posture.py :: docstring-omits-unreadable-refusal -- FIXED; the `relaxed_only` summary now names the unreadable-tier refusal.
WAS: plato/posture.py :: blocked-term-redundant-in-guard -- FIXED; the guard is `if unrecognised:` and the dropped conjunct is explained in a comment.
WAS: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer -- STILL OPEN; untouched by this diff.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; untouched by this diff.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested

[DEFECT] plato/posture.py:55 :: boot-refusal-not-fatal-for-jobs -- the new comment's load-bearing claim, "Refusing once at boot is what covers those [`wiring_default`'s store and Knowledge Hub choices]", does not hold for a `job:*` role, which prints the refusal and then runs the work anyway against the local-shaped wiring the unreadable tier selected.
`JAPES_RUN_MODE=job:<name>` with `ENVIRONMENT=prodution` (the diff's own example): `load_wiring()` at `plato/__main__.py:351` has already called `_db(deployed_posture())` with `None`, so the job holds the sqlite/in-process store and Mock KH. `check_posture(wiring)` at `:373` returns the new PostureError text, and `:374-376` only `print`s it and calls `record_boot_note(..., gating=True)` -- a job consults no gate, by the comment at `:378-381`. Control reaches `:389 return run_job(role, wiring)` and the job writes through the wrong backend. The serving half is covered (`build_app` → `create_plato_app` → `check_settings` raises → `_serve_info_only`), and the schema check one block down does return `5` for a job, so this is the one refusal of the three that a job walks past.
IMPORTANCE: medium -- enumerated all three `check_settings` call sites (`plato/app.py:131`, `plato/__main__.py:108` via `check_posture`, and the tests); reachable only under `JAPES_RUN_MODE=job:*`, which is the ACA Jobs shape per `plato/__main__.py:38-40`. It prints the reason to stderr rather than failing silently, but the job still completes its writes. Nothing outside this repo calls `check_posture`.

[SYMMETRY] jazzx_sdk/config/envvars.py:247 :: blank-rule-restated-not-reused -- the new loop re-implements `env_text`'s "blank means unset" rule with `os.getenv` plus two `.strip()` calls, in the same module as `env_text` (`envvars.py:37`) and while the docstring cites it by name as the reason the rule exists; `value = env_text(name)` then `if value and value.lower() not in _TIERS` is strictly less code and is the rule rather than a copy of it.
IMPORTANCE: low -- one function, two call sites (`plato/posture.py:59` and `:217`), no behavioural difference today; the only visible change would be that an offender is reported stripped, which is what `env_text` already does everywhere else.

[NOTE] plato/posture.py:44 :: check-settings-docstring-omits-unreadable -- the summary still reads "Refuse to start if a deployed posture is signalled and identity is not required", which is now narrower than the code in two ways: the new refusal fires whatever `require_identity` says and whatever tier is signalled, and passing `environment=` explicitly (which the next paragraph recommends for tests) silently disables it. This is the same shape as the `relaxed_only` docstring fixed in this diff, on the sibling function.
IMPORTANCE: low -- docstring only; the PostureError text explains itself, and the only shipped caller passes no `environment`.

[NOTE] tests/test_plato_packs_api.py:390 :: start-refusal-test-omits-job-role -- `test_an_unreadable_tier_refuses_to_start` asserts the `check_settings` half and the docstring claims it "covers the readers a per-request gate cannot reach", but it never exercises the entry point that decides what a refusal costs; a case driving `plato.__main__.main` with `JAPES_RUN_MODE=job:*` is what would have caught the defect above.
IMPORTANCE: low -- test-only; the shipped defect it would catch is reported separately at `plato/posture.py:55`.

VERDICT: BLOCK
