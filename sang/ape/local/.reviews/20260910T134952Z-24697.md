# adversarial review -- japes

- when: 2026-09-10T13:49:52Z
- range: origin/v2.5.1...HEAD (674d1c5)
- diff: 10086 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Reviewed against the working tree (the diff shown is truncated at `abl_revolver.md`; I verified the elided files — `plato/packs_api.py`, `plato/seed.py`, `plato/migrations/versions/0004_pack_version_retired_at.py` — exist and are consistent). `ruff check jazzx_sdk plato` passes; `tests/test_policy_rule_activation.py test_policy_not_activated_reaches_callers.py test_pack_store.py test_plato_packs_api.py` = 94 passed, 1 skipped.

## CHECKED

- `TODO(activation-vs-active-rules)` (`policy.py:758`) — claim "no caller reads these accessors yet" holds; `executable_rules`/`activation_blocks`/`is_executable` appear only in `tests/test_policy_rule_activation.py`, nothing in `jazzx_sdk`, `plato`, or `/Users/sangit/src/jaci/src`.
- `TODO(orphan-blob-gc)` (`store_db.py`) — holds; `DbPackVersionStore` is tenant-scoped at construction and no cross-tenant `content_digest` count is reachable from it.
- `TODO(omit-info)` (`info_api.py:82`) — holds; `_info_only_app`'s `degraded_nav` still does not contain `"info"`, and it is the only caller passing `omit` to `dashboard_html`.
- `TODO(layout-key-resolution)` / `TODO(layout-live-formula-order)` (`workbook.py`) — both hold; `load_workbook_layout` takes neither accounts nor metrics, and `GovernedWorkbook` does read `inputs_by_statement.get(...)`.

## Findings

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:220` (the `TODO(threshold-grid-provenance)` block) — the TODO's blast-radius claim is wrong in the direction that matters, and the bare-value path fabricates a provenance. It says "the gate covers seven of its numbers and not the rest"; the only shipped `PolicyProfile` (`plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml`) authors its seven `thresholds:` as bare strings with no `provenance`/`status`, so `looks_authored` is False and `get()` serves all seven ungated — the gate covers zero, not seven. Running it: `get_threshold('dscr_min_high_ltv')` → `value='1.20' provenance=INSTITUTION_AUTHORED status=ACTIVE`, for a figure that file's own header attributes to "DSCR Program Summary, version 6.15.2026 V1.1. (Acra Lending)". That is exactly the attribution the same function's `looks_authored` branch refuses to make ("reporting it institution-authored and active would state a provenance nobody authored"), made unconditionally for every bare value.
IMPORTANCE: medium -- I enumerated every `profile.get_threshold(` call site in this repo (only the definition) and in `/Users/sangit/src/jaci/src` (none), so nothing reads the fabricated provenance today; but `get_threshold`, `Threshold` and `ThresholdProvenance` are all in `jazzx_sdk/fabric/canonical/__init__.__all__`, so an out-of-repo consumer can call it as soon as this lands, and the whole `POLICY_NOT_ACTIVATED` path it feeds has no shipped trigger yet either (no pack anywhere under `.` or `/Users/sangit/src/jaci` contains `status: seed` or `secondary_source`).

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:128` — the bundled seed pack declares five runtime classes under `jaci.*` (`modes.evaluator`, `experts.policy.CIPolicyExpert`, `experts.playbook.CIPlaybookExpert`, `conductor.CIConductor`, `conductor.CI_PIPELINE`), and `jaci` is not a japes dependency (`grep jaci pyproject.toml` → nothing). `POST {prefix}/packs/initialize` publishes this pack into any japes deployment; anything that then resolves its conductor or experts raises `ModuleNotFoundError: jaci`. Line 100 also pins `source_doc: "docs/LoanSamples/YETI/Yeti Credit Package/..."`, a path that exists in no japes checkout.
IMPORTANCE: medium -- reachable from one endpoint (`plato/packs_api.py:180` `initialize`, relaxed posture only, gated by `_refuse_unless_relaxed`), and the pack ships in the image since the Dockerfile copies `plato/`. It fails loudly at class-resolution time, and a jaci-hosted deployment does have `jaci` importable, so the break is confined to japes-only installs.

**[SYMMETRY]** `jazzx_sdk/experts/policy/default.py:78` (`_refusal_in`) — this walks a composite's `limbs` recursively in the caller to recover the refusal text, but `_evaluate_composite` (`condition_evaluator.py:459`) already knows which child raised it: it has that child's `inputs` in hand and is already deciding `indeterminate_reason=_composite_reason(...)` on the same principle. Lifting the refusal string onto the composite's own `inputs` beside `limbs` is one line there and deletes all 14 lines of `_refusal_in`, which is otherwise the only place in the repo that knows the `{"limbs": [{"inputs": ...}]}` shape from outside the module that builds it.
IMPORTANCE: low -- no wrong output; `_refusal_in` handles arbitrary nesting correctly today. Affects `default.py` only.

**[NOTE]** `jazzx_sdk/finance/validation.py:810` — every entry of `ComplianceResult.warnings` becomes a `DefectClass.POLICY_VIOLATION` / `Severity.POLICY` finding. `jaci/src/jaci/scenarios/aml/experts/policy.py:236` is a real `BasePolicyExpert` that appends genuinely advisory text ("Confidence below group threshold: 0.82 (min 0.85)") with `allowed=True`, and `validate_package(compliance_result=...)` accepts any expert's result. The docstring names this trade honestly, and `Severity.POLICY.blocks_promotion` is False so it cannot gate promotion.
IMPORTANCE: low -- I traced the one jaci call site (`capabilities/credit_validation/provides.py:36`, via `ctx.policy_expert`), and the AML expert never produces a `SpreadPackage`, so no shipped path reaches it; the visible cost is a spurious informational finding if a future pack expert is wired there.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/chart_of_accounts.yaml:1` and 8 sibling YAMLs — 15 comment lines name `plan_JACI_CL_*.md` planning docs. Those files are gitignored and live outside this repo, and these YAMLs ship in the wheel and the image, so the citation resolves to nothing for any reader.
IMPORTANCE: low -- comments only; affects `reclassifications.yaml`, `spread_template{,_rb}.yaml`, `workbook_layout{,_rb}.yaml`, `addbacks.yaml`, `breakouts.yaml`, `metrics.yaml`, `chart_of_accounts.yaml`.

**[NOTE]** `plato/info_api.py:92` — `nav_html`'s docstring now says "the guide, config, database and both pack entries" but still ends "the guide and those two routers belong to the working app"; three routers are omitted, not two, since `_info_only_app` grew `"Packs", "packs"`.
IMPORTANCE: low -- docstring only, one function, no caller reads it.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `plato/posture.py:97` `TODO(if-match-post)` — claim holds. `require_if_match` appears only at its own definition and in `__all__` (`grep -rn require_if_match plato jazzx_sdk` → 2 hits, both in `posture.py`); no router mounts it, so `POST {prefix}/packs/initialize` cannot 428 today.
- `dscr_core/policies/eligibility.yaml` `DSCR-HIGH-LTV-PROPERTY-TYPE` / `DSCR-PROPERTY-TYPE-CLTV` — the Florida carve-out being unenforced is declared in the rule `description` itself and in `ENCODING_NOTES.md` §3 G1. Deferral holds; not raised.
- Both seed packs load: `ci-spread-core` → 6 policies / 0 profiles, `dscr_core` → 1 policy / 1 profile (`dscr_v6_15_2026_v1_1`). `dscr_core` dir vs `dscr-core` pack_id is fine — `id_spellings` tries both.

---

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125,127` — two `required_documents` entries are orphan PDF-extraction fragments (`schedule`, `available)`), and the heads of the two real items they came from are gone; the file's own `description` says "Extracted verbatim from the source checklist" and "missing items become DATA_GAP hypotheses". Trigger: any C&I application run through `assess_package()` against `policies/checklist_ci.yaml` emits a DATA_GAP for a required document literally named `available)`, and never checks for the equipment-quote and acquisition-agreement items whose text was truncated.
IMPORTANCE: high -- shipped seed data in the image; reached by `POST {prefix}/packs/initialize` → `pack_archive` → any consumer resolving `policies.checklist` from `ci-spread-core/pack_manifest.yaml:52`. I did not find another checklist asset in either seed pack, so this is the only instance; jaci is the external consumer that would read it.

**[DEFECT]** `plato/packs_api.py:73` — the field is named `latest_version` and rendered under a `Latest` column (`plato/static/packs.html:174,341`), but it is `list_packs()`'s row, which that method's own docstring says is chosen by `published_at` and is "the wrong [answer] for a caller reading it as the latest version". Trigger: publish `0.2.0`, then backport-publish `0.1.1` — the page shows `Latest 0.1.1` while the expanded rows list `0.2.0`.
IMPORTANCE: medium -- one producer (`pack_inventory`), two consumers I enumerated (`GET {prefix}/packs` JSON, `packs.html`); wrong but visibly self-contradicting once a row is expanded, and no code outside this repo reads the field yet.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:92` — the description states "10% of borrowing base **or $2M, whichever is greater**"; the condition encodes only `excess_availability_pct >= 0.1`, so the dollar floor is not enforced anywhere in the file. Trigger: borrowing base $8M with $1.0M excess availability (12.5%) → `CI-ABL-MIN-AVAILABILITY` SATISFIED, though the stated policy requires ≥ $2M.
IMPORTANCE: medium -- one rule; loaded via `policies: dir: policies` (verified: `policy_files()` picks up `conventions.yaml` and `core.yaml`, skips `checklist_ci.yaml` by shape). Fails open in the permissive direction, but a reviewer reading the rendered description would see the discrepancy.

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:397` — `DSCR-IO-MIN-LOAN`'s description asserts "minimum loan amount of $250,000 **and minimum FICO 640**"; the condition checks `loan_amount` only. Trigger: interest-only product, loan $500,000, FICO 600 → SATISFIED, no FICO gate anywhere. `ENCODING_NOTES.md` §3 G4 defers the *unused profile key* `fico_min_io`, but not the rule text that claims the gate exists.
IMPORTANCE: medium -- one rule in a `certification_status: draft` pack; the same shape appears in `DSCR-ITIN-CLTV` and `DSCR-SUB-1-DSCR-FICO`, but those two split the claim into real sibling rules (`DSCR-ITIN-LOAN-MAX`, the ITIN grid's `NA` cells below 640), so IO is the only one where the description is unbacked.

---

**[SYMMETRY]** `plato/packs_api.py:148-160` — `_store` wraps `tenant_from_request` in `try/except ValueError → HTTPException(400)`, and its own comment explains why (`wiring_default.py:504` raises `X-Tenant-Id is required on a deployed Plato request`). `plato/assistants_api.py:167` calls the *same* resolver — `plato/app.py:142` and `:229` pass one `tenant_from_request` to both — with no guard, so every chat route still 500s with a stack trace where the packs route now says which header is missing. The consistent version is smaller than the diff: move the four lines into a shared `_tenant(request)` helper (or into `wiring_default`'s resolver) rather than one router owning the guard.
IMPORTANCE: medium -- I checked both call sites of `tenant_from_request` in `plato/app.py`; `assistants_api._tenant` backs every chat route on any deployed-posture replica, and a 500 is what a caller sees for a missing header.

---

**[NOTE]** `plato/data/seed_packs/ci-spread-core/reclassifications.yaml:1`, `spread_template_rb.yaml:1`, `workbook_layout.yaml:1`, `dscr_core/pack_manifest.yaml:6`, `dscr_core/ENCODING_NOTES.md` — newly shipped pack data names the consumer repo (`jaci.capabilities.commercial_lending.*`, `cd jaci && pytest tests/scenarios/dscr/`), gitignored plan filenames (`plan_JACI_CL_RECLASSIFICATION_ENGINE.md`, `plan_JAPES_2_3_0_GOVERNED_WORKBOOK.md`, `Acra_DSCR_on_Platform_v2_Reuse_and_Build_Plan.md`), one checkout's document layout (`docs/LoanSamples/Regional Bank/Output Cleaned.xlsx`) and jaci/japes commit SHAs. Some of `ci-spread-core` already did this before the diff (`chart_of_accounts.yaml`, `metrics.yaml`); the eight files above are new.
IMPORTANCE: medium -- these ship inside `plato/data/seed_packs`, i.e. inside the container image, and `POST {prefix}/packs/initialize` writes them into a tenant's `pack_version` rows. No behavioural effect; the exposure is that plan filenames and a client's program-summary provenance leave the private repos.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md` §1b — "6 of the 14 rules ... combine `property_state` with ..." then lists nine (IL×2, MS, NJ, OH, PA, RI, VA, VT); the §1 table likewise says "8 plain `expression`, 6 `all_of`". Actual counts in `eligibility.yaml` are 9 `all_of` (IL-1, IL-2, MS, NJ, OH, PA, RI, VA, VT) and 5 plain (KS, MD, MI, MN, NM). Total 41 rules and every other count in that table check out.
IMPORTANCE: low -- documentation-only, in a companion notes file; nothing reads the counts.

**[NOTE]** `plato/info_api.py:303` — `packs_html(prefix, *, omit=...)` takes an `omit` tuple no caller passes: `packs_api.packs_page` calls `packs_html(prefix)`, and the degraded app drops `"Packs"` from the nav and never mounts the page at all (`plato/__main__.py:304`).
IMPORTANCE: low -- one function, one call site; unused parameter, no reachable trigger.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No `[DEFECT]` survived verification. Ran the four new/changed test files plus `test_pack_store.py`: 208 passed, 2 skipped.

**CHECKED:**
- `TODO(azure-blob-container-singleton)` (`plato/wiring_default.py:640`) — claim holds exactly. `AsyncAzureBlobStorageProvider.get_instance` (`common/core/storage/async_azure_provider.py:54`) returns `cls._instance` before ever reading `container_name`, and `jazzx_sdk/fabric/blob/store.py:77` is a second construction path, so "not unreachable" is right.
- `TODO(local-wiring-build-test)` (`tests/test_plato_packs_api.py:...`) — claim holds, and I checked the risk it admits: the lines it can't execute do resolve. `env_text` is imported at `plato/wiring_local.py:91`, `_anchored` at `:143`, `CACHE` at `:159`, `db` at `:326`, and both `BlobStore(backend=, local_dir=)` and `DbPackVersionStore(db, blob=, tenant_id=, cache_root=)` match the shipped signatures. No latent `NameError`.
- Separately, out of this diff's scope but now made reachable by it: `TODO(publish-tick-tie)` in `jazzx_sdk/pack/store_db.py` still says "left because no caller invokes `publish` yet". `plato/seed.py:135` and `POST {prefix}/packs/initialize` invoke it. The deferral itself still stands (two *different* versions of one pack inside one microsecond is not reachable from a directory-per-version seed run), only its stated reason is stale.

---

**[SYMMETRY]** `plato/static/packs.html:190` — the identity field stores under `plato-packs-identity` while `plato/static/info.html:141` stores the same `x-user-id` value under `plato-info-identity`, so an operator who typed a UUID on the Details page has to retype it on Packs, and the two can hold different actors for the same browser. The nav puts both pages one click apart; the consistent version is reusing info.html's key string, which is smaller than what the diff already writes.
IMPORTANCE: low -- two static pages in this repo, same origin, no server or external consumer reads the key; cost is retyping a UUID.

**[NOTE]** `plato/wiring.py:44` — "After the defaulted fields, because a dataclass cannot take a required one after them" describes neither the placement nor a constraint that applies: `pack_store_for` is itself defaulted and is inserted *before* every other defaulted field (`tenants`, `settings`, `handler`, …), so the cited rule permitted any position after `client_layer`, including the end. The insertion at index 4 also shifts positional construction, though every in-repo caller uses kwargs (`plato/wiring_default.py:509`, `plato/wiring_local.py:383`, `tests/test_plato_wiring.py:27`, `tests/test_plato_wiring_default.py:482`).
IMPORTANCE: unknown -- I enumerated the four in-repo constructions and all are keyword-only; I could not establish whether any out-of-repo `PLATO_WIRING` factory builds `PlatoWiring` positionally.

**[NOTE]** `plato/wiring_default.py:472` — the new docstring says "cached per (tenant, live db)", which the same diff contradicts 34 lines above ("`_pack_stores` is cleared on every miss, so … once more than one tenant is configured"). `_pack_stores.clear()` on every miss makes the dict a one-entry cache, so with two tenants under interleaved traffic every request constructs a fresh `DbPackVersionStore`. Behaviourally harmless (`register_metadata` is idempotent, `__init__` opens nothing), and identical to the pre-existing `manifest_store_for`/`session_store_for`, so this is the claim and not the code.
IMPORTANCE: low -- reached by `GET/DELETE {prefix}/packs*` on any multi-tenant deployment; cost is object churn per request, no state is lost.

**[NOTE]** `tests/test_plato_packs_api.py:339` — the assertion is literally `x == sorted(x)` on one list, which the comment directly above says it is not. `seed_packs` builds `published` by iterating `candidates = sorted(...)` and stores `"pack": pack_dir.name`, so the list is a subsequence of a sorted list and the assertion cannot fail for any seed tree. The comment's stated reason ("a pack whose id disagrees with its directory") only bites if the compared key were `pack_id`, which it isn't.
IMPORTANCE: low -- test-only; it would not have caught a shipped defect, since ordering is structurally guaranteed by `plato/seed.py:97`.

VERDICT: PASS
