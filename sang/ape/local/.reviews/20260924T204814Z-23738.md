# adversarial review -- .

- when: 2026-09-24T20:48:14Z
- range: HEAD~2...HEAD (2fc96f8)
- diff: 256 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[NOTE] jazzx_sdk/pack/lint.py:104 :: none-evidence-types-skipped -- The docstring says only an object with *no* `evidence_types` at all is skipped. But `getattr(pack, "evidence_types", None)` is None-checked, so an object that has the attribute set to `None` is skipped too.
Trigger: a duck-typed pack (e.g. a test double or a manifest-summary dict wrapper) with `evidence_types = None` gets no findings, even though it has the attribute. `Pack.evidence_types` (`jazzx_sdk/pack/pack.py:351`) always returns a list, so the real `Pack` never reaches this.
IMPORTANCE: low -- the only caller is `lint_pack` (`lint.py:147`). The shipped `Pack` and `ManifestLoader.evidence_types` return `[]` when unset, so no shipped configuration reaches the None branch.

VERDICT: PASS
