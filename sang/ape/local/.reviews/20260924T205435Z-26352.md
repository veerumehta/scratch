# adversarial review -- .

- when: 2026-09-24T20:54:35Z
- range: HEAD~1...HEAD (0c81796)
- diff: 898 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[DEFECT] jazzx_sdk/pipelines/policy_extract.py:517 :: section-documents-inflate-sources -- `propose_step` counts a JTBD candidate's sources from `segment.documents`, which lists every document in the whole section. It should count only the documents behind the units the job actually cites.
Trigger: the planner groups U1 (summary.pdf) and U3 (guide.pdf) into one section, and the job's `source_units` is `["U1"]`. The proposal still reports `distinct_sources=2` and `source_refs=["guide.pdf","summary.pdf"]`, and it passes `min_documents=2` even though only one document supports it. `accumulated_over` at :546 overcounts in the same way. The pipeline test misses this because its job cites U1 and U3, which covers both documents anyway.
IMPORTANCE: high -- this is silent wrong data in `ProposalEvidence`, and it defeats the `min_documents` corroboration gate (policy_extract.py:541). It affects every run that pairs `SectionSegmenter` with `JtbdExtractor`: currently `scripts/policy_jtbd_extract.py` and tests only. jaci, plato and policy-workbench do not import `policy_extract`.

[DEFECT] jazzx_sdk/pipelines/policy_jtbd.py:283 :: cross-section-rule-id-collision -- `JtbdExtractor.extract` only de-duplicates rule ids inside one section (`seen` is local to each call). Two sections whose names slug the same produce identical `rule_id`s, and `propose_step` then merges them by rule id, keeping only the first candidate.
Trigger: the planner returns two sections both named "Credit", or "Credit & Collateral" alongside "Credit/Collateral", or names one "Unassigned provisions" while there are leftover units. Each section yields `extracted.credit_collateral.verify_income`. The second job's steps, checks and citations are dropped with no log line and no `failed_segments` entry. The only record is an extra source in the survivor's evidence.
IMPORTANCE: high -- a job is lost silently. The trigger is LLM planner output that `SectionSegmenter.segment` never de-duplicates (policy_jtbd.py:196-203). It is reachable from the script and from any caller of `SectionSegmenter`.

[NOTE] jazzx_sdk/pipelines/policy_extract.py:482 :: list-extract-silently-empty -- `_candidates` and `observe_step` now treat any "extract" emission that is not `ExtractedCandidates` as zero candidates. A caller that overrides the `extract` component with a function returning `list[RuleCandidate]` (the step's contract before this diff) gets an empty `ProposalSet` and no error. I found no in-repo or sibling override, so nothing reaches this today.
IMPORTANCE: low -- the only reachable callers are overrides of the `run_policy_extract` components dict, and I found none in japes, jaci, plato or policy-workbench.

[NOTE] jazzx_sdk/pipelines/policy_extract.py:460 :: failed-section-name-ambiguous -- a failed section is recorded by its marker alone. If two same-named planner sections fail, `failed_segments` shows the name twice and gives no way to tell which units are missing.
IMPORTANCE: low -- output quality only. It surfaces in the script's stderr and in the `CorpusObligations.failed_segments` field.

VERDICT: BLOCK
