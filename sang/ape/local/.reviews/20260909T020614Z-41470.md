# adversarial review -- japes

- when: 2026-09-09T02:06:14Z
- range: 447ad5e..HEAD (since the last review) (3e281d6)
- diff: 178 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Previous round

- WAS: `[DEFECT] store.py:743` `_get_test_fixture` traversal — **FIXED**; `:749` routes the identifier and `:761` the sidecar through `_safe_name(..., fixtures_dir)`, and `tests/test_fabric_docs.py:208` fails against the old code (unguarded `fixtures_dir / identifier` returns the victim's bytes, so `pytest.raises` never fires).
- WAS: `[SYMMETRY] store.py:521` (and `:777`, `:1812`) hoist above the `try` — **FIXED**; `:525`, `:784`, `:1820` all call `_safe_name` outside the handler, and `tests/test_fabric_docs.py:226` distinguishes the two errors.
- WAS: `[NOTE] store.py:499-500` and `tests/test_fabric_docs.py:166-167` provenance claim — **STILL OPEN** for the store.py half: the test docstring is now precise, but `store.py:500-501` still says the name "arrives from an agent tool call (`agents.interactive.reads`) or a caller-supplied `doc_id` (`tools.documents.local_cache`), so it is not ours to trust", which the test file immediately below it now contradicts.
- WAS: `[NOTE] tests/test_fabric_docs.py:185` `assert secret.read_text() == "SENSITIVE"` — **FIXED**; removed, with `:187-188` saying why an assertion on the victim cannot detect a read-out.

## New review

**[DEFECT]** `jazzx_sdk/fabric/docs/store.py:654` (and `:695`, `:740`, `:359`, `:1803`) — every `Raises:` block on the public surface still enumerates only `KnowledgeFabricError`, but this diff is exactly what made that false: before it, `_local_path`'s `ValueError` was rewrapped by each handler, so the documented contract held; now `await fabric.docs.get("../x")` under `retrieval_mode` `local`/`cached`/`test`, and `put`/`delete` with the same name, raise `ValueError` past a caller that catches the documented type. Trigger: any consumer doing `try: await fabric.docs.get(name) except KnowledgeFabricError:` per the docstring.
IMPORTANCE: medium — in-repo callers are safe (`agents/interactive/reads.py:137` catches bare `Exception`; `tools/documents/local_cache.py:124,131` catch nothing at all, so the new type propagates identically to the old), but `docs.get/put/delete` is public API and the docstring is the contract external consumers code against; it fails loudly rather than silently.

**[SYMMETRY]** `jazzx_sdk/fabric/docs/store.py:761` — the metadata sidecar is routed through `_safe_name` in the TEST branch only; `_put_local:534`, `_get_local:794` and `_delete_local:1829` still build it with a raw `self._local_dir / f"{name}.metadata.json"`. All four are provably safe (the name was already refused above), so the consistent shape is the *smaller* one: drop the redundant `:761` call rather than add three, since every sidecar derives from an already-validated name.
IMPORTANCE: low — no reachable trigger; a suffix append cannot introduce `..` or an absolute prefix, so the four sites differ only in whether they re-check something already checked.

**[NOTE]** `jazzx_sdk/fabric/docs/store.py:510-512` and `tests/test_fabric_docs.py:173` — both say `get()` "dispatches to **four** branches"; `get()`'s four modes are STRICT/CACHED/LOCAL/TEST and STRICT joins no path, while the four *guarded* sites are `_put_local`, `_get_local`, `_delete_local`, `_get_test_fixture` — three of which are not branches of `get()`. The count is right, the thing being counted is not.
IMPORTANCE: low — comment-only; a later reader auditing "the four branches of `get()`" for this guard finds three.

**[NOTE]** `tests/test_fabric_docs.py:144` — the docstring line `` `docs.get` is public API and the name is a caller's, which is reason enough -- `` starts at column 0 inside an indented docstring, so the paragraph renders with a dedented first line.
IMPORTANCE: low — test-only, cosmetic; ruff passes.

**[NOTE]** `jazzx_sdk/fabric/docs/store.py:493` — the rename `_local_path` → `_safe_name` breaks the name pairing the guard was justified by: `store.py:503`, `blob/store.py:152` and `tests/test_fabric_blob.py:154` still refer to `BlobStore._local_path`, so the two "same shape, same reason" siblings now carry different names for the same function.
IMPORTANCE: low — private method, no external callers (`grep _local_path` over `jazzx_sdk` and `tests` hits only `blob/store.py` and prose references).

VERDICT: BLOCK
