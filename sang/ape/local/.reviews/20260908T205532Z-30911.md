# adversarial review -- japes

- when: 2026-09-08T20:55:32Z
- range: origin/dev...HEAD (580c026)
- diff: 95 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**[DEFECT] plato/oidc.py:222** — the failure stamp overwrites a good key set with `keys={}`, so one transient JWKS blip during a forced refetch rejects *valid* tokens for the whole 30s window, even after the issuer recovers. Reproduced: verify a `k1` token successfully (cache = {k1}), then verify a token with kid `k9` while the fetch raises → cache becomes `{}` with `fetched=True`; the very next `k1` verify with the issuer back up fails `token signed by unknown key 'k1'`. Fix is to keep the existing keys and only stamp `fetched_at`/`fetched`.
IMPORTANCE: high -- turns an issuer hiccup into blanket auth rejection of valid tokens. In-repo `TokenVerifier` has no call site outside `tests/test_plato_oidc.py` (grepped `plato/`, `jazzx_sdk/`), but it is in `plato/oidc.py:332`'s `__all__`, so any external consumer wiring it into a middleware reaches this.

**[DEFECT] plato/oidc.py:226** — the stamp is inside the fetch `try` only, so a fetch that *returns* a malformed document raises outside it, leaves `fetched=False`, and the unbounded re-fetch the comment at 217-221 claims to close stays open on that path. Reproduced with `fetch_jwks` returning `{"keys": None}` (`TypeError`) and returning a top-level list `[{"kid": "k1"}]` (`AttributeError`): 5 verifies → 5 issuer fetches, frozen clock. Both are shapes a misconfigured issuer or an intercepting proxy can return with a 200.
IMPORTANCE: medium -- same class as the defect the diff fixes, fails loudly at the caller (`TokenError` via the new wrap in `verify`) but the rate limit is the thing protecting the issuer. Same call-site reach as above.

**[DEFECT] tests/test_plato_oidc.py:492** — `_verifier`'s first positional parameter is `jwks`, not a fetcher, so the lambda is returned *as the document*; the test passes on `'function' object has no attribute 'get'`, never raising `httpx.ConnectError` at all. Confirmed: the raised message is `could not fetch signing keys: 'function' object has no attribute 'get'`, so `match="could not fetch signing keys"` is satisfied by the harness bug rather than by the transport path. Pass `fetch_jwks=` explicitly, as the sibling test at 508 does.
IMPORTANCE: low -- test-only; the transport-raise path is still covered by `test_a_down_issuer_is_not_refetched_on_every_verify`, so nothing shipped is uncovered, but this test would stay green if the `verify` wrap were reverted (the AttributeError would still surface as a plain exception — it asserts the wrong path).

**[NOTE] plato/oidc.py:217** — "Stamped even on failure" is true only for a fetch that *raises*; a fetch that returns garbage is not stamped (see the second defect), so the comment claims a guarantee the placement does not give.
IMPORTANCE: low -- comment accuracy on the same line as the defect above.

**[NOTE] tests/test_plato_oidc.py:509** — with `now=lambda: 1000.0` the clock never advances, so the test proves the cooldown holds but not that it *expires*; a fetch stamped and never retried would also pass. Advancing past `MIN_REFRESH_INTERVAL_SECONDS` and asserting a second fetch would pin both halves.
IMPORTANCE: low -- test-only; the constant is already imported at the top of the file.

VERDICT: BLOCK
