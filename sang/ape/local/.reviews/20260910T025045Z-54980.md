# adversarial review -- japes

- when: 2026-09-10T02:50:45Z
- range: origin/v2.5.1...HEAD (4977375)
- diff: 1134 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## [DEFECT] The new `POLICY_NOT_ACTIVATED` cause is discarded by the two compliance consumers that decide outcomes

`jazzx_sdk/experts/policy/default.py:429` (and its gate twin at `:415`), `jazzx_sdk/agents/adjudication/segment.py:107`

The diff routes "declared but unapproved" to a distinct cause in five places (`check.py:_untested_rationale`, `validation.py:601` re-raise, `engine.py:93` refusal, `RatioEvaluator`, `segment.py:197`) and misses the two that turn an outcome into a decision. `check_compliance` treats *any* `INDETERMINATE` as "Required context data missing -- continue", so a rule whose `RatioCondition` threshold is a `status: seed` `Threshold` is dropped from `applied_policies`, produces no violation and no warning (`warnings=[]` is hardcoded), and the caller gets `allowed=True, rationale="No policy violations detected"`. The applicability-gate paths are worse: `segment.py:107` records `Verdict.NOT_APPLICABLE`, whose own field doc says "a structural applicability check said this rule doesn't apply" — for a rule that does apply and whose threshold nobody approved. `segment.py:95`'s comment ("INDETERMINATE (missing data) both mean skip") is now false for the same reason.

Trigger: `dscr_min: {value: '1.20', provenance: secondary_source, status: seed}` in a profile + any existing `RatioCondition(threshold="profile:dscr_min")`.

IMPORTANCE: high -- silent `allowed=True`. I enumerated every consumer of `Verdict.INDETERMINATE` in the repo (`check.py:194,278,352`, `default.py:415,429`, `segment.py:104`); `check_compliance` is the shipped compliance entry point used by jaci's `dscr_core`, whose policies do use `profile:` ratio thresholds. No pack wraps a threshold *yet*, so it fires on first adoption of the feature this diff adds.

## [DEFECT] `_as_utc` is applied to the bounds but not to `as_of`

`jazzx_sdk/fabric/canonical/policy.py:559` and `:766`

`moment = as_of or datetime.now(timezone.utc)` keeps a caller-supplied naive datetime, then compares it to an aware `_as_utc(bound)`. Measured: `Rule(..., effective_from=datetime(2020,1,1)).is_executable(PolicyStatus.ACTIVE, as_of=datetime(2026,1,1))` → `TypeError: can't compare offset-naive and offset-aware datetimes`. The naive input is the exact shape the diff documents two lines above ("Pack dates arrive naive"), and `test_a_naive_bound_is_read_as_utc` covers only the bound side. Same line in `Policy._out_of_force`, so `executable_rules(as_of=policy.effective_date)` crashes for a naive manifest date.

IMPORTANCE: medium -- fails loudly. Public exported API (`Rule.is_executable/activation_block`, `Policy.executable_rules/activation_blocks`); no in-repo caller yet (nothing calls them outside the new test file), so the first consumer is an external one — jaci reads pack YAML dates, which arrive naive.

## [DEFECT] Both coercion escape hatches leave the gate off *and* mis-state the provenance

`jazzx_sdk/fabric/canonical/profiles.py:137-145`

`set(found) <= fields` and the swallowed `ValidationError` both fall through to `out[key] = found`, leaving the raw mapping. Measured, on YAML-authored profiles:
- `{value: '1.25', provenance: secondary_source, status: seed, edition: '2024'}` (one extra authored key) → `get()` returns the whole dict, `get_threshold()` reports `INSTITUTION_AUTHORED` / `ACTIVE`.
- `status: approved` (any non-lifecycle word) → same, and this is the likeliest authoring slip.

`test_a_yaml_authored_threshold_is_gated` calls this mis-attribution "an affirmative false statement about provenance, worse than the absent gate"; it is still reachable through both branches. Downstream, `get()` returning a dict reaches `evaluate_guard` as `decimal.InvalidOperation` — an `ArithmeticError`, so neither of `engine.apply`'s handlers (`ThresholdNotActivated`, `KeyError`) catches it and it escapes `apply` instead of becoming a `Refusal`.

IMPORTANCE: medium -- authoring path is pack YAML in consumer repos; jaci's only profile (`config/packs/dscr_core/profiles/dscr_profile.yaml`) has no mapping-valued threshold today, so nothing hits it until a pack adopts the wrapped form. `get_threshold` has no shipped caller, so the false provenance is currently only reachable via the gate being silently off.

## [DEFECT] `except ValueError` at the same site still misses `KeyError`

`jazzx_sdk/finance/validation.py:600`

The diff enumerates what this block raises and adds the `ThresholdNotActivated` arm, but `profile.get(...)` raises `KeyError` for an absent key — uncaught, so passing any profile that does not declare `cross_period_addback_severity` crashes the detector, against the docstring's "``profile``, if given, **may** override the finding's severity".

Trigger: `detect_cross_period_addback_inconsistency(pkg, adj, profile=<jaci's dscr_v6_15_2026_v1_1>)`.

IMPORTANCE: low -- I enumerated every caller: none in `jazzx_sdk`, and in jaci only `tests/unit/test_validation.py` and `test_rb_reference_case.py`, all of which pass either no profile or one that declares the key. Pre-existing, but it is the line this diff is editing.

## [SYMMETRY] `Policy.is_active` should ask `_out_of_force`

`jazzx_sdk/fabric/canonical/policy.py:726`

`_out_of_force` (line 760) now owns the "is this policy in force" window, and its docstring states that `is_active` gets a future-dated policy wrong. The fix landed only in the new opt-in path, so `is_active` still says `True` for a policy whose `effective_date` is in the future while `executable_rules()` says nothing executes — two accessors on one model disagreeing. `return self.status is PolicyStatus.ACTIVE and self._out_of_force(None) is None` is smaller than the code the diff already added. Shipped callers: `modes/operational/governor.py:105`, `experts/policy/default.py:303` and `:572`. No test pins the current answer for a future-dated policy.

IMPORTANCE: medium -- enforces a not-yet-effective policy in the governor and the staleness/resolve paths.

## [SYMMETRY] A fourth open-coded naive→UTC coercion

`jazzx_sdk/experts/policy/default.py:577`

The diff collapsed two inline `if x.tzinfo is None: x.replace(tzinfo=utc)` copies into `_as_utc`; `detect_staleness` still hand-rolls the same three lines on `policy.effective_date`.

IMPORTANCE: low -- one function, same behavior today; only cost is the next reader adding a fifth copy.

## [NOTE] `POLICY_NOT_ACTIVATED` points at a symbol that never sets it

`jazzx_sdk/fabric/canonical/policy.py:99` — "The rule may not execute -- see `Rule.activation_block`", but the only producer is `RatioEvaluator`'s threshold refusal, and the diff states plainly that nothing in the evaluation path reads `activation_block`. A reader chasing the cross-reference finds no connection.

IMPORTANCE: low -- comment only.

## TODO check

`TODO(threshold-grid-provenance)` (`profiles.py:196`) — **the deferral condition is already met.** The claim is "deferred until a pack authors a grid that needs it. No shipped profile does." jaci's only profile, `config/packs/dscr_core/profiles/dscr_profile.yaml`, is headed `Source: DSCR Program Summary, version 6.15.2026 V1.1. (Acra Lending)` and carries ~200 transcribed `tables:` cells (`max_cltv_grid`, `max_cltv_itin`, …) from the same document as its seven `thresholds:` — so the gate now covers the seven numbers and not the two hundred, drawn from one unapproved source. What holds is the second half: closing it is a nested shape, not a wider version of `_coerce_threshold_mappings`.

IMPORTANCE: medium -- not a regression (a grid cell is served exactly as it was before this diff, and there is no way to mark one unapproved), but the TODO's stated trigger has already happened in the one real consumer.

VERDICT: BLOCK
