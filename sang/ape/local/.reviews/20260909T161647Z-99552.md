# adversarial review -- japes

- when: 2026-09-09T16:16:47Z
- range: e69721c..HEAD (since the last review) (8885b76)
- diff: 161 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` published_at.desc() tie -- **STILL OPEN**; ordering unchanged, `TODO(publish-tick-tie)` at `:58` unchanged.
- WAS: `store_db.py:312` marker short-circuit -- **STILL OPEN**; `:309-312` still returns the marker's text as the pack root, no containment check, no digest re-verify.
- WAS: `store_db.py:181` publish never opens `archive` -- **STILL OPEN**; `publish` still commits arbitrary bytes; the failure now surfaces from `_unpack` as `BadZipFile`/`ValueError`.
- WAS: `pack/__init__.py:29` `__all__` -- **STILL OPEN**; `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` -- **STILL OPEN**; `:320`, `:375`, `:395`, `:424` all call `reason` without a length bound.
- WAS: `store.py:54` "sha256 over the archived asset bytes" -- **STILL OPEN**; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race -- **STILL OPEN**; no unique-violation handling, blob still written before the row.
- WAS: `store_db.py:140` `visibility` unvalidated -- **STILL OPEN**.
- WAS: `loader.py:214` domain pack aborts composition -- **FIXED**; `:231` skips rows without `assistant_id`, and `test_a_domain_pack_in_the_store_does_not_make_the_tenant_unservable` fails against the old body.
- WAS: `loader.py:214` manifest-only row raises `PackNotFound` -- **FIXED** as stated (no longer raises); the skip it was replaced with has its own defect, reported below.
- WAS: `tests/test_pack_store.py:353` vacuous freeze assertion -- **FIXED**; `skills.register_dict({"name": "late_skill"})` reaches `NamedRegistry._put` and `RegistryFrozenError` is asserted specifically (16 passed, ruff clean).
- WAS: `store_db.py:364` `_unpack` docstring -- **STILL OPEN**; the body paragraph now names `_MANIFEST_NAMES`, but the summary line still says "return the directory holding ``pack_manifest.yaml``", which is the half a reader sees.
- WAS: `loader.py:196` MappingPackSource prose -- **FIXED**.
- WAS: `tests/test_pack_store.py:326` redundant `pack_id` re-set -- **FIXED**.

## 2. New findings

**[DEFECT] `plato/packs/loader.py:236`** — an assistant pack published without an archive is now dropped from composition at `logger.debug` level, where it previously raised: `await store.publish(assistant_manifest)` (no `archive=`) followed by `published_registries(store)` returns `manifests == []`, so the tenant serves no assistant and nothing above sees why. The docstring's justification ("legal by construction ... a pack declares its assets inline") can only describe a *domain* pack, and line `:231` has already skipped those, so the only population this branch can reach is an assistant pack whose assets are missing -- a publish-time mistake returned as an ordinary empty result.
IMPORTANCE: medium -- enumerated every caller of `published_registries` (`plato/packs/loader.py:39` export plus the three tests in `tests/test_pack_store.py`); nothing in `plato` or `jazzx_sdk` composes this way yet, and no other repo can reach it, but this is the serving entry point the store was built for and the failure is invisible rather than loud.

**[NOTE] `tests/test_pack_store.py:411`** — `test_a_manifest_only_pack_is_skipped_not_raised` publishes `MANIFEST`, which has no `assistant_id`, so the row is skipped by the `:231` check and the `content_ref is None` branch at `:236` is never executed; the test passes identically with that branch deleted. Verified by reading both fixtures: `MANIFEST` (`:24`) carries no `assistant_id`, `_plato_pack_archive` (`:329`) is the only assistant-shaped manifest in the file.
IMPORTANCE: low -- test-only; the shipped defect it would have caught is the one above, and publishing `assistant_manifest` with no archive is the one-line change that reaches it.

**[NOTE] `tests/test_pack_store.py:324`** — the rewritten comment states the same fact twice: `:324-325` ("the fixture's manifest is an *assistant* manifest, which names its pack by id and range rather than carrying a version of its own") is repeated verbatim at `:327-328`.
IMPORTANCE: low -- comment-only, one fixture helper with three callers in this file.

CHECKED: `TODO(publish-tick-tie)` at `store_db.py:58` is unchanged from the round that argued its blast-radius claim; not re-raised here.

VERDICT: BLOCK
