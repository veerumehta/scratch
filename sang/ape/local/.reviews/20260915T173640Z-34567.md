# adversarial review -- japes

- when: 2026-09-15T17:36:40Z
- range: d8f286b..HEAD (since the last review) (c4002e5)
- diff: 132 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/api/packs.py :: packs-page-ignores-mount -- FIXED
WAS: plato/__main__.py :: degraded-dashboard-ignores-mount -- FIXED
WAS: tests/test_plato_info_api.py :: mount-sweep-covers-one-page -- FIXED
WAS: tests/test_plato_info_api.py :: mark-sweep-omits-info-page -- FIXED
WAS: plato/prefix.py :: mount-prefix-not-exported -- FIXED

Verified against the tree rather than the diff: all six `HTMLResponse(` sites in `plato/` now carry the mount (`app.py:270,276,283`, `__main__.py:324`, `packs.py:150`, and `info.py:429` which reads `mount_prefix(request)` itself), the degraded `/` returns `/plato/...` hrefs with CSP intact, and the `request: Request`-as-query-param claim in the new `__main__.py` comment reproduces as a literal 422 in a module carrying `from __future__ import annotations`. `starlette` 1.3.1 `add_route` accepts `include_in_schema` and emits no deprecation warning. 191 passed, 1 skipped across `test_plato_info_api.py`, `test_plato_boot_contract.py`, `test_plato_packs_api.py`.

No defects introduced.

[NOTE] tests/test_plato_info_api.py:1692 :: html-family-omits-redoc -- the comment says `_HTML_PAGES` is "every page Plato serves as HTML" and the tuple has four entries; `/redoc` (`plato/app.py:276`) is a fifth HTML page on the same app.
It calls `guide_html(prefix, mount=mount_prefix(request))` with arguments identical to `/docs`, so there is no live gap behind the claim, only a family count that is one short of what the sentence asserts.
IMPORTANCE: low -- enumerated all four `plato/static/*.html` templates and every `guide_html`/`dashboard_html`/`packs_html`/`nav_html` call site in `plato/`; `/redoc` is a duplicate route of `/docs`, and nothing outside this repo reaches it.

[NOTE] tests/test_plato_info_api.py:1713 :: prefix-const-check-hits-one-page -- the `const PREFIX` loop and its comment ("the pages' own fetches are built from this") apply to one of the four swept paths: `grep` over `plato/static/` shows `const PREFIX = "__API_PREFIX__"` only in `packs.html`, while `dashboard.html:209` derives its prefix from the `api-info` href and `info.html`/`guide.html` have no `__API_PREFIX__` at all.
For `/`, `/api/v1/info/ui` and `/docs` the `re.findall` returns `[]` and the loop body never executes, so those three paths are covered only by the href assertion.
IMPORTANCE: low -- the dashboard's href-derived prefix is covered by the `outside` assertion in the same test, so no shipped path is unchecked; test-file precision only.

[NOTE] tests/test_plato_info_api.py:1658 :: info-page-fetch-unasserted -- `pages["info"] = _mounted_client().get("/api/v1/info/ui").text` takes the body without checking the status, unlike the sweep below it which asserts 200.
If `/info/ui` regressed to 404 or 503, `"__FAVICON__" not in html` would pass and the failure would surface as `info: mark missing` rather than as the route being broken.
IMPORTANCE: low -- one call site in one test; fails loudly either way, just with a misleading message.

VERDICT: PASS
