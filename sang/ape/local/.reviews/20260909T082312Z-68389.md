# adversarial review -- japes

- when: 2026-09-09T08:23:12Z
- range: 793b313..HEAD (since the last review) (abe3bf4)
- diff: 115 lines


## whole

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## 1. Previous round

- WAS: `fabric/docs/store.py:1552` `materialize` needs `bare_name` -- **FIXED**. `bare_name` is used at `store.py:1559`, and `tests/test_fabric_docs.py:278-281` now sweeps `../../evil.pdf`, `.japes/manifest.json`, `sub/x.pdf` with `match="bare name"`; the two nested names raise `FileNotFoundError` (not `ValueError`) under the old `relative_name`, so the test discriminates. 131 tests pass across `test_fabric_docs/materialize/blob/materialize_idempotent`.
- WAS: `path_safety.py:48` `"." in parts` is dead -- **FIXED for `relative_name`**: the `"."` literal is gone from that function and the split messages now say what each branch actually tests. It reappears in `bare_name`, with a reason that does not hold; reported below as its own finding.
- WAS: `path_safety.py:42` `PurePosixPath` check vs `Path` join -- **PARTLY FIXED**. Switching to `PurePath` closes `..\x` on Windows. Two Windows names still pass `relative_name` and still discard the base on join (measured with `PureWindowsPath`): `C:x` (`is_absolute()` False, parts `('C:','x')`, `base/"C:x"` → `C:x`) and `\x` (parts `('\x',)`, `base/"\x"` → `\x`). Still low for the same reason as before -- no Windows target -- but the new comment is written as if the disagreement is now closed. See NOTE below.

## 2. New findings

**[NOTE]** `jazzx_sdk/path_safety.py:66` — the added docstring claim "pathlib reports both of those as their own name" is false for `"."`: `Path(".").name` is `''`, so `"."` fails the existing `name != Path(name).name` test and the `"."` literal in the tuple at line 69 is unreachable. Only `".."` needs naming (`Path("..").name == '..'`). Concrete input: `bare_name(".")` raises via the third clause, never the second.
IMPORTANCE: low -- no behaviour difference (`"."` is refused either way); the comment is the whole defect. Callers checked: `agents/document/agent.py:86`, `pipelines/document_ingest.py:181`, `fabric/docs/store.py:1559`.

**[NOTE]** `jazzx_sdk/path_safety.py:32-35` — the comment states the reason for `PurePath` as "the check has to read separators the same way the join will" and presents the disagreement as closed, but on Windows the check and the join still disagree for drive-relative and root-relative names (`C:x`, `\x`), both of which pass `relative_name` and then replace the base on `Path(base) / name`. Trigger: `relative_name("C:x")` on Windows, then `self._local_dir / "C:x"` at `fabric/blob/store.py:71`.
IMPORTANCE: low -- measured, but no Windows target is established for this repo; on posix `PurePath` is `PurePosixPath` so the diff is behaviour-identical there. Both `relative_name` call sites (`blob/store.py:71`, `docs/store.py:505`) are in-repo; whether an external consumer runs japes on Windows I could not determine.

VERDICT: PASS
