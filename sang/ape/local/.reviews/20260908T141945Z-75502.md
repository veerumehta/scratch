# adversarial review -- japes

- when: 2026-09-08T14:19:45Z
- range: 9c7d79c..HEAD (since the last review) (1137294)
- diff: 33 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Step 1 — prior round

WAS: `plato/oidc.py:76` + `:131-133` both comments overstate -- **FIXED**. The "Every read below narrows or defaults" claim is gone; the replacement at `:75-77` claims only that `__post_init__` leaves no `None` on a *constructed* instance, which holds (the strip loop at `:93-94` runs before either `raise`, so any instance that exists has three `str`s), and the `:129-132` comment no longer claims `resolved_jwks_uri` is the sole un-narrowed read.

WAS: `plato/oidc.py:134` `or ""` turns a mutated-to-`None` `issuer` into a relative URL -- **STILL OPEN**, code unchanged, but the diff now states the swallow and its unreachability inline rather than justifying it as narrowing; the "moot" claim checks out (see CHECKED), so it is declined, not outstanding.

## CHECKED

- `plato/oidc.py:129-132` "that state makes `enabled` false and `verify` refuses before any fetch" — holds. `enabled` is `bool(self.issuer)` (`:123`), `verify` raises at `:236` before `_load_keys`, and `resolved_jwks_uri()` has exactly one call site, `:174` inside `_fetch_jwks_over_http`, reachable only past that gate.
- `plato/oidc.py:68` `TODO(frozen-settings)` — still holds; this diff changes no code, so the mutation path stays confined to `plato/oidc.py` and `tests/test_plato_oidc.py`.
- `plato/oidc.py:129` "the one place widening it cost something" — holds: the only attribute access on `issuer`/`audience`/`jwks_uri` outside `__post_init__` is the `.rstrip` at `:133`; every other read (`:114`, `:123`, `:127`, `:265`, `:266`) is a truthiness test, a `bool()`, or an argument whose PyJWT stub already accepts `None`.

## Step 2 — new findings

None. Comment-only diff, no behavioural change, and no new claim in it that the code does not enforce.

VERDICT: PASS
