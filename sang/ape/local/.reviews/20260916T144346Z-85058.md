# adversarial review -- japes

- when: 2026-09-16T14:43:46Z
- range: 8b41c9b..HEAD (since the last review) (32d96ab)
- diff: 554 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/api/packs.py :: local-blob-backend-detected-by-equality -- FIXED. The route now calls `pack_archives_are_durable()` (`plato/wiring/default.py:627`), the same predicate `_pack_blob` branches on, and the test sweeps `Azure`/`azure-blob`/`AZURE`/`blob`.
WAS: jazzx_sdk/pack/store.py :: zip-read-errors-escape-as-500 -- FIXED. `_manifest_bytes` catches `RuntimeError`/`NotImplementedError`/`BadZipFile`; I probed the remaining shapes (declared-size overrun) and zipfile answers `BadZipFile("Overlapped entries")`, which is in the tuple, and the bare `EOFError` at `zipfile.py:1084` needs a data region that ends early with an intact EOCD, which truncation cannot produce.
WAS: jazzx_sdk/pack/store.py :: publishable-archive-may-not-materialize -- FIXED. The lexical member check runs before the manifest search and the new test asserts 422 plus no published row.
WAS: plato/api/packs.py :: config-cannot-move-the-upload-cap -- FIXED. `PLATO_PACK_UPLOAD_MAX_BYTES` is in `plato_settings_catalog()`, and `DbSettingsStore.write` / `EnvSettingsStore.write` both export to `os.environ`, which is what `_upload_cap` reads.
WAS: tests/test_common_submodule_deps.py :: relative-imports-in-package-init-dropped -- FIXED. `_absolute` now takes the importing package; the reached set gained `azure.storage.queue{,.aio}`, `azure.identity`, `redis.asyncio` and two more, and the new assertion pins it.
WAS: plato/wiring/settings_store.py :: settings-failure-not-at-info -- STILL OPEN. The justification was rewritten, and the boot half of the new claim is still false: `_apply_durable_settings` (`plato/wiring/default.py:695-734`) only `logger.warning`s, it never calls `record_boot_note`, so nothing about a settings-layer failure reaches `/info`'s `degraded` at boot either.
WAS: plato/api/packs.py :: upload-cap-has-no-floor -- FIXED. `floor=1` passed, with a test.
WAS: Dockerfile :: image-check-covers-one-of-four -- FIXED as reported (all four modules named), but the new line does not import: see the first finding below.
WAS: plato/api/packs.py :: part-header-precheck-never-fires -- FIXED. The pre-check is gone and the docstring says why.
WAS: pyproject.toml :: otlp-http-caps-the-otel-sdk -- STILL OPEN as a fact; the transitive `opentelemetry-sdk <1.39` ceiling is unchanged, now stated at the dependency, which is the only available response.
WAS: plato/wiring/settings_store.py :: recovery-branch-unreachable-at-boot -- FIXED. The reset moved into `read`, and the flapping test (2 warnings across fail/fail/ok/fail/fail) passes.

[DEFECT] Dockerfile:143 :: otel-fastapi-instrumentor-unlocked -- the check now imports `common.utils.telemetry`, whose line 17 is an unguarded `from opentelemetry.instrumentation.fastapi import FastAPIInstrumentor`, and no distribution in `poetry.lock` provides that module, so the image build fails at this RUN.
`docker build` with the committed lock: `poetry install --no-root --only main --extras plato` (Dockerfile:69) resolves `opentelemetry-instrumentation 0.41b0` (the only `instrumentation` entry in the lock, line 3825) which ships the base instrumentor API, not the FastAPI one; `pyproject.toml:36` declares only `opentelemetry-instrumentation`. My venv imports it solely because `opentelemetry-instrumentation-fastapi 0.65b0` is installed there as a standalone dist (`pip show` → `Required-by:` empty) that the lock does not contain. The underlying condition predates the diff and is the sqlmodel shape exactly: `common/core/__init__.py` wraps the auto-setup in `except Exception`, and `jazzx_sdk/observability/telemetry.py:95` answers the same `ImportError` with `logger.error` + `return False`, so every image built so far has had telemetry silently off.
IMPORTANCE: high -- the deployment artefact cannot build, and without this line the telemetry path is dead in the image behind one log line. Reachable in every image build; `common.utils.telemetry` is reached from `jazzx_sdk/observability/telemetry.py` (6 call sites), `observability/log.py:263` and `observability/trace_context.py` (3), all guarded, plus `common.core.telemetry_setup` at `common.core` import.

[DEFECT] tests/test_common_submodule_deps.py:219 :: resolves-checks-the-venv-not-the-lock -- the sweep asserts `find_spec` resolves in whatever venv runs pytest, not that japes declares the distribution, so a module present incidentally passes while the image, installed `--only main` from the lock, lacks it.
`opentelemetry.instrumentation.fastapi` is in the reached set (`common.utils.telemetry`), the case passes here, and the module is absent from `poetry.lock` -- the finding above. The test's own failure text tells the author to "add this one to japes' [tool.poetry.dependencies]", which is the thing it does not check. `defusedxml.*` passes the same way (declared `optional = true` behind the `plato`/`bpmn` extras, not in `_BEHIND_AN_EXTRA`), so a bare `poetry install` would red it for a correct configuration.
IMPORTANCE: medium -- test-only, but it is the gate for the one defect that has now shipped twice, and it named a shipped-code defect on this branch. One test file; nothing outside the repo consumes it.

[DEFECT] tests/test_plato_packs_api.py:1730 :: config-patch-test-reads-ambient-tier -- the new PATCH test takes neither `clean_env` nor `strict=`, so it reads the ambient posture, and its write leaks into `os.environ` for the rest of the session.
Ran it: `JAPES_ENVIRONMENT=production .venv/bin/python -m pytest -k settable_through_config` fails with 403 `config_writes_disabled` -- `create_config_router(prefix=...)` resolves `strict` from `strict_mode()` (`plato/api/config.py:231`) and `_add_refusing_patch` replaces PATCH. Ran it again from a two-test file: after it, `os.environ["PLATO_PACK_UPLOAD_MAX_BYTES"]` is `1048576` in the next test, because `EnvSettingsStore.write` sets the real environment and nothing undoes it. The fix the previous round applied to `test_plato_info_api.py` (delenv the posture vars) is the same one; the fixture at line 32 already does it.
IMPORTANCE: low -- test-only and no current test asserts the default cap, so the leak is inert today; the ambient failure is a false red on any machine with a tier set.

[SYMMETRY] tests/test_plato_packs_api.py:1755 :: env-set-by-hand-not-clean-env -- `test_a_nonsense_cap_does_not_refuse_every_upload` hand-rolls `os.environ` save/restore where this file's own `clean_env` fixture (line 32, `monkeypatch`-backed) is the shape every other env-touching test in it uses; taking the fixture and calling `clean_env.setenv` is four lines shorter than the try/finally.
IMPORTANCE: low -- test-only, correct as written; it is the sibling of the leak above and fixing both the same way is what keeps them from diverging.

[NOTE] plato/api/packs.py:188 :: unset-backend-renders-empty -- the refusal interpolates `env_text(PACK_BLOB_BACKEND_ENV)` with no default while the predicate beside it uses `default="local"`, so the most common trigger (variable simply unset on a deployed tier) reads `PLATO_PACK_BLOB_BACKEND='' is not durable storage`.
`JAPES_ENVIRONMENT=staging` and no `PLATO_PACK_BLOB_BACKEND`: the guard is right, the message loses the fact that unset means local, which the docstring above it and the docs row both state.
IMPORTANCE: low -- one operator-facing string on a 503 that also names the fix; no behaviour depends on it.

[NOTE] docs/DEPLOYMENT_ENV.md:155 :: config-patch-needs-auth-when-strict -- the row says `PATCH {prefix}/config` moves the cap on a running replica without the qualification `plato/api/packs.py:262-266` states: on staging/production `strict_mode()` is true by default and PATCH answers 403 unless the deployment wired an auth dependency, which no shipped wiring does.
No operator consequence that I can find, and that is why this is a NOTE: in the one configuration where the claim is false (strict, no auth) `POST {prefix}/packs` is refused by the same `withholding` call, so the cap is moot there. Both gates move together with `auth`.
IMPORTANCE: low -- documentation only; the two tiers it misdescribes cannot upload at all in that configuration.

VERDICT: BLOCK
