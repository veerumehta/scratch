# adversarial review -- .

- when: 2026-09-24T20:58:12Z
- range: c1881a4..HEAD (since the last review) (3d3678e)
- diff: 306 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/pipelines/policy_extract.py :: section-documents-inflate-sources -- FIXED
WAS: jazzx_sdk/pipelines/policy_jtbd.py :: cross-section-rule-id-collision -- FIXED
WAS: jazzx_sdk/pipelines/policy_extract.py :: list-extract-silently-empty -- FIXED
WAS: jazzx_sdk/pipelines/policy_extract.py :: failed-section-name-ambiguous -- FIXED

[DEFECT] jazzx_sdk/pack/rule_merge.py:130 :: existing-status-overwritten -- The docstring says that when the file already holds the policy, "the file's header is kept and only its rules change". But `merged = target.model_copy(update={..., "status": status})` always replaces the file's `status` with the `status` argument, ACTIVE by default.
Trigger: the draft's `policies/rent.yaml` holds `rent` with `status: retired` (or a hand-authored `draft`), and one accepted rule proposal is merged with default arguments. The written policy now says `status: active`, so publishing the draft puts the whole policy back in force, not just the one rule. Nothing in `RuleMergeResult` reports the status change. The replace test only checks that `name` survives, not `status`.
IMPORTANCE: medium -- `merge_rules_into_draft` is new and has no callers in japes outside its test. The flipped status only takes effect when a person publishes the draft, and it shows in the draft diff. But no field in the merge result flags it, so a reviewer who reads only `added`/`replaced` will miss it.

[NOTE] jazzx_sdk/pack/rule_merge.py:78 :: dot-prefixed-dir-unreachable -- `manifest_reaches` compares raw strings. The loader instead joins `dir`/`core`/`overlays` onto the pack root with `relative_name`.
Trigger: `policies: {dir: ./policies}` or `core: ./policies/core.yaml` loads the file, but `manifest_reaches` returns `reachable=False`. The failure is loud (a false "enforces nothing"), not silent.
IMPORTANCE: low -- this is a false negative only. Every in-repo manifest I grepped uses bare `policies` paths.

[NOTE] jazzx_sdk/pack/rule_merge.py:120 :: duplicate-accepted-rule-id -- If two accepted proposals share a `rule_id`, both are marked MERGED, the last one wins, and the second is counted under `replaced` even though it replaced a sibling proposal, not a rule in the file.
Trigger: a caller passes a hand-built `list[Proposal]` that joins two sets. `propose_step` already dedupes by `rule_id` inside one `ProposalSet`, so a single set cannot trigger this.
IMPORTANCE: low -- the pipeline cannot produce this. It needs a caller to concatenate sets.

VERDICT: BLOCK
