# adversarial review -- japes

- when: 2026-09-11T00:42:19Z
- range: origin/v2.5.1...HEAD (f471ff9)
- diff: 12129 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No `[DEFECT]`. Ran `ruff` (clean) and 648 tests across the policy/profile/pack/plato/adjudication surfaces (all green).

## CHECKED

- `TODO(activation-vs-active-rules)` (`policy.py:750`) — "no caller reads these accessors yet" holds: `executable_rules`/`activation_blocks`/`is_executable` appear only in `tests/test_policy_rule_activation.py`.
- `TODO(threshold-grid-provenance)` (`profiles.py`) — holds. `dscr_profile.yaml` authors all 7 `thresholds:` as bare quoted decimals and `tables` as bare cells, so `looks_authored` is False for every one; the gate covers none, as the note now says.
- `TODO(publish-tick-tie)` (`store_db.py:62`) — the revised claim holds: `plato/seed.py:175` is the only `publish` loop and does one blob write + one round trip per directory.
- `TODO(refused-rule-field-claim)` (`default.py`) — holds: `allowed = not violations and not not_activated`, so a refusal already denies; the cost is a misleading violation entry.
- `TODO(orphan-blob-gc)` (`store_db.delete`) — holds: the store is tenant-scoped at construction, so the cross-tenant `count(*)` it names genuinely can't be asked here.

## Findings

**[SYMMETRY]** `jazzx_sdk/agents/adjudication/segment.py:121` — the `NOT_APPLICABLE` branch still hand-builds its outcome (`action` dropped, `inputs={"applicability_gate_verdict": ...}`) while its twin at `fabric/graph/check.py:397` builds `action=rule.action, inputs=gate_outcome.inputs`. This diff added `blocked_gate_outcome` (`condition_evaluator.py:103`) citing exactly that drift — "`run_segment`'s copy dropped `action` where `check_graph`'s kept it" — and shared only the blocked case, leaving the unsatisfied-gate case divergent in the same two ways. A `not_applicable_outcome(rule, gate_outcome)` beside it is the same amount of code.
IMPORTANCE: low -- enumerated both gate call sites (`_apply_applicability_gate`, `check._evaluate`) plus `check_compliance`, which `continue`s and builds no outcome; no in-repo consumer of `run_segment`'s outcomes reads `.action` (checked `adjudication/agent.py`, `tracing.py`). `run_segment` is exported from `jazzx_sdk.agents`, so an out-of-repo consumer could.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/policy_lint.py:49` — writes `(*policy.rules, *policy.exception_rules)` inline while its own comment names `Policy._all_rules()`, which this same diff added at `policy.py:750`. `_all_rules` carries `TODO(activation-vs-active-rules)` about reconciling with `active_rules`; when that moves, the lint's copy won't.
IMPORTANCE: low -- one call site; `find_profile_literal_drift`/`assert_no_profile_literal_drift` are the only readers, both in-repo plus `tests/test_policy_lint.py`.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:626` — the docstring still says "Three independent checks, first match wins per policy" while the rewritten body deliberately gathers every cause and joins them with `" and "`. The diff edited check #1's text on line 627 without touching the claim above it.
IMPORTANCE: medium -- a policy that is both deprecated and has a superseded pin now reports both reasons; a reader of this docstring will expect one.

**[NOTE]** `jazzx_sdk/manifest/lifecycle.py:28` — `__all__ = ["PolicyStatus"]` sits above `as_utc` and omits it, so `import *` misses the function the module docstring says six siblings should migrate onto. `manifest/__init__.py` re-exports only `PolicyStatus`; `fabric.canonical` is the only package re-exporting `as_utc`.
IMPORTANCE: low -- every current importer (`policy.py`, `profiles.py`, `guidance/schema.py`) uses an explicit `from ... import as_utc`, which ignores `__all__`.

**[NOTE]** `jazzx_sdk/fabric/canonical/__init__.py:230` — exports `gate_blocks` but not its paired `blocked_gate_outcome`. `experts/policy/default.py` imports `gate_blocks` from the package while `segment.py` and `check.py` reach into `condition_evaluator` for the builder, so the two halves of one contract have different public surfaces.
IMPORTANCE: low -- all three call sites are in-repo and resolve fine today.

VERDICT: PASS

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED:**
- `TODO(conductor-pipeline-from-yaml)` (ci-spread-core/pack_manifest.yaml:~140): holds. `Pack.conductor` is `None` for this pack, `ConductorPipeline.from_yaml(pipelines.yaml, name="credit_analysis")` loads only when a caller names the file, and nothing in japes/plato does.
- `DSCR-HIGH-LTV-DSCR`'s "masked, not absent" comment: holds. At `cltv_pct=85, dscr_documentation_type=no_ratio` the ratio condition is INDETERMINATE, and `DSCR-NO-RATIO-CLTV` returns VIOLATED (cap 75).
- `workbook_layout.yaml`'s "every key … does resolve today; that was checked by hand": holds. All 84 statement keys + 16 metric keys + `normalization_target: ebitda` in `workbook_layout.yaml` and `workbook_layout_rb.yaml` resolve against the pack's own `chart_of_accounts.yaml` / `metrics.yaml`, statement types included.
- `TODO(seed-pack-vendoring)` + the six `tests/test_plato_packs_api.py` seed-pack tests pass against this diff; the six-id registry claim in ci-spread-core's `policies` comment is exact, `overlays/deal_si_rb_example.yaml` stays out, `checklist_ci.yaml` is skipped by shape.

---

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:160` — `RB_CI_OVERLAY` ships inside the `dir: policies` corpus with nothing declaring it an overlay, so `DefaultPolicyExpert` defaults `core_policy_ids` to all six policies, `RB-CI-LEVERAGE-CEILING` claims `leverage_x` ahead of `CI_CORE_LEVERAGE_POLICY` (purely because `conventions.yaml` sorts before `core.yaml`), and both core leverage rules are dropped from evaluation.

Trigger: `Pack.from_manifest("ci-spread-core", …).policy_registry` into `DefaultPolicyExpert`, context `leverage_x=3.2`, no `program_id`. Result: zero leverage findings; `applied_policies` contains `RB-CI-LEVERAGE-CEILING` and neither `CI-LEVERAGE-CEILING` nor `CI-LEVERAGE-WARNING`. Removing `RB_CI_OVERLAY` from the same registry yields `CI-LEVERAGE-WARNING` VIOLATED. The doc-extracted leverage policy `core.yaml` calls "the YAML source of truth" never runs, and the manifest comment at `pack_manifest.yaml:37` ("overlays: applied when program_id matches") is not enforced on this route — `resolve()` puts `RB_CI_OVERLAY` in `precedence_order` with and without `program_id: rb-overlay`. It survives today only because the placeholder 3.5x happens to equal the core ceiling; the overlay's own `calibration_status: placeholder` invites the edit that removes the 3.5x cap too.
IMPORTANCE: high -- silent loss of a policy gate with no error. Verified through `Pack.policy_registry` → `DefaultPolicyExpert.check_compliance`, the exact route `pack_manifest.yaml:36` advertises. Nothing in plato evaluates these policies (plato/seed.py only publishes archives), so the reachable callers are outside this repo: any consumer hydrating the expert from this pack's registry without passing `core_policy_ids`/`overlay_map`. `Pack.policy_registry` flattens to `PolicyRegistry(policies)` and discards the core/overlay split that `manifest_loader.policy_files()` preserves in file order, so no pack-side declaration can fix it.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:184` — `RB-CI-FCCR-FLOOR` declares `supersedes_core_rule: CI-FCCR-FLOOR` but never evaluates: `CI_CORE_FCCR_POLICY` sits earlier in the same registry, claims `fccr_x`, and the overlay rule is the one skipped — the inverse of the leverage case above, decided by nothing but ordering.

Trigger: same construction, `fccr_x=1.20`. `applied_policies` contains `CI-FCCR-FLOOR` and `CI-FCCR-WARNING`; `RB-CI-FCCR-FLOOR` is absent. So one file states two supersession relationships and the runtime honours one of them, backwards.
IMPORTANCE: medium -- no verdict differs today because both thresholds are the same placeholder 1.15x; it becomes wrong output the first time the RB floor is calibrated away from core. Same call path and same external callers as the finding above; verified empirically, not inferred.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:106` — the rule text returned as guidance says "10% of borrowing base", but the encoded leg reads `excess_availability_pct`, which the comment on :104 states is computed against total availability; on a line capped below the base the percentage leg is permissive and the `$2M` leg does not cover it.

Trigger: borrowing base $30M, line capped at $10M, excess availability $2.5M → `excess_availability_pct = 0.25 >= 0.10` and `2_500_000 >= 2_000_000`, so `CI-ABL-MIN-AVAILABILITY` is SATISFIED; 10% of the base is $3.0M, so the rule as described is violated. (Below a ~$20M base the `$2M` leg dominates and hides this; above it, it does not.) The comment records the field mismatch but not this consequence — `rule.description` is handed back verbatim as guidance text, as `DSCR-IO-MIN-LOAN`'s own comment in this diff establishes.
IMPORTANCE: medium -- reachable only on facilities with a base over ~$20M and an availability cap below the base, and it fails silently rather than loudly. I could not establish who produces `excess_availability_pct`: no producer of that field exists in this repo, so whether the comment's "against total availability" is accurate rests on the comment alone.

---

**[SYMMETRY]** `plato/data/seed_packs/dscr_core/pack_manifest.yaml:38` — the manifest declares `policies:` and `profiles:` but no `evidence_types:`, so `Pack.evidence_types` is `[]` for dscr-core while four of its rules name eight evidence types in `parameters.evidence_types` (`nmls_verification`, `hoa_budget`, `hoa_financials`, `operating_agreement`, `articles_of_organization`, `certificate_of_good_standing`, `bank_statement`, `asset_statement`). This is the identical gap, and identical fix, that `ci-spread-core/pack_manifest.yaml:50`'s `evidence_types: evidence_types.yaml` line was added for in this same diff — with the argument spelled out in its own comment ("the tool registry derives a requestable set of nothing"). Verified: `Pack.from_manifest("dscr-core", …).evidence_types` returns 0 entries; ci-spread-core returns 7, including all three ids its `convergence_evidence_required` names.
IMPORTANCE: medium -- one manifest line plus the asset file it points at; same blast radius as the ci-spread-core line, i.e. any consumer building a tool registry from the pack.

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/spread_template_rb.yaml:126` — "Cash Interest" is left `unbound: true` while "Less Cash Taxes (Refund)" two rows up binds `income_taxes_paid_net_of_refunds`, on the same reasoning the unbound row rejects. Both keys exist in this pack's `chart_of_accounts.yaml`, both are already bound side by side in `spread_template.yaml`'s `supplemental_disclosures` section and in `workbook_layout.yaml`'s `inputs_cash_flow` rows, so `interest_paid` is the specific symbol this lines up with and binding it is one line. The file's own comment at :118 notes the inconsistency and leaves it.
IMPORTANCE: low -- no japes/plato code reads `spread_template_rb.yaml` at all (no loader exists in this repo); it changes one row of a consumer-rendered spread.

---

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:209` — "12 of the 42 rules … use `kind: all_of`/`any_of`" then names ten (nine PPP rules plus `DSCR-NMLS-VERIFIED`). The count is right; the enumeration omits `DSCR-IO-MIN-LOAN` and `DSCR-OVER-2M-NO-RATIO`, both of which this diff added and both of which use `all_of`. Re-derived from the file: 2 `all_of` conditions, 10 `all_of` applicability gates.
IMPORTANCE: low -- doc-only, and the §1 table's own counts (42 rules; 32/6/2/2 by kind) are exact.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:24` — records the version bump as `1.1.0` → `1.2.0`, but `policies/eligibility.yaml:4` ships `version: 1.3.0`; the notes describe the PPP rules that caused the 1.3.0 cut without recording the bump.
IMPORTANCE: low -- doc-only; the shipped version is the higher one, so nothing reads a stale number.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/spread_template.yaml:36` — `{metric: da}` and `{metric: stock_based_compensation}` (:38) resolve against neither the pack's `metrics.yaml` (16 ids) nor `jazzx_sdk.finance.metrics`' `credit_metrics`/`working_capital_days` dicts; the other eleven `metric:` ids in the file resolve against the latter. `:110`'s `{statement: cash_flow, key: net_income}` also names a key the chart of accounts declares as `income_statement`.
IMPORTANCE: unknown -- I could not determine who consumes `spread_template.yaml`: no loader for it exists anywhere in japes or plato, so I cannot say whether an unresolvable `metric:` id produces an empty row (the failure mode `TODO(layout-key-resolution)` documents for the workbook path) or resolves against a consumer-side namespace I cannot see.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No `[DEFECT]` survived verification. Ran the affected suites (279 passed, 2 skipped), `ruff` clean on every changed file, and checked the new profile data directly.

## CHECKED

- `TODO(omit-info)` (`plato/info_api.py:82`) — holds. `_info_only_app`'s tuple is `("Guide", "config", "database", "Packs", "packs")`; `info` is absent and no other caller passes `omit` to `dashboard_html`.
- `TODO(if-match-post)` (`plato/posture.py:96`) — holds. `require_if_match` is referenced only by `tests/test_plato_preconditions.py`; no router mounts it, so the new `POST /packs/initialize` cannot 428.
- `TODO(seed-pack-vendoring)` (`plato/seed.py:48`) — holds. `Pack.from_manifest` + `policy_registry` + `policy_profile()` resolve for both bundled packs with only japes importable; remaining consumer-name hits are the pinned data/prose files.
- `TODO(local-wiring-build-test)` (`tests/test_plato_packs_api.py`) — holds. The only other importer of `plato.wiring_local` internals is `tests/test_plato_wiring.py:160` (`_find_root`), which the reload re-derives from `__file__`; nothing reads the frozen `DB_PATH`/`CACHE`.

Also verified on the new `dscr_profile.yaml`: `max_cltv_grid` is complete at 99/99 cells for the 3×11×3 axes, `max_cltv_itin` at 33/33, every `profile:`/`profile_custom_key:`/`profile_table:` reference in `eligibility.yaml` resolves, `find_profile_literal_drift` returns 0, and the grid is monotone in FICO, non-increasing in loan band, with no `NA` above an eligible tier. `"NA"` is handled by `_NA_SENTINELS` in `MatrixEvaluator`, and `DSCR-PROPERTY-TYPE-CLTV`'s applicability gate exactly matches the four keys in `max_cltv_property_type`, so no live cell-miss path exists.

## Findings

**[NOTE]** `plato/posture.py:181` — `relaxed_only(strict=False)` short-circuits *both* halves of the gate, so an explicit `False` re-opens the tier hole the comment three lines above says was closed: a `staging`/`production` replica would accept `POST /packs/initialize` and `DELETE /packs/{id}/{v}` unauthenticated, contradicting the docstring and the 403 text. `withholding`'s `strict=` override is only over `strict_mode()`, which has no tier component, so the two are not the same override.
`IMPORTANCE: low -- unreachable in any shipped configuration: I enumerated every `create_packs_router` call site (`plato/app.py:228` only) and `create_plato_app` never forwards a `strict`; only tests pass it, and they pass `True`. Nothing outside this repo constructs the router.`

**[NOTE]** `plato/wiring_default.py:443` and `:657` — both comments justify `.resolve()` as anchoring "like `default_seed_root()` and both of `wiring_local`'s equivalents (`_anchored`)", but `.resolve()` resolves against the *process cwd*; `_anchored` (`plato/wiring_local.py:143`) resolves against the fixed `_REPO`. `.resolve()` pins the cwd at `build()` time (which does close the "cwd moved later" case the blob comment names) but does not make launching from a different directory yield the same path, which is what `_anchored` guarantees.
`IMPORTANCE: low -- comment-only; the code's behaviour is unchanged and the container WORKDIR is fixed, which the comment itself says. Blast radius is a reader who copies the pattern expecting `_anchored`'s guarantee.`

**[NOTE]** `plato/info_api.py:306` — `packs_html`'s "No `omit`, unlike its two siblings" is true of one sibling: `dashboard_html(prefix, *, omit=())` takes it, `guide_html(prefix)` (line 245) does not.
`IMPORTANCE: low -- docstring only; I checked all three functions in plato/info_api.py and their callers in plato/app.py and plato/__main__.py.`

VERDICT: PASS

## slice4

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
All 21 tests pass. One defect, one symmetry, two notes.

**CHECKED:** `TODO(threshold-grid-provenance)` at `jazzx_sdk/fabric/canonical/profiles.py:257` — holds. `dscr_profile.yaml` has 7 bare-string `thresholds:` (all `looks_authored` → False) and 150 `tables:` cells across 5 grids = the 157 it names; `get_table` has no gate. The deferral is accurate.

---

**[DEFECT]** `tests/test_policy_threshold_provenance.py:322` / `jazzx_sdk/fabric/canonical/profiles.py:84` — the suite sweeps *absent* `value` but not `value: ` **present and null**, and that input produces an approved `None`.

`Threshold.value` is `Any` with no validator, so YAML

```yaml
dscr_min:
  value:                      # blank scalar -> None
  provenance: official_source
  status: active
```

builds `Threshold(value=None, is_executable=True)`. Verified: `get("dscr_min")` returns `None`, `get_threshold("dscr_min").is_executable` is `True`. `test_a_governance_block_with_no_value_is_refused_by_both` covers the *omitted* key only (`ValidationError` → the "not a readable Threshold" refusal), so the one-character difference between omitting `value:` and writing it blank is refusal vs. serving `None` as an official-source, active figure. The field's own description, `"The number (decimal string or int)"`, is the guarantee the type does not enforce.

Downstream it lands as a crash, not a refusal: `evaluate_guard` (`jazzx_sdk/statemachine/schema.py:140`) raises `decimal.InvalidOperation` — confirmed by running it — which escapes `engine.py:93`'s `except ThresholdNotActivated` / `except KeyError`, so the transition evaluator raises instead of returning a `Refusal`. `condition_evaluator.py:324` does `float(threshold)` outside its `ThresholdNotActivated` handler → `TypeError`. That is the exact shape `test_a_non_string_governance_key_is_refused_not_a_crash` exists to prevent, one field over. `finance/validation.py:601` survives it (`Severity(None)` → `ValueError` → caught).

IMPORTANCE: medium -- three `profile.get` call sites enumerated (`statemachine/schema.py:135`, `condition_evaluator.py:315`, `finance/validation.py:601`); two of the three turn it into an uncaught exception, the third degrades to a default. No shipped profile authors the governance form yet, so it is reachable only once a pack adopts it — which is the feature's whole point. jaci is editable-installed against this tree.

---

**[SYMMETRY]** `tests/test_policy_threshold_provenance.py:110` — this test reads the bundled DSCR profile through `get_threshold`, but `tests/test_pack_store.py:498` and `tests/test_pack_policy_profile.py:115,155,251` read the same key as `profile.thresholds["dscr_min_high_ltv"] == "1.20"`, by raw dict access. The moment `dscr_profile.yaml` adopts the governance form the gate was built for, those four assertions compare a `Threshold` object against a string and fail, while the new test keeps passing. The consistent version is `profile.get("dscr_min_high_ltv")` — same length, and it is the accessor this diff establishes as the contract.

IMPORTANCE: low -- four assertions in two test files, no shipped code reads `.thresholds` directly (grepped `jazzx_sdk/` and `plato/`: only `profiles.py:194,224`).

---

**[NOTE]** `tests/test_policy_threshold_provenance.py:322` — the docstring describes the fixed case as "a block declaring `status: seed` and forgetting its number," but the fixture also carries `provenance: secondary_source`, which is the branch that actually fires. The status-only variant still behaves as the docstring says the old code did: `PolicyProfile.model_validate({"profile_id": "p", "thresholds": {"dscr_min": {"status": "seed", "source": "trade grid"}}}).get("dscr_min")` returns the raw dict. That is deliberate per `looks_authored`'s docstring (status alone is an ordinary word), but the test's prose reads as a guarantee over both.

IMPORTANCE: low -- test prose only; the code's own docstring at `profiles.py:143` states the real rule correctly.

**[NOTE]** `tests/test_policy_threshold_provenance.py:173` — the claim that `get`'s three buckets are covered is precise about `confidence_floors` being unreachable, but `custom` also holds ~20 bare scalars in the shipped DSCR profile (`loan_amount_min`, `fico_min_no_ratio`, …) that `get` serves ungated and that `policy_lint` resolves via `profile_custom_key`. The TODO's "157 numbers" counts thresholds + grid cells only.

IMPORTANCE: low -- counting precision in a deferral note; no behaviour depends on it.

VERDICT: BLOCK
