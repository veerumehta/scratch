# adversarial review -- japes

- when: 2026-09-08T22:45:07Z
- range: 824fa36...HEAD (0ddc73b)
- diff: 299 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(swap-schema-placement)` in `plato/wiring_default.py:506` — the "eight" is right. `register_all` registers exactly 8 tables (`agent_session, assistant_manifest_record, config_audit_event, model_overlay, pack_version, plato_setting, turn_run, turn_run_event`) and `TABLE_SCHEMAS` has the same 8, symmetric difference empty. The rest of the deferral (relaxed-posture only, needs a later alembic run to bite) still holds.

**CHECKED:** `_unpack`'s new docstring claim. Measured: a zip member `../escaped.yaml` extracted by `ZipFile.extractall` lands at `root/escaped.yaml`, so the removed assertion in `test_an_archive_member_outside_the_destination_is_refused` really was vacuous and the guard really is a loud-failure choice, not a containment fix. The docstring rewrite and the assertion removal are both accurate.

**CHECKED:** removing `seq` loses nothing. `autoincrement=True` on a non-primary-key `Integer` is a no-op in SQLAlchemy, so every row's `seq` was `0` and the tiebreak never broke a tie. Migration `0003` is safe to edit — `824fa36` is on no remote branch (`git branch -r --contains` empty, no `origin/v2.5.1`), so no database has applied it.

---

**[SYMMETRY]** `jazzx_sdk/pack/store_db.py:172` — the content-addressed key rule now exists twice: here as `f"packs/sha256/{digest}.zip"` with `jazzx_sdk.digest.content_digest`, and at `jazzx_sdk/fabric/blob/store.py:121` as `f"sha256/{hashlib.sha256(payload).hexdigest()}"` inside `offload(dedup=True)`. The comment names `offload(dedup=True)` as the precedent but reimplements it rather than reusing it, and the two reach the hash by different functions (they agree for `bytes`, diverge for anything else). Concrete consequence: the same archive routed through `offload(dedup=True)` lands at `sha256/<hex>` while `publish` lands it at `packs/sha256/<hex>.zip` — two copies of identical bytes under two keys, which is the thing dedup exists to prevent. The consistent version is `BlobStore` owning key derivation (extract line 121 into a `content_key(data, *, prefix, suffix)` and call it from both), which is smaller than the comment explaining why it wasn't done.
IMPORTANCE: low — I enumerated every `put(key=)` call site in `jazzx_sdk/` and `plato/`; there are exactly two (this one and `offload`), nothing outside japes references `PackVersionStore` (grepped `/Users/sangit/src`), and no pack archive is currently offloaded, so the double-write is latent rather than live.

---

**[NOTE]** `jazzx_sdk/pack/store_db.py:57-58` — "the only tie would be two *different* versions of one pack published in the same microsecond, where either order is correct" overstates. `get(pack_id)` is documented (`store.py`) as "the most recently published one", and callers use it as *latest version*; on a tie between `0.1.0` and `0.2.0` returning `0.1.0` is not "correct", it is the wrong pack under a contract that reads as if it can't be. Same for `list_packs`'s `newest.setdefault` at line 273. No shipped caller reaches it and a same-microsecond tie needs two `publish` round-trips inside one `datetime.now()` tick, so the deferral is fine — the sentence is what claims more than the code does.
IMPORTANCE: low — no caller in this repo invokes `publish`/`get`; `DbPackVersionStore` is only constructed by `plato/models.py:49` for metadata registration, and no sibling repo imports it.

**[NOTE]** `jazzx_sdk/pack/store_db.py:310-320` — the digest re-hash sits below the cache short-circuit at line 299, so the guarantee the comment states ("nothing else would notice bytes that no longer match the row") only holds on a cold cache: once `<cache_root>/<digest16>/.pack_manifest_root` exists, `materialize` returns without hashing anything. Not wrong data today — the cached tree was written from bytes that matched — but the check is narrower than the comment says, and the default `cache_root` is `tempfile.gettempdir()/japes-packs`, which survives process restarts and is shared with every other process on the box.
IMPORTANCE: low — reachable only by mutating the cache tree or the blob out of band; the only exercise of this path is `tests/test_pack_store.py`, which always runs cold against `tmp_path`.

**[NOTE]** `tests/test_pack_store.py:219` — the new cross-tenant test gives `other` its own `cache_root=tmp_path / "other-cache"`, while the sibling test at line 189 shares `tmp_path / "cache"` and production sets no `cache_root` at all (the single construction site, `plato/models.py:49`, takes the shared `/tmp/japes-packs` default). `_cache_dir` keys on digest alone with no tenant, so the shared-cache path is the one worth pinning; the test passes either way, and with a shared root it would additionally cover it at zero cost.
IMPORTANCE: low — test-only; the shipped defect it would catch (one tenant's tree served from another's cache entry) requires a 64-bit digest-prefix collision, so there is nothing behind it today.

VERDICT: PASS
