# adversarial review -- japes

- when: 2026-09-24T04:46:15Z
- range: HEAD~1...HEAD (1e3872f)
- diff: 256 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
I found no defects. The five inventory tests moved over intact. `pack_inventory` is still exported from `jazzx_sdk.pack` (`jazzx_sdk/pack/__init__.py:73`). Plato's `/api/v1/packs` route is still covered by route tests in `tests/test_plato_packs_api.py` (for example lines 1205 and 1415). The removed `pack_inventory` import had no remaining users in that file. The `_seeded` helper in that file is still used, and it lives in a different module from the new `_seeded` in `test_pack_store.py`, so the two don't clash.

[NOTE] tests/test_pack_store.py:843 :: seeded-store-never-disposed -- The moved tests no longer close the database engine, which the originals did (`await db.dispose()` in a `finally`), and every other store test in this file does too (`await store._db.dispose()`).
Trigger: `test_the_inventory_groups_versions_under_their_pack`, `test_one_unreadable_pack_does_not_blank_the_view` and `test_an_unreadable_pack_is_flagged_not_shown_as_empty` each create a sqlite engine through `_seeded` and never dispose it. The seed test just above (around line 820) already had the same gap before this diff.
IMPORTANCE: low -- this only affects tests. Each engine is scoped to `tmp_path`, so the worst case is a ResourceWarning or a file handle left open until garbage collection. No shipped code is involved.

VERDICT: PASS
