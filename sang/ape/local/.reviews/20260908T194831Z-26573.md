# adversarial review -- japes

- when: 2026-09-08T19:48:31Z
- range: 23a555e..HEAD (since the last review) (23bf1c1)
- diff: 35 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Step 1 — prior round

WAS: `tests/test_plato_wiring_default.py:412-417` disposal ordering — **FIXED**; the predicate is captured at `:413`, both disposals run at `:414-415`, and the assert at `:416` now fires after cleanup, matching the twin at `:459-461`.

WAS: `plato/wiring_default.py:494-496` docstring narrating its own edit history — **FIXED**; the "not kept referenced, which is what this said before" clause is gone and only the `del previous` / in-flight-reference explanation survives.

CHECKED: `wiring_default.py:505` `TODO(swap-schema-placement)` — unchanged and its claim still holds (relaxed posture only, `/v1/config` is the sole caller path via `plato/app.py`); not reopened.

## Step 2 — new findings

No defects. The diff is a docstring rewrite and a reordering inside one test; the reordering does not change what the test proves (`resolve_degraded` is still evaluated at `:413`, before either store is disposed), and the docstring's surviving claim ("never disposed") matches `swap_database` at `:516-522`, which only calls `handle.replace` and `del previous`.

VERDICT: PASS
