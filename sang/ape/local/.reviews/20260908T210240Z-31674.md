# adversarial review -- japes

- when: 2026-09-08T21:02:41Z
- range: origin/dev...HEAD (40cfdc4)
- diff: 197 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** no `TODO`/`skip` markers in the diff.

---

**[DEFECT]** `plato/oidc.py:243` — stamping `fetched=True` on failure makes the *next* 30 seconds of verifies report an issuer outage as a bad token: the cooldown returns the (empty) cached key set, so `verify` falls through to `raise TokenError(f"token signed by unknown key {kid!r}")` at line 282 instead of the fetch error. Measured with a cold verifier, `fetch_jwks` raising `httpx.ConnectError`, frozen clock:

```
0 -> could not fetch signing keys: jwks down
1 -> token signed by unknown key 'k1'
2 -> token signed by unknown key 'k1'
```

Trigger: fresh process (or any empty-key cache) plus an unreachable JWKS endpoint. The first verify names the outage; every one after it blames the caller's token, and nothing logs the still-failing issuer. Not a wrong-accept — old keys were served on the unforced path before this diff too — but the operator-facing diagnosis inverts. The three new rate-limit tests all assert bare `pytest.raises(TokenError)` for verifies 2..5, which is exactly why this passed review; `match="could not fetch"` on the second iteration would have caught it.
IMPORTANCE: low -- no in-repo caller: `grep -rn TokenVerifier plato/ jazzx_sdk/ tests/` returns only `oidc.py:146` and its own `__all__:344`, and `plato/oidc.py:153` still says nothing wires it into a request path. Reachable today only by an external consumer importing `plato.oidc`.

**[DEFECT]** `plato/oidc.py:223` vs `225` — the retained comment "one unusable key must not void the whole set" no longer holds for an entry that isn't a mapping: `entry.get("kid")` on line 218 raises `AttributeError` *outside* the inner guard, so the new outer handler discards every key parsed so far and rate-limits for the window. Trigger: a JWKS containing one good key and one non-dict entry, or `keys` served as an object instead of an array (`{"keys": {"k1": {...}}}` iterates to `str` keys) — the same 200-with-garbage shape the new comment on line 239 argues for. Moving `kid = entry.get(...)` inside the existing inner `try` restores the stated guarantee.
IMPORTANCE: low -- same radius as above: no in-repo call site; requires a misconfigured issuer *and* a direct consumer of `plato.oidc`.

**[NOTE]** `plato/oidc.py:271-280` — the new wrapper's `except Exception` branch has no reachable trigger: `_fetch_and_cache` now converts every fetch/parse failure to `TokenError` itself, and both handlers emit the identical string `could not fetch signing keys: {exc}`. The only non-`TokenError` escapes left from `_load_keys` are an injected `now` raising and the two module-level imports at lines 207-209. Consequently `test_an_unreachable_issuer_is_a_token_error_not_a_500` passes with the wrapper deleted — it exercises the line 245 conversion, not the line 279 one it documents.
IMPORTANCE: low -- test-only; the shipped behaviour is correct either way, cost is one redundant handler.

**[NOTE]** `plato/oidc.py:179-185` — `_fresh_enough`'s docstring now understates what it returns: `fetched_at` means "last *attempt*", not "last fetch", and the keys it hands back can predate that stamp by any number of failed refreshes. A permanently-down issuer re-stamps every 30s, so the returned set has no upper bound on age.
IMPORTANCE: low -- comment accuracy on a private helper with one caller (`_load_keys`, lines 195 and 201).

No [SYMMETRY] findings: `grep -rn "jwks\|MIN_REFRESH\|_fresh_enough" plato/*.py` shows `oidc.py` is the only remote-key fetcher in the repo, so there is no sibling cache to line up with.

VERDICT: BLOCK
