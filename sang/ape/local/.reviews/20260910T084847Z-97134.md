# adversarial review -- japes

- when: 2026-09-10T08:48:47Z
- range: origin/v2.5.1...HEAD (ab71dbd)
- diff: 9870 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED
- `TODO(threshold-grid-provenance)` (profiles.py:214) — holds: `get_table` (profiles.py:229) has no status gate and `tables` is `dict[str, dict[str, Any]]` with no coercion validator, exactly as the deferral says; a cell is served as before, so not a regression.
- `TODO(activation-vs-active-rules)` (policy.py:758) — holds: `executable_rules`/`activation_blocks`/`is_executable(policy_status)` have no shipped callers (only `tests/test_policy_rule_activation.py`); `active_rules` at policy.py:792 does filter `legacy_alias` and does ignore `exception_rules`, as stated.
- `TODO(orphan-blob-gc)` (store_db.py:delete) — holds: the store is tenant-scoped at construction and blob keys are content digests, so a cross-tenant refcount is genuinely unaskable here.
- `TODO(layout-key-resolution)` / `TODO(layout-live-formula-order)` (workbook.py:177,183) — both hold: `item is None: continue` at workbook.py:413, and `inputs_by_statement.get(...)` read at workbook.py:690 before the populating write at :694.

## Findings

**[DEFECT]** `jazzx_sdk/experts/policy/default.py:622` (with `policy.py:731`) — folding `effective_date` into `Policy.is_active` makes a *future-dated* policy report as **stale**, and `detect_stale_pinned_policies` (finance/validation.py:854) returns `report.stale_policies` only, discarding the `staleness_reasons` sentence that would have said "is not effective until…". Trigger: a registry `Policy` with `effective_date` = tomorrow, pinned in `package.policy_versions`; the wrapper's own docstring says the returned ids "have since been superseded in the registry". Pre-diff this policy was not stale (`age_days` negative, so the 18-month check missed it). `resolve`'s `stale` list (default.py:331) mislabels it the same way; `check_compliance` does not filter on `is_active`, so the only effect is the wrong label.
IMPORTANCE: medium -- two in-repo readers of the flag (`DefaultPolicyExpert.resolve` stale_policies, `finance.validation.detect_stale_pinned_policies`), and the second is a public FR-SOP-5 entry point that jaci's spread validation calls, where the reason string is dropped and only the id survives.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:128` — the newly bundled seed pack points `modes.evaluator.class`, `experts.policy.class`, `experts.playbook.class`, `conductor.class` and `conductor.pipeline` at `jaci.scenarios.ci_spread.*`, `evaluation.gold_cases_path`/`runner` at jaci's `tests/eval/...`, and `playbooks[].source_doc` at `docs/LoanSamples/YETI/...` — a consumer repo's module namespace and directory layout inside japes' own package data. `Pack._resolve` (pack/pack.py:43) does a bare `importlib.import_module` with no guard, so `pack.conductor` on this pack raises `ModuleNotFoundError` wherever jaci is not installed; the pack itself seeds and publishes fine (`plato/seed.py:90`, `MANIFEST_NAMES` includes `pack_manifest.yaml`). The pack also ships `pipelines.yaml`, the declarative pipeline this manifest does not point at. The sibling seed pack `dscr_core` declares no `class:` pointers at all.
IMPORTANCE: unknown -- I could not establish an in-repo caller that reaches the resolution: `plato/packs/domain.py:89` composes `Pack.from_loader` and reads only policies/profiles, and `plato/packs/loader.py` only materializes; jaci itself does have those modules, so its own consumption resolves. What I could not determine is whether any external japes-only deployment builds `.conductor`/`.experts` off a seeded pack.

**[SYMMETRY]** `jazzx_sdk/finance/validation.py:830` — `_WARNING_REFS`/`_warning_refs` regex-parse `policy_id`/`rule_id` back out of the string that `_not_activated_warning` (experts/policy/default.py:71) formatted three modules away, while the violations loop directly above it at validation.py:806 reads the same two fields structurally from `v["policy_id"]`/`v["rule_id"]`. The consistent version is no larger: have `check_compliance` carry the not-activated entries in the same dict shape the violations already use (or a parallel structured list on `ComplianceResult`), which removes the regex and the cross-module format coupling the comment at :828 has to document.
IMPORTANCE: low -- one producer (`DefaultPolicyExpert.check_compliance`, the only setter of `warnings` in the repo) and one consumer; the failure mode is a `None`/`None` detail on a policy_id containing whitespace, not wrong numbers.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/policy.py:126` — `as_utc` duplicates `jazzx_sdk/pack/store_db.py:_as_utc` (same naive→UTC normalization, one extra `None` case), and its home in the policy *schema* module forces a function-local import in `fabric/guidance/schema.py:96` to dodge a cycle. Guidance needing a deferred import of `canonical.policy` for a two-line datetime helper is the signal that both copies want one shared location.
IMPORTANCE: low -- two definitions, three call sites (`Policy.not_in_force`, `Rule.activation_block`, `GuidanceAsset.is_active`) plus `store_db._record`; no behavioural difference today.

**[NOTE]** `jazzx_sdk/agents/adjudication/segment.py:108` — the `INDETERMINATE` gate outcome is appended to the list literally named `not_applicable` and returned as the not-applicable half of `_apply_applicability_gate`'s tuple. `run_segment` uses it as an outcome bag so behaviour is right, but the name now claims a verdict the contents can contradict.
IMPORTANCE: low -- one caller (`run_segment`), which reads each outcome's own `verdict`; nothing counts the list.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:399` — the new seed pack documents `Policy.status` as `active | draft | retired`, but `PolicyStatus` (manifest/lifecycle.py) has no `retired`; the members are `seed | draft | active | suspended | deprecated | archived`. A pack authored against this ontology comment fails `Policy` validation.
IMPORTANCE: low -- documentation-only field in the ontology YAML; nothing validates policy status against it, and the enum itself rejects the bad value loudly at load.

VERDICT: BLOCK

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `plato/posture.py:97` `TODO(if-match-post)` — the deferral's premise holds: `require_if_match` appears only in `posture.py` and `tests/test_plato_preconditions.py`; no router mounts it, so the new `POST .../packs/initialize` cannot 428.
- `plato/info_api.py:84` `TODO(omit-info)` — holds: `_info_only_app`'s `degraded_nav = ("Guide", "config", "database", "Packs", "packs")` does not contain `info`, and nothing else passes `omit`.

## Findings

**[DEFECT] `plato/packs_api.py:73`** — `latest_version` (and the page's "Latest" column, `packs.html:328`) is whatever `list_packs()` returned, which `PackVersionStore.list_packs`' own docstring says is the most recently *published* row, explicitly "the wrong [answer] for a caller reading it as the latest version".
Trigger: publish `dscr-core 0.2.0`, then publish the backport `0.1.1` — the pack row reports `latest_version: "0.1.1"` while `0.2.0` is live in the expanded list.
IMPORTANCE: medium -- one producer (`pack_inventory`, exported in `__all__`) and one consumer (`GET {prefix}/packs`, mounted by `plato/app.py:228` in both `wiring_default` and `wiring_local`); display-only, the per-version table beside it is correct; grep finds no consumer outside this repo.

**[DEFECT] `plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml:5`, `ENCODING_NOTES.md:14`, `ci-spread-core/reclassifications.yaml:1`** — the bundled seed packs carry a named client's program encoding (`institution_ref: acra_lending`, `authority: Acra Lending`, the full rate/CLTV grid and buyout rules), consumer-repo internals (`jaci.capabilities.commercial_lending.*`, `tests/unit/test_reclassification.py`, `cd jaci && pytest`, jaci commit SHAs), source-document layout (`docs/LoanSamples/YETI/...`, `docs/LoanSamples/Regional Bank/Output Cleaned.xlsx`), and gitignored plan filenames (`plan_JACI_CL_*`, `plan_JAPES_2_3_0_*`, `docs/plans/Acra_DSCR_on_Platform_v2_*`).
Trigger: `plato/data/**` ships in the wheel and image (poetry `packages = [{include = "plato"}]`; `info_api.py:65` relies on the same mechanism for `guide.md`), so `default_seed_root()` resolves inside site-packages and one `POST {prefix}/packs/initialize` publishes `dscr-core 0.1.0-draft` with that content into any tenant's `pack_version` rows.
IMPORTANCE: high -- reaches every installer of the japes wheel or image, not just this checkout; `tests/test_plato_packs_api.py:447,469` already read the bundled tree, so it is load-bearing rather than inert; nothing scrubs it at publish.

**[SYMMETRY] `plato/packs_api.py:147-155`** — the `except ValueError -> HTTPException(400, ...)` guard around `tenant_from_request` is added here only; `plato/assistants_api.py:166` `_tenant` calls the same resolver with no guard, so `wiring_default.tenant_from_request`'s `ValueError("X-Tenant-Id is required on a deployed Plato request")` escapes as a 500 with a stack trace from every chat/session route (there is no global `ValueError` handler in `jazzx_sdk/server/app.py`). The consistent version is the same three lines, or one shared `_tenant` helper both routers call.
IMPORTANCE: medium -- seven call sites in `assistants_api` (lines 213, 243, 284, 296, 303, 313, 325), reachable on any deployed posture from a client that omits the header; fails loudly, and the caller cannot tell which header is missing.

**[NOTE] `plato/packs_api.py:12`** — "a pack store whose rows can go in staging is not one" is a claim `_refuse_unless_relaxed` does not enforce: it gates on `strict_mode()`, and `JAPES_STRICTNESS=relaxed` loosens every tier except `production` by design (`envvars.strict_mode` docstring), so on staging both writes are admitted. `deployed_posture()`/`environment_tier()` are the predicates that would hold the sentence. The `(local, dev-daily)` gloss itself matches house convention (`config_api.py:22`); only the staging sentence overreaches.
IMPORTANCE: medium -- the two write routes in this file; `tests/test_plato_packs_api.py:218,302` parametrize staging/production with `clean_env` clearing `JAPES_STRICTNESS`, so the loosened case is untested.

**[NOTE] `plato/packs_api.py:64-66`** — the comment says a pack whose versions will not read "renders, with none: the operator learns the pack exists and is unreadable", but the entry carries no marker, so the row is byte-identical to a pack with zero live versions and `packs.html` prints "0". Nothing conveys the failure except the server log.
IMPORTANCE: low -- one page; the operator gets a confusing count rather than wrong data, and the warning is in the log.

**[NOTE] `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:14,241`** — the shipped notes state `pack_manifest.yaml` is "**still not wired** (`policies.registry` needs a Python `PolicyRegistry` object)", which the manifest committed beside it contradicts (`pack_manifest.yaml` declares `policies: dir: policies` and says the registry is now built from it), and §5 points a reader at `config/packs/dscr_core/` and `cd jaci && pytest` — neither path exists in this repo.
IMPORTANCE: low -- documentation inside the seed pack; misleads whoever inspects the bundled pack, changes no behaviour.

VERDICT: BLOCK

## slice3

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(local-wiring-build-test)` (tests/test_plato_packs_api.py:707) — the claim holds. `build()` → `_assemble_pack()` raises `SystemExit` at plato/wiring_local.py:267 without a sibling `jazzx-assistant` profile, so the source-text assertion is the honest scope. Not raised as a finding.

---

**[DEFECT]** `plato/static/packs.html:329` (and the total at :309) — a pack whose `versions()` query failed renders as `Versions: 0` with a populated `Latest`, and nothing on the page says it failed.

`pack_inventory` deliberately answers `versions: []` for that pack (the shape this diff's own `test_one_unreadable_pack_does_not_blank_the_view` pins), and the JSON carries no flag distinguishing it from "no versions". Concrete trigger: `list_packs()` succeeds and the per-pack `versions()` round-trip raises (pool recycle / dropped connection against Postgres, which is two separate queries) — the row then shows `Latest 0.2.0-seed / Versions 0`, the header undercounts ("1 version" instead of 3), and expanding shows a header-only table. A pack row cannot exist with zero live versions, so the rendered count is wrong data whose only trace is a server-side `logger.warning`. packs_api's own comment claims "the operator learns the pack exists and is unreadable"; the page does not deliver that.
IMPORTANCE: medium -- operator surface only. I enumerated the consumers of `GET {prefix}/packs`: the page at `/packs/ui` (this file, the sole render path) and any human curling the JSON; nothing in this repo or in `plato/app.py`'s routers reads it programmatically, and no consumer outside the repo does. Fails invisibly to the operator, loudly in the log.

**[SYMMETRY]** `plato/wiring_default.py:469` — `_pack_blob()` is resolved inside `pack_store_for`, where the sibling degradation resolver `_db(posture)` is resolved once in `build()` (line 423). Its two `logger.warning` calls therefore land at first pack request rather than at boot beside `_db`'s, and `_pack_stores.clear()` (line 467) makes every request a cache miss once `PLATO_TENANTS` lists more than one tenant, so both warnings re-fire per request in a staging replica with no `PLATO_PACK_BLOB_*` set. Hoisting `pack_blob = _pack_blob()` next to line 423 and closing over it is one line shorter than the current call site.
IMPORTANCE: low -- log volume, not behaviour. Reachable in any deployed posture with ≥2 tenants configured (`_tenants(posture, [])`); I checked all three factories in `build()` and only this one logs from inside the cache-miss path.

**[SYMMETRY]** `tests/test_plato_packs_api.py:191` — the page-family invariants in `tests/test_plato_info_api.py` enumerate the pages by hand (`test_all_three_pages_share_one_nav`:668, `test_each_page_marks_itself_current`:681, `test_no_page_is_left_with_an_unsubstituted_placeholder`:698, and the parked host scan at :631) and none was extended to `packs_html`; this diff instead re-states two of them locally. Adding `packs_html()` to those existing tuples is less code than the local assertions, and the parked test's own docstring records this exact miss happening once already ("the version of this that listed two left `plato/static/info.html` scanned by nothing").
IMPORTANCE: low -- test-only. The shipped defect it would catch is a page shipping a literal `__NAV_CSS__`/`__NAV__`; I verified by hand that `packs_html("/api/v1")` currently substitutes all three placeholders and marks itself current, so nothing is broken today.

**[NOTE]** `tests/test_plato_packs_api.py:644` — `page.count("headers(") >= 4` is satisfied by exactly 4 occurrences in `packs.html`, one of which is the prose comment at packs.html:187. Editing that comment reds the test with no behaviour change.
IMPORTANCE: low -- one test file.

**[NOTE]** `plato/wiring_default.py:617` — `container=env_text(PACK_BLOB_CONTAINER_ENV) or None` is correct (the provider falls back to settings on `None`), but `AsyncAzureBlobStorageProvider.get_instance` is a process-wide singleton that ignores `container_name` after the first call, so `PLATO_PACK_BLOB_CONTAINER` only takes effect if pack archives are the first azure blob consumer in the process. No reachable trigger today: `_pack_blob` is the only `BlobStore(backend="azure", ...)` construction in `plato/`. A `TODO` is a complete response.
IMPORTANCE: low -- unreachable in any shipped plato configuration; would matter if a second azure blob user is wired in.

**[NOTE]** `plato/static/packs.html:318` — `#empty` doubles as the "Nothing published yet" note and the error channel, so a failed delete's message (e.g. the 403 detail in a strict posture) is wiped by `empty.hidden = true` on the next expander click, which re-runs `render` without any retry.
IMPORTANCE: low -- one page, message loss only.

VERDICT: BLOCK
