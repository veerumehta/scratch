# adversarial review -- japes

- when: 2026-09-09T07:25:31Z
- range: 432f038..HEAD (since the last review) (18c8e7e)
- diff: 87 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**Step 1 — prior round**

WAS: `wiring_default.py:277` redaction left at callers — FIXED (`redact_secrets(note)[:BOOT_NOTE_MAX_CHARS]` at the single write point, before dedup and `_GATING_NOTES.add`, so all three consumers compare the same string).
WAS: `wiring_default.py:334-339` `missing_config` unbounded — CHECKED (see below), not open.
WAS: `wiring_default.py:250-252` "long enough for a driver message with its cause" — FIXED (the constant's comment now states the budget covers prefix + cause and points at `reason(exc, limit=...)`).

**CHECKED**

`TODO(bound-missing-config)` (`wiring_default.py:339-343`) holds: I enumerated every `missing.append` reachable from `missing_config` (`wiring_default.py:98, 328, 333, 345, 352`) and only `:345` interpolates a caller-supplied value, `os.getenv(PLATO_PACK_DIR)!r`, a path an operator wrote. No credential-shaped producer feeds `resolve_degraded` unbounded, so "a long `/info` field, not a leak" is accurate and the deferral is rated correctly.

**Step 2 — this diff**

No defects. Verified: `redact_secrets` is idempotent over the shapes these notes carry (postgres/redis userinfo including `p@ss@`, `Bearer`, JWT, `api_key=`, and already-redacted output), so the comment's "callers that already went through `reason` pass through unchanged" is true; the redact-then-truncate order matches what the comment claims; all 7 `record_boot_note` call sites (`__main__.py:179, 360, 374, 385, 405, 418`, `wiring_default.py:205`) survive redaction without over-scrubbing, since none contains a `SENSITIVE_KEY_NAMES` word followed by `:`/`=`; the new test fails against the pre-diff line and `ruff` is clean.

[NOTE] `plato/wiring_default.py:278-282` — the comment asserts commit history ("two of these call sites passed a raw exception until the commit that added this"), which the repo's convention puts in the changelog rather than in code; it also goes stale silently, since all 7 call sites redact today and nothing in the tree will contradict the sentence when that stops being the relevant history. The test docstring at `tests/test_plato_wiring_default.py:397-400` repeats it.
IMPORTANCE: low -- comment text only; no behaviour, and the rule it states ("both at the source") is independently true from the code beneath it.

Unrelated to this diff, but it will be in the push: `tests/test_plato_info_api.py::test_config_reports_what_the_replica_actually_resolved` fails (`assert None == 'sqlite'`) when run after `tests/test_plato_settings_store.py`, passes alone. I confirmed it predates this change by reverting the `redact_secrets` line and reproducing the same failure.

VERDICT: PASS
