# adversarial review -- japes

- when: 2026-09-11T17:32:32Z
- range: 6e602d9..HEAD (since the last review) (7a5ec04)
- diff: 137 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_mock_knowledge_hub_app.py` → 31 passed; `ruff check` clean on all three files. New behaviour re-probed through `TestClient` (outputs below are actual).

WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: explicit-null-field-poisons-key -- FIXED for the PATCH route; `moving` now tests `is not None`, `_effective` falls back to `was[n]` for a `null`, and both probes behave: `PATCH {"name":null}` → 200 with name still `e-1`, then re-`POST` the original body → 409 (was 201); `PATCH {"name":"e-2","collection_id":null}` → 200 keeping `col-1`, then `POST {name:"e-2",collection_id:"col-1"}` → 409. The `""` half is now 422 (`PATCH {"collection_id":""}` → 422) rather than silently discarding the key.
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: non-validating-delegate-claim -- FIXED; the comment now says the conflict returns before the write, so such a body is still a 409, which is what the code does.
WAS: jazzx_sdk/clients/mocks.py :: dead-lookup-before-return -- FIXED; the lookup moved below the early return.

CHECKED: `mock_knowledge_hub_app.py :: TODO(missing-fields-still-400)` still holds as written -- the absent-field guard is still a 400 where the real body model is a 422, and the detail shape still differs (flat string vs FastAPI's list), so the deferral's reason is unchanged by this diff.

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:222 :: null-required-field-passes-create -- the create route's required-field guard stringifies a JSON `null` (`str(entity_data.get(f, ""))` → `"None"`), so the sibling of the hole this diff just closed on PATCH is still open: a null `name` or `collection_id` is accepted where the service 422s, and the phantom `"None"` half is recorded in `_created_names`.
Probed, both halves:
`POST {name:null, collection_id:"col-1", ontology_id:"test-ontology", entity_type:"test_type", json_value:{"a":1}}` → **201**, body `"name":null`, and the key `("col-1","None")` is added -- so a later legitimate `POST {name:"None", collection_id:"col-1"}` → **409** for an entity whose name is not `"None"`.
`POST {collection_id:null, name:"e-1", ...}` → **201** with `"collection_id":null`, key `("None","e-1")`; the real client cannot even send this (`knowledge_hub_client.py:1595` does `uuid.UUID(collection_id)`), and the service's `UUID` body field 422s it.
The PATCH route's own new rule is the fix: treat `None` as not-supplied (there, absent) and `""`/whitespace as the 422, instead of letting `str(None)` pass a truthiness test. As it stands the same body is a 201 on create and a 422 on patch.
IMPORTANCE: medium -- silent, and in the direction this app exists to catch (it accepts a body the service refuses, then 409s one it accepts). In-repo reachers are `tests/test_mock_knowledge_hub_app.py` only; out of repo `create_knowledge_hub_mock_app` is exported from `jazzx_sdk.server` for packs to point an HTTP client at, and the client shape the `_effective` docstring names (unset optionals serialized as `null`) reaches it on a create as readily as on a PATCH. No real-service path touched.

[NOTE] tests/test_mock_knowledge_hub_app.py:357 :: null-case-asserts-only-collection -- for the `{"name": None}` parameter the only direct assertion is `collection_id == "col-1"`, which is trivially true for that case; that the name was left alone is proven only indirectly, by the later 409 on the held key. An `assert patched.json()["name"] == "e-1"` under that branch would pin it directly.
IMPORTANCE: low -- test-only; the indirect assertion does fail against the pre-fix code (the old key was discarded, so the clash returned 201), so the regression is covered either way.

VERDICT: BLOCK
