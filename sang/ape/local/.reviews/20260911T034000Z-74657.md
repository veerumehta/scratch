# adversarial review -- japes

- when: 2026-09-11T03:40:00Z
- range: 8b49b90..HEAD (since the last review) (10e5501)
- diff: 503 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS lines first, then what this diff introduces.

WAS: jazzx_sdk/manifest/lifecycle.py :: as-utc-copies-unconverted -- STILL OPEN; the docstring no longer claims the copies were converted, but it still says "Six" and still omits the seventh, `plato/reference/model_overlay.py:79`.
WAS: jazzx_sdk/finance/validation.py :: violation-detail-has-no-source -- FIXED.
WAS: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped -- STILL OPEN; `ontology/ci_lending.yaml` still carries five `source_schema: jaci.*` data values and the metric/template bindings are unchanged, deferred by `TODO(seed-pack-vendoring)` and recorded by the pinning test.
WAS: jazzx_sdk/experts/policy/base.py :: wrong-todo-pointer -- FIXED.
WAS: jazzx_sdk/fabric/canonical/profiles.py :: get-threshold-raises-unnamed-error -- FIXED; `_value_is_present` only rejects empty, so the new "is empty" message cannot mislabel a non-empty value.
WAS: plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml :: ddtl-vocabulary-mismatch -- FIXED in the ontology and every matcher; one manifest list is still short, reported new below.
WAS: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer -- STILL OPEN; declaring the field in the ontology does not compute it, no producer exists, so the rule still returns INDETERMINATE on a context without it and `check_compliance` still skips it unrecorded, and the new ontology comment reads in the past tense as though the skip were closed.
WAS: plato/data/seed_packs/ci-spread-core/playbooks/term_loan.md :: leverage-rating-bands-contradict -- FIXED.
WAS: plato/data/seed_packs/ci-spread-core/pack_manifest.yaml :: truncated-digest-named-sha -- FIXED at all four sites; nothing in japes reads any of them.
WAS: plato/data/seed_packs/ci-spread-core/playbooks/abl_revolver.md :: availability-denominator-uncaveated -- FIXED.
WAS: plato/data/seed_packs/dscr_core/ENCODING_NOTES.md :: threshold-count-six-vs-seven -- FIXED for thresholds; the custom count in the same row went stale in this diff, reported new below.
WAS: plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml :: excess-availability-description -- FIXED.
WAS: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim -- STILL OPEN; the file still says it is loaded by `CIConductor`, the retraction lives only in the manifest TODO.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; ENCODING_NOTES now states the consequence plainly, but the four rules still report NOT_APPLICABLE when the field is absent.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: itin-cap-not-profile-linked -- FIXED; `find_profile_literal_drift` over the bundled pack reports zero findings and the declaration sits inside `condition`, where the lint walks.
WAS: plato/info_api.py :: packs-route-family-not-enumerated -- FIXED; both guide rows land and the `/api/v1/packs` row is the one `startswith` matches, ahead of `/packs/ui`.
WAS: plato/seed.py :: nonexistent-test-name-cited -- FIXED.
WAS: plato/seed.py :: vendoring-todo-truncated -- STILL OPEN; `seed.py:44` still ends "...without that consumer's checkout. Two" mid-sentence.
WAS: plato/posture.py :: relaxed-only-fails-open-on-unknown-tier -- STILL OPEN; untouched.
WAS: tests/test_plato_packs_api.py :: getattr-default-hides-attributeerror -- FIXED, and the `hasattr` pre-check drops the dead `conductor_pipeline` name.
WAS: tests/test_plato_packs_api.py :: header-count-literal -- FIXED.
WAS: tests/test_plato_packs_api.py :: parametrize-literal-pack-ids -- FIXED.
WAS: tests/test_plato_packs_api.py :: hardcoded-manifest-name -- FIXED.
WAS: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised -- STILL OPEN; untouched.
WAS: tests/test_plato_packs_api.py :: local-wiring-todo-overstates -- STILL OPEN; untouched.
WAS: tests/test_policy_rule_activation.py :: activation-boundary-untested -- STILL OPEN; untouched.

[DEFECT] plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:110 :: ddtl-missing-from-loanpkg-applies-to -- the ddtl vocabulary pass covered diagnose_map and two `applies_to` lists but not the fourth, `pb-ci-loanpkg-001`, which is still `[abl, revolver, term_loan]`.
A facility with `loan_type: delayed_draw_term_loan` now matches `term_loan_standard` in `diagnose_map.yaml:57` and is in `pb-ci-term-001` and `pb-ci-memo-001`'s `applies_to`, but is the only term-loan variant excluded from the C&I loan-package scaffold; the new ontology comment at `ci_lending.yaml:51` enumerates "the manifest's two playbook `applies_to` lists" and treats the manifest as covered.
IMPORTANCE: medium -- I enumerated all four `applies_to` lists in the manifest and every `ddtl`/`delayed_draw` occurrence in the pack (now 8 hits, 4 files). Nothing in `jazzx_sdk` or `plato` reads playbook `applies_to` (grepped; the only hits are the interactive guard registry), so the cost lands entirely on the consumer's `CIPlaybookExpert`, which I could not run from this repo.

[SYMMETRY] tests/test_plato_packs_api.py:588 :: manifest-names-rehardcoded-in-helper -- `_bundled_pack_ids` hardcodes `("pack_manifest.yaml", "manifest.yaml")` and breaks on the first file found, nine lines below the sibling this same diff converted to `plato.seed._manifest_of` for exactly that reason; `_manifest_of` also owns the anonymous-sibling fallback (`seed.py:80`), so a pack whose `pack_manifest.yaml` has no `pack_id` beside a complete `manifest.yaml` is dropped from the parametrize here while `seed_packs` publishes it.
IMPORTANCE: low -- both bundled packs carry an identified `pack_manifest.yaml`, so no current gap; the generalization is smaller than the loop it replaces and lines up with the edit already made at line 544.

[NOTE] plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:54 :: custom-key-count-stale -- the row rewritten in this diff fixes "6 ratio thresholds" to 7 but keeps "11 custom scalars" and "those 18 keys", while the same diff adds `itin_loan_amount_max` to `custom:`; the file now holds 7 + 12 = 19, and §3 G4's "six of the profile's eighteen keys" is the same count one section down.
IMPORTANCE: low -- shipped doc only; I re-derived both buckets from `dscr_profile.yaml` (thresholds 7, custom 12) and re-confirmed the six unreferenced keys are still six.

[NOTE] plato/data/seed_packs/ci-spread-core/playbooks/term_loan.md:134 :: only-rating-scale-claim -- the new parenthetical says §term-007 "is the only one in this playbook", but §term-004 (`:94-96`) also assigns PASS/WATCH and SPECIAL MENTION from FCCR bands. Those bands agree with §term-007 exactly, so nothing contradicts; the claim is just wider than the file.
IMPORTANCE: low -- advisory text; I compared all FCCR and leverage bands in §004, §005 and §007.

[NOTE] tests/test_plato_packs_api.py:600 :: empty-parametrize-is-a-skip -- `_bundled_pack_ids()` runs at collection time and `clean_env` clears only `JAPES_ENVIRONMENT`/`JAPES_STRICTNESS`, so with `PLATO_SEED_PACKS_DIR` pointed at a tree whose manifests carry no `pack_id` (the workflow `TODO(seed-pack-vendoring)` recommends) the list is empty and the guard degrades to an empty parameter set rather than the loud failure the literal pair used to give.
IMPORTANCE: low -- pytest reports an empty set as a skip, so it is visible in the summary, and `test_every_bundled_seed_pack_loads` still reports such a directory through `broken`; test-only, no shipped-code defect it would have caught.

VERDICT: BLOCK
