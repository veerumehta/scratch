# adversarial review -- japes

- when: 2026-09-24T04:36:27Z
- range: d002db7..HEAD (since the last review) (a57c3f3)
- diff: 92 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_plato_database_rows.py :: literal-sql-restated -- FIXED
WAS: plato/api/database.py :: redundant-second-cache -- FIXED
WAS: scripts/run_tests.sh :: stale-test-path-example -- FIXED
WAS: jazzx_sdk/net_safety.py :: stale-oidc-path-comment -- FIXED
WAS: jazzx_sdk/oidc.py :: plato-scoped-prose-in-sdk -- FIXED
WAS: jazzx_sdk/pack/seed.py :: service-name-in-sdk-docstring -- FIXED

What I checked in this diff, with nothing found:
- The new cross-reference in `net_safety.py` is accurate. `jazzx_sdk/oidc.py:278` sends `_fetch_jwks` through `call_maybe_async`.
- The cache claim in the `_statements()` docstring is accurate. `owned_tables()` is `@lru_cache(maxsize=1)` at `plato/models.py:61`. `_statements()` has one caller, `database.py:169`, so it now builds one small dict per request. That costs almost nothing.
- The example path `tests/test_oidc.py` exists. No remaining references to `test_plato_oidc` or `plato/oidc.py` turned up in `jazzx_sdk`, `scripts` or `tests`.
- The looser test still catches the two properties it names. It fails if the table name isn't quoted in the SQL, or if the `:limit` bind is missing.

VERDICT: PASS
