# adversarial review -- japes

- when: 2026-09-09T03:22:02Z
- range: origin/dev...HEAD (973b438)
- diff: 1021 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(jwks-error-detail)` (plato/oidc.py:~288) holds: `grep` for `TokenVerifier`/`TokenError` outside `plato/oidc.py` and its tests returns nothing, so no middleware renders the message today; log-only is a complete response.
- `TODO(malformed-jwks-clock)` (tests/test_plato_oidc.py:~555) holds: the malformed-document path stamps the same `_KeyCache(...fetched_at=self._now()...)` line that `test_a_down_issuer_is_not_refetched_on_every_verify` exercises with an advancing clock.

---

**[DEFECT] `jazzx_sdk/pipelines/policy_extract.py:579`** — `default_step_error` is the only one of the five sibling handlers that does not set `state.halt = True`, so the run continues past the failed step, `finalize` emits a `PolicyExtraction`, and `run_policy_extract` returns it — the `Refusal` this diff just fixed is discarded and the run reports `complete`.

Trigger, measured with the real engine (`ConductorEngine` breaks only on `state.halt`; `finalize` carries no guard):

```
policy_extract    (['finalize'], 'complete', 'EXTRACTION')   # lint raised TimeoutError
vocabulary_build  ([],           'halted',   None)
```

So with `on_step_error=default_step_error` and a failing `lint`, the caller gets `PolicyExtraction(..., lint=<Refusal>)` and `.unreachable` then raises `AttributeError` on `Refusal.findings`. It also makes the recovery branch at policy_extract.py:698 (`next(v for v in emitted.values() if isinstance(v, Refusal))`) dead — its comment says "a halt before finalize", and nothing halts. `ingest_step`'s own docstring (line 361) states "A refusal halts the run", which this handler does not enforce. Secondary: the two `Refusal`-returning siblings both pass `remediation="retry, or inspect the underlying error"`; this one drops it.

IMPORTANCE: medium -- no caller in japes or jaci passes `on_step_error` for this pipeline today (grepped both repos; only tests construct it), but it is in `policy_extract.__all__` and `catalog.py:70` lists the pipeline as a public entrypoint, so the first external adopter of the documented opt-in gets a silently-complete extraction rather than a refusal. One line.

**[DEFECT] `jazzx_sdk/server/app.py:293` and `:299`** — the `/invoke` error envelope that inbound.py:133's new comment names as its mirror ("surface as an error response, mirror /invoke") still puts raw `str(e)` in both `payload.data.error` and `error.message`; line 286's `logger.error(f"... {e}")` is unredacted too. The sibling got `reason()`, this one did not.

Trigger: a handler whose exception quotes a credential. Concrete and already-known in this repo — an `httpx` failure on a KH signed-URL blob fetch names the final hop with `sig=` intact (the exact leak `_blob_failure`/`redact_urls` exist for), and a SQLAlchemy URL error carries `postgresql+asyncpg://user:pw@host` (verified `redact_secrets` now scrubs both). That body is returned to the HTTP caller. `/invoke` is served by every japes server-mode app, including jaci's shipped React thin client (`jaci/src/jaci/main.py:4`).

IMPORTANCE: high -- credential in an unauthenticated-reachable response body; two sites in one handler plus the log line, and an external consumer (jaci) fetches this route from a browser.

---

**[NOTE] `tests/test_policy_extract.py:437`** — the new test passes `None` for `state`, which only works because the handler never touches it; that is precisely why the missing `state.halt` is invisible here. Asserting `state.halt` on a real `ConductorState` is what the four siblings' tests can do and this one cannot.

IMPORTANCE: low -- test-only; it would have caught the defect above, which I am reporting on the code.

**[NOTE] `jazzx_sdk/server/inbound.py:141`** — `error.message` now carries the `"TypeName: text"` prefix while `error.type` repeats it, so the envelope's `message` field changed shape for existing consumers even though the comment says "`type` stays its own field".

IMPORTANCE: low -- format change in a field a caller reads for display, not for branching; no parser in japes or jaci matches on it.

VERDICT: BLOCK
