# adversarial review -- .

- when: 2026-09-25T04:10:57Z
- range: d0810df..HEAD (since the last review) (462510d)
- diff: 688 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Two new defects, both BLOCK: a Python rule that fails the static check gets silently skipped (so the case can still be allowed), and `rule_segment` breaks the citations on formalized rules. Three of last round's four findings are fixed.

WAS: tests/test_default_policy_expert.py :: shared-id-gate-untested -- FIXED
WAS: jazzx_sdk/pack/manifest_loader.py :: scalar-program-split-chars -- FIXED
WAS: jazzx_sdk/pack/lint.py :: product-policy-in-no-program -- FIXED
WAS: jazzx_sdk/pack/lint.py :: program-check-needs-policies -- STILL OPEN. The new `overlay_in_no_program` check sits behind the same `if policies:` gate, so `lint_pack(pack)` with no policies passed still reports neither check.

[DEFECT] jazzx_sdk/fabric/canonical/condition_evaluator.py:836 :: bad-source-skips-not-withholds -- The static check runs before the executor check and returns `CONDITION_UNEVALUABLE`. `DefaultPolicyExpert` (default.py:505-515) skips that reason silently, while `POLICY_NOT_ACTIVATED` withholds the allow. The result is that an unsafe rule gets milder treatment than a safe one.
Trigger: a pack YAML has a `DENY` rule with `condition: {source: "def check(ctx):\n import os\n return False", reads: [dscr]}` and no executor is installed. `check_compliance` returns `allowed=True` with no warning. The same rule with clean source returns `allowed=False` with "no Python executor is installed". The executor-exception branch (line 848) and the wrong-return-type branch (line 852) end the same way: a crashing or timed-out executor turns a DENY into an allow.
IMPORTANCE: high -- the case is allowed with no warning. I found no shipped pack that declares a `python` rule yet. Any pack author reaches this through the `source`+`reads` discriminator or `kind: python`, and jaci/Plato reach it through `from_pack`. The only guard is `_python_findings` in lint, which catches the static-check branch only when someone runs it. The executor-failure branches have no guard at all.

[DEFECT] jazzx_sdk/pipelines/policy_code.py:66 :: rule-segment-mangles-citations -- `rule_segment` passes the rule's first citation (a `source_refs` ref_id) as `document`. `extract` then writes `[segment.citation]`, which is `"<ref_id>#<rule_id>"`. That is not a ref_id on any policy, and every citation after the first is dropped.
Trigger: formalizing `Rule(rule_id="DSCR-FLOOR", citations=["summary", "term-sheet"])` gives `citations=["summary#DSCR-FLOOR"]`. It should carry `["summary", "term-sheet"]`. The generated rule no longer resolves against `Policy.source_refs`, which is what `Rule.citations` is declared as (policy.py:588). The test asserts nothing about citations.
IMPORTANCE: medium -- the provenance link is silently wrong on every rule-formalized candidate, but a human reviews each candidate in the proposal queue before it lands. The only callers are `PythonExtractor.extract` via `rule_segment`, and they are new in this diff with no consumers outside it yet.

[NOTE] jazzx_sdk/fabric/canonical/condition_evaluator.py:840 :: optional-read-never-runs -- A rule written as `ctx.get('x', default)`, which the static check and the prompt both explicitly allow, never runs when `x` is absent. The missing-evidence check treats every declared read as required.
Trigger: `reads: [guarantor_fico]` with source `return ctx.get('guarantor_fico', 0) >= 680 or True`, on a case with no guarantor. The result is `EVIDENCE_MISSING` and the rule is skipped.
IMPORTANCE: low -- the rule is skipped the same way as any other kind with missing data. The only effect is that the `.get` form suggests an optionality that doesn't exist.

VERDICT: BLOCK
