# adversarial review -- japes

- when: 2026-09-09T07:44:56Z
- range: origin/dev...HEAD (587bd36)
- diff: 896 lines


## whole

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(bound-missing-config)` (`plato/wiring_default.py:337`) — holds. `missing_config` interpolates only the `PLATO_PACK_DIR` repr, the posture string and env var *names*; `_tenants` interpolates nothing. No exception message reaches that list.
- `TODO(settings-store-env-leak)` (`tests/test_plato_settings_store.py:26`) — holds, reproduced verbatim: `pytest tests/test_plato_settings_store.py tests/test_plato_info_api.py` gives `1 failed` on `test_config_reports_what_the_replica_actually_resolved`; no random-order plugin is installed (pytest 9.1.1, asyncio + cov only) and the full suite is green (5232 passed, 18 skipped).
- `TODO(jwks-discovery)` (`plato/oidc.py:143`) — holds. Grep finds no construction of `OidcSettings`/`TokenVerifier` outside `tests/`, so no deployment resolves the Keycloak-shaped default today, and the new `allow_hosts` knob has no env plumbing to be missing.

## Findings

**[DEFECT]** `jazzx_sdk/fabric/docs/store.py:1552` — `materialize()` writes `out / filename` where `filename` is `name_fn(entity)`, which for `sync_collection` is the Knowledge Hub's stored document name; it gets none of the `_local_path` shape rule this diff added to `_put_local`/`_get_local`/`_delete_local`/`_get_test_fixture` in the same class. A collection holding a document named `../../evil.pdf` writes those bytes to `out.parent.parent` (the write succeeds — the parent exists, and there is no `mkdir` to fail on), and `process_dir` then globs `base` and never sees the file, so the run also silently drops it. Reached by `DocStore.sync_collection` → `DocumentAgent.process_dir(collection_id=...)` (`agents/document/agent.py:610`) → `process_collection_step`, i.e. the exact `collection_id` path `_scratch_dir` was just guarded for.
IMPORTANCE: high -- enumerated every local write in `fabric/docs/store.py` (`_put_local`, `materialize`, `_get_test_fixture`) and both callers of `sync_collection` (`process_dir`, the ingest pipeline's collection route); the name is remote and user-uploaded, and nothing on the path inspects `MaterializedDoc.file_path`. Predates this diff, but it is the sibling of the two paths the diff did guard. No caller found in the `/Users/sangit/src/jaci` checkout, so external exposure is unestablished.

**[DEFECT]** `jazzx_sdk/fabric/blob/store.py:70` (and the same predicate at `fabric/docs/store.py:503`) — `PurePosixPath(".").parts` is `()`, so `"."` is neither empty, absolute, nor `..`-bearing and passes the guard, and `Path("blobs") / "."` collapses to the store root. Measured: `await store.put(b"x", key=".")` returns `blob://.` and writes a *file* at the blob directory's own path; every later `put` then fails `FileExistsError: .../blobs`, and against a normal directory `get("blob://.")` raises `IsADirectoryError` out of a method documented to return `None`. `_blob_filename` (`agents/document/agent.py:95`) excludes `"."`/`".."` explicitly and its docstring says "Refusing also matches the two guards this one was found beside, on `BlobStore` and `DocStore`, so the family answers a bad name the same way" — that claim is false for `"."` for both of them.
IMPORTANCE: medium -- `fabric.blob.put`/`offload` are public SDK surface; no in-repo call site passes `"."` (checked all five `offload(`/`put(` call sites: `llm/cost_tracker.py:237`, `fabric/conversation_store.py:91`, `runs/store_db.py:113`, and the two docstring examples), so the trigger needs an external caller or a crafted stored `__blob__` ref. Fails loudly, but the blob-root case leaves the store permanently unwritable until someone deletes the stray file.

**[SYMMETRY]** `jazzx_sdk/pipelines/document_ingest.py:180` — the bare-name predicate here (`not part or part != Path(part).name or part in (".", "..")`) is a character-for-character re-derivation of `_blob_filename`'s in `jazzx_sdk/agents/document/agent.py:95`, and `document_ingest.py:48` already imports `unpack_zip` from that exact module. Exporting the one predicate and calling it is smaller than the copy, and it is what keeps the two from drifting the way the `"."` case above shows the `_local_path` pair already has.
IMPORTANCE: low -- two files, both in this diff; the copies agree today.

**[NOTE]** `tests/test_fabric_docs.py:181` — `pytest.raises((ValueError, KnowledgeFabricError))` accepts either error, so this test stays green if the guard slides back inside the `try` in `_get_local`, which is precisely the regression `test_a_refused_name_is_not_the_same_error_as_a_missing_document` was written to catch. `ValueError` alone passes today (verified by running the file).
IMPORTANCE: low -- test-only; the neighbouring test currently covers the distinction for the `local` branch, so nothing is unguarded, only redundantly asserted.

**[NOTE]** `jazzx_sdk/pipelines/document_ingest.py:181` — the guard now rejects a legal input the code previously accepted: `turn.file_path` ending in `"..zip"` gives `Path("..zip").stem == "."`, so a `.zip` with that name raises `ValueError` and fails the whole ingest run, where before it unpacked into `<base>/unpacked`. The docstring names this case and says it is refused deliberately, so this is the excluded-input note only, not a disagreement with the code.
IMPORTANCE: low -- one absurd filename; loud failure, no data effect.

VERDICT: BLOCK
