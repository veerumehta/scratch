# adversarial review -- japes

- when: 2026-09-09T01:59:56Z
- range: a095df2..HEAD (since the last review) (447ad5e)
- diff: 154 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Previous round

- WAS: `[SYMMETRY] blob/store.py:48` (DocStore has the identical join at `docs/store.py:502,757,1792`) — **FIXED** for the three sites it named; all now go through `_local_path` (`docs/store.py:521,777,1812`) and `tests/test_fabric_docs.py:161-186` fails against the old code (verified: old code returns `/etc/passwd` bytes, so `pytest.raises` fails). The fourth branch of the same dispatch is still open — see below.
- WAS: `[NOTE] blob/store.py:97` `get`'s docstring says "or None if absent" — **FIXED**; `:97-103` now states the local/Azure divergence and why.
- WAS: `[NOTE] tests/test_fabric_blob.py:151` `# so blobs/ resolves` — **FIXED**; the comment now says it is not a precondition.

## New review

**[DEFECT]** `jazzx_sdk/fabric/docs/store.py:743` — `_get_test_fixture` is the third branch of the same `get()` dispatch (`:710` LOCAL, `:715` TEST) and still does `fixtures_dir / identifier` with the same caller-supplied identifier, so `await fabric.docs.get("../../../etc/passwd")` under `retrieval_mode=TEST` reads that file and returns its bytes as `{"content": ..., "source": "test_fixture"}`; the sidecar read at `:754` follows it. `docs.get` is public API, and after this diff LOCAL refuses exactly this input while TEST serves it.
IMPORTANCE: medium — I enumerated the TEST-mode configurations: `FabricConfig.for_testing()` (`fabric/config.py:332`), `from_env` with `FABRIC_RETRIEVAL_MODE=test` (`:357`), and in the only real consumer, jaci, TEST mode appears in `tests/conftest.py:85`, `tests/unit/test_tools.py:25`, `tests/integration/test_workflow_integration.py:28` only — no production config reaches it, so the reachable callers are test harnesses and any library consumer calling `docs.get` directly under `for_testing()`.

**[SYMMETRY]** `jazzx_sdk/fabric/docs/store.py:521` (and `:777`, `:1812`) — the `_local_path` call sits *inside* each `try:`, so the `ValueError` is rewrapped by `except Exception as e: raise KnowledgeFabricError(f"Failed to retrieve document ...")` at `:783`/`:1830`/`:534`; a traversal is then the same exception type a missing document raises at `:778`, which is the distinction `BlobStore._local_path`'s docstring (`blob/store.py:66-67`, "Raised rather than reported as absence") introduced the guard to preserve, and which `blob/store.py:110` keeps by calling `_local_path` outside any handler. Lining up is three line moves: hoist `doc_path = self._local_path(name)` above the `try:`.
IMPORTANCE: medium — reaches every `fabric.docs.get/put/delete` caller under LOCAL/CACHED (`agents/interactive/reads.py:136`, `tools/documents/local_cache.py:143,155`); the reason string survives in the message, so a caller can still see *what* happened, just not branch on it.

**[NOTE]** `jazzx_sdk/fabric/docs/store.py:499-500` and `tests/test_fabric_docs.py:166-167` — both say the name "arrives from an agent tool call (`agents.interactive.reads`)", but `reads.py:129-134` rejects any `document_id` not in `{_doc_id(d) for d in await _list_docs()}` before calling `fabric.docs.get`, so that call site cannot deliver a traversal name; the other named provenance, `tools/documents/local_cache.py:143,155`, takes its `doc_id` from an on-disk `.japes` manifest (`read_manifest_entry`), not from a model. The guard is right; the stated exposure is not one of these two.
IMPORTANCE: low — comment-only; the risk is a later reader trusting `reads.py` as the untrusted-input proof and dropping the membership check.

**[NOTE]** `tests/test_fabric_docs.py:185` — `assert secret.read_text() == "SENSITIVE", "the file was read out"` cannot detect a read-out (reading does not change the file); it proves only that nothing clobbered or truncated the victim, and the actual read-out is caught by the `pytest.raises` above it.
IMPORTANCE: low — test-only; no shipped-code defect behind it, and the surviving `raises` covers the case.

VERDICT: BLOCK
