# adversarial review -- japes

- when: 2026-09-10T16:34:47Z
- range: origin/v2.5.1...HEAD (1604d7e)
- diff: 10644 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(threshold-grid-provenance)` (profiles.py) — holds exactly: `dscr_profile.yaml` has 7 `thresholds:` (all bare strings, so `looks_authored` is False for every one) + 150 `tables:` cells = the 157 the note claims, gate covers none.
- `TODO(layout-key-resolution)` (workbook.py) — holds: `_write_statement_sheet` writes `ws.cell(i, 1, binding.label)` at workbook.py:410 before the per-period loop `continue`s at :414, and unlike the adjacent provenance-`None` branch it appends no `_ExceptionEntry`.
- `TODO(layout-live-formula-order)` — holds: workbook.py:690 reads `inputs_by_statement.get(...)`, :694 populates it only from non-live sheets, in declaration order.
- `TODO(publish-tick-tie)` / `TODO(orphan-blob-gc)` / `TODO(activation-vs-active-rules)` — hold. PK is `(tenant_id, pack_id, version)` so the republish race is refused by the constraint; `executable_rules`/`activation_blocks` have no non-test callers.

## Findings

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:135` — a shipped seed pack hard-references the consumer repo: `experts.policy.class`/`experts.playbook.class`, `conductor.class`, and `conductor.pipeline` all name `jaci.*` modules, `evaluation.runner`/`gold_cases_path` are jaci-repo-relative paths, and `pb-ci-loanpkg-001.source_doc` is one machine's layout (`docs/LoanSamples/YETI/Yeti Credit Package/...`). japes declares no `jaci` dependency. Reproduced: `Pack.from_manifest('ci-spread-core', Path('plato/data/seed_packs')).conductor` → `ModuleNotFoundError: No module named 'jaci'`. The pack also ships its own `pipelines.yaml`, which `conductor.pipeline` bypasses in favour of the jaci symbol. Same pattern in `breakouts.yaml:4` (`jaci.capabilities...BreakoutRule`), `ontology/ci_lending.yaml` (`jaci.scenarios.ci_spread.schemas.*`), `chart_of_accounts.yaml` header (`dsl_catalog.py`, `analytics.py`, `template.py`).
IMPORTANCE: medium — `plato/seed.py:default_seed_root` publishes every directory under `plato/data/seed_packs`, so the `/packs` "initialize" route ships this to any tenant. I checked `plato/packs/loader.py` and `plato/packs_api.py`: neither touches `.conductor` or `.experts`, so nothing in plato resolves it today and it fails at the first consumer that does. The sibling seed pack `dscr_core` carries no `experts.class`, no `conductor.class` and no external module refs, so this is new to this pack, not a house convention.

**[SYMMETRY]** `jazzx_sdk/agents/adjudication/segment.py:108` and `jazzx_sdk/fabric/graph/check.py:379` — `gate_blocks` was hoisted into `condition_evaluator.py` precisely because "stating it inline got it into two of the three"; the RuleOutcome it gates (verdict `INDETERMINATE`, `status="INDETERMINATE"`, reason and inputs copied from `gate_outcome`) is still built inline in both, identically. A `blocked_gate_outcome(rule, gate_outcome)` beside `gate_blocks` is smaller than the two blocks and is where the next caller would look.
IMPORTANCE: low — three gate call sites total (`segment.py`, `check.py`, `experts/policy/default.py:444`, enumerated by grep on `rule.applicability`); all three are correct today.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/refusal.py:22-52` — `format_not_activated`/`parse_not_activated` write a sentence and read it back with an anchored regex, in the same module that already defines `Refusal` (`reason_code`, `reason_class`, `message`) as the SDK's typed "declined, as data not an exception" carrier — which `statemachine/engine.py:93` in this very diff returns for the identical `ThresholdNotActivated` condition. A `ComplianceResult.refusals: list[Refusal]` read directly by `policy_violations_to_findings` is less code than the prefix constant, the regex, the two functions and the `getattr` fallback.
IMPORTANCE: low — one producer (`DefaultPolicyExpert.check_compliance`), one consumer (`finance/validation.py:820`), both in this repo; grep found no other `ComplianceResult.warnings` reader.

**[NOTE]** `jazzx_sdk/fabric/canonical/profiles.py:71` — the `extra="forbid"` comment claims "a misspelled governance key is a refusal rather than a default", but the refusal depends on `looks_authored`, which keys only on `provenance`/`status`. `thresholds: {min_dscr: {value: "1.20", provenence: official_source}}` (the sole governance key misspelled) makes `looks_authored` False, so no coercion and no refusal — `get("min_dscr")` returns the raw dict to arithmetic. The comment's own example survives only because it also carries a correctly-spelled `status`.
IMPORTANCE: low — no shipped profile authors a mapping in `thresholds`/`custom` (checked `dscr_profile.yaml`, the only file in the repo with a `profile_id`), and the dict reaching `evaluate_ratio` fails loudly rather than silently.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:617` — with `is_active` now reading `effective_date`, a policy authored `status: active, effective_date: 2027-01-01` is reported through `detect_staleness` as stale with reason "is not effective until 2027-01-01". Not-yet-effective is the opposite of stale; the same policy also lands in `resolve()`'s `stale_policies` at default.py:321.
IMPORTANCE: low — reporting only; the enforcement side (skipping the policy) is the intended fix.

**[NOTE]** `jazzx_sdk/agents/adjudication/segment.py:96` — "Mirrors ``DefaultPolicyExpert.check_compliance`` exactly" is no longer true after this diff: `check_compliance` now runs its `fields_claimed` skip *before* the gate and routes a blocked gate into `warnings` (withholding `allowed`), while this function has no field precedence and emits an `INDETERMINATE` outcome into a list still named `not_applicable`.
IMPORTANCE: low — comment and local variable name only.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
All green: `pytest tests/test_plato_packs_api.py tests/test_pack_store.py tests/test_plato_packs.py tests/test_plato_info_api.py tests/test_plato_boot_contract.py` → 195 passed, 2 skipped; ruff clean on the five changed Python files. No defects found.

## CHECKED

- `plato/posture.py:96` TODO(if-match-post) — claim "nothing mounts this dependency" holds: `require_if_match` appears only in `tests/test_plato_preconditions.py`. The new `POST /packs/initialize` does not take it.
- `plato/info_api.py:83` TODO(omit-info) — holds: `_info_only_app`'s `degraded_nav = ("Guide", "config", "database", "Packs", "packs")` (`plato/__main__.py:300`) contains no `info`, and `dashboard_html(omit=…)` has exactly that one caller.
- `plato/seed.py` TODO(seed-pack-vendoring) — holds. `Pack.from_manifest` resolves both seeded packs' policy and profile corpora (`policy_files()` → `conventions.yaml`+`core.yaml` for ci-spread-core, `eligibility.yaml`+`dscr_profile.yaml` for dscr-core); `policies/overlays/deal_si_rb_example.yaml` correctly stays out because `_declared_files` globs one level, and `checklist_ci.yaml` is skipped by `is_policy_document`. One gap the TODO does not cover: see [NOTE] 3.
- `DSCR-HIGH-LTV-PROPERTY-TYPE` / `DSCR-PROPERTY-TYPE-CLTV` Florida carve-outs — both rules disclose the unenforced qualifier in their own `description`, and ENCODING_NOTES §3 G1 names them as the two that remain unauthored. The deferral is accurate; not raised.

## Findings

**[SYMMETRY]** `plato/packs_api.py:172` — the withheld response keeps `available=` but drops `packs`, where all three siblings keep their collection key empty: `logs_api.py:110` (`records=[]`), `metrics_api.py:133` (`routes=[]`), `database_api.py:166` (`connected=False`). A JSON caller reading `response["packs"]` gets a `KeyError` on this route and an empty list on the other three; `packs.html:319` survives only via `data.packs || []`. The consistent version is one kwarg, `packs=[]`.
IMPORTANCE: low -- no shipped wiring reaches the branch: `wiring_default.py:533` sets `require_identity=strict or identity_requested()`, so a strict posture always passes `identity_required=True` and `withholding` returns False, and `wiring_local` is non-strict. Unlike logs/metrics, `packs` is not mounted on the degraded app (`plato/__main__.py:300` omits it), so there is no unauthenticated path either. Nothing outside this repo consumes the route.

**[SYMMETRY]** `plato/seed.py:113` — `"assistant_id" in manifest` re-spells the discriminator that `jazzx_sdk.pack.store.is_assistant_pack` exists to own, and whose docstring says exactly why: "two modules spelling the discriminator independently is how they come to disagree about what a pack is." `seed.py` already imports `MANIFEST_NAMES` from that module for that reason. `is_assistant_pack` takes a `PackVersionRecord`, so lining up means a sibling `is_assistant_manifest(dict)` next to it — one line, and no more code than the inline check.
IMPORTANCE: low -- one call site, and no behavioural difference today; both seeded packs are domain packs. Reachable only by putting an assistant pack in a seed root, which no shipped `PLATO_SEED_PACKS_DIR` default does.

**[NOTE]** `plato/packs_api.py:159` — `if hasattr(found, "__await__"): found = await found` has no predicate behind it. Both shipped factories are sync (`wiring_default.py:471`, `wiring_local.py:385`), the tests pass `lambda t: …`, and the docstring one line above says the shape is `session_store_for`'s — which `assistants_api.py:190` calls synchronously (`await session_store_for(tenant_id).touch(...)`). `tenancy.py:104` has the same branch for `tenant_from_request`, so it may be a house pattern rather than an oversight.
IMPORTANCE: low -- dead branch, no wrong behaviour; cost of hitting it is nothing.

**[NOTE]** `plato/packs_api.py:118` — the factory docstring enumerates `GET {prefix}/packs`, its page, and the `DELETE`, and omits `POST {prefix}/packs/initialize`, which the same factory defines and which the module docstring's "delete is gated harder" sentence also does not mention.
IMPORTANCE: low -- documentation only; the route is registered and tested.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:246` — §5 tells the reader the pack is at `config/packs/dscr_core/` and to run `cd jaci && pytest tests/scenarios/dscr/`; neither exists from this location, and `pack_manifest.yaml:44`'s `gold_cases_path: tests/eval/gold_cases/dscr` is the same kind of consumer-relative path. `default_seed_root`'s TODO admits the vendoring only for *Python module* references, not for paths and run instructions that are false in the image that ships them.
IMPORTANCE: low -- shipped data read by a human, not resolved by code; `Pack.from_manifest` does not touch `evaluation.gold_cases_path` (tests/test_plato_packs_api.py:469,489 pass without it).

VERDICT: PASS

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
The diff I was handed is one iteration behind the branch: `tests/test_plato_packs_api.py` at HEAD already replaced the `pack_manifest.yaml` pin and added `test_a_bundled_seed_pack_is_loadable_in_a_japes_only_process`. `plato/static/packs.html` (468 lines), `tenancy.py`, `wiring.py`, `wiring_default.py`, `wiring_local.py` match HEAD exactly, so findings on those apply as written.

**[DEFECT]** `tests/test_plato_packs_api.py` (diff line ~"test_every_bundled_seed_pack_loads") / `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml` — the docstring's reasoning that the manifest's `experts.*.class`, `conductor.class`/`conductor.pipeline` and `modes.evaluator.class` pointers "still fail when something resolves *them*, not on load" is wrong for `conductor`: `jazzx_sdk/pack/pack.py:338-346` is a `cached_property` that calls `_resolve(ref)` on plain attribute access, so `Pack.from_manifest("ci-spread-core", default_seed_root()).conductor` raises `ModuleNotFoundError: No module named 'jaci'` (confirmed: `jaci` is not importable in `.venv`) for a pack the new `/packs/initialize` route publishes into any tenant. The diff's load test only touches `policy_registry`/`policy_profile()`, which is why it stayed green.
Consequence right now: HEAD's committed manifest still carries 5 `jaci.` references (`git show HEAD:plato/data/seed_packs/ci-spread-core/pack_manifest.yaml | grep -c 'jaci\.'` → 5) while HEAD's committed pin test expects `ci-spread-core` to carry them in 5 files *excluding* `pack_manifest.yaml` — so the committed branch fails both that pin and `test_a_bundled_seed_pack_is_loadable_in_a_japes_only_process[ci-spread-core]`. The only thing making them pass locally is the **uncommitted** working-tree edit to that manifest. Commit it before pushing.
IMPORTANCE: medium -- fails loudly, not silently. Reached by: `plato/packs_api.py` `initialize` (relaxed postures only) publishing the bundled snapshot, and any process that then loads a published `ci-spread-core` and reads `conductor`/`conductor_pipeline`. Nothing outside this repo consumes `plato/data/seed_packs`; the push-blocking part is the two red tests in the committed tree.

**CHECKED:** `TODO(azure-blob-container-singleton)` (`plato/wiring_default.py:645`) — the claim holds verbatim. `AsyncAzureBlobStorageProvider.get_instance` returns `cls._instance` before it looks at `container_name` (`common/core/storage/async_azure_provider.py:54`), and `jazzx_sdk/fabric/config.py:228-233` does build a second azure `BlobStore` from `JAPES_BLOB_*`, so "not unreachable" is accurate and the deferral is honest.

**[NOTE]** `tests/test_plato_packs_api.py`, `test_non_durable_pack_archives_do_not_make_a_replica_unready` — the guard filters `missing_config("staging")` notes on the substring `"Pack archive"`, which no note in `plato/wiring_default.py` has ever contained; the module's own wording for this condition is lowercase (`"plato: pack archives are going to local storage"`, line 631). A re-added readiness note copied from that warning passes the guard, so it does not hold the regression it names. A `TODO` on the assertion is a complete response.
IMPORTANCE: low -- test-only; the shipped defect it guards (staging 503 on the container probe) is fixed in this diff.

**[NOTE]** `plato/tenancy.py:106` — `raise HTTPException(400, str(exc))` serves a consumer resolver's exception text verbatim to an unauthenticated caller, and is the one served-text path in plato that does not go through `jazzx_sdk.failures.reason`/`redact_secrets` (both imported in `wiring_default.py:57`). Both shipped resolvers' messages are benign; a `KeyError`-shaped resolver yields the detail `'X-Tenant-Id'`, a bare quoted key rather than the sentence the docstring promises.
IMPORTANCE: low -- the two shipped resolvers (`wiring_default.py:502`, `wiring_local.py:391`) are safe; I could not establish what a consumer-supplied `tenant_from_request` puts in its message, and `PlatoWiring.tenant_from_request` is a documented extension point.

**[NOTE]** `plato/static/packs.html:250` — `v.depends_on.length` is the only version field read without a null guard, while its neighbours on lines 242 and 246 guard `published_at` and `content_digest`. `_version_row` (`plato/packs_api.py`) always emits a list, so there is no reachable trigger from plato's own route.
IMPORTANCE: low -- one page, one data source, which always supplies the key.

VERDICT: BLOCK
