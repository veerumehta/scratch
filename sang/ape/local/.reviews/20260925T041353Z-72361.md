# adversarial review -- .

- when: 2026-09-25T04:13:53Z
- range: 462510d..HEAD (since the last review) (45b53c0)
- diff: 291 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/fabric/canonical/condition_evaluator.py :: bad-source-skips-not-withholds -- FIXED
WAS: jazzx_sdk/pipelines/policy_code.py :: rule-segment-mangles-citations -- FIXED
WAS: jazzx_sdk/fabric/canonical/condition_evaluator.py :: optional-read-never-runs -- FIXED
WAS: jazzx_sdk/pack/lint.py :: program-check-needs-policies -- FIXED

CHECKED: jazzx_sdk/pack/lint.py :: program-check-needs-policies. The TODO says the check is skipped like every other policy check here. That holds: with no policies, `_unknown_evidence_types`, `_dangling_relations` and `_python_findings` all return nothing too, so `_program_findings` is not the only check left out.

[SYMMETRY] jazzx_sdk/pipelines/policy_code.py:84 :: marker-slug-diverges-from-sibling -- `extract` rewrites the marker into a slug before building the rule id. Its sibling `ModalClauseExtractor.extract` (policy_extract.py:259) uses `marker or ordinal` as it is. The slug turns distinct markers into the same id, and the proposal step then drops one of them.
Trigger: one document has clauses marked `3.1` and `3-1`, or `3(a)` and `3.a`. Both become `extracted.python.3_1` (or `..._3_a`). The proposal step (policy_extract.py:530-535) keys candidates by `rule_id` and keeps only the first candidate's generated source, so the second clause's source is lost with no warning. Using `segment.marker or segment.ordinal` as the sibling does is less code.
IMPORTANCE: low -- `PythonExtractor` has no caller in `jazzx_sdk` other than its own module and tests, and nothing outside this repo uses it yet. To hit this, one corpus would need two markers that slug to the same string.

VERDICT: PASS
