# adversarial review -- japes

- when: 2026-09-09T02:19:06Z
- range: origin/dev...HEAD (1953959)
- diff: 382 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(jwks-error-detail)` at `plato/oidc.py:300` holds — nothing in this repo constructs `TokenVerifier` outside `tests/test_plato_oidc.py`, and `grep` for `TokenVerifier`/`plato.oidc` in `/Users/sangit/src/jaci` and `/Users/sangit/src/juno` returns nothing, so the leak depends on middleware not yet written.

**[DEFECT] plato/oidc.py:312** — the no-usable-keys branch builds `_KeyCache(...)` without `keys=self._cache.keys`, so it discards a warm key set that the sibling failure handler at line 289 deliberately keeps, and every valid token is rejected for the whole 30s window; the `_KeyCache.error` docstring at line 155 and `_or_last_error`'s at line 240 both promise "keys from before the failure still answer", which this branch does not honour. Trigger, measured: verify a `k1` token against a healthy issuer, advance past `MIN_REFRESH_INTERVAL_SECONDS`, verify an unknown kid so the forced refetch sees `{"keys": []}` (the rotation gap the comment on line 308 names), then verify the same valid `k1` token with the issuer back up → `TokenError: the issuer published no usable signing keys`. This is exactly what `test_a_blip_during_a_refetch_does_not_reject_valid_tokens` guards for the raising path, and no test covers the empty-document path.
IMPORTANCE: medium -- enumerated every `TokenVerifier` construction in the repo (tests only) and found no reference in jaci or juno, so nothing shipped reaches it today; once the identity middleware mounts it, this is a 30-second 401 storm on valid tokens whenever an issuer serves an empty JWKS mid-rotation, and it fails loudly.

**[NOTE] plato/oidc.py:314** — `return keys` (empty) instead of raising means the *first* verify that hits this branch on a warm-but-forced refetch still reports the misdiagnosis the branch exists to remove: measured in the trace above, that verify raised `token signed by unknown key 'unknown-kid'`, not `no usable signing keys`; only verifies later in the window get the right message.
IMPORTANCE: low -- same unmounted code path as above; cost is one confusing message per window.

**[NOTE] tests/test_plato_oidc.py:593** — `now=lambda: 2000.0` is a constant clock, so this test cannot distinguish a working cooldown from a cache stamped once and never retried; `test_a_down_issuer_is_not_refetched_on_every_verify` advances its clock for precisely that reason, and the malformed-document path is the one that stamps `fetched` from inside the parse guard.
IMPORTANCE: low -- test-only; the shipped stamp it covers is the same line the other test already exercises with an advancing clock.

**[NOTE] tests/test_plato_oidc.py:501** — the comment states the test passes with `verify`'s outer `except Exception` deleted, so that backstop (line 336) has no coverage; a fetcher raising from `_load_keys` rather than inside `_fetch_and_cache`'s try is untested.
IMPORTANCE: low -- reaching it requires a failure outside `_fetch_and_cache`'s guard, which no current code path produces.

VERDICT: BLOCK
