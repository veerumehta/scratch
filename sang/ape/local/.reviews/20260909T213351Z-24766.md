# adversarial review -- japes

- when: 2026-09-09T21:33:51Z
- range: HEAD~1...HEAD (d4154f3)
- diff: 3189 lines


## whole

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No defect I can point at with a reachable trigger. Two symmetry findings and three notes.

## CHECKED

- `TODO(publish-tick-tie)` (`jazzx_sdk/pack/store_db.py:47`) — holds. `grep '\.publish('` across `plato/` and `jazzx_sdk/` finds no non-test caller, and two awaited `publish` calls cannot share one microsecond of `datetime.now(timezone.utc)`.
- `TODO(if-match-post)` (`plato/posture.py:97`) — holds. `require_if_match` appears only in its own `__all__` and `tests/test_plato_preconditions.py`; no route mounts it.
- `TODO(partial-registration-marker)` (`plato/database_api.py:78`) — holds. The only in-repo callers are `plato/database_api.py:163,166`, both passing the `DatabaseHandle`; the duck-typed-store tests fail on the *first* store constructor, so nothing partially registers.
- `TODO(swap-schema-placement)` (`plato/wiring_default.py:527`) — holds, and is worse than a no-op only after an alembic run, as stated. Confirmed nothing in the repo sets `search_path`.
- `TODO(migration-concurrent-index)` (`plato/migrations/env.py:182`) — holds. The offline branch at `plato/migrations/env.py:86` also wraps in `begin_transaction`, and no revision in `plato/migrations/versions/` uses `CREATE INDEX CONCURRENTLY`.
- `TODO(jwks-error-detail)` (`plato/oidc.py:329`) — holds, and covers the two sites this diff converted to `reason(exc)`: `grep TokenError` finds no consumer outside `plato/oidc.py`, so none of the four is served, which is why leaving them uncapped is not the `console_api` gap.

## Findings

**[SYMMETRY]** `jazzx_sdk/pack/store_db.py:372` — `MANIFEST_NAMES` is a pack-*root* filename constant ("what a pack root is recognised by") living in the SQLAlchemy-backed store module, so `plato/packs/domain.py:59` imports `store_db` at call time purely to read a two-string tuple, pulling sqlalchemy into a module whose only declared dependency is the abstract `PackVersionStore`. It lines up with `jazzx_sdk/pack/store.py` — the dependency-free abstract half that `store_db` already imports and that `domain.py` already names under `TYPE_CHECKING` — or `jazzx_sdk/pack/roots.py`, whose whole subject is finding a pack's directory. The move is one line and deletes an import edge rather than adding one.
IMPORTANCE: low -- enumerated every reader: `store_db._unpack` (lines 406, 409, 413) and `plato/packs/domain.py:59,81,85`. Both symbols are new in this diff, so nothing outside this repo imports either.

**[SYMMETRY]** `tests/test_evaluation_config_paths.py:29` — three separate ad-hoc bad-id lists (`:29`, `:38`, `:45`) restate what `tests/test_pack_roots.py:24` `_BAD_PACK_IDS` already owns and that `tests/test_modes_framework.py:208` already imports, under a comment saying exactly why ("two drifted into a subset the moment they were written apart"). They have already drifted: `"."` is absent from all three, and `test_pack_roots.py:167` calls it out as one of the two ids with a distinct mechanism. Importing the one name and deleting the literals is less code than what is there.
IMPORTANCE: low -- test-only; `bare_name` refuses `"."` today (verified against `jazzx_sdk/path_safety.py:70`), so this is coverage drift, not a live gap.

**[NOTE]** `jazzx_sdk/modes/prompt_loader.py:56` — the `Raises:` block names only `pack_id`, but `mode_name` is `bare_name`-validated first and unconditionally (`:67`), as is `compose_mode_prompt`'s (`:136`, whose one-line raises sentence also names only `pack_id`). The reason given for adding `ValueError` to this block is that `jazzx_sdk/modes/base.py:180` quotes it as the resolver contract, and the contract as quoted still omits the argument that is checked first. Trigger: `resolve_mode_prompt("a/b")` with no `pack_id` at all.
IMPORTANCE: low -- in-repo callers pass `self.mode_name` from `ModeContract`, all 13 bare (`tests/test_modes_framework.py:202`); a wrong-shaped mode name fails loudly with the right message.

**[NOTE]** `plato/packs/loader.py:238` — the two skips above it exist so one bad row cannot make a tenant unservable, but a published *assistant* pack whose archive lacks `profile/profile.yaml` reaches `tenant_registries` → `load_pack`, which raises `PackLoadError` out of `published_registries` and composes nothing for the tenant. `publish` accepts that row (the docstring says the gate "is not built"). `plato/packs/domain.py:74` has the same shape: `store.materialize` (digest mismatch) and `loader.depends_on()` (`PackDependencyError` on an unknown `fragments:` value) both propagate out of the per-record loop.
IMPORTANCE: low -- no shipped caller: `grep` finds `published_registries`/`published_domain_packs` only in `tests/test_pack_store.py`, and `domain_pack_summary`'s stated `/info` consumer is not mounted. Both are in `__all__`, so an external consumer could reach it.

**[NOTE]** `jazzx_sdk/pack/store_db.py:335` — the cache short-circuit returns an unpacked tree without re-hashing it, from `tempfile.gettempdir()/japes-packs/<digest[:16]>` (`_cache_dir`, `:358`), and what that tree contains is served as a pack's policies. The comment records the deferral but carries no `TODO(...)`, unlike the five others in this diff, so it will not surface in a grep for parked work. Trigger: any local process that can write `/tmp/japes-packs/<digest16>/policies/*.yaml` between two `materialize` calls.
IMPORTANCE: low -- requires write access to the box's shared temp dir; no shipped code constructs a `DbPackVersionStore` with a blob store to materialize from (`plato/models.py:49` constructs it for metadata only, with `blob=None`).

VERDICT: PASS
