# adversarial review -- japes

- when: 2026-09-24T05:12:26Z
- range: f6aa182..HEAD (since the last review) (6ff5aa3)
- diff: 47 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Both findings from the last round are closed, and this diff adds no new defects. `tests/test_backend_names.py` passes (3 tests).

WAS: jazzx_sdk/backend_names.py :: posture-refusal-implied-for-backends -- FIXED; the false claim that posture handles backend refusals is gone. The gap is now covered by a TODO (see CHECKED below).

CHECKED: `TODO(posture-refusal-implied-for-backends)` holds. A search for "backend" in `jazzx_sdk/config/posture.py` finds nothing, so it does refuse only on tier and identity, as the TODO says. The call sites that warn are exactly five: `blob/store.py:42`, `db/store.py:174`, `fabric.py:169`, `fabric.py:217` and `run_tracer.py:178`. It is rated medium, not high, and deferring it is the author's call.

WAS: tests/test_backend_names.py :: silence-test-covers-two-of-five -- FIXED; `resolve_tracer()` and a default `KnowledgeFabric` now run the tracer, guidance and conversation checks through their defaults (`fabric.py:169`, `fabric.py:217`, `run_tracer.py:178`).

[NOTE] tests/test_backend_names.py:52 :: silence-covers-defaults-only -- The docstring says a name dropped from `CONVERSATION_BACKENDS` or `GUIDANCE_BACKENDS` would be caught, but the test only runs each set's default name, so dropping a non-default name would go unnoticed.
Trigger: removing `"sql"` from `CONVERSATION_BACKENDS` or `"eval_service"` from `GUIDANCE_BACKENDS` makes the configured deployments warn, and this test still passes. The docstring's "warns on every boot" is only true for the default name.
IMPORTANCE: low -- test-only; the worst case is a spurious warning line in deployments that set a non-default backend. A `TODO` is enough.

VERDICT: PASS
