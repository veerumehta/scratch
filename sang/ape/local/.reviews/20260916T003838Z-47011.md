# adversarial review -- japes

- when: 2026-09-16T00:38:38Z
- range: c4002e5..HEAD (since the last review) (217591a)
- diff: 815 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_plato_info_api.py :: html-family-omits-redoc -- FIXED
WAS: tests/test_plato_info_api.py :: prefix-const-check-hits-one-page -- FIXED
WAS: tests/test_plato_info_api.py :: info-page-fetch-unasserted -- FIXED

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped

CHECKED: `upgrade_to_head`'s "Concurrency is `migrations/env.py`'s advisory lock" holds -- `_take_migration_lock` (env.py:118) takes a session-scoped `pg_advisory_lock` on the non-sqlite path and `_run` skips it only for sqlite, which has one writer.

[DEFECT] plato/boot/schema_version.py:152 :: upgrade-ignores-the-db-it-is-given -- `upgrade_to_head(db)` uses `db` only for the read-back; the migration itself runs against the store `migrations/env.py:55` builds from `PLATO_DB_BACKEND`/`PLATO_SQLITE_PATH`, so the database that gets migrated and the database that gets reported can be two different databases.
`PLATO_DB_BACKEND=sqlite` with `PLATO_SQLITE_PATH` unset: `plato/wiring/default.py:177` builds the serving store as `:memory:`, env.py builds its own `:memory:`, and the run lands in the throwaway. Also reachable with `PLATO_DB_BACKEND` unset in a local posture, where wiring resolves `sqlite` (default.py:167) and env.py defaults to `common`. The docstring's "the credentials the replica connects with" describes `db`, which is not what it connects with.
IMPORTANCE: high -- two call sites, both enumerated: `plato/jobs.py:_migrate` (reached via `__main__.py:421` for `JAPES_RUN_MODE=job:migrate`) and `plato/api/database.py:migrate`, mounted unconditionally at `plato/app.py:222`. Reproduced: the four revisions applied to a throwaway in-memory database and the job exited 0.

[DEFECT] plato/jobs.py:90 :: migrate-success-not-checked-against-head -- neither caller compares the revision reached to `head_revision()`, so `before == after == None` -- the database still unmigrated -- is reported as "schema already at None; nothing to apply" with exit 0, and as HTTP 200 `{"applied": false, "after": null}` at `plato/api/database.py:192`.
Same trigger as above; observed output was `INFO:plato.jobs:schema already at None; nothing to apply` / `exit 0` after the migrations had run elsewhere. `None` from `current_revision` means "never migrated", which is the one value that cannot be a successful outcome of an upgrade. `upgrade_to_head`'s own docstring claims "a migration that half-applied must not report success"; the check that would enforce it is in neither caller. `migrations/env.py:58` already names this exact failure ("reports success and leaves nothing behind, which looks exactly like a run that worked").
IMPORTANCE: high -- an operator running `job:migrate` to clear the exit-5 refusal sees a zero exit and a "nothing to apply" log while the schema is untouched; the next `job:*` still exits 5 and the serving replica still 503s chat. Same two call sites as above; nothing outside this repo calls `upgrade_to_head`.

[DEFECT] plato/api/database.py:176 :: route-exists-in-every-posture -- "the tier gate is what keeps the route from existing in staging or production at all" is false: the route is registered unconditionally and `relaxed_only` only raises 403 when called.
With `ENVIRONMENT=production`, `POST /api/v1/database/migrate` is present in `app.openapi()` (verified: the path is in `paths`, and this very sentence is rendered into its published `description`). The sibling in the same family, `plato/api/packs.py:167`, claims only "Relaxed posture only", which is what the code does.
IMPORTANCE: medium -- one route, mounted in every posture from `plato/app.py:222`; the behaviour is a loud 403, so the cost is an operator reading the OpenAPI description and believing a destructive endpoint is absent from production when it is listed there.

[SYMMETRY] plato/api/database.py:171 :: migrate-logs-no-actor -- the sibling relaxed-only irreversible write, `delete_pack_version` at `plato/api/packs.py:196`, logs `acting_user_id() or "an unattributed caller"`; the migrate route records nobody, and `logger` is already bound at `plato/api/database.py:16`.
The generalization is the one `logger.warning` line packs.py already writes.

[SYMMETRY] plato/api/database.py:172 :: stray-auth-query-param -- `_: Any = Depends(auth) if auth else None` is not the shape the rest of the family uses: `database` in the same file and `initialize`/`delete_pack_version` in `plato/api/packs.py:169,181` rely on the router-level `dependencies=[Depends(auth)]` and declare no auth parameter. With `auth=None` the default turns it into a published query parameter (verified in `app.openapi()`: `{"name": "_", "in": "query", "required": false}`). Dropping the parameter is strictly less code.

[NOTE] plato/__main__.py:136 :: exemption-still-hypothetical -- the docstring the new comment at line 413 cites still states the exemption in the future tense ("A role added later whose whole purpose is to *fix* the schema is the one thing that would need exempting") and still says a `job:*` role refuses, while `job:migrate` now exists and is exempted three lines below the sentence that says it refuses.
IMPORTANCE: low -- one docstring on one function with one caller (`_run_resolved`); the behaviour is correct and the contract row now carries the exemption.

[NOTE] plato/migrations/env.py:32 :: dashboard-button-does-not-exist -- "the dashboard's migrate button" (and `tests/test_plato_schema_version.py:215,242`, "the dashboard button", "a replica that migrated from the dashboard") names UI that is not in this repo: grep over `plato/static/` and `plato/api/info.py` finds no reference to `migrate`, and nothing calls `POST /database/migrate`.
IMPORTANCE: low -- the route exists, so the substance of the logging fix stands; only the caller named in the comment does not.

[NOTE] plato/jobs.py:97 :: revision-wrapper-adds-nothing -- `_revision_or_none` is a one-line pass-through to `current_revision`, whose own docstring already states it returns `None` for a database that has never been migrated; `plato/api/database.py:189` calls `current_revision` directly for the identical read. (`MIGRATE_ROLE` is also absent from `plato/jobs.py:71`'s `__all__` while `SESSION_REAPER_ROLE` is in it.)
IMPORTANCE: low -- module-private, one call site.

VERDICT: BLOCK
