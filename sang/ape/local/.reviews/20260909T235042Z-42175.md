# adversarial review -- japes

- when: 2026-09-09T23:50:42Z
- range: HEAD~1...HEAD (eef13ec)
- diff: 4451 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
No defects found. Verified by running the commit's own suites (341 passed, 3 skipped across `test_pack_store`, `test_pack_roots`, `test_pack_policy_profile`, `test_evaluation_config_paths`, `test_plato_*`, `test_events`, `test_import_boundary`) and `ruff` clean on every changed file.

**CHECKED** — every deferral in the diff holds:
- `TODO(publish-tick-tie)`, `TODO(share-published-row-loop)`, `TODO(compose-per-pack-guard)`: `PackVersionStore.publish`, `published_registries`, `published_domain_packs` and `materialize` have no shipped caller — grep for each in `jazzx_sdk/` and `plato/` returns only their own definitions and docstrings. The shared-tmp cache-tree trust `materialize` records is therefore unreachable in any shipped configuration.
- `TODO(if-match-post)`: `require_if_match` appears only in `plato/posture.py`'s `__all__` and in `tests/test_plato_preconditions.py` / `tests/test_agent_config_api.py`. No route mounts it.
- `TODO(swap-schema-placement)`: `create_all()` is called from exactly one place (`plato/wiring_default.py:543`), on the fresh replacement store. The reporting-side `register_all(store)` added to `plato/database_api.py:81` is therefore inert — nothing else iterates the live store's metadata and acts on it.
- `wiring_local._find_root`'s "nothing above `/app/plato/` carries a `pyproject.toml`": confirmed — `Dockerfile:46` copies `pyproject.toml` into the *builder* stage at `/build`, and the runtime stage (`WORKDIR /app`) copies only `common/`, `jazzx_sdk/`, `plato/`, `jazzx_eval_contracts/`. Discovery returns `None` and the cwd fallback yields `/app`, matching the old `parents[1]` answer.

Family enumeration also checks out: all 11 asset joins in `manifest_loader.py` now route through `_asset_path`, `pack/loader.py`'s two joins are guarded, and the remaining `self._loader.root / default_ref` at `pack/pack.py:242` is reached only after `profile_files()` has already validated that same ref. `tools/documents/local.py:286` and `split.py:551` are the other `dest_dir / name` writes in the repo; both names are already reduced to a single component before the join.

---

[NOTE] `jazzx_sdk/failures.py:255` — `SERVED_REASON_LIMIT`'s docstring says the value "had become a bare `200` at nineteen call sites"; on `HEAD~1` there were four (`plato/database_api.py:65,87`, `plato/info_api.py:310,320`), and line 261's "Fifteen of those nineteen were passing no ``limit`` at all" contradicts line 255, since a site passing no limit was never a bare `200`. Actual counts: 4 pre-existing bare-`200` plus ~19 newly bounded, 23 usages now. `reason()`'s own docstring nine lines below states that a call-site survey "does not belong in a docstring, because it is wrong as soon as a caller is added".
IMPORTANCE: low -- comment only; measured by `git grep -E 'limit=200|\[:200\]' HEAD~1` over `jazzx_sdk/` and `plato/` against the 26 current `SERVED_REASON_LIMIT` hits. No behaviour depends on it.

[NOTE] `jazzx_sdk/pack/store.py:61` — `content_ref: None` is documented as "legal: a pack whose assets are all declared inline has nothing to archive", but the only domain-pack reader (`plato/packs/domain.py:77`) treats exactly that row as "a publish-time mistake" and drops it, while `published_registries` justifies *its* skip by asserting manifest-only domain packs are legitimate. Trigger: `publish({"pack_id": "x", "pack_version": "0.1.0", "evidence_types": [...]})` with no `archive` — accepted by `publish`, then invisible to `published_domain_packs`. `Pack` needs a filesystem root, so skipping is the only option available; it is the "legal" claim that has no consumer, not the skip.
IMPORTANCE: low -- both readers are test-only (verified above), so nothing shipped can reach the row. Two sibling docstrings in one commit disagree about whether the input is supported, which is the part worth reconciling before a publish route exists.

[NOTE] `jazzx_sdk/modes/prompt_loader.py:134` — the `Raises` line promises `ValueError` "if ``mode_name`` or ``pack_id`` is not a bare name", but `pack_id` is only checked inside `if pack_id and packs_root:`, so with `packs_root=None` an escaping id is silently ignored. Measured: `compose_mode_prompt('governor', pack_id='../../etc', packs_root=None, base_dir='/tmp/nt3/base')` returns `'the platform base'` with no raise. No traversal results (the join never happens), so this is the doc over-reaching, not a hole — `resolve_mode_prompt`'s sibling clause is accurate because `pack_root` defaults to `Path.cwd()` and is always set.
IMPORTANCE: low -- one exported function, two in-repo callers (`prompt_loader.py:62` docstring example, `:156` which always passes `packs_root`). Out-of-repo consumers reading the contract would expect a raise they will not get; the failure mode is a missing exception, not a read outside the pack.

VERDICT: PASS

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
All 216 tests in the touched files pass; ruff clean. No `[DEFECT]` found.

**CHECKED:** `TODO(publish-tick-tie)` in `jazzx_sdk/pack/store_db.py:63` — the claim ("left because no caller invokes `publish` yet") holds. I enumerated every non-test construction site of `DbPackVersionStore` (`plato/models.py:49`, inside `register_all`, discarded) and every caller of the store (`published_registries`, `published_domain_packs`) — all read-only. The new tests do depend on `published_at` ordering across sequential publishes, but each publish is separated by a blob write plus a DB round trip, so a same-microsecond tie is not reachable here.

---

**[SYMMETRY]** `tests/test_pack_store.py:448` — third verbatim copy of the `REAL_PACKS = Path(os.environ["JAPES_REAL_PACKS_DIR"]).expanduser() if ... else None` constant plus its skip reason string; the siblings are `tests/test_pack_policy_files.py:182` and `tests/test_pack_policy_profile.py:32`. There is no `tests/conftest.py` entry for it, and a single fixture there is smaller than the three copies. Also note the two older copies gate with `@pytest.mark.skipif`, so their skip shows the directory requirement at collection time; the new one gates with an in-body `pytest.skip`, so the same condition is spelled two ways in one suite.
IMPORTANCE: low -- test-only; three test modules in this repo, nothing outside it reads the constant.

**[SYMMETRY]** `tests/test_pack_store.py:395` (`test_an_assistant_pack_with_no_archive_is_reported_and_the_rest_compose`) — the diff mirrors the *unreadable-archive* guard onto both readers (`...is_reported_and_the_rest_compose` for `plato/packs/loader.py` and `...domain_archive...` for `plato/packs/domain.py`), but the *archive-less* guard is covered on the assistant reader only. `plato/packs/domain.py:88-93` has the same `record.content_ref is None` → warn-and-skip branch with its own distinct log line, and no test reaches it — publishing a manifest-only domain pack and asserting the `"was published with no archive"` warning is the same size as the assistant test already written.
IMPORTANCE: low -- test-only; the guarded branch is shipped and reachable (`publish(MANIFEST)` with no archive), but it fails safe by construction.

---

**[NOTE]** `tests/test_pack_store.py:47` and `:481` — the domain half's rules are never loaded in a test that runs. `_archive()`'s policy file (`policies:\n  - id: ...\n    statement: ...`) satisfies `is_policy_document` but not `Policy.model_validate`: loading it raises a 7-field `ValidationError` (measured — missing `policy_id`, `version`, `name`, `description`, `policy_type`, `effective_date`, `pack_id`). Every executed domain test (`test_the_two_pack_halves_do_not_serve_each_other`, both `..._domain_archive...`, `..._dependency_block...`) only reads `pack_id`, so it passes against a `Pack` whose `policy_registry` raises on first access. The one test that would notice is `test_a_published_domain_pack_becomes_a_usable_pack`, which is env-gated and skipped. Consequence: `domain_pack_summary` has zero executed coverage in the suite. I confirmed there is no shipped defect behind this — with a schema-valid policy the path returns `has_policy_registry: True, policies: 1`.
IMPORTANCE: low -- test-only; `plato/packs/domain.py` has no non-test caller, so nothing shipped is affected today.

**[NOTE]** `tests/test_pack_store.py:203` — `assert (second / "extracted-marker").is_file(), "the tree was unpacked a second time"` cannot fail for the stated reason: `ZipFile.extractall` writes only its own members and removes nothing, so the hand-placed marker survives a re-unpack. The real check is the `st_mtime_ns` assertion on the line below (load-bearing: CPython's `_extract_member` does not restore mtime).
IMPORTANCE: low -- one assertion in one test; deleting the guard it claims to protect would still leave the test red via the mtime line.

**[NOTE]** `tests/test_pack_store.py:225` — the comment justifies the shared cache root as "what production has: the single construction site passes no `cache_root`". The only `DbPackVersionStore(...)` outside tests is `plato/models.py:49`, constructed inside `register_all` purely to register metadata and immediately discarded; it never calls `materialize`. There is no production wiring that constructs a serving pack store, so the sentence describes a configuration that does not exist yet rather than one that does.
IMPORTANCE: low -- comment only; I grepped the whole repo for the constructor, two hits total (that line and the tests).

VERDICT: PASS
