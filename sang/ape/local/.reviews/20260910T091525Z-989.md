# adversarial review -- japes

- when: 2026-09-10T09:15:25Z
- range: origin/v2.5.1...HEAD (dd75589)
- diff: 10009 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(activation-vs-active-rules)` — `policy.py:758` claims "no caller reads these accessors yet." Holds: `executable_rules` / `activation_blocks` / `is_executable` / `activation_block` appear only in `tests/test_policy_rule_activation.py`, and `not_in_force` only in `default.py:623` and `policy.py` itself.
- `TODO(orphan-blob-gc)` — `store_db.py:delete` leaves the archive. Holds: `delete` issues only the `retired_at` update, no blob call anywhere in the file, and the row still carries the digest.
- `TODO(layout-key-resolution)` / `TODO(layout-live-formula-order)` — `workbook.py:177,183`. Both hold: `workbook.py:413` is the `if item is None: continue`, and `690` reads `inputs_by_statement.get(...)` before `694` populates it for a later sheet.
- Every `Verdict.INDETERMINATE` construction in `condition_evaluator.py` (lines 152, 171, 210, 219, 261, 284, 356, 366, 394, 580) now carries an `indeterminate_reason`; `NaturalLanguageEvaluator`'s missing-agent path raises rather than returning INDETERMINATE. The enumeration is complete.
- `ruff check jazzx_sdk plato` clean; `test_policy_rule_activation` / `test_policy_not_activated_reaches_callers` / `test_pack_store` / `test_plato_packs_api` = 92 passed, 1 skipped (`test_pack_store.py:485`, env-gated on `JAPES_REAL_PACKS_DIR`, not TODO-parked).

## Findings

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:218` — the `TODO(threshold-grid-provenance)` deferral says the gap is `tables` only and that "the gate covers seven of its numbers and not the rest." It covers none of them. The pack it describes is in this repo: `plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml` has exactly 7 `thresholds:` and 5 grids totalling 150 cells, and all seven thresholds are bare decimal strings (`dscr_min_high_ltv: '1.20'`), not `Threshold` blocks — so `looks_authored` is False and `get()` serves them unrefused. Verified by running it: `PolicyProfile(**yaml.safe_load(...)).get('dscr_min_high_ltv')` → `'1.20'`. `policies/eligibility.yaml:145,203,223,246` route four live `RatioCondition`s through `profile:dscr_min_*`, so the `ThresholdNotActivated` → `POLICY_NOT_ACTIVATED` → `allowed=False` chain this diff builds fires on 0 of 157 numbers in the only shipped pack it was built for — while `ENCODING_NOTES.md:5` names Acra Lending's DSCR Program Summary v6.15.2026 as the source of truth and `pack_manifest.yaml:20` says `certification_status: draft`. The deferral's "not a regression, and there was never a way to mark one unapproved" half is true; its coverage count is not.
IMPORTANCE: high -- I enumerated the four `profile:dscr_min_*` call sites in `dscr_core/policies/eligibility.yaml`, the single `RatioEvaluator` path at `condition_evaluator.py:279`, and the `check_compliance` gate at `default.py:462` that consumes it. `dscr_core` is a bundled seed pack the new packs page initializes from, so any deployment that seeds it reaches this. Nothing outside this repo needs to change to fix it — wrapping the seven in `dscr_profile.yaml` is a data edit.

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:210` — `get_threshold`'s bare-value fallback asserts a provenance nobody authored: `return Threshold(value=found, provenance=INSTITUTION_AUTHORED, status=ACTIVE)`, under the comment "A bare value is the institution's own and active." Verified: `PolicyProfile(**yaml.safe_load('plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml')).get_threshold('dscr_min_high_ltv')` returns `status=PolicyStatus.ACTIVE, provenance=ThresholdProvenance.INSTITUTION_AUTHORED` for a figure transcribed from a third party's program summary. That is the exact failure `looks_authored`'s own docstring calls out for the other shape ("`get_threshold` reported it institution-authored and active"), left standing for the shape the shipped pack actually uses.
IMPORTANCE: medium -- `get_threshold` has no non-test caller (grepped: only `tests/test_policy_threshold_provenance.py`), so nothing in-repo acts on the false claim today; but it is a method on `PolicyProfile`, which is exported from `jazzx_sdk.fabric.canonical`, so a consumer reading provenance to decide whether it may cite a figure as the institution's own gets a silent false positive with no way to see it.

**[SYMMETRY]** `jazzx_sdk/finance/validation.py:830` — `_WARNING_REFS` regex-parses `policy_id`/`rule_id` back out of a string that `_not_activated_warning` (`experts/policy/default.py:71`) formatted three modules earlier, so the producer discards structure the consumer then reconstructs. `ComplianceResult.policy_violations` (`experts/policy/base.py:47`) already carries both as dict keys, and the sibling loop directly above reads them that way. A `not_activated: list[dict]` field shaped like `policy_violations` is no more code than `_not_activated_warning` + the format string + `_WARNING_REFS` + `_warning_refs` combined, and removes the "a line in any other shape simply contributes no refs" failure mode entirely.
IMPORTANCE: medium -- the only producer is `default.py:503` and the only consumer `policy_violations_to_findings`, both in this repo; the string format is not part of any published contract yet, so this is cheapest to change now.

**[SYMMETRY]** `jazzx_sdk/experts/policy/default.py:462` — `gate_blocks` exists at `condition_evaluator.py:91` precisely because "stating it inline got it into two of the three and the third kept the old behaviour," and this line states it inline as the fourth site: `outcome.indeterminate_reason is IndeterminateReason.POLICY_NOT_ACTIVATED`. Reusing the predicate here (renamed off `gate_`, since it is not gate-specific) is strictly less code and drops `IndeterminateReason` from `default.py`'s imports, where it is used for nothing else.
IMPORTANCE: low -- the three gate sites (`segment.py:109`, `check.py:311`, `default.py:443`) all call the helper; only this condition-path site duplicates it, and both spellings agree today.

**[NOTE]** `jazzx_sdk/agents/adjudication/segment.py:99` — `not_applicable` now also accumulates `Verdict.INDETERMINATE` outcomes, so the local's name states a verdict its contents no longer guarantee; the tuple it feeds `run_segment` is read positionally, so nothing breaks.
IMPORTANCE: low -- one caller (`run_segment`, which merges it into `outcomes`); no downstream code in `jazzx_sdk/agents/adjudication/` branches on `NOT_APPLICABLE`.

**[NOTE]** `jazzx_sdk/fabric/canonical/policy_lint.py:25` — `find_profile_literal_drift` iterates `policy.rules` only, so a `profile_custom_key` declared on an exception rule is never linted, while `Policy._all_rules` added in this same diff establishes the opposite rule ("an exception rule is a rule, and gating only `rules` left a seed one absent"). Pre-existing, but the diff creates the inconsistency between the two files.
IMPORTANCE: low -- `find_profile_literal_drift` / `assert_no_profile_literal_drift` have callers only in `tests/test_policy_lint.py`; no shipped pack declares `profile_custom_key` on an exception rule.

**[NOTE]** `jazzx_sdk/fabric/canonical/profiles.py:161` — `get` now raises `ThresholdNotActivated` for *any* `custom` mapping carrying a `status` or `provenance` key, with a message saying it "declares a provenance or status and is not a readable Threshold." `custom` is documented as the untyped escape hatch, so a nested config mapping that happens to use `status:` would be refused under a message that misdescribes it.
IMPORTANCE: low -- I checked the only shipped profile (`dscr_core/profiles/dscr_profile.yaml`): `custom` holds ints and string lists only, and `confidence_floors` is typed `dict[str, float]` so a mapping there fails at construction. No reachable trigger in any shipped configuration.

VERDICT: BLOCK

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
Reviewed the full commit `dd75589`, not just the pasted subset (the paste omits the SDK half: `experts/policy`, `fabric/canonical`, `pack/store_db`, `wiring_*`).

**CHECKED**
- `TODO(threshold-grid-provenance)` (`profiles.py:214`) — holds. `get_table` has no gate, and `dscr_profile.yaml`'s five tables total exactly 150 cells as claimed.
- `TODO(orphan-blob-gc)` (`store_db.py:delete`) — holds. The store is tenant-scoped at construction; the cross-tenant `count(*)` it names is genuinely unaskable here.
- `TODO(if-match-post)` (`posture.py:99`) — holds. `require_if_match` is still referenced only from tests, so `POST /packs/initialize` cannot 428.
- `TODO(omit-info)` (`info_api.py:81`) — holds. `__main__.py:304` `degraded_nav = ("Guide","config","database","Packs","packs")` does not contain `info`.
- `TODO(activation-vs-active-rules)` (`policy.py:_all_rules`) — holds. `executable_rules`/`activation_blocks`/`activation_block` have test-only callers; `check_compliance` still uses `active_rules()`.

---

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:128,138,141,149,151` — the bundled seed pack points its evaluator, both experts, conductor and pipeline at `jaci.scenarios.ci_spread.*`, a package japes neither depends on nor ships; `evaluation.gold_cases_path`/`runner` are likewise jaci-repo paths absent from the archive. Trigger: `POST {prefix}/packs/initialize` (the only supported way to populate a fresh DB), then `Pack.conductor` or `.experts` on the materialized pack → `ModuleNotFoundError: No module named 'jaci'` at `jazzx_sdk/pack/pack.py:43`.
IMPORTANCE: medium — nothing in plato resolves these today (`packs_api` reads only `_MANIFEST_KEYS`), but `pyproject.toml:13` ships `plato/` whole, so the wheel now carries a default pack that no japes-only consumer can resolve; `dscr_core` is clean (no `class:` pointers). Also contradicts the rule commit `5fd0293` established.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125,127` — `purpose_specific_documents.required_documents` carries two orphan PDF-wrap fragments, `schedule` and `available)`, and the equipment-purchase row they were split off is missing entirely; the section header (line 3) claims "Extracted verbatim from the source checklist". Trigger: the manifest wires this file as `policies.checklist` (`pack_manifest.yaml:50`) and its own description says missing items become DATA_GAP hypotheses — so a completeness check raises a gap for a document named `available)` and never asks for the equipment schedule.
IMPORTANCE: medium — wrong data served by the one seed pack wired to a checklist consumer; fails silently as a nonsense requirement rather than an error.

**[DEFECT]** `plato/packs_api.py:140` — `_refuse_unless_relaxed` gates on `strict_mode()`, but the module docstring (lines 10-13) claims the writes are "relaxed posture only (`local`, `dev-daily`) … a pack store whose rows can go in staging is not one". `strict_mode()` honours `JAPES_STRICTNESS=relaxed` on every tier except production (`envvars.py:217-224`), so a staging replica with that variable set admits `DELETE {prefix}/packs/{id}/{version}` and `POST {prefix}/packs/initialize`. `plato/posture.py:59` already separates these two questions with `deployed_posture()` + `strict_for`.
IMPORTANCE: medium — both write routes, mounted unconditionally by `plato/app.py:228`; production stays protected by `strict_mode`'s own asymmetry, staging and dev-daily do not. `check_settings` forces `require_identity` on staging, so the caller is identified but not authorized — which is exactly what the docstring says must not be sufficient.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/profiles.py:135` — `_coerce_threshold_mappings` is registered on `"thresholds", "custom"` only, while `get` (line 169) and `get_threshold` (line 198) both search three buckets including `confidence_floors`. A profile authoring `confidence_floors: {evidence_floor: {value: 0.8, provenance: official_source, status: active}}` is never coerced, so `looks_authored` fires and `get` raises `ThresholdNotActivated` on an *approved* threshold. The consistent version is one string added to the validator's field list — smaller than what the diff already does.
IMPORTANCE: low — no shipped profile authors `confidence_floors` at all (`dscr_profile.yaml` has none), and it fails loudly at the three enumerated `profile.get` call sites (`condition_evaluator.py:279`, `statemachine/schema.py:135`, `finance/validation.py:601`). A `TODO` beside the validator is a complete response.

**[NOTE]** Shipped pack data names the consumer repo, plan files, and one machine's document tree: `jaci.*` module paths in `reclassifications.yaml:2`, `breakouts.yaml:2`, `metrics.yaml:13`, `spread_template_rb.yaml:12,48,124`, `workbook_layout.yaml:4,19`, `ontology/ci_lending.yaml:179,207,235,254`, `policies/core.yaml:2`; `plan_JACI_*`/`plan_JAPES_*` filenames in `chart_of_accounts.yaml` (6 sites), `metrics.yaml:1,189,271`, `reclassifications.yaml:1`, `addbacks.yaml:1`, `spread_template.yaml:10`, `spread_template_rb.yaml:1`, `workbook_layout{,_rb}.yaml:1`; `docs/LoanSamples/...` checkout paths as served provenance in `checklist_ci.yaml:7`, `core.yaml:29`, `pack_manifest.yaml:101`, `playbooks/loan_package_scaffold.md:1`. These were internal notes when the files lived in a consumer; they are now in the japes wheel.
IMPORTANCE: low — no runtime effect beyond the manifest pointers above, but `provenance.source` is data a citation surface renders.

**[NOTE]** `plato/seed.py:78,101` — the docstrings say "a tree of authored pack directories" and "every pack directory under ``root``", but `candidates = sorted(p for p in base.iterdir() if p.is_dir())` is one level. A `PLATO_SEED_PACKS_DIR` pointed at a checkout whose packs sit under `config/packs/<pack>` reports "no readable pack manifest" per intermediate directory rather than recursing.
IMPORTANCE: low — the failure is reported per directory in the result, not silent; only affects the env-var override, not the bundled root.

**[NOTE]** `plato/static/packs.html:155` — the comment says the `x-user-id` field fills `published_by_user_id`, but `_actor()` → `CallerIdentity.from_context()` → `get_current_user_id()` (`jazzx_sdk/identity.py:299`) parses the header as a `UUID` and returns `None` otherwise. An operator typing a name gets a silent no-op: rows still show `—` in the By column and the retire still logs "an unattributed caller", with no indication the value was discarded.
IMPORTANCE: low — attribution only, relaxed postures only, and a UUID entered in the field does work.

VERDICT: BLOCK

## slice3

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(local-wiring-build-test)` (tests/test_plato_packs_api.py:757) — claim holds: `plato/wiring_local.build()` reaches `_assemble_pack`, which exits when no profile checkout is present, and the `wiring_default` twin (`test_the_shipped_wiring_actually_reaches_a_pack_store`) does build and fetch. Not raised.

---

**[DEFECT] plato/static/packs.html:416-424** — `load()` sets `#problem` on failure but nothing clears it on a later success, and `render()` (the success path) never touches it; symmetrically the failure path clears `#rows` but leaves `#empty`'s text visible. Trigger on a dev-daily replica: first load with the tenant field blank → 400, red "X-Tenant-Id is required…" in `#problem`; operator types the tenant and presses Enter → table renders with `N packs`, and the stale refusal is still on the page beside it. Reverse order (successful empty load, then a `request failed` during a restart) shows "Nothing published yet." and the failure line at once. Only `remove()` (line 265) resets the channel.
IMPORTANCE: medium — page-only; served by `GET {prefix}/packs/ui` from `create_packs_router` (the one route in `plato/packs_api.py` that renders it), no non-browser consumer, and reachable in every posture where the read is not withheld. Operator can reload, but the stale line points at a header problem that no longer exists.

**[DEFECT] tests/test_plato_packs_api.py:647** — the assertion counts `headers(` anywhere in the page and its own comment says it deliberately does not; the page contains 4 occurrences (3 call sites plus the prose line "goes through \`headers()\`" at packs.html:99), minus 1 for `const headers =`, so it sits exactly at the `>= 3` threshold on the strength of a comment. Trigger: delete or reword that one comment sentence in `packs.html` and the count drops to 2 → red test, zero behaviour change, which is the failure mode the comment claims was fixed. Line 646 already covers the call sites (`page.count("headers: headers(")` is 3).
IMPORTANCE: low — test-only; no shipped-code defect it would have caught that line 646 misses. A `TODO` or dropping line 647 is a complete response.

---

**[SYMMETRY] plato/wiring_default.py:613-620** — the "pack archives are going to local storage in a `%s` deployment; the rows outlive the bytes" degradation is logged once at boot only, while its stated model, `_db`'s sqlite-in-deployment degradation, is *also* reported through `missing_config()` (plato/wiring_default.py:359-363) and so reaches `/info` and the dashboard. `PLATO_PACK_BLOB_BACKEND`/`PLATO_PACK_BLOB_DIR` are environment-read per call, which is exactly the class `missing_config`'s docstring says it re-reads from the environment, so this is a 3-line `if posture and env_text(PACK_BLOB_BACKEND_ENV, default="local") != "azure": missing.append(...)` rather than a new mechanism. As it stands the only trace of an unmaterializable-on-restart pack store is one log line at container start.
IMPORTANCE: medium — `missing_config` is read by `/info` and `plato/static/dashboard.html`; affects any deployed replica that does not set `PLATO_PACK_BLOB_BACKEND=azure`, which is the default.

---

**[NOTE] plato/wiring_default.py:622-627** — the `TODO(azure-blob-container-singleton)` deferral rests on "Unreachable today -- this is the only `backend="azure"` construction in plato", but the singleton is process-wide and `jazzx_sdk/fabric/fabric.py:198` builds a second azure `BlobStore` from `JAPES_BLOB_BACKEND`/`JAPES_BLOB_CONTAINER`, which plato instantiates through `_client_layer()` → `get_client_layer()`. With both set to azure and different containers, whichever consumer calls `get_instance` first fixes the container for both, silently.
IMPORTANCE: unknown — I could not establish whether any shipped plato request path performs `fabric.blob` I/O (`get_instance` is lazy, so an unused fabric blob store never registers), so I cannot say the collision is reachable in a running configuration; the "only construction in plato" half of the claim is literally true, the "unreachable in this process" half is not.

**[NOTE] plato/wiring_default.py:627** — `PLATO_PACK_BLOB_BACKEND=azure` with `PLATO_PACK_BLOB_CONTAINER` unset passes `container=None`, which `AsyncBlobStorageClient` resolves from `common` settings, and this branch emits none of the two warnings the local branch does — so the module comment's "`azure` plus a container points it at one" is not enforced or reported; the first publish either lands in whatever container settings default to or fails at request time.
IMPORTANCE: low — needs a deployer to set the azure backend and omit the container; no shipped configuration sets `PLATO_PACK_BLOB_BACKEND` at all (default `local`), and nothing outside this repo reads these variables.

VERDICT: BLOCK
