# adversarial review -- japes

- when: 2026-09-11T17:25:43Z
- range: 73afa22..HEAD (since the last review) (6e602d9)
- diff: 301 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_mock_knowledge_hub_app.py` → 28 passed; `ruff check` on all three files clean. New behaviour re-probed through `TestClient` (create/patch/recreate sequences below are actual output).

WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: conflict-checked-before-validation -- FIXED; `validate_entity` is asked of the delegate above the `_created_names` check and `test_a_schema_invalid_duplicate_is_a_422_not_a_409` pins 422.
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: collection-move-leaves-stale-key -- FIXED for a plain move (`moving` is `{"name","collection_id"} & fields.keys()`, both directions asserted); the explicit-`null` variant below is a separate slug.
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: patch-accepts-blank-name -- FIXED; 422 before any write, and the test now asserts the stored name is untouched.
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: padded-name-false-conflict -- FIXED; `conflict_key`/`was`/`target` all use raw strings and `test_a_padded_name_is_a_different_name` covers it.
WAS: tests/test_mock_knowledge_hub_app.py :: test-name-states-the-opposite -- FIXED; renamed to `test_the_client_double_leaves_name_uniqueness_to_the_service` and the docstring now says the double enforces nothing.

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:320 :: explicit-null-field-poisons-key -- a JSON `null` for `name` or `collection_id` is stringified into the key as `"None"` (the delegate ignores it and leaves the field alone), so the entity's live key is discarded and a phantom one recorded, and the mock then accepts a duplicate the service refuses.
Reproduced, `collection_id` half (newly introduced here: the old line was `str(fields.get("collection_id") or existing.get("collection_id", ""))`, whose `or` treated `null` as absent):
`POST {name:"e-1", collection_id:"col-1", ...}` → 201; `PATCH {"name":"e-2","collection_id":null}` → 200, response shows `collection_id="col-1"`, `name="e-2"`; then `POST {name:"e-2", collection_id:"col-1"}` → **201** where the service 409s, because `target` was `("None","e-2")`.
Reproduced, `name` half (survives the blank-name guard at :307, since `str(None).strip()` is `"None"`): `PATCH {"name":null}` → 200, stored name still `e-1`; `POST` the original body → **201** where the service 409s, because `("col-1","e-1")` was discarded and `("col-1","None")` added.
Same shape with `""`: `PATCH {"collection_id":""}` → 200 and the old key is discarded while `all(target)` suppresses the add, so the new guard's blank check covers only one of the two halves of the key it protects. Taking `was[n]` whenever the field is absent *or* `None` closes both.
IMPORTANCE: medium -- the missed-409 direction, i.e. the case this app exists to catch, and it is silent (200, plausible body). In-repo reachers are tests/test_mock_knowledge_hub_app.py only; out of repo the factory is exported from `jazzx_sdk.server` for packs to point an HTTP client at, and a client that serializes unset optionals as `null` (the module docstring names jazzx-assistant's hand-rolled mock as the shape being replaced) hits it on any partial PATCH. No real-service path touched.

[NOTE] jazzx_sdk/server/mock_knowledge_hub_app.py:243 :: non-validating-delegate-claim -- "for any other the write below still validates, one step later than the service would" overstates: for the both-codes body the conflict 409 at :252 returns before the write, so the validation never runs at all and the precedence this fix restores holds only for a delegate exposing `validate_entity`.
IMPORTANCE: low -- in-repo the only `KnowledgeHubLike` implementations are `MockKnowledgeHubClient` (has the method) and the real `KnowledgeHubClient` (nobody passes it as the delegate; grepped `create_knowledge_hub_mock_app` call sites: tests only). Reachable only by an out-of-repo pack delegate, and it fails as a wrong code.

[NOTE] jazzx_sdk/clients/mocks.py:1586 :: dead-lookup-before-return -- `entity = self._entities.get(...)` runs above the "nothing schema-relevant changed" early return, so a name- or collection-only update does a lookup it discards; harmless, but it reads as if the early return depends on the entity.
IMPORTANCE: low -- one dict lookup on the mock's own update path; no behaviour difference.

VERDICT: BLOCK
