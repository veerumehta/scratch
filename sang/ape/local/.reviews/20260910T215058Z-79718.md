# adversarial review -- japes

- when: 2026-09-10T21:50:58Z
- range: origin/v2.5.1...HEAD (e0f8442)
- diff: 11838 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Reviewed against the tree (the handed diff omits `plato/packs_api.py`, `plato/seed.py`, `plato/tenancy.py`, migration `0004`, and all tests, which the presented hunks depend on — I confirmed all exist; `packs_api.py` itself I did not review). `ruff check jazzx_sdk plato` passes.

## CHECKED

- `TODO(publish-tick-tie)` (`store_db.py:61`) — holds. `plato/seed.py:124` iterates candidates serially with one `await store.publish(...)` per directory (line 176); no `gather`. `publish` does have callers, as the amended note now says.
- `TODO(refused-rule-field-claim)` (`default.py:498`) — holds. `allowed=not violations and not not_activated` (`default.py:540`), so the allow is already withheld; the cost is a spurious violation entry, not a wrong verdict.
- `TODO(activation-vs-active-rules)` (`policy.py:750`) — holds. No caller of `executable_rules`/`activation_blocks` exists outside `policy.py`.
- `TODO(threshold-grid-provenance)` (`profiles.py:243`) — holds, and the correction in it is right: the one shipped profile (`plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml`) authors all seven `thresholds:` as bare strings and carries no `provenance:` or `status:` anywhere, so `looks_authored` is False for every entry and the gate covers none of its numbers.

## Findings

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/policy.py:728` / `jazzx_sdk/experts/policy/default.py:627` — `Policy.is_active` was widened to cover the whole effective window, and the diff updated exactly one of the three docstrings that describe that predicate. `policy_registry.py:126` still says `current_policies` returns "active (non-expired, non-deprecated) policies", and `store/core.py:179` still documents `active_only` as "exclude deprecated/archived policies"; both now also drop a future-dated `status: active` policy, with no `not_yet_effective` companion of the kind `resolve` and `detect_staleness` grew in this same diff. State: a KH-stored policy authored `status: active, effective_date: 2027-01-01` disappears from `list_policies()` (default `active_only=True`) and from `current_policies()`, reported as nothing. Lining up the two descriptions with `default.py:627` is no more code than the one that was changed.
IMPORTANCE: medium -- I enumerated every `is_active` reader in `jazzx_sdk`: `policy_registry.py:127`, `store/core.py:206`, `governor.py:105`, `default.py:351` and `:680`. The last three were handled in this diff; the first two were not. `list_policies` is on the KH-backed `CanonicalStore` facade (`store/facade.py:302`) with `active_only=True` as the default, so an external consumer listing policies is reachable; I could not determine whether any consumer outside this repo authors future-dated policies today.

**[SYMMETRY]** `jazzx_sdk/agents/adjudication/segment.py:120` — `blocked_gate_outcome` was extracted because "`run_segment`'s copy dropped `action` where `check_graph`'s kept it", but only the *blocked* branch was routed through it. The `NOT_APPLICABLE` branch immediately below (`segment.py:121-124`) still omits `action` and still reports `inputs={"applicability_gate_verdict": ...}`, where the sibling at `fabric/graph/check.py:397-400` sets `action=rule.action` and passes `gate_outcome.inputs` through. Input: any rule with an `applicability` gate that evaluates to `VIOLATED` — `run_segment` yields an outcome with `action=None`, `check_graph` yields one carrying the rule's action. A second builder beside `blocked_gate_outcome` (or one taking the verdict) is the same amount of code the diff already wrote.
IMPORTANCE: low -- I enumerated every reader of `RuleOutcome.action` in the repo: `check.py:317` reads `rule.action`, not the outcome's, and nothing under `agents/adjudication/` reads `.action` at all, so no shipped path observes the difference. `RuleOutcome` is exported from `jazzx_sdk.fabric.canonical`, so an external consumer routing on `outcome.action` would see the divergence; I could not establish whether one does.

**[NOTE]** `jazzx_sdk/experts/policy/base.py:37` and `:110` — both `not_yet_effective` field descriptions say "Policy IDs whose `effective_date` is in the future", but both producers additionally require `status is PolicyStatus.ACTIVE` (`default.py:353`, `default.py:684`). Input: a policy authored `status: draft, effective_date: 2027-01-01` — its id lands in `stale_policies` (with a recommended action telling the caller to review and update it) and `not_yet_effective` comes back empty, which the description says cannot happen. The behaviour looks deliberate; the two descriptions are what's wrong, and they're identical text in two models.
IMPORTANCE: low -- one confusing surface entry; both fields are new in this diff so nothing outside the repo reads them yet.

**[NOTE]** `jazzx_sdk/fabric/canonical/profiles.py:91` — `Threshold.status` defaults to `SEED` while `looks_authored` (`profiles.py:156`) treats `provenance` alone as a governance claim, so recording a source on an existing working threshold is what breaks it. Input: adding `provenance: official_source` (and nothing else) under `dscr_min_high_ltv:` in `dscr_profile.yaml` turns `profile.get("dscr_min_high_ltv")` from `'1.20'` into `ThresholdNotActivated`, and every `RatioCondition` reading that key into `POLICY_NOT_ACTIVATED` — which now withholds `allowed` in `check_compliance`. It is fail-closed and both docstrings state the default; the surprise is that `looks_authored` frames provenance-alone as an incomplete block ("declares one and forgets its `value`") rather than as the natural way to document a good number.
IMPORTANCE: low -- no shipped profile authors `provenance` at all (verified: the sole profile has none), so there is no reachable trigger in any current configuration; the cost is one pack-authoring session.

**[NOTE]** `jazzx_sdk/finance/validation.py:797` — the docstring says "``warnings`` is read only when ``refusals`` is empty, and then for its text alone", but the guard at `:843-847` additionally requires `allowed` falsy *and* `policy_violations` empty. State: a pack expert on the older shape returning `allowed=False`, one `policy_violations` entry, and a withholding `warnings` line — the warning is dropped, though `refusals` is empty. The finding list is non-empty in that case, so it is not the clean-spread failure the surrounding paragraph closes, but the guard is narrower than the sentence describing it.
IMPORTANCE: low -- one lost advisory line on a result that already carries a POLICY finding; `ComplianceResult` is exported, so only an out-of-repo expert can produce this shape.

VERDICT: PASS

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(layout-key-resolution)` (`jazzx_sdk/finance/workbook.py:176`) — the claim holds and the diff's counter-claim holds too: every `key` in `workbook_layout.yaml` and `workbook_layout_rb.yaml` resolves against this pack's own `chart_of_accounts.yaml` lines / `metrics.yaml` ids (checked programmatically, 0 unresolved).
- `TODO(layout-live-formula-order)` — holds: `workbook_layout.yaml` declares each `Inputs - <Statement>` sheet before its `live_formula: true` twin, and `workbook_layout_rb.yaml` declares no live sheets at all.
- `spread_template.yaml`'s 7 metric ids (`da`, `net_leverage_x`, `dso_days`, `dio_days`, `dpo_days`, `ccc_days`, `stock_based_compensation`) do not exist in this pack's `metrics.yaml`, but they are not meant to — they resolve against the consumer's own analytics metric map. Not a finding.

## Findings

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:235` — `DSCR-OVER-2M-DSCR` states the >$2M DSCR floor as a `ratio` condition only, so on a No Ratio file the condition reads INDETERMINATE and the rule is skipped in the permissive direction. Trigger: `loan_amount=2_500_000, dscr_documentation_type="no_ratio", fico=720, loan_purpose="purchase", cltv_pct=70` → `DefaultPolicyExpert.check_compliance` returns `allowed: True` with zero violations and zero warnings; the identical loan with a documented DSCR of 0.9 is denied by this same rule. This is the exact hazard `ENCODING_NOTES.md` §3 G2 describes, and the fix it prescribes (a twin rule keyed on `dscr_documentation_type == no_ratio`) was applied to `DSCR-SUB-1-DSCR-CLTV`/`-FICO` and not here. `DSCR-HIGH-LTV-DSCR` (line 148) has the same shape but is masked: `DSCR-NO-RATIO-CLTV` caps a no-ratio file at 75% CLTV, below its own `cltv_pct > 80` applicability.
IMPORTANCE: high -- silent permissive pass on a deny-action rule. Verified by running the shipped corpus through `DefaultPolicyExpert.check_compliance` (japes' own expert), so any consumer of this pack reaches it; the identical corpus is jaci's `config/packs/dscr_core/`, whose `DSCRConductor`/`run_eligibility_assessment` feeds the Governor gate, and `compose_caps`' `blocked_by_indeterminate` guard only covers matrix `cltv_pct` cap rules, not this ratio rule.

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:149` — the pack ships `evidence_types.yaml` (7 ids, in the `{evidence_types: [...]}` shape `PackManifestLoader.evidence_types()` already reads) but the manifest declares no `evidence_types:` key, so `Pack.evidence_types` returns `[]` — while this same manifest's `convergence_evidence_required` at line 155 names three of those ids. The consistent version is the one-line declaration `evidence_types: evidence_types.yaml`, which is exactly the argument `dscr_core/pack_manifest.yaml:33` makes for its own `profiles: {dir: profiles}` ("without this the profile beside them is never loaded").
IMPORTANCE: medium -- fails as an empty list, not a raise. Checked `Pack.from_manifest("ci-spread-core", …).evidence_types` → `0`, vs `playbooks` → `4` and `policy_registry` → 6 policies, so this is the one declared-asset channel japes exposes that this manifest leaves dark; the file's own header says the tool registry derives the requestable set from it, so an Investigator built from this pack can request nothing.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:94` — "`find_profile_literal_drift` only checks a rule's top-level `condition`/`applicability`, not `all_of`/`any_of` children — so a `profile_custom_key` on a nested Expression would be decorative … Composite-condition drift-lint coverage is a real japes-side gap" is false: `_declaring_expressions` (`jazzx_sdk/fabric/canonical/policy_lint.py:19`) recurses into composites, and its docstring records closing that gap. This same diff depends on the recursion — `DSCR-IO-MIN-LOAN`'s two nested `profile_custom_key`s are checked (corpus verified drift-clean, 9 keys reached). The stale paragraph is the stated reason four PPP literals were left unlinked. Three further counts in the same file went stale on the `DSCR-IO-MIN-LOAN` → `all_of` change in this diff: line 54 ("11 `expression` as `condition`" — actual 10, plus 2 `all_of` conditions), line 207 ("10 of the 41 rules … use `kind: all_of`/`any_of`" — actual 11), line 235 (`fico_min_io` "no consuming rule at all today" — now consumed; only `fico_min_str` and `seller_concession_max_pct` remain).
IMPORTANCE: low -- prose in a shipped notes file; the cost is a future author skipping lint coverage the lint would in fact apply. Counts re-derived by loading the corpus; no code reads this file.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:151` — "pipelines.yaml beside this file is the declarative form of the same pipeline" is a claim nothing backs: `Pack.conductor` (`jazzx_sdk/pack/pack.py:343`) reads only `conductor.pipeline` and `_resolve`s it as a dotted Python pointer, this manifest declares no such key, and no manifest key names `pipelines.yaml`. `Pack.from_manifest("ci-spread-core", …).conductor` is `None`. `ConductorPipeline.from_yaml(path, name=…)` exists and would load it, but only a caller that already knows the filename can reach it.
IMPORTANCE: low -- reads as available and is inert; verified `Pack.conductor is None` for both seed packs. Closing it needs a japes-side change to `Pack.conductor`, which is larger than the manifest edit, so a `TODO` is a complete response.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/policies/overlays/deal_si_rb_example.yaml:2` and `pipelines.yaml:2` — these shipped pack assets name consumer-only symbols (`load_deal_special_instructions`, which exists nowhere in japes; `CIConductor`). `tests/test_plato_packs_api.py`'s consumer-reference pin searches for the bare word `jaci`, so it cannot see either, and neither file is on its pinned list — the pin's "the record complete" claim holds only for references that spell the repo name.
IMPORTANCE: low -- comment text in data, no behaviour. Confirmed `load_deal_special_instructions` is absent from `jazzx_sdk/` and present only in the consumer's tests; the 55 tests in `tests/test_plato_packs_api.py` pass, so nothing currently flags it.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED**
- `plato/info_api.py:84` `TODO(omit-info)` — holds: `plato/__main__.py:304` sets `degraded_nav = ("Guide", "config", "database", "Packs", "packs")`, which does not contain `info`, and it is the only caller passing `omit`.
- `plato/posture.py:96` `TODO(if-match-post)` — holds: `require_if_match` appears only in `tests/test_plato_preconditions.py`; no router mounts it, so the new `POST .../packs/initialize` cannot 428.
- `plato/wiring_default.py:660` `TODO(azure-blob-container-singleton)` — holds and is not overstated: `jazzx_sdk/fabric/fabric.py:198` does construct a second `BlobStore`, so "not unreachable" is accurate.
- `plato/seed.py:44` `TODO(seed-pack-vendoring)` — holds: `git ls-files plato/data/seed_packs` shows the vendored copies, and `test_which_seed_pack_files_name_consumer_modules_is_pinned` pins the consumer references that remain.

**[SYMMETRY]** `plato/seed.py:137` — the shadow check spells "which manifest name a reader would open" (`next(n for n in MANIFEST_NAMES if …)`) in the caller, and `_manifest_of` walks the same order a second time; `jazzx_sdk/pack/store.py` is where that rule already lives (`MANIFEST_NAMES`, `pack_archive`, `is_assistant_manifest`, each with a docstring saying it sits there so a caller does not respell it) and `store_db._unpack:494` does the same walk on the read side. A `manifest_name_in(pack_dir)` beside `pack_archive`, used by both `_manifest_of` and the shadow check, is the same amount of code.
IMPORTANCE: low -- one call site (`plato/seed.py`), reached only by `POST /packs/initialize`; nothing outside this repo imports `plato.seed`.

**[NOTE]** `plato/packs_api.py:199` — `"archive_retained": True` is unconditional, so a version published manifest-only (`content_ref is None`) is reported as having had its archive retained. No shipped path creates such a row: both wirings pass a `blob`, and `seed_packs` always publishes with `pack_archive(pack_dir)`.
IMPORTANCE: low -- reachable only through a direct `store.publish(manifest)` with `archive=None`; the field is read by nothing in this repo (the page does not render it).

**[NOTE]** `plato/tenancy.py:148` — when `tenant_from_request` is `None` (the routers' own default) the 400 says "Supply the tenant header it reads (X-Tenant-Id by convention)", which no header can satisfy because the deployment wired no resolver at all. Not reachable via `create_plato_app`: `PlatoWiring.tenant_from_request` is a required field and `plato/app.py:229`/`:142` always forward it; only a direct `create_packs_router()`/`create_assistants_router()` call hits it.
IMPORTANCE: low -- both in-repo call sites pass a resolver; the cost is one misleading 400 body for an embedder wiring the router by hand.

**[NOTE]** `plato/seed.py:183` with `plato/static/packs.html:396` — a version refused because it is *retired* is bucketed into `skipped`, and the page renders `skipped.length` as "already there", so retiring a version and clicking Initialize reports it "already there" while the table refreshed underneath shows it gone. This is the same split the diff made for `not_packs` (`packs.html:391`), one input over; the JSON carries `reason: "retired"` and the page drops it.
IMPORTANCE: low -- packs page only, and only in `local`/`dev-daily`, the two postures where both the delete and the seed are accepted; the JSON caller sees the distinction.

VERDICT: PASS
