# adversarial review -- japes

- when: 2026-09-10T03:17:30Z
- range: origin/v2.5.1...HEAD (55c582f)
- diff: 1563 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(threshold-grid-provenance)` (jazzx_sdk/fabric/canonical/profiles.py:213) — claim holds. jaci's `config/packs/dscr_core/profiles/dscr_profile.yaml` has exactly 5 tables totalling 150 cells and 7 `thresholds:`, all bare strings; `_coerce_threshold_mappings` is registered on `thresholds`/`custom` only and `get_table` returns the cell unchanged, so a grid cell is served exactly as before. Not a regression, gap correctly scoped.

---

**[DEFECT] jazzx_sdk/fabric/graph/check.py:375** — the third applicability-gate site was not fixed: `_evaluate` still collapses any non-SATISFIED gate to `NOT_APPLICABLE`, so a rule whose gate reads an unapproved threshold is recorded as inapplicable and, because `check_graph` only builds findings for VIOLATED/INDETERMINATE, disappears from the result entirely — the same silent pass the diff fixes in `experts/policy/default.py:428` and `agents/adjudication/segment.py:107`.
Trigger (reproduced): `check_graph(graph, policy, context={..., RATIO_PROFILE_CONTEXT_KEY: profile})` where the rule's `applicability` is a `RatioCondition(threshold="profile:dscr_min")` and the profile holds `Threshold(value="1.20", provenance=SECONDARY_SOURCE)` → `outcomes == [("GATED", NOT_APPLICABLE, indeterminate_reason=None)]`, `findings == 0`. The same file's new `_UNTESTED_RATIONALE[POLICY_NOT_ACTIVATED]` entry is unreachable from the gate path for this reason.
IMPORTANCE: high — silent wrong data with no caller-visible signal. Callers enumerated: `check_graph` is called from japes' own `tests/test_graph_contradictions.py` / `tests/test_graph_derive.py`; it is not re-exported from `jazzx_sdk/fabric/graph/__init__.py` (line 100 says import it directly), and grep across `/Users/sangit/src` found no sibling repo importing it, so no shipped consumer reaches it today — but it is a public module and the fix is the four lines already written twice in this diff.

**[DEFECT] jazzx_sdk/experts/policy/default.py:611** — routing `is_active` through `_out_of_force` makes it False for a not-yet-effective policy, and `detect_staleness`'s reason string (unchanged) then asserts a status the policy does not have.
Trigger (reproduced): `Policy(status=ACTIVE, effective_date=2099-01-01)` → `detect_staleness(["p2027"])` returns `stale_policies=['p2027']`, `reasons={'p2027': "status is 'active' (or past expiry), not active"}`. Before this diff the policy was not stale at all. `resolve()` (default.py:314) likewise lists it in `stale_policies` while `check_compliance` still evaluates its rules, and the method docstring at default.py:574 still says the check is "status not ACTIVE, or past its `expiry_date`".
IMPORTANCE: medium — visible but self-contradictory output. Reached by `DefaultPolicyExpert.detect_staleness` and `.resolve` (both on the expert contract, dispatched through `execute`), plus `PolicyRegistry.active_policies` (policy_registry.py:127), `store/core.py:206` `active_only`, and `governor.py:105`, all of which now drop a future-dated policy. No in-repo test covers a future `effective_date` in `tests/test_default_policy_expert.py`.

**[DEFECT] jazzx_sdk/fabric/canonical/policy_lint.py:41** — the guard checks `key not in profile.custom` but the lookup is `profile.get_threshold(key)`, which searches `thresholds` first, so a key present in both buckets is compared against the wrong value under a message naming `custom` (and the call can now raise `ThresholdNotActivated` out of a function documented to return findings).
Trigger (reproduced): profile with `thresholds={"max_x": 99.0}`, `custom={"max_x": 3.0}` and a rule whose literal is `3.0` → `["R1.condition: literal 3.0 diverges from PolicyProfile.custom['max_x']=99.0"]` for a profile that has not drifted; a `custom` entry of `{"value": 3.0, "status": "ok"}` raises instead of reporting.
IMPORTANCE: low — the only shipped consumer is jaci's `dscr_core` profile (`tests/scenarios/dscr/test_profile_literal_drift.py`, 8 declared `profile_custom_key` links), whose `thresholds` (7) and `custom` (11) key sets are currently disjoint, so nothing hits it until someone adds a same-named key; failure is a loud test break, not silent.

**[SYMMETRY] jazzx_sdk/fabric/canonical/condition_evaluator.py** — "a gate outcome carrying `POLICY_NOT_ACTIVATED` does not mean the rule is inapplicable" is now stated three times in three callers (`experts/policy/default.py:427`, `agents/adjudication/segment.py:107`, and missing at `fabric/graph/check.py:375`) instead of once next to `get_condition_evaluator`, which every one of them already calls. A one-line predicate there (`gate_satisfied(outcome)` / `gate_blocks(outcome)`) is smaller than the three inline branches and is what would have caught the check.py miss.
IMPORTANCE: medium — same three call sites as above; nothing outside this repo dispatches gates.

**[NOTE] jazzx_sdk/experts/policy/base.py:51** — `warnings` is described as "Compliance warnings (non-blocking)", but `check_compliance` now derives `allowed` from exactly that list (`allowed=not violations and not not_activated`), so a non-empty `warnings` is precisely what blocks.
IMPORTANCE: medium — `ComplianceResult` is the expert contract packs implement and read; a caller that trusts the description and inspects only `policy_violations` sees an empty list beside `allowed=False`.

**[NOTE] jazzx_sdk/experts/policy/default.py:73** — `outcome.inputs.get("not_activated", ...)` only finds that key on a `RatioCondition` outcome; a composite (`all_of`/`any_of`) that propagates `POLICY_NOT_ACTIVATED` via `_composite_reason` carries `inputs={"limbs": [...]}`, so the warning degrades to the bare `"p/RULE: not activated"` with no threshold key.
IMPORTANCE: low — one less-informative warning line; the allow is still withheld correctly.

**[NOTE] jazzx_sdk/fabric/graph/check.py:18** — the module docstring's quoted rendering of an INDETERMINATE answer was replaced with a 210-character line that quotes text the code never emits and points at a private module-level dict (`_UNTESTED_RATIONALE`); it reads as a stray find/replace rather than an edit.
IMPORTANCE: low — documentation only, ruff passes on the file.

VERDICT: BLOCK
