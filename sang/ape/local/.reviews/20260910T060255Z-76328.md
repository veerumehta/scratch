# adversarial review -- japes

- when: 2026-09-10T06:02:55Z
- range: origin/v2.5.1...HEAD (6c33e01)
- diff: 9653 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## [DEFECT] plato/wiring.py:133 — the packs feature is never wired in the shipped app

`create_plato_app` gained `pack_store_for` and always mounts the packs router, but `build_app` — the only non-test construction path (`plato/__main__.py:396`) — doesn't pass it, and `PlatoWiring` has no field for it (`plato/wiring_default.py:470-485` builds `manifest_store_for` and `session_store_for`, no pack equivalent). Trigger: start Plato normally and open `/api/v1/packs/ui`; `GET /packs` returns `{"available": false, "reason": "no pack store is wired", "packs": []}`, and `POST /packs/initialize` and `DELETE /packs/{pack_id}/{version}` both 503, in every posture. The nav renders the "Packs"/"packs" links regardless.

IMPORTANCE: medium -- enumerated every caller of `create_plato_app`: `plato/wiring.py:133` plus tests only (`tests/test_plato_packs_api.py:185`, `tests/test_plato_packs.py:203,274` all pass `pack_store_for=` explicitly, which is why the suite is green). Reaches every deployed replica and the seed-pack initialize path this diff adds; `plato/models.py:49` still registers the table so migrations create it. It fails legibly rather than silently, and nothing outside this repo consumes the route.

## [DEFECT] jazzx_sdk/experts/policy/default.py:444 — the gate branch withholds `allowed` for rules precedence already discarded

The new `gate_blocks` branch sits above the `fields_claimed` precedence check, while its twin on the condition path (line 461) sits below it. State that triggers it: a higher-precedence overlay claims `leverage_x`, and a lower-precedence core policy has a rule on `leverage_x` whose `applicability` is a ratio condition reading a `status: seed` threshold. The gate branch appends a "rule not activated" warning and flips `allowed=False` for a rule that precedence says does not govern this deal; move the same seed threshold from the rule's `applicability` to its `condition` and the identical deal returns `allowed=True`.

IMPORTANCE: medium -- reachable by the intended use of the `Threshold` type this diff adds, but nothing authors one yet: the only shipped profile, `plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml:6-32`, uses bare strings and ints throughout, and jaci's copy (`config/packs/dscr_core/profiles/dscr_profile.yaml`) is the same file. It errs toward over-refusal, and `check_compliance` is advisory (`output_classification: GUIDANCE_REFS`), so the Governor still decides.

## [SYMMETRY] jazzx_sdk/fabric/canonical/policy.py:126 — `as_utc` belongs in `jazzx_sdk/manifest/lifecycle.py`

The diff created `manifest/lifecycle.py` precisely so a dependency-free `PolicyStatus` could be shared without cycling back through `policy.py`, then put `as_utc` — a datetime helper with no policy content — in `policy.py`, which forces `fabric/guidance/schema.py:95` to import it inside the method body to dodge the cycle. Moving it beside `PolicyStatus` is the same move one field over and no more code; `fabric/canonical/__init__.py` can keep re-exporting it exactly as it re-exports `PolicyStatus`.

IMPORTANCE: low -- two import sites (`fabric/guidance/schema.py:95`, `experts/policy/default.py:31`), no behavior change.

## [NOTE] plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:434 — `UCAcashFlow` reads three identifiers `FinancialPeriod` doesn't declare

`net_income`, `D&A` and `working_capital_change` are not fields on `FinancialPeriod`, and the `inputs` list names `FinancialPeriod.ebitda`, which the formula never uses. `ExcessAvailability:411` has the same shape — its formula subtracts `reserves`, declared on `BorrowingBaseResult` but not `CollateralPool`, and absent from its own `inputs`. This is the class the v0.2.1 header says it just swept for FCCR and `DebtServiceCoverage`.

IMPORTANCE: low -- the file's own header states formulas are documentation only and runtime computation lives in the Reasoner and `compute_ci_metrics()`; no loader parses them, so nothing reads the undeclared names.

## [NOTE] plato/info_api.py:86 and :92 — two copies of the degraded-nav tuple went stale

`__main__.py:304` grew to `("Guide", "config", "database", "Packs", "packs")`, but the `TODO(omit-info)` comment and `nav_html`'s docstring both still spell it `("Guide", "config", "database")`. The TODO's actual claim (no caller passes `omit=("info",)`) still holds; the enumeration it rests on no longer describes the caller.

IMPORTANCE: low -- comment only; `nav_html`'s behavior is unchanged and `plato/__main__.py:319` is the sole `omit=` caller.

## [NOTE] plato/data/seed_packs/ci-spread-core/addbacks.yaml:1 — new shipped pack data cites gitignored plan files

`plan_JACI_CL_ADDBACK_LIBRARY.md`, `plan_JACI_CL_CHART_OF_ACCOUNTS_AND_CATALOG.md`, `plan_JACI_CL_RECLASSIFICATION_ENGINE.md` and `plan_JACI_CL_VALIDATION_COMPLETION.md` are named 19 times across the new `ci-spread-core` files; none is in the repo, so a reader of a published pack archive cannot open any of them.

IMPORTANCE: low -- comments in pack data, and the same convention already exists in the shipped `cl_of_core` and `cl_sp_core` manifests, so this is consistent with the family rather than new drift.

CHECKED:
- `TODO(orphan-blob-gc)` (`jazzx_sdk/pack/store_db.py:279`) holds: the store binds `_tenant_id` at construction and the blob key is the content digest, so the cross-tenant refcount it defers genuinely cannot be asked here.
- `TODO(threshold-grid-provenance)` (`jazzx_sdk/fabric/canonical/profiles.py:216`) holds: `_coerce_threshold_mappings` is registered on `thresholds` and `custom` only, `get_table` is unchanged, so a grid cell is served exactly as before.
- `TODO(activation-vs-active-rules)` (`jazzx_sdk/fabric/canonical/policy.py:758`) holds: `active_rules()` at :792 does filter `legacy_alias` and does ignore `exception_rules`, and the only readers of `executable_rules`/`activation_blocks` are `tests/test_policy_rule_activation.py`.
- The three applicability-gate call sites (`agents/adjudication/segment.py:105`, `experts/policy/default.py:443`, `fabric/graph/check.py:377`) are the complete family and all three now call `gate_blocks`; the three `PolicyProfile.get` call sites (`statemachine/schema.py:135` via `evaluate_guard`, `finance/validation.py:600`, `condition_evaluator.py:279`) all handle `ThresholdNotActivated`.
- `confidence_floors` is typed `dict[str, float]`, so it cannot hold an authored threshold mapping and correctly needs no coercion validator — `tests/test_policy_threshold_provenance.py:114` already states this.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

`plato/info_api.py:83` `TODO(omit-info)` — claim holds. No caller passes `omit=("info",)`: the only `omit` caller is `_info_only_app`, whose `degraded_nav` is `("Guide", "config", "database", "Packs", "packs")` (`plato/__main__.py:305`), so `id="api-info"` still renders. The `packs.html` page substitutes `__API_PREFIX__` directly and reads no `api-info` element, so the new page does not widen the deferral.

## Findings

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:1` — nothing can load this file's five policies (FCCR, ABL, LIEN, `RB_CI_OVERLAY`, addback ceilings): the pack manifest declares `core: policies/core.yaml`, one overlay and a checklist, and no path to `conventions.yaml`, and the one thing that does read it is `policies.registry: jaci.scenarios.ci_spread.policies.registry.CI_REGISTRY`, which is unimportable in this image. Reproduced: `Pack.from_loader(PackManifestLoader("…/ci-spread-core/pack_manifest.yaml")).policy_registry` raises `ModuleNotFoundError: No module named 'jaci'`, so `domain_pack_summary` returns `{'policies_error': 'ModuleNotFoundError'}` for ci-spread-core while the other four seed packs return 1, 2 and 1 policies; `policy_files()` returns `[core.yaml]` only, so removing the `registry:` pointer still would not pick `conventions.yaml` up. `dscr_core/pack_manifest.yaml` in this same diff shows the shape that works (`policies: dir: policies`, `profiles: dir: profiles`).
IMPORTANCE: medium -- enumerated every consumer in this repo: `pack_inventory`/`/packs/ui` only echo manifest keys (unaffected), and `published_domain_packs`/`domain_pack_summary` are the loaders that break but are still test-only per their own TODO. jaci reads its own byte-identical copy through `registry.py`, which does load `conventions.yaml` by path, so the dead data is specific to the plato copy this diff ships.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/overlays/deal_si_rb_example.yaml:1`, `plato/data/seed_packs/cl_of_core/pack_manifest.yaml:50`, `plato/data/seed_packs/cl_sp_core/pack_manifest.yaml:49` — three declared assets are absent from the shipped packs, and both loader paths absorb the miss rather than reporting it. The new `policies/overlays/` directory holds only the deal-scoped example; the manifest's only declared overlay is `policies/overlays/rb_ci.yaml`, which does not exist, and `PackManifestLoader.policy_files()` drops it with `logger.warning("Policy file not found: …")`. `cl_of_core` and `cl_sp_core` both declare `experts.playbook.diagnose_map: diagnose_map.yaml` and ship no such file; `load_diagnose_map()` returns `[]`, so a playbook expert built from either pack matches nothing and looks like a pack with no rules. Verified by running all three.
IMPORTANCE: medium -- the missing overlay is masked today only because `registry:` shadows `policy_files()`; fixing the finding above turns it into a silently absent institution overlay. The diagnose-map case is reachable wherever the two CL scaffolds' experts get built (jaci), not in plato.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125` — `purpose_specific_documents.required_documents` carries two line-wrap fragments, `schedule` (125) and `available)` (127), and the two real requirements they came from are gone; the file's own `description` says "Extracted verbatim from the source checklist" and that missing items become DATA_GAP hypotheses. A consumer enumerating this section (the manifest's `checklist:` is read by `assess_package()`) raises a gap for a document named "schedule" and never asks for the equipment-purchase or acquisition documents the source lists.
IMPORTANCE: medium -- nothing in japes reads `policies.checklist` (`PackManifestLoader` deliberately excludes it from `policy_files`), so the trigger is jaci's checklist consumer over this pack; wrong requirement text reaches an operator, and the omission is invisible.

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:658` — `DSCR-PPP-IL-2`'s `all_of` applicability includes `interest_rate > 8`, and `interest_rate` has no value on most files (`float | None = None`, jaci `dscr_schemas.py:171`, as ENCODING_NOTES §1b states). An Illinois individual-borrower loan of, say, $300,000 with no rate captured evaluates that limb INDETERMINATE, which dominates in the `all_of` verdict, and `gate_blocks()` (`jazzx_sdk/fabric/canonical/condition_evaluator.py`) blocks only on `POLICY_NOT_ACTIVATED` — so the gate excuses the rule, records NOT_APPLICABLE, and the prepayment-penalty buyout requirement silently does not apply. ENCODING_NOTES §1a chose `False` over `None` for `duplicate_ssn_cleared` and the two NMLS fields for exactly this reason; the same reasoning was not applied to this field, and §1b does not flag it as deferred.
IMPORTANCE: high -- silent permissive eligibility outcome with no signal to the caller. Reachable wherever this corpus is evaluated (jaci's `run_eligibility_assessment`/`compose_caps`, the Acra path); the plato copy is seed/display only, but the bytes are identical.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/workbook_layout.yaml:8` — the header states that every `key` must resolve against `chart_of_accounts.yaml`/`metrics.yaml` and that this "is this file's own load-time acceptance check (see workbook_layout.py's validator)". There is no `workbook_layout.py` in the repo, and `load_workbook_layout` (`jazzx_sdk/finance/workbook.py:169`) is `yaml.safe_load` plus `model_validate`; a layout with `key: totally_not_a_real_key` loads without complaint (verified). Every key in both layouts does resolve today, so nothing is currently wrong in the data.
IMPORTANCE: medium -- an author trusting the stated check ships a mistyped key that blanks a workbook row instead of failing at load; the two layouts here and jaci's reporter are the callers.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:1`, `plato/data/seed_packs/dscr_core/pack_manifest.yaml:8` — these ship inside the wheel and image and are written against another checkout: jaci commit hashes and version numbers, `cd jaci && pytest tests/scenarios/dscr/ -q`, `config/packs/dscr_core/`, `docs/LoanSamples/…` paths, and gitignored plan filenames (`docs/plans/Acra_DSCR_on_Platform_v2_…md`, `plan_JACI_CL_*`, `plan_JAPES_2_3_0_GOVERNED_WORKBOOK.md` in `reclassifications.yaml`, `workbook_layout*.yaml`, `spread_template_rb.yaml`, the CL manifests). None of it resolves from where it is installed.
IMPORTANCE: low -- documentation inside seed data; no code reads it. Named here because the plan filenames are unresolvable by anyone reading the image.

**[NOTE]** `plato/data/seed_packs/` — the bundled set is 5 of the 8 packs authored in jaci's `config/packs`: `aml_investigation_core`, `cre_underwriting_core` and `insurance_diligence_core` are absent, and size is not the reason (44K, 32K, 4K against ci-spread-core's 232K). "Initialize from bundled seed packs" therefore seeds a partial inventory.
IMPORTANCE: unknown -- I could not determine whether the omission is deliberate; nothing in `plato/seed.py` or the packs page states a selection rule.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(orphan-blob-gc)` (cited at `plato/packs_api.py:172`): claim holds — `DbPackVersionStore.delete` deletes only the row, and `test_delete_removes_one_version_and_keeps_the_archive` counts the blobs before/after. Nothing further owed.

## PARKED

- `test_every_bundled_seed_pack_loads` (`TODO(seed-pack-consumer-pointers)`) — deliberate `xfail(strict=True)`. Finding 2 below is against the shipped seed data, not the test.

---

**[DEFECT]** `plato/seed.py:96` / `plato/static/packs.html:331` — the "nothing found" case only gets a `reason` when the directory is *absent*. A directory that exists but holds no pack subdirectories returns `{"published": [], "skipped": [], "failed": []}` with no `reason`, and the page renders `"0 published, 0 already there"`. Trigger: `PLATO_SEED_PACKS_DIR=/path/that/exists/but/is/empty` (or a path whose children are all files, e.g. pointed one level too deep at a single pack's `policies/`… no, at a flat dir of yaml files) → Initialize reports a successful no-op. This is the exact misread the comment at `packs.html:329-330` says it fixed for `no such directory`, one input over: the empty root is the excluded boundary.
IMPORTANCE: medium -- reachable only through `POST {prefix}/packs/initialize` and any direct `seed_packs()` caller (one: `plato/packs_api.py:158`), and only in `local`/`dev-daily` where `_refuse_unless_relaxed` admits the write. Nothing outside this repo calls `plato.seed` today. Fails quietly rather than loudly, which is why it is not `low`.

**[DEFECT]** `tests/test_plato_packs_api.py:405` (against `plato/data/seed_packs/clinical-intake-core/pack_manifest.yaml:62`) — the deferral enumerates three packs carrying consumer-module pointers (`ci-spread-core`, `cl_of_core`, `cl_sp_core`) and misses a fourth: `clinical-intake-core` declares `modes.evaluator.class: jaci.scenarios.clinical_intake.modes.evaluator`. I grepped all five bundled manifests for `jaci`: four hit, only `dscr_core` is clean. The parked test cannot see this one either — it exercises `policy_registry` and `policy_profile()`, which never touch `modes:`, so the pack is neither loadable-in-image nor covered. Trigger: click Initialize on dev-daily, then resolve the `evaluator` mode for `clinical-intake-core` → `ModuleNotFoundError: jaci`. The part of the deferral that does not hold is its enumeration: a fix that follows the TODO literally will drop pointers from three packs and leave the fourth.
IMPORTANCE: medium -- shipped image data (all 47 files under `plato/data/seed_packs` are git-tracked and inside the `plato` package poetry includes, so they ride in the wheel). Reached by the Initialize button in relaxed postures plus anything that later resolves a mode on that pack; I could not determine whether any deployment actually loads `clinical-intake-core`, so the *second* half of the trigger is unestablished — the mis-enumeration itself is certain.

**[SYMMETRY]** `plato/seed.py:126` and `plato/packs_api.py:174` — neither write records who did it, while `plato/settings_store.py:221` already owns this exact helper (`_actor()`: `CallerIdentity.from_context()`, best-effort, "an unattributed row is worth more than a failed write"). `DbPackVersionStore.publish` takes `published_by_user_id` and the page renders a **By** column, so every seeded row shows `—` on the surface built to display it; the delete the module docstring calls "a governed act" logs pack and version with no actor. The consistent version is one kwarg on the `publish` call plus one `%s` in the warning, using `_actor` as it stands — smaller than what the diff already does.
IMPORTANCE: medium -- affects the two mutating routes this diff adds and the rows they write; `_seeded` in the new test file passes `published_by_user_id="operator-1"`, so the store side is already proven. No external consumer.

---

**[NOTE]** `plato/packs_api.py:41` vs `:65` — the docstring says `pack_inventory` "Never raises", but the `_version_row(...)` comprehension sits *outside* the per-pack `try` at `:55-58`; only `store.versions()` is guarded. Anything in the record→row transform (`published_at.isoformat()`, iterating `depends_on`, subscripting `manifest`) escapes as a 500 on the reporting route.
IMPORTANCE: low -- I could not establish a reachable trigger: with `DbPackVersionStore` all four fields come back typed from SQLAlchemy columns, so this is a guarantee the code does not enforce rather than a live bug. Every caller is `pack_inventory`'s two in-repo sites (`plato/packs_api.py:145`, the new test file).

**[NOTE]** `plato/seed.py:75-86` — the docstring says "Either name in `MANIFEST_NAMES`", but the `except` at `:81` does `return None` rather than `continue`, so a pack whose `pack_manifest.yaml` is unparseable YAML is skipped even when a valid `manifest.yaml` sits beside it.
IMPORTANCE: low -- one confusing skip line in the Initialize result; no shipped seed pack carries both names.

**[NOTE]** `plato/packs_api.py:146` — `TODO(if-match-post)` at `plato/posture.py:97` defers the POST question on the grounds that "nothing wires this dependency up yet… there is no route to fix against. Decide it with the first real POST route." This diff adds that route (`POST {prefix}/packs/initialize`), so the deferral's stated premise is now false while the decision is unmade and `require_if_match` still has no non-test caller.
IMPORTANCE: low -- no behaviour change today; `require_if_match` is not wired into any router, so nothing 428s. Callers checked: `plato/posture.py:149` (export), `tests/test_plato_preconditions.py` only.

VERDICT: BLOCK
