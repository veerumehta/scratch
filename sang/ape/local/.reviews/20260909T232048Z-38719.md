# adversarial review -- japes

- when: 2026-09-09T23:20:48Z
- range: HEAD~1...HEAD (05d8187)
- diff: 4293 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
Full suite: 5322 passed, 20 skipped.

## CHECKED

- `TODO(share-published-row-loop)` / `TODO(compose-per-pack-guard)` (`plato/packs/domain.py`, `plato/packs/loader.py`) — claim "both still test-only" / "no publish route" holds: nothing outside `tests/` constructs a `DbPackVersionStore` or calls `published_registries`/`published_domain_packs`; `plato/models.py:49` constructs one for metadata only.
- `TODO(publish-tick-tie)` (`jazzx_sdk/pack/store_db.py:66`) — holds, no caller invokes `publish`.
- `TODO(if-match-post)` (`plato/posture.py:97`) — holds, `require_if_match` is referenced only from `tests/test_plato_preconditions.py`.
- `TODO(swap-schema-placement)` (`plato/wiring_default.py:534`) — holds; boot goes through the alembic chain (`plato/wiring_local.py:304`), so `create_all` DDL is swap-only.
- `plato/wiring_local.py:_find_root` docstring claim "nothing above `/app/plato/` carries a `pyproject.toml`" — verified against `Dockerfile:81-99`: the runtime stage copies `plato/`, `jazzx_sdk/`, `common/` into `/app` but not `pyproject.toml`.

---

**[DEFECT]** `jazzx_sdk/pack/store_db.py:330` — the cache-hit branch trusts `.pack_manifest_root` without checking the unpacked tree still exists, so a cache entry whose files were removed while the marker survived is returned forever as a valid pack root, and the intact archive in the blob store is never re-read.

Trigger, reproduced: publish + `materialize` (writes `/tmp/japes-packs/<digest16>/`), then delete everything under that directory except `.pack_manifest_root` — the shape a `/tmp` cleaner produces, since the marker's atime is refreshed by every `materialize` while the extracted files' are not. The second `materialize` returns the same path with only the marker in it, no log, no raise. Downstream `plato/packs/domain.py:96` then logs "materialized to … with none of […]" and drops a pack that is fully published, and `plato/packs/loader.py:264` hands the empty root to `tenant_registries`, which is the unguarded path `TODO(compose-per-pack-guard)` describes. Recovery needs manual deletion of the marker. The digest re-verification that would catch a substituted archive is on the fetch path only, which this branch skips.

IMPORTANCE: medium -- I enumerated both `materialize` callers (`plato/packs/loader.py:264`, `plato/packs/domain.py:88`); both are test-only today and nothing in this repo constructs a `DbPackVersionStore` outside `tests/` and `plato/models.py:49`, so no shipped configuration reaches it. It becomes silent-pack-loss for the first wired caller, and the default `cache_root` (`tempfile.gettempdir()/japes-packs`) is exactly the directory a cleaner targets.

---

**[SYMMETRY]** `jazzx_sdk/queue/processor.py:772` — the diff refactored this served error envelope to a single `detail`, but left it unbounded, while `SERVED_REASON_LIMIT` (`jazzx_sdk/failures.py:262`) was introduced in this same diff as the answer for exception text that is served or stored. Its two siblings build the identical envelope the same unbounded way: `jazzx_sdk/server/app.py:291` and `jazzx_sdk/runtime/__init__.py:566`. All three put `redact_secrets(str(exc))` into `payload.data.error` and `error.message` of a response a caller reads; the consistent version is `[:SERVED_REASON_LIMIT]`, the same length as what is there. `server/app.py:287` explains why these use `redact_secrets` rather than `reason` (the type is already its own field) — that reason does not extend to the length bound.

IMPORTANCE: medium -- three call sites, all on the response path of a shipped surface (`/events` sync mode, the queue processor's terminal-error reply, the runtime handler). A multi-line driver traceback in an exception message goes out whole.

---

**[NOTE]** `plato/oidc.py:397` — the three other refusals in this method got `reason(exc, limit=SERVED_REASON_LIMIT)` in this diff; this one interpolates `kid` verbatim, and `kid` comes straight from `jwt.get_unverified_header(token)`, i.e. it is entirely attacker-supplied and unbounded.

IMPORTANCE: low -- nothing in this repo imports `TokenVerifier` (grepped all of `plato/` and `jazzx_sdk/`); it is a public surface for an external consumer to catch and turn into a 401, so no shipped configuration echoes it. Truncating `kid` is a complete fix, or a `TODO`.

**[NOTE]** `jazzx_sdk/pack/roots.py:21` — the module docstring says "`env()` tries them in order", but `resolve_packs_root` uses `env_text`, and its own inline comment at line 126 says citing `env`'s contract was wrong. In a module whose blank-vs-unset behaviour is pinned by a dedicated test (`test_a_blank_override_falls_through_to_discovery`), the header states the semantics the code deliberately does not have.

IMPORTANCE: low -- documentation only; the code and the test agree.

**[NOTE]** `jazzx_sdk/pack/store.py:54` — "sha256 over the archived asset bytes" is false for a manifest-only row, which the field two lines below documents as legal: `publish` computes `content_digest(manifest)` when `archive is None` (`store_db.py:167`). So `content_digest` means two different things depending on `content_ref`, and the same comment claims it is "what `materialize` keys its cache on" — a manifest-only row never reaches `_cache_dir`.

IMPORTANCE: low -- comment only; the manifest-only path raises `PackNotFound` before the digest is used for anything.

VERDICT: BLOCK

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
All 193 tests in the six touched files pass; ruff is clean. No defects found.

**CHECKED**

- `TODO(partial-registration-marker)` (`plato/database_api.py:82`) — holds. The only shipped call sites are `create_database_router`'s route (line 165/168), reached from `plato/app.py:219` with whatever `create_plato_app(db=...)` was given; every in-repo wiring passes `DatabaseHandle`, whose `.current` is a real `DbStore` with `.repository`. Only a duck-typed store passed to the exported function reaches the partial path, as the TODO says.
- `TODO(swap-schema-placement)` (`plato/wiring_default.py:533`) — holds, and the new `test_a_swapped_database_has_the_tables` does not weaken it: it swaps sqlite-to-sqlite, so the unqualified-DDL-on-postgres case the TODO defers is untouched by this diff.
- `swap_database` not disposing the retired store is deliberate and documented (`plato/wiring_default.py:521-526`, in-flight requests), so the two tests' `retired = wiring.db.current` + `finally` disposals are the right shape and not a missing library capability.

**Findings**

[NOTE] `tests/test_plato_schema_version.py:72` — the head is derived with `re.search(r'^revision: str = "([^"]+)"')`, but `plato/migrations/script.py.mako:16` emits `revision: str = ${repr(up_revision)}`, which Python renders with single quotes; the next `alembic revision`-generated file is skipped by both regexes (and by the `[0-9]*.py` glob at line 70 if alembic's default hex filename is kept), so `heads` is computed from the pre-existing tree and the final `assert head_revision() == heads.pop()` fails — the same "test fails over a number nobody updated" the new docstring says it removed. Trigger: run `alembic -c plato/alembic.ini revision -m "…"` and commit the file as generated.
IMPORTANCE: low -- test-only, and it fails loudly rather than passing wrongly. Checked: the three existing files under `plato/migrations/versions/` all use double quotes today, so nothing is broken now; nothing outside this repo reads this test.

[NOTE] `tests/test_plato_info_api.py:1612` — "`DbTurnRunStore` is the one of the six" is off by one: `plato/models.py:register_all` constructs seven stores (manifest, audit, pack version, session, turn run, model overlay, settings) covering eight tables. Line 1608 also restates "three of seven", the exact phrasing `plato/database_api.py:70-73` deliberately dropped ("it was written as 'three of seven', `pack_version` made it eight, and the comment did not move").
IMPORTANCE: low -- one test docstring; the assertion itself reads `TABLE_SCHEMAS` so it cannot go stale with the count.

[NOTE] `tests/test_plato_wiring_default.py:556` — `_fake_swap` never raises, so the branch `plato/app.py:191-193` calls out ("a swap that raised would otherwise record a wiring the process is not in, and the next write would then decline to fix it") is the one path of `_rewire` this test leaves uncovered: a raising fake followed by a fourth `rewire()` would pin that `_wired_to` stays stale and the swap is retried.
IMPORTANCE: low -- test coverage only; the ordering it would pin is already correct in `plato/app.py:189-193`, and `_rewire` is reached solely through `create_config_router`'s `on_change` at `plato/app.py:202`.

VERDICT: PASS
