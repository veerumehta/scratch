# adversarial review -- japes

- when: 2026-09-24T05:08:53Z
- range: HEAD~1...HEAD (f6aa182)
- diff: 245 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No defects found. I have two notes, and neither blocks the push.

[NOTE] jazzx_sdk/backend_names.py:18 :: posture-refusal-implied-for-backends -- The docstring says "the deployment-level refusals live in `jazzx_sdk.config.posture`". But `posture.py` only refuses on the tier and identity; it has no backend check. So a deployed service with `JAPES_BLOB_BACKEND=azuer` still writes to local disk behind rows that claim durability, and the only signal is one warning line.
Trigger: a deployed posture with any misspelled `*_BACKEND` env var. `grep -niE "sqlite|azure|blob|db_|conversation" jazzx_sdk/config/posture.py` finds nothing.
IMPORTANCE: medium -- It's reachable through all five env-driven fields in `fabric/config.py:190-230` in every deployed tier. The failure it describes is the silent-durability one the module names itself. It is still no worse than before this diff, which said nothing at all.

[NOTE] tests/test_backend_names.py:49 :: silence-test-covers-two-of-five -- `test_a_recognised_backend_says_nothing` checks only `BlobStore` and `DbStore`. The recognised defaults for guidance (`rag`), conversation (`in_process`) and the tracer (`noop`) are never checked for silence, so a later edit that drops a name from `GUIDANCE_BACKENDS` or `CONVERSATION_BACKENDS` would log on every boot and no test would fail.
Trigger: the default `KnowledgeFabric(...)` / `resolve_tracer()` path isn't included in the silence test.
IMPORTANCE: low -- Test-only; the worst case is a spurious warning in every process log. Adding a `TODO` is enough.

VERDICT: PASS
