# adversarial review -- japes

- when: 2026-09-09T20:19:25Z
- range: 9dbebcc..HEAD (since the last review) (c2f2e48)
- diff: 146 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` publish-tick tie — CHECKED, `TODO(publish-tick-tie)` at `pack/store_db.py:58` unchanged; not re-raised.
- WAS: `store_db.py:312` marker short-circuit — STILL OPEN; `:308-312` still returns the marker's text verbatim with no containment against `root`.
- WAS: `store_db.py:181` publish never opens `archive` — STILL OPEN; untouched.
- WAS: `pack/__init__.py:29` `__all__` — STILL OPEN; `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent from `:33-45`.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` — STILL OPEN; untouched.
- WAS: `store.py:54` "sha256 over the archived asset bytes" — STILL OPEN; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race — STILL OPEN; untouched.
- WAS: `store_db.py:140` `visibility` unvalidated — STILL OPEN; untouched.
- WAS: `store_db.py:364` `_unpack` docstring summary — STILL OPEN; untouched.
- WAS: `roots.py:99` candidate tuple duplicates `manifest_loader.py:103-106` — STILL OPEN; both still spell all three variants.
- WAS: `[SYMMETRY] prompt_loader.py:70` / `:128` unguarded `pack_id` join — FIXED; `bare_name` at `:75` and `:137`, new test at `test_pack_roots.py:181` fails against the old code (with `pack_id="/etc/passwd"` the old `resolve_mode_prompt` returned the priority-2 file instead of raising).
- WAS: `[NOTE] manifest_loader.py:97` "`Pack.from_pack_id`" — FIXED; `:97` now names `Pack.from_manifest`, which `pack.py:59-61` confirms calls `PackManifestLoader.from_pack_id`.
- WAS: `[NOTE] test_pack_roots.py:158` subset id list — FIXED; both parametrizations now take `_BAD_PACK_IDS` (`:23`).
- WAS: `[NOTE] wiring_local.py:124` `pyproject.toml`-only marker undocumented — FIXED; `:115-119` states the tradeoff, and the unreachability claim checks out (`Dockerfile:75-99`: runtime stage `WORKDIR /app`, no `pyproject.toml` copied into it).

## 2. New findings

`ruff` clean on the four files; `tests/test_pack_roots.py` 29 passed.

**[DEFECT] `jazzx_sdk/modes/prompt_loader.py:52`** — the `Raises:` block still lists only `FileNotFoundError`, but `:75` now raises `ValueError` for any `pack_id` that is not a bare name; `compose_mode_prompt`'s `:127` ("Raises FileNotFoundError if no base prompt exists") is incomplete the same way. Trigger: `resolve_mode_prompt("investigator", pack_id="a/b")`. This is the same class as the `roots.py:112` finding fixed last round, and here it is quoted downstream as *the* contract: `modes/base.py:178-180` justifies propagation by citing "Raises: FileNotFoundError: If no skill found at any level", and `tests/test_modes_framework.py:149` repeats it.
IMPORTANCE: low — I enumerated every caller: `base.py:116` (default resolver) and `:189`, whose `run()` wraps the access in a broad `except Exception`, so a bad id surfaces as `ModeResult(success=False)` rather than an uncaught escape; no in-repo caller catches `FileNotFoundError` narrowly. Out-of-repo consumers reading the docstring are the exposure I could not check.

**[SYMMETRY] `jazzx_sdk/evaluation/harness/config.py:147`** — `self.output_dir / self.pack_id / f"eval_{run_id}.json"` is a fifth member of the family the diff's own comment at `:72-75` says is now covered ("the path-returning members of this family were guarded first; these two are the ones…"), and it is a *write* target, not a read: `pack_id="../.."` puts eval output two directories above `output_dir`. The fix is the one line the diff applied twice, `bare_name(self.pack_id, what="pack id")`, or a pydantic validator on the field at `:47`.
IMPORTANCE: unknown — `grep -rn "EvaluationConfig("` over `jazzx_sdk/` and `plato/` finds only docstring examples, so nothing in this repo constructs one; whether any consumer builds it from a request payload I could not determine. If it is always developer-authored this is low.

**[NOTE] `tests/test_pack_roots.py:181`** — `test_a_pack_id_cannot_smuggle_a_file_into_a_prompt` covers `jazzx_sdk/modes/prompt_loader.py`, not `pack/roots.py`; `tests/test_modes_framework.py:160-195` is where the other `resolve_mode_prompt` cases live, and `_BAD_PACK_IDS` would have to move or be imported to follow it.
IMPORTANCE: low — test-only placement; the assertion itself is correct and fails against the old code.

VERDICT: BLOCK
