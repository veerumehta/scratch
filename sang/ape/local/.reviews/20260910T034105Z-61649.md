# adversarial review -- japes

- when: 2026-09-10T03:41:05Z
- range: origin/v2.5.1...HEAD (e7487fe)
- diff: 1788 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED**

- `profiles.py:214` `TODO(threshold-grid-provenance)` — holds. `_coerce_threshold_mappings` is registered on `"thresholds", "custom"` only; `tables: dict[str, dict[str, Any]]` is untouched and `get_table` returns the cell raw, exactly as before the diff. Not a regression.
- `policy.py:758` `TODO(activation-vs-active-rules)` — holds, and understates nothing. `executable_rules`/`activation_blocks`/`Rule.is_executable`/`Rule.activation_block` have zero non-test callers anywhere in `jazzx_sdk` or in `/Users/sangit/src/jaci`; the evaluation paths (`default.py:437`, `check.py:349`) both go through `active_rules()`.

(42 new tests pass; 265 related existing tests pass; ruff clean on all 16 changed files.)

---

**[DEFECT]** `jazzx_sdk/finance/validation.py:803` — the new warning findings put nothing in `detail` but `{"source": "compliance_warning"}`, dropping the `policy_id`/`rule_id` that `_not_activated_warning` had already formatted into the message string (`f"{policy_id}/{rule.rule_id}: {why}"`), while the sibling loop 12 lines above at :796 puts both in `detail`.
Trigger: one `check_compliance` result with two unapproved rules in the same policy, passed to `validate_package(compliance_result=...)`. In jaci's ci_spread "Why?" panel (`demo_page.py:2456`) the picker keys are `f"{f.detail.get('rule_id')} · {f.period}"`, so both findings key as `"None · FY2025"` and the dict silently keeps one; `source_view.py:152` then resolves `policy_id=None` and falls back to the raw warning string instead of the rule's clause.
IMPORTANCE: medium -- in-repo the only caller is `validate_package` (`validation.py:851`); outside, I enumerated the two jaci consumers above (`source_view.py:152` uses `.get()` so it degrades rather than raises, `demo_page.py:2456` drops the duplicate key). Fails visibly as a "None" label, silently as the dropped picker entry.

---

**[SYMMETRY]** `jazzx_sdk/agents/adjudication/segment.py:111` — the second of the three gate sites hardcodes `IndeterminateReason.POLICY_NOT_ACTIVATED` where `jazzx_sdk/fabric/graph/check.py:384` propagates `gate_outcome.indeterminate_reason`. `gate_blocks` is documented as "blocks rather than excuses", not as "is exactly POLICY_NOT_ACTIVATED", so the moment a second blocking reason is added to that predicate segment.py relabels it. Propagating is the same line length.
IMPORTANCE: low -- `gate_blocks` currently tests one enum member, so the two spellings agree today; the third site (`default.py:444`) doesn't build an outcome at all.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/policy_lint.py:53` — reaches through the underscore into `PolicyProfile._looks_authored`, making a private staticmethod part of the cross-module contract in the same commit that introduces it. `profiles.py:121` is the owner and already exposes the public classification (`get_threshold`, `Threshold.is_executable`); dropping the underscore is strictly less code than the reach-through.
IMPORTANCE: low -- one call site, in-repo only; the diff's own comment explains why `get_threshold`'s three-bucket search is wrong here, so the naming is the only part left.

---

**[NOTE]** `jazzx_sdk/finance/validation.py:800` — the loop converts *every* `ComplianceResult.warnings` entry into `DefectClass.POLICY_VIOLATION`/`Severity.POLICY`, but `base.py:50`'s own new description calls warnings "Mostly non-blocking, with one exception", and the function's docstring at :782 still says it converts `policy_violations` and that "`policy_id`/`rule_id` land in `detail`". The only in-repo producer is `DefaultPolicyExpert`, which emits nothing but not-activated warnings, so I could not reach the mislabelling; a pack expert overriding `check_compliance` can.
IMPORTANCE: unknown -- I could not establish whether any external `BasePolicyExpert` subclass populates `warnings`; grep found no other `ComplianceResult(...)` construction in this repo or in jaci.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:622` — `is_active` now folds in `effective_date`, so a policy with `status=ACTIVE` and a future `effective_date` (the pre-published-successor pattern the `Policy` docstring at :651 describes) lands in `stale_policies` with the reason "is not effective until 2099-…". "Stale" means the opposite of what happened. Enforcement is unaffected: `resolve()` builds `precedence_order` from `core_policy_ids`/`overlay_map`, never from `is_active`, so `check_compliance` still evaluates that policy's rules.
IMPORTANCE: low -- four in-repo readers of `Policy.is_active` (`governor.py:105` log line, `default.py:331`, `default.py:622`, `store/core.py:206`, `policy_registry.py:127`); no `Policy.is_active` usage in jaci. Reachable only by authoring a future `effective_date`, which no policy in this repo does.

**[NOTE]** `jazzx_sdk/agents/adjudication/segment.py:151` — the inserted clause splits the sentence into "short-circuits to a `NOT_APPLICABLE` outcome, unless its gate was blocked … and never reaches its main `condition`", which now reads as if the unless-case is what never reaches the condition. The line is 170 chars in a file whose other docstring lines wrap under 100 (ruff's `line-length = 100` is configured but E501 is not in the selected rules, so this is not a lint failure).
IMPORTANCE: low -- docstring only.

VERDICT: BLOCK
