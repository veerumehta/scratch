# adversarial review -- japes

- when: 2026-09-10T03:29:49Z
- range: origin/v2.5.1...HEAD (4858fc2)
- diff: 1715 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

`TODO(threshold-grid-provenance)` at `jazzx_sdk/fabric/canonical/profiles.py:216` — claim holds. `tables` is `dict[str, dict[str, Any]]`, `_coerce_threshold_mappings` is registered only on `thresholds`/`custom`, and `get_table` (profiles.py:222) is byte-identical in behaviour to before. A grid cell is served exactly as it was; the deferral is accurate.

---

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:121` — `_looks_authored` requires `"value" in found`, so a mapping that declares governance keys but no `value` is not recognised as a threshold: `get()` hands the raw dict to arithmetic and `get_threshold()` wraps it and reports `INSTITUTION_AUTHORED` / `ACTIVE` / `is_executable=True` about a block that literally says `status: seed`. Verified:

```
PolicyProfile.model_validate({'profile_id':'p','thresholds':{'dscr_min':
    {'provenance':'secondary_source','status':'seed','source':'trade grid'}}})
get()           -> {'provenance': 'secondary_source', 'status': 'seed', 'source': 'trade grid'}
get_threshold() -> value=<that dict>  INSTITUTION_AUTHORED  ACTIVE  is_executable=True
```

This is the exact failure `test_a_yaml_authored_threshold_is_gated` names as "an affirmative false statement about provenance, worse than the absent gate" — the guard just keys on the wrong field. The sibling guard `test_a_mapping_claiming_governance_it_cannot_back_is_refused_by_both` only covers `{value, status}`, not `{status}` alone.
IMPORTANCE: high -- reachable from the only shipped authoring path (YAML `from_yaml`/`model_validate`). Feeds `RatioEvaluator` (condition_evaluator.py:279), `evaluate_guard` (statemachine/schema.py:135) and `detect_cross_period_addback_inconsistency` (finance/validation.py:600); all three then see a dict rather than a refusal, and any governance report built on `get_threshold` states a provenance nobody authored.

**[DEFECT]** `jazzx_sdk/fabric/canonical/policy.py:739` — `executable_rules()`/`activation_blocks()` use `_all_rules()`, which does not apply the `legacy_alias` filter that `active_rules()` (policy.py:786, the list `check_compliance` actually iterates at default.py:425) applies. A sunset rule is therefore reported as executable by the new query and never evaluated by the evaluator. Verified: `Policy(status=ACTIVE, rules=[Rule(rule_id='LEGACY', parameters={'legacy_alias': True})])` → `active_rules() == []`, `executable_rules() == ['LEGACY']`, `activation_blocks() == {}` — i.e. "empty means every rule is live" is stated about a rule nothing runs. The two accessors also disagree in the other direction (`active_rules` ignores `exception_rules`, which `_all_rules` was added to include).
IMPORTANCE: low -- no in-repo call site for `executable_rules`/`activation_blocks` (grepped: none outside the new tests), and the method is new and unpushed so no external consumer exists yet. A `TODO` on `_all_rules` naming the legacy-alias divergence is a complete response.

**[SYMMETRY]** `jazzx_sdk/finance/validation.py:788` — `policy_violations_to_findings` iterates `compliance_result.policy_violations` only, so the new `allowed=False` with empty `policy_violations` yields zero `ValidationFinding`s and `validate_package(..., compliance_result=r)` (validation.py:836) reports a clean spread for a deal the policy expert refused. This is the in-repo instance of the caller shape the diff explicitly reasoned about ("a caller that surfaces `rationale` rather than iterating `policy_violations` lost the reason for the denial") — the fix went to `rationale` and this sibling reads neither `rationale` nor `warnings`. One extra loop over `compliance_result.warnings` emitting `DefectClass.POLICY_VIOLATION` findings is no more code than the rationale-join that landed.
IMPORTANCE: medium -- one in-repo caller (`validate_package`), reachable whenever a pack passes a `compliance_result` through; fails silently rather than loudly, but the pre-diff behaviour was equally silent so this is an incomplete fix, not a regression.

---

**[NOTE]** `jazzx_sdk/fabric/canonical/profiles.py:134` — `_coerce_threshold_mappings`' docstring says coercion happens only for a mapping with "``value``, at least one governance field, **and nothing else**", which contradicts `_looks_authored`'s own docstring three lines above ("Extra keys are *included*") and `test_an_unmodelled_key_does_not_switch_the_gate_off`, which asserts `{value, provenance, status, edition}` *is* coerced.
IMPORTANCE: low -- docstring only; the code and the test agree with each other.

**[NOTE]** `jazzx_sdk/agents/adjudication/segment.py:151` — `run_segment`'s docstring still says "an inapplicable rule short-circuits to a ``NOT_APPLICABLE`` outcome", which the new `gate_blocks` branch at segment.py:108 makes false for `POLICY_NOT_ACTIVATED`; `_apply_applicability_gate`'s docstring was updated in this diff and this one was not.
IMPORTANCE: low -- docstring only, same file, same change.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:76` — the `limbs` fallback searches one level, so a nested `all_of`/`any_of` (allowed up to `_MAX_COMPOSITE_DEPTH`) whose unapproved threshold sits in a grandchild yields the bare string `"not activated"` and drops the `threshold_key`; the outer limb's `inputs` is `{"limbs": [...]}` and has no `not_activated` key.
IMPORTANCE: low -- one less-specific warning line; the composite still withholds the allow and `limbs` still carries every child's own inputs.

VERDICT: BLOCK
