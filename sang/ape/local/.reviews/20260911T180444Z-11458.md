# adversarial review -- japes

- when: 2026-09-11T18:04:44Z
- range: dc5a73a..HEAD (since the last review) (1934b7d)
- diff: 34 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_mock_knowledge_hub_app.py :: seeded-body-unreported -- FIXED at lines 289 and 550; both `seeded` asserts now carry `.text`.
WAS: tests/test_mock_knowledge_hub_app.py :: changed-field-unasserted -- FIXED; mutating `mocks.py:1632-1633` to stop writing `name` now fails `test_a_null_field_is_not_supplied_not_changed[patch0-a null collection_id]` at line 388, and it is the only one of the 38 that fails, so the new assertion is the sole guard on the applied-value path.

No defects introduced. The comment at lines 386-387 checks out: with `name` dropped, `held` becomes `"e-1"` and the clash POST is a 409 under either name, so the trailing assertion cannot see it. `pytest tests/test_mock_knowledge_hub_app.py` → 38 passed; `ruff check` clean.

[NOTE] tests/test_mock_knowledge_hub_app.py:350 :: bare-201-asserts-remain -- the `, resp.text` diagnostic landed on 2 of the 14 create asserts in this file; 11 are still bare (98, 127, 151, 350, 378, 440, 454, 478, 494, 512, 530), and 350 is the worst of them because `first.status_code == 201 and second.status_code == 201` reports neither the body nor which of the two failed.
IMPORTANCE: low -- test-only, all call sites are in this file; cost of hitting it is one uninformative failure line.

[NOTE] tests/test_mock_knowledge_hub_app.py:388 :: applied-branch-one-case-only -- only `patch0` carries a non-null value, so the new branch is unreachable for the other three parametrized cases; a delegate that dropped a non-null `collection_id`, `ontology_id` or `entity_type` on PATCH is still unasserted here (the create-side sibling at line 400 sweeps all four).
IMPORTANCE: low -- test-only; the four `if X is not None` writes in `mocks.py:1632-1641` are one shape, and the `name` arm now covers that shape.

VERDICT: PASS
