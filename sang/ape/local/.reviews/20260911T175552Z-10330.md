# adversarial review -- japes

- when: 2026-09-11T17:55:52Z
- range: 54e69f4..HEAD (since the last review) (2b0a7eb)
- diff: 13 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: refusing-line-points-upward -- FIXED; line 234 now names "the `is None` in the comprehension below", and that predicate is real and five lines down (`entity_data.get(f) is None`, line 238), so a null lands in `missing` and answers 400.
WAS: tests/test_mock_knowledge_hub_app.py :: nulled-field-unasserted -- STILL OPEN; `test_a_null_field_is_not_supplied_not_changed` (tests/test_mock_knowledge_hub_app.py:381-383) still asserts only `collection_id` and the `name` arm, so the two added `ontology_id`/`entity_type` cases would pass with the delegate's `is not None` guards on those fields removed.

No new findings. The diff rewrites one comment clause and touches no executable line, so the previous round's `pytest tests/test_mock_knowledge_hub_app.py` result stands; I did not re-run it. `ruff check` on the file passes (E501 is not in the selected set), though line 234 is now 120 chars against the declared `line-length = 100`.

VERDICT: PASS
