# adversarial review -- japes

- when: 2026-09-24T05:32:16Z
- range: 8a72aae..HEAD (since the last review) (5534f5e)
- diff: 985 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Nothing is left open: the earlier finding is still fixed, and this diff adds no defects.

WAS: tests/test_backend_names.py :: db-default-not-exercised -- FIXED. The tree still builds `DbStore(backend="common")`, which is the constructor default.

What I checked in this diff:
- **Leftover old paths:** no mention of `plato.reference`, `plato.packs.draft`, `plato_model_overlay` or `test_plato_pack_draft` remains in this repo's code, tests, config or docs (`docs/plans` and `docs/status` excluded). None appears in jaci, juno or jazzx-assistant either.
- **Tenant constant:** `jazzx_sdk.pack.store.DEFAULT_TENANT` is `"default"`, the same value the old module defined. `draft_db` now imports it from `pack.store` in the same package and still lists it in `__all__`, so importing it from `draft_db` keeps working.
- **`OVERLAY_SOURCE` rename:** the value is only used as the source label on the in-memory `overlay_registrations()` record, and it is never written to the database. Its only in-repo reader is the test, which compares against the constant rather than a literal string.
- **Plato text in the moved SDK files:** `jazzx_sdk/llm/model_overlay_db.py` and `jazzx_sdk/pack/draft_db.py` no longer mention "plato".
- **Count in `as_utc`:** the docstring still names seven modules, which is correct once `model_overlay` is replaced by `llm/model_overlay_db`.
- **Split tests:** the store tests moved to `tests/test_model_overlay.py` unchanged apart from the import path. The route file uses its own model name, so the two files' pricing fixtures don't collide.
- **Lint and tests:** ruff passes on every touched Python file. 131 tests pass across `test_model_overlay`, `test_plato_model_overlay`, `test_pack_draft` and `test_plato_packs_api`.

VERDICT: PASS
