# adversarial review -- japes

- when: 2026-09-09T20:03:06Z
- range: 75cac21..HEAD (since the last review) (723cdd1)
- diff: 179 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` `published_at.desc()` tie — CHECKED, still not open; `TODO(publish-tick-tie)` at `pack/store_db.py:58` unchanged, claim still holds.
- WAS: `store_db.py:312` marker short-circuit — **STILL OPEN**; `pack/store_db.py:308-312` still returns the marker file's text verbatim, no containment against `root`, no digest re-verify.
- WAS: `store_db.py:181` publish never opens `archive` — **STILL OPEN**; `publish` still content-keys and `put`s arbitrary bytes at `:182-183` without a zip open; first failure is `_unpack` at materialize.
- WAS: `pack/__init__.py:29` `__all__` — **STILL OPEN**; the four `roots` symbols are exported, `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` — **STILL OPEN**; `jazzx_sdk/failures.py:253` defaults `limit=None` and all four oidc sites call it bare.
- WAS: `store.py:54` "sha256 over the archived asset bytes" — **STILL OPEN**; `pack/store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race — **STILL OPEN**; blob written at `:183` before the row, no unique-violation handling.
- WAS: `store_db.py:140` `visibility` unvalidated — **STILL OPEN**; string goes straight into the row at `:195`.
- WAS: `store_db.py:364` `_unpack` docstring summary — **STILL OPEN**; `:364` summary still says "the directory holding ``pack_manifest.yaml``" while `:366` and the body accept either `_MANIFEST_NAMES` entry.
- WAS: `loader.py:214` domain pack / manifest-only row / `:196` prose / `:236` debug level — FIXED (unchanged this round).
- WAS: `tests/test_pack_store.py:353`, `:326`, `:411`, `:324`, `:424` — FIXED (unchanged this round).
- WAS: `[DEFECT] roots.py:100` unvalidated `pack_id` join — FIXED; `bare_name(pack_id, what="pack id")` at `roots.py:121`, parametrized test green.
- WAS: `[SYMMETRY] roots.py:81` `env` vs `env_text` — FIXED; `env_text(*env_names)` at `:107`, docstring now cites `env_text`.
- WAS: `[SYMMETRY] wiring_local.py:118` still counting — **REGRESSED**; the migration to `discover_root` landed and broke `_find_root()` in this checkout (see below).
- WAS: `[NOTE] roots.py:57` `.exists()` accepts a `config` *file* — FIXED for `config` via the `config/` marker and `_marker_present`; not for the other marker (see NOTE below).
- WAS: `[NOTE] roots.py:99` candidate tuple duplicates `manifest_loader.py:93-96` — **STILL OPEN**; three spellings still written out in both places.
- WAS: `[NOTE] test_pack_roots.py` no discovery success path — FIXED; `test_the_discovered_root_resolves_to_the_packs_subdir` at `:113`.

## 2. New findings

17 passed in `tests/test_pack_roots.py`, ruff clean on the three touched files.

**[DEFECT] `plato/wiring_local.py:122`** — `discover_root(Path(__file__))` returns `None` in this repo, because `DEFAULT_MARKERS` now requires a `config/` directory and the japes checkout has none (`ls -d config` → nothing). So `_find_root()` falls through to `Path.cwd().resolve()` in a source/editable checkout, which is exactly what `parents[1] + pyproject.toml` prevented. Measured: `cd /tmp && PYTHONPATH=/Users/sangit/src/japes python -c "from plato.wiring_local import _find_root; print(_find_root())"` → `/private/tmp`; before the diff it printed the checkout. The docstring's new claim at `:112` ("A discovered root exists only in a source or editable checkout") is false for the one checkout that ships this file.

`tests/test_plato_wiring.py:163` asserts `_find_root() == parents[1]` and passes only because pytest is invoked from the repo root — cwd is now the answer, so the assertion no longer tests what its docstring says.

IMPORTANCE: high — `_REPO` is module-level at `:125` and feeds `_anchored` (`:140`), `DB_PATH`, `CACHE` (`:144-145`), `REPO` (`:152`), `_DEFAULT_PROFILE = REPO.parent / "jazzx-assistant/..."` (`:156`) and `cwd=REPO` for the alembic subprocess (`:302`). Launching `python -m plato` from anywhere but the repo root now writes `.plato-local.db` and `.plato-local-pack` into cwd and runs alembic there — the precise "alembic migrates one, the runtime creates an empty other" failure `_anchored`'s own docstring at `:130-138` was written against, and the sibling-profile default points at a nonexistent `<cwd>/../jazzx-assistant`. Reachable in every local-wiring configuration; nothing outside this repo imports `plato.wiring_local`.

**[DEFECT] `jazzx_sdk/pack/roots.py:112`** — the summary line still promises "One pack's directory under ``packs_root``, or :class:`PackRootNotFoundError`", but `bare_name` at `:121` raises `ValueError` for exactly the ids the paragraph below then describes. `resolve_pack_root("", packs)` raises `ValueError`, not `PackRootNotFoundError`. `PackRootNotFoundError`'s own docstring at `:45-49` says it exists precisely so callers can decide what to do ("a demo page can render nothing, a conductor cannot start"); a caller that catches it and renders nothing now gets an unhandled `ValueError` instead.

IMPORTANCE: medium — grepped `resolve_pack_root` across `jazzx_sdk/`, `plato/`, `tests/`: only `tests/test_pack_roots.py` calls it in-repo, so nothing shipped here breaks yet. It is in `jazzx_sdk.pack.__all__` for the out-of-repo demo-page/conductor callers the module docstring names, and any of them taking a pack id from a request reaches it. Fails loudly.

**[SYMMETRY] `jazzx_sdk/pack/manifest_loader.py:92-96`** — `from_pack_id` builds the same three `packs_root / <spelling>` candidates with no validation, and `roots.py:114` names it as "the same reconciliation". `from_pack_id("", root)` yields `root / "pack_manifest.yaml"` and `from_pack_id("../secret", root)` reaches outside `packs_root` — the two cases the diff just closed one door on. One `bare_name(pack_id, what="pack id")` line, the same as the diff's.

IMPORTANCE: medium — `jazzx_sdk/pack/pack.py:60` (`Pack.from_pack_id`) is a shipped caller, and `Pack` is the primary access object exported from `jazzx_sdk.pack`; the rest of the call sites are `tests/test_pack_composition.py`. I could not establish whether any out-of-repo caller passes a request-derived pack id to `Pack.from_pack_id`.

**[NOTE] `jazzx_sdk/pack/roots.py:61`** — `_marker_present` only tightens the markers carrying a trailing `/`, so a *directory* named `pyproject.toml` still satisfies the `pyproject.toml` marker via `.exists()` — the same file-vs-directory confusion the diff fixed for `config`. The new `test_a_config_file_is_not_a_config_directory` at `tests/test_pack_roots.py:128` covers one direction only.
IMPORTANCE: low — needs a directory named `pyproject.toml` beside a real `config/`; no such layout in this repo or in the consumers the module docstring names.

**[NOTE] `jazzx_sdk/pack/roots.py:37`** — `DEFAULT_MARKERS` is in `__all__`, so the trailing-`/` convention is now public API, but `discover_root`'s docstring at `:66` still says only "containing every marker"; an out-of-repo caller passing `markers=("pyproject.toml", "config")` silently gets the pre-diff file-accepting behaviour with no signal that the spelling matters.
IMPORTANCE: low — one in-repo caller of `discover_root` (`plato/wiring_local.py:122`, default markers); the cost off-repo is the old behaviour, not a new one.

VERDICT: BLOCK
