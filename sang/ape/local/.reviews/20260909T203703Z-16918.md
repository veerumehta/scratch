# adversarial review -- japes

- when: 2026-09-09T20:37:04Z
- range: 1a6ef52..HEAD (since the last review) (71f79cf)
- diff: 330 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` publish-tick tie — CHECKED, `TODO(publish-tick-tie)` unchanged; not re-raised.
- WAS: `store_db.py:312` marker short-circuit — STILL OPEN; `:308-312` still returns the marker's text verbatim, no containment against `root`.
- WAS: `store_db.py:181` publish never opens `archive` — STILL OPEN; file untouched since HEAD~1.
- WAS: `pack/__init__.py:33-46` `__all__` — STILL OPEN; `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` — STILL OPEN; untouched.
- WAS: `store.py:54` "sha256 over the archived asset bytes" — STILL OPEN; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race — STILL OPEN; untouched.
- WAS: `store_db.py:140` `visibility` unvalidated — STILL OPEN; untouched.
- WAS: `store_db.py:364` `_unpack` docstring summary — STILL OPEN; untouched.
- WAS: `roots.py:99` candidate tuple duplicates `manifest_loader.py:103-106` — STILL OPEN; both still spell all three variants.
- WAS: `[DEFECT] config.py:141` validator docstring vs. assignment — FIXED; `validate_assignment=True` at `:118`, verified: `c.pack_id = '../../..'` now raises `ValidationError`.
- WAS: `[NOTE] config.py:161` unvalidated `run_id` — FIXED; `:169` guards it, verified `'../../x'`, `'a/b'` and `''` all raise `ValueError`.
- WAS: `[NOTE] test_modes_framework.py:212` unused `monkeypatch` — FIXED.

## 2. New findings

71 passed across the four touched test files; `ruff` clean.

**[DEFECT] `jazzx_sdk/pack/pack.py:188`** — an unknown `profile_id` returns `None`, the same answer the docstring reserves for "the pack declares none", so a caller's wrong id is returned as an ordinary result; every other member of this family (`PolicyProfile.get`, `get_table`) is deliberately fail-closed and raises naming the profile. Verified: a pack declaring `acme_v1` answers `pack.policy_profile("typo_v1") -> None`, and the caller cannot tell that from a profile-less pack.
IMPORTANCE: medium — no tracked caller passes an id today (`grep policy_profile` over `jazzx_sdk/`, `plato/`, `tests/` and `/Users/sangit/src/jaci`: only `tests/test_pack_policy_profile.py:106`); the one live consumer, untracked `plato/packs/domain.py:104`, reads `policy_profiles` not the id lookup. This is the pre-caller window.

**[DEFECT] `jazzx_sdk/pack/manifest_loader.py:245`** — the `dir:` glob takes every `*.yaml`/`*.yml`, unlike the sibling it is copied from (`policy_files`, `:196-197`, which filters `is_policy_document(p)` precisely because "a pack's policy folder legitimately holds other assets"); one non-profile YAML in the folder makes the whole accessor raise. Verified: adding `profiles/README.yaml` (`note: ...`) to a pack with a valid `acme.yaml` makes `pack.policy_profiles` raise `ValidationError`, and it takes the *named* lookup down with it — `pack.policy_profile("acme_v1")` raises too.
IMPORTANCE: medium — `jaci/config/packs/dscr_core/profiles/` currently holds exactly one file, so no shipped pack triggers it; `plato/packs/domain.py:105` catches `Exception` and degrades `/info` to `policy_profiles_error`, so the operator surface shows a type name rather than the pack's profiles. Any pack author who drops a note or a schema file beside a profile hits it.

**[DEFECT] `jazzx_sdk/pack/pack.py:195`** — the default branch re-reads `root / default_ref` by path instead of looking the profile up in the dict it just built, so it contradicts `profile_files`'s own contract that a missing ref is warned-and-skipped. Verified: manifest `profiles: {dir: profiles, default: profiles/gone.yaml}` with one real profile in the dir logs "Policy profile not found", `policy_profiles` succeeds with 1 entry, and `policy_profile()` raises `FileNotFoundError` instead of falling through to the single-profile branch that is right there at `:196`.
IMPORTANCE: medium — reachable only by a manifest typo, and it fails loudly; in `plato/packs/domain.py` the asymmetry shows as `/info` listing the profiles while a pack load using `policy_profile()` explodes. No out-of-repo caller (no `policy_profile` hits in jaci).

**[SYMMETRY] `jazzx_sdk/pack/manifest_loader.py:208-247`** — `profile_files` restates `policy_files` (`:174-201`) line for line: same `refs` list, same `self._root / ref` + `exists()` + warn loop, same `dir:` glob with the same `p not in files` dedup. The consistent version is one private `_declared_files(refs, directory, *, keep=None)` on this loader that both call, which is smaller than the copy the diff adds — and it is where the missing `keep=is_policy_document`-style filter above would have had a single home.
IMPORTANCE: medium — two call sites in this file; a third asset kind will copy whichever it finds first.

**[NOTE] `tests/test_pack_policy_profile.py:26`** — `_DSCR = Path("/Users/sangit/src/jaci/config/packs/dscr_core")` puts one machine's layout and the consumer repo's name in committed code, and there is no env override, so the one test that checks the case this change exists for (`:119`) skips everywhere except that checkout; it also asserts jaci's data (`profile_id == "dscr_v6_15_2026_v1_1"`, `"1.20"`), which jaci can change without this repo noticing.
IMPORTANCE: low — test-only; the shipped-code paths it covers are covered by the tmp_path tests above it.

**[NOTE] `jazzx_sdk/evaluation/harness/config.py:150`** — `get_output_path` now raises `ValueError` on a bad `run_id` but its docstring still lists only `Args:`/`Returns:`; this is the same omission the previous round raised as a `[DEFECT]` on `prompt_loader.py:52` and that `:56-57` fixed.
IMPORTANCE: low — one in-repo caller (`harness/runner.py:225,231`) passes a generated id; the contract matters to out-of-repo callers of an exported model.

**[NOTE] `jazzx_sdk/evaluation/harness/config.py:118`** — neither of this diff's two config guards has a test: nothing asserts that assignment now validates, nor that `get_output_path("../../x")` raises. Both are one-liners in `tests/` and both were confirmed by hand here, so the guards are live but unpinned.
IMPORTANCE: low — test-only; a later `model_config` edit could drop `validate_assignment` with the suite still green.

VERDICT: BLOCK
