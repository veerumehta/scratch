# adversarial review -- japes

- when: 2026-09-24T04:40:36Z
- range: a57c3f3..HEAD (since the last review) (09b3370)
- diff: 235 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_plato_database_rows.py :: literal-sql-restated -- FIXED
WAS: plato/api/database.py :: redundant-second-cache -- FIXED
WAS: scripts/run_tests.sh :: stale-test-path-example -- FIXED
WAS: jazzx_sdk/net_safety.py :: stale-oidc-path-comment -- FIXED
WAS: jazzx_sdk/oidc.py :: plato-scoped-prose-in-sdk -- FIXED
WAS: jazzx_sdk/pack/seed.py :: service-name-in-sdk-docstring -- FIXED

[NOTE] tests/test_plato_packs_api.py:25 :: sdk-tests-in-plato-file -- `pack_inventory` now lives in `jazzx_sdk.pack`, but its five direct tests (lines 87, 109, 121, 148, 1182) are still in the Plato API test file. The sibling SDK lift in this branch put its tests beside the SDK module (`tests/test_oidc.py` for `jazzx_sdk/oidc.py`).
Trigger: someone who changes `jazzx_sdk/pack/inventory.py` and runs only the pack tests misses these five. Behaviour doesn't change, since the same tests still run in the full suite.
IMPORTANCE: low -- test location only. I found one shipped caller, `plato/api/packs.py:350`, plus the tests above. Nothing outside this repo imports `jazzx_sdk.pack.inventory` yet.

Checked in this diff, nothing found:
- It's a straight move. The function body and `_version_row` are the same logic, and the only changes are log prefixes, prose, and inlining the `ordered` variable.
- There are no stale references left. Grepping `jazzx_sdk`, `plato`, `tests` and the repo docs finds no `plato.api.packs.pack_inventory` import and no `_MANIFEST_KEYS`.
- `plato/api/packs.py` still uses `Any`, `reason` and `SERVED_REASON_LIMIT` at lines 58, 168, 184 and 285, so no imports were orphaned. Ruff passes on both paths.
- The route still returns the SDK result unchanged, and the withheld path still carries `packs=[]`.

VERDICT: PASS
