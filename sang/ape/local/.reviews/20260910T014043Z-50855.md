# adversarial review -- japes

- when: 2026-09-10T01:40:43Z
- range: origin/v2.5.1...HEAD (369244e)
- diff: 951 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**[DEFECT]** `jazzx_sdk/fabric/canonical/policy.py:545` — when *both* statuses are non-ACTIVE and differ, `_blocking` returns the rule's own and names the rule, so `effective_status` reports a status looser than the policy's and `activation_block` blames the wrong party. Verified: `Rule(status=DRAFT).effective_status(PolicyStatus.ARCHIVED)` → `DRAFT`, and `activation_block(ARCHIVED)` → `"rule 'R1' may not execute: rule is draft, not active"` — no mention that the policy is archived. This directly contradicts `effective_status`'s "its policy's, unless its own is narrower" (policy.py:552) and "never promotes itself above it": there is no ordering behind "narrower", only an `is not ACTIVE` test. Same for a DRAFT rule under a SEED or SUSPENDED policy.
IMPORTANCE: medium -- `effective_status`/`activation_block`/`executable_rules` have no callers in `jazzx_sdk/` (grepped; definitions + the two new test files only), so nothing shipped reads it yet; the tests parametrize only the ARCHIVED-rule-in-DRAFT-policy direction, so the inverse is uncovered, and the first consumer to adopt the opt-in query gets a wrong sentence in a governance report.

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:134` — the coercion fires on *any* mapping whose keys are a subset of `Threshold`'s, so a one-key `{value: N}` under `custom`/`thresholds` becomes a SEED `Threshold` and `get` now refuses data that previously resolved. Verified: `PolicyProfile(profile_id='p', custom={'window': {'value': 7}}).get('window')` raises `ThresholdNotActivated ... is seed (institution_authored)`; `get_threshold` likewise reports it as an institution-authored threshold. `test_an_unrelated_mapping_is_left_alone` only covers the extra-key side of the guard.
IMPORTANCE: low -- I enumerated every `profile_id:` YAML across the sibling checkouts (juno, kernel, assistant, jazzx-assistant, knowledge-hub, macer, k9 have none; jaci has exactly one, `config/packs/dscr_core/profiles/dscr_profile.yaml`, whose `custom` holds only scalars and lists), so no shipped profile triggers it, and it fails loudly when one does.

**[DEFECT]** `jazzx_sdk/finance/validation.py:600` — `ThresholdNotActivated` subclasses `ValueError`, and this pre-existing `except ValueError` sits around `Severity(profile.get("cross_period_addback_severity"))`, so a refused threshold is swallowed and the finding silently gets `_DEFAULT_SEVERITY` instead of surfacing that the institution's figure is unapproved. Trigger: a profile authoring `cross_period_addback_severity: {value: warn, status: seed}`. `statemachine/engine.py:94` and `condition_evaluator.py:269` both handle the new exception explicitly; this is the third `profile.get` site and it converts the refusal into an ordinary result.
IMPORTANCE: medium -- one call site (`detect_cross_period_addback_inconsistency`, reached only when a caller passes `profile=`); no shipped profile authors that key, but the failure is invisible once one does, which is the mode this feature exists to prevent.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/profiles.py:184` — `get` and `get_threshold` gate on `Threshold`, but `get_table`, the third resolver on the same class and the one `MatrixEvaluator` uses (`condition_evaluator.py:368`), has no gate and `tables` is not in the `_coerce_threshold_mappings` field list, while `Threshold.value`'s own description says "decimal string, int, **or table cell**". The transcribed-from-a-source grid is exactly jaci's `max_cltv_grid`, i.e. the content this feature governs. Either extend the validator + `isinstance` check to `tables` (one nested loop, comparable to what the diff already writes) or drop "or table cell" from the description.
IMPORTANCE: medium -- reaches `MatrixEvaluator` in the DSCR pack; today an authored `{value: …, status: seed}` cell returns the raw dict and dies at `float(cell_value)` (condition_evaluator.py:388) rather than refusing.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/condition_evaluator.py:447` — every leaf evaluator in this file now sets `indeterminate_reason`, but the composite `all_of`/`any_of` outcome does not, so a `RatioCondition` with a seed threshold nested in an `AllOfCondition` yields `verdict=INDETERMINATE, indeterminate_reason=None` and a caller filtering on `POLICY_NOT_ACTIVATED` misses it (the string survives only inside `limbs[i]["inputs"]`). Carrying the first non-None child reason is smaller than the seven leaf edits the diff already made.
IMPORTANCE: medium -- affects any pack rule using a composite condition; `experts/policy/default.py:429` and `fabric/graph/check.py:246` currently branch on `verdict` only, so nothing breaks today.

**[NOTE]** `jazzx_sdk/fabric/canonical/policy.py:459` — `RuleOutcome.not_activated` has no caller anywhere in `jazzx_sdk/` or the new tests, and its default `status="UNABLE_TO_EVALUATE"` introduces a third pack-vocabulary string next to the `"INDETERMINATE"` every other construction in `condition_evaluator.py` uses.
IMPORTANCE: low -- dead on arrival; nothing consumes the status string yet.

**[NOTE]** `jazzx_sdk/fabric/canonical/policy.py:445` — `_reason_belongs_to_indeterminate` is a construction-time validator and `RuleOutcome` sets no `validate_assignment`, so the contradiction the docstring calls impossible is one assignment away: building an INDETERMINATE outcome with a reason then setting `o.verdict = Verdict.SATISFIED` leaves the reason attached.
IMPORTANCE: low -- no in-repo code mutates a built `RuleOutcome`; the field would just be misleading in a serialized outcome.

VERDICT: BLOCK
