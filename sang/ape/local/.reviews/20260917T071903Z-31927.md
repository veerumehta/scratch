# adversarial review -- japes

- when: 2026-09-17T07:19:03Z
- range: 3c3e755..HEAD (since the last review) (f2626f6)
- diff: 385 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/wiring/default.py :: activation-failure-marks-unready -- FIXED. `_registries(..., record=False)` raises instead of noting, the activation call site passes it, and `test_a_failed_activation_does_not_take_the_replica_out_of_rotation` asserts `boot_notes()` is unchanged (it fails against the old code: the loader raises `PackLoadError` on that archive, so `record=True` would have added a note).
WAS: plato/api/packs.py :: persist-order-reversed-in-docstring -- FIXED. The docstring now says swap-then-persist, matching lines 402/411 and the inline comment.
WAS: plato/api/packs.py :: upload-docstring-says-no-activation -- FIXED in this file; the factory docstring now lists both new routes. The same sentence survives in `docs/DEPLOYMENT_ENV.md`, reported below as its own finding.
WAS: .env.dev-daily :: demo-tenant-in-deployed-template -- FIXED. Back to `<comma,separated,ids>`; `tests/test_dev_daily_env_template.py` still passes (13/13).
WAS: plato/wiring/default.py :: asyncio-run-without-loop-guard -- FIXED. `get_running_loop()` is checked before `asyncio.run`, and no coroutine is created on the in-loop branch, so nothing is left un-awaited.
WAS: plato/wiring/default.py :: tenant-sentinel-list-misses-new-default -- FIXED. Both the docstring and `tests/test_plato_wiring_default.py:919` now list four.
WAS: plato/api/config.py :: active-pack-absent-from-catalog -- FIXED. `PLATO_ACTIVE_PACK` is a `SettingField` with `editable=False`, and `settings_api.py:215` refuses a PATCH to it server-side, so the read-only claim is enforced rather than only rendered.
WAS: tests/test_plato_packs_api.py :: activation-test-name-overstates -- FIXED. Renamed to `test_the_activate_route_carries_the_tenant_and_reports_the_swap`, the unused fixture and publish are gone, and the docstring points at the wiring test by its real name.

[DEFECT] docs/DEPLOYMENT_ENV.md:171 :: deployment-doc-says-no-activation -- the operator-facing env reference still carries the exact sentence this diff corrected in `plato/api/packs.py`: "serving an uploaded pack needs a restart until activation ships."
An operator reading the "Pack upload" section after uploading a version restarts the replica instead of calling `POST {prefix}/packs/{pack_id}/{version}/activate`, which the same branch ships and which the packs router docstring now advertises. The two renderings of one statement disagreed before this diff and still do; the doc is also the only place that would have told an operator the route exists, and it does not mention it.
IMPORTANCE: medium -- doc-only, fails by wasted restarts rather than wrong data. Checked both occurrences of the claim in that file: line 41's table row is narrowly about `PLATO_PACK_DIR` and is still true, line 170-172 is not. No test holds `DEPLOYMENT_ENV.md` against the routes (the template guard in `tests/test_dev_daily_env_template.py` covers `.env.dev-daily` only), so nothing else catches this.

[DEFECT] plato/wiring/default.py:662 :: in-loop-note-contradicts-cited-sibling -- the new in-loop branch calls `record_boot_note` and says it does so "the way `_apply_durable_settings` handles the same situation"; that sibling deliberately does the opposite, and its `_durable_settings_degraded` docstring says a note here "cannot be right".
`build()` called from inside a running loop with `PLATO_ACTIVE_PACK` set: the note reaches `readiness_provider`, `/health` answers `ready: false`, and `python -m plato --health` exits non-zero for the life of the process, while the replica serves `PLATO_PACK_DIR`'s pack correctly. `_apply_durable_settings` (line 1076-1082) meets the identical condition with `_durable_settings_degraded`, which logs at warning and records nothing, because -- in its own words -- there is no "reported but healthy" channel. Either the citation is wrong or one of the two paths is.
IMPORTANCE: low -- no shipped serving role reaches it: `build()` is resolved from `PLATO_WIRING` before uvicorn starts, and nothing in `plato/` calls it from async code (`_rewire` swaps the database instead). Reachable today only by a test or an embedding consumer building mid-run.

[NOTE] tests/test_plato_wiring_default.py:969 :: profile-shape-not-what-fails -- the comment says the archive cannot load because "a profile that is not a mapping", but `pack_registries` raises `PackLoadError: manifest.yaml: 7 validation errors for AssistantManifest` before the profile is read, so the three-key manifest is what fails and the profile line never participates.
IMPORTANCE: low -- test-only, and the test still proves what it claims (the raise happens and no note is recorded); the comment names the wrong half, so an edit that fixed the manifest would leave the test green for a reason nobody intended.

[NOTE] docs/DEPLOYMENT_ENV.md:41 :: active-pack-absent-from-env-doc -- `PLATO_ACTIVE_PACK` is now read at boot by `plato/wiring/default.py:647` and written durably by `_persist_active_pack`, but appears in no table in the env reference, so the variable an operator would find in `plato_setting` or on `/config` is undocumented.
IMPORTANCE: low -- doc-only; grepped the whole file, the variable occurs nowhere in it.

VERDICT: BLOCK
