# adversarial review -- japes

- when: 2026-09-09T02:57:05Z
- range: origin/dev...HEAD (3277926)
- diff: 841 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED**
- `TODO(jwks-error-detail)` (plato/oidc.py:288): holds. `grep TokenVerifier` across `jazzx_sdk/`, `plato/`: only `plato/oidc.py`'s own definition, its `__all__`, and `tests/test_plato_oidc.py`. No router or middleware constructs one, so the JWKS URL cannot reach a 401 body today.
- `TODO(malformed-jwks-clock)` (tests/test_plato_oidc.py:~636): holds. The stamp it can't distinguish is the same `_KeyCache(..., fetched=True)` line in the same `except` block that `test_a_down_issuer_is_not_refetched_on_every_verify` drives with an advancing clock.

---

**[DEFECT]** `jazzx_sdk/failures.py:100` — `_URL_USERINFO_RE` requires at least one character before the `:` and forbids `/` in the password, so two ordinary userinfo shapes pass through unredacted while the comment above it claims to cover "A URL's userinfo password: `scheme://user:pass@host`. Any scheme". Measured: `redact_secrets("redis://:secretpw@host:6379/0")` and `redact_secrets("postgresql://u:pa/ss@h:5432/db")` both return their input byte-for-byte (`postgresql://u:p@ss@h/db` leaks the tail, `...:[redacted]@ss@h/db`). The empty-user form is the documented Redis convention, and a `/` in the password is exactly the paste that produces the DSN failure this pattern was added for.
IMPORTANCE: high -- `redact_secrets` is the sole scrub on `plato/logs_api.py:41`, whose own comment is "a connection string logged once would otherwise sit in memory and be served to anyone who opens the page"; it is also the scrub on `jazzx_sdk/llm/cost_tracker.py:236` (serialized LLM payloads), `jazzx_sdk/channels/notify.py:74` (webhook bodies), `jazzx_sdk/agents/run_kit.py:380`, and now every `reason()` call site. Exported from `jazzx_sdk/__init__.py:142`, so external consumers call it directly. I did not find a shipped code path that puts a userinfo URL into `str(exc)` itself; the reachable producer is arbitrary logged text through `/logs` (posture-gated) and cost_tracker's payload serialization.

**[SYMMETRY]** `jazzx_sdk/pipelines/policy_extract.py:582` and `jazzx_sdk/pipelines/financial_spread.py:221` — three of the five `default_step_error` implementations got `reason()`; these two still hand-format. `policy_extract` builds `Refusal(reason=f"...failed: {exc}")`, which for a `httpx.ReadTimeout()` (empty `str()`, the case `reason` exists for) yields `"policy extraction step 'propose' failed: "` in a caller-facing typed refusal, unredacted; `financial_spread` logs `type(exc).__name__, exc` into the same ring buffer `logs_api` serves. `investigation_loop.py:305` carries no message and needs nothing. Two one-line edits, smaller than what the diff already did.
IMPORTANCE: medium -- enumerated all five `def default_step_error` in `jazzx_sdk/pipelines/`; both are opt-in handlers a caller must pass explicitly, so nothing in this repo reaches them by default and I could not establish which consumer passes them.

**[SYMMETRY]** `jazzx_sdk/failures.py:242` — `reason` redacts and truncates but does not collapse whitespace, which `_safe_detail` twelve lines below already does (`" ".join(str(error).split())`) for the same reason. Measured: `reason(OperationalError("SELECT * FROM plato_settings", {}, Exception("no such table")), limit=120)` returns `'OperationalError: (builtins.Exception) no such table: plato_settings\n[SQL: ...]\n(Background on this error at: htt'` — a three-line `logger.warning`, cut mid-URL. One `" ".join(...split())` inside `reason` covers all four `limit=120` log call sites at once.
IMPORTANCE: medium -- reaches `plato/settings_store.py:95,148` and `plato/wiring_default.py:527,548`, both of which fire on a normal first boot (settings table absent until migrations run), so the boot log is the common case, not the rare one.

**[NOTE]** `jazzx_sdk/failures.py:98` — the stated motivation is stale for the pinned dependency: SQLAlchemy 2.0.44 (`poetry.lock:5340`) raises `ArgumentError: Could not parse SQLAlchemy URL from given URL string` with no DSN in it (`sqlalchemy/engine/url.py:923`); the quoted-DSN wording is 1.4's. The pattern is still worth having, the *because* is not measurable here.
IMPORTANCE: low -- comment only.

**[NOTE]** `plato/database_api.py:65` — the engine-build handler returns `reason(exc)` unbounded into the HTTP body while the reachability handler 19 lines below caps the same field at `limit=200`; the diff touched both lines and kept the asymmetry.
IMPORTANCE: low -- one route (`GET {prefix}/database`), posture-gated per `create_database_router`'s docstring; worst case is an oversized `reason` string.

**[NOTE]** `plato/oidc.py:258` — the per-entry log widened from `%r` on `kid` to `%r` on the whole `entry`, and `redact_secrets` has no JWK pattern. A JWKS is public by definition so there is nothing secret in the expected case; it only matters for a misconfigured issuer serving private material, which would parse successfully and never reach this handler.
IMPORTANCE: low -- the entry reaches `logs_api`'s ring buffer; no shipped configuration puts a secret in it.

VERDICT: BLOCK
