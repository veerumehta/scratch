# adversarial review -- japes

- when: 2026-09-11T18:00:27Z
- range: 2b0a7eb..HEAD (since the last review) (dc5a73a)
- diff: 57 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: refusing-line-points-upward -- FIXED; the rewrap keeps the pointer at "the `is None` in the comprehension below" and every line of that comment is now ≤100, so the 120-char overhang is gone too.
WAS: tests/test_mock_knowledge_hub_app.py :: nulled-field-unasserted -- FIXED; the loop over `_ENTITY.items()` now asserts all four fields plus `json_value`, and mutating `mocks.py:1636-1639` to drop the `ontology_id`/`entity_type` guards fails all four parametrized cases (previously only the `collection_id`/`name` arms could fail).

CHECKED: `TODO(non-string-value-passes-guard)` holds -- `entity_data.get(f) is None` five lines below refuses null, and `KnowledgeHubClient.create_entity` (`knowledge_hub_client.py:1555-1562`) and `create_entity_v2` both type `name` as `str`, so only a hand-built body reaches the scalar/container case.

No defects introduced. `pytest tests/test_mock_knowledge_hub_app.py` → 38 passed; `ruff check` clean on both files.

[NOTE] tests/test_mock_knowledge_hub_app.py:288 :: seeded-body-unreported -- binding the response to `seeded` and then asserting only `status_code` loses the body on failure, where the sibling asserts in this file (`again.status_code == 409, again.text`, `other` at line 547) carry `.text`; the same applies at line 546.
IMPORTANCE: low -- test-only, both call sites are in this file; a failure costs one uninformative line.

[NOTE] tests/test_mock_knowledge_hub_app.py:385 :: changed-field-unasserted -- the `continue` skips the field the case deliberately changes, so the `patch0` case (`name: "e-2"`) never asserts the rename landed; a PATCH that silently dropped a non-null `name` would still pass, since the trailing clash-on-`held` is a 409 for either name. Same gap as before the diff, not introduced by it.
IMPORTANCE: low -- test-only; the rename-lands path is covered separately by `test_renaming_an_entity_onto_a_taken_name_is_a_409`.

VERDICT: PASS
