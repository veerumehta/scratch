# adversarial review -- japes

- when: 2026-09-09T02:45:42Z
- range: origin/dev...HEAD (ebbf793)
- diff: 696 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
Reviewed the diff against the code, ran `tests/test_plato_oidc.py tests/test_failures.py tests/test_plato_info_api.py` (190 passed, 1 pre-existing skip) plus the wider `-k "plato or settings or wiring or console or failures or schema"` subset (611 passed).

**CHECKED**
- `TODO(jwks-error-detail)` (plato/oidc.py:288): claim holds. `grep -rn 'TokenVerifier\|plato.oidc'` outside `tests/` finds only the class definition and the `__all__` entry (plato/oidc.py:417); nothing mounts it in a request path, so the JWKS-URL-in-`TokenError` leak has no HTTP surface today.
- `TODO(malformed-jwks-clock)` (tests/test_plato_oidc.py): claim holds. The constant clock can only prove the cooldown is entered, and the stamp line it covers is the same one `test_a_down_issuer_is_not_refetched_on_every_verify` drives with an advancing clock.

---

**[DEFECT] plato/oidc.py:310** — the new empty-document branch carries the previous cache's keys forward (`keys=self._cache.keys`), and `_fresh_enough` never expires a non-empty cache on the unforced path, so a key the issuer has *authoritatively stopped publishing* keeps verifying tokens for the process lifetime; every later empty fetch re-copies it forward. Pre-diff, that branch stored `{}` and the key was rejected after the first forced refetch. The comment at :304 defends this as keeping keys "for the window", a bound the code does not apply.

Trigger, measured: warm cache holding `k1`, issuer then serves `{"keys": []}` (key withdrawn / realm re-init), clock advanced past the cooldown and again by an hour → `verify(_token(key, "k1"))` returns claims (`withdrawn k1 still verifies: True`). The failed-fetch branch keeping keys is right; a fetch that *succeeded* and said "no keys" is different information.

IMPORTANCE: medium -- nothing in this repo mounts `TokenVerifier` (only plato/oidc.py:417 and tests reach it), so no shipped configuration hits it today; but it inverts the revocation behavior the pre-diff code had, silently, for whichever middleware mounts it next, and nothing outside this repo imports `plato.oidc`.

**[SYMMETRY] jazzx_sdk/failures.py:232** — `reason` is the repo's new shared type+message formatter but does no redaction, while the two existing formatters in this file's own family do: `_safe_detail` (failures.py:246) pipes through `redact_secrets`, and `jazzx_sdk/clients/knowledge_hub_client.py:78` is literally `f"{type(error).__name__}: {redact_urls(str(error))}"`. Its output now reaches HTTP bodies (`plato/database_api.py:65` `reason`, `jazzx_sdk/server/console_api.py:131` `error`), where a SQLAlchemy URL-parse failure echoes the DSN it was handed. One wrapped call inside `reason` is the same size as the diff.
IMPORTANCE: medium -- checked all 8 new call sites; two of them (`describe_database`, `registry_inventory`) return the string in a response body, both behind gated routers, and the pre-diff code leaked identically, so this is a missed opportunity rather than a new leak.

**[SYMMETRY] jazzx_sdk/pipelines/vocabulary_build.py:370** — the diff normalizes this formatter across all of plato plus `server/console_api.py`, but leaves the identical `f"{type(exc).__name__}: {exc}"` in the SDK's own family: `pipelines/vocabulary_build.py:370`, `pipelines/document_ingest.py:269`, `pipelines/chat.py:378`, `conductor/engine.py:561` and `:574`. Same one-call change, and `failures` sits at the bottom layer so none of them has an import-boundary reason to abstain.
IMPORTANCE: low -- cosmetic there (the type is still printed, only a trailing colon is left), and these strings go to step messages/span attributes, not to a truthiness test as in oidc.

**[NOTE] plato/settings_store.py:28** — the import lands between `from sqlalchemy import ...` and `from sqlalchemy.orm import ...`, splitting the third-party block; `[tool.ruff]` (pyproject.toml:159) selects no `I` rules, so nothing will flag it.
IMPORTANCE: low -- one file, no runtime effect.

**[NOTE] jazzx_sdk/__init__.py:133** — every other public helper in `failures.py` is re-exported there and listed in `__all__` (`classify_failure`, `redact_secrets`, `redact_fields`, `redact_for_zero_retention`, ...); `reason` is not, so consumers reach it by module path while its siblings are top-level.
IMPORTANCE: low -- affects import ergonomics only; six plato modules already import it by path successfully.

**[NOTE] plato/oidc.py:261** — the warning now logs the whole `entry` rather than the `kid`, so an unparseable JWK's material lands in the log verbatim; only reachable for an entry PyJWK rejects, which is unlikely to be key-bearing.
IMPORTANCE: low -- log content only, and the path requires a malformed JWKS entry.

VERDICT: BLOCK
