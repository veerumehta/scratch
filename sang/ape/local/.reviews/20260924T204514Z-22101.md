# adversarial review -- .

- when: 2026-09-24T20:45:14Z
- range: 5534f5e..HEAD (since the last review) (127b19d)
- diff: 735 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_backend_names.py :: db-default-not-exercised -- FIXED

[DEFECT] jazzx_sdk/fabric/canonical/condition_evaluator.py:797 :: batch-drops-outcome-conditions -- In the batch path, `ProcedureEvaluator.batch_outcome` never passes `conditions` to `_procedure_outcome`, `_BatchItemVerdict` has no field to carry them, and neither the batch header nor `render_for_batch` asks for them, so a CONDITIONAL outcome from adjudication comes back as SATISFIED with `narrative=None`.
Trigger: `run_segment` over a procedure rule where the model answers `status="CONDITIONAL"`. The result is `verdict=SATISFIED, status="CONDITIONAL", narrative=None`. The same rule run through `ProcedureEvaluator.evaluate` records `narrative={"conditions": [...]}` (the test at tests/test_procedure_condition.py covering the status-to-verdict mapping asserts exactly this). Batching is the path that adjudication uses for every LIVE rule (`partition_rules` → `_split_batchable` → `_process`), so the conditions attached to a conditional pass are lost with no signal.
IMPORTANCE: medium -- Every `run_segment` / `AdjudicationAgent` caller that has a procedure rule reaches this. The kind is new in this diff, and no pack YAML in this repo declares `procedure` yet, so nothing depends on it today. The first pack that authors a CONDITIONAL outcome will get SATISFIED with its conditions stripped.

[NOTE] jazzx_sdk/agents/adjudication/segment.py:242 :: collapse-priority-lacks-pack-statuses -- Procedure votes carry pack-vocabulary statuses such as PASS or CONDITIONAL, not SATISFIED/VIOLATED. With a caller's `DeterministicVote` whose priority lists only SATISFIED/VIOLATED, every procedure vote is unranked, `min` returns replica 0, and the collapse gives an arbitrary winner with no error. The test works around this by building `_STATUS_VOTE` with the procedure's statuses. Nothing in the diff warns a caller that their collapse must rank each procedure's declared statuses.
IMPORTANCE: low -- `collapse` is supplied by the caller (`agent.py:75`, `segment.py:171`). I found no in-repo caller that builds a SATISFIED/VIOLATED-only vote and also runs procedure rules.

[NOTE] jazzx_sdk/pack/lint.py:107 :: none-guard-never-fires -- `Pack.evidence_types` is a property that returns `[]` when the manifest has no `evidence_types`, so the `is None` early return never fires for a real `Pack`. A pack that declares no evidence types but has procedure rules gets one `unknown_evidence_type` finding per referenced type. That may well be the intended behaviour, but the guard reads as if it handles that case.
IMPORTANCE: low -- The callers are `lint_pack` from `pipelines/catalog.py`, `pipelines/policy_extract.py` and the tests. Only `ProcedureEvaluator` (directly or nested in all_of/any_of) reports non-empty `doc_types`, so existing packs get no new findings.

The 14 tests in tests/test_procedure_condition.py pass.

VERDICT: BLOCK
