# adversarial review -- .

- when: 2026-09-25T04:18:30Z
- range: 0968ca51..HEAD (since the last review) (ee8b2d5f)
- diff: 41 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/pipelines/jtbdset.py :: stale-whole-flag-truncates -- STILL OPEN, narrowed. Added steps and checks now export, for base rules and overlays, because both go through `_render`. Edits to `purpose` or `outcomes` on a whole-imported rule are still dropped without warning. `_procedure` stores `purpose=fallback_purpose` and `DEFAULT_OUTCOMES` for these rules, and the new gate at line 122 checks neither. So the `# unedited since import` comment claims more than the gate checks. To trigger it, import a COMMON block with no step headings, set the rule's `condition.purpose` to new text or change an outcome criterion, then export: only `steps[0]` comes out. Adding `and condition.purpose == <fallback> and condition.outcomes == DEFAULT_OUTCOMES` closes it. For base rules the fallback is `rule.description`; for overlays it is `f"{program} overlay for {mnemonic}"`.
IMPORTANCE: medium -- edited content is lost from the exported JTBDSet with no error. The callers are the same as last round: only `export_jtbdset` calls `_render`, twice, at lines 238 and 245. Outside this module the only user is tests/test_jtbdset.py, and nothing outside the repo uses it.

WAS: jazzx_sdk/pipelines/jtbdset.py :: merged-section-name-rewritten -- STILL OPEN, the first spelling seen still wins at line 162. It was low last round and this diff does not touch it.

WAS: jazzx_sdk/pipelines/jtbdset.py :: empty-sequence-now-raises -- FIXED. `""` now defaults to 100.

The diff adds no new defects. The new test fails against the old `if whole:` code, because only `steps[0]` came out, so it does catch the regression.

VERDICT: BLOCK
