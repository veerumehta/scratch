# adversarial review -- japes

- when: 2026-09-08T22:53:59Z
- range: 0ddc73b..HEAD (since the last review) (f40e4d4)
- diff: 121 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**WAS (previous round)**

- WAS: `store_db.py:172` "the content-addressed key rule now exists twice" — **FIXED**; `BlobStore.content_key` (blob/store.py:99) now owns derivation and both `offload(dedup=True)` and `publish` call it. Two residues, both narrower than the finding: identical bytes still land under two keys (`sha256/<hex>` vs `packs/sha256/<hex>.zip`) because the prefixes differ, and the hash inside `content_key` is still `hashlib.sha256` rather than `content_digest`.
- WAS: `store_db.py:57-58` "the only tie would be two *different* versions…either order is correct" — **FIXED**; the sentence is gone, replaced by `TODO(publish-tick-tie)` which states the wrongness explicitly.
- WAS: `store_db.py:310-320` re-hash sits below the cache short-circuit — **FIXED**; the comment now scopes the guarantee to the fetch path and names the trusted-tree gap and the shared `/tmp/japes-packs` default.
- WAS: `tests/test_pack_store.py:219` per-tenant `cache_root` — **FIXED**; both tenants now share `tmp_path / "cache"`, matching `_store`'s root; 12/12 pass.

**CHECKED:** `TODO(publish-tick-tie)` (`store_db.py:58`) holds. `publish` has no non-test caller — the only construction site is `plato/models.py:49` (`DbPackVersionStore(db)`, metadata registration only, `blob=None`), and grep over `jazzx_sdk/`, `plato/` finds no `publish(` on this store outside `tests/test_pack_store.py`. `published_at` is `datetime.now(timezone.utc)` per publish with a DB round-trip between, so a same-microsecond tie is not reachable. The `get`/`list_packs` shape it describes is accurate.

---

**[SYMMETRY]** `jazzx_sdk/fabric/blob/store.py:105` — `content_key` single-sources the *key shape* but re-implements the *hash* with a bare `hashlib.sha256`, while `jazzx_sdk/digest.py:26` `content_digest` exists precisely as "one implementation, importable from any layer" with no dependencies (and `jazzx_sdk/fabric/idempotency.py:27` `content_fingerprint` is fabric's own copy of the same rule). Concretely in this diff: `store_db.py:180` derives the row's `content_digest` from `content_digest(archive)` and line 181 derives the key from `content_key(archive)` — two functions that coincide only because the argument is `bytes`. `return f"{prefix}/{content_digest(data)}{suffix}"` plus one import is smaller than the docstring arguing the helper prevents divergence.
IMPORTANCE: low — I enumerated every `content_key` caller (exactly two: `offload` at store.py:132, `publish` at store_db.py:181) and every `put(key=)` site in `jazzx_sdk/` and `plato/`; both pass `bytes`, so the two hashes agree today, and nothing outside japes imports `BlobStore.content_key` (grepped `/Users/sangit/src`).

---

**[NOTE]** `jazzx_sdk/fabric/blob/store.py:100-102` — the docstring says two callers deriving the key separately "is how the same bytes end up under two keys -- which is the thing dedup exists to prevent", but sharing the function does not prevent that here: `offload(dedup=True)` keys an archive `sha256/<hex>` and `publish` keys the same bytes `packs/sha256/<hex>.zip`, so the double-write the sentence disclaims is still what the code does. What the helper actually guarantees is one hash rule and one key *shape*, not one key per content.
IMPORTANCE: low — same two call sites as above; no pack archive is routed through `offload` in any shipped configuration, so the double-write remains latent, as the previous round found.

**[NOTE]** `jazzx_sdk/fabric/blob/store.py:105` — `content_key` is now public with an unvalidated `prefix`, and `prefix=""` yields `"/<hex>"`, which on the local backend makes `self._local_dir / key` an absolute path (`Path("./blobs") / "/abc"` → `/abc`), writing outside `blob_dir`; `prefix="../x"` escapes likewise. No shipped caller does this, and `put(key=...)` already had the same shape before this diff, so a `TODO` on the parameter is a complete response.
IMPORTANCE: low — both existing callers pass a literal non-empty prefix; reaching it needs a new caller inside japes, and no external repo references this method.

VERDICT: PASS
