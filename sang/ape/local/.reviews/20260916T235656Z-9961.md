# adversarial review -- japes

- when: 2026-09-16T23:56:56Z
- range: a2045d5..HEAD (since the last review) (e52aa49)
- diff: 489 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/wiring/default.py :: forget-engine-repoints-serving-store -- FIXED. `_forget_common_engine` returns a `restore` closure over `(_db_module._async_engine, _settings._common_settings)`, and the `except BaseException` around `_db`/`register_all`/`create_all` calls it before re-raising; both globals exist in the submodule (`common/core/db.py:12`, `common/core/common_settings.py:168`), so the read cannot itself raise.
WAS: plato/wiring/default.py :: bootstrap-write-not-persisted -- FIXED as a claim. All three statements I enumerated now say it: `swap_database`'s docstring (`:618-622`), the `BOOTSTRAP_KEYS` comment (`plato/api/config.py:44-46`), and the `DB_ASYNC_CONNECTION_STR` row in `docs/DEPLOYMENT_ENV.md`. Behaviour unchanged, which was the finding's point.
WAS: tests/test_common_submodule_deps.py :: installed-check-skips-image-extras -- FIXED. `extra not in _IMAGE_EXTRAS` now matches the sibling; `-rs` over the file shows only `redis.asyncio` skipping (`:356`), so the five plato-extra modules including `opentelemetry.instrumentation.fastapi` are asserted.
WAS: tests/test_plato_packs_api.py :: posture-list-still-handwritten -- STILL OPEN in one member: the three sites in `test_plato_packs_api.py` and the three in `test_integration_followups.py` are converted, but `tests/test_plato_wiring_default.py:28` still spells the same four names by hand. Raised below as the remaining sibling.
WAS: plato/wiring/default.py :: pack-blob-warning-restates-the-complaint -- FIXED. The azure path calls `pack_archive_complaint()` once; the two hand-written branches are gone. It changed what gets logged, though -- below.
WAS: plato/__main__.py :: complaint-appended-to-any-refusal -- FIXED. Gated on the refusal shape and covered by `test_a_stale_schema_refusal_carries_no_connection_string_complaint`; `check_schema` returns only the two shapes, so a `SchemaStaleError` no longer collects a URL diagnosis.
WAS: tests/test_common_submodule_deps.py :: skip-reason-says-uninstalled -- FIXED. The reason now names both causes, and the observed skip at `:384` reads correctly for an installed namespace package.
WAS: tests/test_dev_daily_env_template.py :: gitignore-checked-by-exact-line -- FIXED. Asked of `git check-ignore` rather than of `.gitignore`'s text; a broadened `.env*` would now fail it.
WAS: tests/test_plato_packs_api.py :: config-patch-test-reads-ambient-tier -- FIXED. The test seeds `UPLOAD_MAX_BYTES_ENV` with `DEFAULT_UPLOAD_MAX_BYTES` through `monkeypatch` before the PATCH, so teardown restores it, and it now asserts the write landed.
WAS: plato/wiring/settings_store.py :: settings-failure-not-at-info -- FIXED for the paths in `_apply_durable_settings`: all three now go through `_durable_settings_degraded`, which calls `record_boot_note(..., from_wiring=True)` and so reaches `/info`'s `degraded` via `resolve_degraded`. The path a real deployment hits is still silent -- below as a [NOTE].

[DEFECT] plato/wiring/default.py:913 :: wiring-note-not-dropped-on-swap -- the docstring's stated reason for `from_wiring=True` ("so a later `swap_database` drops it") is not something any code does: `_WIRING_NOTES` is cleared only in `build()` (`:439-440`), and `swap_database` neither calls `build()` nor re-applies durable settings, so the note lives for the process.
Boot a replica whose `durable_settings_store(db)` construction fails (the state `test_a_durable_settings_failure_reaches_info` builds), then `PATCH {prefix}/config` a bootstrap key so `_rewire` runs `swap_database` successfully. The note is still in `_WIRING_NOTES`, so `/info` keeps `"configured": false` (`plato/api/info.py:509`) and `python -m plato --health` keeps exiting non-zero for the life of the process -- and a restart to clear it discards the `os.environ`-only PATCH this same diff documents as the recovery lever. `build()` is called once per process, so nothing in a serving role ever reaches the clear.
IMPORTANCE: medium -- fails visibly at `/info` and the note's *text* stays true (nothing re-reads `plato_setting` after a swap), so the damage is the health probe and the docstring, not wrong data. I enumerated every reader of `_WIRING_NOTES` (`boot_notes`, `gating_notes`, `build`, `reset_boot_notes`) and the one caller of `swap_database` (`plato/app.py:202`); reachable in any posture where `/config` admits a write, and nothing outside this repo calls these.

[DEFECT] plato/wiring/default.py:853 :: blob-complaint-order-claim -- the comment says the two hand-written branches answered "the same two conditions in the same order as the predicate thirty lines above", but `pack_archive_complaint` checks `STORAGE_CONNECTION_ENV` *before* `PACK_BLOB_CONTAINER_ENV` (`:803`, `:807`) and returns on the first hit, where the removed code warned about the container first and then, separately, about the credentials.
`PLATO_PACK_BLOB_BACKEND=azure` with both `STORAGE__CONNECTION_STRING` and `PLATO_PACK_BLOB_CONTAINER` unset: the boot log used to carry both warnings and now carries only the connection-string one, so the "archives would go to `common`'s default container" gap is invisible until the credentials are set and the replica restarted. `container` is still read at `:846` and passed to `BlobStore`, so the condition is live.
IMPORTANCE: low -- one boot log line, on the azure path only; `test_missing_storage_credentials_are_reported_at_boot` covers the surviving message and no test asserted the container one. The 503 on `POST {prefix}/packs` already reported one complaint at a time, so the route is unchanged.

[SYMMETRY] tests/test_plato_wiring_default.py:28 :: posture-list-still-handwritten -- this diff replaced the hand-written posture tuple in `test_plato_packs_api.py` and `test_integration_followups.py` with `_POSTURE_ENV_VARS` from `jazzx_sdk/config/envvars.py`, and left the third copy, `_POSTURE_VARS` at the top of the file it also edits. `_POSTURE_VARS = _POSTURE_ENV_VARS` at the import is smaller than the literal it replaces and closes the family.
IMPORTANCE: low -- correct today (same four names, same order); drift risk only, scope is this one test module.

[NOTE] plato/wiring/default.py:904 :: unreachable-layer-still-configured-true -- the paths `_durable_settings_degraded` covers are store *construction* and the running-loop skip; the failure an operator actually hits -- the durable layer unreachable or `plato_setting` unreadable -- is swallowed inside `apply_to_environment` (`plato/wiring/settings_store.py:166-175`, which returns `()` for every read exception, not just a missing table), so `asyncio.run(pending)` does not raise and `/info` still answers `configured: true`. The helper's framing ("a replica serving environment-only settings ... answered `configured: true` for the rest of its life") is narrower in the code than it reads: `test_a_durable_settings_failure_reaches_info` has to break `register_metadata` to reach it. Separating "no such table" from "connection refused" one layer down is what would actually close it, and the second new test is the argument for why that separation has to be deliberate.
IMPORTANCE: low -- no wrong data; the condition is logged at warning by `apply_to_environment` either way. Checked both callers of `apply_to_environment` (`_apply_durable_settings`, and `DbSettingsStore.read`'s own `_last_failure` path).

[NOTE] plato/__main__.py:170 :: refusal-matched-by-substring -- the new branch is keyed on `"cannot verify" in refusal`, a substring of the message built at `plato/boot/schema_version.py:191`, with no shared constant between the two modules. Rewording that message silently drops the connection-string diagnosis; `test_the_startup_log_line_carries_the_complaint` asserts both fragments and would catch it, so this is pinned rather than free-floating.
IMPORTANCE: low -- one branch, one caller (`plato/__main__.py:431`), test-pinned in both directions.

VERDICT: BLOCK
