# adversarial review -- japes

- when: 2026-09-10T14:30:09Z
- range: origin/v2.5.1...HEAD (d305c11)
- diff: 10259 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED**
- `store_db.py:59` `TODO(publish-tick-tie)` — claim holds: `publish` is reached from `plato/wiring_default.py`, `wiring_local.py` and the initialize route, one version per seed directory with a blob write between each.
- `store_db.py` `TODO(orphan-blob-gc)` — holds; the store is tenant-scoped at construction and `content_digest` keys are shared across tenants, so the count genuinely cannot be asked here.
- `profiles.py` `TODO(threshold-grid-provenance)` — holds, and the corrected numbers are right: `plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml:7-13` authors all seven thresholds as bare strings, so `looks_authored` is False for every one.

---

**[DEFECT]** `jazzx_sdk/experts/policy/default.py:431` — the new `gate_blocks` branch runs *before* the cross-policy field-precedence check at line 437, so a rule whose field a higher-precedence policy already owns can still withhold `allowed`. Trigger: an overlay rule on `leverage_x` evaluates and claims the field; a lower-precedence core policy has a rule that also reads `leverage_x` whose `applicability` is a `RatioCondition` with `threshold: "profile:<seed_key>"`. The core rule would have hit the `continue` at line 441 had its gate been readable; instead `check_compliance` returns `allowed=False` and a "rule not activated" warning for a rule the overlay supersedes. The pre-existing `verdict != SATISFIED` skip above it is harmless in the same position because it only `continue`s.
IMPORTANCE: medium — reachable only through pack authoring (a profile-backed applicability gate plus an overlay claiming the same field); no such pack ships here today (`dscr_core`'s profile has no seed-status thresholds). It fails loudly. Callers checked: `finance/validation.py:policy_violations_to_findings` and everything reading `ComplianceResult.allowed`; `DefaultPolicyExpert` is exported from `jazzx_sdk.experts`, so out-of-repo consumers exist that I could not enumerate.

**[DEFECT]** `jazzx_sdk/fabric/canonical/condition_evaluator.py:446-448` — `refusal` is first-child-wins while `_composite_reason` on line 478 is POLICY_NOT_ACTIVATED-outranks, so the two disagree and the warning names a threshold that did not block. Trigger: `all_of[ any_of[ ratio(profile:seedA), expr(satisfied) ], ratio(profile:seedB) ]`. The inner `any_of` is SATISFIED but lifts `inputs["not_activated"] = msg(seedA)`; the outer loop takes that first, then child 1's genuine POLICY_NOT_ACTIVATED refusal for `seedB` is discarded because `refusal is None` is already False. The outer outcome carries `indeterminate_reason=POLICY_NOT_ACTIVATED` with `inputs["not_activated"]` naming `seedA`, so `_not_activated_warning` (`default.py:71`) tells the operator to approve the wrong key. The comment on line 436 claims this loop decides "on the same principle" as the reason; it does not. Same divergence gives the milder case where the outer reason is `EVIDENCE_MISSING` but `inputs` still carries a stray `not_activated`.
IMPORTANCE: medium — needs a depth-2 composite with a profile-backed limb; the depth cap allows it. Consumers of the wrong text: `experts/policy/default.py:71` → `ComplianceResult.warnings` → `finance/validation.py:_warning_refs`, and `fabric/graph/check.py:_untested_rationale`.

---

**[SYMMETRY]** `jazzx_sdk/finance/validation.py:833` — `_warning_refs` regex-parses `policy_id`/`rule_id` back out of a string that `experts/policy/default.py:71` formatted from those exact values twelve lines above the loop that already puts them in a `detail` dict as structured keys. The producer has them; the consistent shape is `ComplianceResult.warnings` carrying the same `{policy_id, rule_id, ...}` dicts `policy_violations` uses (`experts/policy/base.py:48`), which is no more code than the regex plus the `"policy_id": None, "rule_id": None` defaults. As written, any change to the warning prefix silently degrades every finding to `rule_id: None`.
IMPORTANCE: medium — one producer and one consumer, both in this repo; `ComplianceResult.warnings` is a public exported field, so changing its element type is the reason not to, and that is the author's call.

---

**[NOTE]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:128,138,141,149,151` — japes ships a bundled seed pack declaring `jaci.scenarios.ci_spread.*` and `jaci.capabilities.commercial_lending.*` import paths, plus `gold_cases_path: tests/eval/gold_cases/ci`, `runner: tests/eval/run_ci_eval.py`, and `source_doc: docs/LoanSamples/YETI/...` — a consumer repo's module and directory layout inside this repo's shipped data. The sibling seed pack `dscr_core/pack_manifest.yaml` declares no `class:` at all. Separately, nine files in this pack cite `plan_JACI_CL_*.md` filenames in comments, which the repo rule forbids in committed code.
IMPORTANCE: medium — I checked `plato/packs/loader.py` and `plato/packs/domain.py`: neither resolves manifest `class:` paths today, and `pyproject.toml` has no jaci dependency, so nothing crashes now. The blast radius is whoever adds that resolution, and every operator who clicks "initialize from bundled seed packs."

**[NOTE]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:411,432` — the `v0.2.1` header records fixing exactly this defect class (an undeclared `taxes`, a missing `scheduled_principal`) in `FCCR` and `DebtServiceCoverage`, but two siblings in the same `concept_graph` still have it: `ExcessAvailability` lists `Facility.outstanding_balance`, which is not a field of `Facility` (it is on `BorrowingBaseResult` and `Lien`), and omits `reserves` entirely; `UCAcashFlow`'s formula reads `net_income`, `D&A` and `working_capital_change`, none of which appear in its `inputs` and the first of which is not a `FinancialPeriod` field.
IMPORTANCE: low — the file states "Formulas are documentation only - runtime computation is in the Reasoner", and nothing in this repo parses `concept_graph`. Cost of hitting it is a wrong lineage view.

**[NOTE]** `jazzx_sdk/fabric/graph/check.py` — the gate-blocked branch passes `inputs=gate_outcome.inputs` while the same branch in `jazzx_sdk/agents/adjudication/segment.py:110` passes `inputs=dict(gate_outcome.inputs)`. Pydantic copies the dict on validation either way, so no aliasing reaches a caller; the two just read as if one of them found a reason to defend.
IMPORTANCE: low — no reachable trigger; both call sites enumerated.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `plato/posture.py:96` `TODO(if-match-post)` — claim holds. `require_if_match` is imported only by `tests/test_plato_preconditions.py`; no router mounts it, so `POST /packs/initialize` cannot 428 today.
- `plato/info_api.py:82` `TODO(omit-info)` — claim holds. `_info_only_app`'s tuple is `("Guide", "config", "database", "Packs", "packs")` (`plato/__main__.py:304`); `info` is not in it, and nothing else passes `omit`.

## Findings

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125,127` — the `purpose_specific_documents` section carries two orphan fragments from a broken PDF extraction (`- schedule`, `- available)`), and the two real requirements they were split from lost their heads (the equipment/acquisition line, and the projections line). Trigger: any package assessment over this checklist emits required-document gaps literally named `schedule` and `available)`, and never asks for the two documents that were actually listed.
IMPORTANCE: medium -- this file is byte-identical to `/Users/sangit/src/jaci/config/packs/ci-spread-core/policies/checklist_ci.yaml`, which that pack's manifest declares as `checklist:` and jaci's package assessment reads; the copy here additionally reaches any plato tenant that runs `POST /packs/initialize`. Fails visibly as nonsense text rather than silently.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:83-92` — `CI-ABL-MIN-AVAILABILITY`'s description states "10% of borrowing base or $2M, whichever is greater", but the only predicate is `excess_availability_pct >= 0.1`; the `$2M` leg has nothing behind it. Trigger: a $10M borrowing base with $1.5M excess availability (15%) evaluates SATISFIED while the stated policy requires ≥$2M — permissive direction.
IMPORTANCE: medium -- reached through `Pack.policy_registry`, which I confirmed picks conventions.yaml up via the manifest's `dir: policies` one-level glob and `is_policy_document` shape test (`jazzx_sdk/pack/manifest_loader.py:184`); consumed by jaci's ci_spread policy checks. The pack is `certification_status: draft`, which caps the damage but not the wrong verdict.

**[DEFECT]** `plato/seed.py:113,125,131` — `skipped` conflates "already published" with "not a pack at all": a directory with no readable manifest, an assistant pack, and a manifest with no `pack_id` all land in the same list as the genuine "already published" case at :136/:145, and the only consumer renders `skipped.length` as "already there" (`plato/static/packs.html`, seed()). Trigger: `PLATO_SEED_PACKS_DIR=/Users/sangit/src/jaci/config/packs` — the documented use case — where `aml_investigation_core/`, `cre_underwriting_core/` and `insurance_diligence_core/` are asset-only directories whose manifest is a sibling top-level `.yaml`; the page reports "5 published, 3 already there", claiming three packs are in the store that were never published.
IMPORTANCE: medium -- operator-visible wrong count; the only reader is the packs page shipped in this same push. Nothing outside this repo consumes `seed_packs`' return shape.

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:730` (`DSCR-PPP-MS`), same shape at `:798` (`DSCR-PPP-OH`) — these key on `residential_unit_count`, which defaults to `1` (`jaci/src/jaci/scenarios/dscr/schemas/dscr_schemas.py:167`, passed through at `eligibility/context.py:64`), and nothing reconciles it with `property_type`. Trigger: `{property_state: "MS", property_type: "two_to_four_unit", prepayment_penalty_requested: "5yr_5pct_fixed"}` with `residential_unit_count` left unset → MS's single-unit PPP restriction fires and returns VIOLATED on a loan the source rule does not cover; the Ohio rule (`<= 2`) denies a 3-4 unit Ohio loan the same way. jaci's own cases set `residential_unit_count` explicitly (`tests/scenarios/dscr/test_dscr_eligibility.py:106,112`), so the default path is untested.
IMPORTANCE: medium -- restrictive direction, and per ENCODING_NOTES Phase 2.3 the Governor enforces the deterministic verdict, so this blocks an eligible loan rather than passing an ineligible one. Reached by any jaci DSCR assessment that sets `property_type` without the unit count.

**[SYMMETRY]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:212` — `DSCR-SUB-1-DSCR-FICO` hardcodes `640` with no `domain_extensions.profile_custom_key`, while its twin `DSCR-NO-RATIO-FICO` at `:432` — same threshold, same sentence in the source ("DSCR < 1.0 or No Ratio: minimum FICO 640") — declares `profile_custom_key: fico_min_no_ratio` and is therefore covered by `find_profile_literal_drift`. Recalibrating `profile.custom.fico_min_no_ratio` leaves the lint green and this rule silently on the old number. The consistent version is the one line the sibling already has.
IMPORTANCE: low -- no reachable trigger until the profile is recalibrated; the cost today is that ENCODING_NOTES' "8 twins kept honest" covers 8 of 9 rules that should be.

**[NOTE]** `plato/data/seed_packs/` — the two seeded packs are byte-identical copies of `/Users/sangit/src/jaci/config/packs/{dscr_core,ci-spread-core}` (only `.DS_Store` differs), and the files in this diff carry the consumer's name and one machine's layout into the shipped image: `jaci.capabilities.commercial_lending.*` module paths (`workbook_layout.yaml:1-20`, `spread_template_rb.yaml:11-13`, `reclassifications.yaml:1-2`), `docs/LoanSamples/...` document paths (`core.yaml:29`, `playbooks/loan_package_scaffold.md:1`), and gitignored plan filenames (`plan_JACI_CL_RECLASSIFICATION_ENGINE.md`, `plan_JAPES_2_3_0_GOVERNED_WORKBOOK.md`, `docs/plans/Acra_DSCR_on_Platform_v2_...md`), plus `cd jaci && pytest` instructions at `ENCODING_NOTES.md:238-245`. `PLATO_SEED_PACKS_DIR` already exists for pointing at the consumer's checkout.
IMPORTANCE: medium -- ships in the image and is published into tenant rows; the two copies have no mechanism keeping them in step and jaci's is the one being edited.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:183,200` — §3 says "no rule in `policies/eligibility.yaml` uses `kind: all_of`/`any_of` yet" and that "the 13-state prepayment-penalty rules G1 was blocking remain unauthored", contradicted by §1a/§1b of the same file and by the 7 `all_of` rules in the file beside it. `:14`'s "`pack_manifest.yaml` is **still not wired**" is likewise contradicted by the `pack_manifest.yaml` shipped in this diff, which declares `policies: dir:`/`profiles: dir:`.
IMPORTANCE: low -- documentation only; the corpus itself is correct.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:219` — "matches addbacks.yaml's owner_compensation cap" has nothing to match: `addbacks.yaml:63` declares `cap: "min(amount, ceiling)"` with the numeric ceiling read from a `PolicyProfile`, and this pack declares no `profiles:` block and ships no profile, so `250000` here is the only place that number exists. The dscr_core manifest's "Non-policy assets in the folder are skipped by name" is also inaccurate — `_declared_files` filters by shape (`is_policy_document`), as ci-spread-core's own manifest correctly says.
IMPORTANCE: low -- a false provenance claim on a threshold already labelled `calibration_status: placeholder`.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Reviewed against the tree; ran the four new/changed test files (87 passed) plus `test_pack_store.py`/`test_plato_info_api.py` (123 passed, 2 pre-existing skips). No defect with a reachable trigger survived checking.

## CHECKED

- `TODO(publish-tick-tie)` (`jazzx_sdk/pack/store_db.py:61`) — holds. `published_at` is a Python-side `datetime.now(timezone.utc)` (store_db.py:213), not `CURRENT_TIMESTAMP`, so the sqlite second-resolution tie I looked for does not exist; `seed_packs` does a blob write plus a round trip per directory, so the "newest first" ordering `packs.html` and `last_published_version` rest on is stable.
- `TODO(azure-blob-container-singleton)` (`plato/wiring_default.py`, `_pack_blob`) — holds as written: `AsyncAzureBlobStorageProvider.get_instance` returns the singleton before looking at `container_name` (`common/core/storage/async_azure_provider.py:54`), and `jazzx_sdk/fabric/fabric.py:198` is the second azure construction it names. One consequence it does not spell out: with both azure and different containers, a restart that reverses first-caller order leaves already-published archives in the other container, so `materialize` fails while the row still reports `has_archive: true`. Doesn't change the deferral.
- `TODO(local-wiring-build-test)` (`tests/test_plato_packs_api.py`, `test_the_local_wiring_passes_a_pack_store_factory`) — holds: `_assemble_pack` raises `SystemExit` without a `profile.yaml` (`plato/wiring_local.py:266`), so the test genuinely cannot call `build()`. I separately confirmed `env_text` is imported at `wiring_local.py:91`, so the lines it can only read are at least import-clean.

## Findings

**[SYMMETRY]** `tests/test_policy_threshold_provenance.py:115` — the shipped-profile assertion locates the pack with a CWD-relative `Path("plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml")` where the sibling test added in this same diff (`tests/test_plato_packs_api.py`, `test_the_dscr_seed_pack_can_resolve_its_own_profile_references`) uses `plato.seed.default_seed_root()`; same size, and it is the function that already owns "where seed packs live". Trigger: `pytest /Users/sangit/src/japes/tests/...` from any other working directory (e.g. driven from jaci's venv, where japes is editable-installed) — the import resolves and the `read_text` does not.
IMPORTANCE: low -- test-only, one test; blast radius is this file. `default_seed_root()` is the only other reader of that path in the diff, and nothing outside the repo reads either.

**[SYMMETRY]** `plato/static/packs.html:370` — `#seed-result` is the fourth message channel on this page and the only one `load()` does not reset; `load()`'s reset block (packs.html:407) was extended three times to cover `#problem`, `#empty` and `#meta` for exactly this staleness, and the same one-line `result.textContent = ""` belongs there. Trigger: click Initialize ("6 published, 0 already there"), then delete a version — the table re-renders under a message that no longer describes it.
IMPORTANCE: low -- cosmetic, single page; the three call sites that write messages are all in this file and nothing outside the repo renders it.

## Notes

**[NOTE]** `plato/tenancy.py:86,101` — the docstring promises "the request's tenant as an HTTP answer, or `HTTPException(400)`", but the handler enumerates `ValueError` alone, so a resolver raising anything else escapes as a 500 from a helper whose stated purpose is to prevent exactly that. Both shipped resolvers (`wiring_default.py:502`, `wiring_local.py:388`) raise only `ValueError`, so the trigger is a consumer-supplied `PlatoWiring.tenant_from_request` — e.g. one doing `request.headers["X-Tenant-Id"]` and raising `KeyError`.
IMPORTANCE: low -- no shipped configuration reaches it; call sites enumerated: `packs_api.py:157` and the seven `await _tenant(request)` sites in `assistants_api.py`. `PlatoWiring` is a documented extension point, so an external wiring is the only reachable path.

**[NOTE]** `plato/wiring_local.py:381` — `cache_root=CACHE` points the pack unpack cache at the assembled assistant-pack directory, which `_assemble_pack` `shutil.rmtree`s on every boot (`wiring_local.py:282`) and which `pack_registries` loads as a pack root; the diff gave archive bytes their own `PLATO_LOCAL_PACK_BLOBS` directory but reused `CACHE` for the unpacked trees. No wrong data today: nothing in the local wiring calls `materialize` (`plato/packs/loader.py:262` and `plato/packs/domain.py:89` are the only callers and both are test-only), so the effect is a cache discarded per boot and digest directories appearing beside `profile/` and `manifest.yaml`.
IMPORTANCE: low -- local wiring only, opt-in via `PLATO_WIRING`; `wiring_default` passes no `cache_root` at all and gets the tempdir default, so nothing deployed is affected.

VERDICT: PASS
