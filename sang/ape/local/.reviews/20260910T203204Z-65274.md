# adversarial review -- japes

- when: 2026-09-10T20:32:04Z
- range: origin/v2.5.1...HEAD (af238d4)
- diff: 11466 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(threshold-grid-provenance)` (`profiles.py:229`) — holds and is precise: `dscr_profile.yaml` has 7 bare-string `thresholds` (0 authored dicts), 11 bare `custom`, 5 tables / 150 cells = the 157 it names; `looks_authored` is False for every one, so the gate covers none of them.
- `TODO(publish-tick-tie)` (`store_db.py:61`) — holds: the only `publish` caller is `plato/seed.py:160`, one call per pack directory with a blob write in between.
- `TODO(refused-rule-field-claim)` (`default.py:498`) — holds: `allowed` is already False from the refusal, and the `EVIDENCE_MISSING` `continue` two lines down skips `this_policy_fields.update` identically.
- `TODO(activation-vs-active-rules)` (`policy.py:758`) — holds: `executable_rules`/`activation_blocks` have no callers outside `tests/test_policy_rule_activation.py`.
- `TODO(layout-key-resolution)` / `TODO(layout-live-formula-order)` (`workbook.py:177,183`) — both hold: `workbook.py:413` is the `item is None: continue`, and `workbook.py:690` reads `inputs_by_statement.get(...)` before line 694 populates it.
- `TODO(orphan-blob-gc)` (`store_db.py:319`) — holds; the store is tenant-scoped at construction so it cannot run the cross-tenant count.

Full suite: 5430 passed, 20 skipped. `ruff check` clean.

---

**[DEFECT]** `jazzx_sdk/experts/policy/default.py:663` — adding `effective_date` to `Policy.is_active` routes a future-dated ACTIVE policy into the `not policy.is_active` branch, which `continue`s before check 3, so the pinned-version supersession check no longer runs for it. Trigger: registry holds `P1` `status: active, effective_date: now+30d, version: "2.0"`, package pins `{"P1": "1.0"}` → `detect_staleness(["P1"], pinned_versions={"P1":"1.0"})` returns `stale_policies=[]` (verified by running it); before this diff `is_active` ignored `effective_date`, so `P1` fell through to check 3 and was reported as `"pinned version '1.0' superseded by current version '2.0'"`. `detect_stale_pinned_policies` returns `report.stale_policies` only, so `not_yet_effective` does not compensate — the pin is invisible to the FR-SOP-5 gate on both counts.
IMPORTANCE: medium -- silent (returns `[]`, no error), but no shipped configuration reaches it: every `effective_date` in `plato/data/seed_packs/*/policies/*.yaml` is in the past, and there is no in-repo caller of `detect_stale_pinned_policies`. It is re-exported by jaci at `capabilities/commercial_lending/__init__.py:106` and reached through `validate_package`, so an externally authored future-dated policy — the case this diff added support for — triggers it.

**[DEFECT]** `jazzx_sdk/finance/validation.py:839` — the `if not refusals and warnings` fallback converts *every* string in `warnings` into a `DefectClass.POLICY_VIOLATION` / `Severity.POLICY` finding, but `ComplianceResult.warnings` is documented one file over (`experts/policy/base.py:57`) as "Compliance warnings. Mostly non-blocking, with one exception" — so a producer that fills it with ordinary advisories has all of them reclassified as policy violations. Concrete producer: jaci `src/jaci/scenarios/aml/experts/policy.py:236` appends `"Confidence below group threshold: 0.72 (min 0.85)"` and constructs `ComplianceResult(allowed=..., warnings=warnings, ...)` at line 265 with no `refusals`; feeding that result to `validate_package(compliance_result=...)` yields a POLICY-severity violation finding for a confidence advisory that violated nothing, even when `allowed=True`.
IMPORTANCE: medium -- fails visibly (a spurious finding, not a wrong number). I enumerated the producers: `DefaultPolicyExpert` (both fields set, unaffected), `tests/test_experts_contracts.py:70`, and jaci's AML expert. The path in is `jaci/capabilities/credit_validation/provides.py:36` → `validate_package`; I could not establish that the AML expert is currently wired to that call, so the trigger depends on which expert a pack binds.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/policy.py:126` — `as_utc` is a pure datetime coercer placed in `policy.py`, which forces `fabric/guidance/schema.py:95` to do a function-local `from jazzx_sdk.fabric.canonical.policy import as_utc` inside `is_active` to avoid the import cycle. This diff just created `jazzx_sdk/manifest/lifecycle.py` as the dependency-free home for exactly this problem (`PolicyStatus` moved there so `profiles` could use it without cycling back). Putting `as_utc` there too is the same one-symbol move and drops the deferred import; six sibling modules already carry private copies (`manifest/store_db.py:85`, `pack/store_db.py:115`, `agents/interactive/session_db.py:64`, `observability/span_mapping.py:67`, `audit/events_db.py:65`, `queue/execution_db.py:36`), so a shared public one has somewhere to land.
IMPORTANCE: low -- no behaviour change; the deferred import works. Affects only where the symbol lives.

**[NOTE]** `jazzx_sdk/fabric/graph/check.py:226` — `_UNTESTED_RATIONALE` enumerates 3 of `IndeterminateReason`'s 4 members and leans on the fallback for `EVIDENCE_MISSING`, so a fifth member added later silently gets the "the case does not supply `<reads>`" sentence — the exact false assertion this helper was added to stop. A dict comprehension over the enum, or a mapping that includes `EVIDENCE_MISSING` explicitly and raises/omits on an unmapped one, makes the omission a load-time error rather than a wrong sentence.
IMPORTANCE: low -- one misleading rationale string in a `Finding`; no wrong verdict. Reached from `finding_from` (`check.py:277`), whose only in-repo caller is `check_graph`.

**[NOTE]** `jazzx_sdk/pack/store.py:94` — `_SKIP_NAMES` excludes `.DS_Store` and `__pycache__` but not `.git`, so `pack_archive` on a pack directory that is its own checkout embeds the whole object store in the published archive. Not reachable from `plato/seed.py:160` (the bundled seed dirs are not repos), but `pack_archive` is exported from `jazzx_sdk.pack` for consumers publishing arbitrary directories.
IMPORTANCE: low -- archive bloat, no wrong data; only reachable through the exported function, not any in-repo call site.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:681` — `" and ".join(causes) or "not active"`: inside the `if not policy.is_active` branch, `is_active` is `status is ACTIVE and not_in_force() is None`, so at least one of the two `causes` appends always fires. The `or` fallback is unreachable.
IMPORTANCE: low -- dead expression, no behaviour.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED**
- `TODO(omit-info)` (plato/info_api.py:83) holds: the only `dashboard_html` callers are plato/app.py:279 (no `omit`) and plato/__main__.py:319 with `degraded_nav = ("Guide","config","database","Packs","packs")`, which does not contain `info`. The reworded claim about `_info_only_app` is accurate.
- `TODO(seed-pack-vendoring)` (plato/seed.py) holds: both bundled packs load in a japes-only process (`Pack.from_manifest` → 6 policy ids for `ci-spread-core`, 1 for `dscr-core`, profile/table refs resolve, `find_profile_literal_drift` clean, grid is 99 cells / 70 eligible / 29 `NA` as the notes claim). `policies/dir` globs one level, so `overlays/deal_si_rb_example.yaml` stays out and the checklist is skipped by shape, as the manifest says.
- `TODO(layout-key-resolution)` (jazzx_sdk/finance/workbook.py:169) holds: `load_workbook_layout` is shape-only. Both new layouts and `spread_template_rb.yaml` validate and every `key` in them resolves against `chart_of_accounts.yaml`/`metrics.yaml`. `tests/test_plato_migration.py` passes with 0004 (6 passed), including the sqlite up/down round trip.

---

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:803` (same shape at `:825` PA and `:735` MS) — the `all_of` applicability gates on `residential_unit_count`, which reads INDETERMINATE when the field is absent or `None`; `check_compliance` skips a rule whose gate is not SATISFIED (`jazzx_sdk/experts/policy/default.py:487`) and records nothing, so the state PPP-buyout rule silently does not run in exactly the case the carve-out exists to catch. Verified directly: gate verdict is INDETERMINATE for `residential_unit_count: None` and for the key being missing, SATISFIED for `2`.
Trigger: Ohio two-unit property submitted as `property_type: two_to_four_unit` with the unit count unstated, `prepayment_penalty_requested: 3yr_3pct_fixed` → `DSCR-PPP-OH` never evaluates and the loan clears with a penalty Ohio requires bought out.
IMPORTANCE: medium -- the corpus ships here (`plato/seed.py` publishes it into any tenant that calls `POST /packs/initialize`) and the same rules are the live jaci corpus, whose `LoanApplication._derive_residential_unit_count` (jaci `src/jaci/scenarios/dscr/schemas/dscr_schemas.py:203`) deliberately leaves the field `None` for `two_to_four_unit`; jaci added a validator for the Illinois `interest_rate` limb only, so OH/PA/MS have no equivalent guard. Nothing in japes evaluates the corpus outside tests, so the wrong verdict lands in the consumer, not in plato.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/workbook_layout.yaml:8` — "every `key` here must resolve against one of those two, which is this file's own load-time acceptance check (see workbook_layout.py's validator)" claims a check that does not exist: `load_workbook_layout` validates shape only, its own `TODO(layout-key-resolution)` names this false claim in shipped packs, and there is no `workbook_layout.py` (the code is `jazzx_sdk/finance/workbook.py`). An unresolvable key loads clean and `_write_statement_sheet` emits a labelled empty row with no exception entry.
IMPORTANCE: medium -- no live effect today (I resolved all rows in both layouts against the pack's chart of accounts and metric catalog); the cost is the next pack author trusting a check that is not there.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:67` — states `residential_unit_count` has "default `1`". The consumer's schema no longer defaults it for `two_to_four_unit`, which is precisely the case that produces the defect above; as written the notes read as if the OH/PA/MS gates always have a value.
IMPORTANCE: low -- documentation inside a vendored pack snapshot; no code reads it.

**[NOTE]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:663` — `interest_rate > 8` declares no unit, and this corpus mixes conventions (`cltv_pct` whole percent here, `ar_advance_rate: 0.85` decimal in `ci-spread-core/policies/conventions.yaml`). A consumer supplying `0.085` makes the limb false rather than an error, so `DSCR-PPP-IL-2` skips permissively.
IMPORTANCE: low -- jaci's field documents "percent (e.g. 7.5)" and requires it for the IL case, so the only known consumer is correct; unknown for any other consumer of the bundled corpus.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(if-match-post)` (`plato/posture.py:96`) — holds. `require_if_match` is referenced only from `tests/test_plato_preconditions.py`; no router mounts it, so the new `POST .../packs/initialize` cannot 428.
- `TODO(azure-blob-container-singleton)` (`plato/wiring_default.py:657`) — holds. `common/core/storage/async_azure_provider.py:54` returns `cls._instance` before reading `container_name`.
- `TODO(seed-digest-drift)` (`plato/seed.py:151`) — holds. `pack_archive` uses `zipfile.write`, which records each file's mtime, so a rebuilt image would digest differently.
- `TODO(seed-pack-vendoring)` / `TODO(local-wiring-build-test)` — hold; both are accurately scoped to what they name.

`tests/test_plato_packs_api.py` is green (51 passed), ruff clean on every changed file.

---

**[DEFECT]** `plato/posture.py:168` — `relaxed_only` gates on `strict_mode()`, which `JAPES_STRICTNESS=relaxed` turns off on every tier except `production`, so the "(local, dev-daily)" guarantee in the docstring (152), in the served 403 text (171), and in `plato/packs_api.py:10-12` ("a pack store whose rows can go in staging is not one") is not what the code enforces.

Trigger, verified by running it: `JAPES_ENVIRONMENT=staging JAPES_STRICTNESS=relaxed` → `strict_mode()` is `False` and `relaxed_only("pack deletion")` returns. On that replica `DELETE {prefix}/packs/{pack_id}/{version}` tombstones a published pack version and `POST .../packs/initialize` writes rows — and since no shipped wiring sets `PlatoWiring.auth` (per `withholding`'s own docstring), the caller need not be authenticated.

IMPORTANCE: medium — blast radius is the two `relaxed_only` call sites, which are the only ones in the repo (`plato/packs_api.py:161,182`; `config_api.py:249` keeps its own `withholding` gate). Reachable only when an operator has set `JAPES_STRICTNESS=relaxed`, which `strict_mode`'s docstring explicitly permits for staging/dev-daily; nothing outside this repo calls `relaxed_only`.

**[DEFECT]** `plato/seed.py:86` — the anonymous/unreadable-manifest fallback publishes a row derived from `manifest.yaml` while `pack_archive` (line 160) archives the whole directory including the bad `pack_manifest.yaml`, and every reader of the archive prefers `pack_manifest.yaml` (`MANIFEST_NAMES` order, `plato/packs/domain.py:97`). The directory lands in `published` (174), so an authoring error is returned as an ordinary success.

Trigger, verified by running it: a seed root directory holding `pack_manifest.yaml` = `pack_id: [unclosed` beside a valid `manifest.yaml` publishes `{'published': [{'pack_id': 'two-names', 'version': '0.1.0'}]}`, and the materialized tree contains both files with the unparseable one winning — so `compose_domain_packs` logs "could not be loaded; not composed" and the pack is never in the tenant's registries, while `/packs` shows it with `has_archive: true`. The same divergence occurs one input over for the *readable*-but-anonymous case (`tests/test_plato_packs_api.py:test_an_anonymous_manifest_does_not_hide_an_identified_sibling`), where the row's `pack_id` is `real-core` and the archive's primary manifest declares none.

IMPORTANCE: medium — reached only through `POST .../packs/initialize` with `PLATO_SEED_PACKS_DIR` pointed at a tree carrying both manifest names (the documented developer path); the two bundled seed packs each carry only `pack_manifest.yaml`, so no default configuration triggers it. Consumers are `plato/packs/domain.py:89` and `plato/packs/loader.py:262`, both in-repo.

---

No [SYMMETRY] findings: `tenant_of` was extracted and applied to both tenant-resolving routers, `relaxed_only` was lifted out of the router closure, `_NAV_LINKS`/`degraded_nav`/`_info_only_app` were all updated together, and `withholding` is computed at factory time exactly as `logs_api`/`metrics_api`/`database_api` do.

**[NOTE]** `plato/packs_api.py:81` — the `try`/`except` around the `entry` dict literal has no reachable trigger for the same reason the comment at line 68 gives for *omitting* a handler on `sorted(latest, ...)`: `PackVersionRecord` is frozen with every field defaulted and `pack_id` is a non-null primary key. The comment at 71-74 nonetheless calls both guards "load-bearing".
IMPORTANCE: low — only `pack_inventory`'s two callers (`GET {prefix}/packs`, tests); worst case is one dead branch to keep true.

**[NOTE]** `plato/packs_api.py:93` — "a pack row cannot exist with zero live versions, so an empty list here is unreadable rather than absent" is true of one snapshot, not of two queries: `list_packs()` and `versions()` are separate reads, so a delete of a pack's last live version landing between them yields `versions == []` with no `versions_error`, and the page renders exactly the "Versions 0 beside a populated Last published" the comment says is impossible.
IMPORTANCE: low — needs two concurrent operators (the page's own `remove()` awaits before calling `load()`), and the cost is one misleading row until refresh.

**[NOTE]** `plato/static/packs.html:152` — the comment names `_actor()`, which this same diff renamed to `acting_user_id` (`plato/settings_store.py:221`).
IMPORTANCE: low — comment only; the page's behaviour and the UUID-parsing claim beside it are both accurate (`get_current_user_id` returns `None` for a non-UUID).

VERDICT: BLOCK
