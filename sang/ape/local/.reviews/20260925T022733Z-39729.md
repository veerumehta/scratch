# adversarial review -- .

- when: 2026-09-25T02:27:33Z
- range: 3974065..HEAD (since the last review) (07e5db8)
- diff: 90 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Nothing blocks. All four findings from the last round are fixed or checked, and this diff adds no defects.

WAS: jazzx_sdk/pack/rule_merge.py :: skipped-duplicate-remains-mergeable -- STILL OPEN. The TODO's deferral still holds (CHECKED last round), and this diff does not touch the file.
CHECKED: jazzx_sdk/pack/rule_merge.py :: skipped-duplicate-remains-mergeable -- the TODO says the skipped proposal stays ACCEPTED and a second merge is last-wins. That is still true.
WAS: jazzx_sdk/experts/policy/default.py :: conditionless-gated-rule-replaces-nothing -- FIXED. The gate now runs whenever `applicability` is set (line 479). A conditionless rule whose gate is satisfied reaches line 491 with `gate_outcome` set, so it replaces its own id.
WAS: jazzx_sdk/experts/policy/default.py :: carryover-ignores-own-gate -- FIXED. A carry-over now needs the superseded rule's own gate to be SATISFIED (lines 469-472), the same rule the main path applies at line 487.
WAS: jazzx_sdk/experts/policy/default.py :: suppression-carries-replacements -- FIXED. Suppressions are now merged after replacements and tracked in `suppressed_ids`, and a suppressed rule's `replaces` no longer carry. Merge order within one policy is unchanged: before this diff `.update(replacing)` overwrote the pre-seeded suppression, and now the replacement lands first and the suppression is skipped as already present.

[NOTE] tests/test_default_policy_expert.py:302 :: shared-id-gate-untested -- The fix for `conditionless-gated-rule-replaces-nothing` has no test.
Trigger: the only conditionless test, `test_a_conditionless_rule_replaces_only_where_its_gate_passes`, uses `replaces=["BASE-LTV"]`, so it took the old `or rule.replaces` branch and passes against the old code. A lower rule `X` plus a higher, conditionless, gated rule that reuses the id `X` would fail against the old code and pass now.
IMPORTANCE: low -- no shipped YAML has a conditionless rule reusing a lower id (checked last round). The new test covers the other two fixes, and both of its assertions fail against the old unconditional carry-over.

VERDICT: PASS
