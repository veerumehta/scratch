# adversarial review -- japes

- when: 2026-09-09T21:45:35Z
- range: HEAD~1...HEAD (d4154f3)
- diff: 3189 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `plato/posture.py:97` `TODO(if-match-post)` — claim holds: `require_if_match` appears only in `plato/posture.py` and `tests/test_plato_preconditions.py`; no route wires it as a dependency.
- `jazzx_sdk/pack/store_db.py:47` `TODO(publish-tick-tie)` — claim holds: nothing outside `tests/` calls `publish` (grep across `jazzx_sdk`, `plato`).
- `plato/database_api.py` `TODO(partial-registration-marker)` — claim holds: `store = getattr(db, "current", db)` hands `register_all` a real `DbStore`, and the "eight" in the comment matches the 8 keys now in `TABLE_SCHEMAS`.
- `plato/wiring_local.py:_find_root` docstring "`WORKDIR /app` has no `pyproject.toml`" — holds: `Dockerfile:81-99` copies only `common/`, `jazzx_sdk/`, `plato/`, `jazzx_eval_contracts/` into `/app`.
- `plato/wiring_default.py` docstring "`del previous` below" — holds; the statement is at the end of `swap_database`.

---

**[DEFECT]** `jazzx_sdk/pack/manifest_loader.py:247` — `profile_files()` calls `_declared_files(..., what="policy profile")` with no `keep`, so every `*.yaml`/`*.yml` under the declared `profiles/` dir is fed to `PolicyProfile.from_yaml`, whose `profile_id` is required. One non-profile file in that folder makes `Pack._profiles_by_path` (`pack.py:201`) raise a pydantic `ValidationError`, so `pack.policy_profiles` and `pack.policy_profile()` return nothing at all instead of the pack's real profile. `policy_files()` on line 213 passes `keep=is_policy_document` for exactly this reason, and `is_policy_document`'s own docstring names the case ("a folder that holds policies *and* other pack assets"). Trigger: a pack with `profiles:\n  dir: profiles` and a `notes.yaml`, a `schema.yaml`, or a half-written profile in `profiles/`.
IMPORTANCE: medium -- reachable by any pack author; hits `Pack.policy_profile`/`policy_profiles`, both new public accessors, and `plato/packs/domain.py:domain_pack_summary` swallows it into a `policy_profiles_error` type name so the operator surface shows the pack as profile-less. It fails loudly at the direct call site. Nothing outside this repo calls the accessors yet (grep: only `tests/`), so the exposure is the first consumer that adopts them.

**[DEFECT]** `plato/packs/loader.py:246` — `roots.append(await store.materialize(...))` is unguarded, and `tenant_registries` → `load_pack` raises `PackLoadError` on any pack it cannot read, so one broken published row aborts the whole composition. That is the failure the surrounding docstring says it refuses ("Raising would let one broken row make the tenant unservable, which is the defect directly above"); only the `content_ref is None` and non-assistant cases were actually guarded. Trigger: `publish` validates nothing about `archive`, so `publish(assistant_manifest, archive=b"not a zip")` succeeds and every later `published_registries(store)` for that tenant raises `zipfile.BadZipFile` out of `_unpack`; a well-formed zip with no `profile/` dir gives `PackLoadError` the same way, and a blob the store no longer has gives `PackNotFound`. `plato/packs/domain.py:78` has the identical gap, plus `loader.depends_on()` on line 84, which raises `PackDependencyError` for a manifest declaring an unknown `fragments:` value.
IMPORTANCE: low -- no shipped caller: grep for `published_registries`, `published_domain_packs`, `DbPackVersionStore` outside `jazzx_sdk/pack/` and `plato/models.py` hits only `tests/test_pack_store.py`, and there is no publish route, so no row exists in any deployed configuration. A `TODO` on both call sites is a complete response; it becomes medium the moment a publish endpoint lands, since the blast radius is the whole tenant rather than the one row.

**[SYMMETRY]** `plato/packs/domain.py:63` and `plato/packs/loader.py:236` — the assistant/domain discriminator is the bare literal `"assistant_id"` spelled independently in both modules, while `MANIFEST_NAMES` in `jazzx_sdk/pack/store_db.py:396` was deliberately shared between the same two readers, with the comment "a second module picking one side is how the store and its reader disagreed about what a pack is once already". The consistent version is one predicate beside `PackVersionRecord` in `jazzx_sdk/pack/store.py` (e.g. `is_assistant_pack(record)`), which is less code than the two docstrings currently spent explaining that the two modules agree.
IMPORTANCE: low -- both call sites are in this repo and both are test-only today; a drift here silently routes a pack to neither resolver.

**[NOTE]** `plato/oidc.py:375`, `:395`, `:424` — this diff adds `limit=200` to `jazzx_sdk/server/console_api.py:132` and writes "pass `limit` wherever the result is served or stored" into `reason`'s docstring, then adds three unbounded `reason(exc)` calls whose text becomes a `TokenError` message a consumer turns into a 401 body. `TokenError` is caught nowhere in this repo, so the cap is the consumer's to choose, but the family got the knob unevenly in a single change.
IMPORTANCE: low -- no in-repo caller renders these; the cost is a long 401 detail for an external consumer.

**[NOTE]** `jazzx_sdk/pack/store.py:120` — the abstract `materialize` docstring names only `PackNotFound`, but the implementation can also raise `ValueError` (digest mismatch, no blob store, no manifest in the archive) and `zipfile.BadZipFile` (archive that is not a zip, which `publish` accepts).
IMPORTANCE: low -- affects a caller writing a `try` against the abstraction; there are none yet.

**[NOTE]** `jazzx_sdk/pack/pack.py:174` — the duplicate-`profile_id` message reads `({path.name} and another)`; it names the second file found and not the first, so an author still has to grep the folder to locate the pair.
IMPORTANCE: low -- one extra step for a pack author debugging a copied profile.

VERDICT: BLOCK
