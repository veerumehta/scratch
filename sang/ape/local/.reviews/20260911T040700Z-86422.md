# adversarial review -- japes

- when: 2026-09-11T04:07:00Z
- range: 10e5501..HEAD (since the last review) (457172f)
- diff: 160 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/manifest/lifecycle.py :: as-utc-copies-unconverted -- FIXED; the enumeration is now seven helpers plus `automation/schemas`' two inline coercions, which matches every `tzinfo` coercion site in `jazzx_sdk` and `plato` (10 hits, 9 modules).
WAS: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer -- STILL OPEN; untouched, the rule still returns INDETERMINATE on a context without the field.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; untouched, the four rules still report NOT_APPLICABLE on an absent field.
WAS: plato/seed.py :: vendoring-todo-truncated -- FIXED; the dangling "Two" is gone and the paragraph ends cleanly.
WAS: plato/posture.py :: relaxed-only-fails-open-on-unknown-tier -- STILL OPEN and now carries a `TODO` whose third claim does not hold in plato's process; raised below rather than restated, per the `TODO`-check rule.
WAS: plato/data/seed_packs/ci-spread-core/pack_manifest.yaml :: ddtl-missing-from-loanpkg-applies-to -- FIXED; `pb-ci-loanpkg-001` now carries all four values, and the only remaining list without it (`:78`, `[abl, revolver]`) is the ABL playbook, which should not have it.
WAS: tests/test_plato_packs_api.py :: manifest-names-rehardcoded-in-helper -- FIXED; `_manifest_of` is imported and the anonymous-sibling fallback comes with it.
WAS: plato/data/seed_packs/dscr_core/ENCODING_NOTES.md :: custom-key-count-stale -- FIXED; 7 + 12 = 19 re-derived from `dscr_profile.yaml`, both §1 and §3 G4 updated, and the six unreferenced keys are still exactly six (`itin_loan_amount_max` is consumed at `eligibility.yaml:420`).
WAS: plato/data/seed_packs/ci-spread-core/playbooks/term_loan.md :: only-rating-scale-claim -- FIXED; §term-004's FCCR bands do agree with §term-007's band-for-band.
WAS: tests/test_plato_packs_api.py :: empty-parametrize-is-a-skip -- FIXED; the collection-time `assert ids` turns an unidentified tree into a collection error.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested

[DEFECT] plato/posture.py:179 :: relaxed-only-fails-open-on-unknown-tier -- the new deferral rests on three claims and the third is false for the process this gate runs in: nothing in `plato` ever constructs `JAPESSettings`, so the "validates the name separately and loudly" half never fires at startup, and it would not cover three of the four variables `environment_tier` reads even if it did.
`ENVIRONMENT=prodution` (or `APP_ENV=stg`, or `JAPES_ENV=staging2`) on a deployed plato replica: `environment_tier()` resolves LOCAL, `relaxed_only` admits the pack delete and the seed write unauthenticated, `check_settings` returns at `posture.py:50` without refusing to start, and `withholding` serves. `JAPESSettings.model_config` sets `env_prefix="JAPES_"` with field `environment`, so its `Literal[ENVIRONMENT_NAMES]` binds `JAPES_ENVIRONMENT` alone and never sees the other three names in `_POSTURE_ENV_VARS`. The first two claims do hold: `envvars.py:242-245` documents the LOCAL fallback in those words, and `deployed_posture`/`withholding` do read it the same way.
IMPORTANCE: medium -- I grepped all of `plato` for `JAPESSettings`/`get_runtime_settings` (zero hits outside this comment), and the SDK's only construction site is lazy inside `client_layer.py:405` on first LLM use, so a typo produces no startup error and no log line. Reachable only through operator misconfiguration, but silent when it is: the gates it opens are `POST {prefix}/packs/seed` and the pack delete, and the caller sees an ordinary 200.

[NOTE] plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:52 :: all-four-applies-to-overstated -- the replacement for "two" says "all four playbook `applies_to` lists" spell the value long, but only three do; `pb-ci-abl-001` at `pack_manifest.yaml:78` is `[abl, revolver]` and correctly names no term-loan variant at all.
IMPORTANCE: low -- comment only; I enumerated all four `loan_type:` lists in the manifest and all 9 `delayed_draw`/`ddtl` occurrences in the pack.

VERDICT: BLOCK
