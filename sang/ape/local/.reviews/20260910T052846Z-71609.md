# adversarial review -- japes

- when: 2026-09-10T05:28:46Z
- range: origin/v2.5.1...HEAD (44eb108)
- diff: 9526 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(threshold-grid-provenance)` (profiles.py:213) — holds. `tables` is excluded from `_coerce_threshold_mappings` and `get_table` is unchanged, so a grid cell resolves exactly as before; not a regression.
- `TODO(activation-vs-active-rules)` (policy.py:754) — holds. `active_rules()` filters `legacy_alias` and ignores `exception_rules`; `_all_rules()` does the inverse. `executable_rules`/`activation_blocks` have no non-test callers (grepped `jazzx_sdk`, `plato`, `tests`).
- `TODO(orphan-blob-gc)` (store_db.py:279) — holds. `delete` issues only the row delete; the blob key is `packs/sha256/<content_digest>` with no tenant component (store_db.py:186), so a cross-tenant count is genuinely required.

## Findings

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:41` — the seeded pack points `policies.registry` (and `conductor.pipeline`, :148; `experts.*.class`, :135/:138; `modes.evaluator.class`, :125) at `jaci.*` dotted paths. `Pack.policy_registry` resolves that pointer through `importlib.import_module` (`jazzx_sdk/pack/pack.py:147`, and `:346` for the conductor); `jaci` is not in japes' `pyproject.toml` and `import jaci` raises `ModuleNotFoundError` in this venv. `plato/seed.py:44` makes `plato/data/seed_packs` the default and publishes it "with no configuration", so the image ships a pack that cannot be loaded. The pointer also shadows the working declarative route the pack already carries (`policies/core.yaml`, `pipelines.yaml`), which `policy_registry`'s fallback would have used.
Trigger: `plato` initialize → `seed_packs` publishes `ci-spread-core` → any consumer touching `Pack(...).policy_registry` or `.conductor`.
IMPORTANCE: medium -- enumerated call sites `pack.py:147` and `pack.py:346`; reachable in every deployment that seeds the bundled directory (the documented default); fails loudly at load. Sibling manifests `cl_of_core`, `cl_sp_core`, `clinical-intake-core` carry the same `jaci.*` pointers, so this is the whole seed tree, not one file. External consumers (jaci) are the only ones for whom the import currently resolves.

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:65` — `Threshold` sets `frozen=True` but not `extra="forbid"`, so pydantic silently drops unmodelled keys. `_coerce_threshold_mappings`' own comment claims the opposite ("an unmodelled key … Left as authored rather than failing the whole profile load; both accessors refuse it below"). A misspelled governance key therefore fails *open* on the field the module exists to govern: `{value: "1.2", status: active, provinance: secondary_source}` builds a `Threshold` with `provenance=INSTITUTION_AUTHORED, status=ACTIVE`, which `get` serves and `get_threshold` reports as the institution's own. Verified by running it — `{value:'1.2', status:'active', provenance:'official_source', sourse:'typo'}` yields `source=None` with no error.
IMPORTANCE: medium -- silent wrong provenance, which is what `ThresholdProvenance`/`policy_lint` are for; reachable from any pack `profiles/*.yaml`; consumers are `PolicyProfile.get` (3 call sites: `condition_evaluator.py:279`, `statemachine/schema.py:135`, `validation.py:600`), `get_threshold`, and `policy_lint.find_profile_literal_drift`. `extra="forbid"` lands it in the `except ValidationError` branch that already refuses it.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:432` — `UCAcashFlow`'s formula uses `D&A`, `+/-` and `working_capital_change`, none of which are field names or declared on `FinancialPeriod`, and its `inputs:` list names four different fields. `derivations_from_pack_ontology` rejects it (`"term 'D&A' is not a field name or number"`), which makes `DebtServiceCoverage` (:438) — whose numerator is the bare name `UCAcashFlow` — a *supported* derivation that can never resolve. Ran it against this file: `{'values': {'Leverage': 3.0, 'FCCR': 4.25}, 'unresolved': {'DebtServiceCoverage': "'UCAcashFlow' is not asserted"}}`. This is the same undeclared-field class the `v0.2.1` note at :13 says it fixed for FCCR and DebtServiceCoverage's denominator; the numerator was not audited.
IMPORTANCE: medium -- one caller, `Pack.derivations` (`jazzx_sdk/pack/pack.py:125`), reached by any consumer of the seeded pack; fails visibly in `unresolved` rather than producing a wrong number.

**[SYMMETRY]** `jazzx_sdk/fabric/guidance/schema.py:92` — this diff generalized naive-bound handling into `as_utc` (`policy.py:129`) and exported it from `fabric.canonical`, then applied it only inside `Policy`/`Rule`. `GuidanceAsset.is_active` in the same fabric package has the identical shape (`self.expires_at > now` against an aware `_now()`) and raises on the exact input `as_utc` exists for: a YAML `expires_at: 2027-01-01` gives `TypeError: can't compare offset-naive and offset-aware datetimes` (verified). One call: `as_utc(self.expires_at) > now`.
IMPORTANCE: medium -- reached on every retrieval via `fabric/guidance/retrieval.py:49`, which calls `is_active(now)` for each hit; triggered by any pack authoring a date-only `expires_at`. Loud failure, but it takes out the whole retrieval, not one asset.

**[SYMMETRY]** `jazzx_sdk/finance/validation.py:817` — `experts/policy/default.py:503` formats `policy_id`/`rule_id` into a warning string, and `_WARNING_REFS` regex-recovers them one layer up; both files are in this diff. The structured pair could travel on `ComplianceResult` (a dict-shaped warning, or a `not_activated` field beside `warnings`) so the consumer never parses prose. That is no more code than the formatter plus the regex plus `_warning_refs`, and it removes the `detail`-key asymmetry below.
IMPORTANCE: low -- affects only the two files in this diff plus `ComplianceResult`'s schema; the regex works on the one producer, so nothing is wrong today.

## Notes

**[NOTE]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:398` — "Formulas are documentation only - runtime computation is in the Reasoner and compute_ci_metrics()" is false: `jazzx_sdk/fabric/graph/derive.py:154` consumes `concept_graph[*].formula`, and `Leverage`, `FCCR` and `DebtServiceCoverage` all become live `RatioDerivation`s. The comment is plausibly why the DebtServiceCoverage numerator above went unchecked.
IMPORTANCE: low -- comment only, but it is the one an SME editing this file reads before changing a formula.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/addbacks.yaml:1` — the shipped pack data cites `plan_JACI_CL_ADDBACK_LIBRARY.md`, `plan_JACI_CL_CHART_OF_ACCOUNTS_AND_CATALOG.md`, `plan_JACI_CL_RECLASSIFICATION_ENGINE.md` and `plan_JACI_CL_VALIDATION_COMPLETION.md` (also `chart_of_accounts.yaml`, `metrics.yaml`, `breakouts.yaml`, `reclassifications.yaml`, `spread_template*.yaml`). Those are gitignored plan docs in another repo, so no reader of the published pack can resolve them — and naming `plan_*.md` in committed files is a standing rule here.
IMPORTANCE: low -- dangling references in data files; no behaviour depends on them.

**[NOTE]** `jazzx_sdk/finance/validation.py:808` — the warning loop's `detail` omits `policy_id`/`rule_id` entirely when `_warning_refs` finds nothing, whereas the violation loop three lines up always sets both (`None` when absent). A consumer doing `f.detail["rule_id"]` across `POLICY_VIOLATION` findings gets `KeyError` rather than `None`. `_warning_refs` returning `{"policy_id": None, "rule_id": None}` on no match matches the sibling.
IMPORTANCE: low -- no in-repo consumer indexes it that way (only `tests/test_policy_not_activated_reaches_callers.py:227` and `:246`); the review surface the docstring refers to is outside this repo, so I could not confirm which access form it uses.

VERDICT: BLOCK

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
Reviewed against the tree as checked out at the start; partway through, HEAD was rewritten (the packs commit is no longer checked out), so the last item I was verifying — whether `packs.html` hides its delete/seed buttons in a strict posture — is unverified.

**CHECKED:** `DbPackVersionStore.delete`'s `TODO(orphan-blob-gc)`, cited by the new delete handler, holds: the key is the content digest, the store is tenant-scoped at construction (store_db.py:271-292), so reclaiming genuinely needs a cross-tenant count. `TODO(omit-info)` in `info_api.py` also still holds — `packs_html` is a new caller of `nav_html` but passes `omit` through unchanged, and no caller passes `("info",)`.

---

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125,127` — the `purpose_specific_documents` section carries two PDF line-wrap fragments as required documents (`schedule`, `available)`), and the entries they were wrapped from lost their leading text, so the section requires two documents that do not exist and silently no longer requires the two real ones. Trigger: any application assessed against `ci_application_checklist` (the manifest's `policies.checklist`) emits DATA_GAP hypotheses for a document named `available)`. The `description` on line 4 ("Extracted verbatim from the source checklist") states the transcription is faithful, which here it is not.
IMPORTANCE: medium -- silent wrong data, but nothing in japes reads the checklist: `policy_files()` deliberately excludes `checklist:` (manifest_loader.py:220-222), so the only consumer is a caller's `assess_package()` outside this repo.

**[DEFECT]** `plato/data/seed_packs/dscr_core/pack_manifest.yaml:27` — the manifest declares `policies: dir: policies` but no `profiles:` block, so the `profiles/dscr_profile.yaml` this diff adds is never loaded. Verified: `Pack.from_manifest("dscr-core", seed_root).loader.profiles()` is `{}` and `.policy_profile()` is `None`, while `policy_registry` loads all 41 rules — 6 of which resolve `table: profile_table:...` and 4 `threshold: profile:...`. Evaluating any of them with the profile the pack can supply raises `ValueError("MatrixCondition needs a PolicyProfile…")` (condition_evaluator.py:375, and 272 for the ratio rules). `profile_files()` exists for exactly this and its docstring calls a pack in this state "half a pack" — the fix is `profiles: {dir: profiles}`.
IMPORTANCE: medium -- fails loudly, and no japes code evaluates dscr-core today; reachable by any consumer that materializes the published pack, which is the point of publishing it.

**[DEFECT]** `plato/data/seed_packs/cl_of_core/pack_manifest.yaml:53` — `conductor.pipeline: jaci.capabilities.commercial_lending.cl_capability.CL_SPREAD_PIPELINE` points into a downstream consumer that is not a japes dependency (no `jaci` in `pyproject.toml`). Verified: `Pack.from_manifest("cl-of-core", seed_root).conductor` raises `ModuleNotFoundError: No module named 'jaci'` via `_resolve` (pack.py:346). Same for `ci-spread-core`'s `policies.registry` on `.policy_registry`/`.policies`. These packs now ship inside the japes image and are published into every dev-daily database by `POST /packs/initialize`.
IMPORTANCE: medium -- fails loudly at the accessor; reachable from the plato image itself once a pack is materialized, and from any consumer other than jaci. `Pack.experts`/`Pack.modes` return the pointers unresolved, so those are the caller's problem, not an import here.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/core.yaml:1` (and `conventions.yaml`) — the policy YAML added here can never be read in the shipped pack: `Pack.policy_registry` short-circuits on `policies.registry` and only falls through to the declared files when that pointer is absent (pack.py:145-147), and ci-spread-core's manifest has one. Trigger: `Pack.from_manifest("ci-spread-core", seed_root).policies` raises on the jaci import without touching either file; with jaci installed it returns `CI_REGISTRY` and still never reads them. `policies/overlays/deal_si_rb_example.yaml` is doubly unreachable — the manifest's only declared overlay is `policies/overlays/rb_ci.yaml`, which is not in the tree (warn-and-skip, manifest_loader.py:177).
IMPORTANCE: medium -- three added files are dead in every published copy; same call sites as the finding above, nothing outside this repo depends on them yet.

**[DEFECT]** `plato/info_api.py:70,74` — the two new nav entries (`Packs` → `/packs/ui`, `packs` → `/packs`) were added to `_NAV_LINKS` without adding them to `_info_only_app`'s `degraded_nav` (`plato/__main__.py:302`), which mounts only info, logs, metrics and `/`. Verified by rendering the degraded `/`: the nav contains `<a href="/api/v1/packs/ui">Packs</a>` and `<a href="/api/v1/packs">packs</a>`, and `GET /api/v1/packs` on that app returns 404 — the exact dead-link-while-debugging case `nav_html`'s own docstring says the `omit` list exists to prevent. `test_the_degraded_nav_offers_only_what_it_mounts` passes because it asserts only on `/docs`, `config`, `database`.
IMPORTANCE: medium -- two 404s on the unconfigured replica, which is the one an operator opens in a browser; both the dashboard and `/info/ui` render it. No effect on a configured replica, where the router is always mounted.

---

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md` — a dated status document (internal commit hashes `68cfcae`/`272f1e3`/`44f9b55`, "Status, 2026-08-14", `cd jaci && pytest`) ships inside the package data and is zipped into the published archive and every materialized tree by `pack_archive`, which only excludes `.DS_Store`/`__pycache__`.
IMPORTANCE: low -- no behaviour; it is bytes in the blob and a repo-history question, not a runtime one.

**[NOTE]** `plato/data/seed_packs/cl_of_core/pack_manifest.yaml:47` and `cl_sp_core/pack_manifest.yaml:46` — both declare `experts.playbook.diagnose_map: diagnose_map.yaml`, and neither directory contains that file (ci-spread-core does). `Pack.experts` returns the dict unresolved, so nothing in japes notices; the consumer that reads the key gets a missing path.
IMPORTANCE: low -- no japes call site resolves it; the two packs are drafts and the file is named for a Phase 2 import that has not run.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:7` and `core.yaml:37` — `provenance` records source documents by one checkout's relative layout (`docs/LoanSamples/YETI/…`) with `source_type: local_file`. Nothing resolves these, but they are unresolvable anywhere outside the machine they were extracted on.
IMPORTANCE: low -- metadata only; no code reads the `provenance` block.

VERDICT: BLOCK

## slice3

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `profiles.py:213 TODO(threshold-grid-provenance)` — claim holds. `tables` is `dict[str, dict[str, Any]]`, `_coerce_threshold_mappings` is registered only on `thresholds`/`custom`, and `get_table` returns the cell verbatim, so a grid cell is served exactly as it was before this change. Not a regression; the deferral is accurate.
- `info_api.py:83 TODO(omit-info)` — claim holds. `packs_page()` calls `packs_html(prefix)` with no `omit`, so this diff adds no caller that passes `omit=("info",)`.

---

**[DEFECT]** `plato/info_api.py:70,75` (with `plato/__main__.py:302`) — `_NAV_LINKS` gained `"Packs"` and `"packs"`, but the degraded app's `degraded_nav = ("Guide", "config", "database")` was not extended and `_info_only_app` never mounts `create_packs_router`, so the degraded nav offers two links that 404. Verified against `_info_only_app(["boot failed"])`: `/` renders `/api/v1/packs/ui`, and both `GET /api/v1/packs/ui` and `GET /api/v1/packs` return 404. `nav_html`'s own docstring at line 92 still asserts the degraded omission set is those three, and the comment at line 105 states this is precisely what must not happen.
IMPORTANCE: medium -- both callers of the shared nav on the degraded path (`__main__.py:317` dashboard and `create_info_router(omit_nav=degraded_nav)` → `/info/ui`); reached whenever a replica fails to build its real app, which is the one moment an operator is following these links. Working app is unaffected (`app.py:228` mounts packs unconditionally). Nothing outside this repo consumes the nav.

**[DEFECT]** `plato/seed.py:70` — the docstring claims "a directory holding an assistant pack seeds as readily as a domain one", but an `AssistantManifest` carries `assistant_id`/`pack_id`/`pack_version_range` and no `pack_version` or `version`, so line 106 yields `version == ""` and the pack is always skipped. Ran it against `tests/fixtures/manifest_binding/manifest.yaml`: `{'published': [], 'skipped': [{'pack': 'docs-assistant', 'reason': 'manifest declares no pack_id/pack_version'}]}` — and that reason is itself false, the manifest does declare `pack_id: internal-docs`.
IMPORTANCE: medium -- trigger is the documented workflow: `PLATO_SEED_PACKS_DIR` pointed at a consumer checkout containing an assistant pack. Reached from `packs_api.initialize` (relaxed posture only) and direct `seed_packs` calls; no bundled seed pack is an assistant pack today, so no shipped configuration hits it. It reports in `skipped`, so the operator sees *something*, just the wrong reason.

**[DEFECT]** `plato/static/packs.html:329` — `seed()` renders only `published`/`skipped`/`failed` and drops `reason`, so `seed_packs`' `{"reason": "no such directory"}` path (seed.py:97) displays as `"0 published, 0 already there"`. Trigger: `PLATO_SEED_PACKS_DIR` set to a path that does not exist (a typo, or a container that did not mount the checkout) → click Initialize → the page reports a successful idempotent no-op for a misconfiguration. The same string appears for a root that exists but holds no pack directories.
IMPORTANCE: medium -- the only consumer of this text is this page; the route is gated to `local`/`dev-daily` by `_refuse_unless_relaxed`, and `GET`/`POST {prefix}/packs/initialize` still carries `reason` in its JSON for anyone reading the response directly. Fails silently only in the browser.

**[SYMMETRY]** `plato/seed.py:44` — `default_seed_root()` does `Path(override).expanduser().resolve()`, resolving a relative `PLATO_SEED_PACKS_DIR` against the process cwd, while every other `PLATO_*` path variable (`PLATO_LOCAL_DB`, `PLATO_LOCAL_CACHE`) goes through `wiring_local._anchored`, whose docstring records exactly the "launched from anywhere but the root" failure this reproduces. Same one-call shape.
IMPORTANCE: low -- `_anchored` is bound to `wiring_local._REPO` and seed.py sits below that layer, so lining them up means moving `_anchored`, which is more than the diff does. A relative value fails as "no such directory" in the result dict, so the cost is one confusing message. Only affects a developer who exports a relative path.

**[NOTE]** `tests/test_plato_packs_api.py:334` — the assertion compares `[p["pack_id"] for p in published]` against `sorted(...)` of itself under the label "published in directory order", but `seed_packs` sorts by *directory name* and publishes *pack ids*. The five bundled packs happen to sort identically in both (`cl_of_core`/`cl-of-core` vs `clinical-intake-core` agree because `-` < `_` < `i`); a sixth pack whose directory name and `pack_id` disagree on ordering fails the test with nothing wrong.
IMPORTANCE: low -- test-only; names no shipped-code defect.

**[NOTE]** `plato/seed.py:106,108` — the `or manifest.get("version")` fallback has no counterpart in `DbPackVersionStore.publish`, which reads `pack_version` only (store_db.py:145), so the comment "`publish` refuses this too" does not hold for that arm: a manifest declaring `pack_id` + `version` passes seed's identity check, is looked up under a version that can never exist, and lands in `failed` as a bare `ValueError` rather than `skipped` with a reason. No manifest in the repo authors bare `version:`.
IMPORTANCE: low -- no reachable trigger in any shipped seed pack; fails loudly when it does fire.

**[NOTE]** `tests/test_plato_packs_api.py` — nothing exercises `POST {prefix}/packs/initialize` reaching `seed_packs` through the route in a relaxed posture, nor `GET {prefix}/packs` under `withholding`. The strict-refusal side of both writes is covered; the success side of `initialize` is covered only by calling `seed_packs` directly, so the route's `503`-when-unwired arm and its store-per-request resolution are untested.
IMPORTANCE: low -- coverage gap only; the delete twin has both halves (`test_deletion_reaches_the_store_in_a_relaxed_posture`), so the shape to copy is already there.

VERDICT: BLOCK
