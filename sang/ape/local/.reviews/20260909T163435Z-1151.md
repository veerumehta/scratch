# adversarial review -- japes

- when: 2026-09-09T16:34:35Z
- range: 8885b76..HEAD (since the last review) (137a8db)
- diff: 105 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` `published_at.desc()` tie -- **STILL OPEN**; deferred by `TODO(publish-tick-tie)` at `:58`, whose claim still checks out (no `publish` caller ships yet).
- WAS: `store_db.py:312` marker short-circuit -- **STILL OPEN**; `:310-313` still returns the marker file's text as the pack root, no containment check against `root`, no digest re-verify.
- WAS: `store_db.py:181` publish never opens `archive` -- **STILL OPEN**; `publish` still commits arbitrary bytes and the failure first surfaces from `_unpack` at materialize time.
- WAS: `pack/__init__.py:29` `__all__` -- **STILL OPEN**; `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent from the package exports.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` -- **STILL OPEN**; `:320`, `:375`, `:395`, `:424` all interpolate without a length bound.
- WAS: `store.py:54` "sha256 over the archived asset bytes" -- **STILL OPEN**; `store_db.py:167` still hashes the manifest when `archive is None`, so the field comment is false for every manifest-only row (this diff changed `:122`, not `:54`).
- WAS: `store_db.py:167` get-then-insert race -- **STILL OPEN**; no unique-violation handling, blob still written before the row.
- WAS: `store_db.py:140` `visibility` unvalidated -- **STILL OPEN**.
- WAS: `loader.py:214` domain pack aborts composition -- **FIXED** (unchanged from last round).
- WAS: `loader.py:214` manifest-only row raises `PackNotFound` -- **FIXED**; and the replacement's own defect below is now addressed.
- WAS: `tests/test_pack_store.py:353` vacuous freeze assertion -- **FIXED** (unchanged).
- WAS: `store_db.py:364` `_unpack` docstring summary -- **STILL OPEN**; `store.py:122` was corrected to "the directory holding its manifest" while `_unpack`'s summary line still says "return the directory holding ``pack_manifest.yaml``", the one name that is *not* the only accepted one.
- WAS: `loader.py:196` MappingPackSource prose -- **FIXED**.
- WAS: `tests/test_pack_store.py:326` redundant `pack_id` re-set -- **FIXED**.
- WAS: `loader.py:236` archive-less assistant pack dropped at debug level -- **FIXED**; `:240-243` warns and names the pack. Verified reachable and regression-proving: reverting the branch to `logger.debug` makes `test_an_assistant_pack_with_no_archive_is_reported_and_the_rest_compose` fail (`AssertionError: []`). A programmatic caller still gets only a short `manifests` list, which the docstring now states deliberately.
- WAS: `tests/test_pack_store.py:411` test never reached the branch -- **FIXED**; it now publishes `{**assistant_manifest, "pack_id": "broken-pack"}` with no archive, which carries `assistant_id` and so passes the `:238` check into `:239`.
- WAS: `tests/test_pack_store.py:324` comment stated the same fact twice -- **FIXED**.

CHECKED: `TODO(publish-tick-tie)` (`store_db.py:58`) unchanged and its blast-radius claim still holds; not raised.

## 2. New findings

No defects introduced. 16 passed, ruff clean on the three touched files.

**[NOTE] `plato/packs/loader.py:230`** — "The publish gate is where this stops being possible" reads as a description of an existing gate; `publish` (`store_db.py:140-155`) accepts an assistant manifest with `archive=None` without complaint, so nothing stops it today. Triggered by anyone reading the paragraph to decide whether an archive-less assistant row can still be created.
IMPORTANCE: low -- comment-only; I enumerated every `publish` call site (three in `tests/test_pack_store.py`, none in `plato` or `jazzx_sdk`), so no shipped path relies on the gate existing.

**[NOTE] `tests/test_pack_store.py:424`** — the comment records what a draft of the assertion line did (`r.message % r.args` raising `TypeError`) rather than stating the rule the line follows; the repo convention is to state the rule and leave the history to the changelog.
IMPORTANCE: low -- test-only, one file, no shipped-code defect behind it.

VERDICT: PASS
