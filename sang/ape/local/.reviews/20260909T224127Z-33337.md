# adversarial review -- japes

- when: 2026-09-09T22:41:27Z
- range: HEAD~1...HEAD (9511db0)
- diff: 3884 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
Ran the full suite (5320 passed, 20 skipped) and `ruff` over the changed files (clean; the one hit in `tests/test_import_boundary.py:33` is pre-existing). No defects found.

## CHECKED

- `plato/posture.py:97` `TODO(if-match-post)` — holds. `require_if_match` is referenced only by `plato/posture.py`'s `__all__` and `tests/test_plato_preconditions.py`; no route mounts it.
- `plato/migrations/env.py:182` `TODO(migration-concurrent-index)` — holds. The offline branch it names does exist and does wrap the run: `env.py:86` `with context.begin_transaction(): context.run_migrations()`.
- `jazzx_sdk/pack/store_db.py` `TODO(publish-tick-tie)`, `plato/packs/loader.py` `TODO(compose-per-pack-guard)`, `plato/packs/domain.py` `TODO(share-published-row-loop)` — all hold. `DbPackVersionStore`, `publish`, `published_registries` and `published_domain_packs` have no non-test callers anywhere in the repo.
- `plato/database_api.py:96` `TODO(partial-registration-marker)` — holds. `describe_database`'s only in-repo callers are the two inside `create_database_router`, which is constructed in `plato/app.py:208` with the wiring's `DatabaseHandle`, so `store` is always a real `DbStore` with `.repository`.

## Findings

**[SYMMETRY]** `jazzx_sdk/pack/store_db.py:399` — the archive-member guard is a hand-rolled resolve-and-compare (`(root / name).resolve()` then `is_relative_to`) where `jazzx_sdk/path_safety.relative_name` is the shared rule this same diff adopts at eight other new join sites (`manifest_loader._asset_path`, `pack/loader.py:117`, `prompt_loader`, `roots.py`, `evaluation/harness/config.py`), and `jazzx_sdk/manifest/bundle.py:34` already shape-checks zip members with the identical predicate. `relative_name(name, what="pack archive member")` replaces both lines and needs no `root.resolve()` per member. Same applies to the twin at `plato/packs/loader.py:103`, which the diff rewrote rather than converted.
IMPORTANCE: low -- both guards are correct as written; this is placement only. Enumerated: the two archive-member guards in this diff, `bundle.py:34`, and the eight `path_safety` adopters. Nothing outside the repo calls either function (both are test-only today).

**[NOTE]** `jazzx_sdk/pack/store_db.py:333` vs `:346` — `materialize` returns `manifest_root` unresolved on the unpack path and `settled = named.resolve()` on the cache-hit path, so consecutive calls for one version can return two different `Path` values for the same directory. Measured on this box: `tempfile.gettempdir()` is `/tmp`, resolving to `/private/tmp`, and the default `_cache_dir` is built from it — so call 1 returns `/tmp/japes-packs/<digest>` and call 2 returns `/private/tmp/japes-packs/<digest>`. Both open the same tree, and `test_materialize_unpacks_once_per_digest` only asserts equality because pytest's `tmp_path` is already resolved.
IMPORTANCE: low -- no in-repo consumer compares the return value; `published_domain_packs` and `published_registries` only join onto it. No external caller exists.

**[NOTE]** `jazzx_sdk/conductor/engine.py:562` and 18 other sites — `limit=200` is spelled as a bare literal at every new call site, alongside four pre-existing `limit=120` sites (`plato/wiring_default.py:566,587`, `plato/settings_store.py:96,149`) and the hardcoded `1000` in `failures._safe_detail`. Three uncoordinated caps for the same "how much exception text may we serve" question, none named.
IMPORTANCE: low -- cosmetic today; the risk is a future site picking a fourth number. Enumerated all 19 `limit=200` sites plus the four `limit=120` and `_safe_detail`.

VERDICT: PASS
