# adversarial review -- japes

- when: 2026-09-11T02:08:09Z
- range: origin/v2.5.1...HEAD (8b49b90)
- diff: 12526 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No `[DEFECT]` survived verification. Ruff clean; 845 related tests pass (`-k "policy or condition or lint or validation or statemachine or governor or guidance or graph or pack"`, 3 env-gated skips).

Scope note: the diff text I was given is truncated — `plato/packs_api.py`, `plato/tenancy.py::tenant_of`, `wiring_default.py`, the new tests, and most of `ci-spread-core/` (manifest, policies, ontology, spread/workbook templates) are referenced by it but not shown. I read those from the working tree.

## CHECKED

- `TODO(threshold-grid-provenance)` (profiles.py) — holds, and the revised wording is the accurate one: `dscr_profile.yaml` authors all 7 `thresholds:` as bare strings (`dscr_min_high_ltv: '1.20'` …), so `looks_authored` is False for every one, and the grid lives in `tables:` which `get_table` does not gate.
- `TODO(publish-tick-tie)` (store_db.py) — holds. `publish` does have callers, and `datetime.now()` is microsecond-resolution with a blob write and a DB round trip between publishes; two *different* versions of one pack in one tick is not reachable from the seeder or the initialize route.
- `TODO(refused-rule-field-claim)` (default.py) — holds. `allowed=not violations and not not_activated` is already False whenever that branch fires, so the cost is a misleading `policy_violations` entry, not a wrong verdict.
- `TODO(activation-vs-active-rules)` (policy.py `_all_rules`) — holds against `active_rules()` at policy.py:802: it filters `legacy_alias` and reads `self.rules` only.
- `is_not_yet_effective`'s "`effective_date` is required on `Policy`" — holds (policy.py:673, `Field(...)`).
- `IndeterminateReason` family — complete. All 11 `RuleOutcome(verdict=INDETERMINATE)` constructions in `condition_evaluator.py` plus the composite and `segment.py:212` set a reason; no other module in `jazzx_sdk` constructs one.
- `ThresholdNotActivated` family — complete. All three `PolicyProfile.get` call sites (`condition_evaluator.py:318`, `statemachine/schema.py:135` via `engine.apply:92`, `finance/validation.py:601`) handle it, and `evaluate_guard` has no other caller.

## Findings

**[SYMMETRY]** `jazzx_sdk/manifest/lifecycle.py:31` — `as_utc` is introduced with a docstring enumerating six modules that "now have somewhere public to land", and none is converted; `jazzx_sdk/pack/store_db.py:108` is a file this diff edits and keeps its own `_as_utc`. The enumeration also misses a seventh, `plato/reference/model_overlay.py:73`. Converting `store_db._as_utc` to `return as_utc(value) if value is not None else None` is smaller than the copy it replaces.
IMPORTANCE: low — I enumerated all seven copies by grep; each is module-private with in-repo callers only, and behaviour is identical, so this is duplication rather than drift today.

**[SYMMETRY]** `jazzx_sdk/finance/validation.py:813` — both new branches set `detail["source"]` (`"compliance_refusal"`, `"compliance_warning"`) and justify always-present `policy_id`/`rule_id` keys by pointing at "the violations loop above", but that loop (line ~808) sets no `source`. A consumer switching on `detail["source"]` to tell a violation from a refusal gets `None` for the one case the function has always produced.
IMPORTANCE: low — one in-repo caller (`validate_package`); `ValidationFinding.detail` is a free-form dict so nothing breaks, but the review surface this was added for can't distinguish the three kinds.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/` — the new seed pack ships 13 references to the consumer repo across 7 files: `ontology/ci_lending.yaml:179,207,235,254,291` carry `source_schema: jaci.scenarios.ci_spread.schemas.*` as *data* (nothing in `jazzx_sdk` or `plato` reads `source_schema`), `policies/core.yaml:2` names `jaci/scripts/extract_policy.py`, and `metrics.yaml:13` / `breakouts.yaml:2` / `reclassifications.yaml:2` / `workbook_layout.yaml:24` / `spread_template_rb.yaml:12,48` describe the pack against `jaci.capabilities.commercial_lending.*` modules that do not exist in this repo. `pyproject.toml:13` includes `plato` in the wheel, so these ship to every consumer.
IMPORTANCE: medium — reaches anyone who installs the wheel. `dscr_core/ENCODING_NOTES.md` already sets this precedent (6 more, plus jaci commit SHAs), so the fix is a pass over both, not just the new pack.

**[NOTE]** `jazzx_sdk/experts/policy/base.py:38` and `:113` — both `not_yet_effective` descriptions end "…is what `TODO(activation-vs-active-rules)` in policy.py records." That TODO (policy.py:752) records the `_all_rules()`/`active_rules()` divergence over `legacy_alias` and `exception_rules`; it says nothing about `check_compliance` ignoring `stale_policies`/`not_yet_effective`. A reader following the pointer lands on an unrelated deferral.
IMPORTANCE: low — docstring only, but `ComplianceResult`/`PolicyResolutionResult` are exported from `jazzx_sdk.experts.policy`, so pack authors see the text.

**[NOTE]** `jazzx_sdk/fabric/canonical/profiles.py:249` — `get_threshold`'s "A bare value comes back wrapped" is narrower than stated: the new `_value_is_present` validator makes a bare falsy-but-present value raise `pydantic.ValidationError` from inside the accessor, which is neither of the two exceptions the accessor's contract names. `PolicyProfile(profile_id="p", thresholds={"x": ""})` → `get("x")` returns `""`, `get_threshold("x")` raises `ValidationError`; the two accessors disagree about whether the key resolves.
IMPORTANCE: low — grep shows `get_threshold` has no caller outside `tests/test_policy_threshold_provenance.py`, and no shipped profile authors an empty threshold.

VERDICT: PASS

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(conductor-pipeline-from-yaml)` (`pack_manifest.yaml:151`): holds. `Pack.conductor` is `None` for the bundled pack and nothing in japes reads `pipelines.yaml` (`Pack` has no `conductor_pipeline` attribute at all). The deferral is honest.
- `pack_manifest.yaml:55` "it globs one level, so the example deal overlay under `overlays/` stays out": holds — `Pack.policy_registry` resolves exactly the six ids, and `RB_CI_OVERLAY` lands in `overlay_policy_ids` via `scope: product` as claimed (`jazzx_sdk/pack/pack.py:187`).
- `evidence_types.yaml:11` (dscr) "every id here is one `eligibility.yaml` already names": holds exactly — set difference is empty in both directions.

---

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:51` — the ontology enumerates `Facility.loan_type` as `abl | revolver | term_loan | ddtl`, but every matcher in the pack spells the fourth value `delayed_draw_term_loan` (`diagnose_map.yaml:45,54,62`; `pack_manifest.yaml:88,98`). A facility authored to the ontology's own vocabulary with `loan_type: "ddtl"` misses all three `term_loan_*` diagnose rules and falls through to the `fallback` rule, which returns **pb-ci-abl-001, the ABL revolver playbook**, for a delayed-draw term loan — a wrong playbook, not an empty result, and `pb-ci-memo-001`'s `applies_to` excludes it too. `ddtl` appears in exactly one file in the pack; the other five occurrences are the long spelling.
IMPORTANCE: high — silent wrong guidance. I enumerated every `ddtl`/`delayed_draw` occurrence in the pack (6 hits, 3 files) and read `diagnose_map.yaml`'s full rule list including its `fallback`. The consumer (`CIPlaybookExpert.diagnose`) lives outside this repo, so I could not run the match; the vocabulary mismatch itself is entirely inside the diff.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:93` — the first leg of `CI-ABL-MIN-AVAILABILITY` reads `excess_availability_pct`, which is declared nowhere in the pack (the ontology's `BorrowingBaseResult` has `excess_availability` and `total_availability`, no pct; grep finds this field only here). I ran the rule through `AllOfEvaluator`: with `{"excess_availability": 3_000_000}` it returns `INDETERMINATE / evidence_missing`, and `DefaultPolicyExpert.check_compliance` (`jazzx_sdk/experts/policy/default.py:509`) `continue`s on that without recording the rule, so the gate is reported as `allowed=True, "no violations"` having never run. The `>= $2M` leg still catches the underfunded case, so what is lost is exactly the *passing* verdict: the rule can return `VIOLATED` or `INDETERMINATE` but never `SATISFIED` in this pack. The comment above it reasons at length about the *borrowing-base* denominator having no producer and never notices the pct itself has none either.
IMPORTANCE: medium — fails quietly, but only on the permissive side and only for a consumer that builds a `PolicyExpert` from this pack; nothing in japes evaluates it. Verified by running the evaluator against the loaded `Pack` object over four contexts.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/playbooks/term_loan.md:135` — §term-005's leverage rating bands contradict §term-007's in the same file for every value above 2.0x. §005: `3.0–3.5x → PASS/WATCH`, `3.5–4.5x → SPECIAL MENTION`, `>4.5x → SUBSTANDARD`. §007 (`:178`): `3.0–3.5x → SPECIAL MENTION`, `>3.5x → SUBSTANDARD`. A borrower at 4.0x leverage reads SPECIAL MENTION from one section and SUBSTANDARD from the other — a full notch apart, and both sections reach the Reasoner as `guidance_refs` on the same playbook. §term-007 is the one that agrees with `abl_revolver.md:171`'s framework and with `core.yaml`'s 3.5x ceiling; §term-005 is the outlier.
IMPORTANCE: medium — advisory text under a `RECOMMEND_ONLY` autonomy ceiling with a human credit-committee checkpoint, so a reviewer sees it; I compared all three rating scales in the diff (`term-005`, `term-007`, `abl-006`) and the leverage thresholds in `policies/core.yaml`.

---

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:116` — `source_sha: "866474328438fd68"` is the same 16-hex-character truncated digest that `policies/checklist_ci.yaml:11` deliberately renamed to `sha256_prefix` in this diff, with a comment explaining that naming it for what it is matters because the full digest is unrecoverable and nothing verifies against it. The manifest's `source_sha`, `playbooks/loan_package_scaffold.md:1`'s `sha=`, and the ontology's `SourceDocument.sha256` field (`ci_lending.yaml:317`) all keep the name that argument rejects. Renaming the manifest key is the same size edit that was already made one file over.

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/playbooks/abl_revolver.md:98` (and `:215`) — `conventions.yaml:113-124` reworded `CI-ABL-MIN-AVAILABILITY`'s description at length specifically so the source's "10% of the borrowing base" and the encoded "10% of total availability" are both visible to an SME diffing pack against source. `abl_revolver.md` states the bare "10% of BBC or $2M" twice with no such qualifier, and it is the asset the Narrator cites. Adding the same one-clause caveat is smaller than the note already written.

---

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:54` — the profile row says "6 ratio thresholds"; `dscr_profile.yaml` has 7 (`dscr_min_high_ltv`, `dscr_min_over_2m`, `dscr_min_no_ratio_floor`, `dscr_sub_1_ceiling`, `dscr_min_rural`, `dscr_min_fthb`, `dscr_min_thin_file`). The same row's rule counts were explicitly re-derived ("Counts re-derived from the file"), and all of those check out (42 rules, 32/6/2/2 kinds, 12 combinator rules, 99+3+3+12+33 cells) — the profile counts were not. Relatedly, §3 G4 records two orphaned `custom` keys (`fico_min_str`, `seller_concession_max_pct` — both confirmed unreferenced); four of the seven thresholds are equally unreferenced (`dscr_min_no_ratio_floor`, `dscr_min_rural`, `dscr_min_fthb`, `dscr_min_thin_file`) and the note doesn't say so, so "two keys are dead" reads narrower than the 6-of-18 that actually are.
IMPORTANCE: low — a shipped doc only; I re-derived every count in the table against `eligibility.yaml` and `dscr_profile.yaml`.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:412` — `ExcessAvailability.description` says "Committed line minus outstanding balance minus reserves" while the `formula` on the next line is `BorrowingBase - outstanding_balance - reserves`. The committed line and the borrowing base are the two quantities `conventions.yaml`'s availability comment spends a paragraph distinguishing; the inline comment here audited the `inputs` list against the formula and left the description naming the third quantity.
IMPORTANCE: low — the file states formulas are documentation only; nothing evaluates it.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/pipelines.yaml:2` — "Loaded by CIConductor via `ConductorPipeline.from_yaml(..., name=...)`" is the claim `pack_manifest.yaml:151`'s TODO was written to retract ("nothing reads it… reads as available and is inert"). The retraction landed in the manifest and not in the file it describes.
IMPORTANCE: low — comment text; the manifest's TODO is the authoritative record and it is correct.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## [DEFECT] `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:849` (and `:871`, `:781`, `:709`)

Four new PPP rules gate on fields nothing in japes supplies or defaults, and an absent field makes the gate INDETERMINATE, which `_apply_applicability_gate` turns into `NOT_APPLICABLE` — the rule is skipped in the permissive direction. Trigger: evaluate `DSCR_ELIGIBILITY` with `{property_state: "OH", prepayment_penalty_requested: "5yr_step"}` and no `residential_unit_count` → `DSCR-PPP-OH` reports NOT_APPLICABLE instead of denying; same for `DSCR-PPP-PA` (:890), `DSCR-PPP-MS` (:797) and `DSCR-PPP-IL-2` with no `interest_rate` (:709). This is the exact failure mode the file refuses to accept one rule over — `DSCR-OVER-2M-NO-RATIO` exists as a twin precisely because an indeterminate gate skips silently. `ENCODING_NOTES.md:66-71` records the risk and discharges it by declaring both fields required on `LoanApplication`; there is no `LoanApplication` in japes, and `grep residential_unit_count` hits only this YAML and those notes, so the deferral's premise does not hold for the copy this diff ships.

IMPORTANCE: medium -- 4 of the 14 PPP rules in the seed pack that the new `POST {prefix}/packs/initialize` publishes into every tenant. I verified the chain: `ExpressionEvaluator.evaluate` returns `INDETERMINATE/EVIDENCE_MISSING` on a missing field (`jazzx_sdk/fabric/canonical/condition_evaluator.py:187`), `AllOfCondition` propagates INDETERMINATE, `_apply_applicability_gate` (`jazzx_sdk/agents/adjudication/segment.py:117-123`) routes a non-blocking indeterminate gate to `inapplicable_gate_outcome`, and evaluation does not gate on `status: draft` (`policy.py:736` — "evaluation does not use it"). No shipped japes caller evaluates this pack today; the exposure is any caller that hand-builds a context. Outside this repo, jaci holds the authoritative copy whose schema does require both fields, so only the bundled copy is affected.

## [SYMMETRY] `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:415`

`DSCR-ITIN-LOAN-MAX`'s `1000000` is a bare literal with no `domain_extensions.profile_custom_key`, and `profiles/dscr_profile.yaml` has no `itin_loan_amount_max` key — while every other loan-amount bound in the same file resolves through the profile (`loan_amount_min`/`loan_amount_max` at :141/:155, `io_loan_amount_min` at :456). So `find_profile_literal_drift` cannot check it, which is the same silent-drift argument this diff wrote for `DSCR-SUB-1-DSCR-FICO` (:239: "recalibrating the profile left this rule silently on the old number"). The consistent version is one profile key plus a `domain_extensions` block. `ENCODING_NOTES.md:102` already asserts this rule's `$1,000,000` is linked that way, which is false against the shipped rule.

IMPORTANCE: medium -- one rule in the bundled pack; the cost is realized only when the ITIN cap is recalibrated in the profile, which the lint would then not flag. I enumerated every `value:` literal in the file: the remaining unlinked ones are the PPP thresholds (IL `250000`/`8`, PA `319777`, VT `1000000`), which `ENCODING_NOTES.md:94-101` deliberately defers and names; this one is not on that list.

## [SYMMETRY] `plato/info_api.py:70` / `plato/info_api.py:76`

The nav gained `Packs` → `/packs/ui` and `packs` → `/packs`, but the two places that enumerate this family were not extended: `plato/guide.md:32-43` lists every other mounted route (including `/api/v1/info/ui`) and has no `/packs` row, and `tests/test_plato_info_api.py:1441` — a guard named `test_the_guide_lists_the_withholding_on_every_gated_route` — iterates `logs, metrics, database`, so it now covers 3 of the 4 routes that withhold in a strict posture. Trigger: an operator follows the new nav link to `/docs` and finds the route it just came from undocumented, and the guard passes while its name is no longer true. Two lines, in the same file family the diff already edits.

IMPORTANCE: low -- the guide is served content, not behaviour; the only reachable cost is a stale routes table and a guard whose claim is wider than its tuple. Checked all four gated routers (`logs_api`, `metrics_api`, `database_api`, `packs_api`) for the `withheld(...)` call; packs is the fourth.

## [NOTE] `plato/seed.py:83`

The comment credits `test_an_unparseable_manifest_does_not_hide_a_readable_sibling`, which does not exist; the tests that hold that line are `test_an_unparseable_manifest_is_named_not_published_under_its_siblings_identity` (`tests/test_plato_packs_api.py:722`) and its anonymous twin at `:465`.

IMPORTANCE: low -- one comment; the next reader greps for a test name that returns nothing.

## [NOTE] `plato/seed.py:44`

The `TODO(seed-pack-vendoring)` docstring ends a sentence mid-word ("...without that consumer's checkout. Two" / newline / "What it defers:"), and its next claim ("every remaining consumer reference ... is a YAML comment or a `reason:` string") is narrower than the tree: `ci-spread-core/ontology/ci_lending.yaml:179,207,235,254,291` carry `source_schema: jaci.*` as data values, and `test_which_seed_pack_files_name_consumer_modules_is_pinned` (`tests/test_plato_packs_api.py:630`) enumerates those as "ontology `source_schema` values, metric and template bindings". The substance of the deferral (nothing resolves them; nothing keeps the copies in step) holds.

IMPORTANCE: low -- the docstring of one function; the pinning test is what actually guards the claim, and it is accurate.

## [NOTE] `plato/posture.py:176`

`relaxed_only` inherits `environment_tier()`'s fail-open: an unrecognised value for `JAPES_ENVIRONMENT`/`ENVIRONMENT`/`APP_ENV` (anything outside `_TIERS`, e.g. `uat`, `prod-eu`) reads as `local`, so with `strict_mode()` false the pack delete and the seed write are admitted on that replica against a docstring and a 403 text that both promise "local, dev-daily". Mitigated but not closed: `ENVIRONMENT_NAMES` validates the value only where `JAPESSettings` is constructed, and `_TIERS`' own comment already records that unknown names lose the fail-safe.

IMPORTANCE: low -- no shipped configuration I could find sets an unlisted tier (`.env.template:16` uses `dev`, `scripts/deploy.sh:31` defaults to `dev`, both aliased in `_TIERS`); reaching it needs a deployment to name its tier something this table does not know. A `TODO` on the gate is a complete response.

CHECKED:
- `TODO(seed-digest-drift)` (`plato/seed.py:180`) — holds: `pack_archive` writes through `zipfile.write`, which records each file's mtime, so `content_digest(archive)` is not content-only and a rebuilt image would report drift on identical content.
- `TODO(orphan-blob-gc)` (`plato/packs_api.py:194` → `store_db.py:317`) — holds: the key is content-addressed via `BlobStore.content_key`, and the store is tenant-scoped at construction, so the cross-tenant refcount genuinely cannot be asked there.
- `TODO(if-match-post)` (`plato/posture.py:96`) — the revised claim holds: `require_if_match` is imported only by `tests/test_plato_preconditions.py`, no router mounts it, so the new creating POST cannot 428.
- `TODO(azure-blob-container-singleton)` (`plato/wiring_default.py:670`) — holds, and the correction is right: `jazzx_sdk/fabric/fabric.py:198` builds a second `BlobStore` with its own `blob_container`, so this is not plato's only azure construction.
- `TODO(omit-info)` (`plato/info_api.py:82`) — holds: `plato/__main__.py:305` passes `("Guide", "config", "database", "Packs", "packs")`, which does not include `info`, and nothing else calls `dashboard_html` with `omit`.

PARKED: `test_no_page_references_an_external_host`'s neighbour at `tests/test_plato_info_api.py:622` (`TODO(protocol-relative)`).

`ruff check` clean on all changed Python; `pytest -k "plato or seed or posture or tenancy or nav"` plus the six changed test files: 474 passed, 6 skipped.

VERDICT: BLOCK

## slice4

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
All 123 tests in the diff pass locally.

**CHECKED:**
- `TODO(activation-vs-active-rules)` (`policy.py:754`) claims `executable_rules`/`activation_blocks` have no caller outside tests — grep across the repo confirms it; nothing to raise.
- `TODO(refused-rule-field-claim)` (`experts/policy/default.py:497`) claims `allowed` is already `False` so the skipped field-claim costs a misleading entry, not a wrong verdict — `allowed=not violations and not not_activated` makes that hold.
- `TODO(threshold-grid-provenance)` (`profiles.py`) claims the gate covers none of the shipped DSCR profile's numbers — `test_the_shipped_profile_claims_no_provenance_it_was_not_given` asserts exactly that; accurate.

---

**[DEFECT] `tests/test_plato_packs_api.py:598`** — `getattr(pack, attribute, None)` suppresses `AttributeError`, which is the exception `_resolve` raises when a manifest pointer's module imports but the symbol is missing, so the loop's comment ("raises here if it resolves a symbol this process cannot import") is not enforced for half the resolution failures.
Trigger, reproduced: a seed pack with `conductor: {pipeline: plato.seed.NoSuchSymbol}` → `pack.conductor` raises `AttributeError: module 'plato.seed' has no attribute 'NoSuchSymbol'`, while `getattr(pack, "conductor", None)` returns `None` and the test is green. The same default is already silently absorbing a dead entry in the list: `conductor_pipeline` is not an attribute of `Pack` (`hasattr(Pack, "conductor_pipeline")` is `False`), so one of the 14 names checks nothing.
IMPORTANCE: medium -- this test is the only guard that the two bundled packs under `plato/data/seed_packs/` resolve with just japes importable; `POST {prefix}/packs/initialize` (`plato/packs_api.py:170`) publishes them into any tenant in a relaxed posture, and `TODO(seed-pack-vendoring)` in `plato/seed.py:41` makes re-vendoring those manifests the expected workflow. No consumer outside this repo reads the seed tree.

**[DEFECT] `tests/test_plato_packs_api.py:859`** — `page.count("headers: headers(") == 3` counts fetches that *do* carry the header, not fetches, so the assertion message ("every fetch, and nothing but the fetches") holds only by coincidence of the current count.
Trigger: add a fourth `fetch(api(...))` to `packs_html` that omits `headers: headers({...})` — e.g. an archive download — and the count stays 3, the test stays green, and a deployed replica answers that call 400 for a missing `X-Tenant-Id`, which is the exact regression this test's docstring says it was written for ("the first version sent nothing"). `packs_html` currently has `fetch(` == 3 and `headers: headers(` == 3; comparing the two counts is the assertion that enforces the claim.
IMPORTANCE: low -- test-only today; all three shipped fetches carry the header, and the failure when it does bite is a visible 400 on the packs page, not silent data. Checked against the three `fetch(` call sites in `plato/info_api.py`'s `packs_html` only.

**[SYMMETRY] `tests/test_plato_packs_api.py:572`** — `test_a_bundled_seed_pack_is_loadable_in_a_japes_only_process` is parametrized on the literal `["ci-spread-core", "dscr-core"]` while its sibling nine lines up, `test_every_bundled_seed_pack_loads` (line 543), already iterates `default_seed_root().iterdir()`. A third bundled pack gets the sibling's check (`policy_registry`, `policy_profile()`) but never the conductor/experts/modes resolution this one performs — which is where the `jaci.*` bindings actually lived. Deriving the parametrize list from `default_seed_root()` is no more code than the literal.
IMPORTANCE: low -- two packs ship today, so no current gap; reachable only when a pack directory is added.

**[SYMMETRY] `tests/test_plato_packs_api.py:544`** — hardcodes `pack_dir / "pack_manifest.yaml"`, and the read sits outside the `try`, while `plato.seed.MANIFEST_NAMES` is `("pack_manifest.yaml", "manifest.yaml")` and `plato.seed._manifest_of` owns that resolution (and is already imported by `test_an_anonymous_manifest_does_not_hide_an_identified_sibling` in this same file). A bundled pack authored with only `manifest.yaml` — which `seed_packs` publishes without complaint — makes this test error with `FileNotFoundError` instead of reporting through its `broken` dict.
IMPORTANCE: low -- both shipped seed packs carry `pack_manifest.yaml`; verified by listing `plato/data/seed_packs/*/`.

**[NOTE] `tests/test_plato_packs_api.py:965`** — the backend sweep is `["Azure", "azure-blob", "blob", "s3"]`, all unrecognised; neither this test nor its sibling at line 986 pins the two recognised values. Nothing asserts that the default `local` produces no "not a recognised pack-archive backend" line, so widening `_pack_blob`'s inner `if backend != "local"` guard (`plato/wiring_default.py:640`) would put a false typo warning in every default deployment's boot log with both tests still green.
IMPORTANCE: low -- one confusing log line; `_pack_blob` has one call site, `build()` in `plato/wiring_default.py:440`.

**[NOTE] `tests/test_plato_packs_api.py:928`** — the `TODO(local-wiring-build-test)` deferral names `DB_PATH`/`CACHE`/`PACK_BLOBS` as the three module globals the `importlib.reload` freezes to `tmp_path`; `plato/wiring_local.py` defines only `DB_PATH` (158) and `CACHE` (159) at module level — `PLATO_LOCAL_PACK_BLOBS` is read inside `build()` at line 377, so the reload freezes nothing for it. The rest of the deferral holds: of the five test files mentioning `wiring_local`, only `test_plato_wiring.py` imports from it (`_find_root`, `_ALEMBIC_INI`), and neither reads the frozen paths.
IMPORTANCE: low -- the TODO overstates the leak rather than understating it; nothing reads either frozen global.

**[NOTE] `tests/test_policy_rule_activation.py:122`** — the effective-dating sweep uses 2099 and 2020, never the bound itself. `Rule.activation_block` uses `moment < start` and `moment > end`, so a rule is executable exactly at `effective_from` and exactly at `effective_to`; nothing pins that inclusivity, and `as_of=rule.effective_from` is the natural call for a backdated determination.
IMPORTANCE: low -- `activation_block`/`is_executable` have no caller outside tests (grep confirmed), so no shipped path can reach the boundary yet.

VERDICT: BLOCK
