# adversarial review -- japes

- when: 2026-09-24T05:13:03Z
- range: 6ff5aa3..HEAD (since the last review) (0f88673)
- diff: 17 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
The earlier finding is closed, and this diff adds no defects. One inaccurate docstring claim is left, and it's a low-importance note.

WAS: tests/test_backend_names.py :: silence-covers-defaults-only -- FIXED. The docstring now says that a non-default name leaving a set is not covered.

[NOTE] tests/test_backend_names.py:51 :: db-default-not-exercised -- The rewritten docstring says the test covers "All five defaults", but it builds `DbStore(backend="sqlite")`. The real default is `backend="common"` (`jazzx_sdk/fabric/db/store.py:170`), so the test never runs the store's actual default.
Trigger: remove `"common"` from `DB_BACKENDS` and every `DbStore()` built without arguments warns on each boot. This test still passes because it only passes `"sqlite"`. The other four calls do use their real defaults: blob `"local"` (`blob/store.py:38`), tracer `"noop"` (`run_tracer.py:175`), guidance `"rag"` (`config.py:191`) and conversation `"in_process"` (`config.py:209`).
IMPORTANCE: low -- test-only. The shipped code is correct today, because `check_backend` is only wrong if someone edits `DB_BACKENDS`. The fix is either `DbStore()` inside a sqlite-free guard, or a docstring that says four of the five, or a `TODO`.

VERDICT: PASS
