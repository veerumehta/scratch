# adversarial review -- japes

- when: 2026-09-11T17:15:33Z
- range: 3c9805d..HEAD (since the last review) (73afa22)
- diff: 320 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_mock_knowledge_hub_app.py` → 25 passed. Real-Hub behaviour re-read at `/Users/sangit/src/knowledge_hub/app/reasoning/api.py` (v1 POST: ontology validation in `create_entity_with_validation` runs *before* the insert whose `IntegrityError` becomes the 409, `service.py:239-245`; v1 PATCH `EntityUpdate.validate_name` rejects a blank name, `models.py:224-229`; `sanitize_model_fields` strips NUL only, not whitespace). Mock behaviour reproduced through `TestClient` (`create 201 / dup+invalid 409 / move-collection 200 / recreate-in-new-collection 201 / recreate-in-old-collection 409 / padded-name 409`).

WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: patch-route-collapses-codes -- FIXED; the PATCH route now maps `EntityValidationError` to 422 ahead of the general `ValueError`, and the new update test asserts it.
WAS: jazzx_sdk/clients/mocks.py :: mock-raises-where-client-returns-none -- FIXED; `EntityNameConflict` and `_reject_duplicate_name` are gone, the client creates duplicates again, and the new test pins that against `EntityStore.create`'s "None on failure".
WAS: jazzx_sdk/clients/mocks.py :: v2-docstring-dup-claim-stale -- FIXED; removing the client-level guard made the sentence true again (two `create_entity` calls with one name now return two ids).
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: module-doc-400-example-stale -- FIXED; the paragraph now names 422 for `EntityValidationError` and 409 for a repeat name.
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: missing-fields-still-400 -- FIXED as a NOTE; closed with `TODO(missing-fields-still-400)` on the guard plus the docstring divergence.
WAS: plato/wiring_default.py :: swap-docstring-sentence-duplicated -- FIXED.
WAS: jazzx_sdk/clients/mocks.py :: update-skips-name-uniqueness -- FIXED at the app layer; the PATCH route now checks a rename against `_created_names` and answers 409.

CHECKED: `TODO(mock-conflict-ignores-preseeded)` holds -- a `data_dir` seeded before startup yields a *missing* 409 (permissive, the safe direction), and the delegate offers only `read_entities` (OData + skip/limit) for read-back, so closing it is more machinery than the gap.
CHECKED: `TODO(missing-fields-still-400)` holds -- `EntityCreate.validate_name` makes it a `RequestValidationError`, so the real route answers 422 with `{"detail": [...]}`; matching the code alone would leave the envelope wrong.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested
ACCEPTED: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer

New findings against this diff:

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:239 :: conflict-checked-before-validation -- the 409 check sits above the `try`, so a body that is both a repeat name and schema-invalid answers 409 where the service answers 422, inverting the precedence the route's own comment claims to preserve.
Reproduced: POST `{"name": "e-1", "collection_id": "col-1", "ontology_id": <registered>, "entity_type": "flag", "json_value": {"severity": "high"}}` → 201, then the same body with `"json_value": {"severity": "catastrophic"}` → `409 "entity name 'e-1' already exists in collection 'col-1'"`. On the real v1 create, `create_entity_with_validation` runs `EntityValidator.validate_entity_json` (`service.py:241`) before the insert whose `IntegrityError` becomes the 409 (`api.py:545-551`), so that body is a 422. The comment at :294 above the mapping says "a caller branching on 422 against the real Hub has to see 422 here, or the mock passes the case it exists to catch" -- this is that case. Moving the conflict check to after the `create_entity` call (where `_created_names.add` already is) fixes it.
IMPORTANCE: medium -- the only entity-create path this app exposes; in-repo reachers are tests/test_mock_knowledge_hub_app.py, but the factory is exported from `jazzx_sdk.server` for out-of-repo packs to point a container at (the module docstring names jazzx-assistant's hand-rolled mock as the shape being replaced). Fails loudly as a wrong code, no silent data.

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:298 :: collection-move-leaves-stale-key -- `_created_names` is keyed on `(collection_id, name)` but the maintenance runs only when `"name" in fields`, so a PATCH that moves the entity to another collection leaves the old key held and never records the new one, and both directions of the resulting answer are wrong.
Reproduced: create `name="e-2"` in `col-1` → 201; `PATCH {"collection_id": "col-2"}` → 200; POST `name="e-2"`, `collection_id="col-2"` → **201** (the service 409s, the entity is there); POST `name="e-2"`, `collection_id="col-1"` → **409** (the service 201s, nothing holds that name). `collection_id` is patchable on both sides: `MockKnowledgeHubClient.update_entity` takes it (`mocks.py:1565`) and real `EntityUpdate` is `@optional_fields` over `EntityBase` (`models.py:219`). The rename fix covered one dimension of a two-dimensional key; `renaming` should be `{"name", "collection_id"} & fields.keys()`.
IMPORTANCE: medium -- same route and same set as the rename fix this diff adds; the missed-409 direction is the quiet one (the mock accepts a duplicate the service refuses, which is the case this app exists to catch). Mock/dev configurations only, no real-service path touched.

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:294 :: patch-accepts-blank-name -- the route treats `{"name": ""}` as a legitimate rename and answers 200, storing an entity with an empty name; the real v1 PATCH rejects it, so the comment's premise ("a blank one still vacates the name the entity held") describes a state the service never reaches.
Reproduced by the diff's own `test_blanking_a_name_frees_it_too`: PATCH `{"name": ""}` → 200, and `MockKnowledgeHubClient.update_entity` writes `entity["name"] = ""` (`mocks.py:1599`, guarded on `is not None`). Against the service, `EntityUpdate.validate_name` raises "Entity name cannot be empty" for `""` (`models.py:224-229`) → 422 before anything is written. The create route in this same file already refuses a blank name (:225-228), so the two routes disagree about the same field. Answering 422 for a present-but-blank `name` would make the free-the-old-name branch unreachable rather than needing its own test.
IMPORTANCE: medium -- a pack asserting "blank the name" goes green here and 422s against the live Hub; reachable from any caller of the PATCH route, and the new test locks the behaviour in. Mock-only, and the stored blank name is visible in the response.

[NOTE] jazzx_sdk/server/mock_knowledge_hub_app.py:236 :: padded-name-false-conflict -- `conflict_key` strips the name while the entity is created with the raw one, so a distinct padded name collides: after `name="e-1"` → 201, `name=" e-1 "` → 409 (reproduced), where the service's unique constraint compares the exact string and `sanitize_model_fields` removes NUL bytes only (`app/reasoning/utils/sanitize.py:41-56`).
IMPORTANCE: low -- no japes caller sends a padded entity name (grepped `create_entity(` call sites: `fabric/entities/store.py:58`, `fabric/canonical/store/core.py:109`, both passing ids); the strip is internally consistent with the `was`/`target` keys, so nothing else drifts.

[NOTE] tests/test_mock_knowledge_hub_app.py:296 :: test-name-states-the-opposite -- `test_the_client_returns_none_on_a_duplicate_rather_than_raising` asserts `first and second` and two distinct ids, i.e. the client does *not* return `None`; the `None` belongs to the real `KnowledgeHubClient`, which the test does not touch, so the name reads as a contract the body disproves.
IMPORTANCE: low -- test-only; the docstring explains the real intent, and no shipped-code defect follows from the name.

VERDICT: BLOCK
