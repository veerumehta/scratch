# adversarial review -- japes

- when: 2026-09-09T02:35:00Z
- range: 93e2a8c...HEAD (b3dc645)
- diff: 223 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
No `[DEFECT]` found. Verified `tests/test_plato_info_api.py:93`'s stub is the production shape (`current_revision` at `plato/schema_version.py:81` calls `db.engine()`, and `DatabaseHandle.__getattr__` at `plato/wiring_default.py:135` delegates it), and that it fails against the old string (`"...: TimeoutError: "`). `pytest tests/test_plato_info_api.py` → 96 passed, 1 skipped.

**[SYMMETRY]** `plato/settings_store.py:95` (also `:149`, `plato/wiring_default.py:528`, `:549`) — four sites hand-roll `type(exc).__name__, str(exc)[:120]`, which is exactly `reason(exc, limit=120)`; the `limit` parameter added in this diff has a single user (`plato/database_api.py:87`), so these are the family it was shaped for and they still render `(TimeoutError: )` for an empty-message exception. Trigger: an `asyncio.TimeoutError`/`ReadTimeout` from the durable settings read, which is the failure those handlers exist to log.
IMPORTANCE: low -- log lines only, no response field; I enumerated all four sites plus the seven converted ones by grepping `type(exc).__name__` across `plato/` and `jazzx_sdk/`. Nothing outside this repo reads these strings.

**[SYMMETRY]** `plato/reasons.py:11` vs `jazzx_sdk/server/console_api.py:130` — the identical diagnostic formatter (`{"error": f"{type(exc).__name__}: {exc}"}`, a response field, not a log) lives in the SDK layer and cannot import `plato.reasons`: nothing in `jazzx_sdk/` imports `plato` (grepped, zero hits), so the dependency only runs the other way. Placing the module under `jazzx_sdk` and importing it from the five plato modules is the same amount of code as this diff.
IMPORTANCE: low -- I checked the plato side exhaustively; I did not enumerate external consumers of `create_console_router`, so the SDK-side reach is unestablished.

**[NOTE]** `plato/reasons.py:18-23` — `limit=0` truncates the message to `""` and returns `"TimeoutError: "`, the trailing-colon output the docstring says the function exists to prevent; the guard runs before the truncation instead of after. No shipped caller passes 0 (only `plato/database_api.py:87`, with 200), so a `TODO` or nothing is a complete response.
IMPORTANCE: low -- one call site passes a literal; the function is not exported past `plato/`.

**[NOTE]** `tests/test_plato_info_api.py:83` covers only the empty-`str(exc)` path; the `limit` branch that `plato/database_api.py:87` depends on has no test, so a regression in truncation would surface as an oversized `/info` `reason` field rather than a failure.
IMPORTANCE: low -- test-only; I cannot name shipped-code behaviour it would have caught.

VERDICT: PASS
