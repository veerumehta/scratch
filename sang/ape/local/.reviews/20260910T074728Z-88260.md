# adversarial review -- japes

- when: 2026-09-10T07:47:28Z
- range: origin/v2.5.1...HEAD (919e277)
- diff: 10095 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(activation-vs-active-rules)` (policy.py:758) — holds. `executable_rules`/`activation_blocks`/`not_in_force` have no non-test callers; the only `active_rules()` readers are `default.py:437` and `graph/check.py:349`, neither of which touch the new accessors.
- `TODO(orphan-blob-gc)` (store_db.py:277) — holds. Blob keys are content digests shared across tenants and the store is tenant-scoped at construction, so the count it would need is unaskable here.
- `TODO(threshold-grid-provenance)` (profiles.py:214) — holds. `tables` is absent from `get`/`get_threshold`'s bucket tuple and `get_table` is untouched, so a grid cell is served exactly as before; no new inconsistency.

## Findings

**[DEFECT]** `jazzx_sdk/pack/store.py:163` — the docstring's guarantee ("a deleted version is auditably absent rather than silently different … `content_digest` only identifies bytes for as long as a version cannot be edited") is not enforced: `DbPackVersionStore.delete` is a hard `sa_delete` of the row with no archive/tombstone record, so delete-then-republish edits a version. Trigger, verified against the real store: publish `dscr-core@0.1.0`, `delete("dscr-core","0.1.0")`, publish `0.1.0` again with different archive bytes — the second publish succeeds and the digest changes (`460a2f…` → `811ac0…`), defeating `test_republishing_a_version_is_refused` (tests/test_pack_store.py:76). Over HTTP the sequence is `DELETE {prefix}/packs/{id}/{version}` then `POST {prefix}/packs/initialize`, and since bundled seed-pack content changes without a `pack_version` bump (the new `ci-spread-core` ships `0.2.0-draft`), a re-initialize after an image upgrade substitutes content under the same version. The only trace is `logger.warning` in `packs_api.py:196`.
IMPORTANCE: medium -- reachable only in relaxed-posture deployments (`_refuse_unless_relaxed` gates both endpoints), but silent once reached: the row carries no evidence the version ever held other bytes. Enumerated: the single `PackVersionStore` implementation (`DbPackVersionStore`), both HTTP endpoints in `plato/packs_api.py`, and `plato/wiring_default.py:457` as the only wiring. `delete` has no test against the real store — the only coverage is the two fakes at tests/test_plato_packs_api.py:249,268 — so I exercised it directly.

**[SYMMETRY]** `jazzx_sdk/finance/validation.py:601-605` — this diff makes one findings function `raise` `ThresholdNotActivated` while its sibling in the same diff reports it. `policy_lint.find_profile_literal_drift` (policy_lint.py:45-58) explicitly appends a finding for an unapproved/unreadable threshold "because a refusal is reported rather than raised out of a findings function"; `detect_cross_period_addback_inconsistency` re-raises instead, so a profile that authors `cross_period_addback_severity` as a governed threshold with `status != active` loses every finding the call would have produced — over a knob that only picks a severity label. The consistent version is no larger: append a `ValidationFinding` as policy_lint does, and keep the default severity.
IMPORTANCE: medium -- no in-repo caller (`validate_package:873` calls `detect_cross_period_addbacks`, not this function), so the reachable callers are external (jaci's add-back library path); a japes consumer authoring that profile key sees an exception in place of a findings list.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:414,434` — the v0.2.1 header note says the undeclared-formula-reference class was swept (`taxes`→`tax_expense`, adding `scheduled_principal`), but two `concept_graph` entries still carry it: `UCAcashFlow`'s formula reads `net_income` and `working_capital_change`, neither declared on `FinancialPeriod` nor listed in its `inputs`; `ExcessAvailability.inputs` names `Facility.outstanding_balance`, and `Facility` declares no such field (it is on `Lien:79` and `BorrowingBaseResult:194`), while the formula's `reserves` is in neither list.
IMPORTANCE: low -- the file states `concept_graph` formulas are documentation only and runtime computation lives in the Reasoner/`compute_ci_metrics()`, so no evaluation path reads them; cost is a wrong schema doc for an SME.

**[NOTE]** `jazzx_sdk/finance/validation.py:826` — `_warning_refs` returns `{}` on a non-matching warning, so `detail` omits `policy_id`/`rule_id` entirely, whereas the violations loop above always sets both keys. The docstring's own story is about a consumer keying on `rule_id`; such a consumer gets `KeyError` on those warnings rather than the `None` the story implies. `detail = {"source": ..., "policy_id": None, "rule_id": None, **refs}` matches the sibling loop.
IMPORTANCE: low -- the only in-repo `warnings=` producer is `default.py:503`, whose format always matches the regex, so this needs a non-japes policy expert to trigger.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/diagnose_map.yaml:67,75` — the file states "rules evaluated top-to-bottom; first match wins", and `credit_memo_all` (line 67) has `conditions: {}`, so it matches everything ahead of the `fallback` rule at line 75, which then has no reachable input. It only works if the reader special-cases `always_include: true` and continues scanning.
IMPORTANCE: unknown -- I could not establish this: the reader is `CIPlaybookExpert.diagnose()` in jaci, not in this repo, so whether `always_include` short-circuits the match loop is not checkable here.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(seed-pack-consumer-pointers)` (tests/test_plato_packs_api.py:404) — claim holds: `jaci.*` pointers sit in exactly `{ci-spread-core, cl_of_core, cl_sp_core, clinical-intake-core}`, the strict xfail flips loudly, and the enumeration test pins the set. Not re-raised as a finding — but see Defect 1, where the deferral's proposed *remedy* does not hold.
- `TODO(omit-info)` (plato/info_api.py:80) — holds: `_info_only_app`'s `degraded_nav = ("Guide", "config", "database", "Packs", "packs")` (plato/__main__.py:304) contains no `"info"`, so the amended note and the nav docstring's "the guide, config, database and both pack entries" both match the code.

## Findings

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:2` — the header says these policies are "loaded via jazzx_sdk load_policies", but the pack manifest never declares this file, and the one overlay it does declare (`policies/overlays/rb_ci.yaml`) does not exist in the vendored tree. Trigger: `PackManifestLoader('.../ci-spread-core/pack_manifest.yaml').policy_files()` returns `['core.yaml']` and logs one `WARNING: Policy file not found`, so FCCR, ABL, LIEN, `RB_CI_OVERLAY` and the add-back ceiling — 5 of the pack's 6 authored policies — are absent from the declarative path. Today that is masked by `policies.registry` raising `ModuleNotFoundError`; the TODO's stated fix ("drop the pointers in favour of the declarative files they already carry") therefore converts a loud failure into a pack that silently enforces leverage only. Same shape: `cl_of_core`/`cl_sp_core` declare `experts.playbook.diagnose_map: diagnose_map.yaml`, absent from both directories.
IMPORTANCE: high -- checked `manifest_loader._declared_files` (warn-and-skip, no raise), `Pack.policy_registry`, and ran the loader over all five bundled packs; reachable from `plato.packs_api` initialize → `store.materialize` → `Pack.from_manifest`, and from jaci once the dotted pointer goes.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125` — two `required_documents` rows in `purpose_specific_documents` are PDF line-wrap fragments (`- schedule`, `- available)` at :127), so the equipment-purchase and acquisition requirements are truncated into meaningless entries while the section's `description` claims "Extracted verbatim from the source checklist." Trigger: any completeness pass over `purpose_specific_documents` asks for a document called "schedule" and one called "available)" and never asks for the purchase agreement / target financials.
IMPORTANCE: medium -- `checklist:` is declared in ci-spread-core's manifest and deliberately excluded from `policy_files()`, so no japes code reads it; the consumer is jaci's `assess_package()`, and the rows are also rendered wherever the pack is browsed. Fails visibly as a nonsense checklist item rather than silently.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/spread_template_rb.yaml:114` — the "Less Cash Taxes (Refund)" row of the fixed-charge-coverage section binds to `income_statement.income_tax_expense`, i.e. book tax expense including deferred, not cash taxes; the pack already carries the right key (`income_taxes_paid_net_of_refunds`, used by `spread_template.yaml`'s supplemental section). Trigger: any borrower with a nonzero `deferred_income_taxes` renders a wrong figure under a "Cash Taxes" label in the FCC block. This file leaves "Cash Interest" `unbound: true` for exactly this reason ("binding it would overstate confidence") two rows below, so the discipline exists and was not applied here.
IMPORTANCE: medium -- display-only (the "CIT FCC" row uses the computed `fixed_charge_coverage_ratio` metric, not these rows), rendered by jaci's RB reporter; a credit analyst reads the wrong number, no code sees it.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/workbook_layout.yaml:8` — the header claims every `key` "must resolve against one of those two, which is this file's own load-time acceptance check (see workbook_layout.py's validator)". There is no `workbook_layout.py`; `load_workbook_layout` (jazzx_sdk/finance/workbook.py:169) is `WorkbookLayout.model_validate` on shape only, with no access to the pack's chart of accounts or metric catalog. Trigger: change any `key` to a typo (`net_salez`) — the layout loads clean, and `_write_statement_sheet` (workbook.py:397) hits `item is None` and `continue`s, emitting a labeled row of empty cells in the governed workbook with no exception entry. All keys in both shipped layouts resolve today, so the defect is the claim licensing the next edit to this file — which is a config file authored by hand by design.
IMPORTANCE: medium -- checked both layouts against `chart_of_accounts.yaml` + `metrics.yaml` (0 unresolved), `load_workbook_layout`, and the statement/metric writers; consumers are `GovernedWorkbook` in this repo and jaci's CL reporter.

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:39` — `dscr_core/pack_manifest.yaml:29` already uses `policies: dir: policies`, whose whole stated point is that a growing corpus should not be enumerated in a second place; that one-line shape here would pick up `conventions.yaml` automatically and skip the checklist by shape, fixing Defect 1 with less YAML than the current `core:`/`overlays:`/`checklist:` block.
IMPORTANCE: medium -- same call sites as Defect 1.

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/workbook_layout.yaml:14` — "the reporter's own convention requires the Inputs sheet to be declared first" is a rule stated in pack data that the library cannot apply: `GovernedWorkbook` (jazzx_sdk/finance/workbook.py:675) does `inputs_by_statement.get(...)` and, if a `live_formula: true` sheet precedes its Inputs sheet, silently writes literals instead of formulas. The ordering belongs in `WorkbookLayout` (a model validator, where `live_formula` is already declared) rather than in a comment in each layout — `workbook_layout_rb.yaml` is the second layout already relying on the same unenforced convention by omitting the split.
IMPORTANCE: medium -- affects any pack authoring a layout; failure mode is a non-live workbook that looks correct.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:1` — this file ships inside the wheel and is zipped into every published `dscr-core` archive by `plato/seed.pack_archive`, carrying jaci commit hashes (`68cfcae`, `272f1e3`, `44f9b55`), jaci-relative test instructions ("`cd jaci && pytest`"), gitignored plan filenames, and client-internal source paths. The plan-filename leak is broader: 11 seed-pack files name `plan_*.md`.
IMPORTANCE: low -- no behaviour; a published pack archive is what a customer can materialize.

**[NOTE]** `plato/data/seed_packs/dscr_core/pack_manifest.yaml:30` — "Non-policy assets in the folder are skipped by name" describes the wrong mechanism: `_declared_files` filters a `dir:` scan by `is_policy_document(...)`, i.e. by document shape, not filename. A pack author trusting the comment could name a file `notes.yaml` and still have a malformed policy skipped silently, or vice versa.
IMPORTANCE: low -- comment only; the shape filter already logs at debug.

**[NOTE]** `plato/data/seed_packs/clinical-intake-core/policies/core.yaml:108` — `"suicid"` is a bare stem with no negation handling, so "no suicidal thoughts" fires CI-ESC-002 and halts the intake. `mode_tuning/governor.md` explicitly accepts false escalations as cheap, so this is deliberate; worth recording only because the keyword list is the demo's whole matcher.
IMPORTANCE: low -- keyword matching lives in jaci's guardrails; the cost is one spurious nurse handoff in a synthetic demo.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED:** `plato/posture.py:96` TODO(if-match-post) — the claim holds. `require_if_match` is referenced only by `tests/test_plato_preconditions.py` and no router or `Depends` mounts it, so `POST /packs/initialize` cannot 428 today. Not raised.

**PARKED:** `test_every_bundled_seed_pack_loads` (TODO(seed-pack-consumer-pointers)).

---

**[DEFECT] plato/wiring_local.py:371** — the diff threads `pack_store_for` through `PlatoWiring`, `build_app` and `wiring_default.build()`, but the repo's other `PlatoWiring` construction never got it, so it defaults to `None`.
Trigger: `PLATO_WIRING=plato.wiring_local:build JAPES_RUN_MODE=server python -m plato`, then open `/api/v1/packs/ui` — the page renders "Pack store not readable: no pack store is wired", and both `POST /packs/initialize` and the delete return 503. This is the `local` posture, i.e. one of the exactly two postures `_refuse_unless_relaxed` admits the writes in. `wiring_local` already has a migrated sqlite `db` (the alembic tree includes `0004_pack_version_retired_at`), so the table is there; only the factory is missing. The diff's own `test_the_shipped_wiring_actually_reaches_a_pack_store` was written for precisely this failure and covers `wiring_default` only.
IMPORTANCE: medium — I enumerated all `PlatoWiring(...)` sites (`plato/wiring_default.py:495`, `plato/wiring_local.py:371`, plus three test constructions); nothing outside this repo builds one. It fails visibly rather than silently, but it disables the whole feature in its primary posture.

**[DEFECT] plato/seed.py:129** (`if await store.get(pack_id, version) is not None`) — the idempotency pre-check reads through `get`, which filters `retired_at IS NULL` (`jazzx_sdk/pack/store_db.py:264`), while `publish` checks `_row_including_retired` and raises `ValueError` on a tombstone (`store_db.py:170-178`).
Trigger: on the packs page, delete a seeded version (say `dscr-core 0.1.0`), then click "Initialize from bundled packs". `get` returns `None` for the retired row, `publish` raises `pack ... is already published and retired`, `seed_packs` catches it into `failed`, and the page reports "N failed" with reason `ValueError`. The correct answer for a deliberately retired version is a `skipped` reason, not a failure. This is the same shape as the padded-key bug the comment three lines below documents: the pre-check and `publish` disagreeing about the key's existence. Established by reading the two queries; my repro script hung on import and was killed, so this is not runtime-verified.
IMPORTANCE: medium — reachable from the shipped page in `local`/`dev-daily` only, fails loudly, and no data is lost (the tombstone is intended); the cost is an operator reading a permanent "failed" against a version they retired on purpose.

---

**[SYMMETRY] plato/wiring_default.py:588-596** — `_pack_blob()` falls back to `backend="local", local_dir=".plato-pack-blobs"` in every posture and records nothing, while its sibling `_db()` at line 161-169 in the same file already owns the rule for exactly this: a deployed posture that lands on non-durable storage gets a `logger.warning` naming the posture and saying the data dies with the replica. A published archive on a deployed replica has the same property — the `pack_version` rows go to Postgres and survive a restart, the bytes under a cwd-relative directory do not, after which `has_archive: true` on the page is an affirmative claim about bytes that are gone. The consistent version is the warning `_db` already writes, in the `backend == "local" and posture` branch.
IMPORTANCE: medium — I could not establish whether the dev-daily container mounts a volume (japes ships no Dockerfile; the image build lives outside this repo), so the durability outcome is unknown; the asymmetry with `_db` in the same function family is not.

---

**[NOTE] plato/packs_api.py:55-57** — the comment says the `record.*` reads are inside the guard "with everything else it reads", but the `packs.append({...})` block reading `record.domain`, `record.segment` and `record.version` sits outside both `try`s, so `pack_inventory`'s "Never raises" is still narrower than the sentence for a duck-typed `store` whose `list_packs` returns records lacking those attributes.
IMPORTANCE: low — every in-repo caller passes `DbPackVersionStore`, whose `PackVersionRecord` is frozen with those fields defaulted; the trigger is a third-party store object, which nothing constructs today.

**[NOTE] tests/test_plato_packs_api.py:~470** — the TODO's "only dscr_core is clean" is false at the level the enumeration guard claims to pin. `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md` names jaci commits, `jaci.settings.parse_model_tier`, and `cd jaci && pytest tests/scenarios/dscr/` (it is also a status document shipped inside a package directory). And `test_every_bundled_manifest_that_points_at_consumer_code_is_enumerated` greps `pack_manifest.yaml` only, while eight further tracked files carry `jaci.*` — `ci-spread-core/ontology/ci_lending.yaml` (five `source_schema: jaci.*`), `breakouts.yaml`, `metrics.yaml`, `reclassifications.yaml`, `spread_template_rb.yaml`, `workbook_layout.yaml`, `policies/core.yaml`, `clinical-intake-core/agent/profile.yaml`. A fix that strips the manifests makes both tests agree while the consumer name still ships in the wheel.
IMPORTANCE: low — the seed pack data is not in this diff and the load failures are parked; the cost is that the guard written to keep the record honest will pass over the remaining instances.

**[NOTE] plato/static/packs.html:~/headers()** — `localStorage.setItem` runs only when the field is non-empty, so clearing the tenant field and reloading restores the previous value and every fetch silently re-sends the old `X-Tenant-Id`.
IMPORTANCE: low — one confusing page load for an operator switching tenants; the field is visible and shows the restored value.

VERDICT: BLOCK
