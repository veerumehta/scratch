# adversarial review -- japes

- when: 2026-09-09T15:56:48Z
- range: 5843320..HEAD (since the last review) (e69721c)
- diff: 172 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` get orders on `published_at.desc()` -- **STILL OPEN**; line 250 unchanged, `TODO(publish-tick-tie)`'s "inside one microsecond" blast-radius claim still does not cover an ordinary patch on an older release line.
- WAS: `store_db.py:312` marker short-circuit -- **STILL OPEN**; `:309-312` still returns the marker file's text as the pack root with no containment check and no digest re-verify.
- WAS: `store_db.py:181` publish never opens `archive` -- **STILL OPEN**; `publish` still commits arbitrary bytes, and `_unpack` now raises `BadZipFile` (or the new `ValueError` naming both manifest names) rather than the two documented failures.
- WAS: `pack/__init__.py:29` `__all__` -- **STILL OPEN**; `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent from the list.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` -- **STILL OPEN**; `:320` and `:395` still call `reason` without `limit`.
- WAS: `store.py:54` "sha256 over the archived asset bytes" -- **STILL OPEN**; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race -- **STILL OPEN**; no unique-violation handling, blob still written first.
- WAS: `store_db.py:140` `visibility` unvalidated -- **STILL OPEN**.

## 2. New findings

**[DEFECT] `plato/packs/loader.py:214`** — `published_registries` feeds *every* record `list_packs` returns to `tenant_registries` → `load_pack`, which requires `manifest.yaml`, so a single domain pack published to the tenant's store aborts the whole composition; this diff's own `_MANIFEST_NAMES` comment states the store deliberately carries both shapes, and nothing here filters on which. Verified against the tree: `publish(MANIFEST, archive=_archive())` (the module's own domain-pack fixture) then `published_registries(store)` → `PackLoadError: the pack has no manifest.yaml`, and no argument on the function can exclude that row (`domain`/`segment` do not distinguish shape).
IMPORTANCE: medium -- grepped `published_registries`: the only callers are the two new tests, so nothing shipped composes this way yet; it is the entry point the plan's §10 step-2 criterion is written against, it fails loudly, and any tenant store holding a domain pack (which is the shape every other test in the file publishes) cannot compose at all.

**[DEFECT] `plato/packs/loader.py:214`** — the same line materializes unconditionally, so a manifest-only publish -- documented legal at `store.py:57` ("`None` for a manifest-only pack, which is legal") -- makes the whole call raise `PackNotFound`, which the sibling test at `tests/test_pack_store.py:358` asserts is *not* how an absence is reported. Verified: `publish(MANIFEST)` with no archive, then `published_registries(store)` → `PackNotFound: pack dscr-core version 0.1.0 was published without an archive`.
IMPORTANCE: medium -- same caller set (two new tests only); reachable through the public `publish` in its documented manifest-only mode, and the failure takes down every other pack in the tenant with it rather than skipping the one row.

**[DEFECT] `tests/test_pack_store.py:353`** — the freeze assertion is vacuous: `SkillRegistry` has no `values()` (nor `__iter__`) on `NamedRegistry`, so `next(iter(skills.values()))` raises `AttributeError`, which `pytest.raises(Exception)` swallows and `register` is never called. Verified: `SkillRegistry().values()` → `AttributeError: 'SkillRegistry' object has no attribute 'values'`; the file's 14 tests pass with the freeze path untouched. The shipped defect it would fail to catch is `published_registries` passing `freeze` through wrongly (e.g. defaulting or forwarding `False`), which would leave a serving registry writable.
IMPORTANCE: low -- test-only; `freeze=True` is correct in the tree today, so the assertion is dead weight rather than a live wrong answer. `skills.get("lookup_policy")` plus `pytest.raises(RegistryFrozenError)` is the version that catches it.

**[NOTE] `jazzx_sdk/pack/store_db.py:364`** — `_unpack`'s docstring still says it "return[s] the directory holding ``pack_manifest.yaml``" and "the manifest may sit at the archive root or one level down", after the body was changed to accept `manifest.yaml` too.
IMPORTANCE: low -- one private caller (`materialize`, `:339`).

**[NOTE] `plato/packs/loader.py:196`** — "rather than teaching the loader to read a store … A mapping-based second implementation of what a pack means is the thing worth not having twice" describes an alternative that is not the one on offer: `MappingPackSource` (`plato/packs/sources.py:104`) already exists, `load_pack` already accepts it, and `sources.py:107` calls it "the seam Phase 2 lands on", so that route reuses the same `load_pack` rules rather than reimplementing them.
IMPORTANCE: low -- prose only; the materialize route also gets the digest-keyed cache, which is a real reason the docstring does not give.

**[NOTE] `tests/test_pack_store.py:326`** — `{**manifest, "pack_id": manifest["pack_id"], ...}` re-sets a key the spread already carries; only `pack_version` is added.
IMPORTANCE: low -- test-local.

VERDICT: BLOCK
