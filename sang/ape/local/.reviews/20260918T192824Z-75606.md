# adversarial review -- japes

- when: 2026-09-18T19:28:24Z
- range: HEAD~1...HEAD (da20009)
- diff: 29 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[DEFECT] pyproject.toml:77 :: soupsieve-floor-not-in-pyproject -- the commit claims `soupsieve >= 2.9.0` but only `poetry.lock` moved; soupsieve has no constraint in `pyproject.toml`, and its only dependent, `beautifulsoup4`, requires `soupsieve = ">=1.6.1"`, so the vulnerable 2.8.4 is still an allowed resolution.

Trigger: any install that does not consume this lock file resolves soupsieve freshly against `>=1.6.1`. That covers `pip install japes` / `pip install -e /Users/sangit/src/japes` (how jaci's `dev` venv gets japes, per CLAUDE.md "Related repos"), any consumer whose own lock resolver re-resolves japes' deps, and a `poetry lock` refresh here after an unrelated dep edit. The other five transitive CVE floors in this file are all enforced the other way round: `pillow = ">=12.3.0"  # ...; transitive via matplotlib` (pyproject.toml:74), `gitpython = ">=3.1.57"  # ...; transitive` (:75), `pyasn1 = ">=0.6.4"` (:76), `setuptools = ">=83.0.0"` (:77), `pygments = ">=2.20.0"` (:148) — each a floor plus a one-line CVE comment, under the `# Security: Explicit version constraints for vulnerable transitive dependencies` header at :64. soupsieve is the one member of that family that got a lock bump instead of a floor.

IMPORTANCE: medium -- I enumerated every soupsieve dependent in `poetry.lock` (exactly one: `beautifulsoup4`, floor `>=1.6.1`) and every `# Fix CVE`-annotated transitive pin in `pyproject.toml` (pillow, gitpython, pyasn1, setuptools, pygments, cryptography — all floored in pyproject). Reaches anything outside this repo that installs japes from its metadata rather than this lock, which per CLAUDE.md includes jaci's editable install. It fails quietly rather than loudly: once this lands, Dependabot on `main` reads the default branch's graph — i.e. the updated lock — and goes silent, so the missing floor is no longer visible from the alerts page. The one-line fix is `soupsieve = ">=2.9.0"` alongside the other transitive pins.

VERDICT: BLOCK
