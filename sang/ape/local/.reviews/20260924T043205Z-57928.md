# adversarial review -- japes

- when: 2026-09-24T04:32:05Z
- range: 1714651..HEAD (since the last review) (d002db7)
- diff: 601 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_plato_database_rows.py :: literal-sql-restated -- STILL OPEN. Line 94 still compares each statement to a hand-typed f-string of the template.
WAS: plato/api/database.py :: redundant-second-cache -- STILL OPEN. `@lru_cache` is still on `_statements()` (line 143), and it sits on top of the cached `owned_tables()`.
WAS: scripts/run_tests.sh :: stale-test-path-example -- STILL OPEN. Line 13 and `scripts/README.md:99` still name `tests/test_plato_oidc.py`.
WAS: jazzx_sdk/net_safety.py :: stale-oidc-path-comment -- STILL OPEN. The comment at lines 267-270 still cites `plato/oidc.py`.
WAS: jazzx_sdk/oidc.py :: plato-scoped-prose-in-sdk -- FIXED. The tree is unchanged since last round, when it was already fixed.

CHECKED: jazzx_sdk/pack/seed.py :: seed-digest-drift. The TODO moved over verbatim. Its reasoning still holds: `pack_archive` is still built with `zipfile.write`, which records file mtimes, so the digest changes on every image rebuild.

[NOTE] jazzx_sdk/pack/seed.py:10 :: service-name-in-sdk-docstring -- The new SDK module's docstring names `plato.seed` and `PLATO_SEED_PACKS_DIR`. That is the same kind of Plato-scoped prose that was just removed from `jazzx_sdk/oidc.py`.
Trigger: none at runtime. The prose names one consumer's module and env var inside a library module that `jazzx_sdk.pack` now exports.
IMPORTANCE: low -- docstring only. No code reads it, and nothing outside this repo depends on its wording.

What I checked for regressions, with nothing found:
- **Old import sites:** `acting_user_id` is no longer importable from `settings_store_db`. Every caller now imports it from `jazzx_sdk.identity`:
  - `settings_store_db`
  - `pack/seed.py`
  - `plato/api/database.py`, `model_overlay.py` and `packs.py`
- **Monkeypatch targets:** nothing patches `plato.seed.*`, and no test asserts on the old `plato seed:` log prefix.
- **Import cycles:** `jazzx_sdk.identity` has no top-level `jazzx_sdk` imports, so `jazzx_sdk.pack.__init__` → `seed` → `identity` cannot cycle.
- **Call sites:** all `seed_packs` calls match their signatures. Plato's wrapper is keyword `root=`, and the SDK's `root` is positional.

VERDICT: PASS
