# adversarial review -- japes

- when: 2026-09-09T08:08:19Z
- range: 587bd36..HEAD (since the last review) (793b313)
- diff: 324 lines


## whole

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## 1. Previous round

- WAS: `fabric/docs/store.py:1552` `materialize()` writes `out / filename` — **FIXED** for the reported trigger: `../../evil.pdf` now raises before any write (verified, and `tests/test_fabric_docs.py::test_materialize_cannot_write_outside_the_output_directory` passes). The rule chosen there introduces a different reachable failure, reported below as its own finding.
- WAS: `fabric/blob/store.py:70` `PurePosixPath(".").parts` is `()` — **FIXED**. `relative_name(".")` and `relative_name("./")` both raise; `_blob_filename`'s docstring no longer makes the false claim about the family, and `DocStore._local_path` gets the same rule from the same function.
- WAS: `pipelines/document_ingest.py:180` re-derived bare-name predicate — **FIXED**. Both sites now call `path_safety.bare_name`.
- WAS: `tests/test_fabric_docs.py:181` `raises((ValueError, KnowledgeFabricError))` — **FIXED**. Now `raises(ValueError, match="document name must")`, which fails if the guard slides back inside the `try`.
- WAS: `document_ingest.py:181` `"..zip"` → `stem == "."` refused — **STILL OPEN**, unchanged and deliberate: `bare_name(".")` refuses exactly as the old inline predicate did, and the `_scratch_dir` docstring names the case.

## 2. New findings

**[DEFECT]** `jazzx_sdk/fabric/docs/store.py:1552` — `materialize` guards the remote name with `relative_name`, which permits nesting, but the site needs `bare_name`: `out` is a flat directory whose consumer globs it non-recursively (`agents/document/agent.py:605`, `pattern="*.pdf"`), so a nested name is written and then never seen. Measured with the production stub shape: a collection holding a document named `.japes/manifest.json` and `out` already containing `.japes` (which `process_dir` itself creates at `agent.py:599`, so every run after the first) overwrites the run manifest with the document's bytes — `materialize` returns it as an ordinary `MaterializedDoc`, `process_dir` never globs it, and the next run reads a manifest that is `%PDF...`. On the first run the same name instead raises `FileNotFoundError: .../out2/sub/x.pdf` out of `write_bytes` (no per-file `mkdir`), aborting the whole sync. `bare_name` refuses both, and it is what the two neighbouring guards in this diff (`_blob_filename`, `_scratch_dir`) use for this same shape.
IMPORTANCE: high -- silent data loss plus a clobbered manifest with no error to the caller. Reachers enumerated: `DocStore.sync_collection` → `DocumentAgent.process_dir(collection_id=...)` (`agent.py:597`) and the pipeline collection route (`pipelines/document_ingest.py:202,227`, a documented entry in its module docstring). The name is remote and user-supplied at KH upload. Nothing in the `/Users/sangit/src/jaci` checkout passes `collection_id` (its only `process_dir` call is `scenarios/portfolio_monitoring/document_intake.py:34`), so today this is SDK surface without an in-repo consumer.

**[NOTE]** `jazzx_sdk/path_safety.py:48` — pathlib strips single-dot components (`PurePosixPath("a/./b").parts == ("a","b")`), so `"." in set(parts)` can never match and the `"."` member of `_DOT_PARTS` does nothing in `relative_name`; the bare `"."`/`"./"` case is caught by `not parts`, as the comment two lines up correctly says. The consequence is only the message: `relative_name("a/./b")` returns it unchanged while the error text on the neighbouring branch claims a name "must not contain a '.' or '..' segment". The comment at line 32 asserts the "." has to be named for a parts-based check, which is true only of `bare_name`'s separate `name in _DOT_PARTS` test.
IMPORTANCE: low -- `a/./b` joins inside the base, so no wrong behaviour; both call sites of `relative_name` are in this diff.

**[NOTE]** `jazzx_sdk/path_safety.py:42` — the shape check is `PurePosixPath`, the join is `Path`, so on Windows `relative_name("..\\x")` passes (`PurePosixPath` sees one component) while `Path(base) / "..\\x"` escapes; same for `bare_name`, where `Path("a\\b").name` is `"a\\b"` on posix and `"b"` on Windows, so the two platforms disagree on which names are legal. Pre-existing in both inline guards, now centralized in a module documented as "the same rule" for every join site.
IMPORTANCE: low -- no Windows target established for this repo; on posix the behaviour is correct and `a\b` is a legal single filename.

VERDICT: BLOCK
