# adversarial review -- .

- when: 2026-09-25T04:26:19Z
- range: 050ca9c9..HEAD (since the last review) (1396072a)
- diff: 271 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/server/vocabulary_review.py :: review-router-not-tenant-scoped -- FIXED. `store_for` / `comments_for` / `drafts_for` now resolve the store per request, and `test_review_reads_the_requesting_tenants_queue` would fail against the old code. The one remaining unscoped injection is raised below as a new finding.
WAS: jazzx_sdk/fabric/graph/proposal_store_db.py :: table-rename-orphans-rows -- STILL OPEN. This diff does not touch it: there is still no migration from `vocabulary_proposal` to `proposal`, and `DbProposalStore(` is still constructed only in tests.
WAS: jazzx_sdk/server/policy_extract_api.py :: pack-for-hint-stale -- FIXED. The annotation is now `Callable[[str, str], Any]`, which matches the call at line 144.

[DEFECT] jazzx_sdk/server/vocabulary_review.py:425 :: vocabulary-for-not-tenant-scoped -- the diff gives three of the router's injected dependencies (store, drafts, comments) a per-request `_for` form, but `vocabulary_for` is still called with `pack_id` alone. `merge_accepted` therefore folds one tenant's accepted proposals into a vocabulary chosen without knowing the tenant.
Trigger: a host mounts `create_vocabulary_review_router(store_for=per_tenant, vocabulary_for=load_vocab)`, where each tenant has its own vocabulary for pack `dscr`. `POST /review/dscr/merge` with `x-tenant: globex` reads globex's accepted rows from globex's store. It then calls `load_vocab("dscr")`, which cannot tell globex from acme, so it merges into whichever vocabulary it returns. The merged proposals are then written back into globex's store. The extract side already passes the tenant along (`pack_for(pack_id, tenant_id)` at policy_extract_api.py:144), so this router is the one member of the pair that doesn't.
IMPORTANCE: medium -- I found no `create_vocabulary_review_router` / `create_proposal_review_router` call site in `jazzx_sdk/` or `plato/` outside the module's own docstring and the alias at line 508, so no shipped host can reach this yet. The first multi-tenant host that mounts `vocabulary_for` would get a merge against the wrong tenant's vocabulary with no error, because the result is returned rather than rejected.

[NOTE] jazzx_sdk/server/vocabulary_review.py:397 :: audit-event-tenant-unset -- now that one router serves several tenants, `_audit` still builds `ConfigAuditEvent` without `tenant_id`, even though the field exists (audit/events.py:36). All tenants' decisions land in the single `audit_store` with nothing to tell them apart, and proposal ids can collide across tenants.
IMPORTANCE: low -- this gap predates the diff: I found no `ConfigAuditEvent` construction in `jazzx_sdk/server/` that sets `tenant_id`, and no router here is wired with `audit_store`.

[NOTE] jazzx_sdk/server/vocabulary_review.py:434 :: store-resolved-per-iteration -- `merge_accepted` and `merge_accepted_rules` call `await _store(request)` again inside the record loops. The two merge routes (lines 427/434 and 461/478) are the only places that do this. A `store_for` that builds a new `DbProposalStore` or session on each call would pay that cost once per proposal. Resolving the store once per handler, as `add_comment` already does with `thread`, would avoid it.
IMPORTANCE: low -- it only costs speed, and no `store_for` host exists.

VERDICT: BLOCK
