# adversarial review -- japes

- when: 2026-09-09T07:34:10Z
- range: 18c8e7e..HEAD (since the last review) (9c27c5c)
- diff: 63 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**Step 1 — prior round**

WAS: `[NOTE] plato/wiring_default.py:278-282` comment asserting commit history — FIXED (both the code comment and the test docstring at `tests/test_plato_wiring_default.py:397-399` now state the rule without the "two of these call sites passed a raw exception until…" history).

WAS: "unrelated failing test `test_config_reports_what_the_replica_actually_resolved`" — not a finding, now documented by the new TODO; see CHECKED.

**CHECKED**

`TODO(settings-store-env-leak)` (`tests/test_plato_settings_store.py:26-33`) holds on both claims. I measured the leak by running that file inside a harness that diffs `os.environ`: it leaves `PLATO_DB_BACKEND=sqlite`, `PLATO_PACK_DIR=/p`, `JAPES_KNOWLEDGE_HUB_URL=https://kh.example`, and `PLATO_DB_BACKEND=sqlite` alone reproduces `assert None == 'sqlite'` on that test with no other file involved. The unreachability claim also holds: no `addopts` in `pyproject.toml:169-183`, only `pytest-asyncio`/`pytest-cov` installed (no randomizer, no xdist), and the full suite in default order is `5232 passed, 18 skipped`. "Predates the surrounding work" is right — `git diff origin/dev..HEAD` touches neither `test_plato_info_api.py`, `settings_store.py`, nor `config_api.py`.

**Step 2 — this diff**

No defects. `ruff` clean on all three files.

[NOTE] `tests/test_plato_settings_store.py:26` — the TODO names `apply_to_environment` as the sole writer, but `tests/test_plato_settings_store.py:123` sets `os.environ["PLATO_PACK_DIR"] = "/from-env"` directly in the test body, so a fix confined to `apply_to_environment` (or to `store.write`) still leaks that key; the accurate statement is that this file writes `os.environ` outside `monkeypatch`, in both places.
IMPORTANCE: low -- test-only, and it only misdirects whoever picks the TODO up; the leak itself is unreachable in the suite's order as verified above.

[NOTE] `plato/wiring_default.py:279-280` — "A caller that already went through `reason` passes through unchanged" is narrower than stated: `reason(exc, limit=None)` is the default (`jazzx_sdk/failures.py:253`), so a driver message over 500 chars is truncated here, which is what the `BOOT_NOTE_MAX_CHARS` comment 25 lines above says will happen ("a long prefix eats into the cause"). Only the redaction is unchanged for such a caller.
IMPORTANCE: low -- comment text; the behaviour is correct and is the behaviour `test_a_boot_note_is_bounded` asserts.

VERDICT: PASS
