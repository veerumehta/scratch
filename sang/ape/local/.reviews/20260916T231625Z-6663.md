# adversarial review -- japes

- when: 2026-09-16T23:16:25Z
- range: 1e3123e..HEAD (since the last review) (a2045d5)
- diff: 2169 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_plato_packs_api.py :: posture-fixture-misses-three-tier-vars -- FIXED. `_POSTURE_KEYS = (*_POSTURE_ENV_VARS, "JAPES_STRICTNESS")` in both `test_plato_packs_api.py:36` and `test_plato_info_api.py:30`, and all four hand-written `delenv` sites now loop over it; `ENVIRONMENT=production .venv/bin/python -m pytest tests/test_plato_packs_api.py -k settable_through_config` passes.
WAS: tests/test_common_submodule_deps.py :: lock-presence-is-not-image-install -- FIXED. `_locked_distributions` now parses both TOML files, drops optional roots outside `_IMAGE_EXTRAS`, and evaluates each locked package's `markers` with `packaging.markers.Marker`; `redis` is excluded and its case skips at `:375` rather than passing. A new gap opened on the sibling assertion -- below.
WAS: tests/test_common_submodule_deps.py :: namespace-provider-arbitrary -- FIXED. The `rglob` walk is gone and `_providing_distribution` returns `None` for a namespace package; `opentelemetry` now skips instead of asserting a walk-order distribution.
WAS: tests/test_plato_packs_api.py :: config-patch-test-reads-ambient-tier -- STILL OPEN in its second half: the ambient-tier part is closed by `_POSTURE_KEYS`, but `EnvSettingsStore.write` still sets `os.environ["PLATO_PACK_UPLOAD_MAX_BYTES"]` at `:1778` and neither `clean_env` nor `monkeypatch` restores a key monkeypatch did not set.
WAS: plato/wiring/settings_store.py :: settings-failure-not-at-info -- STILL OPEN. All three failure paths in `_apply_durable_settings` are still `logger.warning` only, with no `record_boot_note`, so nothing reaches `/info`'s `degraded`.
WAS: pyproject.toml :: otlp-http-caps-the-otel-sdk -- FIXED. The exporter is now `optional = true` behind `plato`/`telemetry`, so a consumer installing neither never resolves against it, and the declaration's comment no longer claims a ceiling it does not export.

[DEFECT] plato/wiring/default.py:629 :: forget-engine-repoints-serving-store -- `_forget_common_engine()` clears `common.core.db._async_engine` before the replacement is built, but `DbStore._resolve_engine` does not memoise on the `common` backend (`jazzx_sdk/fabric/db/store.py:324-326` calls `get_async_engine()` on every call), so a swap that fails leaves the *retained* store's engine path resolving the new connection string while its sessions still use the old one.
`PATCH /api/v1/config` with `DB_ASYNC_CONNECTION_STR` pointing at a reachable Postgres where the role lacks `CREATE` on the schema: `_forget_common_engine()` runs, `_db()` builds the replacement, `replacement.create_all()` raises, `handle.replace` is never called. From then on `db.session()` reads and writes database A (cached `_sm`, line 205-207) while `db.engine()` returns an engine on database B -- so `plato/boot/schema_version.py:96` reports B's revision as the serving replica's, and `plato/migrations/env.py:225`, reached by the in-process migrate route and `job:migrate`, migrates B. Nothing restores the global, so the state persists until the next PATCH or a restart. The docstring at `:605` claims the opposite ("`_forget_common_engine` clears a module global, not that store's own handle") -- true of the sessionmaker, false of the engine.
IMPORTANCE: medium -- reachable through `PATCH {prefix}/config` wherever `withholding` admits a write (relaxed posture, or strict with `PlatoWiring.auth`; no shipped wiring sets `auth`, so in practice local and dev-daily). I enumerated both shipped `.engine()` call sites (`plato/boot/schema_version.py:96`, `plato/migrations/env.py:225`) and the single caller of `swap_database` (`plato/app.py:212`'s `_rewire`); nothing outside this repo calls it.

[DEFECT] plato/wiring/default.py:602 :: bootstrap-write-not-persisted -- the docstring says a `DB_ASYNC_CONNECTION_STR` PATCH "persists through whichever settings layer is available", but `BOOTSTRAP_KEYS` are by construction the keys that *cannot* go to `plato_setting` (`plato/wiring/settings_store.py:9-11`), so the write lands in `os.environ` only -- the layer that module's own opening line describes as dying with the replica.
An operator follows the new `docs/DEPLOYMENT_ENV.md` row, PATCHes a parseable connection string onto a broken dev-daily replica, and it recovers. The next ACA restart, scale event, or second replica resurrects the unparseable value from the container environment with no record that a correction was made -- and on a multi-replica app the PATCH only ever reaches the one replica that served it, while `/info` on the others reports nothing new.
IMPORTANCE: medium -- fails visibly (the replica comes back degraded and `/info` says so) and the operator can redeploy, but the docs now present this as the lever, and `plato/api/config.py:46` plus the `swap_database` and `DEPLOYMENT_ENV.md` prose all state the durable half without the caveat. Checked: `BOOTSTRAP_KEYS` is read at `plato/app.py:162,191,197` and `plato/api/config.py:208` only; `DbSettingsStore._bootstrap` routes those keys to the environment in every configuration.

[DEFECT] tests/test_common_submodule_deps.py:350 :: installed-check-skips-image-extras -- `test_a_dependency_common_needs_is_installed` skips on `_extra_providing(module) is not None`, with no `_IMAGE_EXTRAS` test, while its sibling twenty lines down (`:370`) gates the same skip on `extra not in _IMAGE_EXTRAS`; so the install-side assertion now skips every module the image *does* install.
`.venv/bin/python -m pytest tests/test_common_submodule_deps.py -q -rs` skips five cases at `:351` -- `azure.monitor.opentelemetry.exporter`, `azure.storage.blob`, `azure.storage.blob.aio`, `opentelemetry.exporter.otlp.proto.http.trace_exporter`, `opentelemetry.instrumentation.fastapi` -- all in the `plato` extra CI installs, and the last of these is the module whose absence the file's own docstring cites as the failure it exists to catch. The skip reason ("which a bare install omits") is also false for the environment the test just ran in.
IMPORTANCE: low -- test-only, and `test_a_dependency_common_needs_is_locked` still covers the lock side of the same five, so the residual hole is a plato-extra distribution that is locked but fails to import in CI. Blast radius is this one parametrized test; the image build's own import check is the outside consumer and it is unaffected.

[SYMMETRY] tests/test_plato_packs_api.py:381 :: posture-list-still-handwritten -- the same file now imports `_POSTURE_ENV_VARS` at `:21` and builds `_POSTURE_KEYS` from it, but three loops in it still spell `("JAPES_ENVIRONMENT", "JAPES_ENV", "ENVIRONMENT", "APP_ENV")` by hand (`:381`, `:404`, `:439`), which is exactly the duplication the fixture change removed. `tests/test_integration_followups.py:182,202,211` carry the same three copies. Replacing each with `_POSTURE_ENV_VARS` is smaller than the loop it replaces.
IMPORTANCE: low -- correct today (all three enumerate all four), so this is drift risk rather than a failure; scope is two test files.

[SYMMETRY] plato/wiring/default.py:825 :: pack-blob-warning-restates-the-complaint -- `_pack_blob`'s azure branch now hand-writes two warning conditions (`PLATO_PACK_BLOB_CONTAINER` unset at `:820`, `STORAGE__CONNECTION_STRING` unset at `:829`) that `pack_archive_complaint()` -- added in this diff, thirty lines above, in the same module -- already answers in the same order with the same wording. `if (c := pack_archive_complaint()) is not None: logger.warning("plato: %s", c)` on the azure path is less code than the two branches and cannot drift from the 503 the upload route raises.
IMPORTANCE: low -- log text only; the route's refusal already goes through the predicate. Two warning branches in one function, no external reader.

[NOTE] plato/__main__.py:165 :: complaint-appended-to-any-refusal -- the complaint is attached to every non-`None` refusal, including a `SchemaStaleError` where the database was reached fine, and it is keyed on Plato's own `PLATO_DB_BACKEND`/`DB_ASYNC_CONNECTION_STR` regardless of what `PLATO_WIRING` resolved. A deployment on `postgresql+psycopg` (a valid async driver `create_async_engine` accepts) with a stale schema gets "names the 'postgresql+psycopg' driver" appended to a refusal that has nothing to do with the URL.
IMPORTANCE: low -- the complaint only ever accompanies a refusal that already exists, so the cost is one misleading clause; no shipped configuration uses a non-asyncpg driver and `resolved_db_backend`'s own docstring already names this failure class.

[NOTE] tests/test_common_submodule_deps.py:379 :: skip-reason-says-uninstalled -- the namespace fallback's skip reads "`opentelemetry` is not installed here, so its distribution cannot be identified", but it is installed; it is unattributable by design after this diff removed the walk. The message sends a reader looking for a missing package.
IMPORTANCE: low -- one skip line in one test.

[NOTE] tests/test_dev_daily_env_template.py:43 :: gitignore-checked-by-exact-line -- the docstring's claim is "a dotfile that `.gitignore` swallowed would be reviewed by nobody", but the check is membership of the literal string `.env.dev-daily` among `.gitignore`'s lines. `.gitignore:51-52` currently holds `.env` and `.env.local`, so the file is tracked (`git check-ignore .env.dev-daily` exits 1) -- but broadening either entry to `.env*` would ignore the template and this test would still pass. `git check-ignore` is the question being asked.
IMPORTANCE: low -- test-only, and the template is tracked today; the trigger is a future `.gitignore` edit.

VERDICT: BLOCK
