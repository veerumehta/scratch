# adversarial review -- japes

- when: 2026-09-10T01:29:14Z
- range: origin/v2.5.1...HEAD (35a3c99)
- diff: 750 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** no `TODO`/`skip` markers in this diff; the "nothing in the evaluation path reads them" comment on `Rule` (policy.py:540) holds — no shipped caller reads `status`/`effective_from`/`approval_refs`.

---

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:145` — the gate only fires for a `Threshold` *instance*, but `thresholds`/`custom` are `dict[str, Any]`, so a YAML-authored threshold stays a plain dict and is served unrefused; `get_threshold` then reports it as `INSTITUTION_AUTHORED`/`ACTIVE`. Verified against `PolicyProfile.from_yaml`:

```yaml
thresholds: {dscr_min: {value: "1.25", provenance: secondary_source, status: seed}}
```
`get("dscr_min")` → `{'value': '1.25', 'provenance': 'secondary_source', 'status': 'seed'}`; `get_threshold("dscr_min").status` → `PolicyStatus.ACTIVE`, `.provenance` → `INSTITUTION_AUTHORED`, `.value` → the whole dict. Both new tests construct `Threshold` in Python, which is why they pass.
IMPORTANCE: high — YAML is the only authoring path in shipped packs (`profiles.py:198` ← `pack/pack.py:203` ← `manifest_loader.py:249`); consumers `condition_evaluator.py:259`, `statemachine/schema.py:135`, `finance/validation.py:599` all receive the dict, and a report calling `get_threshold` states approved provenance for an unapproved figure. jaci loads packs through `Pack.profiles`.

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:38` — `ThresholdNotActivated` is deliberately not a `KeyError`, but both threshold consumers only handle `KeyError`, so the refusal escapes as an uncaught exception instead of the refusal/indeterminate each path defines. `statemachine/engine.py:92` catches `KeyError` and returns a `guard_unresolved` Refusal; a guard on a seed threshold raises out of the transition instead. `condition_evaluator.py:259` (`RatioEvaluator.evaluate`) has no handler and `experts/policy/default.py:428` doesn't wrap it, so `check_compliance` raises rather than yielding `INDETERMINATE`/`POLICY_NOT_ACTIVATED` — the reason member this diff added.
IMPORTANCE: medium — reachable today only via a Python-constructed profile (see the finding above), fails loudly; call sites enumerated: `engine.py:92`, `condition_evaluator.py:259`→`default.py:428`, `validation.py:599`.

**[DEFECT]** `jazzx_sdk/fabric/canonical/policy.py:786` — `executable_rules`/`activation_blocks` gate on `self.status` and ignore `expiry_date`, so an expired policy reports every rule executable, contradicting "the rules that may execute now". Verified: `Policy(status=ACTIVE, expiry_date=2021-01-01)` → `is_active` `False`, `executable_rules()` `['R1']`, `activation_blocks()` `{}`.
IMPORTANCE: medium — new API, no in-repo callers outside `tests/test_policy_rule_activation.py`, so nothing external can have adopted it yet; wrong answer is silent when it does.

**[DEFECT]** `jazzx_sdk/fabric/canonical/policy.py:571` — `effective_status` claims "a rule can restrict itself below its policy", but the early return discards the rule's own status whenever the policy is not `ACTIVE`: `Rule(status=ARCHIVED).effective_status(PolicyStatus.DRAFT)` returns `DRAFT`. Executability is unaffected; the reported status and the `activation_block` sentence are wrong.
IMPORTANCE: medium — same blast radius as above (new methods, tests only); the test for this direction (`test_a_rule_may_restrict_itself_below_its_policy`) only exercises the `ACTIVE`-policy case, so it does not catch it.

**[SYMMETRY]** `jazzx_sdk/agents/adjudication/segment.py:199` — one `INDETERMINATE` site gets `indeterminate_reason` while nine siblings in `jazzx_sdk/fabric/canonical/condition_evaluator.py` (139, 157, 195, 203, 244, 328, 337, 364, 530) build the same verdict with the cause already in hand: `inputs={"missing_field": ...}` is `EVIDENCE_MISSING`, the `ZeroDivisionError/ValueError` branch at 203 is `CALCULATION_UNAVAILABLE`, the malformed-condition ones are `CONDITION_UNEVALUABLE`. One kwarg each, and those are the sites `check_compliance` actually reaches.
IMPORTANCE: medium — `condition_evaluator` is the default evaluation path for every pack; the adjudication replica-failure path this diff annotated is the rarer one.

---

**[NOTE]** `tests/test_policy_threshold_provenance.py:110` — "the floors and custom buckets are covered too" only tests `custom`; `confidence_floors: dict[str, float]` cannot hold a `Threshold` at all (`PolicyProfile(confidence_floors={'f': Threshold(...)})` → `ValidationError`), so the `confidence_floors` arm of the gate in `get`/`get_threshold` has no reachable input.
IMPORTANCE: low — test-only claim plus one unreachable branch; no shipped caller affected.

**[NOTE]** `jazzx_sdk/manifest/lifecycle.py:11` — the stated reason for the module ("so `profiles` can hold a threshold status without cycling back through here") is not what the diff does: `profiles.py:24` imports `PolicyStatus` from `fabric.canonical.policy` at runtime, keeping the hard dependency; only the new test imports from `lifecycle`.
IMPORTANCE: low — no import error today (`policy` does not import `profiles`), but the docstring describes a decoupling the code doesn't take.

**[NOTE]** `jazzx_sdk/fabric/canonical/policy.py:582` — `whose = "rule" if status is self.status else "its policy"` blames the rule when both statuses coincide: a `DRAFT` rule inside a `DRAFT` policy reports "rule is draft, not active" though the policy is the block.
IMPORTANCE: low — one misleading sentence in a report; no in-repo callers yet.

VERDICT: BLOCK
