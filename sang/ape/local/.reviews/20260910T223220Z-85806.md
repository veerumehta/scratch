# adversarial review -- japes

- when: 2026-09-10T22:32:20Z
- range: origin/v2.5.1...HEAD (75cbc25)
- diff: 11895 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
CHECKED (TODO claims that hold — not raised as findings):
- `TODO(threshold-grid-provenance)` profiles.py — verified: `dscr_profile.yaml` authors all 7 `thresholds:` as bare strings and `custom:` as bare ints, so `looks_authored` is False for every one and the gate covers none of its numbers, exactly as the note says.
- `TODO(publish-tick-tie)` store_db.py — verified: `plato/seed.py:176` is a real `publish` caller, and it publishes one directory per iteration with a blob write between.
- `TODO(activation-vs-active-rules)` policy.py:754 — verified: `active_rules()` (policy.py:801) filters `legacy_alias` and ignores `exception_rules`; `executable_rules`/`activation_blocks`/`is_executable` have no non-test callers.
- `TODO(refused-rule-field-claim)` default.py — verified: `allowed` is already False from the refusal in both sub-cases, so the cost is a misleading violation entry, not a flipped verdict.
- `TODO(layout-live-formula-order)` workbook.py:183 — verified: `inputs_by_statement` is populated at workbook.py:694 after the read at :690, and both shipped layouts state the ordering only as a comment.
- `TODO(orphan-blob-gc)` store_db.py — verified: `delete` issues a row `UPDATE` only.

---

**[DEFECT] `jazzx_sdk/experts/policy/base.py:37`** (and the twin at `:109`) — `not_yet_effective` is described as "withheld from execution like a stale one", but nothing withholds either list: `resolve()` builds `precedence_order` from `applicable` without consulting `is_active` (default.py:334, 351), and `check_compliance` iterates `resolution.precedence_order` and evaluates `reg[policy_id].active_rules()` with no reference to `stale_policies` or `not_yet_effective` (default.py:453-460).
Trigger: a core policy authored `status: active, effective_date: '2027-01-01T00:00:00'` with rule `leverage_x <= 3.0`. `resolve()` lists it under `not_yet_effective`; `check_compliance()` still evaluates the rule and returns `allowed=False` with a `policy_violations` entry citing a threshold from a policy that has not started.
IMPORTANCE: medium -- `PolicyResolutionResult`/`StalenessReport` are exported from `jazzx_sdk.experts.policy`; the only in-repo executor is `DefaultPolicyExpert.check_compliance`, and `plato/data/seed_packs/dscr_core/policies/eligibility.yaml` is `status: draft`, so the "reported withheld, still evaluated" path is already live for `stale_policies`. Pack authors subclassing `BasePolicyExpert` read these descriptions; no behaviour changed, the new claim did.

**[DEFECT] `jazzx_sdk/pack/store.py:103`** — `pack_archive` returns a valid empty zip for a directory that does not exist or holds no files, so a caller's path mistake comes back as an ordinary archive rather than an error.
Trigger: measured — `pack_archive(Path('/nonexistent/pack'))` returns 22 bytes `PK\x05\x06...`; same for an empty directory. Published, that yields a row with an archive whose `_unpack` can never find a `MANIFEST_NAMES` entry, surfacing at `materialize()` far from the cause.
IMPORTANCE: low -- the one shipped caller, `plato/seed.py:176`, gates on `_manifest_of(pack_dir)` first, so no shipped configuration reaches it; `pack_archive` is newly exported from `jazzx_sdk.pack` in this diff, so external callers are possible but none exists yet. A `TODO` or a one-line raise is a complete response.

**[SYMMETRY] `jazzx_sdk/fabric/canonical/policy_lint.py:45`** — `find_profile_literal_drift` iterates `policy.rules` only, while this same diff adds `Policy._all_rules()` (`jazzx_sdk/fabric/canonical/policy.py:760`) whose docstring states the rule this lint breaks: "an exception rule is a rule, and gating only `rules` left a seed one absent from both". A `domain_extensions["profile_custom_key"]` declared on an `exception_rules` entry is never checked for drift, which is the same silent coverage loss the diff's `_declaring_expressions` recursion was written to close for nesting. `[*policy.rules, *policy.exception_rules]` is one line, smaller than the recursion the diff already added.
IMPORTANCE: low -- `find_profile_literal_drift` has no non-test caller in this repo and no shipped pack declares `profile_custom_key` on an exception rule; the cost today is a lint that under-reports.

---

**[NOTE] `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:432`** — the v0.2.1 header entry fixes exactly this on `FCCR` (undeclared `taxes`, missing `scheduled_principal`), but `UCAcashFlow.formula` still names `net_income`, `D&A` and `working_capital_change`, none of which is a `FinancialPeriod` field or in its own `inputs`; `ExcessAvailability` (`:411`) likewise divides on `reserves`, which is on `BorrowingBaseResult`, not `CollateralPool` or its declared inputs.
IMPORTANCE: low -- the file states "Formulas are documentation only"; I found no Python reader of `concept_graph` in this repo, so nothing evaluates them.

**[NOTE] `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:179`** — this diff removes "(e.g. jaci)" from `jazzx_sdk/finance/workbook.py:171` while adding the consumer package name back in shipped seed-pack bytes: 5 `jaci.*` references in `ci_lending.yaml` (`:179, 207, 235, 254, 291`, four of them as `source_schema:` values), plus `breakouts.yaml:2`, `metrics.yaml:13`, `reclassifications.yaml`, `workbook_layout.yaml:4`, `spread_template_rb.yaml`. Commit `5fd0293` on this branch was dedicated to removing exactly this.
IMPORTANCE: low -- `grep` finds no Python consumer of `source_schema` in this repo, so the strings are inert; the cost is a repo-rule violation shipped in `plato/data/`.

**[NOTE] `jazzx_sdk/fabric/canonical/policy_registry.py:125`** — `current_policies()` still documents itself as "active (non-expired, non-deprecated)", which no longer covers what `is_active` now filters; the diff updated the equivalent sentence at `jazzx_sdk/experts/policy/default.py:627` to "outside its effective window" but not this sibling or `store/core.py:206`'s `active_only`.
IMPORTANCE: low -- docstring only; the behaviour change is intentional and both call sites want it.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No `[DEFECT]` survived verification. What I ran: both packs loaded through `Pack.from_manifest` against `plato/data/seed_packs` (ci-spread-core → 6 policies, checklist and `overlays/` correctly excluded, 7 evidence types; dscr-core → 1 policy, 1 profile); all five DSCR matrix tables checked complete against their declared axes (99/3/3/12/33, zero missing cells, 29 authored `NA`); `find_profile_literal_drift` → no drift; every `key` in both workbook layouts and both spread templates resolved against `chart_of_accounts.yaml`/`metrics.yaml`; and the three rules this diff changed behave as claimed (`DSCR-IO-MIN-LOAN` violates at FICO 620, `DSCR-OVER-2M-NO-RATIO` violates a $2.5M no-ratio file where `DSCR-OVER-2M-DSCR` returns indeterminate, `CI-ABL-MIN-AVAILABILITY` violates at $1.5M availability).

## CHECKED

- `TODO(conductor-pipeline-from-yaml)` (ci-spread-core/pack_manifest.yaml): holds exactly. `Pack.conductor` is `None` for this pack, and `ConductorPipeline.from_yaml(pipelines.yaml, name="credit_analysis")` loads 13 steps / 1 loop — only for a caller that already knows the name, as the TODO says.
- `TODO(seed-pack-vendoring)` (plato/seed.py:41, governs this whole diff): holds. Both packs load in a japes-only process and the policy/profile corpora resolve; only the consumer-symbol references are inert.
- `TODO(layout-key-resolution)` and the by-hand claim layered on it in workbook_layout.yaml: holds. Zero unresolved keys in either layout.

## Findings

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:110` — `source_sha: "866474328438fd68"` is a 16-char truncation, the same truncation `policies/checklist_ci.yaml:11` renamed to `sha256_prefix` with a comment explaining the full digest is unrecoverable; the sibling provenance record in this diff still calls its prefix *the* sha. Trigger: anyone verifying `loan_package_scaffold.md` against its source computes a 64-char digest and finds no match. Rename is the same size as the fix already applied one file over.
IMPORTANCE: low -- provenance metadata only; nothing in japes or plato reads `source_sha` (grepped: no consumer in `jazzx_sdk/` or `plato/`), so the cost is one confused reader.

**[SYMMETRY]** `plato/data/seed_packs/dscr_core/pack_manifest.yaml:35` — the manifest declares `policies:` and `profiles:` but no `evidence_types:`, while four of its rules (`eligibility.yaml:171,533,577,652`) name eight evidence ids (`nmls_verification`, `hoa_budget`, `hoa_financials`, `operating_agreement`, …) that no pack-level vocabulary declares. This is the exact argument the ci-spread-core manifest makes for its own `evidence_types:` line in this same diff. Trigger: `Pack.evidence_types` returns `[]` for dscr-core (verified), so a tool registry built from this pack derives a requestable set of nothing while three `require_evidence` rules ask for evidence by id. Unlike ci-spread-core the file does not exist yet, so this is one manifest line plus a small new asset, not one line.
IMPORTANCE: low -- no consumer in this repo builds a tool registry from dscr-core (the pack declares no `modes`, `experts` or `conductor`); reachable only from jaci's own Investigator wiring, which I could not read.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:93` — `excess_availability_pct >= 0.1` encodes the 10% floor as a fraction, but `_pct` in this pack's own metric catalog means whole percent (`metrics.yaml`: `gross_margin_pct`, `ebitda_margin_pct` etc. all end `* 100`), and the sibling pack states the same convention outright (ENCODING_NOTES §4: "Values are whole percents (85, not 0.85)… `compare_field` is therefore `cltv_pct`"). Nothing in the pack defines `excess_availability_pct` — the ontology declares `excess_availability` and `total_availability` but not the ratio. Verified: with the field as a whole percent, borrowing base $30M / excess availability $2.5M (8.33%, below the stated 10% floor) returns **satisfied**, because `8.33 >= 0.1` and the $2M leg passes. With it as a fraction the same case correctly violates. The `_rate` fields beside it (`ar_advance_rate: 0.85`) are fractions, so the file's local convention argues the other way — hence a note, not a defect.
IMPORTANCE: unknown -- I could not determine which unit the consumer supplies; `excess_availability_pct` is computed in jaci, which is not readable from here, and no japes or plato call site produces it. If it is whole-percent, this is silent permissive wrong data on a covenant floor.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:230` — "Eight rules linked here" names eight rule ids, but the corpus now carries `profile_custom_key` on nine rules / ten declarations: this diff added `fico_min_no_ratio` to `DSCR-SUB-1-DSCR-FICO` and both `io_loan_amount_min`/`fico_min_io` to `DSCR-IO-MIN-LOAN`, and only the latter is in the list. Same shape at `:209`, which states "12 of the 42 rules use `kind: all_of`/`any_of`" and then enumerates ten — the count is right (verified 12), the enumeration omits `DSCR-OVER-2M-NO-RATIO` and `DSCR-IO-MIN-LOAN`, both added by this diff.
IMPORTANCE: low -- documentation shipped inside the pack; the aggregate counts it leads with (42 rules, 32/6/2/2 by kind, 99+3+3+12+33 cells, 70 eligible + 29 `NA`) all re-derived correct, so only the enumerations are stale.

**[NOTE]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:121` — `DSCR-STATE-INELIGIBLE` and all 14 `DSCR-PPP-*` rules key on `property_state` by exact string equality, and D2's fix (recorded in ENCODING_NOTES §2) made `==` compare raw values with no coercion. A context carrying `"il"`, `"Illinois"` or `" IL"` makes every Illinois buyout rule not-applicable, and `"ak"` passes the ineligible-state gate — a caller's input-shape mistake returned as an ordinary eligible result rather than an error. Nothing in the pack declares `property_state`'s domain (the ontology-less dscr pack has no vocabulary for it).
IMPORTANCE: unknown -- whether `LoanApplication.property_state` is normalized to a 2-letter uppercase code is jaci-side and not readable from here; nothing in japes or plato evaluates this corpus.

VERDICT: PASS

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED**
- `plato/posture.py:96` `TODO(if-match-post)` — the deferral holds: `require_if_match` is referenced only by `tests/test_plato_preconditions.py`; no router passes it as `auth`, so `POST {prefix}/packs/initialize` cannot 428 today.
- `plato/info_api.py:82` `TODO(omit-info)` — holds: the only caller passing `omit` is `_info_only_app` (`plato/__main__.py:304`), whose tuple is `("Guide", "config", "database", "Packs", "packs")` and does not contain `info`.
- `plato/seed.py:170` `TODO(seed-digest-drift)` — holds: `pack_archive` uses `zipfile.write`, which records each file's mtime, so a content digest over the archive would differ on a rebuild with identical bytes.

**PARKED**: `test_no_page_references_an_external_host` variant at `tests/test_plato_info_api.py:622` (`TODO(protocol-relative)`).

Test run: `tests/test_plato_packs_api.py`, `tests/test_pack_store.py` → 82 passed, 1 skipped (env-gated live pack dir). The six other touched test files → 191 passed, 1 skipped.

---

**[SYMMETRY]** `plato/seed.py:149-152` — the `(pack_id, pack_version)` key derivation is spelled twice: here and identically at `jazzx_sdk/pack/store_db.py:156-157` in `DbPackVersionStore.publish`. The four-line comment above it exists only to explain that the two must stay in step (and records the bug from when they weren't: a padded value missed the pre-check and came back as `failed: ValueError`). `jazzx_sdk/pack/store.py` already owns exactly this family for exactly this reason — `MANIFEST_NAMES` (line 91), `pack_archive` (line 104), `is_assistant_manifest` (line 154), each with a docstring saying it sits there so two modules cannot spell one rule differently. A `pack_key(manifest) -> tuple[str, str]` beside them, called from `publish` and from here, is smaller than the comment justifying the duplicate.
IMPORTANCE: low -- two call sites, both enumerated (`store_db.publish`, `plato.seed.seed_packs`); they currently agree, so nothing is broken. `jazzx_sdk.pack` is exported and jaci/juno are editable-installed against this checkout, so `publish`'s half has out-of-repo callers; `plato.seed` does not.

---

**[NOTE]** `plato/seed.py:42` (`TODO(seed-pack-vendoring)`) and `tests/test_plato_packs_api.py:543` — both claim the bundled packs still name consumer Python modules as resolvable entry points ("those entry points do not resolve in a plato-only deployment" / "still fail when something resolves *them*"). Checked every remaining `jaci` reference in `plato/data/seed_packs/`: all are YAML `#` comments or `reason:` prose strings (`workbook_layout.yaml:4,24`, `reclassifications.yaml:2`, `metrics.yaml:13`, `breakouts.yaml:2`, `spread_template_rb.yaml:12,48,128`, `ontology/ci_lending.yaml`, `dscr_core/ENCODING_NOTES.md`). Nothing resolves them, and `test_a_bundled_seed_pack_is_loadable_in_a_japes_only_process` resolves `experts`/`conductor`/`conductor_pipeline`/`modes` on both packs without raising. The TODO's other half ("nothing keeps the copies in step with the originals") does hold. `test_which_seed_pack_files_name_consumer_modules_is_pinned`'s own docstring already states the corrected version, so the two disagree within this change.
IMPORTANCE: low -- prose only; the deferral it justifies (vendored copies drift) is real and unchanged. Blast radius is a reader of these two docstrings.

**[NOTE]** `plato/migrations/versions/0004_pack_version_retired_at.py:44` — the comment justifies `ix_pack_version_retired_at` as "every read filters on it ... this is the one predicate they share", but all three reads filter `tenant_id` *and* `retired_at IS NULL` (`get`/`versions` add `pack_id`; `store_db.py:269,284,329`), and `retired_at` is NULL for essentially every row, so a single-column index on it is not what those queries will use — the predicate they actually share is the composite. The index is harmless; the stated reason is not the one that would pay for it.
IMPORTANCE: low -- one index on one table; no query-plan claim in shipped code depends on it, and nothing outside this repo reads the migration.

**[NOTE]** `plato/wiring_default.py:441` and `:626` — `pack_cache` and the local `PACK_BLOB_DIR_ENV` default resolve relative to the process CWD (`.plato-pack-cache`, `.plato-pack-blobs`, `.expanduser()` but no `resolve()`), while `default_seed_root()` in the same change does `.expanduser().resolve()` and `wiring_local` runs both of its equivalents through `_anchored()`. A container whose entrypoint runs from a different working directory gets an empty unpack cache and an empty local blob root while the rows still report `has_archive: true`.
IMPORTANCE: low -- reachable only if the image's WORKDIR changes between runs; the deployed-posture path already warns that local pack archives are non-durable (`_pack_blob`, `wiring_default.py:640`), and the durable (azure) configuration is unaffected. I did not check the Dockerfile's WORKDIR.

VERDICT: PASS

## slice4

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(threshold-grid-provenance)` (profiles.py:245): holds exactly. The bundled `dscr_profile.yaml` has 7 `thresholds:`, all bare strings, plus 5 tables totalling 150 cells = the 157 the note claims, and `looks_authored` is False for every one, so the gate covers none. Not raised.

## Findings

**[DEFECT]** `tests/test_policy_threshold_provenance.py:112` — `default_seed_root()` returns `PLATO_SEED_PACKS_DIR` when set (`plato/seed.py:51-55`), so this test asserts about whatever that points at, not "the bundled pack" its docstring names. Trigger: the workflow `plato/seed.py:10-11` documents ("point `PLATO_SEED_PACKS_DIR` at the checkout") — with it set to a consumer checkout, the test either raises `FileNotFoundError` on the missing `dscr_core/profiles/dscr_profile.yaml` or silently validates a different profile corpus. The sibling file this test's own comment cites already fixed this: `tests/test_plato_packs_api.py:36` added the autouse `_no_seed_root_override` fixture for precisely this reason ("every assertion about the *bundled* packs was really about whatever a shell variable pointed at"), and this file borrowed the accessor without the guard.
IMPORTANCE: medium -- test-only; the whole file (19 tests, one of which reaches the seed root); the one variable that reaches it is a documented dev setting, and the failure mode with an existing-but-different root is silent coverage loss, not an error.

**[DEFECT]** `tests/test_policy_threshold_provenance.py:255-258` — the `extra` parametrization sweeps only string keys, so it misses the one key type that makes `get` raise something other than `ThresholdNotActivated`: `sorted(found)` at `profiles.py:202` (and the twin at `profiles.py:233`) raises `TypeError` on mixed key types. Verified: `PolicyProfile.model_validate({"profile_id":"p","thresholds":{"dscr_min":{"provenance":"official_source",2024:"1.25"}}}).get("dscr_min")` → `TypeError: '<' not supported between instances of 'int' and 'str'`. YAML parses a bare `2024:` as an int, so an authored edition-keyed block reaches it, and the `TypeError` escapes all three refusal handlers (`statemachine/engine.py:92`, `finance/validation.py:602`, `canonical/condition_evaluator.py:299`) which catch `ThresholdNotActivated`/`KeyError`/`ValueError` only — the refusal surfaces as a crash instead of `guard_not_activated` / `POLICY_NOT_ACTIVATED`.
IMPORTANCE: medium -- three enumerated catch sites in this repo turn a refusal into a caller-visible outcome and none of them holds for this input; fails loudly; no external consumer of `PolicyProfile.get` established.

**[NOTE]** `tests/test_policy_threshold_provenance.py:181-199` and `:250-262` — both "left alone" tests assert only `get`; `get_threshold` on the same input reports the whole mapping as an approved threshold. Verified: `PolicyProfile(profile_id="p", custom={"ops_flags":{"status":"enabled","region":"west"}}).get_threshold("ops_flags")` → `value={'status':'enabled','region':'west'}`, `status=ACTIVE`, `is_executable=True`. That is the same affirmative-provenance statement `profiles.py:236-240` says it exists to avoid, one shape over.
IMPORTANCE: low -- grepped `jazzx_sdk/` and `plato/` for `get_threshold`: no caller outside `profiles.py` and tests (`policy_lint.py` names it in a comment but reads `profile.custom` directly), so nothing shipped consumes the wrapped record yet.

## Outside this diff

`plato/seed.py:25` currently imports `pack_key` from `jazzx_sdk.pack`, which does not export it (it lives in `jazzx_sdk/pack/store.py:154`). This breaks `test_the_shipped_profile_claims_no_provenance_it_was_not_given` with `ImportError` right now — the file passed 19/19 earlier in this session, before those working-tree edits landed. Not a finding on this diff, but it blocks it.

VERDICT: BLOCK
