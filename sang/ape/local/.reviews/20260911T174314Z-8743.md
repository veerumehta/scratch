# adversarial review -- japes

- when: 2026-09-11T17:43:14Z
- range: 2e2c8ca..HEAD (since the last review) (61ea70d)
- diff: 51 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_mock_knowledge_hub_app.py` → 36 passed; `ruff check` clean on both files. All statuses below are from a `TestClient` probe (`/tmp/probe_patch_nulls.py`), not inference.

WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: patch-null-is-200-not-422 -- FIXED; the comment now states "400 here, 200 there" and gives the reason (required vs optional fields), which is what the routes do. Probed all four fields on the current tree: `PATCH {"<field>":null}` → 200 with every field unchanged, and the full-body variant `{**_ENTITY,"<field>":null}` → 200 as well; create → 400. The false 422 cross-route claim is gone.
WAS: tests/test_mock_knowledge_hub_app.py :: patch-null-is-200-not-422 -- FIXED; the docstring's second paragraph now says 200 on the PATCH side, matching `test_a_null_field_is_not_supplied_not_changed`'s own assertion twelve lines above.
WAS: tests/test_mock_knowledge_hub_app.py :: rejected-create-status-unasserted -- FIXED; line 412 now asserts `rejected.status_code == 400` before the second POST, so a 500 on the first call fails the test instead of passing as proof of a rejection.

CHECKED: `mock_knowledge_hub_app.py :: TODO(non-string-scalar-passes-guard)` holds on its reachability claim -- probed `POST {..., "name": 0}` → 201 storing `"name":0`, `false` → 201, `0.0` → 201, and `KnowledgeHubClient.create_entity` (knowledge_hub_client.py:1557) types `name: str`, so only a hand-built body reaches it. The deferral's reason is intact.

[NOTE] jazzx_sdk/server/mock_knowledge_hub_app.py:231 :: todo-says-scalar-covers-containers -- the TODO scopes the hole to "a non-string JSON scalar", but the same guard passes JSON containers: probed `POST {..., "name": []}` → 201 storing `"name":[]` and keying the conflict set `"[]"`, and `{"name": {}}` → 201 keying `"{}"`. Same blast radius as the scalars (hand-built body only), but a future author greps the TODO for scalars and will not find these.
IMPORTANCE: low -- unreachable through `KnowledgeHubClient.create_entity`; the only in-repo reachers of the raw route are `tests/test_mock_knowledge_hub_app.py`, and `create_knowledge_hub_mock_app` is exported from `jazzx_sdk.server` for pack authors who build bodies by hand. Widening the TODO's wording to "non-string JSON value" is a complete response.

[NOTE] tests/test_mock_knowledge_hub_app.py:393 :: patch-side-claim-cited-narrowly -- the new docstring cites `test_a_null_field_is_not_supplied_not_changed` as the proof that the PATCH route answers 200, but that test parametrizes only `name` and `collection_id`, while this test parametrizes four fields; nothing in the suite asserts `PATCH {"ontology_id":null}` or `{"entity_type":null}` is 200. The claim is true (both probed → 200, fields unchanged), so this is an uncovered half of a stated symmetry, not a wrong sentence.
IMPORTANCE: low -- test-only; I could not name a shipped-code defect it would catch, since the PATCH route treats all four identically via `fields.get(...) is not None` and only `name`/`collection_id` have field-specific guards (lines 323, 343).

VERDICT: PASS
