# adversarial review -- japes

- when: 2026-09-16T07:29:05Z
- range: 1289f9a..HEAD (since the last review) (8b41c9b)
- diff: 1299 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_plato_info_api.py :: degraded-nav-tuple-restated -- FIXED. The test now builds `_info_only_app("server")` and asserts the 404 on `/api/v1/database/migrate` as the premise, so dropping `"database"` from `plato/__main__.py:307` fails it.
WAS: plato/app.py :: relaxed-writes-strict-unsettable -- FIXED. The comment now says "Not a knob: nothing here can set it" and points the override at the router factories, which is what the code does.
WAS: plato/api/info.py :: omit-nav-overloaded-for-mount -- FIXED. The comment now states that `omit_nav` is a proxy, names both callers, and says which way it is wrong.
WAS: tests/test_plato_info_api.py :: details-page-test-reads-ambient-tier -- FIXED. Both tests now `delenv` all four `_POSTURE_ENV_VARS` (`jazzx_sdk/config/envvars.py:161`) plus `JAPES_STRICTNESS`; the loop is complete.

[DEFECT] plato/api/packs.py:181 :: local-blob-backend-detected-by-equality -- the guard treats only the exact string `local` as ephemeral, while `_pack_blob` treats every non-`azure` value as local, so a typo'd backend passes the guard and still writes to the container filesystem.
`JAPES_ENVIRONMENT=staging` with `PLATO_PACK_BLOB_BACKEND=Azure` (or `azure-blob`, or any typo): `plato/wiring/default.py:634` takes the `backend != "azure"` branch and builds `BlobStore(backend="local", ...)`, while this line sees `"azure"` and returns without refusing. The upload answers 200, `content_digest` is set, `has_archive: true`, and the bytes are gone on the next restart with an immutable row pointing at them. That sibling's comment at `plato/wiring/default.py:632-634` records this exact predicate as the bug it already fixed: "`BlobStore` treats every non-`azure` backend as local, so `Azure` with a capital, `azure-blob`, or any typo wrote archives to `./blobs` inside the container with `has_archive: true` on the row".
IMPORTANCE: high -- silent data loss on any deployed tier, invisible to the caller (200) and to `/info`. Reachable through the only caller, the new `POST {prefix}/packs`; `_pack_blob` is the single construction site of plato's pack blob store. No consumer outside this repo mounts this router yet.

[DEFECT] jazzx_sdk/pack/store.py:205 :: zip-read-errors-escape-as-500 -- `_manifest_bytes` catches only decode and YAML errors, but `bundle.read(name)` also raises `RuntimeError` for an encrypted member, `NotImplementedError` for an unsupported compression method, and `BadZipFile` for a bad CRC, none of which the route's `except ValueError` at `plato/api/packs.py:283` converts.
Ran it: a zip whose `pack_manifest.yaml` has the encrypted bit set raises `RuntimeError: File 'pack_manifest.yaml' is encrypted, password required for extraction` straight out of `manifest_from_archive`. An operator who password-protects the archive gets a 500 with a traceback in the log instead of the 422 the docstring's `Raises:` promises.
IMPORTANCE: medium -- fails loudly, but as an unhandled server error rather than a caller-error status. One call site (`upload_pack`); `manifest_from_archive` is newly exported from `jazzx_sdk.pack.store`, so the same escape is now in the SDK surface for any other consumer.

[DEFECT] jazzx_sdk/pack/store.py:159 :: publishable-archive-may-not-materialize -- the docstring claims "an upload that publishes is an upload that will materialize", but the function copies `_unpack`'s manifest search without its member-path check, which is the guard `_unpack` says exists for uploaded archives.
Ran it: an archive holding `pack_manifest.yaml` plus a member named `../escaped.yaml` parses fine here (`{'pack_id': 'p', 'pack_version': '1.0.0'}`) and publishes 200, then `_unpack` raises `ValueError: refusing pack archive member outside the destination: '../escaped.yaml'` on every later `materialize`. The row is immutable, so that version number is permanently burned on a pack that can never be served.
IMPORTANCE: medium -- loud at materialize, but only after publish, and the version cannot be corrected in place. Reachable from `POST {prefix}/packs`; `_unpack`'s comment at `jazzx_sdk/pack/store_db.py:470` names the upload path as its reason to exist.

[DEFECT] plato/api/packs.py:120 :: config-cannot-move-the-upload-cap -- the comment and the shipped doc both say `/config` can move `PLATO_PACK_UPLOAD_MAX_BYTES` on a running replica, but the key is not in `plato_settings_catalog()`, and `settings_api` rejects anything outside it.
`PATCH /api/v1/config {"PLATO_PACK_UPLOAD_MAX_BYTES": "1048576"}` answers 422 `Unknown setting(s): PLATO_PACK_UPLOAD_MAX_BYTES` (`jazzx_sdk/server/settings_api.py:212-214`). `docs/DEPLOYMENT_ENV.md:155` ships the same claim to operators. Unlike the pack-blob variables that `plato/api/config.py:50-57` deliberately excludes, this one genuinely is read per call, so a catalog entry would make the claim true.
IMPORTANCE: medium -- fails loudly with a 422 and the workaround is a redeploy, but the false statement is in shipped documentation, which is the thing an operator acts on.

[DEFECT] tests/test_common_submodule_deps.py:145 :: relative-imports-in-package-init-dropped -- `_absolute` computes the parent package for every relative import, which is correct for a module but wrong for a package `__init__.py`, so whole `common` subtrees are silently skipped by the sweep this file exists to be.
`jazzx_sdk/clients/japes_queue_client.py:25` imports `common.core.queue` unguarded at module level. That resolves to `common/core/queue/__init__.py`, whose `from .azure_queue_provider import ...` (level 1) becomes `common.core.azure_queue_provider`, which does not exist, so `_third_party_reached` hits `if not path.exists(): continue` and drops it. I ran the helpers: `azure.storage.queue`, `azure.storage.queue.aio` and `azure.identity` are absent from the 29 modules the walk finds, despite being reached from an unguarded module-level japes import. The same hole applies to `common.core.streaming` and `common.core.redis_streams`, both also in the japes-reaches set, and the guard test at line 216 only pins `sqlmodel` and `azure.monitor`, which arrive via non-package modules.
IMPORTANCE: medium -- test-only and no dependency is missing today (`azure-storage-queue` and `azure-identity` are both declared at `pyproject.toml:30-31`), but the blind spot covers six third-party imports behind the queue client, so the next undeclared one ships exactly the way `sqlmodel` did.

[DEFECT] plato/wiring/settings_store.py:81 :: settings-failure-not-at-info -- the dedup is justified by "It is already reported as a degradation at `/info`", and nothing reports it: `/info`'s `degraded` is `wiring.degraded` plus `boot_notes()` (`plato/wiring/contract.py:105-109`), and neither `read()` nor `_apply_durable_settings` records a boot note.
A deployed replica whose durable layer becomes unreachable after boot: `read()` logs one warning for the life of the process, `/info` answers `degraded: []` and `configured: true`, and `/config` keeps serving environment-only values as if they were the merged answer. The only `record_boot_note` calls are `plato/wiring/default.py:220` (pack dir) and six in `plato/__main__.py` (role, wiring, boot contract); none is the settings store.
IMPORTANCE: medium -- observability, not wrong data (`read()` still returns the environment values), and the boot-time case is separately logged by `_apply_durable_settings`. Two instances exist per process, both from `durable_settings_store`; the `/config` router's is the one that serves reads.

[SYMMETRY] plato/api/packs.py:130 :: upload-cap-has-no-floor -- `_upload_cap` omits the `floor=` every other bounded `env_int` in the repo passes, so `PLATO_PACK_UPLOAD_MAX_BYTES=0` or `-1` refuses every upload, including a 62 KB bundled pack, with "exceeds the 0-byte limit".
`floor=1` is the existing shape at `jazzx_sdk/fabric/config.py:138`, `:144`, `:155`, `jazzx_sdk/runtime/launcher.py:60`, `:206` and `plato/__main__.py:229`; `env_number` applies it to the default and fallback paths too. One kwarg, and it makes the constant's own claim ("it exists to stop an unbounded upload, not to bound authoring") hold for every value.
IMPORTANCE: low -- operator misconfiguration only, and it fails loudly with a 413 naming the limit.

[SYMMETRY] Dockerfile:138 :: image-check-covers-one-of-four -- the same commit declares four previously-missing `common` dependencies and the in-image import check gained only the module behind one of them.
`pyproject.toml:56-61` adds `azure-monitor-opentelemetry-exporter`, `opentelemetry-exporter-otlp-proto-http` and `azure-storage-blob` alongside `sqlmodel`, and the comment above the check says it imports the module "so the whole path is exercised" -- but `common.utils.telemetry` and `common.core.storage.async_blob_storage_client`, the two modules that import them unguarded, are not in the line. The new pytest file covers them and the image runs no pytest, so the import check at line 135 is the only gate inside the artifact. Three more names on the existing line.
IMPORTANCE: low -- the lock pins all four, so the image resolves them today; it matters only if a wheel fails to resolve for the image's platform, which is the case the check exists for.

[NOTE] plato/api/packs.py:144 :: part-header-precheck-never-fires -- the `content-length` pre-check reads the multipart *part* headers, which carry only `content-disposition` and `content-type`, so the branch the docstring describes as "checked too" has no reachable trigger.
Measured with a `TestClient` upload: `dict(archive.headers)` is `{'content-disposition': ..., 'content-type': 'application/zip'}`. The request-level `Content-Length` that would let it refuse before reading is on `request.headers`, not `upload.headers`.
IMPORTANCE: low -- the chunked measurement below it is the real guard and is correct; cost is a dead branch and a docstring sentence that reads as a live one.

[NOTE] pyproject.toml:57 :: otlp-http-caps-the-otel-sdk -- `opentelemetry-exporter-otlp-proto-http` requires `opentelemetry-sdk (>=1.38.0,<1.39.0)`, so japes' own `opentelemetry-sdk = ">=1.20.0"` floor now resolves to exactly 1.38.x for every consumer.
Visible in the lock: `opentelemetry-proto` moved from 1.42.1 under `markers = extra == "mlflow"` to 1.38.0 in the main group, and `protobuf` lost its extra marker the same way. The ceiling is transitive rather than written, but it is a ceiling, which is what the repo's dependency rule exists to avoid.
IMPORTANCE: low -- resolution succeeds today and `opentelemetry-sdk` was already locked at 1.38.0; it bites the next time something wants a newer SDK.

[NOTE] plato/wiring/settings_store.py:164 :: recovery-branch-unreachable-at-boot -- the "readable again" reset added to `apply_to_environment` cannot fire: `_last_failure` is only ever set by `read()`, and `durable_settings_store` returns a fresh store per call, so `_apply_durable_settings` (`plato/wiring/default.py:700`) calls `apply_to_environment` on an instance whose field is still `None`.
Its failure path is also the un-deduped one, which is harmless because it runs once per boot -- but that makes the pair read as if both halves of the dedup were applied to both methods.
IMPORTANCE: low -- no behaviour change either way; the `read()` copy at line 107 is the one that runs.

VERDICT: BLOCK
