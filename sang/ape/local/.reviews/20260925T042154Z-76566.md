# adversarial review -- .

- when: 2026-09-25T04:21:54Z
- range: HEAD~2...HEAD (82e3ed70)
- diff: 830 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[DEFECT] jazzx_sdk/server/policy_extract_api.py:170 :: submit-skips-deployed-auth-guard -- `start_run` accepts uploads, spends LLM calls and writes proposals with `auth=None` even when japes is in a deployed posture. The sibling routers refuse that case: `vocabulary_review.py:262` `_require_decision_auth` and `_require_resolved_actor_in_deployed_posture`, `agent_config.py:120,126`, and `settings_api.py:202`.
Trigger: a host mounts `create_policy_extract_router(service)` without `auth`, with `deployed_posture()` true. An anonymous `POST /policy-extract/{pack}/runs` returns 202, the reasoning agent runs, and the proposals land in the store. The only gate is the optional `before_write`, so the fail-closed default the rest of the family gives is missing here.
IMPORTANCE: medium -- no in-repo host mounts this router yet (the only caller is `tests/test_policy_authoring_api.py`), but it is exported for shells like Plato. Once it is mounted, anonymous callers can spend tokens and write proposals.

[DEFECT] jazzx_sdk/server/policy_extract_api.py:128 :: proposals-not-tenant-scoped -- `tenant_of` scopes only the run record. `components_for(representation, pack_id, policy_id)` and `pack_for(pack_id)` never receive the tenant, so the store the components write proposals into cannot vary by tenant.
Trigger: a multi-tenant host with `tenant_of=lambda r: r.headers["x-tenant"]` and one `components_for` closure over a store, which is the shape the test and the docstring show. Tenants acme and globex both extract into pack `dscr`. Their proposals share one store, and each tenant sees the other's in the review queue, while `get_run` and `list_runs` still look isolated. The only workaround is reading `CallerIdentity` from the copied contextvar inside `components_for`, and nothing documents it.
IMPORTANCE: medium -- this silently leaks data across tenants once a multi-tenant host mounts the router. I found no in-repo mount, and none of the consumer repos checked here reference it. The docstring's "scopes runs" is literally true but invites exactly this wiring.

[DEFECT] jazzx_sdk/server/policy_extract_api.py:188 :: dot-filename-escapes-422 -- `Path(upload.filename).name` can be `..` or empty (filename `x/..` or `/`). Then `path` is a directory, and `write_bytes` raises `IsADirectoryError` outside the `try` that maps unreadable uploads to 422.
Trigger: a multipart upload with filename `..` gets a 500 instead of a 4xx that names the file.
IMPORTANCE: low -- the request fails loudly, no data is written, and the cause is the caller's filename.

[NOTE] jazzx_sdk/server/vocabulary_review.py:470 :: comment-parent-not-checked -- `add_comment` stores `parent_id` without checking that it names a comment on the same proposal, so a reply can thread under a comment from another proposal, or under an id that does not exist.
IMPORTANCE: low -- the comment is still stored and listed under the right proposal; only the threading is wrong.

[NOTE] jazzx_sdk/server/policy_extract_api.py:95 :: run-registry-never-evicts -- `_runs` grows for the life of the process with no cap or TTL. The module docstring covers losing runs on restart, but not the unbounded growth between restarts.
IMPORTANCE: low -- each record is small, and growth is one entry per submitted run.

VERDICT: BLOCK
