# adversarial review -- japes

- when: 2026-09-08T23:02:39Z
- range: f40e4d4..HEAD (since the last review) (8abeff2)
- diff: 68 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**Previous round**

- WAS: `blob/store.py:105` "content_key re-implements the hash with bare `hashlib.sha256`" — **FIXED**; line 118 now calls `jazzx_sdk.digest.content_digest`, and `hashlib` is dropped from the module imports.
- WAS: `blob/store.py:100-102` docstring claims the shared helper prevents the same bytes landing under two keys — **FIXED**; the docstring now states the opposite explicitly and names the `sha256/<hex>` vs `packs/sha256/<hex>.zip` double-write.
- WAS: `blob/store.py:105` unvalidated `prefix` (`""` → `/<hex>` writes outside `blob_dir`) — **FIXED**; the guard at 123-127 rejects empty, leading-`/`, and any `..` segment, with a parametrized test covering all four shapes (16/16 pass).

**New review**

No defects.

---

**[SYMMETRY]** `jazzx_sdk/fabric/blob/store.py:123` — the guard is on the key *builder*, but the escape happens at `put`, `jazzx_sdk/fabric/blob/store.py:64` (`path = self._local_dir / key`), which `offload(key=...)` (line 154) feeds a caller-supplied key straight into with no check; the same containment rule already exists in this repo at `jazzx_sdk/pack/store_db.py:372-373` (`(root / name).resolve()` vs `root.resolve()`), so `offload(key="../x")` still writes outside `blob_dir` while `content_key(prefix="../x")` refuses. The in-code comment concedes this ("`put(key=...)` has always had this shape") but leaves no `TODO` on `put`.
IMPORTANCE: low — I enumerated every `put(key=)` and `offload(` call site in `jazzx_sdk/`, `plato/` and `tests/`: `store_db.py:182` (key from `content_key`, now guarded), `store.py:154` (from `content_key` under `dedup=True`), and `tests/test_fabric_blob.py:22` (literal). No shipped caller passes an externally-derived key, and nothing outside japes calls `BlobStore.put`.

---

**[NOTE]** `jazzx_sdk/fabric/blob/store.py:125` — the comment says the method's "whole job is building the key, so it is the place to refuse", but only `prefix` is refused; `suffix` is concatenated unchecked, so `content_key(b"x", suffix="/../../x")` produces the same escaping key the guard exists to prevent. Only caller passes the literal `".zip"` (`pack/store_db.py:181`).
IMPORTANCE: low — two call sites total, both literal suffixes; reaching it needs a new in-repo caller.

**[NOTE]** `tests/test_pack_store.py:305` — the test exercises `BlobStore.content_key` and touches nothing in the pack store, while `tests/test_fabric_blob.py` is the file for that class; nothing is untested either way, it just won't be found by someone changing the blob store.
IMPORTANCE: low — test placement only; 16/16 in `test_pack_store.py` pass.

VERDICT: PASS
