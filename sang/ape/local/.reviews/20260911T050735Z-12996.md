# adversarial review -- japes

- when: 2026-09-11T05:07:35Z
- range: b130af0..HEAD (since the last review) (d8da179)
- diff: 157 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree (`pytest tests/test_plato_boot_contract.py tests/test_plato_packs_api.py tests/test_plato_info_api.py` → 178 passed, 1 skipped).

WAS: plato/boot_contract.py :: posture-job-exit-not-in-contract -- FIXED; the row is `job_exit=6`, `JOB_EXIT_CODES` yields `(4, 5, 6)`, and `docs/DEPLOYMENT_ENV.md:249` renders `6`.
WAS: tests/test_plato_packs_api.py :: job-exit-code-hardcoded-in-test -- FIXED; the case moved to `tests/test_plato_boot_contract.py:136` and asserts `entry.main([]) == rule.job_exit` against `_find("a tier no posture variable names")`.
WAS: plato/__main__.py :: module-docstring-posture-split-unstated -- FIXED; the two comment blocks no longer restate each other and the posture split reads as an ordinary `job_exit` row.
WAS: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer -- STILL OPEN; untouched by this diff.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; untouched by this diff.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested

[DEFECT] plato/boot_contract.py:104 :: job-exit-six-never-terminates -- the row (and the table rendered from it) now promises a job "exits 6" on an unreadable tier, but the real process computes 6 and then hangs forever: `load_wiring` has already opened the sqlite store the unreadable tier selects, and aiosqlite's `_connection_worker_thread` is non-daemon, so the interpreter never shuts down.
Reproduced against the working tree, no stubs: `env -u JAPES_ENVIRONMENT -u JAPES_ENV -u APP_ENV ENVIRONMENT=prodution JAPES_RUN_MODE=job:sweeper PLATO_WIRING=plato.wiring_default:build python -m plato` prints the refusal and never returns (killed at 120s). With `faulthandler.dump_traceback_later(25)` around `runpy.run_module('plato')`: `SYSTEMEXIT 6`, then `threads: [('MainThread', False), ('Thread-1 (_connection_worker_thread)', False)]` and a 25s timeout dump inside `aiosqlite/core.py:59`. The same holds for the schema row: `ENVIRONMENT=local JAPES_RUN_MODE=job:session-reaper` gives `SYSTEMEXIT 5` and the identical live thread. Row 1 (`job_exit=4`) is the only one that genuinely exits, because `WiringError` fires before any store opens -- which is why `test_a_job_with_no_wiring_exits_with_the_documented_code` is the only subprocess job test that can pass. The new test at `tests/test_plato_boot_contract.py:155` stubs `plato.wiring.load_wiring`, i.e. it stubs away exactly the call that opens the store, so it asserts the return value of `main()` and not that the process ends; its docstring's reason for going in-process ("a subprocess version of this passed against the unfixed code") is true, but a subprocess version against the *fixed* code does not pass either -- it hangs.
IMPORTANCE: high -- an ACA Job on a mistyped posture variable, or any job against a stale schema, runs to its platform timeout instead of terminating with a code a supervisor can branch on; the failure is invisible because `/info` is not served and the exit code never arrives. Enumerated the reachable paths: every `job:*` role whose wiring resolves through `plato.wiring_default:build` onto sqlite (the unreadable-tier row selects sqlite unconditionally, so exit 6 always hits it), plus any successful job run on the same wiring. Not verified for a postgres-backed wiring -- asyncpg may not leave a non-daemon thread -- but the exit-6 row cannot reach postgres by construction. Nothing outside this repo depends on `run_role`'s return value; the container does.

[DEFECT] docs/DEPLOYMENT_ENV.md:276 :: job-exit-prose-omits-posture-code -- the prose two paragraphs below the regenerated table still enumerates the job codes as "an unresolvable wiring (4) or a stale schema (5)", so the same document now states both that a posture refusal exits 6 (line 249, generated) and that only 4 and 5 exist.
An operator reading the `--check` section writes a supervisor branch on 4/5; `ENVIRONMENT=prodution` with `JAPES_RUN_MODE=job:*` produces 6, which that branch treats as unknown. `test_the_documented_table_is_the_rendered_one` only asserts `as_markdown_table() in DOCS`, so hand-written prose in the same file is outside every check -- this is the eight-places-of-prose drift `plato/boot_contract.py`'s module docstring says the table exists to end.
IMPORTANCE: medium -- documentation only, fails loudly at worst (an unrecognised code), and the generated table one screen above is correct. Enumerated every other enumeration of the codes: `grep -rn "return 6\|job_exit\|(4)\|(5)"` over `*.py`/`*.md`/`Dockerfile` finds no other stale list; `docs/DEPLOYMENT_ENV.md:29` and `:112` speak of "exits non-zero" and stay true.

[SYMMETRY] tests/test_plato_boot_contract.py:136 :: schema-row-job-exit-untested -- the diff adds the missing drift test for the `job_exit=6` row, but the `job_exit=5` row still has none, which is the same gap the new test's own docstring diagnoses ("Row 1 had this test and this row did not, which is why that drift was invisible"); lining it up is a copy of the new test with `_find("the database schema is behind")` and a stubbed `check_schema_or_report`.
IMPORTANCE: low -- test-only, and as written it would not have caught anything shipped today: the row's value and `run_role`'s `return 5` agree. Named because it is the last of the three `job_exit` rows with no assertion against its code path, and because the path it covers is the second instance of the hang above.

VERDICT: BLOCK
