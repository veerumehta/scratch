# adversarial review -- japes

- when: 2026-09-11T04:20:02Z
- range: 457172f..HEAD (since the last review) (e722b47)
- diff: 131 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/posture.py :: relaxed-only-fails-open-on-unknown-tier -- FIXED; `ENVIRONMENT=prodution`/`APP_ENV=staging2`/`JAPES_ENV=stg` now yield a 403 that names the variable, verified by running the router with each.
WAS: plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml :: all-four-applies-to-overstated -- FIXED; three lists name a term-loan variant (`pack_manifest.yaml:88`, `:98`, `:112`) and `pb-ci-abl-001` at `:78` names none, which is what the comment now says.
WAS: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer -- STILL OPEN; untouched by this diff, the rule still returns INDETERMINATE on a context without the field.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; untouched, the four rules still report NOT_APPLICABLE on an absent field.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested

[DEFECT] plato/posture.py:201 :: blank-tier-var-reads-as-unreadable -- a posture variable that is *set but empty* is counted as an unrecognised tier, so a blank `ENVIRONMENT=` refuses the pack writes on a laptop where every other reader resolves `local`.
`ENVIRONMENT=""` in the process environment (docker-compose `ENVIRONMENT: ${ENVIRONMENT}` with the host variable unset, or a blank `APP_ENV=` line in `.env`): `os.getenv` returns `""`, not `None`, so the walrus guard passes and `"".strip().lower()` is not in `_TIERS`. Ran it: `POST /api/v1/packs/initialize` answers `403 "pack seeding is refused because the deployment's tier is unreadable: ENVIRONMENT='' names no known tier"` while `environment_tier()` returns `local` and `deployed_posture()` returns `None`. Whitespace-only (`ENVIRONMENT=" "`) takes the same path. `envvars` already has the named rule for this distinction, `env_text` ("treats an empty string as set, deliberately" is why it exists, per `resolve_kh_token`'s docstring); the new predicate uses raw `os.getenv` instead.
IMPORTANCE: medium -- the two call sites are `plato/packs_api.py:168` (seed) and `:183` (delete), both default `strict=None`. It fails loudly with a message naming the variable, and unsetting it is the workaround, so a local developer is blocked rather than misled. Nothing outside this repo calls `relaxed_only` (grepped: 3 non-test hits, all in `plato`).

[SYMMETRY] plato/posture.py:201 :: unreadable-tier-predicate-belongs-in-envvars -- the "no posture variable names a known tier" test is built inline in a Plato router helper, reaching across the package for two private SDK names (`_POSTURE_ENV_VARS`, `_TIERS`), while the only other two readers of that pair, `environment_tier` and `deployed_posture`, both live in `jazzx_sdk/config/envvars.py`; a sibling predicate there (beside them, using the already-public `ENVIRONMENT_NAMES`) is the same amount of code and is what `plato/wiring_default.py:581` and `:635` would need. Those two still read `deployed_posture()` alone, so `ENVIRONMENT=prodution` on a deployed replica still resolves "not deployed" there and admits the in-process store and Mock Knowledge Hub -- which is the author's own earlier argument about this gate disagreeing with its siblings, now pointed the other way.
IMPORTANCE: medium -- enumerated every `deployed_posture()`/`environment_tier()` caller in `plato` and `jazzx_sdk` (13 non-test hits); the ones that change behaviour on an unreadable tier are `wiring_default.py:330/421/581/635`, `client_layer.py:292-298` and this gate, and only this gate was hardened.

[NOTE] plato/posture.py:152 :: docstring-omits-unreadable-refusal -- the summary line still reads "Refuse a write outside a relaxed posture (``local``, ``dev-daily``)", which is now narrower than the code: the function also refuses *inside* a relaxed posture whenever any of the four variables holds a value `_TIERS` does not know, a case only the body comment describes.
IMPORTANCE: low -- docstring only; the 403 text itself explains the refusal, so an operator hitting it is not left guessing.

[NOTE] plato/posture.py:210 :: blocked-term-redundant-in-guard -- `if blocked and unrecognised and strict is None` cannot be reached with `blocked` false: when `strict is None` and `unrecognised` is non-empty, `blocked` is computed as `... or bool(unrecognised) or ...`, so the first term carries no predicate of its own.
IMPORTANCE: low -- one redundant conjunct in one function; no behavioural difference in any configuration.

VERDICT: BLOCK
