# adversarial review -- japes

- when: 2026-09-09T02:25:46Z
- range: origin/dev...HEAD (93e2a8c)
- diff: 416 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
No [DEFECT] survived verification. I probed the cache state machine directly (cold/warm × fetch-failure/empty-document × inside/outside cooldown, forced and unforced) and the `failed`/`error`/`fetched` triple holds in every combination I could reach; `PyJWT.get_unverified_header` rejects a non-string `kid` before it reaches `.get(kid)`, so the one type-confusion path into the new `except Exception` in `verify` is closed.

**CHECKED**
- `plato/oidc.py:298` `TODO(jwks-error-detail)` — claim holds. `grep -rn "TokenVerifier\|plato.oidc\|oidc"` over `plato/`, `jazzx_sdk/` and `/Users/sangit/src/jaci` returns zero importers outside `tests/test_plato_oidc.py`; nothing mounts it in a request path, so the JWKS URL in the message is log-only today.
- `tests/test_plato_oidc.py` `TODO(malformed-jwks-clock)` — claim holds. Both the malformed-document exception and the `ConnectError` land in the same handler (`plato/oidc.py:270`) and stamp the same line (`plato/oidc.py:296`), which `test_a_down_issuer_is_not_refetched_on_every_verify` does exercise with an advancing clock.

---

**[SYMMETRY]** `plato/oidc.py:161` — `_reason` fixes the empty-`str(exc)` hole privately in `oidc.py`, but `plato/info_api.py:319` (`f"cannot reach the database: {type(exc).__name__}: {exc}"`) and `plato/database_api.py:64` already carry the identical format string with the identical hole, and unlike `oidc.py` they are mounted. Trigger: a DB read timeout raises `asyncio.TimeoutError`, whose `str()` is `""`, so `/info` renders `"cannot reach the database: TimeoutError: "` — the exact operator-facing gap `_reason`'s docstring describes. Same shape also at `plato/schema_version.py:143` and `plato/__main__.py:245`. Lifting `_reason` to a shared plato module and calling it from those sites is a move plus imports, no more code than the current private copy.
IMPORTANCE: medium -- `create_info_router` is mounted from both `plato/app.py:26` and `plato/__main__.py:279`, so `/info`'s schema block is reachable in every shipped server config; `database_api` likewise via `plato/app.py:206`. Nothing outside this repo imports `plato.oidc`, so the oidc half of the pair is currently the *less* exposed one.

**[NOTE]** `tests/test_plato_oidc.py` (blip test, `state["mode"] = "ok"` before the final assert; same shape in `test_an_empty_jwks_does_not_reject_valid_tokens`) — the recovery step is inert. I reran the blip scenario leaving the fetcher raising: the final `verify(_token(key, "k1"))` still passes and the fetch count stays at 2, because `_fresh_enough(force=False)` answers from the warm cache without calling the issuer. The property under test (a cached key survives a failed refetch) is genuinely covered; only the docstrings' "even after the issuer recovered" is unexercised.
IMPORTANCE: low -- test-only, and I can't name shipped-code behaviour it would have caught; the retained-keys branch at `plato/oidc.py:296` is already pinned by the assertion that does run.

**[NOTE]** `plato/oidc.py:348` — "so nothing but its two lazy imports can escape as a non-`TokenError`" is narrower than stated: `plato/oidc.py:305-320` (`self._now()`, `logger.warning`, the `_KeyCache` construction) also sits outside the `try`. No trigger with the default `now=time.monotonic`, and `verify`'s backstop catches it anyway.
IMPORTANCE: low -- only an injected `now` callable could raise there, and the only injections are in this test file.

**[NOTE]** `plato/oidc.py:317` — the empty-keys branch's log line has no assertion, while its twin at `plato/oidc.py:295` does (`test_a_jwks_failure_is_logged_not_only_raised`). Both branches make the same "the operator gets it from the logs" promise; deleting `plato/oidc.py:317` leaves the suite green.
IMPORTANCE: low -- one missing log line in a module nothing mounts yet.

VERDICT: PASS
