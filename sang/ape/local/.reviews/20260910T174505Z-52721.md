# adversarial review -- japes

- when: 2026-09-10T17:45:05Z
- range: origin/v2.5.1...HEAD (78a965c)
- diff: 10946 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## Findings

**[DEFECT]** `jazzx_sdk/finance/validation.py:788` (and the stale comment at 810-812) — the docstring says the function maps `policy_violations` **and `warnings`**, and states "Every warning becomes a `POLICY_VIOLATION` finding"; the loop at 820 reads `refusals`, not `warnings`.
Trigger: `ComplianceResult(allowed=False, policy_violations=[], warnings=["rule not activated -- CI_CORE/R1: ..."], refusals=[])` — the shape a pack `BasePolicyExpert` subclass produces if it fills the documented, pre-existing `warnings` field and not the field added in this diff. Returns `[]`, so the spread validates clean for a deal the expert refused: exactly the failure the docstring paragraph claims to have closed.
IMPORTANCE: medium -- I enumerated in-repo reachers: `validate_package` (validation.py:877) is the only caller, and the only in-repo producer is `DefaultPolicyExpert.check_compliance`, which sets `warnings` and `refusals` from the same list, so nothing in this repo is wrong today. `ComplianceResult` and `BasePolicyExpert` are exported from `jazzx_sdk.experts.policy`, and the function already accommodates a duck-typed consumer (its own comment at 813-815), so an out-of-repo expert is where it bites; I did not check jaci's subclasses.

**[DEFECT]** `jazzx_sdk/experts/policy/default.py:665` — `policy.not_in_force() or (f"status is ...")` prefers the window sentence, so a policy withheld for its *status* is reported with a window reason.
Trigger: `Policy(policy_id="X", status=PolicyStatus.DRAFT, effective_date=<2027-01-01>)` → `stale_policies=["X"]`, `staleness_reasons["X"] = "policy 'X' is not effective until 2027-01-01T00:00:00+00:00"`, `recommended_actions=["Review and update 1 stale policy"]` — the report tells the reader to update a policy whose stated reason is that it has not started, which is the confusion this diff set out to remove. Same shape for `status=DEPRECATED, expiry_date=<past>`: the reason says only "expired at", dropping the deprecation. The `not_yet_effective` bucket is gated on `status is ACTIVE`, so only the sentence is wrong, not the bucketing.
IMPORTANCE: low -- `staleness_reasons` is display text. Reached via `DefaultPolicyExpert.detect_staleness` and its `execute` dispatch (`experts/catalog.py:73`); `detect_stale_pinned_policies` (validation.py:844) reads only `stale_policies`, so no in-repo consumer branches on the sentence.

**[SYMMETRY]** `jazzx_sdk/modes/operational/governor.py:105` — this diff widened `Policy.is_active` (policy.py:732) to be False for a not-yet-effective policy, but the Governor's citation check still logs `"Context %s cites rule from deprecated policy %s v%s"` for any `not policy.is_active`, so a scheduled policy is now newly reported as deprecated. Line it up with `default.py:654-666`, which just learned to split scheduled from overdue and already has the sentence to use (`policy.not_in_force()`); the change is the same size as the one made there.
IMPORTANCE: low -- one `logger.warning` inside a block whose own docstring says "nothing here may fail the turn"; reached from `GovernorMode`'s citation check with a caller-supplied registry.

**[SYMMETRY]** `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml` `concept_graph.UCAcashFlow` — the v0.2.1 header entry fixes precisely this defect class for `FCCR` and `DebtServiceCoverage` (formula naming a field the entity does not declare; `inputs` missing an entry) and leaves the sibling three lines away: `UCAcashFlow`'s formula reads `net_income`, `D&A` and `working_capital_change`, none of which are `FinancialPeriod` fields or appear in its `inputs`, while its `inputs` names `FinancialPeriod.ebitda` and `operating_cash_flow`, which the formula never uses.
IMPORTANCE: low -- the file's own header says concept_graph formulas are documentation and runtime computation is in the Reasoner/`compute_ci_metrics`; nothing in japes parses `concept_graph`. It ships as pack data, so an SME review or a Reasoner prompt is what reads it.

**[NOTE]** `jazzx_sdk/fabric/canonical/condition_evaluator.py` `blocked_gate_outcome` docstring — "Callers that keep the rule id do so themselves -- `check_graph` sets it, `run_segment` reads it off the same rule" describes neither call site: the function sets `rule_id` from the passed rule, and `check.py:381` returns the result unmodified.
IMPORTANCE: low -- docstring only, two call sites, both in this repo.

**[NOTE]** `jazzx_sdk/fabric/canonical/policy.py:773` and `:790` — `Policy.effective_date` is `datetime = Field(...)`, required and non-Optional (policy.py:677), so `not_in_force`'s `if bound is None: continue` can only ever fire for `expiry_date`, and `is_not_yet_effective`'s `if self.effective_date is None: return False` has no reachable predicate.
IMPORTANCE: low -- dead guards, no behaviour attached.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:511` — the `not_activated` branch `continue`s before `this_policy_fields.update(contract.fields)` (line 522), so a refused overlay rule does not claim its field and the lower-precedence core rule on the same field still evaluates. Concrete: overlay rule on `profile:leverage_max_overlay` (seed status) is refused, then `CI_CORE_LEVERAGE_POLICY`'s `leverage_x <= 3.0` runs and lands a violation citing the threshold the overlay was meant to supersede. `allowed` is already False from the refusal, so the cost is a violation entry naming the wrong governing number.
IMPORTANCE: low -- one call path (`check_compliance`), and the pre-existing `EVIDENCE_MISSING` `continue` on the next line has always behaved the same way.

## CHECKED

- `TODO(threshold-grid-provenance)` (profiles.py) — claim holds, and its self-correction is accurate: `plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml` authors all 7 `thresholds:` as bare strings, so `looks_authored` is False for every one and `_coerce_threshold_mappings` gates none of them; `tables` has no gate. `confidence_floors` is typed `dict[str, float]` and so cannot hold a governance mapping, which is why leaving it out of the validator is not a third gap.
- `TODO(publish-tick-tie)` (store_db.py:59) — holds. `publish` has exactly one caller (`plato/seed.py:157`), which publishes one directory per iteration with a blob write and a DB round trip between each.
- `TODO(activation-vs-active-rules)` (policy.py:756) — holds. `active_rules` (policy.py:805) filters `legacy_alias` from `self.rules` only; `_all_rules` filters nothing and adds `exception_rules`. No caller of `executable_rules` or `activation_blocks` outside `policy.py` itself.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## [DEFECT] `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:663`
`DSCR-PPP-IL-2`'s `all_of` applicability includes `interest_rate > 8`, and when that field is absent the limb evaluates INDETERMINATE, the gate is neither SATISFIED nor blocking, and `check_compliance` skips the rule entirely — so the rule silently does not fire instead of surfacing a data gap.

Trigger, run against the bundled pack through `DefaultPolicyExpert.check_compliance` (`registry=load_policies(...)`, `_policy_profile` in context): `property_state=IL, borrower_entity_type=individual, loan_amount=300000, prepayment_penalty_requested="5yr_step"` → with `interest_rate=8.5` gives `allowed=False, ['DSCR-PPP-IL-2']`; with `interest_rate` omitted gives `allowed=True, []`. ENCODING_NOTES §1b itself records `interest_rate` as defaulting to `None`, and §1a's own reasoning ("a field left unset would silently never block, which is worse than a wrong default") is the argument against leaving it that way — but it was applied only to the three attestation booleans.

IMPORTANCE: medium -- I reached it via `jazzx_sdk.experts.policy.default.check_compliance` (default.py:477-487 is the skip), the only evaluator for this corpus. japes itself has no caller that builds a loan context, so the shipped-here impact is the seed copy; the same rule text is in jaci's authoring copy, where `LoanApplication` supplies the context and the PPP rules are enforced. It fails silently, not loudly, which is why I am not rating it low.

## [DEFECT] `plato/data/seed_packs/ci-spread-core/spread_template_rb.yaml:114`
`Less Cash Taxes (Refund)` is bound to `income_tax_expense` (accrued book tax off the income statement) while `income_taxes_paid_net_of_refunds` exists in the same pack's `chart_of_accounts.yaml:584` as the cash-flow figure the row asks for. Eight lines down, `Cash Interest` (:122) is deliberately left `unbound: true` on the reasoning that binding an accrual proxy "would overstate confidence" — the same objection applies to the row above it, and it was bound anyway.

Trigger: any RB spread rendered from this template with `income_tax_expense != income_taxes_paid_net_of_refunds` (deferred taxes, refunds, timing) puts the wrong number in the Adjusted Fixed Charge Coverage numerator, labelled "Cash Taxes", with no unbound marker to say so.

IMPORTANCE: low -- japes has no consumer for `spread_template*.yaml`; I grepped `jazzx_sdk/` and only `finance/workbook.py:172` mentions a template loader, in a docstring. The renderer is jaci's `commercial_lending/template.py`, so the reachable path is the authoring copy, not this bundled snapshot.

## [DEFECT] `tests/test_plato_packs_api.py:563`
`test_which_seed_pack_files_name_consumer_modules_is_pinned` asserts an exact set of files naming the consumer, but detects them with `"jaci." in text` — which misses `jaci/` and `jaci's`. Its own docstring says a guard that reads a fraction of the tree "is worse than none," and this reads a fraction of the spellings.

Trigger: `grep -rlI jaci plato/data/seed_packs` returns 9 files; the pinned dict lists 6. The three invisible ones are `ci-spread-core/metrics.yaml` ("jaci's own MetricResult shape"), `ci-spread-core/pack_manifest.yaml` ("named jaci symbols") and — new in this diff — `ci-spread-core/policies/core.yaml:2` ("see jaci/scripts/extract_policy.py"). The assertion passes while the record it exists to keep honest is incomplete.

IMPORTANCE: low -- the guard is the only enforcement; the three unpinned references are prose, so the concrete cost today is one confusing record rather than a load failure (`test_a_bundled_seed_pack_is_loadable_in_a_japes_only_process` holds the real line, and both bundled packs load — I ran them). Nothing outside this repo reads the pin.

## [SYMMETRY] `plato/packs_api.py:135`
`_refuse_unless_relaxed` hand-rolls "read `strict_mode()`, raise 403 naming the posture" as a router-local closure, next to `plato/posture.py`, which already owns exactly this family for the read side (`withholding`, `withheld`) and for a mutating-request precondition (`require_if_match`). `config_api.py:267` is the second copy of the write-side rule with a different admission (auth is enough there). A `relaxed_only(what, *, strict)` beside `withheld` is smaller than the closure it replaces and puts both write gates where the read gate already is, making the deliberate difference between them a parameter rather than two independently drifting bodies.

IMPORTANCE: low -- two call sites (`initialize`, `delete`), both inside this router; `config_api`'s gate would only move if its differing rule were expressed as an argument. No external consumer imports either.

## [NOTE] `plato/data/seed_packs/ci-spread-core/metrics.yaml:11`
The header states that what stays in Python is "behavior, not knowledge," naming three specific behaviours. But seven `metric:` ids referenced by `spread_template.yaml` are absent from this catalog's 16 entries — `da` (:36), `stock_based_compensation` (:38), `net_leverage_x` (:194), `dso_days`/`dio_days`/`dpo_days`/`ccc_days` (:202-208) — and resolve only against jaci's `analytics.py::compute_metrics` (verified: `src/jaci/capabilities/commercial_lending/analytics.py:207`). Those are definitions, not behaviour, so the catalog is narrower than the sentence claims.

IMPORTANCE: low -- no japes-side renderer resolves `metric:`, so nothing here breaks; the accepted-gap TODO covers the plato-only case (see CHECKED).

## [NOTE] `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:54`
The rule-shape table says the 14 PPP rules are "8 plain `expression`, 6 `all_of`"; the file is 5 plain and 9 `all_of` (counted from the YAML: `DSCR-PPP-IL-1/-IL-2/-MS/-NJ/-OH/-PA/-RI/-VA/-VT`). Line 75's "6 of the 14 rules" then lists nine rule names in the same sentence, and §3's "10 of the 41 rules use `all_of`/`any_of` … the nine state prepayment-penalty rules" is the correct count — so the notes contradict themselves twice over.

IMPORTANCE: low -- documentation inside a bundled pack; totals (41 rules, 14 PPP) are right, only the breakdown is wrong.

## [NOTE] `plato/data/seed_packs/ci-spread-core/policies/core.yaml:79`
`CI-LEVERAGE-WARNING`'s description says "Leverage above 3.0x but below 3.5x" but the condition is a single `leverage_x <= 3.0`, so it is VIOLATED at 4.0x too — the lower bound of the stated band has no predicate. `conventions.yaml:36` (`CI-FCCR-WARNING`, "FCCR between 1.15x and 1.25x") is the same shape, VIOLATED at 1.0x. Both are new in this diff and describe a range the IR does not encode.

IMPORTANCE: low -- behaviour is a defensible superset (the floor rule fires alongside), so the cost is the `required` text a caller renders from `_describe_condition`; I checked both twins and no third instance exists in these two files.

CHECKED: `TODO(layout-key-resolution)` in `jazzx_sdk/finance/workbook.py` claims a shipped layout falsely advertises load-time key validation, and `workbook_layout.yaml:8` is that claim. The deferral holds and the author already named it — and I verified the consequence is not live: every `key` in both `workbook_layout.yaml` and `workbook_layout_rb.yaml` resolves against `chart_of_accounts.yaml` (84 lines) or `metrics.yaml` (16 metrics), so no blank labelled rows today. `TODO(seed-pack-vendoring)` in `plato/seed.py` also holds: the policy and profile corpora it says do resolve, do — both packs load, `policy_registry` yields 6 ids for ci-spread-core and the dscr profile resolves `max_cltv_grid`, and `find_profile_literal_drift` reports zero drift across 9 declared twins.

PARKED: `tests/test_plato_info_api.py:622` (`TODO(protocol-relative)`).

Grid completeness verified independently of jaci's `test_grid_is_fully_authored`: all five matrix tables are exactly authored — 99/99, 33/33, 12/12, 3/3, 3/3 — no missing cell to raise `KeyError`, no extras. `tests/test_plato_packs_api.py` + `tests/test_plato_info_api.py` (143 passed, 1 skipped) and the migration/pack-store selection (159 passed, 1 skipped) are green, and migration 0004 matches 0003's `_schema`/index-naming convention.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Reviewed the diff against the working tree at `c7a20e3` (all four new test files pass; `ruff` clean on the changed modules).

**CHECKED:**
- `plato/posture.py:96` `TODO(if-match-post)` — claim holds. `require_if_match` is referenced only in `plato/posture.py` and `tests/test_plato_preconditions.py`; no router mounts it, so `POST {prefix}/packs/initialize` cannot 428 today.
- `plato/wiring_default.py:646` `TODO(azure-blob-container-singleton)` — claim holds. `jazzx_sdk/fabric/fabric.py:198` does construct a second `BlobStore`, so the note is right that this is not unreachable.
- `TODO(seed-pack-vendoring)` and `TODO(local-wiring-build-test)` both describe what they defer accurately.

---

**[DEFECT]** `plato/tenancy.py:119` — the docstring at `:86` promises "`HTTPException(400)` naming what is missing", but the `if not found` branch answers `"could not resolve a tenant for this request"`, which names nothing, and nothing is logged (the two `raise ... from None` above also drop the cause). Trigger: a resolver written as `lambda r: r.headers.get("X-Tenant-Id")` — the shape one keystroke away from the `request.headers["X-Tenant-Id"]` case the diff added `test_a_resolver_that_looks_the_header_up_directly_still_answers_400` for — returns `None` on an unlabelled request and the operator on the packs page gets a 400 body with no header name. Same input, same posture, worse answer than the `KeyError` path the helper was just extended to cover.

IMPORTANCE: medium -- both mounted callers reach it (`plato/packs_api.py:158`, `plato/assistants_api.py:172`), on every route of both routers, for any deployment whose wiring supplies a `.get`-style resolver. Fails loudly as a 400 and the operator can work around it by supplying the header; nothing outside this repo consumes the message text.

---

**[SYMMETRY]** `plato/wiring_default.py:488` — the deployed `pack_store_for` constructs `DbPackVersionStore(handle.current, blob=pack_blobs, tenant_id=tenant)` with no `cache_root`, while `plato/wiring_local.py:386` passes `cache_root=pack_cache` from `_anchored(env_text("PLATO_LOCAL_PACK_CACHE", ...))`. With `cache_root=None` the store falls back to `Path(tempfile.gettempdir()) / "japes-packs" / <digest[:16]>` (`DbPackVersionStore._cache_dir`) — the shared predictable directory that `materialize`'s own recorded deferral is about trusting an unpacked tree in. So the wiring that runs in a container is the one left on the shared-temp path, and the laptop wiring is the one that isn't. `wiring_default` already owns `PACK_BLOB_DIR_ENV` for the sibling artefact directory; adding `PACK_CACHE_DIR_ENV` beside it is two lines, the same shape.

IMPORTANCE: low -- reachable only through `materialize`, which nothing in the packs page or the `initialize` route calls; I could not find a shipped plato request path that materializes a published pack, so today the difference costs nothing beyond the missing knob. Enumerated: `plato/wiring_default.py`, `plato/wiring_local.py` are the only two `PlatoWiring(` constructions outside tests.

---

**[NOTE]** `plato/seed.py:150` — a version already in the store is reported `"already published"` regardless of whether the bundled bytes still match it. `record.content_digest` is on the row the pre-check just read and `pack_archive(pack_dir)` is one call away, so a re-`initialize` after an image upgrade that changed seed content without a `pack_version` bump (the exact sequence `tests/test_pack_store.py::test_retiring_a_version_does_not_free_its_number` describes) tells the operator "already there" for a version whose content no longer matches the image.

IMPORTANCE: low -- one confusing line on the packs page in a relaxed posture; the immutability rule the tombstone enforces is the intended behaviour, this is only about what the result says.

**[NOTE]** `tests/test_plato_packs_api.py:750` (`test_the_local_wiring_passes_a_pack_store_factory`) — `importlib.reload(wiring_local)` leaves `sys.modules["plato.wiring_local"]` rebound for the rest of the session with `DB_PATH`/`CACHE`/`PACK_BLOBS` frozen to this test's `tmp_path`, which `monkeypatch` deletes on teardown. Nothing else in the suite imports the module today, so it passes; a later test that does would read paths under a removed directory.

IMPORTANCE: low -- test-only, no shipped-code defect behind it; the existing `TODO(local-wiring-build-test)` on the same test is the right place for it.

VERDICT: BLOCK
