# adversarial review -- japes

- when: 2026-09-16T04:23:37Z
- range: c1c5701..HEAD (since the last review) (1289f9a)
- diff: 206 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/api/info.py :: strict-override-not-plumbed-to-pages -- FIXED. Ran both halves: `create_packs_router(..., strict=True)` on `JAPES_ENVIRONMENT=local` now renders `RELAXED_WRITES = "false"` while `POST /packs/initialize` 403s, and `create_info_router(migrate_strict=True)` beside `create_database_router(strict=True)` hides Migrate with the route at 403; the reverse (`strict=False` on `production`) shows the button and the route admits (503, no db wired, not 403).
WAS: tests/test_plato_info_api.py :: unset-tier-row-missing -- FIXED (`({}, True)` and `({"ENVIRONMENT": ""}, True)` added; the row's `delenv` loop covers all four `_POSTURE_ENV_VARS` plus `JAPES_STRICTNESS`, so the empty row is genuinely unset rather than inheriting the shell).
WAS: plato/boot/posture.py :: three-conditions-claim-narrower -- FIXED (the docstring now states the short-circuit and the forwarding obligation; matches `relaxed_only`'s `unrecognised = ... if strict is None else []`).

[SYMMETRY] tests/test_plato_info_api.py:701 :: degraded-nav-tuple-restated -- the test names the degraded replica but restates `_info_only_app`'s local `degraded_nav` literal instead of exercising the app, where every other degraded assertion in this file builds the real one.
`TestClient(_info_only_app("server")).get("/api/v1/info/ui")` is the idiom at lines 984, 1142, 1163, 1414, 1429, 1447, 1497 and 1521 -- including `test_the_degraded_nav_offers_only_what_it_mounts` (1406), the nav twin of this test -- and it is one line shorter than the local `page(**kwargs)` factory. I ran it: the real degraded app renders `RELAXED_WRITES = "false"` and `POST /api/v1/database/migrate` answers 404, so the assertion holds against the shipped object. As written, dropping `"database"` from `plato/__main__.py:307` leaves this test green.
IMPORTANCE: low -- test-only, and the drift is not fully uncovered: line 1406 would fail on that same edit because the nav would start offering `database`. Nothing outside this repo reads either test.

[NOTE] plato/app.py:156 :: relaxed-writes-strict-unsettable -- `relaxed_writes_strict` is a local bound to `None` and `create_plato_app` takes no parameter that could set it, so all three forwards (info, database, packs) are no-ops in every shipped wiring; the comment above it reads as a knob operators or callers can move.
IMPORTANCE: low -- `plato/app.py` and `plato/__main__.py:309` are the only in-repo callers and neither can pass a value; the plumbing that matters is at the router factories, which this diff fixed. Behaviour is unchanged and consistent either way.

[NOTE] plato/api/info.py:456 :: omit-nav-overloaded-for-mount -- `"database" not in omit_nav` uses a nav-label list as the proxy for "the database router is mounted", so a caller that mounts the router and merely drops the `database` JSON link from the nav loses the Migrate button.
IMPORTANCE: low -- fail-safe direction (a hidden working control, not a 404ing one), and the only two callers are `plato/app.py:158` (no `omit_nav`) and `_info_only_app` (omits it and mounts no database router).

[NOTE] tests/test_plato_info_api.py:686 :: details-page-test-reads-ambient-tier -- the new details-page test does no `delenv`, so its first assertion (`RELAXED_WRITES = "true"`) fails on any shell with `ENVIRONMENT=production` or `JAPES_STRICTNESS=strict` set, reporting a posture problem as a mount problem; the sibling test added in the same diff pins the tier explicitly.
IMPORTANCE: low -- test-only, and it matches the pre-existing `test_the_posture_reaches_the_pages_that_gate_on_it` at line 704, which has the same ambient dependence; no shipped code affected. Suite passes here: 198 passed, 1 skipped.

VERDICT: PASS
