# adversarial review -- japes

- when: 2026-09-10T17:45:21Z
- range: origin/v2.5.1...HEAD (78a965c)
- diff: 10946 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
One note before the findings: the `finance/validation.py` hunk in the diff is stale — the tree already has the `if not refusals and ...warnings` fallback and a docstring that matches it, so the `warnings`-never-converted finding I had is already fixed. Everything below was re-verified against the working tree.

**CHECKED**
- `TODO(threshold-grid-provenance)` (profiles.py:229): holds exactly. `dscr_profile.yaml` has 7 `thresholds` (all bare strings) + 150 table cells = the 157 the note claims, `looks_authored` is False for every one, so the gate covers none. It is the only `PolicyProfile` YAML in the repo.
- `TODO(activation-vs-active-rules)` (policy.py:757): holds. `executable_rules`/`activation_blocks` have no non-test callers; `check_compliance` still goes through `active_rules()`.
- `TODO(publish-tick-tie)` (store_db.py:61): the revised claim holds — `publish` does have callers now (`plato/seed.py` imports `PackVersionExists`), and `DbPackVersionStore` is the only `PackVersionStore` subclass, so the new abstract `delete` breaks no other implementation.

---

**[DEFECT] `jazzx_sdk/fabric/canonical/profiles.py:139`** — `looks_authored` keys on the presence of `provenance`/`status` alone, so any unrelated nested mapping in `custom` that happens to carry a `status` key is treated as a claimed `Threshold`, fails `Threshold(**found)` (extra=forbid / non-lifecycle status), is left as a dict, and is then refused by `get`. Profile data that used to resolve now raises — which is the failure `_coerce_threshold_mappings`' own docstring (line 157) gives as its reason for leaving `{value: N}` alone.

Trigger, run against the tree: `PolicyProfile(profile_id="p", custom={"ops_flags": {"status": "enabled", "region": "west"}}).get("ops_flags")` → `ThresholdNotActivated: ... declares a provenance or status and is not a readable Threshold: ['region', 'status']`, where it previously returned the dict.

IMPORTANCE: low -- no shipped configuration reaches it. I enumerated every `PolicyProfile` in the repo: one YAML (`plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml`, 7 flat thresholds, 11 flat `custom` scalars/lists, no nested mapping in either bucket) and the constructions in `tests/test_policy_lint.py` / `tests/test_policy_threshold_provenance.py`, all flat. Reaches only an out-of-repo pack profile, and fails loudly when it does. A `TODO` on `looks_authored` is a complete response.

---

**[SYMMETRY] `jazzx_sdk/experts/policy/default.py:665`** — `staleness_reasons[policy_id] = policy.not_in_force() or (...status sentence...)` picks one cause with `or`, where the sibling this file's own new activation code establishes, `Rule.activation_block` (`policy.py:540`), deliberately names both ("both are named when both block"). A `Policy(status=DRAFT, effective_date=<future>)` is classified `stale` and reported as `"policy 'x' is not effective until ..."`, never mentioning that it is a draft — and `detect_staleness`' comment on the line above claims "The policy's own sentence either way". Lining up with `activation_block`'s join is no more code than the `or`.

IMPORTANCE: low -- `staleness_reasons` is display text; the stale/not-yet-effective classification beside it is right. Reached by `detect_staleness` and `detect_stale_pinned_policies`; nothing in-repo branches on the reason string.

**[SYMMETRY] `jazzx_sdk/pack/__init__.py:37`** — the facade re-exports `PackNotFound` and `is_assistant_pack` from `pack.store`, but the two symbols added beside them, `PackVersionExists` and `is_assistant_manifest`, are only in `store.__all__`. `plato/seed.py:27` consequently imports both from `jazzx_sdk.pack.store` while its neighbours come through `jazzx_sdk.pack`. Two lines in the import block and two in `__all__`.

IMPORTANCE: low -- import path only; `plato/seed.py` is the sole consumer and works today.

---

**[NOTE] `plato/data/seed_packs/ci-spread-core/`** — the new seed pack carries the consumer repo's identity in data that ships in the wheel (`packages = [{include = "plato"}]`): `ontology/ci_lending.yaml` names `jaci.scenarios.ci_spread.schemas.*` and `jaci.capabilities.commercial_lending.analytics.compute_metrics` in `source_schema`, `breakouts.yaml` names `jaci...breakouts.BreakoutRule`, `metrics.yaml` and `chart_of_accounts.yaml` describe another repo's `dsl_catalog.py`/`analytics.py`/`template.py` internals, and `pack_manifest.yaml:135` pins `source_doc: "docs/LoanSamples/YETI/Yeti Credit Package/..."`, a path that resolves in the authoring checkout only and puts a client name in shipped data.
IMPORTANCE: low -- inert provenance strings; `tests/test_plato_packs.py` loads both seed packs green, so nothing resolves them.

**[NOTE] `jazzx_sdk/fabric/canonical/policy.py:790`** — `is_not_yet_effective`'s `if self.effective_date is None: return False` is unreachable: `Policy.effective_date` is required (`policy.py:677`). Same for the `effective_date` arm of `not_in_force`'s `if bound is None: continue`, which only the `expiry_date` arm needs.
IMPORTANCE: low -- dead guard, no behavioural effect.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Reviewed against the repo (loaded both seed packs, ran the evaluator, checked the store/migration/nav wiring, ran `tests/test_plato_packs_api.py tests/test_plato_packs.py tests/test_plato_info_api.py tests/test_pack_store.py` — 179 passed).

## CHECKED

- `TODO(layout-key-resolution)` / `TODO(layout-live-formula-order)` (`jazzx_sdk/finance/workbook.py`) — both hold for the two layouts this diff adds. Every `key` in `workbook_layout.yaml` and `workbook_layout_rb.yaml` resolves against this pack's own `chart_of_accounts.yaml` (84 keys) or `metrics.yaml` (16); both Inputs sheets precede their `live_formula` twin. The layout header's claim that this is a "load-time acceptance check" is false, but that is exactly what the TODO already says.
- `TODO(seed-pack-vendoring)` (`plato/seed.py`) — holds. The `jaci.capabilities.*` references in `reclassifications.yaml`, `workbook_layout*.yaml` and `spread_template_rb.yaml` are inert: `ci-spread-core/pack_manifest.yaml` declares none of those files, so nothing in plato resolves them.
- `TODO(omit-info)` (`plato/info_api.py:83`) — holds. `degraded_nav` is `("Guide","config","database","Packs","packs")`; no caller passes `"info"`.
- `TODO(if-match-post)` (`plato/posture.py`) — the POST it anticipated now exists, and the TODO says so; nothing mounts `require_if_match`, so nothing 428s.

## Findings

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:662` — `DSCR-PPP-IL-2`'s `all_of` applicability includes `interest_rate > 8`, and `interest_rate` defaults to `None` (ENCODING_NOTES:67). I confirmed `ExpressionEvaluator` returns `INDETERMINATE` for `None` and for a missing field, `_evaluate_composite`'s algebra propagates that through `all_of`, and `DefaultPolicyExpert` (`jazzx_sdk/experts/policy/default.py:484-487`) `continue`s on any applicability that is not `SATISFIED` — with no `not_activated` entry and no data-gap record. So the rule is silently dead, not deferred.

Trigger: an Illinois loan, `borrower_entity_type=individual`, `loan_amount=500000`, rate 9%, with `interest_rate` left at its default → `NOT_APPLICABLE`, compliance reads clean, instead of `VIOLATED`. ENCODING_NOTES §1a states this exact reasoning for the eight condo/entity fields ("a field left unset (`None`) would silently never block, which is worse than a wrong default for an affirmative attestation") and §1b does not apply it to the one new field that is read by an ordering operator.

`IMPORTANCE: medium` — nothing in this repo evaluates the corpus; I loaded both bundled packs and `dscr_core`'s registry builds (1 policy, 41 rules, profile `dscr_v6_15_2026_v1_1`), but plato only stores and serves it. The evaluating consumer is jaci, whose `LoanApplication` default I could not read from here — ENCODING_NOTES:67 asserts it. §1b also says none of the 5 existing gold cases/fixtures were updated, so all of them currently have this rule dead.

**[NOTE]** `plato/packs_api.py:41` (and `plato/static/packs.html:160`) — "versions newest-first" is `published_at` order, not version order. `store.versions()` is `published_at ASC` and this reverses it, so a backport (0.2.0 published, then 0.1.1) lists 0.1.1 first. The adjacent comment at `packs_api.py:73-77` makes precisely this distinction for `last_published_version`; the docstring one line above it does not.

`IMPORTANCE: low` — one confusing row order on an operator page. Reached by `GET {prefix}/packs` and the page; no consumer outside this repo reads `pack_inventory`.

**[NOTE]** `plato/packs_api.py:58-62` — the `try` around `sorted(latest, key=lambda r: r.pack_id)` is a handler for a case its own comment says "cannot actually trip it" (`PackVersionRecord` is frozen with every field defaulted, and `pack_id` is a non-null primary-key column). The two guards below it are load-bearing; this one has no reachable trigger.

`IMPORTANCE: low` — dead code only; no behaviour change either way.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(if-match-post)` (`plato/posture.py:96`) — holds. `require_if_match` is referenced only by `tests/test_plato_preconditions.py`; no router in `plato/` mounts it, so `POST .../packs/initialize` cannot 428 today.
- `TODO(azure-blob-container-singleton)` (`plato/wiring_default.py:645`) — holds, and the note is accurate rather than conservative: `AsyncAzureBlobStorageProvider.get_instance` returns `cls._instance` before looking at `container_name` (`common/core/storage/async_azure_provider.py:54`), and `jazzx_sdk/fabric/fabric.py:198` is the second `BlobStore` construction it names.
- `TODO(local-wiring-build-test)` (`tests/test_plato_packs_api.py`) — holds. `wiring_local.build()` shells out to `alembic upgrade head` with a 180s timeout and exits on a missing profile checkout; a source-text assertion is the honest substitute, and `env_text`/`_anchored` do exist in that module (verified), so the `NameError` it cannot catch is not present.

## Findings

**[DEFECT]** `tests/test_plato_packs_api.py:29` — the `clean_env` fixture strips only `JAPES_ENVIRONMENT`/`JAPES_STRICTNESS`, but eight tests assert facts about the *bundled* packs while calling `default_seed_root()`, which returns `PLATO_SEED_PACKS_DIR` when set — the override `plato/seed.py`'s own docstring documents as "how a developer seeds the packs they are actually editing."
Trigger, measured: `PLATO_SEED_PACKS_DIR=/tmp/seedcheck python -m pytest tests/test_plato_packs_api.py tests/test_policy_threshold_provenance.py` → 8 failures (`test_every_bundled_seed_pack_loads`, `test_the_dscr_seed_pack_can_resolve_its_own_profile_references`, both `test_a_bundled_seed_pack_is_loadable_in_a_japes_only_process` params, `test_the_ci_spread_seed_pack_builds_its_registry_from_its_manifest`, `test_which_seed_pack_files_name_consumer_modules_is_pinned`, `test_reinitializing_after_a_retire_reports_skipped_not_failed`, and `test_the_shipped_profile_claims_no_provenance_it_was_not_given` at `test_policy_threshold_provenance.py:110` with a `FileNotFoundError`). Unset, all 99 pass. `_POSTURE_KEYS` gaining `PLATO_SEED_PACKS_DIR` covers six of them; `test_which_seed_pack_files_name_consumer_modules_is_pinned:538` and the provenance test take no fixture at all.
`IMPORTANCE: low -- suite only; I enumerated the two new test files and the shipped `default_seed_root` callers (`plato/packs_api.py` initialize route, `plato/seed.py`), and no shipped path is affected. It fails loudly, but the pinned-file guard is the kind a red run invites someone to re-pin against the wrong tree.`

**[DEFECT]** `plato/tenancy.py:112` and `:117` — both served details call `reason(exc)` with no `limit`, while the comment at `:114-116` claims this is "the redact-and-collapse every other served-text path in plato goes through." `reason` only truncates when asked (`jazzx_sdk/failures.py:288`), and its docstring says to pass `SERVED_REASON_LIMIT` wherever the result is served; every other plato site does (`oidc.py:320,375,395,427`, `database_api.py:65,111`, `info_api.py:329,339`, `schema_version.py:145`, `wiring_default.py:214`). It also embeds the exception class name, so the shipped deployed resolver's 400 reads `ValueError: X-Tenant-Id is required...`.
Trigger: a consumer `tenant_from_request` — the extension point this docstring is explicitly written for — raising `ValueError(str(some_payload))` puts the whole message, uncapped, in a 400 body for an unauthenticated caller.
`IMPORTANCE: low -- both in-repo resolvers (wiring_default.py:502, wiring_local.py:393) raise short fixed strings, so no shipped configuration exceeds the cap today; reachable only through the two routers that call tenant_of (packs_api.py:158, assistants_api.py:172) with a consumer-supplied resolver.`

**[SYMMETRY]** `plato/seed.py:60` — `pack_archive` encodes the *store's* archive format (flat entries, manifest at the archive root per `MANIFEST_NAMES`) in a caller. That format is owned by `jazzx_sdk/pack/store.py` (`MANIFEST_NAMES`) and `store_db.py:463` (`_unpack`), and the same directory-to-zip logic is hand-rolled twice more in tests (`tests/test_pack_store.py::_archive`, `tests/test_plato_packs_api.py::_archive`). Moving the function next to `_unpack` is the same code in a different module, and puts the writer beside the reader that has to agree with it.
`IMPORTANCE: low -- one shipped caller (the initialize route) plus two test helpers; nothing outside this repo builds pack archives yet.`

**[NOTE]** `plato/seed.py:95` — `_manifest_of` returns the first *parseable dict* it finds, so a `pack_manifest.yaml` that parses but carries no `pack_id` shadows a complete `manifest.yaml` beside it and the directory is bucketed `not_packs`. That is the same shadowing `test_an_unparseable_manifest_does_not_hide_a_readable_sibling` fixes, one input over (the `no-identity` fixture only ever has one manifest name).
`IMPORTANCE: low -- needs a pack directory carrying both names with the higher-precedence one incomplete; I found none in plato/data/seed_packs.`

**[NOTE]** `plato/wiring_default.py:620` — the "not a durable pack-archive backend" warning is gated on `posture` being truthy as well as `backend != "local"`, so a typo (`PLATO_PACK_BLOB_BACKEND=s3`, `Azure`) in a local posture writes to local storage with nothing said. The durability half of the warning is rightly posture-gated; the typo half is not about durability.
`IMPORTANCE: low -- local posture only; the parametrized test covers the staging path.`

**[NOTE]** `plato/seed.py:28` — second module to import the private `plato.settings_store._actor` (`plato/packs_api.py` is the first). Two importers is the point at which the leading underscore is no longer true.
`IMPORTANCE: low -- two call sites, both in plato.`

VERDICT: BLOCK
