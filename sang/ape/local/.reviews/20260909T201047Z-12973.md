# adversarial review -- japes

- when: 2026-09-09T20:10:47Z
- range: 723cdd1..HEAD (since the last review) (9dbebcc)
- diff: 140 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` `published_at.desc()` tie — CHECKED (`TODO(publish-tick-tie)` at `pack/store_db.py:58` unchanged; claim still holds, not re-raised).
- WAS: `store_db.py:312` marker short-circuit — STILL OPEN; `:308-312` still returns the marker file's text verbatim, no containment against `root`.
- WAS: `store_db.py:181` publish never opens `archive` — STILL OPEN; `:181-183` still content-keys and `put`s arbitrary bytes, first failure is `_unpack` at materialize.
- WAS: `pack/__init__.py:29` `__all__` — STILL OPEN; `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent from the export list at `:34-45`.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` — STILL OPEN; untouched.
- WAS: `store.py:54` "sha256 over the archived asset bytes" — STILL OPEN; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race — STILL OPEN; blob written before the row, no unique-violation handling.
- WAS: `store_db.py:140` `visibility` unvalidated — STILL OPEN; string goes straight into the row at `:196`.
- WAS: `store_db.py:364` `_unpack` docstring summary — STILL OPEN; `:364` still says "the directory holding ``pack_manifest.yaml``" while the body accepts either `_MANIFEST_NAMES` entry.
- WAS: `roots.py:99` candidate tuple duplicates `manifest_loader.py:93-96` — STILL OPEN; `roots.py:130` and `manifest_loader.py:103-105` both spell all three variants.
- WAS: `loader.py:214` / `:196` / `:236`, `tests/test_pack_store.py:*`, `roots.py:100` join, `roots.py:81` `env_text`, `roots.py:57` `.exists()`, `test_pack_roots.py` discovery success — FIXED (unchanged this round).
- WAS: `[DEFECT] wiring_local.py:122` cwd fallback in a checkout — FIXED. Measured: `cd /tmp && PYTHONPATH=/Users/sangit/src/japes python -c "from plato.wiring_local import _find_root; print(_find_root())"` → `/Users/sangit/src/japes`. New assertion at `test_plato_wiring.py:174` fails against the old code.
- WAS: `[DEFECT] roots.py:112` summary promised only `PackRootNotFoundError` — FIXED; `:118-124` now states both types.
- WAS: `[SYMMETRY] manifest_loader.py:92-96` — FIXED; `bare_name` at `:101`, parametrized test green.
- WAS: `[NOTE] roots.py:61` directory named `pyproject.toml` — FIXED; `is_file` at `:66`, test at `test_pack_roots.py:149`.
- WAS: `[NOTE] roots.py:37` trailing-`/` undocumented in `discover_root` — FIXED; `:70-73`.

## 2. New findings

`ruff` clean on all five files; 78 tests pass across `test_pack_roots`, `test_plato_wiring`, `test_pack_composition`, `test_pack_manifest_loader`, `test_pack_store`.

**[SYMMETRY] `jazzx_sdk/modes/prompt_loader.py:70`** — the third and fourth members of the same family still join `pack_id` unchecked: `prompts_dir / pack_id / f"{mode_name}.md"` at `:70` and `Path(packs_root) / pack_id / "mode_tuning" / f"{mode_name}.md"` at `:128`. Unlike `resolve_pack_root`, which returns a path, both `.exists()`-then-`read_text()`, so `pack_id="../../../etc"` with a matching `<mode_name>.md` returns arbitrary file contents as a system prompt. The fix is the same one line the diff just applied twice: `bare_name(pack_id, what="pack id")`.
IMPORTANCE: unknown — call chain is `make_prompt_resolver` (`:134`) → `modes/base.py:189` → `self.pack_id`. I could not establish whether any consumer sets a mode's `pack_id` from a request; in-repo it comes from manifests. If it is always config-derived this is low; if any route selects a pack by id it is high, because the payload is a prompt the model then follows.

**[NOTE] `jazzx_sdk/pack/manifest_loader.py:97`** — the new docstring names "`Pack.from_pack_id`" as the shipped caller, but no such method exists; it is `Pack.from_manifest` (`jazzx_sdk/pack/pack.py:59`), and `grep -rn "from_manifest(" --include='*.py'` finds only test call sites in this repo, so "shipped caller" is a claim about out-of-repo consumers stated as in-repo fact. `tests/test_pack_roots.py:163` repeats the same wrong name.
IMPORTANCE: low — a reader greps for a symbol that isn't there; no behaviour depends on it.

**[NOTE] `tests/test_pack_roots.py:158`** — the docstring says `from_pack_id` "refuses the same ids", but the param list is `["", "..", "../secret", "a/b"]` against `test_a_pack_id_is_a_name_not_a_path`'s `["", ".", "..", "../secret", "a/b", "/etc/passwd"]` at `:136`. The two dropped cases are the ones with the distinct mechanisms: `"."` collapses to the base directory, and `/etc/passwd` discards `packs_root` entirely under `/`. Both are in fact refused by `bare_name`, so this is coverage, not a live gap.
IMPORTANCE: low — test-only; naming the same six ids in both lists is the whole change.

**[NOTE] `plato/wiring_local.py:124`** — `markers=("pyproject.toml",)` reinstates exactly what `DEFAULT_MARKERS`' own comment at `roots.py:36-37` warns against ("a `pyproject.toml` alone can sit in a nested package"), and the docstring at `:112` still claims "a discovered root exists only in a source or editable checkout" — a wheel installed into a venv nested under any project with a `pyproject.toml` now discovers that project. Not reachable in the shipped container: `Dockerfile:81-99` sets `WORKDIR /app` and copies `jazzx_eval_contracts/jazzx_eval_contracts/` (the package, not the project root), so `/app` has no `pyproject.toml` and `_find_root()` is still cwd. Not reachable in-repo either: the nested `pyproject.toml` files are `common/` and `jazzx_eval_contracts/`, neither an ancestor of `plato/wiring_local.py`.
IMPORTANCE: low — I enumerated every `pyproject.toml` in the tree and read the Dockerfile's install layout; no shipped configuration reaches it.

VERDICT: PASS
