# adversarial review -- japes

- when: 2026-09-08T19:45:11Z
- range: 67b1bea..HEAD (since the last review) (23a555e)
- diff: 48 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Step 1 — prior round

WAS: `wiring_default.py:503` `TODO(swap-schema-placement)` — CHECKED last round, unchanged in the tree; not reopened.

WAS: `tests/test_plato_wiring_default.py:443-445` "keeps the retired store referenced" comment — **FIXED**; both the test comment (`:447-449`) and the docstring (`:493-497`) now say it is never disposed.

WAS: `tests/test_plato_wiring_default.py:411` sibling test disposes only `current` — **FIXED**; `:405` captures `retired`, `:417` disposes it.

## Step 2 — new findings

No defects.

**[NOTE]** `tests/test_plato_wiring_default.py:412-417` — the sibling fix landed with the opposite ordering: here both disposals sit *after* the `assert` at `:412`, so a failing assert leaks both stores and produces the `Event loop is closed` noise on an unrelated test, which is exactly what the twin (`:459-461`, disposals before the assert) was shaped to avoid. Trigger: `resolve_degraded` stops reporting sqlite and the assert fails. Enumerated all three swap tests in this file plus the two in `test_plato_settings_store.py:151,196`; only shipped path into `swap_database` is `plato/app.py:184`, unaffected.
IMPORTANCE: low -- test-only, and the assert has to fail first; a comment or a `TODO` on `:412` is a complete response.

**[NOTE]** `plato/wiring_default.py:494-496` — the docstring now narrates its own edit history ("not kept referenced, which is what this said before"), so a reader of the shipped source is being corrected about a revision they never saw; the statement that survives is the `del previous` / in-flight-reference explanation.
IMPORTANCE: low -- docstring only, no behaviour; `swap_database` is exported from `plato.wiring_default` and read by anyone tracing `/v1/config`.

VERDICT: PASS
