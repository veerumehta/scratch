# adversarial review -- .

- when: 2026-09-25T04:23:55Z
- range: HEAD~1...HEAD (050ca9c9)
- diff: 403 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[DEFECT] jazzx_sdk/server/vocabulary_review.py:210 :: review-router-not-tenant-scoped -- this diff removes `TenantScopedProposalStore` and makes the extraction service hand `tenant_id` to `components_for`, so each tenant's proposals now go to that tenant's own store. `create_vocabulary_review_router` (along with the proposal and comment routes) still takes one fixed `store` and one `comments`, and has no `tenant_of`. The review side of the same authoring flow cannot reach any tenant's proposals except the one it was built with.
Trigger: a host mounts `create_policy_extract_router(..., tenant_of=header)` with `components_for` returning `store_for(tenant_id)`, plus `create_proposal_review_router(store_for("acme"))`. A `globex` submit writes to globex's store, and globex reviewers `GET /review/{pack}/proposals` against acme's store, so they see an empty queue (or acme's rows). Comments behave the same way through the single `DbProposalCommentStore(tenant_id=...)`. `test_documents_become_rules_...` hides this because `tenant_of` is the constant `"acme"`, and the review router gets `stores["acme"]` directly.
IMPORTANCE: medium -- no router is wired in this repo: grep found no `create_policy_extract_router`, `create_*_review_router` or `DbProposalStore(` call site in `plato/`, `jazzx_sdk/`, jaci or policy-workbench outside tests. The first multi-tenant host gets a silently empty review queue. Before this diff, the removed wrapper at least let one tenant scope both sides the same way.

[NOTE] jazzx_sdk/fabric/graph/proposal_store_db.py:40 :: table-rename-orphans-rows -- this diff renames `__tablename__` from `vocabulary_proposal` to `proposal` and adds `tenant_id` to the primary key, with no migration. `vocabulary_proposal` is already on origin/dev, origin/plato and origin/v2.5.1. A host that ran `DbProposalStore` on those branches would come up on an empty `proposal` table (`create_all` never alters or copies), and its pending queue would be gone without any error.
IMPORTANCE: low -- `DbProposalStore(` has no construction site in `plato/`, `jazzx_sdk/`, jaci or policy-workbench, only `tests/test_proposal_store.py:58`. I found no consumer holding rows. If one exists outside these checkouts, this becomes high.

[NOTE] jazzx_sdk/server/policy_extract_api.py:95 :: pack-for-hint-stale -- `pack_for` is still annotated `Callable[[str], Any]`, but line 144 now calls it with `(pack_id, tenant_id)`. A shell that follows the annotation and writes a one-argument `pack_for` gets a `TypeError`, which lands inside the run's `try` and turns into a failed run instead of an error at construction.
IMPORTANCE: low -- no `pack_for=` caller exists in this repo, jaci or policy-workbench, and the error is visible on the run record.

VERDICT: BLOCK
