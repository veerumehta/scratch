# adversarial review -- japes

- when: 2026-09-10T03:04:39Z
- range: origin/v2.5.1...HEAD (2e285a4)
- diff: 1391 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `profiles.py:200` `TODO(threshold-grid-provenance)` — the claim holds. `_coerce_threshold_mappings` is registered on `thresholds`/`custom` only, `tables` has no validator and `get_table` has no status branch, and a cell is returned exactly as before. Not raised.

---

**[DEFECT]** `jazzx_sdk/experts/policy/default.py:418` — the applicability gate in the same function still skips *every* non-SATISFIED gate outcome, so a rule whose `applicability` reads an unapproved threshold is dropped silently, which is the exact failure the `condition` branch at :433 was added to prevent. Verified: a `Rule` with `applicability=RatioCondition(threshold="profile:dscr_min")` against `PolicyProfile(thresholds={"dscr_min": Threshold(value="1.20", provenance=SECONDARY_SOURCE)})` returns `allowed=True, warnings=[], rationale="All policy gates passed."`. `segment.py:95`'s docstring claim that it "Mirrors `DefaultPolicyExpert.check_compliance` exactly" is now false in the direction that matters — `segment.py:107` got the branch, this did not.
IMPORTANCE: high -- silent allow from the SDK's primary compliance entry point; reached by any pack authoring `Rule.applicability` with a `profile:` ratio/matrix threshold, which is the same authoring shape the new tests exercise on `condition`. Consumers outside this repo (jaci packs) call `check_compliance` directly.

**[DEFECT]** `jazzx_sdk/fabric/canonical/condition_evaluator.py:455` — `reasons[0]` picks by child order, so a composite whose first indeterminate child is `EVIDENCE_MISSING` masks a `POLICY_NOT_ACTIVATED` sibling; `default.py:433` then reads the composite as a data gap and skips it. Verified: `AllOfCondition([Expression(field="absent_field"), RatioCondition(threshold="profile:dscr_min")])` with a seeded `dscr_min` returns `indeterminate_reason=EVIDENCE_MISSING`. The comment calls the pick arbitrary, but it is load-bearing for the allow/withhold decision now; `POLICY_NOT_ACTIVATED` needs to win.
IMPORTANCE: medium -- same silent-allow class as above but requires an `all_of`/`any_of` condition; reachable from `check_compliance` and `run_segment`, both of which dispatch composites through `_evaluate_composite`.

**[DEFECT]** `jazzx_sdk/experts/policy/default.py:478` — when a run has both violations and unapproved rules the rationale reports only the unapproved count, dropping the violation text entirely. Verified with one violated `Expression` rule plus one seeded-threshold ratio rule: `policy_violations=[{"rule_id": "VIOL", ...}]` but `rationale="1 governing rule(s) could not be evaluated because their thresholds are not approved for execution; see warnings."`
IMPORTANCE: medium -- `allowed` is still False so nothing is wrongly approved, but any caller that surfaces `rationale` (rather than iterating `policy_violations`) loses the reason for the denial.

**[DEFECT]** `jazzx_sdk/fabric/canonical/policy_lint.py:38` — `find_profile_literal_drift` reads `profile.custom[key]` raw instead of through `get`/`get_threshold`, so an *approved* `Threshold` in `custom` reports false drift and `assert_no_profile_literal_drift` raises. Verified: `custom={"max_leverage": {"value": 3.0, "status": "active", "provenance": "institution_authored"}}` with an `Expression(value=3.0, domain_extensions={"profile_custom_key": "max_leverage"})` yields `"literal 3.0 diverges from PolicyProfile.custom['max_leverage']=Threshold(value=3.0, ...)"`. The diff deliberately gates `custom` (`test_custom_is_gated_as_well_as_thresholds`), so this is the bucket's other reader left behind.
IMPORTANCE: medium -- fails loudly at lint time, not at evaluation; only reachable for a pack that adopts the wrapped form for a `custom` key that also has an Expression literal twin. `policy_lint` is exported from `fabric.canonical`, so external packs can call it.

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:139` — the `set(found) <= fields` arm silently disables the gate for a mapping that explicitly declares `status: seed`, because one unmodelled key is enough. Verified: `dscr_min: {value: '1.25', provenance: secondary_source, status: seed, edition: '2024'}` → `get("dscr_min")` returns the whole dict unrefused while `get_threshold` raises `ThresholdNotActivated`. The two accessors disagree about the same key, and the one used for arithmetic is the permissive one.
IMPORTANCE: medium -- the served dict blows up downstream (`evaluate_ratio`/`Decimal(str(...))`), so it fails loudly rather than computing a wrong number, but the failure names a type error instead of the unapproved figure; trigger is any extra key in an authored threshold block, which YAML authoring invites.

---

**[NOTE]** `jazzx_sdk/fabric/graph/check.py:18` — the module docstring still defines `INDETERMINATE` as "required data was missing or unresolvable", which the same file's new `_UNTESTED_RATIONALE` (`:227`) now contradicts for three of the four causes.
IMPORTANCE: low -- prose only; the code path at `:249` is correct.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:438` — the `not_activated` warning carries `rule.rule_id` but not `policy_id`, unlike the violation dicts built at `:450`, so two policies in `precedence_order` carrying the same rule id are indistinguishable in `warnings`.
IMPORTANCE: low -- one confusing warning line; `policy_violations` already carries `policy_id` for the violation path.

VERDICT: BLOCK
