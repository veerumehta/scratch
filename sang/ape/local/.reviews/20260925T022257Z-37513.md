# adversarial review -- .

- when: 2026-09-25T02:22:57Z
- range: 8629e3a..HEAD (since the last review) (dc3ecfc)
- diff: 592 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/pack/rule_merge.py :: skipped-description-stale -- FIXED
WAS: jazzx_sdk/pack/rule_merge.py :: skipped-duplicate-remains-mergeable -- STILL OPEN -- the docstring at `rule_merge.py:98-99` now states the behaviour, but the skipped duplicate still comes back ACCEPTED, so running the merge again on `result.proposals` makes the later proposal win without a report.

[DEFECT] jazzx_sdk/experts/policy/default.py:462 :: replaced-rule-drops-its-replaces -- When a rule is itself superseded, the loop hits `continue` before `stands_in` is computed, so the rules it `replaces` are never recorded and come back into force.
Trigger: the shipped `ci-spread-core/policies/overlays/deal_si_rb_example.yaml` reuses the id `RB-CI-LEVERAGE-CEILING`. That skips the RB overlay rule, so its `replaces: [CI-LEVERAGE-CEILING, CI-LEVERAGE-WARNING]` is never registered. For an RB deal at `leverage_x=3.1`, the deal rule passes (≤3.25), but core `CI-LEVERAGE-WARNING` (≤3.0) now evaluates and adds a violation from a rule the program overlay retired. At 3.4 the deal gets two violations for one breach, deal plus `CI-LEVERAGE-WARNING` (3.4 is still under the 3.5 core ceiling). Under the old field precedence the deal rule took `leverage_x` and nothing below it ran. `metadata["superseded"]` lists only `RB-CI-LEVERAGE-CEILING`, so nothing marks the reinstated rules as wrong.
IMPORTANCE: high -- a wrong governance result that callers cannot see. I checked the call sites: japes seed pack `ci-spread-core` and jaci's copy (`jaci/config/packs/ci-spread-core/policies/overlays/deal_si_rb_example.yaml`). jaci passes that file through `deal_policy=` in `src/jaci/scenarios/ci_spread/conductor.py`, and jaci runs editable against this tree.

[DEFECT] jazzx_sdk/experts/policy/default.py:492 :: unevaluable-replacement-silently-drops-base -- Once the replacement's gate passes, the rules it stands in for are marked superseded before its own condition runs. If that condition then comes back EVIDENCE_MISSING, it hits a bare `continue`. The result is no violation, no warning and no refusal, and the base rule never runs.
Trigger: the diff's own shape in `test_a_lower_rule_on_the_same_field_still_evaluates_unless_replaced`, with context `{"noi": 150}` and no `debt_service`. `DSCR-FLOOR` is indeterminate, `NOI-FLOOR` is skipped, and `allowed=True` comes back with no violation or warning. The comment at 492-494 says "A replacement that cannot be evaluated is reported as that". That only holds for POLICY_NOT_ACTIVATED.
IMPORTANCE: medium -- every `replaces` in shipped YAML (japes `conventions.yaml`, jaci CI `conventions.yaml`, jaci CRE `policies.yaml`) names rules on the same field, so there the base rule would be indeterminate too. Only a cross-field `replaces` reaches this, and it is written into this diff's tests.

[NOTE] jazzx_sdk/experts/policy/default.py:470 :: conditionless-gated-rule-replaces-nothing -- A rule with `condition=None` and an `applicability` gate reaches `continue` before its gate is evaluated, so its `replaces` never takes effect. `Rule.replaces` says replaced rules are "skipped wherever this rule applies (its applicability gate passes)". The code only honours that for conditionless rules that have no gate.
Trigger: a `procedure` rule (the kind JTBD extraction produces, `condition=None`) with `applicability` and `replaces` set. The replaced rules always evaluate.
IMPORTANCE: low -- no shipped YAML combines a conditionless rule with `replaces`.

VERDICT: BLOCK
