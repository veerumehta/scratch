# adversarial review -- japes

- when: 2026-09-09T22:14:06Z
- range: HEAD~1...HEAD (31ab385)
- diff: 3666 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `plato/posture.py:100` `TODO(if-match-post)` — holds. `require_if_match` appears only in `plato/posture.py` and `tests/test_plato_preconditions.py`; no router mounts it as a dependency.
- `jazzx_sdk/pack/store_db.py:57` `TODO(publish-tick-tie)` — holds. `PackVersionStore.publish` has no shipped caller (`grep '\.publish('` in `jazzx_sdk`/`plato` hits only `manifest/publish.py` and `streaming/publisher.py`, different objects).
- `plato/database_api.py:88` `TODO(partial-registration-marker)` — holds. `store = getattr(db, "current", db)` yields a real `DbStore` for every shipped caller, and `DbTurnRunStore` is the only member needing `.repository`.
- `plato/wiring_local.py:_find_root` docstring claim "unreachable in the image — `WORKDIR /app` has no `pyproject.toml`" — holds. `Dockerfile:46` copies `pyproject.toml` into the *builder* stage's `/build`; the runtime stage (`Dockerfile:81`) copies only `common/`, `jazzx_sdk/`, `plato/`, `jazzx_eval_contracts/` into `/app`.

---

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:103` — the docstring claims "a *malformed* profile still fails loudly at `from_yaml` instead of being skipped as though it were someone's notes", but the one malformation that removes the discriminator is exactly what gets skipped: `profile_id` is a required field on `PolicyProfile`, so a file that misspells it is filtered out by `is_policy_profile_document` and vanishes. Measured on a pack with `profiles: {dir: profiles}` and one `acme.yaml` whose key is `profile-id:` — `profile_files()` → `[]`, `policy_profiles` → `{}`, `policy_profile()` → `None`, which is the "this pack declares no profile" answer `Pack.policy_profile` (pack.py:214) deliberately reserves for one meaning and raises to avoid conflating everywhere else.
IMPORTANCE: medium — reaches every `dir:`-declared profile through `Pack.policy_profiles`/`policy_profile` (jazzx_sdk/pack/pack.py:161, 214) and `plato/packs/domain.py:domain_pack_summary`; jaci consumes packs via `Pack.from_manifest`, so it is reachable outside this repo. Fails as a `None` rather than a crash, and the caller sees it as an unconfigured pack.

**[DEFECT]** `plato/packs/domain.py:106` — `declared = loader.depends_on()` is an advisory warning call sitting inside the per-row `try`, so a manifest whose `depends_on[].fragments` names an unknown kind drops the whole pack with a "could not be loaded" warning even though the pack is fully readable. Verified: with `fragments: [polices]`, `loader.depends_on()` raises `PackDependencyError`, while `Pack.from_loader(loader)` never calls it (`_can_merge` short-circuits on `self._packs_root is not None`) and `pack_id`/`version`/`policy_registry` all resolve. The sibling `published_registries` does not call `depends_on()`, so the same published row composes as an assistant pack and is silently dropped as a domain pack.
IMPORTANCE: low — no shipped caller: `published_domain_packs` and `domain_pack_summary` are imported only from `tests/test_pack_store.py`, and nothing in `plato` mounts them. A `TODO` on the `depends_on()` call, or moving it outside the `try`, is a complete response.

**[SYMMETRY]** `plato/packs/domain.py:66` — `published_domain_packs` and `plato/packs/loader.py:245` `published_registries` are the same loop written twice: `list_packs(domain, segment)` → `is_assistant_pack` filter → `content_ref is None` skip-and-warn → `materialize` inside a per-row `try` → warn. The diff's own test at `tests/test_pack_store.py:553` says so ("Both readers loop over the same rows, so a fix to one and not the other leaves half the store able to take a tenant down") and then keeps both copies. One shared async generator in `plato/packs/` yielding `(record, root)` for a given shape is no more code than the second copy.
IMPORTANCE: medium — the two copies already differ (only the domain one calls `depends_on()`, which is the defect above); both are test-only today, so nothing outside this repo depends on either.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/profiles.py:97` — `is_policy_profile_document` re-derives `is_policy_document` (`jazzx_sdk/fabric/canonical/policy.py:704`) line for line, and that function already takes the `key: str = "policies"` parameter that exists for exactly this reuse; the only real difference is that a bare top-level list counts as a policy document and not as a profile.
IMPORTANCE: low — one call site each (`manifest_loader._declared_files`'s `keep=`); the duplication is 12 lines and both currently behave the same for the dict case.

**[NOTE]** `jazzx_sdk/pack/roots.py:114` — `env_text(*env_names) if env_names else ""` has a dead false branch: `env_text()` with no names already loops zero times and returns its `default=""` (`jazzx_sdk/config/envvars.py:46`).
IMPORTANCE: low — cosmetic; `resolve_packs_root` has no shipped caller passing zero names.

**[NOTE]** `jazzx_sdk/pack/pack.py:232` — a pack that declares `profiles.default: profiles/gone.yaml` and resolves zero profiles returns `None` with no diagnostic, while the same unresolvable `default:` warns at one profile and raises at two or more. The `if not profiles: return None` early return runs before `default_ref` is ever read.
IMPORTANCE: low — reachable only from a manifest that declares a `default:` and no readable profile file; the caller sees the same `None` as a pack with no `profiles:` block at all.

VERDICT: BLOCK
