# adversarial review -- japes

- when: 2026-09-16T03:43:40Z
- range: 4a6ce36..HEAD (since the last review) (9cb6162)
- diff: 325 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/boot/schema_version.py :: literal-memory-path-not-refused -- FIXED (`unmigratable_reason(DbStore(backend="sqlite", sqlite_path=":memory:"))` now returns the refusal; `test_every_spelling_of_in_memory_is_refused` covers `None`, `""`, `"   "`, `":memory:"`, `"  :memory:  "`).
WAS: plato/static/dashboard.html :: config-save-hidden-where-auth-allows -- FIXED (both the page and `create_config_router` now evaluate `withholding(strict=..., auth=...)`; `app.py:286` forwards the app's `auth`, and the strict+auth row of the new parametrize asserts the button stays).
WAS: plato/api/database.py :: alembic-failure-unaudited -- FIXED (bare `except Exception` audits and re-raises; `test_a_migration_that_raises_still_records_who_asked` fails against the old two-branch list).
WAS: plato/api/info.py :: details-page-strict-replace-is-dead -- FIXED (replace removed, and `test_the_details_page_carries_no_dead_posture_placeholder` pins it).
WAS: plato/static/dashboard.html :: patch-405-claim-is-false -- FIXED (no `405` remains in `plato/static/`, `plato/api/info.py`, `plato/api/config.py` or `tests/test_plato_info_api.py`).
WAS: docs/DEPLOYMENT_ENV.md :: sqlite-row-declares-silent-success -- FIXED (the row now scopes the silent success to the shell `alembic` invocation and states 503 / exit 3 for the in-process migrators, which matches `plato/api/database.py:216` and `plato/jobs.py:100-104`).

[DEFECT] plato/api/info.py:358 :: relaxed-only-wider-than-strict-mode -- the new comment says "Delete and Initialize both go through `relaxed_only` ... so strictness is the whole predicate here", but `relaxed_only` blocks on `strict_mode() or unreadable_tier_vars() or environment_tier() not in (LOCAL, DEV_DAILY)`, so two reachable configurations render all three relaxed-only controls while every one of them answers 403.
`JAPES_ENVIRONMENT=staging JAPES_STRICTNESS=relaxed` -- supported, since `strict_mode`'s docstring says the override "can loosen every tier except `production`". Ran it: `strict_mode()` is `False`, `environment_tier()` is `staging`, so `packs_html` substitutes `__STRICT__` -> `false` and `plato/static/packs.html:420` (`seed-actions`) and `:260` (`if (!STRICT)`, Delete) both show, while `plato/api/packs.py:170,185` refuse with 403. Second trigger: `ENVIRONMENT=prodution` -- ran it, `strict_mode()` is `False` and `environment_tier()` falls back to `local`, but `unreadable_tier_vars()` is `["ENVIRONMENT='prodution'"]`, which is the branch `relaxed_only` added specifically so a misspelt tier refuses. The same gap reaches the third control by a different path: `plato/api/info.py:462` reports `"strict": strict_mode()` and `plato/static/info.html:198` hides Migrate on `strict !== false`, so `POST /database/migrate` (`relaxed_only("a schema migration")`) is offered and refused too. `_POSTURE_GATED_CONTROLS`' new docstring ("Three ask about strictness, because `relaxed_only` has no auth escape") reads the absence of an auth escape as strictness being the whole predicate; the escape and the predicate are different questions, and the diff got the escape right and the predicate wrong.
IMPORTANCE: medium -- three controls on two pages, both mounted unconditionally (`plato/app.py:265-287`; the details page is also on the degraded app at `plato/__main__.py:308`, which omits packs). It fails loudly at the click, not silently, and the operator's workaround is to stop pressing the button -- but it is exactly the "page is lying" failure the gate was added for, and the consistent predicate already exists next to `withholding` in `plato/boot/posture.py`. Nothing outside this repo renders these pages.

[NOTE] plato/boot/schema_version.py:157 :: blank-path-misdiagnosed-as-memory -- the guard strips before comparing but `DbStore` does not (`jazzx_sdk/fabric/db/store.py:195`: `str(self._sqlite_path or ":memory:")`), so `PLATO_SQLITE_PATH="   "` produces a real *file* named `"   "` that a second connection can open, and the refusal tells the operator "this replica's database is in-memory sqlite ... Set PLATO_SQLITE_PATH to a file".
IMPORTANCE: low -- reachable only by an operator typing whitespace into the var or the config panel; `plato/wiring/default.py:177` passes `env("PLATO_SQLITE_PATH")` through unstripped, and `plato/wiring/local.py:303` sets a real path, so no shipped configuration hits it. Refusing is the safe direction; only the stated reason is wrong.

[NOTE] plato/api/database.py:146 :: audit-helper-claims-all-branches -- "shared by the failure branches so adding a third cannot quietly skip it" does not hold: of the three `except` branches, `UnmigratableStoreError` (line 212) does not call it, so the helper is shared by two and the sentence reads as an invariant a fourth branch would inherit automatically.
IMPORTANCE: low -- the un-audited branch refuses before `upgrade_to_head` runs any work, so there is nothing to attribute; the cost is a future branch added on the strength of the sentence.

[NOTE] tests/test_plato_info_api.py:679 :: gate-agreement-asserted-against-a-table -- the four rows compare the rendered string to a literal `writable` column rather than to what `create_config_router` actually does with the same `(tier, auth)`, so the page and the router can still drift apart the way they just did (e.g. if the router starts passing `identity_required` to `withholding`, the router would accept writes the page keeps hiding and all four rows still pass).
IMPORTANCE: low -- test-only; the shipped defect it would have caught is the one this diff just fixed, and a single `TestClient(...).patch(f"{prefix}/config", ...)` row asserting the status code matches the rendered boolean is what closes it.

VERDICT: BLOCK
