# adversarial review -- .

- when: 2026-09-25T02:25:33Z
- range: dc3ecfc..HEAD (since the last review) (3974065)
- diff: 183 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/pack/rule_merge.py :: skipped-description-stale -- FIXED
WAS: jazzx_sdk/pack/rule_merge.py :: skipped-duplicate-remains-mergeable -- STILL OPEN, deferred by TODO (see CHECKED below)
CHECKED: jazzx_sdk/pack/rule_merge.py :: skipped-duplicate-remains-mergeable -- the TODO says the skipped proposal stays ACCEPTED and a second merge is last-wins. That matches the code at `rule_merge.py:124-129`, and the TODO names what would change it.
WAS: jazzx_sdk/experts/policy/default.py :: replaced-rule-drops-its-replaces -- FIXED
WAS: jazzx_sdk/experts/policy/default.py :: unevaluable-replacement-silently-drops-base -- FIXED
WAS: jazzx_sdk/experts/policy/default.py :: conditionless-gated-rule-replaces-nothing -- STILL OPEN, narrowed to the shared-id case. At line 474 the gate is only evaluated when `rule.condition is not None or rule.replaces`. A conditionless gated rule with no `replaces` that shares an id with a lower rule never evaluates its gate. `gate_outcome` stays None, line 486 replaces nothing, and the lower rule still runs. The new `Rule.replaces` text says "A shared id replaces the same way", which is not true here. Using `replacing` in place of `rule.replaces` at line 474 closes it. Still low: no shipped YAML has a conditionless rule reusing a lower id.

[NOTE] jazzx_sdk/experts/policy/default.py:468 :: carryover-ignores-own-gate -- A superseded rule's `replaces` carry over without checking its own applicability gate, so they are dropped in cases where that rule would never have replaced them.
Trigger: overlay rule `RB-X` has `applicability: property_type == retail` and `replaces: [CORE-Y]`, and a deal rule reuses `RB-X` with no gate. For an office property, `CORE-Y` used to evaluate because `RB-X` did not apply. Now `CORE-Y` is skipped as "replaced by DEAL/RB-X".
IMPORTANCE: low -- the only shipped chain, `deal_si_rb_example.yaml` → RB overlay → core leverage (japes seed pack plus jaci's copy), has no applicability gate on the RB rule, so it cannot hit this.

[NOTE] jazzx_sdk/experts/policy/default.py:464 :: suppression-carries-replacements -- The carry-over also runs when the superseding reason is a policy `suppresses` entry, not a replacement. The comment "its replacement stands in for it" is then false: no rule stands in, and the core rules the suppressed rule replaced disappear too.
Trigger: a deal policy with `suppresses: [RB-X]`, where `RB-X` replaces `CORE-Y`. `CORE-Y` is now skipped as "suppressed by DEAL".
IMPORTANCE: low -- I grepped japes and `jaci/config` + `jaci/src` and found no YAML using `suppresses:`. Only the API can reach it.

VERDICT: PASS
