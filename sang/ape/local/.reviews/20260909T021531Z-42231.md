# adversarial review -- japes

- when: 2026-09-09T02:15:31Z
- range: 3e281d6..HEAD (since the last review) (d1f4f32)
- diff: 147 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Previous round

- WAS: `[DEFECT] store.py:654` (+`:695`, `:740`, `:359`, `:1803`) `Raises:` enumerates only `KnowledgeFabricError` — **FIXED** (all five sites now carry a `ValueError` line: `:361`, `:646`, `:688`, `:734`, `:1798`); see the new finding for how the added line reads.
- WAS: `[SYMMETRY] store.py:761` sidecar guarded in TEST branch only — **FIXED** in the smaller direction: `:754` is now a raw `fixtures_dir / f"{identifier}.metadata.json"`, matching `:524`/`:787`/`:1823`, and it is equivalent because `identifier` is already refused at `:742`.
- WAS: `[NOTE] store.py:510-512` / `tests:173` "four branches" count — **FIXED**; the docstring no longer counts branches (`:499-500` describes the TEST fixture dir instead) and the test docstring dropped the clause.
- WAS: `[NOTE] tests/test_fabric_docs.py:144` dedented docstring line — **FIXED**; docstring rewritten, no column-0 line.
- WAS: `[NOTE] store.py:493` rename broke the `BlobStore._local_path` name pairing — **FIXED**; renamed back, and `grep _local_path` now shows the same name in `fabric/docs/store.py` and `fabric/blob/store.py`.
- WAS (round before, carried as STILL OPEN) `store.py:499-500` provenance overclaim — **FIXED**; the docstring no longer asserts the name is untrusted from `agents.interactive.reads`/`tools.documents.local_cache`, and the test docstring's "in-repo callers cannot deliver a traversal today" no longer contradicts it.

## New review

**[DEFECT]** `jazzx_sdk/fabric/docs/store.py:361` (and `:646`, `:688`, `:1798`) — the added `ValueError: If the name is not a relative name` is stated unconditionally, but only the *local* path validates: `put`/`delete` default to `location="hub"` and `get` under `RetrievalMode.STRICT` never reach `_local_path`. Trigger: `await fabric.docs.put(b"x", name="", location="hub")` (the documented default location) returns a hub document id for an empty-named document instead of raising, and `await fabric.docs.get("../secret.txt")` under STRICT raises `KnowledgeFabricError`, not the documented `ValueError`. `_get_test_fixture:734` is the one accurate instance, since `:742` is unconditional.
IMPORTANCE: medium — docstring-only, but on the public `docs.put/get/delete` surface, which is the contract external consumers code against; a caller that skipped its own name validation on the strength of this line gets an unnamed hub document with no error. In-repo callers are unaffected (`agents/interactive/reads.py:137` catches bare `Exception`; `tools/documents/local_cache.py` uses the local path where the claim does hold).

**[NOTE]** `jazzx_sdk/fabric/docs/store.py:500` — "the one local read that does not join `self._local_dir`" is true for reads, but `materialize` (`:1150`) writes into a caller-supplied `output_dir` joined with `name_fn(entity)`, so a reader auditing "every local filesystem sink" from this sentence will stop one short.
IMPORTANCE: low — comment-only; `materialize`'s names come from KH entities via the caller's `name_fn`, not from a document `name`, and this diff does not touch it.

19/19 in `tests/test_fabric_docs.py` pass against the tree.

VERDICT: BLOCK
