# adversarial review -- japes

- when: 2026-09-11T06:25:38Z
- range: d8da179..HEAD (since the last review) (f9596d2)
- diff: 374 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree (`pytest tests/test_plato_boot_contract.py tests/test_plato_posture.py tests/test_plato_boundary.py` → 69 passed, plus the two job roles run as real subprocesses with a thread probe at `os._exit`).

WAS: plato/boot_contract.py :: job-exit-six-never-terminates -- FIXED; `ENVIRONMENT=prodution JAPES_RUN_MODE=job:sweeper` now returns 6 and `ENVIRONMENT=local JAPES_RUN_MODE=job:session-reaper` returns 5, both in under a second. A probe that shadows `os._exit` and enumerates threads first prints `non_main_threads=[]` on both paths, so `release_wiring` really closes the aiosqlite pool and `os._exit` is the backstop it claims to be, not the thing doing the work.
WAS: docs/DEPLOYMENT_ENV.md :: job-exit-prose-omits-posture-code -- FIXED; the prose no longer enumerates 4/5 and points at the generated table.
WAS: tests/test_plato_boot_contract.py :: schema-row-job-exit-untested -- FIXED; `test_a_job_against_a_stale_schema_exits_with_the_documented_code` asserts `result.returncode == rule.job_exit` for the `job_exit=5` row as a subprocess.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; the shipped YAML is unchanged, and every guard the notes now describe (`prepayment_penalty_requested` defaulting to `"unspecified"`, `interest_rate` required for DSCR-PPP-IL-2, `residential_unit_count` derived) lives in jaci's `LoanApplication`, not in anything japes ships, so a second consumer of these rules still gets the INDETERMINATE-and-skip pass.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested
ACCEPTED: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer

CHECKED: `TODO(swap-schema-placement)` at `plato/wiring_default.py:576` holds and is untouched by this diff -- `_db` still returns a bare store, `register_all` + `create_all()` still emit unqualified DDL, and `dispose_all` changes nothing about which schema those tables land in.

[SYMMETRY] tests/test_plato_boot_contract.py:158 :: tier-vars-copied-not-derived -- `_TIER_VARS` hardcodes the four posture variable names that `jazzx_sdk/config/envvars.py:161` already owns as `_POSTURE_ENV_VARS`, the tuple `deployed_posture` and `environment_tier` actually loop over; `{name: None for name in _POSTURE_ENV_VARS}` is the same amount of code and is the list the row under test is defined against.
IMPORTANCE: low -- test-only, and it fails loudly rather than silently if a fifth name is added (the subprocess would inherit a real tier and return something other than 6). Enumerated every use of the tuple: three loops in `envvars.py` and no other copy in `plato/` or `tests/`, so this new constant is the first duplicate.

[NOTE] plato/wiring.py:190 :: release-wiring-unbounded-await -- the docstring argues the exit code's delivery does not rest on `release_wiring` succeeding, but `asyncio.run(closer())` runs inside `run_role`'s `finally`, i.e. before `__main__` ever reaches `os._exit`; the guarantee covers a `dispose` that *raises* (which the new test pins) and not one that *blocks*, and a blocking one reproduces exactly the hang the codes exist to replace.
IMPORTANCE: unknown -- I could not establish that any shipped pool blocks: the sqlite path provably does not (both job subprocesses return in under a second with no threads left), and I did not verify whether SQLAlchemy's asyncpg dispose can await a severed connection indefinitely, which is the only configuration that could reach it (`PLATO_DB_BACKEND=common`, a `job:*` role, deployed posture).

[NOTE] plato/__main__.py:459 :: atexit-except-unreachable -- `atexit._run_exitfuncs()` does not propagate a callback's exception; CPython writes it as an unraisable and continues (`python -c "import atexit; atexit.register(lambda: 1/0); atexit._run_exitfuncs()"` prints `Exception ignored in atexit callback` and returns 0), so the `except Exception` branch and its `exit handlers raised:` line have no reachable trigger and the property the comment asserts is atexit's, not this handler's.
IMPORTANCE: low -- no reachable trigger; the one registration in the repo (`jazzx_sdk/llm/config.py:157`, `_safe_unlink`) swallows its own errors, and third-party handlers are equally unable to reach this clause.

[NOTE] plato/wiring_default.py:587 :: retired-never-disposed-claim-stale -- `swap_database`'s docstring still says the retired store "is never disposed" and that "`del previous` below drops the last handle this function holds", which the new `dispose_all` contradicts on the first count and `replace`'s `self._retired.append(previous)` contradicts on the second; the sibling claim in `replace` ("its pool closes when the process does") is the one this diff made true.
IMPORTANCE: low -- documentation on a path reachable only through `/v1/config` bootstrap-key writes; no behaviour depends on the sentence, and nothing outside this repo reads it.

VERDICT: PASS
