# adversarial review -- japes

- when: 2026-09-16T15:22:02Z
- range: 32d96ab..HEAD (since the last review) (1e3123e)
- diff: 340 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: Dockerfile :: otel-fastapi-instrumentor-unlocked -- FIXED. `pyproject.toml:64-69` declares it, the lock pins `opentelemetry-instrumentation-fastapi 0.59b0` as `groups = ["main"]`/`optional = false` (with asgi/util-http/asgiref pulled in), and `poetry check --lock` exits 0, so `poetry install --only main --extras plato` now installs it.
WAS: tests/test_common_submodule_deps.py :: resolves-checks-the-venv-not-the-lock -- FIXED. `test_a_dependency_common_needs_is_locked` attributes each reached module to its distribution by file and asserts the lock pins it; I ran all 28 attributable modules and every provider resolves lock-pinned. Its scope is narrower than the docstring says -- new finding below.
WAS: tests/test_plato_packs_api.py :: config-patch-test-reads-ambient-tier -- STILL OPEN. The write still outlives the test: `monkeypatch` only restores keys *it* set, and `clean_env` touches only the two posture keys, so `EnvSettingsStore.write`'s `os.environ["PLATO_PACK_UPLOAD_MAX_BYTES"] = "1048576"` survives -- I appended a probe test after it and read `1048576`. The new docstring asserts the opposite ("`EnvSettingsStore.write` sets the real environment, which without the fixture outlives the test"). The ambient half is closed for `JAPES_ENVIRONMENT` only; see below.
WAS: tests/test_plato_packs_api.py :: env-set-by-hand-not-clean-env -- FIXED. `test_a_nonsense_cap_does_not_refuse_every_upload` takes `clean_env` and the try/finally is gone.
WAS: plato/api/packs.py :: unset-backend-renders-empty -- FIXED. `default='local'`, the same spelling `pack_archives_are_durable` (`plato/wiring/default.py:637`) and `_pack_blob` (`:644`) use.
WAS: docs/DEPLOYMENT_ENV.md :: config-patch-needs-auth-when-strict -- FIXED. Row now states both halves; verified both gates are the same call, `withholding(strict=..., auth=auth)` at `plato/api/config.py:255` and `plato/api/packs.py:270`.
WAS: plato/wiring/settings_store.py :: settings-failure-not-at-info -- STILL OPEN. `_apply_durable_settings` (`plato/wiring/default.py:695-734`) has three failure paths and all three are `logger.warning` only; no `record_boot_note`, so nothing reaches `/info`'s `degraded`.
WAS: pyproject.toml :: otlp-http-caps-the-otel-sdk -- STILL OPEN as a fact. The lock now carries the whole otel main group at that ceiling's pairing (sdk/proto 1.38.0, semconv and instrumentation 0.59b0); stating it at the dependency remains the only available response.

[DEFECT] tests/test_plato_packs_api.py:29 :: posture-fixture-misses-three-tier-vars -- `_POSTURE_KEYS` names two of the five variables that decide the posture, and the new `clean_env.setenv("JAPES_ENVIRONMENT", "local")` cannot compensate, because `environment_tier` deliberately refuses to let a local value in one variable mask a deployed signal in another.
`ENVIRONMENT=production .venv/bin/python -m pytest tests/test_plato_packs_api.py -k settable_through_config` fails with 403 `config_writes_disabled` on the current tree. `_POSTURE_ENV_VARS` (`jazzx_sdk/config/envvars.py:161`) is `("JAPES_ENVIRONMENT", "JAPES_ENV", "ENVIRONMENT", "APP_ENV")`; the fixture delenvs the first plus `JAPES_STRICTNESS`, and `environment_tier`'s own docstring states why the `setenv` does not help ("deliberately does not let a higher-priority `dev` value mask a deployed signal from another conventional variable"). Same gap in the two sibling fixtures that delenv by hand: `tests/test_plato_info_api.py:207` and `:1303`, and `tests/test_plato_packs_api.py:1137`.
IMPORTANCE: low -- test-only false red, and `.github/workflows/*.yml` set none of the four, so CI is unaffected; the trigger is a developer or container with `ENVIRONMENT`/`APP_ENV` exported. I enumerated every `delenv` of a posture key in `tests/` (4 sites, 3 files); nothing outside this repo reads the fixture.

[DEFECT] tests/test_common_submodule_deps.py:262 :: lock-presence-is-not-image-install -- the docstring says the lock is the authority on "what the image installs (`poetry install --only main --extras plato`)", but `_locked_distributions` accepts every `name =` in the file, which includes 19 dev-group-only distributions and 50 main-group ones gated behind extras the image does not install.
`redis.asyncio` is in the test's own reached set and `redis` is locked behind `extra == "streaming"`, which `--extras plato` does not install: install redis into the venv (`poetry install --all-extras`, which is what the mlflow/finance/pptx tests need) and the case turns from skip to a green pass asserting the image has a module it does not. The same is true today for anything a `common` bump imports that is already locked behind `mlflow`, `templating`, `gemini`, `azure`, `finance` or `pptx` -- e.g. `jinja2`, locked with `markers = 'extra == "mlflow" or extra == "templating"'`. `tests/test_ci_extras_coverage.py:24-28` already parses TOML with `tomllib` and evaluates these markers with `packaging.markers.Marker`; doing the same here is four lines and narrows the set to what the image gets.
IMPORTANCE: low -- test-only, and no module in the reached set is currently mis-attested (all 28 attributable providers are main-group and non-optional except `defusedxml`, which the `plato` extra installs). I ran the sweep and listed every provider; the only outside dependency is the image build, which this test gates.

[NOTE] tests/test_common_submodule_deps.py:236 :: namespace-provider-arbitrary -- the namespace fallback attributes a bare namespace module to whichever distribution `rglob` reaches first, which is the ambiguity the docstring two lines above rejects `packages_distributions()` for.
`opentelemetry` is in the reached set; ten distributions ship files under that one directory here (`-api`, `-proto`, `-semantic-conventions`, `-instrumentation`, `-util-http`, `-instrumentation-asgi`, `-instrumentation-fastapi`, `-sdk`, `-exporter-otlp-proto-common`, `-exporter-otlp-proto-http`) and the case currently checks `opentelemetry-api` by walk order. All ten are locked, so it passes; a standalone otel wheel in a developer venv that the lock does not pin could make it a red naming a distribution japes never imports.
IMPORTANCE: low -- one test case, filesystem-order dependent; no shipped code involved.

VERDICT: BLOCK
