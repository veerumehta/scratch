# adversarial review -- japes

- when: 2026-09-10T07:19:41Z
- range: origin/v2.5.1...HEAD (12563a6)
- diff: 9782 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `TODO(orphan-blob-gc)` (`jazzx_sdk/pack/store_db.py:280`) — holds. `DbPackVersionStore` binds `self._tenant_id` at construction and the blob key is the content digest, so the cross-tenant `count(*)` it defers to a job genuinely cannot be asked here.
- `TODO(threshold-grid-provenance)` (`jazzx_sdk/fabric/canonical/profiles.py:215`) — holds, and the "not a regression" part is right: `tables` gets no `_coerce_threshold_mappings` (it is registered on `"thresholds", "custom"` only) and `get_table` is untouched, so a cell is served exactly as before.
- `TODO(activation-vs-active-rules)` (`jazzx_sdk/fabric/canonical/policy.py:757`) — holds. `executable_rules` / `activation_blocks` / `Rule.is_executable` have no non-test callers; the only `is_executable` in shipped code is `Threshold.is_executable` at `policy_lint.py:45`, a different symbol.

PARKED: `tests/test_plato_packs_api.py::test_every_bundled_seed_pack_loads` (`TODO(seed-pack-consumer-pointers)`) — already enumerates ci-spread-core's `jaci.*` manifest pointers.
PARKED: `tests/test_plato_info_api.py:622` (`TODO(protocol-relative)`).

## Findings

**[DEFECT] `plato/app.py:113` — `pack_store_for` is never wired, so the whole packs feature is dead outside tests.**
`PlatoWiring` has no `pack_store_for` field and `build_app` (`plato/wiring.py:133`, the only non-test `create_plato_app` caller) does not pass one, so `store_for is None` in every deployment: `GET {prefix}/packs` answers `{"available": false, "reason": "no pack store is wired", "packs": []}` (`packs_api.py:46`), and both `POST {prefix}/packs/initialize` and `DELETE {prefix}/packs/{pack_id}/{version}` raise 503 — while `_NAV_LINKS` (`plato/info_api.py:70,75`) now advertises both Packs entries in the working app's nav.
Trigger: start any `server`/`dual` role (`plato/__main__.py` → `build_app(wiring)`), open `/v1/packs/ui`, click Initialize.
Sibling that already has the shape: `session_store_for` is a `PlatoWiring` field (`plato/wiring.py:38`) built at `plato/wiring_default.py:441` from the same db handle a `DbPackVersionStore(handle.current, tenant_id=tenant)` needs.
IMPORTANCE: high -- I enumerated every `create_plato_app(` call site (1 production: `plato/wiring.py:133`; 21 in `tests/`) and every `pack_store_for` reference (`plato/app.py:113,228` and `tests/test_plato_packs_api.py:185`). The only caller that wires it is the test, so the page, the seed-initialize and the version-delete added by this branch are unreachable in every shipped configuration; nothing outside this repo depends on them yet.

**[DEFECT] `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:45` — declared policy overlay does not exist, and is skipped with a log line.**
The manifest declares `policies/overlays/rb_ci.yaml` for `applies_to_program_ids: [RB-CI-001, RB-CI-002]`, but the directory (all 28 files new on this branch) contains `policies/overlays/deal_si_rb_example.yaml` and no `rb_ci.yaml`. `PackManifest._declared_files` warns and `continue`s on a missing declared ref (`jazzx_sdk/pack/manifest_loader.py:176-179`), so `policy_files()` returns `policies/core.yaml` alone; the `policies:` block has no `dir:` key, so the overlay that *does* exist is never picked up either.
Trigger: publish ci-spread-core and read `policy_files()` — a deal on program `RB-CI-001` resolves against the core policy with the institution's narrowing overlay silently absent.
IMPORTANCE: medium -- the narrowing direction is the unsafe one and the only trace is `logger.warning`, but reachability is currently capped: the parked xfail above shows ci-spread-core's `policies.registry` pointer already fails to load, so this becomes live the moment that pointer is resolved. Reached via `plato/seed.py:seed_packs` (the initialize route) and any `Pack`/`PackManifest.policy_files()` consumer; jaci consumes these packs.

**[NOTE] `plato/data/seed_packs/ci-spread-core/ontology/ci_lending.yaml:401` — `UCAcashFlow` and `ExcessAvailability` carry the exact undeclared-reference defect the v0.2.1 header says it fixed.** `UCAcashFlow.formula` reads `net_income`, `D&A` and `working_capital_change`, none of which are `FinancialPeriod` fields or in its own `inputs` list (which instead names `ebitda` and `operating_cash_flow`, neither of which appears in the formula); `ExcessAvailability.inputs` names `Facility.outstanding_balance`, and `Facility` has no such field, while the formula's `reserves` is undeclared. The version note fixed FCCR and `DebtServiceCoverage` and left these two.
IMPORTANCE: medium -- no code path reads `concept_graph` (the file states the formulas are documentation and computation lives in the Reasoner / `compute_ci_metrics()`), so nothing breaks; the cost is that the block an SME is told to edit contradicts the `entities` block above it in the same file, in the class of error the header claims to have swept.

**[NOTE] `plato/info_api.py:92` — the `nav_html` docstring and the `TODO(omit-info)` above it both still state the degraded tuple as `("Guide", "config", "database")`.** `_info_only_app` now passes five entries. The TODO's actual reasoning (no caller passes `omit=("info",)`) still holds; only the quoted tuple is stale in two places.
IMPORTANCE: low -- comment-only, in the one file whose stated purpose is that "three files cannot drift into three link sets."

**[NOTE] Gitignored plan filenames and a consumer-tree document path in shipped pack data.** 12 of the new ci-spread-core files name `plan_JACI_CL_*.md` (13 occurrences: `chart_of_accounts.yaml`, `metrics.yaml`, `addbacks.yaml`, `breakouts.yaml`, `reclassifications.yaml`, `spread_template*.yaml`), and `playbooks/loan_package_scaffold.md:1` plus `pack_manifest.yaml:83` embed `docs/LoanSamples/YETI/Yeti Credit Package/YETI_Secure LOC_Comprehensive_Loan_Package.pdf` and `scripts/extract_policy.py` — paths in jaci's tree, not japes'. These are committed, non-gitignored files pointing at gitignored docs in another repo, against the project's own rule.
IMPORTANCE: low -- data-only, and `cl_of_core`/`cl_sp_core` already carry `plan_JACI_CL_PACK_FOUNDATION.md`, so this branch widens a pre-existing pattern rather than starting it. A reader outside jaci cannot resolve any of the references.

VERDICT: BLOCK

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED:** `TODO(seed-pack-consumer-pointers)` in `tests/test_plato_packs_api.py:405` — enumeration verified against the manifests: ci-spread-core 6 `jaci.*` pointers, cl_of_core 3, cl_sp_core 2, clinical-intake-core 1. Claim holds; not raised.
**PARKED:** `test_every_bundled_seed_pack_loads` (`TODO(seed-pack-consumer-pointers)`).

---

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/spread_template.yaml:36,38` — `{metric: da}` and `{metric: stock_based_compensation}` resolve in neither the pack's `metrics.yaml` (16 ids: ebitda, cit_ebitda, … no `da`, no `stock_based_compensation`) nor `jazzx_sdk/finance/metrics.py`'s `credit_metrics`/`working_capital_days` dicts. Trigger: rendering `ci_credit_spread` for any borrower — the "Depreciation and amortization" and "Stock-based compensation" rows in the income-statement section come out empty for every period. Both exist as `cash_flow` chart-of-accounts keys (`depreciation_and_amortization`, `stock_based_compensation`) and are bound that way in the same file's `cash_flow_operating` section, so the fix is `{statement: cash_flow, key: …}`.
IMPORTANCE: medium -- checked all 5 keys/metrics namespaces (`chart_of_accounts.yaml` 84 keys, `metrics.yaml` 16 ids, the two SDK builtin dicts); `spread_template_rb.yaml` and both `workbook_layout*.yaml` resolve 100%, so this is the only template with dead bindings. Fails visibly (blank rows), and no japes code renders it today — the consumer is jaci.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125,127` — `purpose_specific_documents.required_documents` carries two PDF line-wrap orphans, `schedule` and `available)`, while the two source entries they belong to lost their heads ("Equipment/CapEx: … schedule", "Acquisition: … (if available)"). The file's own `description` (line 4) claims "Extracted verbatim from the source checklist." Trigger: any C&I application run through `assess_package()` with this checklist produces DATA_GAP hypotheses for documents literally named `schedule` and `available)`, and never asks for the equipment schedule or the acquisition documents.
IMPORTANCE: medium -- only this section of the 12 is corrupted; the checklist is declared by `ci-spread-core/pack_manifest.yaml:46` (`checklist: policies/checklist_ci.yaml`) and consumed outside this repo (jaci's `assess_package`). Fails as wrong prompts to a human, not silently wrong math.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/conventions.yaml:1` and `policies/overlays/deal_si_rb_example.yaml:1` — both files are added to the pack but neither is reachable. `ci-spread-core/pack_manifest.yaml:38-47` declares only `registry:`, `core:`, `overlays: [{path: policies/overlays/rb_ci.yaml}]` and `checklist:`; `rb_ci.yaml` does not exist in the pack (`policies/overlays/` holds only `deal_si_rb_example.yaml`), and `conventions.yaml` is declared nowhere. `PackManifestLoader.policy_files` (`jazzx_sdk/pack/manifest_loader.py:230-240`) collects `core` + `overlays[].path` + `dir`, so the FCCR/ABL/LIEN policies and the `RB_CI_OVERLAY` in `conventions.yaml` are never loaded — even though its header claims "loaded via jazzx_sdk load_policies". Trigger: drop the `registry:` pointer as `TODO(seed-pack-consumer-pointers)` proposes, and `Pack("ci-spread-core").policy_registry` returns a registry holding only `CI_CORE_LEVERAGE_POLICY` — the code is worse than that TODO admits, because the declarative files it points to are not all declared. `dscr_core/pack_manifest.yaml:29-31` in this same diff already uses `policies: dir: policies`, which picks all three up and skips `checklist_ci.yaml` by `is_policy_document`.
IMPORTANCE: medium -- verified against `policy_files`/`_declared_files`; no japes code path loads it today (Plato only zips and publishes), but it is the exact state the parked test is waiting to flip on, and the fix it names would not be sufficient.

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:394` — `DSCR-IO-MIN-LOAN`'s description says interest-only "require a minimum loan amount of $250,000 **and minimum FICO 640**", but the rule's only condition is `loan_amount >= 250000`; no rule in the 41 anywhere checks FICO for IO, and `profiles/dscr_profile.yaml:34` (`fico_min_io: 640`) has no consumer — which `ENCODING_NOTES.md` §3 G4 itself records. Trigger: interest-only, FICO 620, $300,000, purchase, CLTV 60 — the main grid cell `la_le_1_5m|620|purchase` is 65, so the loan passes with no violation. Contrast `DSCR-ITIN-CLTV`, whose description makes the same "min FICO 640" claim but is actually enforced, via `"NA"` cells at lt600/600/620 in `max_cltv_itin`.
IMPORTANCE: medium -- checked all 41 rules for a `fico` predicate under IO applicability; none. Pack is `certification_status: draft` and consumed only by jaci's DSCR conductor, but this is an over-permissive eligibility verdict a reader of the rule description would not expect.

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:288` — `DSCR-PROPERTY-TYPE-CLTV` defers the Florida −5% modifier with "see ENCODING_NOTES G1", but `ENCODING_NOTES.md:207` records G1 (`AllOf`/`AnyOf`) as resolved japes-side on 2026-08-14, and 10 rules in this same file already use `kind: all_of` (`DSCR-NMLS-VERIFIED` plus 9 PPP applicabilities). The stated reason for the deferral no longer holds. Trigger: Florida non-warrantable condo purchase at CLTV 78% — the rule caps at 80 and returns SATISFIED; the program caps Florida at 75. Same for `DSCR-HIGH-LTV-PROPERTY-TYPE:173`'s unenforced "outside Florida".
IMPORTANCE: medium -- the two rules are the only Florida-sensitive ones; verified `all_of` is live in this corpus. Draft pack, jaci-side consumer, but it is a silent pass on a real program cap rather than a loud gap.

**[DEFECT]** `plato/data/seed_packs/cl_of_core/pack_manifest.yaml:50` and `cl_sp_core/pack_manifest.yaml:49` — both declare `experts.playbook.diagnose_map: diagnose_map.yaml`, and neither pack directory contains that file (`cl_of_core/` holds `pack_manifest.yaml` + `pipelines.yaml`; `cl_sp_core/` holds only `pack_manifest.yaml`). `ci-spread-core` ships a real `diagnose_map.yaml` beside its identical declaration. Trigger: `CIPlaybookExpert.diagnose()` on either pack, once the `jaci.*` class pointers are settled — a missing file, not a missing rule. Separate from `TODO(seed-pack-consumer-pointers)`, which covers only the dotted paths.
IMPORTANCE: medium -- checked all five pack directories; only these two declare an asset they do not ship. Fails loudly at first diagnose, and no japes code reaches it.

---

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:38` — §1b's "8 plain `expression`, 6 `all_of`" is wrong: the 14 PPP rules split 5 plain / 9 `all_of`, and §1b's own prose then enumerates 9 multi-predicate states (IL ×2, MS, NJ, OH, PA, RI, VA, VT). §3 G1's "no rule in `policies/eligibility.yaml` uses `kind: all_of`/`any_of` yet" is also contradicted by §1a and §1b in the same file. The §1 header table's other counts (41 rules, 14 PPP, 99/29 grid cells, 33 ITIN cells) all verify.
IMPORTANCE: low -- documentation inside the pack; nothing reads it. One confusing paragraph for the next author.

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## CHECKED

- `plato/posture.py:96` `TODO(if-match-post)`: the claim holds. `require_if_match` is referenced only by `tests/test_plato_preconditions.py` and `posture.py`'s own `__all__`; no shipped wiring sets `PlatoWiring.auth` and `app.py` never passes it as a router dependency, so `POST {prefix}/packs/initialize` cannot 428 today.
- `plato/data/seed_packs/*` `TODO(seed-pack-consumer-pointers)`: the `jaci.*` pointers in four of five bundled manifests are real shipped consumer-repo references, but the deferral names the failure mode ("the seed run reported it published") and `tests/test_plato_packs_api.py` pins the pack set with a strict `xfail`. Nothing worse than the TODO admits.

PARKED: `test_every_bundled_seed_pack_loads` (TODO(seed-pack-consumer-pointers)).

## Findings

**[DEFECT] `plato/packs_api.py:128`** — `_store` calls `tenant_from_request(request)` unguarded, but the shipped `plato/wiring_default.py:490` raises `ValueError` (not `HTTPException`) when `X-Tenant-Id` is absent on a deployed posture, and `plato/static/packs.html` never sends that header, so every fetch the new page makes 500s on dev-daily/staging/production. Reproduced against the wiring's exact predicate: `GET /api/v1/packs` → 500, `POST /api/v1/packs/initialize` → 500, and 200 once the header is supplied. dev-daily is the posture the module docstring names as this surface's target.
IMPORTANCE: high -- every browser request to `{prefix}/packs`, `{prefix}/packs/initialize` and `{prefix}/packs/{id}/{ver}` on any `deployed_posture()` replica; `local` and `wiring_local` are unaffected (both fall back to a default tenant). `assistants_api.py:167` shares the missing guard but its callers are API clients that do send the header, so the page is the first surface that structurally cannot. No external consumer yet (routes are new in v2.5.1, unpushed).

**[DEFECT] `plato/static/packs.html:351`** — `load()` maps every non-2xx to `data = null`, which `render` reports as "Pack store not readable", so a tenant/posture refusal is displayed as a store fault; `remove()` and `seed()` in the same file read `body.detail`. Trigger: any request the finding above 500s, or the `_store` 400 ("could not resolve a tenant") — the page blames the pack store for a request problem.
IMPORTANCE: low -- one wrong line on the operator page, and the primary trigger disappears when the finding above is fixed; the read path is the only one of the file's three fetches that discards `detail`.

**[SYMMETRY] `plato/seed.py:116`** — the "already published?" pre-check re-derives `publish`'s primary key in the caller and drops its `.strip()` (`jazzx_sdk/pack/store_db.py:147-148` does `str(...).strip()`), so the two disagree on any manifest whose `pack_id`/`pack_version` carries surrounding whitespace: `store.get(" dscr-core ", ...)` misses, `publish` strips and refuses the duplicate, and a second seed run reports `failed: ValueError` instead of `skipped: already published`. Lining up means either stripping here too or asking the store, which is no more code than the current two lines.
IMPORTANCE: low -- reachable only via a hand-authored manifest with padded values; the five bundled packs are clean, and the only caller is `POST {prefix}/packs/initialize` in a relaxed posture.

**[NOTE] `plato/packs_api.py:42`** — the "Never raises" contract is enforced for `list_packs()` and the row transform, but `sorted(latest, key=lambda r: r.pack_id)` at line 54 and the `record.domain`/`record.segment`/`record.version` reads in the `packs.append` block sit outside both guards. Not reachable through `PackVersionRecord` (frozen, all fields defaulted), so the guarantee holds for every shipped store — it is the guard placement, not the behaviour, that is narrower than the sentence.

**[NOTE] `plato/packs_api.py:36`** — `_MANIFEST_KEYS`' comment says "these are the ones an operator reads", but `packs.html`'s `versionTable` renders no manifest column at all, so the reduced `manifest` (and the `depends_on` it duplicates from the sibling key on line 84) is payload nothing on the page consumes.

VERDICT: BLOCK
