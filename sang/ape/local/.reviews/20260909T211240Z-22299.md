# adversarial review -- japes

- when: 2026-09-09T21:12:40Z
- range: HEAD~1...HEAD (cfad916)
- diff: 3119 lines


## whole

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## Findings

**[DEFECT] `jazzx_sdk/pack/pack.py:170`** — `policy_profiles` re-keys `_profiles_by_path` by `profile_id`, so two declared profile files sharing a `profile_id` silently collapse to one and `policy_profile()` returns whichever parsed last, with no default declared and no warning.

Measured trigger: `profiles/a.yaml` = `profile_id: dup, institution_ref: acme, thresholds.x: '1.20'` and `profiles/b.yaml` = `profile_id: dup, institution_ref: beta, thresholds.x: '9.99'`, `profiles: {dir: profiles}` → `len(_profiles_by_path) == 2`, `list(policy_profiles) == ['dup']`, `policy_profile()` takes the `len(profiles) == 1` branch and returns `beta` / `9.99`. That is precisely the "a run silently uses another institution's thresholds" outcome the same method's docstring says it refuses to produce, and the shape is the one `profile_files`' docstring advertises ("a pack can carry one per institution") — copying a profile file and editing `institution_ref` while leaving `profile_id` is the likely authoring slip. `_profiles_by_path:177` also claims "one load, both views, so the two cannot disagree about which profiles a pack has"; here they disagree 2 vs 1.

IMPORTANCE: medium -- silent wrong thresholds, but I enumerated every in-repo caller of `policy_profile`/`policy_profiles` (`plato/packs/domain.py:127` reads `policy_profiles` only; everything else is `tests/test_pack_policy_profile.py`), and no pack in this repo declares two profiles. Both accessors are new and exported in this diff, so nothing outside the repo depends on them yet; the intended consumers (per the docstring) are the ones who would hit it.

**[DEFECT] `jazzx_sdk/pack/pack.py:188`** — `policy_profile(profile_id)` returns `profiles.get(profile_id)`, so an id the pack does not declare comes back as `None`, indistinguishable from the documented "``None`` when the pack declares none".

Measured trigger: pack declaring `acme_v1` and `beta_v1`, `policy_profile("no-such-profile")` → `None`. A caller's mistake (a stale pinned id after a republish) is returned as the ordinary "this pack has no profile" result, and a caller writing `pack.policy_profile(pinned) or pack.policy_profile()` then silently gets the pack's default institution — while the no-id path raises `ValueError` rather than guess. The docstring only attributes `None` to the empty-pack case.

IMPORTANCE: medium -- reachable by any consumer naming a profile, but no shipped in-repo caller passes an id (grepped: only tests), so today the cost is a confusing `None`.

**[SYMMETRY] `plato/packs/domain.py:77`** — `PackManifestLoader(root / "pack_manifest.yaml")` picks one manifest name while `jazzx_sdk/pack/store_db._MANIFEST_NAMES` is the constant the store introduced *because* "the store recognises either rather than picking a side"; `_unpack` locates the actual manifest file and then discards which name it found, returning only the directory. Line up with `_MANIFEST_NAMES` (or have `materialize` return the path it located) — one import in place of the literal, no more code than the diff already has.

IMPORTANCE: low -- only reaches a break for a manifest-only-named (`manifest.yaml`) row without `assistant_id`, which `AssistantManifest`'s required field makes unlikely; the cost is a `FileNotFoundError` naming a path that never existed in the archive.

**[SYMMETRY] `plato/packs/domain.py:62` / `plato/packs/loader.py:236`** — the two resolvers spell the same three-step loop twice with the predicate inverted: `"assistant_id" in record.manifest`, the `content_ref is None` skip-and-warn, and `await store.materialize(...)`. The discriminator string lives in two modules, so a later change to how the two pack shapes are told apart (a `pack_kind` column, say) reaches one and leaves the other. One shared helper returning materialized roots for a given shape is smaller than the two copies.

IMPORTANCE: low -- both call sites are new in this diff and both are exercised by `tests/test_pack_store.py`; nothing outside the repo imports either yet.

**[NOTE] `plato/database_api.py:71`** — the comment says "three of seven under `wiring_local`"; this diff adds `pack_version` to `TABLE_SCHEMAS`, making it eight (measured: `describe_database` on a bare `DbStore` reports `tables_registered: 8`), and `plato/wiring_default.py:528` in the same diff says "all eight".

IMPORTANCE: low -- comment only.

**[NOTE] `plato/database_api.py:79`** — `register_all(store)` mutates its argument on a read-only reporting path: measured, a bare `DbStore` goes from 0 to 8 registered metadata objects across one `describe_database` call. Benign in-process today (I grepped `plato` for `create_all`/`.metadata` — the only `create_all` on a live store is `wiring_default.swap_database`, which builds a fresh one, and `migrations/env.py:55` runs in a separate process), but a reporting endpoint changing what a later `create_all` would build is a surprising coupling for a function documented as describing state.

IMPORTANCE: low -- no shipped in-process consumer of the mutated list.

**[NOTE] `jazzx_sdk/pack/manifest_loader.py:247`** — `profile_files` passes no `keep`, so every `.yaml`/`.yml` in `profiles/` is fed to `PolicyProfile.from_yaml`; because `Pack._profiles_by_path` is a `cached_property`, one unparseable file in that folder fails the whole `policy_profiles`/`policy_profile` surface rather than being skipped, which is the opposite of `policy_files`' `keep=is_policy_document` behaviour in the same helper. Called deliberate in the docstring, so flagging rather than claiming a defect.

IMPORTANCE: low -- `plato/packs/domain.py:126` already catches it per pack; a consumer calling `policy_profile()` directly gets the raise.

## CHECKED
- `store_db.py TODO(publish-tick-tie)` — holds: grepped, no shipped caller invokes `PackVersionStore.publish` (only `tests/test_pack_store.py`), and two awaited DB round-trips inside one `datetime.now()` microsecond is not reachable.
- `database_api.py TODO(partial-registration-marker)` — holds: a real `DbStore` registers all 8 (measured), so only a duck-typed store lacking `.repository` reaches the `except`.
- `posture.py TODO(if-match-post)` — holds: `require_if_match` appears only in `plato/posture.py` and `tests/test_plato_preconditions.py`; no route wires it.
- `migrations/env.py TODO(migration-concurrent-index)` — holds: no migration in `plato/migrations/versions/` creates an index concurrently.
- `wiring_default.py TODO(swap-schema-placement)` — holds: `swap_database` is reachable only via `/v1/config` in a relaxed posture, and the duplicate-table conflict needs a later alembic run on the same database.

VERDICT: BLOCK
