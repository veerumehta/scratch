# adversarial review -- japes

- when: 2026-09-08T19:40:53Z
- range: 143749f..HEAD (since the last review) (67b1bea)
- diff: 41 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Step 1 — prior round

WAS: `plato/wiring_default.py:503` schema placement on non-sqlite swap — **STILL OPEN, deferred**: the diff adds `TODO(swap-schema-placement)`. Checked below; the deferral's claim holds, so not re-reported as a finding.

WAS: `tests/test_plato_wiring_default.py:452` retired store left undisposed — **FIXED** at `:446` / `:459`; the retired handle is captured before the swap and disposed after `current`, before the assert, so a failing assert still disposes both.

## CHECKED

`plato/wiring_default.py:503` `TODO(swap-schema-placement)` — claim holds. `swap_database` has one shipped caller (`plato/app.py:184` via `_rewire`), reachable only through `POST /v1/config`, which refuses writes under strict posture; the docstring at `:487-490` already stakes out `create_all`-over-alembic on this path for the same reason. Nothing outside this repo calls it. The TODO does not undersell it: it names the double-copy and the missing `search_path` explicitly.

## Step 2 — new findings

No defects.

**[NOTE]** `tests/test_plato_wiring_default.py:443-445` — the comment says `swap_database` "keeps the retired store referenced for in-flight work", but the code does the opposite of keeping a reference: `wiring_default.py:513` is a bare `del previous`, dropping the last non-caller handle. What is actually true is that it is *not disposed*, so an in-flight session's own reference keeps it alive; a reader following this comment into `swap_database` looking for the retaining reference will not find one. (The function docstring at `:492-495` states the same thing the same way, so the diff is copying an existing imprecision rather than inventing it.)
IMPORTANCE: low -- comment-only, in a test; the disposal it justifies is correct either way. No shipped behaviour depends on the distinction.

**[NOTE]** `tests/test_plato_wiring_default.py:411` — the fix landed on one of the two same-shaped tests. Enumerated all three `swap_database` tests: `test_plato_settings_store.py:151` already disposes both (`:158-159`), `:196` swaps into a failure so there is no second store, and `test_a_database_swap_clears_its_own_report` (`:395-417`) still disposes only `wiring.db.current`, leaving the `a.db` store built by `build()` with a live aiosqlite thread — the exact state the diff's new comment describes.
IMPORTANCE: low -- test-only, and the twin has not flaked; no shipped-code defect it would have caught. A `TODO` on `:417` is a complete response.

VERDICT: PASS
