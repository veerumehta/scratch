# adversarial review -- japes

- when: 2026-09-09T03:10:18Z
- range: origin/dev...HEAD (b46428c)
- diff: 944 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(jwks-error-detail)` (`plato/oidc.py:~300`): claim holds. `grep -rn "TokenVerifier\|OidcSettings" plato jazzx_sdk` outside `plato/oidc.py` → no hits, so nothing mounts it in a request path and the leak depends on middleware not yet written.
- `TODO(malformed-jwks-clock)` (`tests/test_plato_oidc.py`): claim holds. Both shapes (`{"keys": None}`, top-level list) raise inside the same `except` that stamps `fetched=True`, and `test_a_down_issuer_is_not_refetched_on_every_verify` exercises that same stamp with an advancing clock, including expiry.

## Findings

**[DEFECT]** `jazzx_sdk/pipelines/policy_extract.py:581` — `default_step_error` cannot construct: `RefusalClass.INSUFFICIENT_EVIDENCE` does not exist on `jazzx_sdk/fabric/canonical/refusal.py:21` (members are `SUB_CONFIDENCE_EVIDENCE`, `HUMAN_ONLY_ACTION`, `PRECONDITION_FAILED`, `AUTHORITY_EXCEEDED`, `OUT_OF_SCOPE`, `POLICY_CONFLICT_UNRESOLVED`), and `refusal_class=`/`reason=` are not the model's fields either (`reason_code`/`reason_class`/`message` are, and the first two are required). Trigger, measured: `run_policy_extract(..., on_step_error=default_step_error)` plus any failing step → `AttributeError: type object 'RefusalClass' has no attribute 'INSUFFICIENT_EVIDENCE'`, raised *inside* the engine's `except` block (`jazzx_sdk/conductor/engine.py:559-563` calls `on_step_error` there), so the opt-in "degrade to a typed Refusal" becomes a crash that replaces the original exception. The diff edited this function's message line and the four working siblings in the same pass.
IMPORTANCE: medium -- exported at `policy_extract.py:103`; ran all five handlers, the other four (`document_ingest`, `vocabulary_build`, `financial_spread`, `chat`) return correctly and each has a test, this one has none; zero callers in japes, jaci, or any repo under `/Users/sangit/src`, so it is loud-on-first-use rather than currently live.

**[SYMMETRY]** `jazzx_sdk/server/inbound.py:135-137` — `reason`'s own docstring says callers put the result in HTTP response bodies, and this diff routed 15 such sites through it; this one still puts bare `str(e)` into both `payload.data.error` and `error.message` of the response envelope, so it is the one caller-facing error string in the family with no `redact_secrets` pass. `"message": reason(e)` is the same size as the change made everywhere else (it also drops the separate `"type"` key's redundancy, so keep or drop that as you prefer).
IMPORTANCE: medium -- reachable from any handler exception on the `/inbound` route in server mode; unlike the OIDC code this one is mounted, and the response goes to whoever called it.

**[SYMMETRY]** `jazzx_sdk/clients/knowledge_hub_client.py:78` — `_blob_failure` keeps the exact `f"{type(error).__name__}: {…}"` shape the diff replaced, on the one module where the motivating exception (httpx `ReadTimeout`, empty `str()`) actually originates, so it still renders `"ReadTimeout: "` with a dangling colon. Not a straight swap: it scrubs with `redact_urls`, not `redact_secrets`, so aligning means keeping that scrub rather than calling `reason` directly.
IMPORTANCE: low -- cosmetic there, since the type is already named; `materialize` logs it at WARNING and `plato/logs_api.py` serves that buffer, so the blast radius is one confusing log line.

**[NOTE]** `jazzx_sdk/failures.py:98-105` — the comment says the secret is "bounded by the last `@` rather than by what a password may contain", but `[^\s?#]*` still bounds it by `?`, `#`, and whitespace: measured, `redact_secrets("postgresql://u:p#w@h/db")` and `redact_secrets("postgresql://u:p?w@h/db")` return their input unchanged, password included (the `%23`-encoded form redacts fine).
IMPORTANCE: unknown -- I could not establish a producer that puts a bare unencoded DSN into exception or log text: the installed SQLAlchemy masks the password in `repr(url)` (`postgresql+asyncpg://u:***@h/db`) and its `ArgumentError` no longer quotes the input string. Reachable only via a caller passing such text to `redact_secrets` directly.

**[NOTE]** `jazzx_sdk/failures.py:253` and `tests/test_plato_info_api.py:86` — both say the old `f"{type(exc).__name__}: {exc}"` left an operator with nothing; it left them the exception type, which is most of the diagnosis. What the change actually buys is the dangling colon, plus redaction and one-lining, which the rest of the docstring already states accurately.
IMPORTANCE: low -- comment text only; no behavioural claim depends on it.

**[NOTE]** `plato/database_api.py:65` vs `plato/info_api.py:310,320` — the two state endpoints now cap differently: `describe_database` bounds both branches at `limit=200`, while `schema_status` puts an uncapped driver message into the same kind of JSON body.
IMPORTANCE: low -- both routes are operator-facing state views; the cost of the difference is response size, not disclosure, since both go through the same redaction.

VERDICT: BLOCK
