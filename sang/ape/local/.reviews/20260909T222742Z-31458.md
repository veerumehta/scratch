# adversarial review -- japes

- when: 2026-09-09T22:27:42Z
- range: HEAD~1...HEAD (eddcb08)
- diff: 3752 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED** (TODO claims that hold — not raised as findings)

- `TODO(migration-concurrent-index)` — `plato/migrations/env.py:190`: the offline branch it names does wrap in `context.begin_transaction()` (`env.py:86`). Claim holds.
- `TODO(if-match-post)` — `plato/posture.py:97`: `require_if_match` appears only in `posture.py` and `tests/test_plato_preconditions.py`. No route wires it. Claim holds.
- `TODO(partial-registration-marker)` — `plato/database_api.py:81`: every in-repo caller reaches `describe_database` through `DatabaseHandle` → real `DbStore`. Claim holds.
- `TODO(share-published-row-loop)` / `TODO(compose-per-pack-guard)` — `published_registries` and `published_domain_packs` have no non-test callers (grepped). Claims hold.
- `TODO(swap-schema-placement)` / `TODO(publish-tick-tie)` — see finding 2 for the part of the latter that does not hold.

Suite state: `test_pack_store / test_pack_roots / test_pack_policy_profile / test_evaluation_config_paths / test_modes_framework / test_plato_* / test_queue_* / test_events / test_import_boundary` → 325 passed, 3 skipped. `ruff` gains no new errors (E501 is not in the selected set; the 102-char `record_boot_note` line at `plato/wiring_default.py:207` is unenforced).

---

**1. [DEFECT] `jazzx_sdk/pack/store_db.py:319`** — the cache-marker containment check is lexical on unresolved paths, so `Path.is_relative_to` accepts a `..` that climbs out; the comment two lines above claims "anything naming a path outside the cache entry is ignored rather than followed", which the code does not enforce.

Trigger: a `.pack_manifest_root` in `gettempdir()/japes-packs/<digest16>/` containing `/tmp/japes-packs/<digest16>/../../elsewhere`. Measured: `Path('/tmp/japes-packs/abcd1234/../../evil').is_relative_to(Path('/tmp/japes-packs/abcd1234'))` → `True` (pathlib does not collapse `..`). `materialize` then returns that path as a pack root, and the marker branch returns *before* the digest re-verification, so nothing else notices. `_unpack` at line 390 in the same file gets this right (`.resolve()` on both sides); `path_safety.relative_name` already rejects a `..` part for exactly this reason.
IMPORTANCE: medium -- I enumerated every caller of `PackVersionStore.materialize`: `plato/packs/domain.py:89` and `plato/packs/loader.py:259`, and both of those are reached only from `tests/test_pack_store.py`, so no shipped Plato route hits it today. It is reachable from outside this repo: `DbPackVersionStore` is a public class and the only construction site (`plato/models.py:49`) passes no `cache_root`, so the shared `/tmp/japes-packs` default is the production path.

**2. [DEFECT] `jazzx_sdk/pack/store.py:130`** — `list_packs` is documented as "the most recent version of each pack" but `store_db.py:281` orders by `published_at.desc()` and `setdefault`s, so it returns the most recently *published* row; `TODO(publish-tick-tie)` defers only the same-microsecond tie, and this needs no tie.

Trigger: `publish(dscr-core 0.2.0)`, then later `publish(dscr-core 0.1.1)` (an ordinary backport to an older line). `list_packs()` returns `0.1.1`, and `published_domain_packs` / `published_registries` compose the older pack while the docstring promises the newest version. The part of the deferral that does not hold is "needs two `publish` round-trips inside one microsecond" — out-of-order publication is a normal single-call sequence. `store_db.py:278`'s comment repeats the same wrong claim.
IMPORTANCE: medium -- checked both readers of `list_packs` (`plato/packs/loader.py:255`, `plato/packs/domain.py:81`), both test-only today, plus `versions()` which is correctly `asc` and unaffected. Fixing it is either honest wording on the two docstrings or ordering on a parsed version; the wrong shape is cheap now and gets baked into a publish route later.

---

**3. [SYMMETRY] `jazzx_sdk/pack/loader.py:115` and `:159`** — `DomainPackFabric` joins two manifest-declared asset paths (`ontology.schema_path`, `policy_bundle.path`) onto `self.pack_path.parent` and reads them, unguarded. That is the same family this diff just closed in `PackManifestLoader._asset_path` (`manifest_loader.py:155`) across eleven joins; `pack/loader.py` is the sibling module in the same package, exported from the same `jazzx_sdk/pack/__init__.py` this diff edits. The consistent version is `relative_name(schema_path, what="ontology schema path")` at each — two lines, strictly smaller than what the diff already did.
IMPORTANCE: low -- I checked what reaches it: `DomainPackFabric` takes a filesystem `pack_yaml_path` from its caller, so no published/untrusted manifest arrives here the way `store_db.materialize` now delivers one to `PackManifestLoader`. No in-repo caller; it is public API for consumers.

**4. [SYMMETRY] `plato/packs/loader.py:100`** — `if not str(target).startswith(str(root.resolve())):` is the weaker spelling of the guard `store_db._unpack` (`store_db.py:392`) was written with the opposite lesson: "`is_relative_to`, not `startswith`: without a separator, `../<same-prefix>extra/x` resolves to a sibling of `root` whose string still starts with root's". Same repo, same purpose, one line to line up.
IMPORTANCE: low -- the comment above it already scopes the guard to `MappingPackSource` (directory and bundle sources are covered elsewhere), and `root` is a `tempfile.TemporaryDirectory()` whose random basename a caller would have to guess to exploit the prefix gap, so I could not construct a trigger. Raised because the diff states the rule and leaves the sibling on the old one.

---

**[NOTE] `jazzx_sdk/pack/store_db.py:401`** — `MANIFEST_NAMES` is a fact about pack-directory layout, not about the DB store, but it lives in the sqlalchemy-importing module; `plato/packs/domain.py:83` imports `jazzx_sdk.pack.store_db` solely to read that tuple. `store.py` (host-free, already imported there for `is_assistant_pack`) is where it fits.
IMPORTANCE: low -- one import edge, no behaviour.

**[NOTE] `plato/database_api.py:83`** — `register_all(store)` mutates the live store's registered metadata as a side effect of a read-only reporting route. Benign in the two shipped wirings (`wiring_local` runs alembic to head, so all eight tables exist and count cleanly — verified by the new `test_plato_info_api` test), but any later `create_all()` on that same store instance would now build tables this process has no store for.
IMPORTANCE: low -- checked: the only `create_all()` calls are at boot (`wiring_local`) and on a freshly built store (`swap_database`), neither after a describe.

**[NOTE] `jazzx_sdk/pack/pack.py:262`** — "declares N policy profiles ... and no `profiles.default`; name one" is unactionable for the list form, which has nowhere to write a `default`. Trigger: `profiles:\n  - a.yaml\n  - b.yaml`, which `profile_files` accepts (`manifest_loader.py:264`).
IMPORTANCE: low -- one confusing message; the dict form is what every fixture and the docstring example use.

VERDICT: BLOCK
