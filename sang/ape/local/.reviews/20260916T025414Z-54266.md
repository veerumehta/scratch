# adversarial review -- japes

- when: 2026-09-16T02:54:14Z
- range: f4051ff..HEAD (since the last review) (4a6ce36)
- diff: 552 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/migrations/env.py :: memory-store-not-shared -- FIXED (refused up front by `UnmigratableStoreError`; `upgrade_to_head(DbStore(backend="sqlite", sqlite_path=None))` now raises before alembic runs, and the 18 tests in `test_plato_schema_version.py` pass). A neighbouring input it does not cover is a new finding below.
WAS: plato/api/database.py :: audit-line-precedes-the-write -- FIXED (the past-tense line is after the write; `test_a_failed_migration_is_not_audited_as_a_successful_one` fails against the old order).
WAS: plato/static/info.html :: posture-gate-on-one-button-only -- FIXED (all four non-GET controls in `plato/static/*.html` are now gated: `dashboard.html:523` PATCH, `info.html:335` POST, `packs.html:286` DELETE, `packs.html:387` POST). One of the four uses the wrong predicate, reported below under its own slug.
WAS: plato/boot/schema_version.py :: absent-identity-asserts-common -- FIXED (`getattr(db, "backend", None)` plus the `store is None` skip keeps `env.py`'s environment fallback).
WAS: plato/wiring/settings_store.py :: importer-count-now-three -- FIXED.
WAS: plato/migrations/env.py :: offline-dialect-from-empty-url -- FIXED (the dialect comes from `db.backend`; `db` is constructed in this module so the attribute always exists).

[DEFECT] plato/boot/schema_version.py:153 :: literal-memory-path-not-refused -- the guard tests the path for truthiness, so the one sqlite path that names in-memory explicitly, `":memory:"`, passes it and reaches exactly the failure the new exception exists to prevent; `unmigratable_reason`'s "or ``None`` when it can be migrated" is false for that input.
`PLATO_SQLITE_PATH=:memory:` -- a text field an operator can edit at runtime from the dashboard config panel (`plato/api/config.py:79`, a `BOOTSTRAP_KEY` so `_rewire` rebuilds the engine from it) and the value `docs/DEPLOYMENT_ENV.md:148` describes as the default in prose. `DbStore` takes the same `StaticPool` branch for it as for `None` (`jazzx_sdk/fabric/db/store.py:195-201`). Run directly: `unmigratable_reason(DbStore(backend="sqlite", sqlite_path=":memory:"))` returns `None`, and `await upgrade_to_head(...)` then raises `SchemaStaleError: ... it was not migrated here` -- the misdiagnosis `UnmigratableStoreError`'s docstring says "reads as the wrong database rather than as no reachable database". `job:migrate` exits with a traceback instead of the 3 its rewritten docstring promises for "one no second connection can reach", and the route answers 500 instead of 503.
IMPORTANCE: medium -- both callers (`plato/jobs.py:99`, `plato/api/database.py:199`) are affected, it fails loudly, and the operator's workaround is to set a file path. Reachable only by configuration, not by any default: `plato/wiring/default.py:177` passes `env("PLATO_SQLITE_PATH")` through unchanged and `plato/wiring/local.py:292` sets a file. Nothing outside this repo calls `unmigratable_reason`.

[DEFECT] plato/static/dashboard.html:501 :: config-save-hidden-where-auth-allows -- the save button is hidden on `STRICT` alone, but `/config`'s PATCH is refused on strict *and unauthenticated*: a strict deployment built with an auth dependency accepts the write and now has no control for it, which is the mirror of the failure the diff set out to fix.
`create_app(auth=<dependency>)` (`plato/app.py:112`, forwarded to `create_config_router` at `plato/app.py:205`) with `JAPES_ENVIRONMENT=production`. `plato/api/config.py:253` then evaluates `withholding(strict=True, auth=<dep>)` as false and returns the real router, so PATCH applies the write and returns 200 -- while `dashboard_html` renders `STRICT=true` and hides `#config-save`. `plato/app.py:183` already names this configuration ("a strict posture or a strict one built with an auth dependency -- not relaxed only, as this said before") as the correction of the identical mistake in that comment. The three siblings are right for the opposite reason: migrate, delete and seed go through `relaxed_only`, which has no auth escape.
IMPORTANCE: medium -- the dashboard is mounted twice (`plato/app.py:283`, `plato/__main__.py:327`), both reachable; `auth` is a public `create_app` parameter and nothing in this repo passes it, so I could not confirm a deployment that hits it, but it is the configuration `_add_refusing_patch`'s own 403 message tells operators to adopt.

[DEFECT] plato/api/database.py:199 :: alembic-failure-unaudited -- the handler enumerates `UnmigratableStoreError` and `SchemaStaleError`, and the success line sits after the `try`, so anything alembic itself raises leaves no record of who triggered the migration; before this diff the pre-write line covered that path.
A migration that raises inside `command.upgrade`: a DDL failure in a revision, or the statement timeout `plato/migrations/env.py` sets for an ALTER queued behind a live reader on Postgres. `upgrade_to_head` lets it out of `asyncio.to_thread`, neither `except` matches, and the route returns 500 with `actor` computed at line 196 and never written -- for an operation the docstring calls irreversible and which may have applied part of the revision chain. Both branches that do log are at least as easy to satisfy with the `except` list extended or a `raise` re-logged.
IMPORTANCE: medium -- one route, mounted unconditionally at `plato/app.py:222`, relaxed postures only; the caller sees the 500, so the gap is in the log a reviewer reads afterwards, not in the response. `delete_pack_version` has the same success-only shape but none of its failure paths write.

[NOTE] plato/api/info.py:435 :: details-page-strict-replace-is-dead -- the details page has no `__STRICT__` in it (`grep -rn __STRICT__ plato/static` matches only `packs.html:192` and `dashboard.html:214`) and no Delete or Initialize controls, so the replace is a no-op and the comment copied from `packs_html` describes a different page; `info.html` gates its one control on the fetched `strict !== false` instead.
IMPORTANCE: low -- a dead string substitution on one route; `test_the_posture_reaches_the_pages_that_gate_on_it` deliberately covers only `dashboard_html` and `packs_html`, so nothing asserts the claim.

[NOTE] plato/static/dashboard.html:211 :: patch-405-claim-is-false -- "`/config`'s PATCH is not registered at all in a strict tier, so the save button there fails as a 405" is contradicted by `plato/api/config.py:262`, where `_add_refusing_patch` registers a PATCH that answers 403 precisely so the route is not absent ("A 403 naming the posture is a better answer than an absent route, which reads as a bad URL"); the same claim is restated in `tests/test_plato_info_api.py:641`.
IMPORTANCE: medium -- prose only, but it is the reasoning that produced the gate above and it now sits in a test docstring a later round will read as the rule.

[NOTE] docs/DEPLOYMENT_ENV.md:148 :: sqlite-row-declares-silent-success -- the row still says "Without it the database is in-memory, so a migration reports success and leaves nothing behind", which is the behaviour this diff removed from both in-process callers (503 from the route, exit 3 from `job:migrate`).
IMPORTANCE: low -- still accurate for `alembic -c plato/alembic.ini upgrade head` from a shell, which reads the environment and passes through no guard; the table section is hand-maintained (only the boot-contract table is generated, `tests/test_plato_boot_contract.py:80`), so nothing fails.

VERDICT: BLOCK
