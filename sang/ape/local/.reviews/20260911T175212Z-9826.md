# adversarial review -- japes

- when: 2026-09-11T17:52:12Z
- range: 625f6c9..HEAD (since the last review) (54e69f4)
- diff: 39 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_mock_knowledge_hub_app.py :: uniformity-attributed-to-wrong-guard -- FIXED; the comment now attributes the two-field `fields.get(...) is not None` to the route (verified: blank guard at mock_knowledge_hub_app.py:322 and `moving` at :344 are both over `("name", "collection_id")` only) and the last two fields to the delegate (`if ontology_id is not None` / `if entity_type is not None`, clients/mocks.py:1636-1639). The "cites this test for all four" clause also checks out: the sibling parametrizes all four fields and its docstring names this test as the 200-side proof.
WAS: tests/test_mock_knowledge_hub_app.py :: nulled-field-unasserted -- STILL OPEN; the two added cases still assert only `collection_id == "col-1"` and the `"name" not in patch` arm, so neither `ontology_id` nor `entity_type` is asserted unchanged and both cases would pass with the delegate's `is not None` guards on those fields removed.
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: todo-any-value-includes-null -- FIXED; the TODO now reads "a non-null, non-string JSON value still passes" and says explicitly that null is refused, so a detached read no longer implies the closed hole is open.

The diff changes only comment text (no executable line in either file), so the previous round's `pytest tests/test_mock_knowledge_hub_app.py` → 38 passed still stands unchanged; I did not re-run it.

[NOTE] jazzx_sdk/server/mock_knowledge_hub_app.py:233 :: refusing-line-points-upward -- "Null is not among them: the line above refuses it" points the wrong direction: the null refusal is `entity_data.get(f) is None` on line 238, five lines *below* the TODO, and everything above the TODO is prose, not code.
A reader checking the claim scans up from line 233 through the two comment paragraphs, finds no predicate, and either concludes the refusal is somewhere else in the module or re-derives it from the guard below; the guard is correct, so nothing but the lookup is lost.
IMPORTANCE: low -- comment only, inside `create_knowledge_hub_mock_app`; the only readers are this file and the review loop, and nothing outside this repo parses it.

VERDICT: PASS
