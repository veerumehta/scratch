# adversarial review -- japes

- when: 2026-09-17T00:39:28Z
- range: e52aa49..HEAD (since the last review) (ac3ee15)
- diff: 290 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/wiring/default.py :: wiring-note-not-dropped-on-swap -- FIXED. `_durable_settings_degraded` no longer records a note at all, and `record_boot_note`'s `from_wiring` docstring now states the real lifetime; `_WIRING_NOTES.clear()` is at the top of `build()` (`:443-444`), and `swap_database` contains no `build()` call, so "dropped at the start of the next one" checks out.
WAS: plato/wiring/default.py :: blob-complaint-order-claim -- FIXED. `pack_archive_complaints()` returns both azure conditions and `_pack_blob` logs each; `test_the_boot_log_names_every_blob_gap_not_just_the_first` asserts both strings and passes, and `pack_archive_complaint()` (the 503) still returns only the first.
WAS: tests/test_plato_wiring_default.py :: posture-list-still-handwritten -- FIXED. `_POSTURE_VARS = _POSTURE_ENV_VARS`; no hand-written posture tuple remains in the three test modules.
WAS: plato/wiring/default.py :: unreachable-layer-still-configured-true -- STILL OPEN, and now deliberate: an unreachable durable layer is swallowed in `apply_to_environment` and `/info` answers `configured: true`. That is the shape settled as `plato/wiring/default.py :: settings-failure-not-at-info`, so nothing further from me.
WAS: plato/__main__.py :: refusal-matched-by-substring -- FIXED. Shared `UNREACHABLE_PREFIX`, matched with `startswith`; `check_schema` is the only producer of `refusal` and builds the string from the same constant, so the two cannot drift.

ACCEPTED: plato/wiring/default.py :: settings-failure-not-at-info

[NOTE] plato/wiring/default.py:942 :: reviews-path-not-in-repo -- the docstring says the trade is "Recorded in `.reviews/accepted.md`", but no `.reviews/` exists anywhere in this tree; the file it means is `/Users/sangit/src/.scratch/sang/ape/local/.reviews/accepted.md`, outside the repo, so the pointer resolves to nothing for anyone reading the checkout. Same citation in `tests/test_plato_wiring_default.py:756`.
IMPORTANCE: low -- documentation only, two sites, both checked by `find . -name 'accepted*.md'` over the repo and `ls -a` at the root. The entry itself does exist in the scratch file (line 65), so the acceptance is real; only the path is unresolvable.

[NOTE] plato/boot/schema_version.py:51 :: prefix-comment-says-substring -- the comment reads "matched as a bare substring, rewording this sentence would drop that diagnosis with nothing failing at the edit", which is the state the diff just left: `plato/__main__.py:170` now does `refusal.startswith(UNREACHABLE_PREFIX)`, and rewording the constant drops nothing. Intended as a counterfactual, it parses as a description of current behaviour.
IMPORTANCE: low -- one comment; the only consumer of the constant is `check_schema_or_report`, plus three literal spellings in `tests/test_plato_schema_version.py` (171, 529, 567) which pin the wording and should stay literal.

[NOTE] plato/wiring/default.py:934 :: health-exits-on-ready-not-configured -- "`python -m plato --health` exits on `configured`" is not what the code does: `_health` probes `/health` and keys on `body["ready"]` (`plato/__main__.py:273`), never reading `/info`. The conclusion survives, because `create_app`'s `readiness_provider` is the same full `degraded` list (`plato/app.py:255`), so a note still fails the probe -- but the named field is wrong for a reader verifying the trade.
IMPORTANCE: low -- docstring only; checked `Dockerfile:156-157` (HEALTHCHECK is `python -m plato --health`), `_health`, and `info.py:509`.

VERDICT: PASS
