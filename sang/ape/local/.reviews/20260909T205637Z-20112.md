# adversarial review -- japes

- when: 2026-09-09T20:56:37Z
- range: HEAD~1...HEAD (9017218)
- diff: 3063 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(publish-tick-tie)` (store_db.py:83) — holds: no non-test caller of `publish` anywhere in `jazzx_sdk`/`plato`.
- `TODO(partial-registration-marker)` (database_api.py:80) — holds: both in-repo callers are database_api.py:162,165 passing the router's `db`; only the new test passes a bare `DbStore`.
- `TODO(migration-concurrent-index)` (migrations/env.py:182) — holds: no `CONCURRENTLY` in any of the three revisions.
- `TODO(if-match-post)` (posture.py:97) — holds: `require_if_match` appears only in `tests/test_plato_preconditions.py`.
- `TODO(swap-schema-placement)` (wiring_default.py:527) — holds: `TABLE_SCHEMAS` is exactly eight entries and no model carries `schema=`.

## Findings

**[DEFECT] `jazzx_sdk/pack/pack.py:201`** — `policy_profile(profile_id)` returns `profiles.get(profile_id)`, so an id that does not exist comes back as `None`, which the docstring three lines above defines as "the pack declares none"; the caller's mistake is returned as an ordinary result while the *less* ambiguous no-id case raises.
Trigger: `pack.policy_profile("dscr_v6")` on the pack whose profile is `dscr_v6_15_2026_v1_1` → `None`, and the caller runs with no thresholds instead of being told the id is wrong.
IMPORTANCE: medium -- exported `Pack` API; in-repo callers are `plato/packs/domain.py:113` and the new tests, neither of which passes an id, so the reachable caller is an external consumer (jaci/acra), which the docstrings name as the intended one.

**[DEFECT] `jazzx_sdk/pack/pack.py:162,218`** — `policy_profiles` is keyed by `profile_id`, so two declared files sharing an id collapse to one entry and `len(profiles) == 1` then returns it silently; that is the "picking the first of an unordered set is how a run silently uses another institution's thresholds" the same function says it refuses, and it falsifies `_profiles_by_path`'s claim that the two views "cannot disagree about which profiles a pack has" (2 paths, 1 id).
Trigger: `profiles/` containing `acme.yaml` and `beta.yaml` both `profile_id: acme_v1` with different `institution_ref`/`thresholds`, no `profiles.default` → `policy_profile()` returns `beta.yaml`'s (last in the sorted glob) with no warning.
IMPORTANCE: medium -- silent wrong thresholds, but needs a mis-authored pack; same call-site set as above.

**[DEFECT] `plato/packs/domain.py:77`** — hardcodes `root / "pack_manifest.yaml"` while `store_db._unpack:360` deliberately accepts either `pack_manifest.yaml` or `manifest.yaml`, so a row the store considers valid raises `FileNotFoundError` out of `PackManifestLoader.__init__` and aborts the whole tenant's list — the exact failure `published_registries` was written to warn-and-skip, per its own docstring.
Trigger: `publish()` a manifest with no `assistant_id` whose zip names the manifest `manifest.yaml`; `published_domain_packs` raises instead of skipping it.
IMPORTANCE: low -- no non-test caller of `published_domain_packs` exists yet (enumerated: only `tests/test_pack_store.py`), so nothing shipped reaches it; a `TODO` is a complete response until it is wired.

**[DEFECT] `jazzx_sdk/pack/store_db.py:309`** — the cache short-circuit returns `Path(marker.read_text().strip() or root)` with no containment check, so the pack root is whatever a file in a predictable shared directory says; the default `cache_root` is `tempfile.gettempdir()/japes-packs/<digest[:16]>`, and `_unpack` is skipped entirely when the marker exists. The in-code deferral above covers trusting an already-unpacked *tree's contents*, not the returned *pointer* leaving the cache dir — and `Pack._resolve` (pack.py:33) `importlib.import_module`s manifest pointers from whatever directory comes back.
Trigger: any local process pre-creates `/tmp/japes-packs/<digest16>/.pack_manifest_root` containing `/tmp/attacker-pack`; `materialize` returns that path and the policies, prompts and module pointers loaded from it are the attacker's.
IMPORTANCE: low -- `materialize` has no non-test caller today (enumerated: `plato/packs/loader.py:published_registries`, `plato/packs/domain.py`, both themselves uncalled outside tests), and no shipped site constructs `DbPackVersionStore` with a blob; rating is low on reachability, not on consequence, which is code execution once wired. Ignoring the marker unless `Path(...)` is inside `root` is one line.

**[SYMMETRY] `jazzx_sdk/modes/prompt_loader.py:84,90,132`** — `bare_name` was added to `pack_id` in the join at line 79 but `mode_name` sits in the same `read_text` join and in three others, so the docstring's reason ("unguarded it returns an arbitrary file's contents *as a prompt*") still holds for the other half of the same expression. The consistent version is the same one call to `jazzx_sdk.path_safety.bare_name` the diff already added, and it is the reasoning the diff applies to `run_id` in `evaluation/harness/config.py:172` — an argument to a public method, guarded where it lands.
IMPORTANCE: low -- every in-repo caller reaches these through `prompt_resolver`/`make_prompt_resolver` with literal contract names (`governor`, `investigator`, …), so no shipped configuration passes a caller-supplied `mode_name`; both functions are in `jazzx_sdk.modes.__all__`, so an external consumer could.

**[SYMMETRY] `jazzx_sdk/pack/roots.py:136`** — the three-spelling variant list is now built in two places: here and `PackManifestLoader.from_pack_id` (manifest_loader.py:103-107), whose docstring this one cites as doing "the same reconciliation". Sharing the variant tuple is smaller than the duplicate.
IMPORTANCE: low -- both are new-or-adjacent in this diff and currently agree; the cost is a fourth spelling being added to one only.

## Notes

**[NOTE] `jazzx_sdk/pack/store_db.py:379`** — the containment check is a bare `str.startswith` with no separator, so a member `../<same-16-hex-prefix>extra/x` resolves to a sibling of `root` that still passes the guard whose stated purpose is to fail on a member that *tried*. Harmless today only because `extractall` sanitizes anyway; `Path.is_relative_to(root)` is the exact check.
IMPORTANCE: low -- defense-in-depth only; the same code path as the finding above, which has no shipped caller.

**[NOTE] `plato/packs/domain.py:112-116`** — the docstring says each accessor is read defensively so one malformed pack cannot blank the view, but `pack_id`, `version`, `domain` and `segment` are read outside both `try` blocks.
IMPORTANCE: low -- those four are plain manifest lookups on `PackManifestLoader`, so no raise I can name; the claim is just wider than the code.

**[NOTE] `tests/test_pack_composition.py:69` / `tests/test_pack_policy_profile.py:56`** — both justify adding `profiles` to `FRAGMENT_KINDS` as letting a dependent "narrow to the profile the way it can to policies or playbooks", but `_MERGE_ID_FIELD`/`_MERGE_GETTER` (manifest_loader.py:40-41) cover only playbooks/evidence_types/skills, and `Pack.policy_profiles` reads `self._loader.profile_files()` alone — so a declared `fragments: [profiles]` validates and imports nothing. Consistent with `ontology`/`mode_tuning`, so it is the rationale that overclaims, not the constant.
IMPORTANCE: low -- test docstrings and a filter-value set; no behaviour depends on it.

VERDICT: BLOCK
