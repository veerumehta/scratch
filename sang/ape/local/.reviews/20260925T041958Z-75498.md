# adversarial review -- .

- when: 2026-09-25T04:19:58Z
- range: ee8b2d5f..HEAD (since the last review) (b7939074)
- diff: 135 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/pipelines/jtbdset.py :: stale-whole-flag-truncates -- FIXED. The digest covers the whole `ProcedureCondition`, so an edit to `purpose`, `outcomes`, `steps` or `checks` fails the match and the block is rendered from its structure.
WAS: jazzx_sdk/pipelines/jtbdset.py :: merged-section-name-rewritten -- STILL OPEN. `sections.setdefault(_slug(section), (section, []))` still keeps whichever spelling it sees first. This diff does not touch it.
WAS: jazzx_sdk/pipelines/jtbdset.py :: empty-sequence-now-raises -- FIXED. Nothing in this diff changes it.

[DEFECT] jazzx_sdk/pipelines/jtbdset.py:132 :: digest-omits-title-references -- The verbatim gate hashes only `condition`, but the rendered base block also contains `rule.description` (as the `# ... - Base Version` title) and `reference_documents`, and edits to either are silently dropped.
Trigger: import ARTIFACT, then change the base rule's `description`, or change `domain_extensions["jtbd"]["reference_documents"]`, leaving the condition alone. On export the digest still matches, so COMMON comes back as the original `source_text` with the old title and old references. For the description edit it gets worse: the exported value's `skill` field carries the new description while its COMMON text carries the old one, so the export contradicts itself. The module docstring's claim, "an edited one is rendered from its structure", is broader than what the gate checks. The fix: include `title` and `references` in the digest, both at import and in `_render`.
IMPORTANCE: medium -- an edit is lost from the exported JTBDSet without warning. I checked every call site: `import_jtbdset` and `export_jtbdset` are called only from tests/test_jtbdset.py, and nothing else in the repo edits `reference_documents`. Nothing outside the repo was found depending on them. It is reachable as soon as a policy editor lets someone rename a rule.

[NOTE] jazzx_sdk/pipelines/jtbdset.py:80 :: unused-whole-return-flag -- Both callers of `_procedure` now discard its third return value (`_`), yet the signature and docstring still promise "whether it had no steps and was kept whole". This diff made the flag dead, so it should come out along with its docstring clause.
IMPORTANCE: low -- the only callers are the two inside this module, and neither reads the flag.

VERDICT: BLOCK
