# adversarial review -- japes

- when: 2026-09-18T19:52:22Z
- range: origin/dev...HEAD (8ef8a5a)
- diff: 60 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[NOTE] pyproject.toml:78 :: soupsieve-advisory-misattributed -- the comment says both Dependabot alerts are "in the IDENTIFIER and VALUE selector sub-patterns", but only GHSA-gjv8-xp57-g29c is; the second, GHSA-j934-xhv5-fg8f, is the whitespace/comment trimming regex.
Checked against the GitHub advisory API for `pip/soupsieve`: two open medium advisories, ranges `< 2.9.0`, both first patched in 2.9.0 (published as `2.9`, which `>=2.9.0` admits under PEP 440). The floor itself is right and closes both; only the parenthetical is wrong.
IMPORTANCE: low -- comment text in `pyproject.toml` only. The pin is correct, `poetry check --lock` passes, and the lock resolves soupsieve 2.9.2 with beautifulsoup4 4.14.3 (`soupsieve >=1.6.1`, so the ">=1.6.1" half of the comment holds). No consumer reads this comment.

[NOTE] .github/workflows/Build-Push-Plato-Docker-Image-GHCR.yml:10 :: branch-default-only-on-dev -- the `dev` → `plato` default lands only on this branch; `origin/main` and `origin/plato` still carry `default: 'dev'` in their copy of the same file.
`workflow_dispatch` input defaults are read from the ref chosen in "Use workflow from". Verified via `git show origin/main:` and `git show origin/plato:` -- both still show `default: 'dev'` at line 10. An operator dispatching from `main` (the repo default, `origin/HEAD -> origin/main`) or from `plato` still gets `dev` prefilled until this reaches those branches. `origin/plato` currently contains all of `origin/dev` (`git rev-list --left-right --count origin/dev...origin/plato` → `0 36`), and this commit is the one unpushed commit on local `dev`, so the gap closes on the next dev→plato merge.
IMPORTANCE: low -- single manual-dispatch input default; the field is editable at dispatch time and a wrong branch fails loudly at the `actions/checkout` step. Nothing outside this repo reads it. The sibling `Build-Push-Docker-Image-GHCR.yml` has no `branch` input at all, so there is no same-file-family gap.

VERDICT: PASS
