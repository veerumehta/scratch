# adversarial review -- japes

- when: 2026-09-09T01:33:39Z
- range: 8abeff2..HEAD (since the last review) (f92e76e)
- diff: 128 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Previous round

- WAS: `[SYMMETRY] store.py:123` guard on the key *builder*, escape happens at `put` — **FIXED**; the guard now sits at `store.py:77`, inside `put`, so `offload(key=...)` and every direct `put` pass through it.
- WAS: `[NOTE] store.py:125` `suffix` concatenated unchecked — **FIXED**; `put` validates the assembled key, so a `suffix` carrying `/..` is refused at the write.
- WAS: `[NOTE] tests/test_pack_store.py:305` blob test in the pack-store file — **FIXED**; removed there, replaced by `tests/test_fabric_blob.py:92-132`. 25/25 pass across both files.

## New review

**[DEFECT]** `jazzx_sdk/fabric/blob/store.py:95` (and `:108`) — the comment at `:74-76` says `put` "is where the write happens, so it is the only place every key passes", but `get` and `delete` build `self._local_dir / key` from the same untrusted key with no guard, so `await blob.get("blob://../../../etc/passwd")` returns that file's bytes and `await blob.delete("blob://../../../x")` unlinks it. Reachable today through `DocumentAgent.process_blob` / `materialize_blob_to` (`jazzx_sdk/agents/document/agent.py:396,424`), whose pointer comes straight from `turn.blob_pointers` (`jazzx_sdk/pipelines/document_ingest.py:193-201`), and through `materialize` (`store.py:174`), which forwards any stored dict carrying `__blob__` — a sub-threshold conversation message that already has that key is passed to `get` verbatim by `fabric/conversation_store.py:97`.
IMPORTANCE: medium — I enumerated every `.get(`/`.delete(`/`.materialize(` call site on `BlobStore`: `pack/store_db.py:314` (pointer from a row this store wrote), `agents/document/agent.py:396,424` (caller-supplied), `fabric/conversation_store.py:97`, `runs/store_db.py:120`, `llm/cost_tracker.py:237`→`tests/test_cost_store.py:181`. Nothing in `plato/` or `jazzx_sdk/server` constructs a `DocumentIngestTurn` or exposes `blob_pointers`, so there is no HTTP route to it in this repo; the reachable callers are library consumers outside it (jaci, juno). `delete` has no shipped caller outside tests.

**[NOTE]** `jazzx_sdk/fabric/blob/store.py:135-138` — the `content_key` docstring states flatly that a `prefix`/`suffix` with an absolute path or `..` "is refused by :meth:`put`", but the guard sits inside the `else` (local) branch at `:77`; with `blob_backend="azure"` the key goes to `provider.write_async` unchecked, so the promise holds for one of the two configured backends.
IMPORTANCE: low — no escape semantics for an Azure blob name; costs a wrong-shaped key, not a wrong file.

**[NOTE]** `tests/test_fabric_blob.py:117` — the "nothing was written" assertion scans `tmp_path.rglob("*")`, which by construction cannot observe the escape the `/etc/escaped` case is named for; that case is carried entirely by `pytest.raises(ValueError)`.
IMPORTANCE: low — test-only; the `raises` still fails if the guard is removed (the write would raise `PermissionError`, not `ValueError`).

VERDICT: BLOCK
