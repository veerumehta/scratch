# adversarial review -- japes

- when: 2026-09-11T17:47:38Z
- range: 61ea70d..HEAD (since the last review) (625f6c9)
- diff: 46 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_mock_knowledge_hub_app.py` → 38 passed; probe (`/tmp/probe_nulls2.py`) for the new cases → `PATCH {"ontology_id":null}` 200 with `ontology_id` still `ont-1`, `{"entity_type":null}` 200 with `entity_type` still `flag`.

WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: todo-says-scalar-covers-containers -- FIXED; the TODO now names containers explicitly and re-keys as `non-string-value-passes-guard`, and I re-probed both halves it claims: `POST {..., "name": []}` → 201 keying `"[]"`, `{"name": 0}` → 201 keying `"0"`.
WAS: tests/test_mock_knowledge_hub_app.py :: patch-side-claim-cited-narrowly -- FIXED; the parametrize now covers all four fields, so the sibling docstring's citation matches what the cited test actually exercises.
WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: patch-null-is-200-not-422 / tests :: patch-null-is-200-not-422 / tests :: rejected-create-status-unasserted -- FIXED, unchanged by this diff.

[NOTE] tests/test_mock_knowledge_hub_app.py:363 :: nulled-field-unasserted -- the two added cases never assert the field they null is unchanged, which is the test's own name; both fields are present in the 200 body (`ont-1`, `flag`), so `assert patched.json()["ontology_id"] == "ont-1"` is one line away.
For `{"ontology_id": None}` the body only asserts `collection_id == "col-1"`, `name == "e-1"` (via the `"name" not in patch` arm) and the 409 clash -- none of which touch `ontology_id`. If `MockKnowledgeHubClient.update_entity` (jazzx_sdk/clients/mocks.py:1636-1640) dropped its `is not None` guard and wrote `None` over the stored ontology, these two cases would still pass.
IMPORTANCE: low -- test-only, and I can name no currently shipped defect it would have caught: the delegate's guards are present and the probe confirms the values survive. Reachers of the added parametrize are this test alone.

[NOTE] tests/test_mock_knowledge_hub_app.py:360 :: uniformity-attributed-to-wrong-guard -- the new comment says the route "treats them uniformly through `fields.get(...) is not None`", but that predicate appears in the route only over `("name", "collection_id")` (mock_knowledge_hub_app.py:322 blank guard, :344 `moving`); `ontology_id` and `entity_type` are passed straight down in `**fields` and their null-is-not-supplied behaviour comes from the delegate (`if ontology_id is not None`, mocks.py:1636), which is what the test's own docstring says one line below.
A reader following the comment to the route looks for a four-field `fields.get(...) is not None` and finds a two-field one, then concludes the two new cases are exercising a route rule that does not exist for them.
IMPORTANCE: low -- comment only, in one test; nothing outside this file reads it.

[NOTE] jazzx_sdk/server/mock_knowledge_hub_app.py:231 :: todo-any-value-includes-null -- "any non-string JSON value still passes" is wider than the guard: `null` is a non-string JSON value and is the one thing the two lines above it exist to refuse (probed `POST {"name": null}` → 400).
Someone grepping the TODO in isolation reads it as "null passes too" and re-opens a hole that is closed; the surrounding sentences contradict it, so only a detached read misleads.
IMPORTANCE: low -- comment only; the guard's actual reachability (hand-built bodies, since `KnowledgeHubClient.create_entity` types `name: str`) is unchanged by this diff.

VERDICT: PASS
