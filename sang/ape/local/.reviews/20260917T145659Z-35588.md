# adversarial review -- japes

- when: 2026-09-17T14:57:00Z
- range: f2626f6..HEAD (since the last review) (e7cd2dd)
- diff: 134 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: docs/DEPLOYMENT_ENV.md :: deployment-doc-says-no-activation -- FIXED. The stale paragraph is gone, the activate route is named, and `tests/test_dev_daily_env_template.py:165` pins both halves (`"until activation ships" not in text`, route fragment present); 91 tests in the two touched modules pass.
WAS: plato/wiring/default.py :: in-loop-note-contradicts-cited-sibling -- FIXED. The branch now logs at warning and records nothing, matching `_durable_settings_degraded` at line 1092; no boot note reaches `readiness_provider` from a mid-run `build()`.
WAS: tests/test_plato_wiring_default.py :: profile-shape-not-what-fails -- FIXED. The comment now names the manifest as the cause and the profile as presence-only, which is the actual order in `plato/packs/loader.py:83-92` (presence check, then `AssistantManifest(**...)`, then `from_dir`).
WAS: docs/DEPLOYMENT_ENV.md :: active-pack-absent-from-env-doc -- FIXED. `PLATO_ACTIVE_PACK` has a row at line 170, and the new parametrized test covers all 21 keys from `plato_settings_catalog()` ∪ `DEFAULTS` (zero missing when run against the tree).

[DEFECT] docs/DEPLOYMENT_ENV.md:174 :: activation-not-visible-to-bound-agents -- the doc states activation "takes effect immediately, because the runtime resolves its registries per request", but `AssistantRuntime` caches bound agents and only calls the registries provider on a cache miss, so an assistant already bound keeps serving the previous pack's profile and skills.
A replica that has served one chat turn for assistant `A` (so `jazzx_sdk/server/assistant_runtime.py:106` stored `_agents[(tenant, A, release_id)]`), then `POST {prefix}/packs/A-pack/2.0.0/activate`: `activate()` in `plato/wiring/default.py:620` rebinds `profiles`/`skills`, the route answers `{"activated": true, "skills": N}` and persists the pointer, and the next `POST .../chat` goes through `plato/api/assistants.py:176` → `agent_for` → the cached agent built from 1.0.0. `release_id` comes from the DB manifest record, which activation does not touch, so the cache key is unchanged. `AssistantRuntime.invalidate` is the mechanism for exactly this and is called from nowhere in `plato/` (grep: one comment hit at `plato/wiring/default.py:551`). Immediate effect holds only on a replica that has not yet bound that assistant, i.e. a cold one, which is the case the sentence says a restart is not needed for.
IMPORTANCE: high -- silent wrong content with a success response: the operator's rollback appears applied and the replica keeps answering from the retired pack. Reached by the only `agent_for` call site (`plato/api/assistants.py:176`, every chat route) in any posture where the activate route is not gated off (relaxed, or strict with an auth dependency). Nothing outside this repo calls `activate` yet; the doc row is what tells a deployment to rely on it.

[SYMMETRY] tests/test_dev_daily_env_template.py:137 :: doc-guard-split-from-boot-contract -- the new doc-vs-code guards define a second `DEPLOYMENT_DOC` path in the `.env.dev-daily` module, while `tests/test_plato_boot_contract.py:37` already declares `DOCS = REPO_ROOT / "docs" / "DEPLOYMENT_ENV.md"` under a section headed "the documentation cannot drift from the table" and holds that same file against generated code.
Moving the two tests into that section is the same number of lines and drops the duplicate constant; it also keeps this module import-free of `plato` (it currently `__import__`s `plato.api.config` and `plato.defaults` at collection time, so an import error there now fails the `.env.dev-daily` guards too).

[NOTE] tests/test_plato_wiring_default.py:970 :: manifest-field-count-off-by-one -- the comment says `AssistantManifest` "requires eight fields and these are three"; it has 9 required fields, and only 2 of the archive's 3 keys are among them (`pack_version` is not a field; the required one is `pack_version_range`), which is why the raise reports 7 errors.
IMPORTANCE: low -- test-only comment; the test's claim (the raise happens, no note is recorded) is correct and passes. Verified by constructing the model with those three keys: `7 validation errors`, required list `['assistant_id', 'name', 'description', 'archetype_type', 'pack_id', 'pack_version_range', 'primary_persona', 'integration_surface', 'autonomy_level']`.

[NOTE] tests/test_dev_daily_env_template.py:158 :: documented-is-substring-only -- `assert key in _documented()` passes on any occurrence anywhere in the file, including inside a sentence saying a variable is unsupported, and would pass for a key that is a strict prefix of a documented one.
IMPORTANCE: low -- no collision exists today (checked all 21 keys pairwise: no key is a prefix of another), so there is no reachable false pass in the current tree; the cost of a future one is a green test over an undocumented variable.

VERDICT: BLOCK
