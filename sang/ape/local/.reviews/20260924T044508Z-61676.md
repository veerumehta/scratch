# adversarial review -- japes

- when: 2026-09-24T04:45:08Z
- range: 09b3370..HEAD (since the last review) (df917c9)
- diff: 652 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_plato_packs_api.py :: sdk-tests-in-plato-file -- FIXED

[NOTE] tests/test_plato_packs_api.py:37 :: decorator-blank-line-gaps -- 43 decorators now have two or more blank lines between them and the `def` they decorate. It looks like a mechanical edit ran over the whole file rather than only the removed block, and it includes a stray `@pytest.mark.asyncio` followed by four blank lines at line 83.
Trigger: `ruff format --diff tests/test_plato_packs_api.py` rewrites every one of them. The decorators still bind, so behaviour is unchanged: `ruff check` passes, and both files run 122 passed, 1 skipped. The cost is roughly 90 lines of whitespace churn in a diff that should only move five tests, and the next formatter run will undo it.
IMPORTANCE: low -- whitespace only, in one test file. No shipped code is affected and nothing outside the repo imports this file.

Checked in this diff, nothing found:
- All five tests moved intact. Each keeps the same assertions and now runs against `_store`/`_archive` in `tests/test_pack_store.py` (lines 36 and 61).
- `_seeded` is defined in both files, but each is module-local, so they don't collide.
- The `pack_inventory` import removed from `test_plato_packs_api.py` has no remaining users there. `_seeded` is still used at 9 call sites in that file.

VERDICT: PASS
