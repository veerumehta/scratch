# adversarial review -- japes

- when: 2026-09-15T16:46:57Z
- range: origin/v2.5.2...HEAD (d8f286b)
- diff: 385 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
[DEFECT] plato/api/packs.py:149 :: packs-page-ignores-mount -- `packs_html` gained a `mount` parameter but its only route still calls `packs_html(prefix)` with no `Request`, so the packs page is the one HTML route in the family that emits no mount.

Behind the gateway this diff exists for (`GET /api/v1/packs/ui` with `X-Forwarded-Prefix: /plato`, verified against `create_plato_app`): every absolute href comes back unprefixed (`/`, `/docs`, `/health`, `/api/v1/info/ui`, …) and `const PREFIX = "/api/v1"`, so the page's own `fetch(api("packs"))` leaves the mount and renders `HTTP 404` in `#problem`. The dashboard's `Packs` nav link, which this diff *does* prefix, is what leads an operator there.
IMPORTANCE: medium -- enumerated all six `HTMLResponse(` sites in `plato/`; four pass `mount`, this and `__main__.py:320` do not. Reachable in any gateway-mounted deployment; fails visibly (404 in the problem row and dead nav links), not silently. Nothing outside this repo calls `packs_html`.

[DEFECT] plato/__main__.py:320 :: degraded-dashboard-ignores-mount -- the degraded app's `/` calls `dashboard_html(prefix, omit=degraded_nav)` without `mount`, the same omission as the packs route.

`_info_only_app("server")` then `GET /` with `X-Forwarded-Prefix: /plato` (run) returns hrefs `/api/v1/info/ui`, `/api/v1/info`, `/api/v1/metrics`, `/api/v1/logs`, `/health`, `/openapi.json` -- all outside the mount -- and `__API_PREFIX__` is substituted unprefixed, so the info/logs/metrics panels 404. `/info/ui` on the same app is fixed (it reads `mount_prefix(request)`), so the two pages of the degraded replica now disagree about where they are.
IMPORTANCE: medium -- one call site; reachable whenever a boot fails behind the gateway, which is exactly the replica an operator opens in a browser. Fails loudly.

[NOTE] tests/test_plato_info_api.py:1683 :: mount-sweep-covers-one-page -- the mount test fetches only `/`, which is why both defects above survived; the same `re.findall(r'href="([^"]+)"')` check run over `/api/v1/packs/ui` and the `_info_only_app` dashboard would have caught each of them.
IMPORTANCE: medium -- names the two shipped defects it would have caught; test-file only.

[NOTE] tests/test_plato_info_api.py:1645 :: mark-sweep-omits-info-page -- the docstring says the favicon was copy-pasted into four files and the loop checks three; `plato/static/info.html` is the fourth, and the only one whose `__FAVICON__` is substituted in a route rather than in a page function, so it is the one that could regress unobserved.
IMPORTANCE: low -- `info_page` does substitute today (read at `plato/api/info.py:427`); this is coverage, not a live defect.

[NOTE] plato/prefix.py:12 :: mount-prefix-not-exported -- `__all__` still lists four names; `mount_prefix` is imported explicitly by `plato/app.py` and `plato/api/info.py` so nothing breaks, but it is the module's new public entry point and is absent from its export list.
IMPORTANCE: low -- two importers, both explicit; no star-import of `plato.prefix` anywhere in the repo.

VERDICT: BLOCK
