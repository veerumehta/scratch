# adversarial review -- japes

- when: 2026-09-09T19:55:58Z
- range: 137a8db..HEAD (since the last review) (75cac21)
- diff: 282 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` `published_at.desc()` tie — CHECKED, not open; `TODO(publish-tick-tie)` at `:58` unchanged and its claim still holds (no `publish` caller ships).
- WAS: `store_db.py:312` marker short-circuit — **STILL OPEN**; `:310-313` still returns the marker file's text verbatim as the pack root, no containment against `root`, no digest re-verify.
- WAS: `store_db.py:181` publish never opens `archive` — **STILL OPEN**; `publish` still commits arbitrary bytes; first failure is from `_unpack` at materialize time.
- WAS: `pack/__init__.py:29` `__all__` — **STILL OPEN**, and this diff edited that exact list: four `roots` symbols were added, `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` — **STILL OPEN**; `:320`, `:375`, `:395`, `:424` all interpolate with no length bound.
- WAS: `store.py:54` "sha256 over the archived asset bytes" — **STILL OPEN**; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race — **STILL OPEN**; no unique-violation handling, blob written before the row.
- WAS: `store_db.py:140` `visibility` unvalidated — **STILL OPEN**; `publish` takes the string straight into the row.
- WAS: `loader.py:214` domain pack aborts composition — FIXED.
- WAS: `loader.py:214` manifest-only row raises `PackNotFound` — FIXED.
- WAS: `tests/test_pack_store.py:353` vacuous freeze assertion — FIXED.
- WAS: `store_db.py:364` `_unpack` docstring summary — **STILL OPEN**; summary line still says "return the directory holding ``pack_manifest.yaml``" while the body accepts either name in `_MANIFEST_NAMES`.
- WAS: `loader.py:196` MappingPackSource prose — FIXED.
- WAS: `tests/test_pack_store.py:326` redundant `pack_id` re-set — FIXED.
- WAS: `loader.py:236` archive-less assistant pack at debug level — FIXED.
- WAS: `tests/test_pack_store.py:411` test never reached the branch — FIXED.
- WAS: `tests/test_pack_store.py:324` comment stated the same fact twice — FIXED.
- WAS: `[NOTE] loader.py:230` "publish gate" reads as an existing gate — FIXED by this diff.
- WAS: `[NOTE] test_pack_store.py:424` comment records a draft — FIXED by this diff.

## 2. New findings

24 passed, ruff clean on the four touched files.

**[DEFECT] `jazzx_sdk/pack/roots.py:100`** — `packs_root / candidate` joins an unvalidated `pack_id`, so an empty id resolves to the packs root itself and a `..` id resolves outside it, contradicting the docstring's "One pack's directory under ``packs_root``, or `PackRootNotFoundError`". Measured: `resolve_pack_root("", root/"packs")` → `.../packs`, and `resolve_pack_root("../secret", root/"packs")` → `.../packs/../secret`, both returned as a pack root instead of raising. `jazzx_sdk/path_safety.py:bare_name` is the in-repo rule for exactly this join and refuses `""`, `"."`, `".."`, and any separator.
IMPORTANCE: medium — grepped `resolve_pack_root` across `jazzx_sdk/`, `plato/`, `tests/`: only `tests/test_pack_roots.py` calls it today, so nothing shipped in this repo is wrong yet. It is exported from `jazzx_sdk.pack.__all__` in this same diff for out-of-repo consumers (the module docstring names the demo-page/conductor callers), and a caller that lost its pack_id or takes one from a request reaches both cases. Fails loudly enough downstream (wrong directory, then a manifest miss) that it is not silent-wrong-data.

**[SYMMETRY] `jazzx_sdk/pack/roots.py:81`** — `override = env(*env_names)` then `if override:` re-spells `env(NAME) or default` at a new call site, which is the exact pattern `jazzx_sdk/config/envvars.py:env_text` was added to own ("callers were spelling `(env(NAME) or "").strip() or default` at each site"); it also makes the docstring's "the first ``env_names`` that is set … the same contract `env` documents" false, since `env`'s docstring says an empty string counts as set while this code falls through to discovery. `plato/wiring_local.py:115` already uses `env_text` for the same override shape. One-word change.
IMPORTANCE: low — behaviour with `NAME=` is benign (discovery, or the named-variable raise); the cost is a docstring that cites the wrong contract.

**[SYMMETRY] `plato/wiring_local.py:118`** — this is the one place in the repo that still counts (`Path(__file__).resolve().parents[1]`) and gates on a single `pyproject.toml`, which is precisely the two failures `roots.py:26-28` names ("a `pyproject.toml` alone can sit in a nested package") and `DEFAULT_MARKERS` was chosen against; `discover_root(Path(__file__))` returning `None` composes with the existing cwd fallback without adding a branch. Left unmigrated, the diff's claim to have replaced counting holds only for callers outside this repo.
IMPORTANCE: low — one call site (`_REPO = _find_root()` at `:123`), reachable only in the local-wiring configuration; `tests/test_plato_wiring.py:163` pins the checkout answer, which `discover_root` from `__file__` also gives.

**[NOTE] `jazzx_sdk/pack/roots.py:57`** — markers are tested with `.exists()`, so a *file* named `config` satisfies the marker the comment describes as "the config directory is what a pack lookup is actually after"; `.is_dir()` for the directory marker would match the stated rule.
IMPORTANCE: low — needs a repo with a `config` file rather than a directory beside `pyproject.toml`; no such layout in this repo or the consumers named in the module docstring.

**[NOTE] `jazzx_sdk/pack/roots.py:99`** — the three-spelling candidate tuple duplicates `jazzx_sdk/pack/manifest_loader.py:93-96`, which the docstring acknowledges ("the same reconciliation `from_pack_id` already does"); a shared candidates helper would keep the two from drifting when a third spelling rule appears.
IMPORTANCE: low — two call sites, both in this repo; a drift would surface as a pack found by one and not the other.

**[NOTE] `tests/test_pack_roots.py`** — no test asserts the discovery success path of `resolve_packs_root`: every case either sets an override or expects the raise, so `DEFAULT_PACKS_SUBDIR` being wrong (or `root / subdir` being mis-joined) leaves the suite green. `_project()` already builds `config/packs`, so the assertion is one line.
IMPORTANCE: low — test-only; the shipped-code defect it would catch is a wrong packs root in the no-override deployment, which the module docstring says surfaces as a confusing "no such pack" several frames away.

VERDICT: BLOCK
