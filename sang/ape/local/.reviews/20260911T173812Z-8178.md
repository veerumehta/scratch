# adversarial review -- japes

- when: 2026-09-11T17:38:12Z
- range: 7a5ec04..HEAD (since the last review) (2e2c8ca)
- diff: 68 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_mock_knowledge_hub_app.py` → 36 passed; `ruff check` clean on both files. All statuses below are from an actual `TestClient` probe (`/tmp/probe_null.py`), not inference.

WAS: jazzx_sdk/server/mock_knowledge_hub_app.py :: null-required-field-passes-create -- FIXED; all four fields now refuse a null: `POST {name:null}` / `{collection_id:null}` / `{ontology_id:null}` / `{entity_type:null}` → 400 `missing required field(s): ['<field>']`, and the phantom key is gone (`POST {name:null}` then `POST {name:"None"}` → 201, was 409).
WAS: tests/test_mock_knowledge_hub_app.py :: null-case-asserts-only-collection -- FIXED; line 375 now asserts `patched.json()["name"] == "e-1"` under the null-name branch directly.

CHECKED: `mock_knowledge_hub_app.py :: TODO(missing-fields-still-400)` still holds -- the guard this diff rewrote still answers 400 (probed) where the real body model gives 422, and the detail is still a flat string rather than FastAPI's list, so nothing in the deferral's reason changed.

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:227 :: patch-null-is-200-not-422 -- the new comment justifies the fix by claiming the divergence it closed was "the same body a 201 here and a 422 there", but the PATCH route answers **200** for a null field, never 422; the 422 there is the blank-string case, which create answered 400 both before and after this change, so the stated cross-route contradiction never existed in either shape.
Probed on the current tree, same entity: `PATCH {"name":null}` → 200 (name still `e-1`), `PATCH {"collection_id":null}` → 200, `PATCH {"ontology_id":null}` → 200; only `PATCH {"name":""}` → 422. Pre-fix create for a null was 201, post-fix 400. So the two routes still differ in outcome for a null (400 on create, 200 on PATCH) -- correctly, since one is a required-field create and the other a partial update -- and the comment reads as if that remaining difference were the bug, which invites a future author to "align" the PATCH route and break `_effective`'s not-supplied rule three lines of comment above it.
IMPORTANCE: medium -- no wrong behaviour, but the prose is the only record of why the two routes differ and it states the opposite of what they do; reachers are `tests/test_mock_knowledge_hub_app.py` in-repo, and `create_knowledge_hub_mock_app` is exported from `jazzx_sdk.server`, so external pack authors read this comment when the mock's code disagrees with the Hub.

[DEFECT] tests/test_mock_knowledge_hub_app.py:391 :: patch-null-is-200-not-422 -- the same false sentence is the test's docstring, and the test file itself disproves it: `test_a_null_field_is_not_supplied_not_changed` twelve lines above asserts `patched.status_code == 200` for `{"name": None}`, which is the body this docstring says is "a 422 there".
Trigger is the same state as above; the two assertions sit in one file and contradict each other, so whichever a reader trusts, one of them is wrong.
IMPORTANCE: medium -- test-only file, but it is the paired half of the shipped comment; fixing one without the other leaves the claim in the repo.

[NOTE] jazzx_sdk/server/mock_knowledge_hub_app.py:229 :: non-string-scalar-passes-guard -- the guard is now `is None` plus a truthiness test over `str(...)`, so a non-string JSON scalar still passes: probed, `POST {..., "name": 0}` → 201 with `"name": 0`, and `{"name": false}` → 201 with `"name": false`, where the service's body model types `name` as a string. The conflict key is then `("col-1", "0")`, so a later legitimate `POST {"name": "0"}` is a 409 for a different entity -- the same phantom-key shape as the null case, one JSON type over.
IMPORTANCE: low -- pre-existing, not introduced here, and unreachable through `KnowledgeHubClient.create_entity` (its `name` is typed `str`); only a hand-built HTTP body gets there. A `TODO` on the guard is a complete response.

[NOTE] tests/test_mock_knowledge_hub_app.py:409 :: rejected-create-status-unasserted -- the docstring says the phantom key "outlived the rejected create", but the rejected create's status is never asserted; only the second POST is checked. If the first call started 500ing, the test would still pass and still read as proof of the rejection. The refusal itself is covered by the parametrized sibling at line 386, so this is duplication of coverage rather than a gap.
IMPORTANCE: low -- test-only; no shipped-code defect it would have caught that line 386 does not already catch.

VERDICT: BLOCK
