# adversarial review -- japes

- when: 2026-09-10T02:34:46Z
- range: origin/v2.5.1...HEAD (d1863ab)
- diff: 997 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

`profiles.py:187` `TODO(threshold-grid-provenance)` — claim holds. No YAML in the repo authors a `tables:` key (grep over `*.yaml`/`*.yml`: zero hits), and `get_table`'s only caller is `MatrixEvaluator` (`condition_evaluator.py:368`). Not raised.

## Findings

**[DEFECT] `jazzx_sdk/fabric/graph/check.py:246`** — `finding_from` still derives the whole indeterminate rationale from `verdict == INDETERMINATE` alone and asserts one cause ("the case does not supply `<reads>`"); this diff adds a new reachable INDETERMINATE cause where that sentence is affirmatively false, and drops the `indeterminate_reason` that now carries the truth (it isn't in `domain_extensions` either).
Trigger: a profile authoring `dscr_min: {value: '1.25', provenance: secondary_source, status: seed}` and a `RatioCondition` with `threshold: profile:dscr_min` — `RatioEvaluator` (`condition_evaluator.py:273`) returns INDETERMINATE/`POLICY_NOT_ACTIVATED`, and `check_graph` emits a `CanonicalDecision(ESCALATION, confidence=0.0)` whose rationale says the case did not supply `noi, total_debt_service`. It did; the threshold was unapproved. Same wrong sentence for `CALCULATION_UNAVAILABLE` and `CONDITION_UNEVALUABLE`.
IMPORTANCE: high -- silent wrong data in the governance-facing rationale of an escalation. `finding_from` is in `check.py.__all__` and is the only findings producer in `check_graph` (check.py:323); reached by `tests/test_graph_check.py`, `test_graph_derive.py`, `test_graph_contradictions.py`, and by jaci's compliance path through the editable install.

**[DEFECT] `jazzx_sdk/fabric/canonical/policy.py:740,746`** — `executable_rules`/`activation_blocks` iterate `self.rules` only and ignore `Policy.exception_rules`, which `Policy.get_rule` (policy.py:775) and `PolicyRegistry._build_rule_index` (policy_registry.py:160) both treat as first-class rules; `activation_blocks`' docstring "Empty means every rule is live" is therefore false.
Trigger, run: `Policy(..., status=ACTIVE, rules=[R1], exception_rules=[Rule(rule_id='X1', status=PolicyStatus.SEED)])` → `executable_rules() == ['R1']`, `activation_blocks() == {}`, while `get_rule('X1').status is SEED`. A seed exception rule is both silently omitted from the executable set and silently absent from the block map.
IMPORTANCE: medium -- fails silently rather than loudly, but no shipped pack sets a rule status yet (only `pipelines/policy_extract.py:555` sets a status, and that is `Policy.status`), so the trigger needs a caller adopting the new field. These two accessors are the only gate, so nothing else would catch it.

**[DEFECT] `jazzx_sdk/fabric/canonical/profiles.py:122`** — the docstring says a mapping is coerced "only when the mapping is unmistakably one", but the predicate accepts any mapping with `value` plus `provenance`/`status`, so a `custom` entry whose `status` is not a lifecycle word now hard-fails profile load where it previously resolved; the error is also misattributed.
Trigger, run: `PolicyProfile.model_validate({'profile_id':'p','custom':{'x':{'value':5,'status':'ok'}}})` → `ValidationError` at path `custom.status` (not `custom.x.status`), "Input should be 'seed', 'draft', ...".
IMPORTANCE: low -- no shipped YAML in this repo authors a `custom:` key at all (grep: zero hits), and it fails loudly at load. A `TODO` is a complete response.

**[SYMMETRY] `jazzx_sdk/manifest/__init__.py:9-12`** — the package exports `AutonomyLevel` (from `autonomy.py`) and `SurfaceType` (from `surface_types.py`); the new `manifest/lifecycle.py`, whose own docstring positions it as "Dependency-free, as `autonomy.py` is", is not added, so `PolicyStatus` is the one member of that family reachable only by module path. Two lines, matching the two beside it.
IMPORTANCE: low -- import-surface only; `contracts.py:21` and `fabric.canonical` re-exports still resolve.

**[SYMMETRY] `jazzx_sdk/fabric/canonical/policy.py:754`** — `_out_of_force` is the third copy in this file of "read a naive bound as UTC, then compare against `now`": `is_active` (policy.py:727-733) does it for `expiry_date` and `Rule.activation_block` (policy.py:562-570) does it for both bounds, each with its own inline `replace(tzinfo=timezone.utc)`. One `_as_utc(bound)` module helper is smaller than the three inline blocks the diff leaves behind, and would keep the boundary semantics (`<` / `>`, exclusive at the edge) from drifting between them.
IMPORTANCE: low -- all three are in this file and currently agree; nothing outside the repo calls them.

**[NOTE] `jazzx_sdk/fabric/canonical/policy.py:454`** — `info.data.get("verdict")` is absent when `verdict` itself failed validation, so `RuleOutcome(verdict="bogus", status="X", indeterminate_reason=EVIDENCE_MISSING)` emits a second, misleading error "on a None outcome" beside the real enum error.
IMPORTANCE: low -- one extra confusing line in a `ValidationError` a caller already has to read.

**[NOTE] `jazzx_sdk/fabric/canonical/condition_evaluator.py:454`** — `reasons[0]` is the first indeterminate *child*, not the limb that decided the verdict; for `any_of` over `[INDETERMINATE(EVIDENCE_MISSING), INDETERMINATE(POLICY_NOT_ACTIVATED)]` the reported cause is arbitrary with respect to the remedy. The comment states this as a rule ("First one, since the verdict is one answer") rather than as the arbitrary pick it is.
IMPORTANCE: low -- `limbs` still carries every child's inputs; only the summary field is imprecise.

**[NOTE] `jazzx_sdk/fabric/canonical/profiles.py:152,174`** — both loops branch on `isinstance(found, Threshold)` for `confidence_floors`, which is `dict[str, float]` and cannot hold one (the new test asserts this raises `ValidationError`), so that arm of the gate has no reachable predicate.
IMPORTANCE: low -- dead branch, already acknowledged in `test_custom_is_gated_as_well_as_thresholds`.

Both new test files pass (28 passed).

VERDICT: BLOCK
