# adversarial review -- japes

- when: 2026-09-17T06:45:08Z
- range: ac3ee15..HEAD (since the last review) (3c3e755)
- diff: 1654 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/wiring/default.py :: reviews-path-not-in-repo -- FIXED. Both sites now say "the adversarial review's accepted-findings list, which lives outside this checkout" and cite the `path :: slug` key instead of a repo-relative path.
WAS: plato/boot/schema_version.py :: prefix-comment-says-substring -- FIXED. Reworded to past-tense counterfactual ("the first version matched a bare substring there ... would have dropped"), which matches `plato/__main__.py:170`'s `startswith`.
WAS: plato/wiring/default.py :: health-exits-on-ready-not-configured -- FIXED. The chain is now spelled correctly: `degraded` → `readiness_provider` → `/health` `ready: false` → `--health` non-zero, which is what `plato/app.py:258` and `plato/__main__.py:273` do.

ACCEPTED: plato/wiring/default.py :: settings-failure-not-at-info

[DEFECT] plato/wiring/default.py:602 :: activation-failure-marks-unready -- a failed activation records a permanent wiring boot note, so the replica goes unready even though the route refused and the serving pack is unchanged.
`POST /api/v1/packs/{id}/{ver}/activate` for a version whose archive is present but unloadable (a corrupt `profile.yaml`, a missing persona). `activate` calls `_registries(str(root))`, which catches and calls `record_boot_note(..., from_wiring=True)` at `plato/wiring/default.py:283`; that appends to `_WIRING_NOTES`, which `boot_notes()` returns, `resolve_degraded` (`plato/wiring/contract.py:109`) merges, and `create_plato_app` passes as `readiness_provider` (`plato/app.py:258`). `_WIRING_NOTES` is cleared only at the top of `build()` (line 505), and `record_boot_note`'s own docstring says a serving role calls `build()` once -- so the note lives for the process. One operator activating a bad pack takes the replica out of rotation permanently, while the route answers 409 and the runtime still serves the old pack. The note also names the wrong variable: it interpolates `PACK_DIR_ENV` and the materialized cache path, neither of which the operator set. The boot call site at line 632 has the same effect, contradicting its own comment at line 626 ("should come up serving *something* and say what it could not do") -- unready means the orchestrator routes nothing to it. This is the exact rule `_durable_settings_degraded`'s docstring states two hundred lines below.
IMPORTANCE: high -- silent to the caller (409 looks like a clean refusal) and invisible in-process; reachable from the one new route this diff adds and from any boot with `PLATO_ACTIVE_PACK` set. Checked both `record_boot_note` call paths into `activate`, the `_WIRING_NOTES` lifetime, the `resolve_degraded`/`readiness_provider`/`Dockerfile HEALTHCHECK` chain, and `plato/api/packs.py:402`. Nothing outside this repo calls `activate`.

[DEFECT] plato/api/packs.py:379 :: persist-order-reversed-in-docstring -- the docstring says the pointer is persisted first and the process rebinds second; the code does the opposite, and its own inline comment and test say so.
Any successful activation. Lines 379-382 read "The pointer is persisted first ... Then the running process rebinds"; line 402 calls `activate(reference)` and line 411 calls `_persist_active_pack` after it, with the comment at 404 saying "Before persisting, so a pointer that cannot be served is never written" and `test_an_activation_that_cannot_be_served_is_not_persisted` asserting "swap first, persist second". A reader reasoning about the restart-mid-activation case gets the opposite guarantee from the two halves of the same function.
IMPORTANCE: medium -- docstring only, one function, no runtime effect; but it is the paragraph an operator reads to decide what a crash between the two steps leaves behind, and it states the wrong answer.

[DEFECT] plato/api/packs.py:307 :: upload-docstring-says-no-activation -- the upload route still tells callers no reload path exists and that serving an uploaded pack needs a restart; this diff ships the reload path directly below it.
`GET /docs` or reading `upload_pack`: "`PLATO_PACK_DIR` is read once at startup, and no reload path exists yet ... until activation ships, serving an uploaded pack needs a restart." `activate_pack_version` at line 371 is that path, and `build()` now re-reads `PLATO_ACTIVE_PACK` on restart. The factory docstring at line 231 has the matching gap: it enumerates the router's routes and lists neither `POST {prefix}/packs` nor the new activate route.
IMPORTANCE: medium -- fails loudly only in the sense that an operator follows it and restarts a replica unnecessarily; two docstrings in one file, both served through the OpenAI-schema-visible handler docstring. No external consumer.

[DEFECT] .env.dev-daily:57 :: demo-tenant-in-deployed-template -- the ready-made deployed env set now ships a plausible-looking tenant id where it previously shipped an obvious placeholder, producing exactly the silent skip its own comment says it avoids.
`docs/DEPLOYMENT_ENV.md:153` calls this file "the whole set an operator enters into the Container App". Pasted unchanged, `PLATO_TENANTS=acme-hospital` makes `plato/jobs.py:41` sweep one fictional tenant and silently skip every real one -- word for word the failure `plato/wiring/default.py:75-78` says was the reason not to default it. `<comma,separated,ids>` could not survive review of a deployed config; `acme-hospital` can. The comment's justification is also unbacked: `plato/data/demo_pack/manifest.yaml` carries no tenant field, and `acme-hospital` appears nowhere in the tree except these two new comments, so "the demo tenant the bundled clinical-intake pack belongs to" names an association that does not exist.
IMPORTANCE: medium -- one file, but it is the documented copy-paste source for a deployed replica, and the failure mode (reaping/digest jobs completing having touched nothing) is silent. Checked the sole `tenants()` consumer (`plato/jobs.py:41`), `_tenants`, and every occurrence of `acme-hospital` in the tree.

[SYMMETRY] plato/wiring/default.py:632 :: asyncio-run-without-loop-guard -- `_apply_durable_settings` in this same file already guards `asyncio.run` against being called inside a running loop; the new activation block calls it bare.
`_apply_durable_settings` (around line 1035) does `get_running_loop()`, closes the coroutine and returns when one exists, because `build()` is called from inside a loop by tests and by a consumer rebuilding mid-run -- the new `test_activation_rebinds_what_the_runtime_serves_without_a_restart` is itself an `@pytest.mark.asyncio` test calling `build()`. With `PLATO_ACTIVE_PACK` set in that situation, `asyncio.run` raises `RuntimeError`, the bare `except` converts it into a boot note claiming the pack could not be activated, and the replica reports degraded for a reason that has nothing to do with the pack. The consistent version is the same three lines the sibling already has.
IMPORTANCE: low -- no shipped serving role reaches it (`build()` runs before uvicorn; `_rewire` calls `swap_database`, not `build()`), so today this is a test-shaped trigger and one misleading note.

[NOTE] plato/wiring/default.py:240 :: tenant-sentinel-list-misses-new-default -- `parse_pack_reference`'s docstring enumerates the tenant sentinels this tree carries as `DEFAULT_TENANT` ("default"), `PLATFORM_TENANT` ("_platform") and "the local wiring's `local`", but the same diff changed `_DEFAULT_LOCAL_TENANT` to `"acme-hospital"`; `plato/wiring/local.py:76` is the only remaining `"local"`, and there are now four.
IMPORTANCE: low -- one docstring plus the test docstring at `tests/test_plato_wiring_default.py` that repeats the same enumeration; grepped all four constants.

[NOTE] plato/api/config.py:71 :: active-pack-absent-from-catalog -- `PLATO_ACTIVE_PACK` is a durable setting the wiring reads at boot but is not a `SettingField`, so `/config` neither shows nor lets an operator correct it, and only `durable_settings_store(db).read()`'s row overlay surfaces it at all. `plato_settings_catalog`'s docstring frames its exclusions as "deliberately no field that nothing reads", which is the opposite case here.
IMPORTANCE: low -- one route's rendering; the write path is the activate route, not the catalog, so nothing is broken. Checked `_catalog_keys`, `DbSettingsStore.write` (persists any non-bootstrap key) and `read` (overlays all rows regardless of `keys`).

[NOTE] tests/test_plato_packs_api.py:1861 :: activation-test-name-overstates -- `test_activating_changes_what_the_runtime_serves_without_a_restart` replaces `_ACTIVATE` with a stub returning `("/cache/swap-me", 1)`, so it asserts the route's plumbing, not what the name and docstring claim; the real swap is covered by `test_activation_rebinds_what_the_runtime_serves_without_a_restart` in `tests/test_plato_wiring_default.py`. The `_assistant_archive` fixture and the published pack it builds are unused by the assertions.
IMPORTANCE: low -- test-only; the shipped behaviour the name claims is covered elsewhere, so nothing is unverified, only mislabelled.

VERDICT: BLOCK
