# adversarial review -- japes

- when: 2026-09-10T09:42:37Z
- range: origin/v2.5.1...HEAD (fbc298d)
- diff: 10080 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(activation-vs-active-rules)` `policy.py:758` — holds. `active_rules()` (`policy.py:792`) filters `legacy_alias` out of `self.rules` only and never reads `exception_rules`; no caller reads `executable_rules`/`activation_blocks`.
- `TODO(threshold-grid-provenance)` `profiles.py:218` — holds, and the numbers are exact: `dscr_profile.yaml` has 5 grids / 150 cells / 7 `thresholds:`, and `get_table` has no gate.
- `TODO(orphan-blob-gc)` `store_db.py:317` — holds. `delete` touches no blob, and the store is tenant-scoped at construction.
- `TODO(layout-key-resolution)` `workbook.py:177` — holds. `workbook.py:412` writes `binding.label`, then `414` `continue`s on `item is None` with no `_ExceptionEntry`.
- `TODO(layout-live-formula-order)` `workbook.py:183` — holds. `inputs_by_statement` is only populated at `694` (non-live sheets) and read at `690`.

## Findings

**[DEFECT] `jazzx_sdk/fabric/canonical/policy.py:732`** — `is_active` now folds "not yet effective" into the same false as "expired / not ACTIVE", and the two reporting paths that read it label the result *stale*, recommending it be updated.

Trigger, run against this tree: `Policy(status=ACTIVE, effective_date=now+90d)` → `detect_staleness(["P_FUTURE"])` returns `stale_policies=['P_FUTURE']`, `recommended_actions=["Review and update 1 stale policy"]`. A policy pre-authored to take effect next quarter is the newest thing in the registry, and it is reported to a reviewer as the oldest. `resolve()` (`experts/policy/default.py:331`) puts it in `stale_policies` the same way.

IMPORTANCE: medium -- I enumerated all five `is_active` readers: `policy_registry.py:127`, `store/core.py:206` and `modes/operational/governor.py:105` *exclude* on it and are now more correct; the two that mislabel are `experts/policy/default.py:331` and `:622`. `StalenessReport` crosses the repo boundary via `finance/validation.py:detect_stale_pinned_policies`, re-exported by jaci's `capabilities/commercial_lending`. No shipped policy is future-dated today (latest of the four seed-pack policy YAMLs is `2026-07-01`), so the trigger needs a pre-authored policy — which is what this diff's new `Rule.effective_from` exists to support.

**[DEFECT] `jazzx_sdk/experts/policy/base.py:52`** — the `warnings` description states an invariant the model cannot enforce, and `policy_violations_to_findings` (`finance/validation.py:816`) now acts on it for every producer: each warning becomes a `DefectClass.POLICY_VIOLATION` at `Severity.POLICY`.

Trigger: `AMLPolicyExpert.check_compliance` (`/Users/sangit/src/jaci/src/jaci/scenarios/aml/experts/policy.py:265`) sets `allowed = len(policy_violations) == 0` and, for `action="escalate", confidence=0.72`, returns `ComplianceResult(allowed=True, warnings=["Confidence below group threshold: 0.72 (min 0.85)"])`. That state contradicts "does withhold `allowed`", and feeding it to `validate_package(compliance_result=…)` files a policy violation for a non-blocking note.

IMPORTANCE: medium -- `ComplianceResult` is a public SDK contract with external subclasses; one already produces the shape the new description says cannot occur. The mapping half is not reached today: the only `validate_package(compliance_result=…)` call I found is `jaci/capabilities/credit_validation/provides.py:36`, on the CL path, whose expert delegates to `DefaultPolicyExpert` (the only in-repo warning producer). Any pack expert on that path that adds one advisory warning reaches it.

**[SYMMETRY] `jazzx_sdk/finance/validation.py:830`** — `_not_activated_warning` (`experts/policy/default.py:71`) formats `policy_id`/`rule_id` into a string and `_WARNING_REFS`/`_warning_refs` parses them back out with a regex. The structured shape already exists 40 lines above the producer: `ComplianceResult.policy_violations` is `list[dict]` carrying exactly `policy_id`/`rule_id`/`action` (`default.py:477`), and `policy_violations_to_findings`'s first loop already reads it. Appending the unapproved rule there under a distinguishing key deletes `_WARNING_REFS`, `_warning_refs` and the warnings loop — strictly less code than the diff adds, and the regex's silent-degradation case (a `policy_id` containing whitespace yields `rule_id=None`) goes away with it.

IMPORTANCE: low -- format and parser are both in this repo and both new in this diff; no external caller reads either yet.

**[NOTE] `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:138`** — this seed pack ships five `jaci.*` dotted import paths (`:128, :138, :141, :149, :151`), jaci-relative eval paths (`gold_cases_path: tests/eval/gold_cases/ci`, `runner: tests/eval/run_ci_eval.py`), a source-document path from another repo's tree naming a client (`source_doc: "docs/LoanSamples/YETI/Yeti Credit Package/…"`), and gitignored plan filenames in the YAML comments (`plan_JACI_CL_*.md`, also in `chart_of_accounts.yaml`, `breakouts.yaml`, `addbacks.yaml`, `metrics.yaml`). The sibling seed pack `dscr_core` has zero `jaci` references.

IMPORTANCE: low -- inert as published: `plato/seed.py` only reads `pack_id`/`pack_version`, and `published_registries` (`plato/packs/loader.py:249`) skips non-assistant packs before materializing, so nothing resolves the dotted paths. `Pack.conductor` would `ModuleNotFoundError` on them, but the only consumer that materializes this pack is jaci, where `jaci.*` resolves.

**[NOTE] `jazzx_sdk/fabric/canonical/profiles.py:169`** — `get` and `get_threshold` both apply the `looks_authored`/`Threshold` gate to all three buckets, but `_coerce_threshold_mappings` (`:135`) is registered only for `thresholds` and `custom`, and `confidence_floors` is `dict[str, float]` (`:105`) so it cannot hold either shape — authoring one there fails pydantic validation at load. Both governance branches are unreachable for that bucket, and a confidence floor cannot be marked unapproved.

IMPORTANCE: low -- no shipped profile authors a governance key at all (`dscr_profile.yaml` is bare values), and the failure is a loud `ValidationError` at profile load.

**[NOTE] `jazzx_sdk/agents/adjudication/segment.py:95`** — the docstring still says it "Mirrors ``DefaultPolicyExpert.check_compliance`` exactly" after the diff made the two diverge: a blocked gate records an `INDETERMINATE` `RuleOutcome` here and a warning that withholds `allowed` there.

IMPORTANCE: low -- one confusing sentence for the next reader of this function; nothing reads the docstring.

VERDICT: BLOCK

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(if-match-post)` (`plato/posture.py:97`): claim holds. `require_if_match` is imported only by `tests/test_plato_preconditions.py`; no router mounts it, so `POST /packs/initialize` cannot 428 today.
- `TODO(omit-info)` (`plato/info_api.py:83`): holds. `_info_only_app`'s `degraded_nav = ("Guide", "config", "database", "Packs", "packs")` does not contain `info`, and no other caller passes `omit`.
- `TODO(layout-key-resolution)` (`jazzx_sdk/finance/workbook.py:175`): holds — `load_workbook_layout` is shape-only. Consequence for the new layout files is exactly what the TODO states; see NOTE 2 for the part the diff adds.
- DSCR corpus counts in `ENCODING_NOTES.md` verified against the YAML: 99 grid cells / 29 `NA`, 33 ITIN cells, 41 rules, 6 `matrix` + 2 `ratio` conditions, 11 `custom` scalars, table sizes 99/3/3/12/33. All match.

## Findings

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125,127` — two `required_documents` entries in `purpose_specific_documents` are PDF line-wrap fragments (`schedule`, `available)`), and the requirements they were split from are gone. The source rows are plainly `Equipment/Capex: … delivery/installation schedule` and `Acquisition: purchase agreement, … (if available)`; the file's own `description` claims "Extracted verbatim from the source checklist". A consumer resolving the manifest's `checklist: policies/checklist_ci.yaml` and turning missing items into DATA_GAP hypotheses asks the borrower for a document called "available)" and never asks for equipment quotes or the purchase agreement.
IMPORTANCE: medium -- grepped `jazzx_sdk` and `plato` for `required_documents`/`document_checklist`: zero readers in this repo, and `manifest_loader.policy_files()` deliberately excludes `checklist:`, so nothing here loads it. It reaches readers as published pack bytes via `PackVersionStore.materialize`; the named reader (`assess_package()`, per `ci-spread-core/pack_manifest.yaml`) is outside this repo, so the wrong data is silent from here.

**[SYMMETRY]** `plato/packs_api.py:151-160` vs `plato/assistants_api.py:167` — the new `_store` translates `tenant_from_request`'s `ValueError` into a 400 naming the missing header; `assistants_api._tenant` calls the same callable with no guard, and the shipped resolver (`plato/wiring_default.py:505`) raises `ValueError("X-Tenant-Id is required on a deployed Plato request")` on any deployed posture. There is no exception handler anywhere in `plato`/`jazzx_sdk`, so every chat/session request without `X-Tenant-Id` on a staging or production replica gets a 500 and a traceback instead of the 400 this diff's own comment says it fixed. The consistent version is the same three lines in `_tenant`, or one shared helper both call.
IMPORTANCE: medium -- enumerated both call sites of `tenant_from_request` (`assistants_api.py:167`, `packs_api.py:152`) and both shipped resolvers (`wiring_default.py:498` raises, `wiring_local.py:388` does not); reachable on every strict-posture deployment, fails loudly, and the caller works around it by sending the header.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:52` — "6 ratio thresholds" is 7 in `profiles/dscr_profile.yaml` (`dscr_min_rural`, `dscr_min_fthb`, `dscr_min_thin_file` are present alongside the four the rules use). Every other count in that table checks out, so this is the one line a reader diffing the profile against the notes will trip on.
IMPORTANCE: low -- documentation-only; the three extra thresholds are unreferenced by any rule, same class as the unconsumed `custom` keys §3/G4 already flags.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/workbook_layout.yaml:8-9` — the header states every `key` "must resolve against one of those two, which is this file's own load-time acceptance check (see workbook_layout.py's validator)". `load_workbook_layout` is shape-only by its own `TODO(layout-key-resolution)`, which names this exact claim as false, and the file is `jazzx_sdk/finance/workbook.py`, not `workbook_layout.py`. Shipping the sentence again puts the false claim in a second repo.
IMPORTANCE: low -- comment only; the behaviour it misdescribes (a labeled row of empty cells with no exception entry) is already deferred under that TODO.

**[NOTE]** `plato/packs_api.py:69` — "a pack row cannot exist with zero live versions, so an empty list here is unreadable rather than absent" is true only between the two awaits: a `DELETE` landing between `store.list_packs()` and `store.versions(record.pack_id)` returns `[]` with no `versions_error`, which is the "Versions 0 beside a populated Latest" wrong-data case the comment rules out.
IMPORTANCE: low -- needs a concurrent retire, and retire is relaxed-posture-only (`_refuse_unless_relaxed`), so the trigger is one operator deleting while another reloads the page on a local or dev-daily replica.

VERDICT: BLOCK

## slice3

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Findings

**[DEFECT] `plato/wiring_default.py:365`** — Appending the pack-archive note to `missing_config` puts a permanently-true entry in `degraded` for every deployed posture (the backend default is `"local"`), and `degraded` is the readiness list: `plato/app.py:244` feeds it to `readiness_provider`, which makes `/health` answer 503 `not_ready` (`jazzx_sdk/server/app.py:214-221`), and in a strict posture `enforce_degraded=strict` (`wiring_default.py:540`) makes every chat route answer 503 `plato_not_configured` (`plato/assistants_api.py:151`).
Trigger, measured: `JAPES_ENVIRONMENT=staging` with tenants, KH URL, an LLM key and a `common` database configured, `PLATO_PACK_BLOB_BACKEND` unset → `missing_config('staging')` returns exactly one note, this one. `dev-daily` gets the same note (chat survives there, `/health` does not). The sibling it copies (`_is_sqlite`, line 359) fires only on a database that genuinely cannot persist and is absent from a correctly configured staging; this one cannot be silenced except by configuring azure blobs, including on deployments that publish no packs.
IMPORTANCE: high -- enumerated both consumers of `degraded`: `/health` readiness for every deployed role (server, and the degraded app in `plato/__main__.py:305`), and the chat gate for strict postures. Nothing outside this repo consumes `missing_config` directly, but every container probe consumes `/health`.

**[DEFECT] `plato/wiring_default.py:368`** — The comment claims "setting the backend through `/v1/config` clears the note without a restart", but `PLATO_PACK_BLOB_BACKEND` is not in `plato_settings_catalog()` (`plato/config_api.py:47-84`), and `jazzx_sdk/server/settings_api.py:212-214` rejects any key outside the catalogue.
Trigger: `PATCH /api/v1/config {"PLATO_PACK_BLOB_BACKEND": "azure"}` → 422 `Unknown setting(s)`. Even if the key were added, the environment read here diverges from the store actually in use: `pack_blobs` is resolved once at `wiring_default.py:441`, so the note would clear while every publish kept writing to local disk. The sqlite check beside it avoids this by reading the running store (`db=handle`); passing the resolved blob store in the same way is the shape that makes the comment true.
IMPORTANCE: medium -- fails loudly on the PATCH (422), so no wrong data today; the risk is the second half, which becomes silent-wrong-report the moment the key is added to the catalogue.

**[DEFECT] `plato/static/packs.html:429`** — `load()`'s failure path clears `#rows`, `#problem` and `#empty` and sets `#count` to "unavailable", but never clears `#meta`, so the version count from the last successful read stays rendered next to the failure; `render()`'s withheld and unavailable branches (lines 300-315) return before touching it too.
Trigger: load a tenant with packs (header reads "Published 3 versions"), then blank or mistype the tenant field and press Enter → the panel reads "Published 3 versions" beside "unavailable" and an empty table, for a request that returned nothing. Same stale-channel class the file's own comments fix for `#problem` and `#empty`.
IMPORTANCE: medium -- operator-facing page only, reachable in every posture that serves `/packs/ui`; the wrong number sits beside a correct "unavailable" pill, so it misleads rather than silently corrupts.

**[NOTE] `plato/static/packs.html:437`** — The `keydown`/Enter re-read is bound to `#tenant` only; the identity field added beside it has no handler, so typing a UUID and pressing Enter does nothing and the value first travels on whatever fetch happens next.
IMPORTANCE: low -- one confusing interaction on a local operator page; no shipped code path depends on it.

**[NOTE] `plato/static/packs.html:305`** — `if (!data || !data.available)` and `data && data.reason` have no reachable predicate: `render` is only called from `load()`'s success path (non-null `data`) and from the expander handler with `window.__packs`, which is non-null whenever a row exists to click.
IMPORTANCE: low -- dead defensive branches, no behaviour difference.

## CHECKED
`TODO(azure-blob-container-singleton)` (`plato/wiring_default.py:643`) holds: `AsyncAzureBlobStorageProvider.get_instance` returns `cls._instance` before looking at `container_name` (`common/core/storage/async_azure_provider.py:50-62`), and `jazzx_sdk/fabric/blob/store.py:73-79` is the shared entry point both consumers use, so whichever constructs first fixes the container. Not raised.

## PARKED
`test_the_local_wiring_passes_a_pack_store_factory` (`TODO(local-wiring-build-test)`) — source-text assertion, cannot catch a runtime fault in `wiring_local.py:377-381`; `env_text` is imported at `wiring_local.py:91`, so the specific `NameError` the TODO worries about is not present today.

Ran `pytest tests/test_plato_packs_api.py tests/test_policy_*.py tests/test_pack_store.py tests/test_plato_info_api.py -q`: 207 passed, 2 skipped.

VERDICT: BLOCK
