# adversarial review -- japes

- when: 2026-09-10T21:11:04Z
- range: origin/v2.5.1...HEAD (d3db880)
- diff: 11655 lines


## slice1

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
No [DEFECT] survived verification. `ruff check` is clean; 567 tests in the touched areas pass.

## CHECKED

- **`TODO(publish-tick-tie)`** (`pack/store_db.py:59`) — holds. `plato/seed.py:124` is a sequential `for pack_dir in candidates` loop, one blob write + one DB round trip per `publish`; the initialize route (`plato/packs_api.py:162`) delegates to it. No concurrent publish path.
- **`TODO(activation-vs-active-rules)`** (`policy.py:_all_rules`) — holds. `executable_rules`/`activation_blocks`/`is_executable`/`activation_block` are referenced only from `tests/test_policy_rule_activation.py`; no shipped caller.
- **`TODO(refused-rule-field-claim)`** (`experts/policy/default.py:497`) — holds. `allowed=not violations and not not_activated` (line 537), so a dropped field claim costs a misleading violation entry, not a wrong allow; the `EVIDENCE_MISSING` `continue` two lines below is identical.
- **`TODO(threshold-grid-provenance)`** (`profiles.py:243`) — holds, including the self-correction. `_coerce_threshold_mappings` is registered on `thresholds`/`custom` only, `tables` has no gate, and every `thresholds:` entry in the shipped profile is a bare string, so `looks_authored` is False for all of them.
- **`TODO(orphan-blob-gc)`** (`store_db.delete`) — holds. `delete` issues only the `sa_update`; the store is tenant-scoped at construction so it cannot run a cross-tenant refcount.
- `.gitignore`'s four names all match the shipped defaults (`wiring_default.py:441,626`, `wiring_local.py:159,378,383`). `retired_at` has migration `0004_pack_version_retired_at` with the matching `ix_pack_version_retired_at` index. `materialize` reaches the tombstone filter via `self.get`, so the abstract `delete` docstring's "reads skip it" holds.

## Findings

**[SYMMETRY]** `jazzx_sdk/manifest/lifecycle.py:28` — `as_utc` is added to `fabric/canonical/__init__.py`'s imports and `__all__` in this diff, but is missing from the two export surfaces on its own module: `lifecycle.py:28`'s `__all__ = ["PolicyStatus"]` (which sits above the `as_utc` definition at line 31), and `manifest/__init__.py:13`, which re-exports `PolicyStatus` from `lifecycle` and not `as_utc`. So the only public path to the new helper is `fabric.canonical` — the package `as_utc` was moved out of to break the cycle. One line in each of the two files, which is smaller than what the diff already does in `fabric/canonical/__init__.py`.
IMPORTANCE: low -- three export surfaces enumerated (`lifecycle.__all__`, `manifest/__init__.__all__`, `fabric/canonical/__init__.__all__`); every in-repo importer (`policy.py:22`, `guidance/schema.py:22`) names the symbol explicitly so nothing breaks today. Nothing outside this repo imports it yet.

**[NOTE]** `jazzx_sdk/manifest/lifecycle.py:37` — the docstring says six named modules "carry private copies of the same three lines", and three of the six do not. `manifest/store_db.py:77`, `agents/interactive/session_db.py:55` and `audit/events_db.py:56` match exactly; but `pack/store_db.py:107` is `_as_utc(value: datetime | None) -> datetime | None` and `queue/execution_db.py:36` guards `value is not None`, so swapping in `as_utc` on the strength of this sentence raises `AttributeError: 'NoneType' object has no attribute 'tzinfo'` on any nullable timestamp column — and `pack/store_db.py` is edited in this same diff and keeps its copy. `observability/span_mapping.py:67` is one branch of a normalizer that also handles int-nanoseconds and float-epoch, not a copy of these three lines at all.
IMPORTANCE: medium -- I read all six named modules. The claim is a work list for the next person and following it breaks two of the six; nothing consumes it today.

**[NOTE]** `jazzx_sdk/experts/policy/default.py:626` — the `detect_staleness` docstring still says "Three independent checks, **first match wins per policy**", which the diff deliberately abandoned: causes now accumulate into a list joined with `" and "`. Trigger: a `DEPRECATED` policy whose pinned version was also superseded gets `staleness_reasons[pid] == "pinned version 'v1' superseded by current version 'v2' and status is 'deprecated', not active"` — two reasons, not the first match. The in-loop comment at line 663 states the new rule correctly; only the method contract 37 lines above was left behind.
IMPORTANCE: low -- one docstring on one method; `StalenessReport` consumers are `finance/validation.py:871` and tests, neither of which parses the reason string.

**[NOTE]** `jazzx_sdk/finance/validation.py:799` — the docstring says "``warnings`` is read only when ``refusals`` is empty", but the code (line 843) also requires `allowed` to be falsey **and** `policy_violations` to be empty. Trigger: an out-of-repo expert returning `allowed=False`, one `policy_violations` entry, and a `warnings` entry naming a refused rule — the warning is dropped, though the docstring says it would be read. The code comment immediately above the branch does state all three conditions, so this is the paragraph, not the logic.
IMPORTANCE: low -- reachable only from a pack expert on the older `ComplianceResult` shape; the in-repo producer (`DefaultPolicyExpert.check_compliance`) always sets `refusals` alongside `warnings`, so the branch is unreachable from any in-repo call site.

VERDICT: PASS

## slice2

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:404` — `DSCR-IO-MIN-LOAN`'s description states "Interest-only products require a minimum loan amount of $250,000 **and minimum FICO 640**", but its condition checks only `loan_amount >= 250000`; no rule in the corpus enforces an IO FICO floor.
Trigger: `product_is_interest_only=True, fico=620, loan_amount=300000, cltv_pct=60, loan_purpose=purchase` (plus clean values on the process-gate fields) — I evaluated all 41 rules against that context and every applicable one returns SATISFIED. `profile.custom.fico_min_io: 640` exists with no consuming rule (a sweep for `product_is_interest_only` returns only this rule; only `DSCR-ELIG-CLTV-GRID`, `DSCR-SUB-1-DSCR-FICO`, `DSCR-ITIN-CLTV`, `DSCR-NO-RATIO-FICO` have a `fico` predicate, and the grid's NA cells only bite below 600). `rule.description` is not inert prose: `jazzx_sdk/fabric/canonical/policy_registry.py:186-187` returns it verbatim as the guidance `title`/`text`, so a caller is told a FICO 640 check ran when it did not. Contrast the sibling `DSCR-ITIN-CLTV`, whose two extra claims *are* each backed (`DSCR-ITIN-LOAN-MAX` for the $1M cap, the ITIN grid's `NA` cells at 620/600/lt600 for the 640 floor) — that is the pattern this rule breaks.
`IMPORTANCE: medium` — I enumerated all 41 rules in this file and confirmed no sibling covers the gap. The bundled copy is inert in plato (nothing in `jazzx_sdk` or `plato` reads it; `seed.py` publishes it and `packs_api` serves it), so the reachable consumer is jaci's own copy of the same corpus, where the text reaches a credit-guidance surface. Not `high` because the verdict itself is correct for what the rule encodes — only the returned prose overstates it.

---

**[NOTE]** `plato/data/seed_packs/ci-spread-core/policies/core.yaml:5-6` — "FCCR / ABL / LIEN remain in registry.py until similarly extracted" is contradicted by `policies/conventions.yaml` added in the same commit, which authors all three, and by `pack_manifest.yaml:42`'s own note listing them. I loaded the pack: `policy_files` resolves to `['conventions.yaml', 'core.yaml']` and the registry holds all six ids with no Python registry involved.
`IMPORTANCE: low` — a reader editing an FCCR threshold is sent to a `registry.py` that does not exist in this deployment; no runtime effect.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md:74` — the §1b argument that "Residential 1-4 needed no unit-count check" rests on enumerating PropertyType as `sfr/condo/townhome/pud/condotel/manufactured/two_to_four_unit`, but every rule in `eligibility.yaml` matches on `warrantable_condo`/`non_warrantable_condo` and never `condo`. One of the two spellings is wrong, and the one in the notes is what the five bare-state PPP rules are justified by.
`IMPORTANCE: unknown` — the enum is defined in jaci's `LoanApplication`, which is not in this repo, so I could not determine which spelling is real. If it is `condo`, the three `DSCR-CONDO-HOA-*` and `DSCR-HIGH-LTV-PROPERTY-TYPE` rules never match a plain condo.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:9` — the field is named `sha256` but holds `dc45ea0d58532f5c`, 16 hex chars, not a 64-char SHA-256; same 16-char shape as `source_sha` in `pack_manifest.yaml:100` and `sha=` in `playbooks/loan_package_scaffold.md:1`.
`IMPORTANCE: low` — provenance metadata only; nothing in `jazzx_sdk` verifies it. Someone trying to re-verify the source PDF against the recorded digest cannot.

---

CHECKED:
- `plato/info_api.py:83` `TODO(omit-info)` — the claim holds. `_info_only_app`'s `degraded_nav = ("Guide", "config", "database", "Packs", "packs")` (`plato/__main__.py:302`) does not include `info`, and it is the only caller passing `omit` to `dashboard_html`. The amended wording is accurate.
- `plato/data/seed_packs/ci-spread-core/workbook_layout.yaml:8-12` — "Every key in this file and in `workbook_layout_rb.yaml` does resolve today" holds; I resolved every `key` against `chart_of_accounts.yaml` (84 keys) and `metrics.yaml` (36) and got zero unresolved in both layouts. `spread_template.yaml`'s seven catalog-absent metric ids (`da`, `stock_based_compensation`, `net_leverage_x`, `dso_days`, `dio_days`, `dpo_days`, `ccc_days`) are already disclosed in `metrics.yaml:16-19` and five of them resolve in `jazzx_sdk/finance/metrics.py`, so that is not the asymmetry with `spread_template_rb.yaml`'s `unbound:` rows it first looks like.
- `pack_manifest.yaml:41-47`'s claim that the `dir:` glob is one level (so `policies/overlays/deal_si_rb_example.yaml` stays out and the checklist is skipped by shape) holds — `manifest_loader.py:184` globs `base.glob("*.yaml")`, and both packs load exactly the declared corpora.
- Migration `0004` matches the model: `PackVersionRow.retired_at` (`jazzx_sdk/pack/store_db.py:102`) declares `index=True`, yielding the same `ix_pack_version_retired_at`, and `pack_version` has a `TABLE_SCHEMAS` entry (`plato/models.py:75`) so `_schema` will not raise on Postgres. `tests/test_plato_migration.py`, `test_plato_schema_version.py`, `test_pack_store.py`, `test_plato_packs_api.py`, `test_plato_info_api.py` all pass (192 passed, 2 skipped).

VERDICT: BLOCK

## slice3

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
**CHECKED**
- `TODO(if-match-post)` (posture.py:96): premise holds — `require_if_match` appears only in `plato/posture.py` and `tests/test_plato_preconditions.py`; no router mounts it, so `POST /packs/initialize` cannot 428 today.
- `TODO(seed-pack-vendoring)` (seed.py:39): holds — the only remaining `jaci` strings under `plato/data/seed_packs/` are data/prose/comments; `Pack.from_manifest` + the attribute sweep resolve with japes alone.
- `TODO(azure-blob-container-singleton)` (wiring_default.py): holds — `BlobStore._azure()` memoizes per instance and calls `get_instance(container_name=...)` lazily, and `container=` is the right kwarg, so the container is fixed by whichever azure consumer touches the provider first.
- `TODO(seed-digest-drift)` (seed.py:170): holds — `pack_archive` uses `ZipFile.write(path, ...)`, which records each file's mtime, so a digest comparison would false-positive on a rebuilt image.

---

**[DEFECT] `plato/posture.py:174` — `JAPES_STRICTNESS=strict` does not close the pack writes, only the tier does.**
The gate was moved off `strict_mode()` to fix the loosening direction, but nothing replaced the tightening direction: `blocked` ignores the strictness override entirely, so a replica told to tighten still accepts the two new writes. Verified: with `JAPES_ENVIRONMENT=dev-daily JAPES_STRICTNESS=strict`, `strict_mode()` is `True` yet `relaxed_only("pack deletion")` returns without raising, so an unauthenticated `DELETE /api/v1/packs/{id}/{version}` retires a published version (burning the version number permanently) while `PATCH {prefix}/config` on that same replica is refused 403 by `withholding(strict=True, auth=None)`. `blocked = strict_mode() or environment_tier() not in (LOCAL, DEV_DAILY)` is the whole fix.
IMPORTANCE: medium -- the two write routes added here (`plato/packs_api.py:161`, `:176`) and only those; reachable on any `local`/`dev-daily` replica that sets the one variable `posture.check_settings` documents as "turns the refusal back on", with no auth dependency in either shipped wiring (`wiring.auth` is `None` in `wiring_default` and `wiring_local`); no shipped caller passes `strict=` to `create_packs_router`, so the env var is the only path; nothing outside this repo mounts these routes.

---

**[NOTE] `plato/config_api.py:47` — the catalogue's "exact set `wiring_default` consults and no more" is now false.**
This diff adds four variables `wiring_default` reads (`PLATO_PACK_BLOB_BACKEND`, `PLATO_PACK_BLOB_DIR`, `PLATO_PACK_BLOB_CONTAINER`, `PLATO_PACK_CACHE_DIR`) plus `PLATO_SEED_PACKS_DIR` in `plato/seed.py`, and none reaches `plato_settings_catalog()`. An operator reading `/v1/config` as the complete set cannot see or fix the non-durable-archive condition `_pack_blob` warns about at boot — and simply adding the fields would not work either, since `pack_blobs = _pack_blob()` is resolved once in `build()` and `_rewire` only re-reads the bootstrap keys.
IMPORTANCE: medium -- `plato_settings_catalog()` feeds `/v1/config` GET/PATCH and the settings page; reachable on every replica; the workaround is setting the env vars at deploy time, so nothing is silently wrong, only unreachable through the surface that claims completeness.

**[NOTE] `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:150,165` — shipped pack data carries archaeology naming the consumer.**
Both comments say what the manifest used to bind and which repo it bound to (`named jaci symbols, so Pack.conductor raised ModuleNotFoundError`; `paths inside the authoring repo`). This file ships in the wheel and the image, and `test_which_seed_pack_files_name_consumer_modules_is_pinned` now pins `pack_manifest.yaml` into the "carrying jaci" set, so the reference is held in place by a test rather than being on its way out.
IMPORTANCE: low -- two comment lines in bundled data; no behaviour depends on them; removing them requires dropping `pack_manifest.yaml` from that test's pinned list.

**[NOTE] `tests/test_plato_packs_api.py` (`TODO(local-wiring-build-test)`) — the deferral's premise "Nothing else imports the module today" is wrong.**
`tests/test_plato_wiring.py:160` and `:189` import `_find_root` and `_ALEMBIC_INI` from `plato.wiring_local`. The conclusion still holds, because `importlib.reload` re-executes those two from `__file__` rather than from this test's `tmp_path`; it is only `DB_PATH`/`CACHE`/`PACK_BLOBS` that stay frozen, and no other test reads them.
IMPORTANCE: low -- test-only, order-dependent, and no current importer touches the frozen names; correcting the TODO's wording is the whole response.

VERDICT: BLOCK
