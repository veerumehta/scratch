# adversarial review -- japes

- when: 2026-09-11T16:37:47Z
- range: 097c049..HEAD (since the last review) (3c9805d)
- diff: 295 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_mock_knowledge_hub_app.py` → 20 passed; `pytest tests -k "entit or fabric or canonical or mock"` → 584 passed. Real-Hub codes checked against `/Users/sangit/src/knowledge_hub/app/reasoning/api.py` (v1 POST 409/422 at 546/570, v1 PATCH 422 at 1629, 409 at 1633). Mock PATCH behaviour reproduced by hand through `TestClient`.

WAS: plato/wiring.py :: timeout-budget-shared-across-pools -- FIXED; the comment now states one budget for the whole call, and `dispose_all` pops one at a time so stores the budget never reached stay on `_retired`.
WAS: plato/wiring.py :: env-const-missing-from-all -- FIXED.
WAS: tests/test_plato_boot_contract.py :: timeout-not-distinguished-from-raise -- FIXED; the assertion now names `TimeoutError`, and the subprocess test still returns `rule.job_exit`.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; the YAML is untouched, no TODO on it, and the guards still live only in jaci's `LoanApplication`.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested
ACCEPTED: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer

New findings against this diff:

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:268 :: patch-route-collapses-codes -- the PATCH route still maps every `ValueError` to 400, so the `EntityValidationError` this diff introduced arrives as 400 there while the same failure is 422 one route above, and the real v1 PATCH answers 422.
Reproduced: create `{"name": "e-1", "collection_id": "col-1", "ontology_id": <registered>, "entity_type": "flag", "json_value": {"severity": "high"}}` → 201, then `PATCH /api/v1/reasoning/entities/<id>` with `{"json_value": {"severity": "catastrophic"}}` → `400 {"detail":"entity (type='flag') failed ontology ... validation"}`. `knowledge_hub/app/reasoning/api.py:1628` maps `EntityValidationError` to 422 on that route and `IntegrityError` to 409. A caller branching on 422 for an update passes here and fails against the service, which is the failure mode the new POST mapping exists to prevent.
IMPORTANCE: medium -- the only route family this app exposes for entities is POST/PATCH; both are reachable by any pack pointing at `create_knowledge_hub_mock_app` (the module docstring names jazzx-assistant as the intended consumer shape). Fails loudly as a wrong status code, no silent data.

[DEFECT] jazzx_sdk/clients/mocks.py:1051 :: mock-raises-where-client-returns-none -- `create_entity` now raises out of the client on a duplicate name, but the real `KnowledgeHubClient.create_entity` logs the 409 and returns `None` (`knowledge_hub_client.py:1609-1615`), so the direct-client consumers reach an exception path production cannot produce.
Trigger: `ClientLayer` substitutes `MockKnowledgeHubClient` when the real client is unavailable (`client_layer.py:280`, `:312`), and `fabric.py:139`/`:318` and `fabric/__init__.py:66` back the fabric stores with it. Against that, `CanonicalStore.store_policy` (`fabric/canonical/store/core.py:109`, entity name = `policy.policy_id`) storing the same policy id twice raises `EntityNameConflict` instead of taking its documented `result is None` branch and raising `RuntimeError("Failed to store policy ...")`; `EntityStore.create` (`fabric/entities/store.py:58`) likewise promises "None on failure (HTTP status logged)" and now propagates a `ValueError`. With `data_dir` set the entities are persisted and reloaded (`_load_from_data_dir`, `_persist_entities`), so a second dev run over a seeded directory hits it, not just a repeat within one process.
IMPORTANCE: medium -- dev/mock configurations only (no real-service path changes), fails loudly, and no test in this repo covers a repeated-name write, so nothing catches it; `ensure()` is unaffected because the mock's `create_entity_v2` is preferred and never reaches the v1 fallback.

[DEFECT] jazzx_sdk/clients/mocks.py:1141 :: v2-docstring-dup-claim-stale -- `create_entity_v2`'s docstring still says of the sibling "calling it twice with the same name is two separate entities in this mock", which this diff made false for the default `validate_entities=True`.
Confirmed: two `create_entity` calls with `name="e-1"`, same `collection_id`, on a default `MockKnowledgeHubClient()` → `EntityNameConflict`, not two entities. The same paragraph is the one a caller reads to decide between v1 and v2.
IMPORTANCE: low -- docstring only, in the same file as the change; `knowledge_hub_client.py:1632` states the v1 contract correctly and is unaffected.

[DEFECT] jazzx_sdk/server/mock_knowledge_hub_app.py:35 :: module-doc-400-example-stale -- the module docstring's error-mapping paragraph says "a `ValueError` (e.g. `MockKnowledgeHubClient.create_entity`'s ontology-schema validation) maps to 400", and that exact example now maps to 422.
Trigger: the new `test_an_entity_failing_its_ontology_schema_is_a_422` asserts the behaviour the docstring denies, in the same commit.
IMPORTANCE: low -- docstring of the file the diff edits; consumers read it to predict the adapter's codes, so a stale example is what sends them to 400.

[NOTE] jazzx_sdk/server/mock_knowledge_hub_app.py:219 :: missing-fields-still-400 -- the absent-scalar-field guard three lines above the changed `json_value` check still answers 400, where the real route's `EntityCreate` body model makes an absent `name`/`collection_id`/`ontology_id`/`entity_type` a 422; the new test calls this "an accepted gap", but there is no `.reviews/accepted.md` in this repo and it is not in the accepted list, so nothing records the deferral. A `TODO(missing-fields-still-400)` on that line would close it.
IMPORTANCE: low -- same route and same class of mismatch as the fixed one, wrong-status only; I did not find a japes consumer branching on 400 vs 422 for a malformed create body.

[NOTE] plato/wiring_default.py:596 :: swap-docstring-sentence-duplicated -- the appended sentence repeats "`release_wiring` drains that list when the process ends" verbatim from three sentences earlier in the same paragraph; only the "within the close budget it allows" clause is new.
IMPORTANCE: low -- docstring of `swap_database`, read by whoever next changes the swap; no behaviour.

[NOTE] jazzx_sdk/clients/mocks.py:1589 :: update-skips-name-uniqueness -- `update_entity` accepts a `name` that collides with another entity in the same collection, where the real v1 PATCH surfaces the integrity error as 409 (`api.py:1633`); the new `_reject_duplicate_name` is called only from `create_entity`.
IMPORTANCE: low -- reachable through the PATCH route and the direct client, but no japes caller renames an entity (grepped `update_entity` call sites: `mock_knowledge_hub_app.py:267` and the client wrapper only).

VERDICT: BLOCK
