# adversarial review -- japes

- when: 2026-09-09T01:39:53Z
- range: f92e76e..HEAD (since the last review) (a095df2)
- diff: 145 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Previous round

- WAS: `[DEFECT] store.py:95` (`get`/`delete` join an unguarded key) — **FIXED**; both now go through `_local_path` (`store.py:105,118`), and `tests/test_fabric_blob.py:137-161` fails against the old code.
- WAS: `[NOTE] store.py:135-138` `content_key` promises the refusal flatly — **FIXED**; `store.py:150-152` now scopes it to the local backend and says the Azure key is unchecked.
- WAS: `[NOTE] tests/test_fabric_blob.py:117` rglob assertion can't see the escape — **FIXED**; the comment at `:119-121` now says which case the assertion covers and which is carried by `raises`.

## New review

**[SYMMETRY]** `jazzx_sdk/fabric/blob/store.py:48` — the guard is added to `BlobStore` only, while the sibling fabric store `DocStore` does the identical unchecked join with a caller-supplied name at `jazzx_sdk/fabric/docs/store.py:502` (`_put_local`), `:757` (`_get_local`) and `:1792` (`_delete_local`); `fabric.docs.get("../../../etc/passwd")` under `retrieval_mode` `LOCAL` or `CACHED` (fallback at `docs/store.py:687`) reads that file and returns its bytes as a document. Lining up means the same three-line shape check, at the same layer.
IMPORTANCE: medium — I enumerated the `fabric.docs.get` call sites: `agents/interactive/reads.py:136` passes a `document_id` that arrives from an agent tool call, `tools/documents/local_cache.py:143,155` passes a caller-supplied `doc_id`; both reach `_get_local` whenever `retrieval_mode` is `LOCAL` or KH is down under `CACHED`. Nothing in `plato/` mounts a route onto it, so the reachable callers are library consumers (jaci, juno). Untouched by this diff, so it does not block.

**[NOTE]** `jazzx_sdk/fabric/blob/store.py:97` — `get`'s docstring still says "or None if absent" and the Azure branch at `:103` funnels every failure to `None`, but the local branch now raises `ValueError`; the same pointer `{"__blob__": "blob://../x"}` read back through `fabric/conversation_store.py:97` raises on `blob_backend="local"` and returns `None` on `"azure"`.
IMPORTANCE: low — the divergence is in how a malformed key is *reported*, not in what is read (an Azure blob name has no traversal semantics); the only in-repo callers passing a non-derived pointer are `agents/document/agent.py:396,424`.

**[NOTE]** `tests/test_fabric_blob.py:151` — the `# so blobs/ resolves` comment states a precondition the test does not have: `_local_path` raises before touching the filesystem, and with no `blobs/` directory `get` would return `None` and `delete` `False` anyway, so the write only makes the fixture read naturally.
IMPORTANCE: low — test-only; no shipped-code defect behind it.

VERDICT: PASS
