# adversarial review -- japes

- when: 2026-09-17T17:20:48Z
- range: 7f1d7b1..HEAD (since the last review) (9a6b5b0)
- diff: 1120 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: docs/DEPLOYMENT_ENV.md :: activation-not-visible-to-bound-agents -- FIXED (unchanged since; `invalidate_all` at `plato/wiring/default.py:629`).
WAS: tests/test_dev_daily_env_template.py :: doc-guard-split-from-boot-contract -- FIXED.
WAS: tests/test_plato_wiring_default.py :: manifest-field-count-off-by-one -- FIXED.
WAS: tests/test_dev_daily_env_template.py :: documented-is-substring-only -- STILL OPEN in the same half: the guard is now `tests/test_plato_boot_contract.py:97` and `re.findall(r"[A-Z][A-Z0-9_]{2,}", ...)` still collects tokens from prose anywhere in the file, so `DEBUG`, `VERSION`, `DESCRIPTION`, `PROJECT_NAME` are satisfied by any English-word occurrence.
WAS: plato/app.py :: swap-leaves-agents-bound -- STILL OPEN: `_rewire` still calls `await swap_database(db)` at `plato/app.py:202` and never `runtime.invalidate_all()`.
WAS: tests/test_plato_boot_contract.py :: token-check-claim-overstated -- STILL OPEN: docstring unchanged; the "appears only in a sentence saying it is unsupported" case is still not covered by a token search.
WAS: plato/wiring/default.py :: invalidate-all-crosses-tenants -- STILL OPEN: `plato/wiring/default.py:629` unchanged, process-wide drop under a tenant-scoped message.
WAS: plato/wiring/default.py :: llm-manager-captured-at-build -- STILL OPEN: `plato/wiring/default.py:644` unchanged.

[DEFECT] jazzx_sdk/config/envvars.py:128 :: dev-alias-rationale-inverted -- the module's tier preamble still argues that `dev` must not mean the cloud tier, thirteen lines above the table that now maps it there.
Lines 128-131 read "`dev` reads as a synonym for 'my laptop' ... so reusing it would flip both to deployed and take away the Mock fallback their developers rely on. The failure would land on laptops" -- which is exactly what `_TIERS["dev"] = DEV_DAILY` (line 145) now does, and what `docs/DEPLOYMENT_ENV.md:136` documents as an accepted consequence. A reader of the preamble gets the opposite rule from the table it introduces; this is the one comment in the file that was not updated with the mapping.
IMPORTANCE: medium -- comment-only, but it is the stated rationale for the decision this diff reverses, in the module every posture reader imports (`posture.py`, `settings.py`, `plato/api/info.py`, `plato/wiring/default.py`). Nothing outside the repo reads it; the cost is the next author reverting the mapping on the strength of the paragraph above it.

[DEFECT] jazzx_sdk/config/settings.py:273 :: production-fills-no-urls -- the three URL validators branch on `prod` only, so the canonical spelling `production` fills in nothing, while this diff's docs newly promise staging/production auto-configuration.
Verified: `JAPESSettings(environment="production").knowledge_hub_url / kernel_url / process_url` are all `''`; with `environment="prod"` they are `https://knowledge-hub.jazzx.ai` / `kernel.jazzx.ai` / `process.jazzx.ai`. `docs/SETTINGS_CONFIGURATION.md`'s new table heads that column "Production", and `README.md:807-809` now says "auto-configured on staging/production", while `README.md:806` presents `production` as the canonical name and `prod` as the alias. All three validators have it (lines 273, 287, 301), i.e. the whole family.
IMPORTANCE: medium -- reachable by any consumer that sets the spelling the new docs recommend; symptom is an empty KH/kernel/process URL, which a deployed posture reports as a degradation rather than silently mis-routing, so it fails visibly. Reachers enumerated: `get_runtime_settings()` consumers of `knowledge_hub_url`/`kernel_url`/`process_url` inside this repo; jaci and juno pin japes and construct `JAPESSettings`, so the spelling is externally reachable.

[DEFECT] .env.dev-daily:19 :: unmarked-defaults-claimed-load-bearing -- the header states that every assignment without `(default)` is load-bearing and that the test holds each default claim, and two assignments state a code default in prose without the marker.
`PORT=8000` (line 62) with `plato/__main__.py:293` reading `env_int('PORT', default=8000, floor=1)`, and `LOG_LEVEL=INFO` (line 96) with `common/core/log_settings.py:161` resolving "LOG_LEVEL env var > INFO" and `DEBUG` listed as absent/false at line 116. Both are omit-and-nothing-changes, so line 19's "Every other assignment is load-bearing: omit it and this replica behaves differently" is false for them, and `_marked_as_default()` skips them, so line 22's "a changed default cannot leave a false one here" does not cover the two claims written as prose.
IMPORTANCE: low -- doc-only: line 3 of the file says nothing reads it. Read by devops entering ACA variables and by whoever diffs this against juno's config; the cost of the stale half is entering two variables that already match, or a prose sentence going false when `env_int`'s fallback moves with no test firing.

[DEFECT] tests/test_dev_daily_env_template.py:178 :: db-default-read-from-process-env -- the guard reads the value it claims to derive from the code out of the same process environment, and feeds a tier name to a parameter that takes a posture.
`resolved_db_backend` (`plato/wiring/default.py:897`) is `env("PLATO_DB_BACKEND") or ("common" if posture else "sqlite")`, and nothing in `tests/conftest.py` clears `PLATO_DB_BACKEND` (its autouse fixtures clear only `PLATO_SEED_PACKS_DIR`). With `PLATO_DB_BACKEND=sqlite` exported in a shell -- an ordinary local-dev value -- `test_a_line_marked_default_really_is_one[PLATO_DB_BACKEND]` fails against an unedited file; with `common` exported the assertion compares the variable to itself. Separately, `posture` is `deployed_posture()`'s output (None-or-name) and the call passes the tier name, so it is only truthiness that is being tested: `resolved_db_backend("local")` returns `"common"`, which falsifies the comment on line 176 that "off a deployed tier the same expression answers sqlite" -- the guard would pass unchanged if the file's tier were edited to `local`.
IMPORTANCE: low -- test-only, and no shipped-code defect follows: `plato/wiring/default.py:179` and `:860` pass `deployed_posture()`'s own return, and `:914` passes nothing. `tests/test_plato_schema_version.py:488-505` already covers the sqlite/common/explicit cases with the process environment controlled, which is the isolation this one omits.

[SYMMETRY] tests/test_host_identity.py:33 :: bare-env-omits-strictness -- `bare_env` clears the platform markers, the four posture variables and `APP_NAME` but not `JAPES_STRICTNESS`, while `tests/test_plato_info_api.py:32` already defines `_POSTURE_KEYS = (*_POSTURE_ENV_VARS, "JAPES_STRICTNESS")` for exactly this reason, and `test_the_container_name_cannot_promote_past_dev_daily` asserts `not strict_mode()` -- which `strict_mode`'s `override == STRICT` branch flips from an ambient `JAPES_STRICTNESS=strict`. Adding the one name to the fixture's tuple is smaller than the assertion it protects.
IMPORTANCE: low -- test-only, and green today: the suite passes here (191 passed) because no runner exports `JAPES_STRICTNESS`. It is the same variable the eight `_POSTURE_KEYS` loops in `test_plato_info_api.py` clear, so the failure mode is a red test on a developer's machine, not a shipped defect.

[NOTE] jazzx_sdk/config/envvars.py:325 :: unrecognised-reads-local-now-narrower -- "An unrecognised value reads as ``local``, which is the permissive answer" is now true only off a container: with `JAPES_ENVIRONMENT=prodution` on ACA, `_strongest_signal()` returns None and the container branch answers `dev-daily`, i.e. deployed. The same claim is restated as the reason for the refusal at `jazzx_sdk/config/posture.py:74` ("so `ENVIRONMENT=prodution` resolves 'not deployed' here *and* in `wiring_default`'s store and Knowledge Hub choices").
IMPORTANCE: low -- behaviour is unaffected: `check_settings` raises on the unreadable variable before either reader is consulted, so the typo still gets one loud failure. Doc-only inaccuracy in the two docstrings a future reader would use to reason about the typo path.

VERDICT: BLOCK
