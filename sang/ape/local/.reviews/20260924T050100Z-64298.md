# adversarial review -- japes

- when: 2026-09-24T05:01:00Z
- range: HEAD~1...HEAD (d93df81)
- diff: 164 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
The diff has no defects. There is one symmetry finding and two notes.

[SYMMETRY] jazzx_sdk/observability/run_tracer.py:178 :: tracer-backend-silent-default -- `resolve_tracer` dispatches on one name and treats everything else as its default, the same way the four fabric stores do, but it got no `check_backend` call.
Calling `resolve_tracer("mlfow")` quietly returns `NoOpTracer()`, so runs go untraced and nothing is logged. The fix is the same one-line call these four sites got, with a new `TRACER_BACKENDS = ("noop", "mlflow", "auto")` next to the others and `falls_back_to="noop"`.
IMPORTANCE: medium -- `resolve_tracer` is public in `jazzx_sdk.observability`. I did not check which consumers pass a backend string from config, so I can't say how often a typo would reach it.

[NOTE] jazzx_sdk/fabric/db/store.py:307 :: backend-property-echoes-typo -- The `DbStore.backend` docstring says it returns ``common`` or ``sqlite``, but after the warning it still returns the unrecognised name.
`DbStore(backend="sqllite").backend == "sqllite"`, while every path through the store (lines 192, 260, 290, 367) resolves through `common`. Storing `falls_back_to` in `self._backend` after warning would make the property say what is actually in use. The same change would work for `BlobStore._backend`.
IMPORTANCE: low -- nothing in `jazzx_sdk` or `tests` reads `.backend` on a `DbStore`. I did not check readers outside this repo (jaci). The warning is already logged.

[NOTE] jazzx_sdk/fabric/fabric.py:169 :: guidance-fallback-not-rag -- An unrecognised `guidance_backend` is reported as "using 'rag' instead", but when `self.rag is None` the code falls into the `else` branch and sets `self.guidance = None`.
Trigger: `guidance_backend="ragg"` with no KH backing, so `rag` is None. The log says rag, and `fabric.guidance` is None. A correctly spelled `"rag"` in the same setup also gives None, so the message matches the default's behaviour but not what the operator ends up with.
IMPORTANCE: low -- the only effect is one misleading log line, in configurations with no KH backing.

VERDICT: PASS
