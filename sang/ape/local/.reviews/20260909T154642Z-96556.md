# adversarial review -- japes

- when: 2026-09-09T15:46:42Z
- range: HEAD~1...HEAD (5843320)
- diff: 1511 lines


## whole

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Three defects, both symmetry slots used.

**[DEFECT] `jazzx_sdk/pack/store_db.py:250`** — `get(pack_id)` with no version orders on `published_at.desc()`, so publishing a *lower* version after a higher one makes it the head; `TODO(publish-tick-tie)`'s deferral says the trigger needs "two `publish` round-trips inside one microsecond", which is not the trigger. Verified: `publish(0.2.0)` then `publish(0.1.0)` seconds apart → `get("p").version == "0.1.0"`, `list_packs()` → `[("p","0.1.0")]` (same shape at `:283`), and `versions()` — documented "oldest first" at `store.py:111` — returns `["0.2.0", "0.1.0"]`. A patch on an older release line is an ordinary publish, not a microsecond race, so the part of the TODO that does not hold is its blast-radius claim.
IMPORTANCE: medium -- callers enumerated: nothing in japes or plato calls `publish`/`get` (only `plato/models.py:49` constructs the store for metadata), so no shipped route is wrong today; it is the documented contract of a new public SDK surface that jaci/plato will call, and the row is immutable so a wrong head cannot be repaired in place.

**[DEFECT] `jazzx_sdk/pack/store_db.py:312`** — the cache short-circuit returns the marker file's contents as the pack root with no check that it is under `root` or that it holds a manifest, so a pre-existing marker redirects `materialize` anywhere; the digest verification below is also skipped on that path. Verified: writing `/etc` into `<cache_root>/<digest16>/.pack_manifest_root` makes `materialize("p","0.1.0")` return `/etc`, which is then handed to `PackManifestLoader`. With the default cache root (`tempfile.gettempdir()/japes-packs`, `:344`) any process on the box can create that path before the first materialize. The comment above it accounts for trusting an *unpacked tree*, not for following an unvalidated path out of `root` — and `_unpack` at `:373` enforces exactly that containment for archive members.
IMPORTANCE: medium -- no in-repo caller reaches `materialize` (grep: tests only), so nothing shipped is exploitable now; it is reachable by any external consumer of the new API in the default configuration, and the failure is a silently wrong pack root rather than an error.

**[DEFECT] `jazzx_sdk/pack/store_db.py:181`** — `publish` never opens `archive`, so non-zip bytes commit as an immutable version, and `materialize` then raises `zipfile.BadZipFile`, which is not a `ValueError` and not `PackNotFound` — the only two failures the method's docstring and every other raise in it use. Verified: `publish(manifest, archive=b"not a zip at all")` returns a record; `materialize` → `BadZipFile: File is not a zip file`; republishing that version is refused, so the version number is spent.
IMPORTANCE: medium -- same call-site set as above (no shipped caller); a consumer catching `(PackNotFound, ValueError)` around `materialize`, which is what the class documents, gets an uncaught exception, and immutability means the bad row cannot be corrected.

**[SYMMETRY] `jazzx_sdk/pack/__init__.py:29`** — `PackVersionStore`, `PackVersionRecord` and `PackNotFound` are not in the package's `__all__`, though it exports every other pack-level type (`PackManifestLoader`, `PackDependency`, `FRAGMENT_KINDS`). `jazzx_sdk/runs/__init__.py` is the same shape one layer over: it exports the ABC and in-process store while leaving `DbTurnRunStore` to a lazy `jazzx_sdk.runs.store_db` import for the SQLAlchemy cost. Three names in the existing list.
IMPORTANCE: low -- import-path ergonomics for consumers; nothing in this repo imports the abstract store.

**[SYMMETRY] `plato/oidc.py:375`** — the diff adds `limit=200` to the served error at `console_api.py:132` and states the rule in the `reason` docstring it also edits ("pass `limit` wherever the result is served or stored"), but routes `oidc.py:375` and `:424` through an unbounded `reason(exc)`; `TokenError`'s own docstring says a caller catches it to emit a 401, and `:320`/`:395` have the same shape. Adding `limit=200` at four sites is smaller than the docstring paragraph.
IMPORTANCE: low -- `TokenVerifier` has no consumer outside `plato/oidc.py` and its tests (grepped `TokenError`/`oidc` across plato and jazzx_sdk), so nothing serves these strings today.

**[NOTE] `jazzx_sdk/pack/store.py:54`** — the comment says `content_digest` is "sha256 over the archived asset bytes", but `store_db.py:172` hashes the manifest JSON when `archive is None`; `test_an_archive_with_nowhere_to_go_is_refused_not_dropped` publishes exactly that row.
IMPORTANCE: low -- no caller reads the field; `materialize` refuses manifest-only packs before it is used.

**[NOTE] `jazzx_sdk/pack/store_db.py:167`** — the `get`-then-insert check makes `publish`'s documented "raises rather than overwriting" an `IntegrityError` from the PK rather than the `ValueError` under two concurrent publishes, and the blob at `:182` is already written, so the loser leaves an orphan.
IMPORTANCE: low -- needs two concurrent publishers; no publisher exists yet.

**[NOTE] `jazzx_sdk/pack/store_db.py:140`** — `visibility` is documented as "`tenant` or `platform`" (`store.py:65`) but any string is stored unvalidated.
IMPORTANCE: low -- one construction site, and no code branches on the value.

CHECKED:
- `TODO(partial-registration-marker)` (`plato/database_api.py:81`) holds: `describe_database` has no caller outside the route in its own file, and the route is passed the `DatabaseHandle`.
- `TODO(swap-schema-placement)` (`plato/wiring_default.py:527`) holds: the models carry no `schema=`, `TABLE_SCHEMAS` has exactly the eight it names, and `/v1/config` gates the path on a relaxed posture.
- `TODO(if-match-post)` (`plato/posture.py:97`) holds: `require_if_match` appears only in `plato/posture.py` and two test files.
- `TODO(migration-concurrent-index)` (`plato/migrations/env.py:182`) holds: 0001–0003 use plain `create_index` only.

VERDICT: BLOCK
