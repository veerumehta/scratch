# adversarial review -- japes

- when: 2026-09-17T19:11:12Z
- range: 9a6b5b0..HEAD (since the last review) (c2b0585)
- diff: 363 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: docs/DEPLOYMENT_ENV.md :: activation-not-visible-to-bound-agents -- FIXED (`invalidate_all` still at `plato/wiring/default.py:629`).
WAS: tests/test_dev_daily_env_template.py :: doc-guard-split-from-boot-contract -- FIXED.
WAS: tests/test_plato_wiring_default.py :: manifest-field-count-off-by-one -- FIXED.
WAS: tests/test_plato_boot_contract.py :: documented-is-substring-only -- STILL OPEN; `re.findall(r"[A-Z][A-Z0-9_]{2,}", ...)` at line 95 still collects tokens from prose anywhere in the file.
WAS: plato/app.py :: swap-leaves-agents-bound -- STILL OPEN; `_rewire` still ends at `await swap_database(db)` (`plato/app.py:202`) with no `runtime.invalidate_all()`.
WAS: tests/test_plato_boot_contract.py :: token-check-claim-overstated -- STILL OPEN; the docstring at lines 90-93 is unchanged.
WAS: plato/wiring/default.py :: invalidate-all-crosses-tenants -- STILL OPEN; line 629 unchanged, process-wide drop logged per tenant.
WAS: plato/wiring/default.py :: llm-manager-captured-at-build -- STILL OPEN; `agent_extra={"llm_manager": client_layer.llm}` at line 644 unchanged.
WAS: jazzx_sdk/config/envvars.py :: dev-alias-rationale-inverted -- FIXED; the preamble now states the reversal and matches `_TIERS["dev"] = DEV_DAILY`.
WAS: jazzx_sdk/config/settings.py :: production-fills-no-urls -- FIXED; verified `JAPESSettings(environment="production").knowledge_hub_url == "https://knowledge-hub.jazzx.ai"`, and all three validators go through `tier_of`.
WAS: .env.dev-daily :: unmarked-defaults-claimed-load-bearing -- FIXED; the header now names PORT and LOG_LEVEL as unchecked prose claims and `test_the_header_names_the_unchecked_defaults_that_are_still_here` pins them.
WAS: tests/test_dev_daily_env_template.py :: db-default-read-from-process-env -- FIXED; `PLATO_DB_BACKEND` and the posture vars are cleared, the tier is set rather than passed, and the conditional half is pinned by the new test.
WAS: tests/test_host_identity.py :: bare-env-omits-strictness -- FIXED via `_TIER_KEYS`.
WAS: jazzx_sdk/config/envvars.py :: unrecognised-reads-local-now-narrower -- FIXED in both docstrings (`envvars.py:343`, `posture.py:70`).

[DEFECT] README.md:779 :: dev-localhost-url-row-stale -- the environment table still promises that `dev` fills in `http://localhost:{port}`, which this branch removed, and it is the row an external consumer reads before setting the variable.
`JAPES_ENVIRONMENT=dev` now resolves `dev-daily`: `JAPESSettings(environment="dev").knowledge_hub_url == ""` (verified), and `deployed_posture()` reports deployed, so a laptop following this row gets no URLs *and* the deployed refusals the branch's own rationale says a laptop must not have. `docs/SETTINGS_CONFIGURATION.md:62-65` and `README.md:806-809` were both corrected in this branch; this table and `jazzx_sdk/config/settings.py:19` were the two the sweep missed (grepped every `*.py`/`*.md` for `localhost:{port}` and `staging.jazzx.ai`).
IMPORTANCE: medium -- doc-only, but README is the external surface: jaci and juno pin japes and set `JAPES_ENVIRONMENT`, and the row recommends the one spelling whose meaning this branch inverted. Reachers: anyone configuring `JAPESSettings`/`JazzXRuntime.from_settings` from the README.

[DEFECT] jazzx_sdk/config/settings.py:19 :: dev-localhost-url-row-stale -- the module docstring's URL-pattern list still says `dev: http://localhost:{port}`, 253 lines above the validator comment that says there is no `dev` branch since 2.5.2.
Same trigger as the README row: `JAPESSettings(environment="dev")` fills nothing. Line 10 of the same docstring also still advertises the variable as `(dev/staging/prod)`, i.e. the three names, two of which are now aliases.
IMPORTANCE: low -- in-repo readers only; the contradicting statement sits in the same file, so the cost is one confused reader rather than a misconfiguration. Nothing outside the repo renders this docstring.

[DEFECT] tests/test_env_resolution.py:138 :: url-defaults-read-from-process-env -- the guard reads the URLs it claims are derived from the tier table out of the process environment, so an ordinary exported value fails all seven cases against unedited code.
Reproduced: `KNOWLEDGE_HUB_URL=http://stale.local JAPES_KERNEL_URL=http://k.local pytest tests/test_env_resolution.py -k spelling` → 7 failed (`assert 'http://stale.local' == ''`). `knowledge_hub_url` carries `AliasChoices("JAPES_KNOWLEDGE_HUB_URL", "KNOWLEDGE_HUB_URL")` and `model_config` sets `env_prefix="JAPES_"` plus `env_file=".env"`, so `KNOWLEDGE_HUB_URL` -- the un-prefixed platform name `.env.dev-daily:*` itself lists -- wins before the validator's `if v:` ever falls through. Nothing in `tests/conftest.py` clears it; `tests/test_plato_wiring_default.py:33` already lists `KNOWLEDGE_HUB_URL` and `JAPES_KNOWLEDGE_HUB_URL` in `_OTHER` for its `clean_env` fixture for exactly this reason.
IMPORTANCE: low -- test-only and green in a clean shell (54 passed across the three touched files). No shipped-code defect follows: this is the same shape as `db-default-read-from-process-env`, fixed in the sibling file in this diff and reintroduced in the new test.

[SYMMETRY] tests/test_env_resolution.py:121 :: tier-spellings-not-derived -- the parametrize list restates the tier spellings instead of deriving them from `ENVIRONMENT_NAMES`, and omits `test`, the one `_TIERS` key of seven with no case, under a test name that says "every spelling of a tier". `tests/test_plato_wiring_default.py:306` (`test_the_settings_literal_is_derived_from_the_tier_table`) already establishes deriving from that tuple as this repo's pattern for the same table, and parametrizing over `ENVIRONMENT_NAMES` with an expected-URL mapping is no more code than the literal list -- it also makes a future alias fail here until someone decides what it fills in.
IMPORTANCE: low -- test-only; `test` maps to `LOCAL` and fills in `""` today, so the gap is a missing case rather than a wrong one. Checked all seven `_TIERS` keys against the seven parametrize rows.

[NOTE] .env.dev-daily:25 :: log-level-default-not-in-a-call -- the exemption's stated reason ("their defaults are literals inside a call (`env_int('PORT', default=8000)`) rather than a named constant") is true of PORT and not of LOG_LEVEL: `common/core/log_settings.py:188` assigns `resolved_level = logging.INFO` in a branch of `setup_logging`, which is a named constant, and is observable by calling `setup_logging()` with `LOG_LEVEL` unset rather than by reading a declaration.
IMPORTANCE: low -- doc-only, in a file whose line 3 says nothing reads it; the practical consequence is that the reason given for skipping the LOG_LEVEL check is not the reason that actually applies.

VERDICT: BLOCK
