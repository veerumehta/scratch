# adversarial review -- japes

- when: 2026-09-11T04:48:53Z
- range: ff39daf..HEAD (since the last review) (b130af0)
- diff: 120 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/posture.py :: boot-refusal-not-fatal-for-jobs -- FIXED; `run_role` returns 6 for a `job:*` role before `record_boot_note`, and `run_job` is unreachable on a posture refusal.
WAS: jazzx_sdk/config/envvars.py :: blank-rule-restated-not-reused -- FIXED; the loop is `value = env_text(name)` / `if value and value.lower() not in _TIERS`.
WAS: plato/posture.py :: check-settings-docstring-omits-unreadable -- FIXED; the summary names both refusals, says the first is unconditional, and states that an explicit `environment=` skips it.
WAS: tests/test_plato_packs_api.py :: start-refusal-test-omits-job-role -- FIXED; `test_a_job_exits_on_an_unreadable_tier` drives `entry.main([])` with `JAPES_RUN_MODE=job:sweeper` and asserts the body never ran.
WAS: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer -- STILL OPEN; untouched by this diff.
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; untouched by this diff.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested

[DEFECT] plato/boot_contract.py:98 :: posture-job-exit-not-in-contract -- the boot policy's single owner still says `job_exit=SERVES` for "a deployed posture that would admit unauthenticated callers", so the shipped contract (and the table rendered from it into `docs/DEPLOYMENT_ENV.md:249`) tells a supervisor a job runs through this finding while `run_role` now exits 6, and 6 is absent from `JOB_EXIT_CODES` whose docstring calls it "the exit codes a `job:*` role can end on, for a supervisor to branch on".
`JAPES_RUN_MODE=job:sweeper` with `ENVIRONMENT=prodution`: the process exits 6. `plato/boot_contract.py:126` yields only `(4, wiring)` and `(5, schema)`; `docs/DEPLOYMENT_ENV.md:249` prints `serves` in the `job:*` column and `:255` says "The `job:*` codes are the only exit codes worth branching on". Nothing catches the drift: `test_every_documented_job_exit_code_is_distinct_and_named` only checks the table against itself, `test_a_job_with_no_wiring_exits_with_the_documented_code` covers row 1 alone, and the doc-table test compares docs to `as_markdown_table()` which is also derived from the stale row, so the whole suite stays green while all three artefacts describe behaviour the code no longer has. `plato/__main__.py:20` states the rule this violates: "A change to the policy is a change to `BOOT_CONTRACT`". Fix is `job_exit=6` on that row plus the rendered docs line.
IMPORTANCE: high -- this is the exact drift `plato/boot_contract.py` exists to prevent, and it is invisible to CI. Enumerated every consumer of the row: `JOB_EXIT_CODES` (the supervisor-facing list), `as_markdown_table()` → `docs/DEPLOYMENT_ENV.md`, and `tests/test_plato_boot_contract.py`. Reachable in any deployment that runs ACA Jobs with a mistyped posture variable; an orchestrator branching on 4/5 sees an undocumented code, and the documentation actively says the job ran.

[SYMMETRY] tests/test_plato_packs_api.py:443 :: job-exit-code-hardcoded-in-test -- the new test hardcodes `assert code == 6` where the sibling job-exit test three files over reads the code from the contract (`tests/test_plato_boot_contract.py:129-132`: `rule = _find("`PLATO_WIRING` unset")` then `assert result.returncode == rule.job_exit`); asserting against `_find("deployed posture").job_exit` is the same line count and is the assertion that would have failed on the stale row above.
IMPORTANCE: medium -- test-only, but it is the assertion that would have caught the `high` above; it also puts an entry-point/boot-policy case in the packs-API file rather than beside `test_a_job_with_no_wiring_exits_with_the_documented_code`, which is where a future policy edit will look.

[NOTE] plato/__main__.py:15 :: module-docstring-posture-split-unstated -- the module docstring's summary of the policy ("A serving role comes up whatever it finds and reports the reason at `/info`; a `job:*` role exits non-zero") is now true of the posture refusal too, but the `run_role` comment at `:370-373` still reads as if the serving/job split for posture is a special case rather than the standard row; once the contract row is corrected this is just another `job_exit` entry and the two new comment blocks at `:374-383` restate each other.
IMPORTANCE: low -- comments only, no caller behaviour depends on them.

VERDICT: BLOCK
