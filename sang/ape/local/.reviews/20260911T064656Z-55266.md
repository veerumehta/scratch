# adversarial review -- japes

- when: 2026-09-11T06:46:56Z
- range: f9596d2..HEAD (since the last review) (097c049)
- diff: 151 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
Verified against the tree: `pytest tests/test_plato_boot_contract.py` → 23 passed (14.97s), `ruff check` clean on all four files. Confirmed on CPython 3.12.2 that `atexit._run_exitfuncs()` swallows even `SystemExit` as unraisable and continues to the next handler, so the new unguarded comment holds.

WAS: plato/boot_contract.py :: job-exit-six-never-terminates -- FIXED
WAS: docs/DEPLOYMENT_ENV.md :: job-exit-prose-omits-posture-code -- FIXED
WAS: tests/test_plato_boot_contract.py :: schema-row-job-exit-untested -- FIXED
WAS: plato/data/seed_packs/dscr_core/policies/eligibility.yaml :: ppp-gates-on-unsupplied-fields -- STILL OPEN; the YAML is untouched and every guard the notes describe still lives only in jaci's `LoanApplication`.
WAS: tests/test_plato_boot_contract.py :: tier-vars-copied-not-derived -- FIXED; `_TIER_VARS = {name: None for name in _POSTURE_ENV_VARS}`, and the tuple is the one `environment_tier` (line 258) and `deployed_posture` (281) loop over.
WAS: plato/wiring.py :: release-wiring-unbounded-await -- FIXED; `asyncio.wait_for` with `env_int(POOL_CLOSE_TIMEOUT_ENV, default=10, floor=1)`, pinned by a subprocess test whose `dispose` sleeps 3600s and which returns `rule.job_exit` in about a second.
WAS: plato/__main__.py :: atexit-except-unreachable -- FIXED; the handler is gone and the comment states atexit's property as atexit's.
WAS: plato/wiring_default.py :: retired-never-disposed-claim-stale -- FIXED for both halves; the sentence now names `replace`'s retired list and `release_wiring` as its drain.

ACCEPTED: plato/data/seed_packs/ci-spread-core :: consumer-repo-names-shipped
ACCEPTED: plato/data/seed_packs/ci-spread-core/pipelines.yaml :: inert-loader-claim
ACCEPTED: tests/test_plato_packs_api.py :: backend-sweep-omits-recognised
ACCEPTED: tests/test_plato_packs_api.py :: local-wiring-todo-overstates
ACCEPTED: tests/test_policy_rule_activation.py :: activation-boundary-untested
ACCEPTED: plato/data/seed_packs/ci-spread-core/policies/conventions.yaml :: availability-pct-has-no-producer

New findings against this diff:

[NOTE] plato/wiring.py:30 :: timeout-budget-shared-across-pools -- the constant's comment reads per-pool ("how long `release_wiring` waits for *a pool* to close"), but the bound wraps `closer()`, which on the shipped handle is `DatabaseHandle.dispose_all` disposing `self.current` and then every retired store sequentially inside the one budget; `dispose_all` also empties `self._retired` before the loop, so a slow current store consumes the whole ten seconds and the retired stores are dropped undisposed, which narrows `wiring_default.py:588`'s "`release_wiring` drains that list when the process ends" that this same diff wrote.
IMPORTANCE: low -- reachable only after a `/v1/config` `swap_database` (bootstrap-key write, serving role) followed by a shutdown where the running pool stalls; `os._exit` follows immediately either way, so the OS closes the sockets and nothing observable differs. No consumer outside this repo calls `dispose_all` -- `release_wiring` at `plato/__main__.py:373` is its only call site.

[NOTE] plato/wiring.py:35 :: env-const-missing-from-all -- `WIRING_ENV`, the module's other public env-name constant, is in `__all__` at line 209; `POOL_CLOSE_TIMEOUT_ENV` and `DEFAULT_POOL_CLOSE_TIMEOUT_SECONDS` are not, though the new test imports the first by name from `plato.wiring`.
IMPORTANCE: low -- `__all__` only governs `import *`, and nothing in the repo star-imports `plato.wiring` (grepped); the direct import in `tests/test_plato_boot_contract.py:26` works regardless.

[NOTE] tests/test_plato_boot_contract.py:479 :: timeout-not-distinguished-from-raise -- the stderr assertion is the same substring the raising sibling at line 430 checks, so the test cannot tell "the bound fired" from "dispose failed some other way"; `reason(exc)` renders the timeout as the bare string `TimeoutError`, which the assertion could pin.
IMPORTANCE: low -- test-only, and with `asyncio.sleep(3600)` as the stub there is no other failure the assertion could be satisfied by; the returncode check plus the 90s harness timeout is what actually distinguishes bounded from unbounded.

VERDICT: PASS
