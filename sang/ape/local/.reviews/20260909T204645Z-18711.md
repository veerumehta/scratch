# adversarial review -- japes

- when: 2026-09-09T20:46:45Z
- range: 71f79cf..HEAD (since the last review) (6bd9ae0)
- diff: 514 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` publish-tick tie — CHECKED again, `TODO(publish-tick-tie)` unchanged; not re-raised.
- WAS: `store_db.py:312` marker short-circuit — STILL OPEN; file untouched by this diff, marker text still returned without containment against `root`.
- WAS: `store_db.py:181` publish never opens `archive` — STILL OPEN; untouched.
- WAS: `pack/__init__.py:33-46` `__all__` — STILL OPEN; verified `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent from the export list.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` — STILL OPEN; untouched.
- WAS: `store.py:54` "sha256 over the archived asset bytes" — STILL OPEN; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race — STILL OPEN; untouched.
- WAS: `store_db.py:140` `visibility` unvalidated — STILL OPEN; untouched.
- WAS: `store_db.py:364` `_unpack` docstring summary — STILL OPEN; still says "the directory holding ``pack_manifest.yaml``" while the body accepts either name.
- WAS: `roots.py:99` candidate tuple duplicates `manifest_loader.py:103-106` — STILL OPEN; both still spell all three variants (the new `_declared_files` share does not touch it).
- WAS: `pack.py:188` unknown `profile_id` returns `None` — STILL OPEN; `:196` is still `profiles.get(profile_id)`.
- WAS: `manifest_loader.py:245` profile `dir:` glob unfiltered — STILL OPEN, and now deliberate: `_declared_files` passes no `keep` for profiles and `:152` states it as the choice ("profiles keep everything in theirs"). Behaviour unchanged, so `profiles/README.yaml` still takes `policy_profiles` down.
- WAS: `pack.py:195` default branch re-reads by path — FIXED; `:205-208` matches against `profile_files()`'s own result, verified a `default: profiles/gone.yaml` now warns and falls through instead of raising `FileNotFoundError`.
- WAS: `[SYMMETRY] manifest_loader.py:208-247` duplication — FIXED; `_declared_files` at `:145`, both callers use it.
- WAS: `test_pack_policy_profile.py:26` hardcoded jaci path — FIXED; `JAPES_REAL_PACKS_DIR`, skips cleanly when unset.
- WAS: `config.py:150` `Raises:` missing — FIXED.
- WAS: `config.py:118` guards untested — FIXED; `tests/test_evaluation_config_paths.py`, 13 cases, pass.

PARKED: none. 36 passed / 2 skipped across the touched test files; `ruff` clean.

## 2. New findings

**[DEFECT] `plato/packs/domain.py:83`** — `Pack.from_loader(loader)` sets no `packs_root`, so `Pack._can_merge()` (`pack.py:70-72`) is permanently false and a published pack that declares `depends_on` returns only its own playbooks/evidence types/skills, with nothing logged. Trigger: publish `cl_sp_core` (`depends_on: [{pack_id: ci-spread-core, fragments: [playbooks]}]`, verified in `/Users/sangit/src/jaci/config/packs/cl_sp_core/pack_manifest.yaml:36`, same in `cl_of_core`) → `pack.playbooks` silently omits the imported ones, and `pack.evidence_types`' own docstring (`pack.py:249`) says the local list is what you get only "when the pack declares no dependencies". `PackVersionRecord.depends_on` exists (`store.py:76`) precisely because published packs carry them; a materialized cache has no sibling packs to merge from, so the honest form is a warning naming the dropped deps, not silence.
IMPORTANCE: medium — silent and invisible to the caller, but no shipped caller reaches it yet: `grep published_domain_packs` over `jazzx_sdk/`, `plato/`, `tests/` and `/Users/sangit/src/jaci` hits only `tests/test_pack_store.py`, `plato/packs/__init__.py:__all__` is empty, and no router imports the module. Fix while it is still pre-caller.

**[SYMMETRY] `jazzx_sdk/pack/pack.py:172`** — `_loader_profiles_by_path`'s docstring claims "one load, both views, so the two cannot disagree", but it is a plain method called twice per `policy_profile()` (once through the `@property` at `:161`, once at `:206`); measured on a 2-profile pack, one `policy_profile()` call ran `PolicyProfile.from_yaml` 4 times, and two reads of `policy_profiles` ran it 4 more. Every sibling accessor on this class is `cached_property` (`policy_registry` `:128`, `ontology` `:99`, `vocabulary` `:104`, `derivations` `:117`); making `_loader_profiles_by_path` a `cached_property` is one word, lines it up with them, and is what makes the sentence the diff just wrote true.
IMPORTANCE: medium — two call sites in this file plus `plato/packs/domain.py:99`, which reads `pack.policy_profiles` once per pack per `/info`-style call, so the repeat is per request rather than per process.

**[NOTE] `plato/packs/domain.py:100-104`** — both handlers reduce the exception to `type(exc).__name__` and nothing calls `logger`, so a pack with one malformed profile YAML reports `"policy_profiles_error": "ValidationError"` on the operator surface and leaves no record anywhere of which file or why; `exc_info=True` on a warning here costs one line.
IMPORTANCE: low — no shipped caller yet (as above); cost when one exists is an operator who can see that a pack is broken and cannot find out how.

**[NOTE] `jazzx_sdk/pack/pack.py:215`** — with `profiles: {dir: profiles, default: profiles/gone.yaml}` over a dir holding two profiles, the new fall-through reaches a `ValueError` reading "declares 2 policy profiles (...) and no `profiles.default`", which is false: the pack declares one and it does not resolve. The warning at `:209` says the true thing, but the exception is what a caller sees.
IMPORTANCE: low — reachable only by a manifest typo and it fails loudly; the misdirection is one debugging round, not wrong data.

**[NOTE] `jazzx_sdk/evaluation/harness/config.py:161`** — "`pack_id` is validated on the field, so it cannot be wrong by the time this runs" overstates what `validate_assignment` buys: `EvaluationConfig.model_construct(pack_id="../..")` and `config.model_copy(update={"pack_id": "../.."})` both skip validation in pydantic v2, and `get_output_path` then writes above `output_dir`.
IMPORTANCE: low — no in-repo caller uses either on this model (`grep model_copy|model_construct jazzx_sdk/evaluation/` hits only `scorers.py:508` on a different type); it matters because this is an exported model and the docstring invites an out-of-repo caller to trust the join.

VERDICT: BLOCK
