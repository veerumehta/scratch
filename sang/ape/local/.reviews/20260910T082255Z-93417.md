# adversarial review -- japes

- when: 2026-09-10T08:22:55Z
- range: origin/v2.5.1...HEAD (b58d84d)
- diff: 9799 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `TODO(activation-vs-active-rules)` (policy.py:758) — holds: `executable_rules`/`activation_blocks`/`is_executable` have no callers outside `tests/test_policy_rule_activation.py`, and `active_rules` (policy.py:792) does filter `legacy_alias` and ignore `exception_rules` exactly as described.
- `TODO(layout-key-resolution)` / `TODO(layout-live-formula-order)` (workbook.py:176,183) — both hold: `_write_statement_sheet` writes `binding.label` then `continue`s on `item is None` (workbook.py:410-414), `inputs_by_statement.get(...)` is read at line 690 and populated at 694, and `workbook_layout.yaml:14` states the ordering as a comment.
- `TODO(orphan-blob-gc)` (store_db.py) — holds: `delete` is an `UPDATE`, no blob call; the store is tenant-scoped at construction so it cannot run the cross-tenant refcount.

PARKED: `tests/test_pack_store.py:485` (env-gated on `JAPES_REAL_PACKS_DIR`).

---

**[DEFECT]** `jazzx_sdk/fabric/canonical/profiles.py:129` — `looks_authored` returns True for *any* mapping containing a `provenance` or `status` key, so a pre-existing `custom` escape-hatch value that is a nested mapping with an unrelated `status` fails `Threshold(**found)` (`extra="forbid"`), stays a dict, and `get()` then raises instead of returning it. Verified: `PolicyProfile(profile_id='p', custom={'escalation': {'status': 'open', 'days': 30}}).get('escalation')` → `ThresholdNotActivated: ... declares a provenance or status and is not a readable Threshold: ['days', 'status']`. The validator docstring states the opposite guarantee ("refusing profile data that used to resolve is a worse failure than not gating it").
IMPORTANCE: medium -- fails loudly, and I checked the only `PolicyProfile` YAML in this repo (`plato/data/seed_packs/dscr_core/profiles/dscr_profile.yaml`, all scalars and lists) so nothing here trips it; reachable from the four `profile.get()` call sites (condition_evaluator.py:279, statemachine/schema.py:135, finance/validation.py:601 — all three guarded — plus any consumer's own), and jaci's/juno's profile YAMLs are outside this checkout so I could not rule them out.

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/pack_manifest.yaml:128,138,141,149,151` — the bundled seed pack declares five Python pointers into `jaci` (`jaci.scenarios.ci_spread.conductor.CI_PIPELINE`, the two experts, the evaluator mode), a package japes does not depend on. `Pack.conductor` resolves `conductor.pipeline` through `importlib.import_module` (pack.py:346, via `_resolve` at :43), so publishing the bundled packs (`plato/seed.py:seed_packs`, reachable from the packs page's initialize) and then reading `pack.conductor` on `ci-spread-core` raises `ModuleNotFoundError: jaci`. The sibling seed pack ships no such sections for this reason (`dscr_core/pack_manifest.yaml`: "No conductor/experts sections yet").
IMPORTANCE: medium -- I enumerated plato's own readers: `published_domain_packs`/`domain_pack_summary` (plato/packs/domain.py) build `Pack` objects but never touch `.conductor` or resolve `experts[*].class`, and grep of `plato/` finds no other access, so nothing in this repo triggers it today; the trigger is a host that binds the pack, i.e. jaci (which has the module) or any deployment that does not.

**[SYMMETRY]** `jazzx_sdk/fabric/canonical/policy.py:126` — `as_utc` is a generic datetime helper placed in the policy-schema module, which forces `fabric/guidance/schema.py:96` to do a function-local `from jazzx_sdk.fabric.canonical.policy import as_utc` to dodge the cycle, and makes it the sixth copy of this function (`pack/store_db.py:105`, `manifest/store_db.py:77`, `agents/interactive/session_db.py:55`, `audit/events_db.py:56`, `plato/reference/model_overlay.py:73`). This diff already created `jazzx_sdk/manifest/lifecycle.py` as the dependency-free home for primitives that `policy`, `profiles` and the pack store all share; `as_utc` belongs beside `PolicyStatus` there, which drops the local import and is smaller than what the diff does.
IMPORTANCE: low -- no behaviour change; the local import works.

**[NOTE]** `jazzx_sdk/fabric/canonical/profiles.py` `TODO(threshold-grid-provenance)` — the claim "the gate covers seven of its numbers and not the rest" does not describe the pack in this repo: `dscr_core/profiles/dscr_profile.yaml`'s seven `thresholds:` are bare decimal strings, so the gate covers none of them, and `get_threshold` reports each as `institution_authored`/`active` (profiles.py:207-209) while the file header records the source as "DSCR Program Summary ... (Acra Lending)".
IMPORTANCE: low -- the deferral of the `tables` gap itself holds; only the coverage count in the note is off for the in-repo copy.

**[NOTE]** `plato/data/seed_packs/ci-spread-core/*.yaml` — nine of the new shipped data files cite gitignored plan documents by filename (`plan_JACI_CL_ADDBACK_LIBRARY.md`, `plan_JACI_CL_CHART_OF_ACCOUNTS_AND_CATALOG.md`, …), and `pack_manifest.yaml:169` records `source_doc: "docs/LoanSamples/YETI/Yeti Credit Package/…pdf"` — another repo's layout plus a client name — none of which a reader of the published pack can resolve.
IMPORTANCE: low -- documentation-only; the files themselves load and every declared asset path (`policies/`, `playbooks/*.md`) exists on disk.

Unrelated verifications that came back clean: `ruff check jazzx_sdk plato` passes; the four target test files are 91 passed / 1 skipped; all three applicability-gate call sites got `gate_blocks`; every `Verdict.INDETERMINATE` producer in the repo now sets `indeterminate_reason`; migration `plato/migrations/versions/0004_pack_version_retired_at.py` exists for the new column; `seed_packs` handles `PackVersionExists(retired=True)` as skipped; both `Packs`/`packs` labels in `_NAV_LINKS` are in `degraded_nav`.

VERDICT: BLOCK

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED**
- `TODO(if-match-post)` (plato/posture.py:97) — claim holds: `require_if_match` appears only in `plato/posture.py` and `tests/test_plato_preconditions.py`; no router passes it as a dependency, so `POST /packs/initialize` cannot 428 today.
- `TODO(omit-info)` (plato/info_api.py:84) — claim holds: `_info_only_app`'s tuple is `("Guide", "config", "database", "Packs", "packs")` with no `"info"`, and neither pack label is in `PANEL_LABELS`, so the revised comment at info_api.py:263 is accurate.

---

**[DEFECT]** `plato/data/seed_packs/ci-spread-core/policies/checklist_ci.yaml:125,127` — two `required_documents` entries are truncated PDF-extraction fragments: `schedule` and `available)`. The file's own `description` says "Extracted verbatim" and "missing items become DATA_GAP hypotheses", and the manifest declares it (`checklist: policies/checklist_ci.yaml`). Trigger: any C&I application assessed against this checklist yields two data gaps named `schedule` and `available)` that no document can close, while the real items they were cut from (an equipment/purchase-order line, and something `…, if available)`) are absent from the section entirely, so that requirement is silently not checked.
IMPORTANCE: medium -- ships in one of the two seed packs `POST {prefix}/packs/initialize` publishes into every tenant. Nothing in `jazzx_sdk`/`plato` reads `kind: document_checklist` (grepped), so the consumer of the gaps is outside this repo; it surfaces as visible nonsense rather than silent bad math, but the dropped requirement is silent.

**[DEFECT]** `plato/data/seed_packs/dscr_core/policies/eligibility.yaml:397` — `DSCR-IO-MIN-LOAN`'s description claims "minimum loan amount of $250,000 **and minimum FICO 640**"; the condition tests only `loan_amount`, and no rule in the corpus consumes the profile's `fico_min_io: 640` (dscr_profile.yaml:23). Trigger: interest-only, $300,000, FICO 610, purchase, CLTV 55%, DSCR 1.30, 12 months reserves -- the grid cell `la_le_1_5m|600|purchase` is 60, `FICO-LT-620-RESERVES` is satisfied, and this rule reports SATISFIED for a loan its own text declares ineligible. The sibling with the same shape got it right: `DSCR-ITIN-CLTV` bundles "max loan amount $1.0M, min FICO 640" in its description and both halves are enforced (`DSCR-ITIN-LOAN-MAX` plus `NA` cells for `lt600/600/620` in `max_cltv_itin`). ENCODING_NOTES §3 G4 records that `fico_min_io` has no consuming rule, but not that a rule description already promises it, so the deferral does not cover this.
IMPORTANCE: medium -- pack data, no consumer of `LoanApplication` in this repo (the schema lives in the consumer), so I could not enumerate live call sites; the failure is a permissive pass a reviewer reading the rule text would not expect.

**[DEFECT]** `plato/packs_api.py:73` — `latest_version` is `record.version` from `store.list_packs()`, which `jazzx_sdk/pack/store.py:159-172` documents as chosen by `published_at`, not by version order; the same applies to this function's "versions newest-first" (`versions()` orders by `published_at.asc()`). Trigger: publish `dscr-core` 0.2.0, then publish the 0.1.1 backport -- the JSON reports `latest_version: "0.1.1"` and `packs.html:163` renders it under the column header **Latest**.
IMPORTANCE: medium -- one call site (`pack_inventory` → `packs.html`), operator-facing and silently wrong; the store went out of its way to warn about this exact read and nothing outside this repo consumes the route yet.

---

**[SYMMETRY]** `plato/packs_api.py:125` — `_refuse_unless_relaxed` is a third posture predicate, defined locally in a router, when `plato/posture.py` already owns `withholding`/`withheld`/`check_settings` and is where `config_api.py:249` reaches for its write gate. Two routes in this file already share it and a pack-activation route would be the third copy; moving the same function into `posture.py` beside `withholding` is no more code than defining it here.

**[SYMMETRY]** `plato/packs_api.py:28`, `plato/seed.py:28` — both new modules import the private `plato.settings_store._actor`. Three modules now depend on it, and it is an identity helper rather than a settings-store one; `plato/posture.py` is where the cross-router identity/posture helpers live, and re-exporting it there under a public name is the same amount of code as the two private imports.

---

**[NOTE]** `plato/data/seed_packs/ci-spread-core/policies/core.yaml:2,29` — `# (see jaci/scripts/extract_policy.py …)` and `path: "docs/LoanSamples/YETI/Yeti Application Package/Yeti FInancials/YETI FY 2025 SEC 10-K_1.3.2026.md"` name a consumer repo and one tree's document layout in image-bundled data. `tests/test_plato_packs_api.py:456`'s pin only matches the substring `"jaci."`, so `jaci/` (no dot) and every `docs/LoanSamples/…` provenance path slip past it -- including `checklist_ci.yaml:7` and `playbooks/loan_package_scaffold.md:1`. That guard's own docstring says a record-keeping test that reads a fraction of the tree is worse than none.
IMPORTANCE: low -- inert provenance strings; nothing resolves them. Cost of hitting it is an unresolvable citation on the packs page.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md` §1 — the completeness guarantee it states ("An absent cell raises `KeyError` at runtime -- hence `test_grid_is_fully_authored`") has no guard in this repo: the only test over this copy (`tests/test_plato_packs_api.py:438`) asserts `"max_cltv_grid" in profile.tables`, not that all 99 cells resolve, and the `loan_purpose` axis has no `bands`, so an unmapped purpose is a raw missing key. The grid as shipped here is complete (3 × 11 × 3, counted), so this is coverage, not a live gap.
IMPORTANCE: low -- test-only; the shipped-data defect it would catch (`KeyError` out of `MatrixEvaluator`) needs a future edit to this file to become reachable.

**[NOTE]** `plato/data/seed_packs/dscr_core/ENCODING_NOTES.md` §3 G1 — "**Still open here:** no rule in `policies/eligibility.yaml` uses `kind: all_of`/`any_of` yet -- the Florida-modifier and 13-state prepayment-penalty rules G1 was blocking remain unauthored" contradicts §1a/§1b of the same file and the shipped YAML, which has eight `all_of` rules and all fourteen PPP rules. Only the Florida modifier is genuinely still open.
IMPORTANCE: low -- a later author of this pack reads §3 and re-authors rules that already exist.

VERDICT: BLOCK

## slice3

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(orphan-blob-gc)` (`jazzx_sdk/pack/store_db.py:316`) — the claim holds: `publish` keys the blob by `content_key(archive)` (store_db.py:199), so two tenants share one key, and this store is tenant-scoped, so it cannot run the cross-tenant `count(*)`. The html note at `packs.html:170` and the delete-route comment restate it accurately.
**CHECKED:** `TODO(threshold-grid-provenance)` (`jazzx_sdk/fabric/canonical/profiles.py:218`) — holds. `tables` is `dict[str, dict[str, Any]]` with no coercion validator, `get_table` has no gate, and the new tests in `tests/test_policy_threshold_provenance.py` deliberately exercise `get`/`get_threshold` only. Not raised.

---

**[DEFECT]** `plato/wiring_default.py:607` — any `PLATO_PACK_BLOB_BACKEND` value that is neither `local` nor `azure` skips the local branch (so `local_dir` is never passed and `PLATO_PACK_BLOB_DIR` is ignored) *and* skips the deployment warning, while `BlobStore.put`/`get` treat every non-`azure` backend as local (`jazzx_sdk/fabric/blob/store.py:85-91`, `local_dir or "./blobs"`). Trigger: `PLATO_PACK_BLOB_BACKEND=Azure` (or `azure-blob`, `blob`, any typo — `env_text` only strips) on a staging replica: archives are written to `./blobs` inside the container, rows carry `has_archive: true`, the bytes die with the pod, and the log line written for exactly this failure never fires. The sibling in this module, `_db` (`wiring_default.py:161-169`), guards the complement — it warns on *any* backend that is not the canonical one — which is the shape the docstring at 597 claims to copy.
IMPORTANCE: medium -- one call site (`pack_store_for` in `build()`, the deployed factory); reachable only via the three env vars this diff introduces, so nothing outside this repo sets them yet; failure is silent and unrecoverable (bytes lost) once hit.

**[DEFECT]** `plato/static/packs.html:384-385` — the fetch-exception path sets `data = null` and leaves `problem` null, so `render` reports "Pack store not readable." for a transport failure. Trigger: open or re-read the page (Enter in the tenant field) while the replica is restarting or unreachable — the operator is told the pack store is broken. The two sibling handlers in the same file say "request failed" for the same exception (`remove` at 260-262, `seed` at 360-361), and the comment at 376-377 records this exact misattribution as the bug it was fixing for the non-`ok` case.
IMPORTANCE: low -- this page only, no other consumer; cost is one misleading line during an incident, and the fix is assigning `problem` in the catch.

**[SYMMETRY]** `plato/static/packs.html:149` — the page copies `info.html`'s tenant affordance but not its `x-user-id` field (`plato/static/info.html:125`, sent at `info.html:290`), and this page is the one with *writes*: `_actor()` (`plato/settings_store.py:221`) reads `CallerIdentity` from the inbound identity header, so every row seeded through the button stores `published_by_user_id=None` and renders `—` in the **By** column the diff added, and every delete logs "an unattributed caller" for what `packs_api.py`'s docstring calls a governed act. The generalization is one `<input>` plus one entry in the existing `headers()` helper — smaller than the tenant plumbing already in this diff. (`tests/test_plato_packs_api.py:585` documents the `None` as by-design for an unauthenticated posture, so this is the author's call, not a defect.)
IMPORTANCE: medium -- affects the two write routes in relaxed postures (`local`, `dev-daily`), which is the only place they are admitted; nothing outside this repo reads `published_by_user_id` yet.

**[NOTE]** `tests/test_plato_packs_api.py:698` — `test_the_local_wiring_reaches_a_pack_store_too` never calls `wiring_local.build()`; it greps the module source for `"pack_store_for=pack_store_for"`, so it cannot fail for a runtime fault in the code it names (a `NameError`, a `BlobStore` arg change), unlike its `wiring_default` sibling at line 668 which actually builds and requests `/api/v1/packs`. `build()` needs a `jazzx-assistant` profile checkout or it `SystemExit`s, so a real call isn't available here — a `TODO` plus the current grep is a complete response; only the test's name overstates what it checks.
IMPORTANCE: low -- test-only; the shipped code it covers is verified by hand at boot, and I could not name a defect this would have caught.

**[NOTE]** `tests/test_plato_packs_api.py:455` — the docstring rejects a guard that "reads a fraction of the tree", but the glob filters to `{.yaml,.yml,.md}`, so a `.py`, `.json`, `.csv` or `.txt` added under a seed pack naming `jaci.*` grows the record unnoticed. Verified the current tree is 23 `.yaml` + 10 `.md` files and the pinned dict matches `grep -rln 'jaci\.'` exactly, so there is no gap today.
IMPORTANCE: low -- test-only, and no non-yaml/md file exists under `plato/data/seed_packs/` today.

Ran `pytest tests/test_plato_packs_api.py tests/test_policy_*.py tests/test_pack_store.py -q` → 101 passed, 1 skipped (the pre-existing `JAPES_REAL_PACKS_DIR` gate, not in this diff); `ruff check` clean on all changed files.

VERDICT: BLOCK
