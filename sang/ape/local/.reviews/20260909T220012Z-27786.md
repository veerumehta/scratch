# adversarial review -- japes

- when: 2026-09-09T22:00:12Z
- range: HEAD~1...HEAD (29ea942)
- diff: 3428 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED

- `plato/posture.py:96` `TODO(if-match-post)` — holds. `require_if_match` appears only in `plato/posture.py` and `tests/test_plato_preconditions.py`; no route wires it.
- `jazzx_sdk/pack/store_db.py:63` `TODO(publish-tick-tie)` — holds. No shipped caller of `PackVersionStore.publish` (`grep '\.publish('` in `jazzx_sdk`/`plato` hits only `manifest/publish.py` and `streaming/publisher.py`).
- `plato/database_api.py:82` `TODO(partial-registration-marker)` — holds. Only shipped callers are `database_api.py:163,166` inside `create_database_router`; `db.repository` (`fabric/db/store.py:294`) only registers metadata and opens no connection, so a real `DbStore` cannot reach the swallow. Duck-typed stubs reach it from `tests/test_plato_info_api.py` only.
- `plato/wiring_default.py:527` `TODO(swap-schema-placement)` and `plato/migrations/env.py:182` `TODO(migration-concurrent-index)` — consistent with the code; nothing to add.

Verified: `ruff check` clean on every file in the diff; `lint-imports` 4/4 kept; all changed/new test files pass (310 passed, 3 skipped).

---

**[DEFECT]** `jazzx_sdk/pack/pack.py:217` — `if not profiles: return None` runs before the `profile_id` branch, so a *named* profile id against a pack that declares no profiles returns `None` instead of raising, which is exactly the conflation the method's own comment at line 221 says it refuses.

Trigger, measured: a pack whose manifest has no `profiles:` block, then `Pack.from_manifest("acme-core", root).policy_profile("pinned_v9")` → `None`. So `pack.policy_profile(pinned) or pack.policy_profile()` — the call shape the comment names — silently yields `None` for a stale pin, and a caller writing `or house_default` picks up the wrong thresholds with nothing raised. The docstring's two sentences contradict each other here ("`None` only when the pack declares none" vs. "a named id the pack does not have raises"), and `test_an_id_the_pack_does_not_declare_raises` covers only the has-one-profile case plus the bare pack with *no* argument.

IMPORTANCE: medium — silent, but I enumerated every caller and none is exposed: `plato/packs/domain.py:141` (`domain_pack_summary`) calls it with no argument, `tests/test_pack_policy_profile.py` and `tests/test_pack_store.py` are the only other callers, and the id-passing overload is new in this diff and unpushed, so nothing outside this repo can reach it yet.

---

**[SYMMETRY]** `jazzx_sdk/pack/manifest_loader.py:158` — `_declared_files` joins manifest-declared `refs` and `dir:` onto `self._root` unguarded, while this same diff put `bare_name` on every `pack_id` join (`roots.py:145`, `manifest_loader.py:98`, `prompt_loader.py:68,144`, `harness/config.py:147`). `jazzx_sdk/path_safety.py:relative_name` is the rule already written for this exact case ("may nest, may not escape"), and `_declared_files` is the one chokepoint the diff just created, so the consistent version is one wrapper: `self._root / relative_name(ref, what="asset path")` plus the same on `directory`. Measured: a manifest with `policies: {dir: ../../<elsewhere>}` returns `PosixPath('.../b/../../<elsewhere>/leak.yaml')` from `policy_files()`, which `load_policies` then reads. The sibling joins at `manifest_loader.py:270,290,385` share the gap. This matters more now than before the diff, because `DbPackVersionStore.publish` accepts an arbitrary manifest and `materialize` hands its root straight to this loader.

IMPORTANCE: low — no publish route exists, so in every shipped configuration the manifest is authored in the same checkout as the pack; the trigger needs an untrusted manifest, which only arrives once the publish gate the plan defers is built.

**[SYMMETRY]** `jazzx_sdk/failures.py:268` — the docstring this diff adds states the rule ("pass `limit` wherever the result is served or stored"), and the diff applies it to eight plato sites but not to the SDK sites that put `reason()` into a *returned* object: `pipelines/chat.py:379` (`InteractiveResponse.incomplete_reason`), `pipelines/document_ingest.py:285`, `pipelines/policy_extract.py:588`, `pipelines/vocabulary_build.py:371` (all `Refusal.message`), and `conductor/engine.py:562,575` (span attribute / Trace note). Same one-token change as `console_api.py:132` got.

IMPORTANCE: low — the cost is an oversized response field or span attribute, not a leak (`redact_secrets` still runs first); the unbounded log-only sites at `server/app.py:293` and `runtime/__init__.py:568` are correctly left alone, since both build their served body from `redact_secrets` separately.

---

**[NOTE]** `plato/database_api.py:73` — the comment says "`register_all` is the module that exists to make the set enumerable"; `register_all` is a function, `plato/models.py` is the module. The preceding clause about the count is also spliced mid-sentence into the same 147-char line.

IMPORTANCE: low — comment only, in a function whose two shipped callers are the routes right below it.

**[NOTE]** `jazzx_sdk/modes/prompt_loader.py:56` and `:133` — both `Raises:` blocks attribute the `ValueError` to `pack_id`, but `bare_name(mode_name, ...)` at lines 68 and 138 runs *first* and unconditionally, so a bad mode name raises even with `pack_id=None`. `modes/base.py` quotes the first block as its contract, per the comment.

IMPORTANCE: low — every in-repo mode name is one of the 13 bare names in `list_all_modes`, and the prompts tree is flat except `fallbacks/`, which the loader appends itself.

**[NOTE]** `plato/packs/domain.py:143` — if `pack.policies` raises after `pack.policy_registry` succeeds, `has_policy_registry` is never assigned, so the entry carries `policies_error` with neither count nor flag; a consumer reading `entry["has_policy_registry"]` gets a `KeyError` rather than the reported error.

IMPORTANCE: low — reachable only via a pack whose registry builds but whose `policies` accessor raises, and `domain_pack_summary` has no shipped caller yet (only `tests/test_pack_store.py`).

VERDICT: BLOCK
