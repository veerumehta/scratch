# adversarial review -- japes

- when: 2026-09-09T23:00:32Z
- range: HEAD~1...HEAD (11341e1)
- diff: 4068 lines


## slice1

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## CHECKED (TODO claims that hold)

- `plato/posture.py:100` (`if-match-post`) — `require_if_match` is referenced only from `tests/test_plato_preconditions.py` and `plato/posture.py`'s own `__all__`. No route.
- `plato/database_api.py:83` (`partial-registration-marker`) — the only shipped caller is `create_database_router` at `plato/app.py:208`, which passes the `DatabaseHandle`.
- `store_db.py:66` (`publish-tick-tie`) and `plato/packs/loader.py` (`compose-per-pack-guard`) — `publish` has no non-test caller.
- `plato/packs/domain.py:74` (`share-published-row-loop`) — both readers are test-only, as claimed.
- `plato/migrations/env.py:182` (`migration-concurrent-index`) — `0003` creates four plain indexes; nothing needs `CONCURRENTLY`.

## Findings

**[DEFECT] `plato/wiring_local.py:129`** — `discover_root(Path(__file__), markers=("pyproject.toml",))` walks up from the *installed module*, so a non-editable install into a venv nested inside another project resolves that project as plato's root. Measured: with `<proj>/pyproject.toml` and the package at `<proj>/.venv/lib/python3.12/site-packages/plato/wiring_local.py`, `discover_root` returns `<proj>`. The old `parents[1]` check returned `site-packages`, failed the marker, and fell back to cwd. Consequence: `_REPO` points at the consumer's tree, so plato writes its sqlite file and runs alembic there. The docstring at :118 offers "unreachable in the image — `WORKDIR /app` has no `pyproject.toml`, so the cwd fallback still answers there" as cover, but cwd is never consulted once discovery succeeds and discovery never reads cwd; the image is safe for an unrelated reason (nothing above `/app/plato/` carries a `pyproject.toml`).
IMPORTANCE: medium — `_REPO` is module-level in `wiring_local` and feeds the sqlite path and the alembic run; I confirmed the shipped `Dockerfile` (`WORKDIR /app`, `COPY plato/ ./plato/`) is unaffected and that jaci's editable install is unaffected. Reachable only via a wheel install under a project dir, which is the default `uv`/`poetry`-in-project layout.

**[DEFECT] `plato/packs/domain.py:163`** — the comment claims recording each field as it is read stops a consumer getting `KeyError` on `has_policy_registry`, but the first statement is the one that raises: `pack.policy_registry` and `pack.policies` read the same files, so a malformed policy file leaves `has_policy_registry` unset. Measured against a published pack with an invalid `policies/eligibility.yaml`: `domain_pack_summary` returned `{'pack_id': 'dscr-core', 'version': '0.1.0', 'domain': ..., 'segment': 'dscr', 'policies_error': 'ValidationError', 'policy_profiles': []}` — no `has_policy_registry` key, which is the exact state the comment says was fixed.
IMPORTANCE: low — `domain_pack_summary` has no caller outside `tests/test_pack_store.py:500`, and that test is skipped without `JAPES_REAL_PACKS_DIR`, so nothing in CI exercises the success path either. A `TODO` is a complete response.

**[SYMMETRY] `jazzx_sdk/pack/__init__.py:21`** — the diff adds the four `pack.roots` names to the package export list but leaves `PackVersionStore`, `PackVersionRecord`, `PackNotFound` and `is_assistant_pack` reachable only as `jazzx_sdk.pack.store`. `jazzx_sdk/runs/__init__.py:18` exports its `TurnRunStore` counterpart from the same shape (abstract store + `store_db` implementation), and both new readers (`plato/packs/domain.py`, `plato/packs/loader.py`) import the deep path. Adding four strings to the list already being edited is the same size as what the diff does.
IMPORTANCE: low — import-path ergonomics only; no external consumer imports `jazzx_sdk.pack.store` today (grep over `jazzx_sdk`, `plato`, `tests`).

**[NOTE] `jazzx_sdk/failures.py:255`** — "it was a bare `200` at nineteen call sites" does not match the diff: four sites carried a literal `200` (`plato/database_api.py` ×2, `plato/info_api.py` ×2); the other fifteen passed no `limit` at all. So this is not a pure extract-a-constant change — it newly truncates fifteen call sites, including `oidc.TokenError` messages and the four `default_step_error` handlers. The `120` and `_safe_detail`'s `1000` parts of the comment do check out.
IMPORTANCE: low — the behaviour change is truncation of already-redacted served text at 200 chars; I enumerated all 19 sites and the remaining unlimited `reason(exc)` calls (`governor.py` ×2, `financial_spread.py`, `plato/__main__.py:246`) are all log/stderr, consistent with the docstring rule.

**[NOTE] `plato/packs/domain.py:86`** — the comment lists "a manifest declaring an unknown fragment kind" among the errors the first `try` catches per row, but `depends_on()` was deliberately moved out to its own `try` at :117 because that is where the unknown-kind error is raised; the comment at :119 and `test_an_unreadable_dependency_block_costs_the_advice_not_the_pack` both say the opposite.
IMPORTANCE: low — comment only; the code and its test agree with each other.

VERDICT: BLOCK

## slice2

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
Both new tests pass and both fail against the pre-fix code (`DbStore._metadatas` is per-instance, so `register_all(replacement)` is load-bearing; the redaction tests assert the exact scrubbed string, not just `status`/`type`).

**CHECKED:** `TODO(swap-schema-placement)` (plato/wiring_default.py:527) — the mechanical part holds: models carry no `schema=` (plato/models.py:19-21), nothing in the repo sets a `search_path`, and `create_all()` on a `common` store would build unqualified tables that a later `0001_initial` duplicates. Its *reachability* clause does not; see below.

**[DEFECT]** plato/wiring_default.py:512 (and the same clause in the TODO at 535) — "Only reachable in a relaxed posture … a staging or production deployment cannot get here at all" is not what the code enforces: `create_config_router` keeps the PATCH route whenever `auth` is not None (plato/config_api.py:249 → `withholding` returns `bool(enforcing) and auth is None`, plato/posture.py:133), and the inner gate is `if auth is None and posture_gate()` (jazzx_sdk/server/settings_api.py:202). Trigger: `create_plato_app(auth=<dep>)` in a strict deployment, authenticated PATCH of `PLATO_DB_BACKEND`/`PLATO_SQLITE_PATH` (both editable, plato/config_api.py:72-75) → `_rewire` → `swap_database` runs unqualified `create_all()` DDL against the production Postgres, with no `alembic_version` row. The 403 body itself advertises this path ("pass an auth dependency to create_config_router to allow authenticated writes").
IMPORTANCE: medium -- checked every caller of `swap_database` (plato/app.py:184 only) and both write gates; no shipped wiring sets `auth` (plato/posture.py:119 says so and I found none), so it needs an external consumer constructing the app with an auth dependency, which is a documented, supported configuration.

**[DEFECT]** plato/app.py:156,180 — `_bootstrap_at_boot` is snapshotted once and never updated after a swap, so the comment's "a re-wire can tell 'the database moved' from 'something else changed'" stops being true after the first swap. Two reachable consequences in a relaxed deployment (no auth needed): (a) PATCH `PLATO_SQLITE_PATH` a→b (swap), then PATCH any unrelated key, e.g. `OPENAI_API_KEY` — `wanted` still differs from the boot tuple, so it swaps again, running `create_all()` and leaking another engine, since `swap_database` never disposes the retired store; (b) PATCH the path back a→b→a — `wanted == _bootstrap_at_boot`, so `_rewire` returns early and the process keeps writing to `b` while `/v1/config` reports `a`, with nothing surfacing the mismatch (`missing_config` reports the backend, not the path).
IMPORTANCE: medium -- one call site (`on_change=_rewire`, plato/app.py:191); reachable in any relaxed/dev-daily replica, which is a shipped configuration; case (b) is silent, case (a) costs a pool per write. No consumer outside this repo calls `_rewire` directly.

**[NOTE]** tests/test_queue_scaling.py:369 — `result.response if hasattr(result, "response") else result` has no live else branch: every `return` in `_process_message` is a `QueueProcessingResult` (three via `delivery_result`, one direct), so the fork only makes the test tolerant of a return-shape change it should be pinning.
IMPORTANCE: low -- test-only; would mask a `_process_message` contract change, which `test_runtime_propagates_receipt_returned_by_visibility_update` in the same file already asserts positively.

**[NOTE]** tests/test_plato_wiring_default.py:502-513 — the `retired` hold only pays off if the body reaches the disposals; if `swap_database` or the `session()` block raises, neither store is disposed and the test produces exactly the `Event loop is closed` noise on a later test that the comment says the hold prevents. Same shape at line 460.
IMPORTANCE: low -- test-only, and only on a failing run; cost is one confusing error attributed to the wrong test.

VERDICT: BLOCK
