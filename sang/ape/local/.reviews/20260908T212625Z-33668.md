# adversarial review -- japes

- when: 2026-09-08T21:26:25Z
- range: origin/dev...HEAD (4d99b1e)
- diff: 304 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(jwks-error-detail)`'s claim holds — `TokenVerifier`/`OidcSettings`/`TokenError` have zero references outside `plato/oidc.py` and `tests/test_plato_oidc.py`; nothing in `plato/app.py`, `wiring*.py`, or `__init__.py` mounts it, so the 401-body leak is not reachable today.

---

**[DEFECT]** `plato/oidc.py:284` — the handler's comment ends "Log-only here", but the handler contains no logging call at all: `reason` goes only into the raised `TokenError` message. Trigger: any JWKS fetch failure (issuer down, timeout, malformed document). The module's sole `logger` call is line 255, for skipped individual keys. Combined with the same TODO's instruction that a future middleware "should not echo this", the diagnosis that `_KeyCache.error` was added to preserve reaches neither the logs nor the response — the operator gets nothing, which is the failure mode the field exists to prevent.
IMPORTANCE: medium -- confined to this module; no call sites outside the test file, so today it costs one missing log line in a test run, but it is a contract the next consumer (the identity middleware named in the class docstring) will build on and it fails silently rather than loudly.

**[SYMMETRY]** `plato/oidc.py:320` vs `plato/oidc.py:276` — the empty-`str(exc)` guard (`f"{type(exc).__name__}: {exc}" if str(exc) else type(exc).__name__`) lives inline in `_fetch_and_cache`; the sibling handler added in this same diff in `verify` re-emits raw `{exc}`, so if it is ever reached with the `httpx.ReadTimeout` the parametrized test was written for it produces exactly the `"could not fetch signing keys: "` this diff fixed one function up. One private `_reason(exc)` helper is smaller than the two divergent formats, and both call sites already share the message prefix.
IMPORTANCE: low -- `_fetch_and_cache` wraps everything except its two lazy imports, so I could not construct a caller-reachable path into `verify`'s generic handler; the cost of hitting it is one degraded message.

**[NOTE]** `plato/oidc.py:151` — the `error` field comment says that without it "every verify after [the first] said 'token signed by unknown key'", implying the pair fixes that for an outage; `_or_last_error` raises only when `not cached`, so with any key in the cache a down issuer still returns "token signed by unknown key". The behaviour is deliberate and stated at line 226, so it is the field comment that reads broader than the code.
IMPORTANCE: low -- comment only; no behavioural change.

**[NOTE]** `plato/oidc.py:229` — a fetch that *succeeds* and yields no usable keys (`{"keys": []}`, or every entry `kid`-less) sets `failed=False`, so it reports "token signed by unknown key" for the whole window — the same operator-facing misdiagnosis the diff fixes for the raising case. `tests/test_plato_oidc.py:365` covers the rate limit for that shape but asserts only `TokenError`, not the message, which is the assertion that would pin the difference.
IMPORTANCE: low -- reachable from a misconfigured realm, but it fails loudly and the token is rejected either way.

**[NOTE]** `tests/test_plato_oidc.py:485` — `test_an_unreachable_issuer_is_a_token_error_not_a_500` names the new wrap in `verify`, but `httpx.ConnectError` from the injected fetcher is caught by `_fetch_and_cache`'s handler and converted there; the test passes with the `verify` wrap removed, so nothing exercises it. Its own comment already flags one round of green-for-the-wrong-reason on this test.
IMPORTANCE: low -- test-only; no shipped-code defect it would have caught, since I could not reach the wrap from any caller.

VERDICT: BLOCK
