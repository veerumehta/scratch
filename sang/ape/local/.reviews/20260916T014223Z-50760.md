# adversarial review -- japes

- when: 2026-09-16T01:42:23Z
- range: 217591a..HEAD (since the last review) (f4051ff)
- diff: 614 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: plato/boot/schema_version.py :: upgrade-ignores-the-db-it-is-given -- FIXED (the store identity now rides `config.attributes["store"]` and `env.py:64` prefers it; the file-backed case is proven by `tests/test_plato_schema_version.py:239`). The in-memory residue is a new finding below, under its own slug.
WAS: plato/jobs.py :: migrate-success-not-checked-against-head -- FIXED (`upgrade_to_head` raises `SchemaStaleError` on any non-head outcome; both callers inherit it, verified by running it against an in-memory store).
WAS: plato/api/database.py :: route-exists-in-every-posture -- FIXED.
WAS: plato/api/database.py :: migrate-logs-no-actor -- FIXED (a line is written); its placement is a new finding below.
WAS: plato/api/database.py :: stray-auth-query-param -- FIXED.
WAS: plato/__main__.py :: exemption-still-hypothetical -- FIXED.
WAS: plato/migrations/env.py :: dashboard-button-does-not-exist -- FIXED (the button exists at `plato/static/info.html:133`).
WAS: plato/jobs.py :: revision-wrapper-adds-nothing -- FIXED (wrapper gone, `MIGRATE_ROLE` in `__all__`).

[DEFECT] plato/migrations/env.py:61 :: memory-store-not-shared -- passing the resolved identity does not make an in-memory store reachable from alembic's thread: `sqlite_path=None` means `:memory:`, and `DbStore` gives every `:memory:` store its own `StaticPool` (`jazzx_sdk/fabric/db/store.py:197`), so "builds an equivalent store" is a second, empty database for exactly the case the comment cites as the one being fixed.
`PLATO_DB_BACKEND=sqlite` (or unset in a local posture) with `PLATO_SQLITE_PATH` unset -- what `plato/wiring/default.py:177` returns for a bare `python -m plato`. Reproduced directly: `upgrade_to_head(DbStore(backend="sqlite", sqlite_path=None))` raises `the migration finished with the database at no revision at all, not at 0004_pack_version_retired_at; it was not migrated here`, and the read-back is still `None`. The same claim appears at `plato/boot/schema_version.py:158` ("`db` is passed through ... rather than left to it") and at `jazzx_sdk/fabric/db/store.py:267` ("a caller that has to build a *second* store against the same database"); none of the three holds for `None`. Consequence on the dashboard: `/info` reports a mismatch, `extra.strict` is `false`, so the new "Migrate now" button is shown in precisely the posture where the route can only answer 500, and the message misdiagnoses it as the wrong database rather than as an unmigratable store. The `db is None` precondition is checked up front (`plato/api/database.py:181`, `plato/jobs.py:88`); this one is not.
IMPORTANCE: medium -- fails loudly, and every path is enumerated: `plato/jobs.py:93`, `plato/api/database.py:197`, and the button. Only the in-memory configuration is affected; file-backed sqlite and `common` are correct. Nothing outside this repo calls `upgrade_to_head`.

[DEFECT] plato/api/database.py:194 :: audit-line-precedes-the-write -- the actor line is written before `upgrade_to_head` runs and is phrased in the past tense, so a migration that fails leaves an audit record saying it succeeded; the sibling it cites, `delete_pack_version`, logs after the write returns (`plato/api/packs.py:198`, following `store.delete` and the 404 check).
Any failing migration: the in-memory posture above (guaranteed), an alembic error, or the `SchemaStaleError` branch three lines below. The log then holds `plato database: an unattributed caller migrated the schema from no revision` and nothing else -- the 500 is returned to the caller but not logged, so the operator's record of the incident says the schema was migrated. `before` is already read at line 191, so moving the line below the `try` costs nothing.
IMPORTANCE: medium -- one route, mounted unconditionally from `plato/app.py:222`, reachable in any relaxed posture; the caller sees the 500, so the wrong data is confined to the log a reviewer reads afterwards.

[SYMMETRY] plato/static/info.html:198 :: posture-gate-on-one-button-only -- the rule this diff adds ("offered only where the route will answer", and its test at `tests/test_plato_info_api.py:638`) is applied to the migrate button alone; `plato/static/packs.html:257` renders a Delete for every version and `packs.html:380` an Initialize, both behind the same `relaxed_only` gate, with no posture check -- they 403 on click in a strict tier, which is the failure the new comment says must not happen. Honest cost: `packs.html` does not fetch `/info` today, so lining it up means reading `extra.strict` there, which is more than the one line `info.html` needed. Weigh it rather than treat it as free.

[NOTE] plato/boot/schema_version.py:179 :: absent-identity-asserts-common -- `getattr(db, "backend", "common")` makes a `db` without the new properties assert the `common` backend rather than fall back to the environment, because `_requested` is then truthy in `env.py:64` and `PLATO_DB_BACKEND` is never read; `getattr(db, "backend", None)` plus an `if` would keep the fallback.
IMPORTANCE: unknown -- I could not establish a reachable caller: `plato/wiring/default.py:177` and `plato/wiring/local.py:302` both hand over a `DbStore` (or the delegating handle), and every test double I found does too. `PLATO_WIRING` is a public extension point, so a third-party factory supplying a duck-typed `db` with only `engine()` is the shape I cannot rule out.

[NOTE] plato/wiring/settings_store.py:224 :: importer-count-now-three -- "two other modules import this (`packs_api`, `seed`)" is stale as of this diff: `plato/api/database.py:13` is the third, and the module named `packs_api` is `plato/api/packs.py`.
IMPORTANCE: low -- a docstring on a function with three importers, all in this repo.

[NOTE] plato/migrations/env.py:99 :: offline-dialect-from-empty-url -- `alembic.ini` declares no `sqlalchemy.url`, so `config.get_main_option(...) or ""` is always `""` and `_version_table_options("")` takes the schema-qualified branch; the previous `PLATO_DB_BACKEND` read returned `{}` for sqlite there.
IMPORTANCE: low -- no reachable trigger: offline mode already dies earlier in `context.configure` for want of a URL (`alembic -c plato/alembic.ini upgrade head --sql` raises inside `MigrationContext.configure`), both before and after this change.

VERDICT: BLOCK
