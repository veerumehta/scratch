# adversarial review -- japes

- when: 2026-09-08T19:25:44Z
- range: 1137294..HEAD (since the last review) (59cc024)
- diff: 62 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Step 1 — prior round

WAS: `plato/oidc.py:76` + `:131-133` overstated comments -- **FIXED** (tree unchanged since that round; `:75-77` and `:129-132` carry the narrowed claims).

WAS: `plato/oidc.py:134` `or ""` returns a relative URL for a mutated-to-`None` issuer -- **STILL OPEN**; code untouched, and the inline deferral still holds (`enabled` is `bool(self.issuer)`, `verify` raises before `_load_keys`), so it stays declined rather than outstanding.

## CHECKED

- `plato/database_api.py:69-74` "three of seven under `wiring_local`" — holds. `wiring_local.py:312-313` builds 2 stores; `config_api.py:228` adds `plato_setting` at router build. Three of the seven in `TABLE_SCHEMAS`.
- `:73` "`register_metadata` ignores a repeat" — holds (`jazzx_sdk/fabric/db/store.py:250`, membership test), so a polled dashboard cannot grow `_metadatas`.
- `:73` "constructing a store opens no connection" — holds for all six: five call `register_metadata` only, `DbTurnRunStore` calls `db.repository`, which just registers metadata (`store.py:295-301`). No `_resolve_engine`.
- `plato/oidc.py:68` `TODO(frozen-settings)` — unchanged, still holds.

`tests/test_plato_info_api.py` 96 passed, 1 skipped; `ruff` clean on both files.
PARKED: `test_plato_info_api.py:608` (TODO(protocol-relative)).

## Step 2 — new findings

**[SYMMETRY]** `plato/wiring_default.py:498` — the same emergent-metadata bug this diff fixes in `describe_database` is what makes `swap_database`'s `await replacement.create_all()` a no-op: `_db()` (`wiring_default.py:138-161`) returns a bare `DbStore` with zero registered metadata, so `create_all` iterates an empty `_metadatas` and creates nothing, then the handle is pointed at it. Trigger: relaxed posture, `POST /v1/config` changing `PLATO_SQLITE_PATH` (or `PLATO_DB_BACKEND`) to a fresh database — the swap logs success and every later session/manifest/settings query fails `no such table`. The fix is the one line the diff already wrote, `register_all(replacement)` before `create_all()`, against the same `plato.models.register_all`.
IMPORTANCE: high -- reachable only in a relaxed posture (strict refuses `/v1/config` writes), so no staging/production path; enumerated the three `swap_database` tests (`tests/test_plato_settings_store.py:151,205`, `tests/test_plato_wiring_default.py:411`) and none asserts a table exists on the replacement, which is why it is not currently caught. Nothing outside this repo calls `swap_database`.

**[NOTE]** `plato/database_api.py:75-81` — a partial `register_all` is reported as an ordinary success: with any db-like object lacking `.repository`, `DbTurnRunStore` raises after three metadatas are already registered (verified: `agent_session`, `assistant_manifest_record`, `config_audit_event`), the `except` swallows it, and the body carries the shortened list and a shortened `tables_registered` with no `reason` — the exact "Plato has three tables" reading this change exists to remove, differing only by a log line. Shipped callers are safe: `plato/app.py:208` passes the `DatabaseHandle` (delegates to a real `DbStore`) and both have `.repository`; the reachable trigger is a caller of the exported `describe_database` passing a duck-typed store.
IMPORTANCE: low -- one shipped call site checked (`plato/app.py:208`) plus the two direct-call tests; no shipped wiring reaches the partial path, and a `TODO` naming the missing `reason` marker is a complete response.

**[NOTE]** `tests/test_plato_info_api.py:1598-1601` — the docstring names the wrong class and the wrong count: `DbModelOverlayStore` uses `db.register_metadata` (`plato/reference/model_overlay.py:91`), and the constructor that "wants `db.repository`" is `DbTurnRunStore` (`jazzx_sdk/runs/store_db.py:108`), the only one of the six that needs anything beyond `register_metadata` — so "three of the six constructors reach past `register_metadata`" describes three *tables registered before the raise*, not three constructors.
IMPORTANCE: low -- test docstring only; the assertion it explains is correct and the test fails against the pre-diff code as claimed.

VERDICT: PASS
