# adversarial review -- japes

- when: 2026-09-08T19:35:02Z
- range: 59cc024..HEAD (since the last review) (143749f)
- diff: 92 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## Step 1 — prior round

WAS: `plato/wiring_default.py:498` swap `create_all` no-op — **FIXED**. `register_all(replacement)` now precedes `create_all` (`wiring_default.py:501-503`); verified empirically that `register_all` + `create_all` produces exactly the 7 `TABLE_SCHEMAS` tables and no extras (no `japes_feedback` leak via shared metadata), and the new test at `tests/test_plato_wiring_default.py:434` fails against the pre-diff code because a bare store's `_metadatas` is empty.

WAS: `plato/database_api.py:75-81` partial `register_all` reported as success — declined with `TODO(partial-registration-marker)`; see CHECKED, not reported again.

WAS: `tests/test_plato_info_api.py:1598-1601` docstring named the wrong class/count — **FIXED**; it now names `DbTurnRunStore` and describes three *tables registered before the raise*.

## CHECKED

- `plato/database_api.py:80-85` `TODO(partial-registration-marker)` — claim holds. The only shipped call sites are `database_api.py:161,164`, both fed the `DatabaseHandle` from `app.py:208`, which delegates to a real `DbStore` (has `.repository`). The only duck-typed reach is `tests/test_plato_info_api.py:369`. The deferral is accurate, including that the fix is a `reason` marker.
- New test's "three swap tests existed and none asserted a table" — holds: `test_plato_settings_store.py:151,205`, `test_plato_wiring_default.py:411`.
- `tests/test_plato_wiring_default.py` 27 passed; `ruff` clean on all four files.

## Step 2 — new findings

No defects.

**[NOTE]** `plato/wiring_default.py:503` — `create_all` places tables in the connection's default namespace, which for a non-sqlite swap is not where `TABLE_SCHEMAS`/`0001_initial` put them: the models carry no `schema=` deliberately, so a relaxed-posture `POST /v1/config` setting `PLATO_DB_BACKEND=common` against a Postgres that has no `plato_*` schemas now creates all 7 unqualified (the diff turned this from a no-op into real DDL). The replica works against them, but a later alembic run on that same database builds a second copy in `plato_control`/`plato_runtime`/`plato_reference` and which one wins depends on the connection's search path — and there is no `search_path` setting anywhere in `plato/` or `jazzx_sdk/`.
IMPORTANCE: low -- enumerated the one shipped caller (`plato/app.py:184`), reachable only in a relaxed posture (`/v1/config` refuses writes strict), and needs a subsequent alembic upgrade on the swapped-to database to bite; the function's own docstring (`:488-491`) already accepts `create_all` over the chain on this path. Nothing outside this repo calls `swap_database`.

**[NOTE]** `tests/test_plato_wiring_default.py:452` — the test disposes only `wiring.db.current` (the replacement), leaving the retired `a.db` store's aiosqlite engine, which `build()` resolved via `_apply_durable_settings`, undisposed; `DbStore.dispose`'s own docstring says that thread's `RuntimeError: Event loop is closed` lands on whichever test happens to be running. The neighbouring `test_a_database_swap_clears_its_own_report` (`:411-413`) has the identical shape, so this is a second instance rather than a new pattern.
IMPORTANCE: low -- test-only; no shipped-code defect it would have caught, and the existing twin has not flaked.

VERDICT: PASS
