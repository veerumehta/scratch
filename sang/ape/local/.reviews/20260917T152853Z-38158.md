# adversarial review -- japes

- when: 2026-09-17T15:28:53Z
- range: e7cd2dd..HEAD (since the last review) (7f1d7b1)
- diff: 253 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: docs/DEPLOYMENT_ENV.md :: activation-not-visible-to-bound-agents -- FIXED. `invalidate_all` exists at `jazzx_sdk/server/assistant_runtime.py:138`, `activate` calls it before logging, and `tests/test_plato_wiring_default.py:917` pins `cached_keys == []` after a second activation; the doc sentence now says what the code does, including the per-replica scope.
WAS: tests/test_dev_daily_env_template.py :: doc-guard-split-from-boot-contract -- FIXED. Both guards now live under `DOCS` in `tests/test_plato_boot_contract.py`; the duplicate `DEPLOYMENT_DOC` constant is gone and the `.env.dev-daily` module no longer imports `plato` at collection.
WAS: tests/test_plato_wiring_default.py :: manifest-field-count-off-by-one -- FIXED. The comment reads nine required fields / two of three keys / seven errors, which matches the model (9 required, 7 errors for that dict).
WAS: tests/test_dev_daily_env_template.py :: documented-is-substring-only -- STILL OPEN in half: the strict-prefix hole is closed by the token regex, but "passes on an occurrence anywhere in the file" still holds, and for the four single-word keys (`DEBUG`, `DESCRIPTION`, `VERSION`, `PROJECT_NAME`) any English-word occurrence anywhere satisfies the assertion. Still `low`, test-only.

[SYMMETRY] plato/app.py:200 :: swap-leaves-agents-bound -- the diff adds the cache drop at one of the two places that invalidate what a bound agent was built from; `_rewire` calls `swap_database(db)` and drops the `ClientLayer` singleton for exactly the reason cited in the new comment, but leaves `runtime._agents` holding agents built from the retired database's manifest records.
`runtime` is a parameter of `create_app` (`plato/app.py:101`) and in scope in `_rewire`, so the consistent version is the same one line the diff just wrote: `runtime.invalidate_all()` after `await swap_database(db)`. `AssistantKey.release_id` is `""` in phase 1, so an assistant id present in both databases re-serves the pre-swap binding rather than missing the cache.
IMPORTANCE: low -- no shipped configuration can reach it: nothing in `plato/wiring/default.py` ever calls `manifest_store.put` (the only `put` in the repo is `plato/wiring/local.py:320`, and `_rewire` returns early there because local passes a `DbStore`, not a `DatabaseHandle`), so `_agents` is empty on every default-wired replica. Reachable only for an external consumer that writes manifest records and PATCHes a bootstrap key through `/v1/config` (relaxed, or strict with auth).

[NOTE] tests/test_plato_boot_contract.py:92 :: token-check-claim-overstated -- the new docstring says tokens fix "a key that appears only in a sentence saying it is unsupported"; `re.findall` over the whole file collects tokens from prose too, so that case is unchanged, and "No such collision exists today" is true only of the prefix class. A table-row match (`| \`KEY\` |`) is what would close both.
IMPORTANCE: low -- test-only; no such sentence exists in `docs/DEPLOYMENT_ENV.md` today (grepped unsupported/not supported/ignored/deprecated: no hits), and all 21 keys currently sit in real rows, so there is no live false pass. Cost of a future one is a green test over an undocumented variable.

[NOTE] plato/wiring/default.py:629 :: invalidate-all-crosses-tenants -- `activate` is tenant-scoped in its reference and its log line, but `registries=` ignores the tenant and `invalidate_all()` is process-wide, so on a `PLATO_TENANTS=a,b` replica an activation for `a` now also drops `b`'s bound agents and rebinds them against `a`'s pack; the message attributes the process-wide swap to one tenant and counts other tenants' agents in "bound assistant(s) dropped".
IMPORTANCE: low -- the global rebind of `profiles`/`skills` pre-dates this diff; what the diff adds is that a warm `b` stops serving its previous binding immediately rather than eventually. Same zero-reachability as above: `_agents` is empty in every default-wired configuration because nothing writes a manifest record.

[NOTE] plato/wiring/default.py:645 :: llm-manager-captured-at-build -- `agent_extra={"llm_manager": client_layer.llm}` captures the ClientLayer built at `build()`; `_rewire` calls `reset_client_layer()` so "the next caller does that on the first `get_client_layer()`", but this dict keeps the retired manager, so a provider key or LLM setting changed through `/v1/config` never reaches an agent, including one bound after the reset.
IMPORTANCE: low -- the lines moved verbatim in this diff rather than changing, and the only reader is `build_from_manifest` via `agent_for`, which no default-wired replica reaches (no manifest writer). Reachable for a consumer that writes manifests and PATCHes `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` in a relaxed or auth'd posture; the symptom is a turn billed against the old key, which fails loudly if the old key was revoked.

VERDICT: PASS
