# adversarial review -- japes

- when: 2026-09-09T20:25:30Z
- range: c2f2e48..HEAD (since the last review) (1a6ef52)
- diff: 151 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
## 1. Previous round

- WAS: `store_db.py:250` publish-tick tie — CHECKED last round, `TODO(publish-tick-tie)` unchanged; not re-raised.
- WAS: `store_db.py:312` marker short-circuit — STILL OPEN; `:308-312` still returns the marker's text verbatim, no containment against `root`.
- WAS: `store_db.py:181` publish never opens `archive` — STILL OPEN; untouched.
- WAS: `pack/__init__.py:33-46` `__all__` — STILL OPEN; `PackVersionStore`/`PackVersionRecord`/`PackNotFound` still absent.
- WAS: `plato/oidc.py:375` unbounded `reason(exc)` — STILL OPEN; untouched.
- WAS: `store.py:54` "sha256 over the archived asset bytes" — STILL OPEN; `store_db.py:167` still hashes the manifest when `archive is None`.
- WAS: `store_db.py:167` get-then-insert race — STILL OPEN; untouched.
- WAS: `store_db.py:140` `visibility` unvalidated — STILL OPEN; untouched.
- WAS: `store_db.py:364` `_unpack` docstring summary — STILL OPEN; untouched.
- WAS: `roots.py:99` candidate tuple duplicates `manifest_loader.py:103-106` — STILL OPEN; both still spell all three variants.
- WAS: `[DEFECT] prompt_loader.py:52` `Raises:` omits `ValueError` — FIXED; `:56-57` and `:129-130` now name it.
- WAS: `[SYMMETRY] evaluation/harness/config.py:147` unguarded `pack_id` write target — FIXED; validator at `:133-143`, and no in-repo or jaci construction site passes a non-bare id (`tests/test_*` use `"t"`/`"x"`; `jaci/tests/eval/run_{cre,ci,kyc}_eval.py` pass `cre_underwriting`/`ci_spread`/`kyc_anthropic`).
- WAS: `[NOTE] test_pack_roots.py:181` test placement — FIXED; moved to `test_modes_framework.py:211` with `_BAD_PACK_IDS` imported.

## 2. New findings

`ruff` clean on the four files; `tests/test_modes_framework.py` + `tests/test_pack_roots.py` 45 passed (also under bare `pytest`: the editable install puts the repo root on `sys.path`, so the cross-module import resolves in CI's `poetry install` layout too).

**[DEFECT] `jazzx_sdk/evaluation/harness/config.py:141`** — the validator docstring claims "construction is the single place an id enters", but `model_config` at `:117` sets no `validate_assignment` and the model is not frozen, so assignment is a second entry that skips the guard entirely. Trigger, run against the tree:

```
c = EvaluationConfig(pack_id='t', golden_cases_dir=Path('/tmp/x')); c.pack_id = '../../..'
c.get_output_path('r')  -> output/evaluations/../../../eval_r.json
```

`validate_assignment=True` in the `ConfigDict` already there closes it and makes the sentence true.
IMPORTANCE: low — I grepped `\.pack_id *=` across `jazzx_sdk/`, `plato/` and `tests/`: every hit is a plain attribute on a non-pydantic object (`modes/base.py:109`, `experts/base.py:149`, …) or a SQLAlchemy comparison; nothing mutates an `EvaluationConfig`. The three jaci construction sites pass literals. Exposure is an out-of-repo caller that builds the config then overwrites the field, which I could not rule out.

**[NOTE] `jazzx_sdk/evaluation/harness/config.py:161`** — `run_id` is joined into the same expression the diff just guarded and is not validated; `get_output_path("../../x")` escapes `output_dir` exactly as `pack_id` did.
IMPORTANCE: low — the only in-repo caller is `harness/runner.py:225,231`, passing `self.run_id` built at `:173` from `datetime.now()` plus a `uuid4().hex[:6]`, so no shipped configuration reaches it; `get_output_path` is a public method on an exported model, which is the only path to it.

**[NOTE] `tests/test_modes_framework.py:212`** — the moved test still takes a `monkeypatch` fixture it never uses (carried verbatim from `test_pack_roots.py`).
IMPORTANCE: low — test-only; no shipped-code defect behind it.

VERDICT: BLOCK
