# adversarial review -- japes

- when: 2026-09-08T21:17:19Z
- range: origin/dev...HEAD (2f91ceb)
- diff: 267 lines


## whole

[33m⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors[39m
**CHECKED:** `TODO(frozen-settings)` (plato/oidc.py:68) holds — every new test in this diff constructs `OidcSettings` fully in the call and never mutates a field afterward, and `grep` finds no `OidcSettings(` outside `plato/oidc.py` and `tests/test_plato_oidc.py`.

**[DEFECT]** `plato/oidc.py:269` (with the truthiness test at `:223`) — `error=str(exc)` plus `if self._cache.error` reintroduces exactly the misreport this diff fixes for any exception whose `str()` is empty, and a JWKS **timeout** is that exception: httpx's `map_httpcore_exceptions` builds `mapped_exc(str(exc))`, and httpcore raises `ReadTimeout(TimeoutError())`, so `str()` is `''`. Ran it against this code with `fetch_jwks` raising `httpx.ReadTimeout(str(TimeoutError()))` and a frozen clock: `['could not fetch signing keys: ', "token signed by unknown key 'k1'", "token signed by unknown key 'k1'"]`. Verify 1 loses the reason and every verify in the 30s window blames the caller's token, which is the failure mode `_KeyCache.error`'s own docstring says it exists to prevent. The field directly above it (`fetched`, `:143`) is a separate bool for precisely this reason — an in-band value tested for truthiness failed open — and the same fix applies (a `failed: bool`, or test `error is not None`). The new tests only exercise `httpx.ConnectError`, which carries `[Errno 61] Connection refused` and so never hits it.
IMPORTANCE: medium -- `TokenVerifier` has no call site outside `plato/oidc.py` and `tests/test_plato_oidc.py` (grepped `plato/` and `jazzx_sdk/`), so nothing shipped reaches it yet; it is exported in `plato.oidc.__all__` for external consumers, it fails loudly as a 401 either way, and the cost is the operator diagnosing a token problem during an issuer hang.

**[NOTE]** `plato/oidc.py:302-305` / `tests/test_plato_oidc.py:485` — after this diff `_fetch_and_cache` wraps its whole body, so no non-`TokenError` is left to escape `_load_keys` (the only unguarded lines are the two function-local imports, and `verify`'s own `import jwt` at `:280` would fail first). The test named for the wrap passes through `_fetch_and_cache`'s conversion at `:270` and would still pass with the `verify` wrap deleted; the wrap is defensive-only now.
IMPORTANCE: low -- reasoned over every statement in `_load_keys`/`_fetch_and_cache`; no behaviour change, the test just does not isolate the guard it documents.

**[NOTE]** `plato/oidc.py:270` — the raised message interpolates the underlying exception verbatim, and `_fetch_jwks_over_http`'s `raise_for_status()` puts the resolved JWKS URL in it (`... for url 'https://keycloak.internal/realms/...'`), so a caller that renders `TokenError` as a 401 detail hands an internal hostname to an unauthenticated caller. Trigger: issuer misconfigured so the certs endpoint 404s.
IMPORTANCE: unknown -- could not determine who renders the message, because nothing mounts `TokenVerifier` in a request path yet; whether it leaks depends on the not-yet-written middleware.

VERDICT: BLOCK
