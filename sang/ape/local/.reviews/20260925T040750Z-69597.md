# adversarial review -- .

- when: 2026-09-25T04:07:50Z
- range: 07e5db8..HEAD (since the last review) (d0810df)
- diff: 191 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: tests/test_default_policy_expert.py :: shared-id-gate-untested -- STILL OPEN. There is still no test where a conditionless, gated rule reuses a lower rule's id. The only conditionless test (line 302) goes through `replaces=`.

[DEFECT] jazzx_sdk/pack/manifest_loader.py:249 :: scalar-program-split-chars -- If a program is given a bare string instead of a list, `programs()` splits it into single characters, and that program's overlay silently never applies.
Trigger: `programs: {rb-abl-2026: RB_CI_OVERLAY}` (a natural YAML shape for a program with one overlay) makes `overlay_map` return `{"rb-abl-2026": ["R","B","_",...]}`. None of those ids are in the registry. So `DefaultPolicyExpert.from_pack` evaluates cases with `program_id: rb-abl-2026` against core rules only, with no error. `lint_pack` reports it only when the caller passes `policies`, and then as one `unknown_program_policy` per character. The fix is to treat a `str` as a one-item list, or reject it, in the same place that handles the `v or []` fallback.
IMPORTANCE: medium -- The only shipped `programs:` block (`ci-spread-core/pack_manifest.yaml`) uses the list form, so nothing ships broken. The code is reached by any pack author through `Pack.overlay_map`, which is used by `DefaultPolicyExpert.from_pack` and `lint._program_findings` in this repo, and by jaci and Plato through `from_pack`. The runtime failure is silent. It only shows up if someone runs lint with policies.

[NOTE] jazzx_sdk/pack/lint.py:195 :: product-policy-in-no-program -- `_program_findings` checks programs that name a policy, but not the reverse: a `scope: product` policy that no program lists.
Trigger: a pack declares `RB_CI_OVERLAY` as `scope: product` but has no `programs:` block. `core_policy_ids` leaves it out and `overlay_map` is `{}`, so it never evaluates for any case, and lint passes.
IMPORTANCE: low -- the one shipped product-scoped policy (`RB_CI_OVERLAY`) is listed by `rb-abl-2026`.

[NOTE] jazzx_sdk/pack/lint.py:195 :: program-check-needs-policies -- The program check only runs when the caller passes `policies`, even though `Pack.policy_registry` can supply them itself for any pack with `policies.dir`.
Trigger: `lint_pack(pack)` with no `policies=` does not report a program that names a policy missing from the pack.
IMPORTANCE: low -- this is only an authoring-time check, and the same gate already applies to `_unknown_evidence_types` and `_dangling_relations`.

VERDICT: BLOCK
