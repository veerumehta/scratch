# adversarial review -- .

- when: 2026-09-24T21:00:09Z
- range: 3d3678e..HEAD (since the last review) (8629e3a)
- diff: 131 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/pack/rule_merge.py :: existing-status-overwritten -- FIXED
WAS: jazzx_sdk/pack/rule_merge.py :: dot-prefixed-dir-unreachable -- FIXED
WAS: jazzx_sdk/pack/rule_merge.py :: duplicate-accepted-rule-id -- FIXED

[NOTE] jazzx_sdk/pack/rule_merge.py:51 :: skipped-description-stale -- The `skipped` field's description still reads "Accepted proposals with no rule, and why." This diff gives it a second reason: a duplicate accepted rule id.
Trigger: a caller who reads the schema or field description to decide how to handle `skipped` entries assumes each one is a missing rule. It is actually one of two reasons. The duplicate case needs a different fix: resolve the conflict, don't re-author the rule.
IMPORTANCE: low -- the text is descriptive only. `merge_rules_into_draft` has no caller in japes outside `tests/test_pack_rule_merge.py`.

[NOTE] jazzx_sdk/pack/rule_merge.py:133 :: skipped-duplicate-remains-mergeable -- The skipped duplicate comes back in `result.proposals` still ACCEPTED. If a caller persists that list and runs the merge again, the second proposal gets merged, silently replaces the first one's rule, and lands under `replaced`.
Trigger: a caller writes `result.proposals` back to its proposal store and then calls `merge_rules_into_draft` again with that set. The "ambiguous" pair then resolves as last-wins across two calls instead of being reported.
IMPORTANCE: low -- this needs a caller to concatenate proposal sets and then re-merge. `propose_step` dedupes by `rule_id` within one `ProposalSet`, and there is no in-repo caller.

VERDICT: PASS
