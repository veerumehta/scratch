# adversarial review -- .

- when: 2026-09-25T04:17:24Z
- range: beced056..HEAD (since the last review) (0968ca51)
- diff: 282 lines


<<<REVIEW whole>>>

⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
WAS: jazzx_sdk/pipelines/jtbdset.py :: steps-heading-drops-procedure -- FIXED
WAS: jazzx_sdk/pipelines/jtbdset.py :: nested-headings-leave-step -- FIXED
WAS: jazzx_sdk/pipelines/jtbdset.py :: overlays-exported-as-base -- FIXED
WAS: jazzx_sdk/pipelines/jtbdset.py :: zero-sequence-becomes-100 -- FIXED
WAS: jazzx_sdk/pipelines/jtbdset.py :: section-slug-collides -- FIXED
WAS: jazzx_sdk/pipelines/jtbdset.py :: fenced-hash-splits-block -- FIXED

[DEFECT] jazzx_sdk/pipelines/jtbdset.py:122 :: stale-whole-flag-truncates -- `_render` now trusts the persisted `whole` flag without checking it, so a rule that was imported whole and then edited is exported as `steps[0]` alone, and any added steps, checks or purpose are silently dropped.
Trigger: import a COMMON block that has no step headings, which stores `domain_extensions.jtbd.whole: True`. Then an author adds a second step or a `checks` entry to that rule in pack YAML, and `export_jtbdset` emits only the original text. The heuristic this replaced (`len(steps) == 1 and not checks`) handled this case. The flag is a claim about the condition that nothing re-checks when the condition changes. Overlay rules have the same problem at line 240. Gating on `whole and len(condition.steps) == 1 and not condition.checks` fixes both.
IMPORTANCE: medium -- this is silent content loss in the exported JTBDSet. It is reachable whenever an imported policy is edited before export. Callers: I grepped the repo and the only users of `import_jtbdset`/`export_jtbdset` are this module and tests/test_jtbdset.py. Nothing outside the repo uses them yet.

[NOTE] jazzx_sdk/pipelines/jtbdset.py:162 :: merged-section-name-rewritten -- when sections are merged by slug, the first-seen spelling wins. A JTBD whose `section` was "income" is exported as "Income", so its section string does not round-trip.
IMPORTANCE: low -- this needs inconsistent section casing inside one artifact, and the only effect is that the label is normalised.

[NOTE] jazzx_sdk/pipelines/jtbdset.py:164 :: empty-sequence-now-raises -- `int(sequence) if sequence is not None` raises `ValueError` on `"sequence": ""`. The old `or 100` defaulted to 100.
IMPORTANCE: low -- none of the fixtures use a string sequence, and the failure is loud.

VERDICT: BLOCK
